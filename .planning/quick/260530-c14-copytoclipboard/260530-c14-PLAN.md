---
phase: quick-260530-c14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/clipboard.ts
  - src/lib/debugReport.ts
  - src/lib/copyStepLog.ts
  - src/lib/clipboard.test.ts
  - src/lib/debugReport.test.ts
autonomous: true
requirements:
  - CARRY-03-extension
must_haves:
  truths:
    - "buildDebugReport() 输出包含原有 5 节诊断报告（环境/Provider/Agent/选区/聊天记录）"
    - "buildDebugReport() 输出末尾拼接完整 buildStepLog() 操作记录（含 # Aster 操作记录 标题）"
    - "tool 消息的 toolResult.data 字段出现在 buildDebugReport() 输出中"
    - "buildDebugReport() 输出不包含任何 sk-* API Key 原文（双重脱敏：原节不读 Key + buildStepLog redactKey）"
    - "copyStepLog.ts 不再 import 任何来自 debugReport.ts 的符号（循环依赖已解）"
    - "InputBar handleCopyDebug 与 SettingsPanel handleCopyStepLog 的 dynamic import 路径和导出名不变（兼容）"
  artifacts:
    - path: "src/lib/clipboard.ts"
      provides: "copyToClipboard 函数（独立中立模块，无上游依赖）"
      exports: ["copyToClipboard"]
    - path: "src/lib/debugReport.ts"
      provides: "buildDebugReport（5 节 + 操作记录拼接）+ re-export copyToClipboard"
      exports: ["buildDebugReport", "copyToClipboard"]
    - path: "src/lib/copyStepLog.ts"
      provides: "buildStepLog + redactKey + re-export copyToClipboard（来自 clipboard.ts）"
      exports: ["buildStepLog", "redactKey", "copyToClipboard"]
    - path: "src/lib/clipboard.test.ts"
      provides: "copyToClipboard 函数的测试覆盖"
    - path: "src/lib/debugReport.test.ts"
      provides: "拼接后报告的 3 个新断言 + 原 4 个断言仍通过"
  key_links:
    - from: "src/lib/debugReport.ts"
      to: "src/lib/copyStepLog.ts"
      via: "import { buildStepLog } from './copyStepLog'"
      pattern: "buildStepLog"
    - from: "src/lib/debugReport.ts"
      to: "src/lib/clipboard.ts"
      via: "export { copyToClipboard } from './clipboard'"
      pattern: "clipboard"
    - from: "src/lib/copyStepLog.ts"
      to: "src/lib/clipboard.ts"
      via: "export { copyToClipboard } from './clipboard'"
      pattern: "clipboard"
---

<objective>
将 buildDebugReport() 输出扩充为「5 节诊断报告 + 完整操作记录」两段拼接，同时解开 debugReport ↔ copyStepLog 循环依赖。

Purpose: 「复制调试信息」按钮移除「复制操作记录」入口后（快速任务 fast），需要一次性给出系统诊断 + 高质量操作记录（含 toolResult.data + redactKey 脱敏）。这是用户锁定的形态（locked decision）。

Output:
- src/lib/clipboard.ts（新建，承载 copyToClipboard，无上游依赖）
- src/lib/debugReport.ts（修改：拼接 buildStepLog + re-export copyToClipboard from clipboard）
- src/lib/copyStepLog.ts（修改：line 17 改 re-export from clipboard，断开 → debugReport 依赖）
- src/lib/clipboard.test.ts（新建，覆盖 copyToClipboard 主路径 + fallback + 双失败）
- src/lib/debugReport.test.ts（修改：新增 3 个拼接断言，原 4 个断言保持不变）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/wb.chen/Documents/Project/Aster/.planning/STATE.md

<!-- 关键接口：执行前无需再探索 codebase，以下已提取 -->
<interfaces>
<!-- src/lib/debugReport.ts 当前导出 -->
export async function buildDebugReport(): Promise<string>
export async function copyToClipboard(text: string): Promise<boolean>
  // 实现：navigator.clipboard.writeText 主路径 + textarea/execCommand fallback + T-vtc-04 静默失败

<!-- src/lib/copyStepLog.ts 当前导出 -->
export function redactKey(text: string): string
  // 替换 /(sk-[A-Za-z0-9\-_]{4,})/g → '[API KEY REDACTED]'
export async function buildStepLog(): Promise<string>
  // 输出三角色 Markdown dump；空 messages → '（无操作记录）'
  // tool role 含：toolName / 描述 / 结果（成功/失败） / data（JSON.stringify）
export { copyToClipboard } from './debugReport'   // ← 本次改为 from './clipboard'

<!-- 调用方（不改） -->
// InputBar.tsx line 70-71:
const { buildDebugReport, copyToClipboard } = await import('../lib/debugReport');
// SettingsPanel.tsx line 56-57:
const { buildStepLog, copyToClipboard } = await import('../../lib/copyStepLog');
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 clipboard.ts + 解循环依赖 + 拼接 buildStepLog</name>
  <files>
    src/lib/clipboard.ts
    src/lib/debugReport.ts
    src/lib/copyStepLog.ts
  </files>
  <behavior>
    - clipboard.ts 新建后，navigator.clipboard.writeText 成功 → copyToClipboard 返回 true
    - clipboard.ts 新建后，writeText reject → 走 textarea/execCommand fallback
    - debugReport.ts 改后，copyStepLog 不再出现在 import 来源列表中（tsc --noEmit 0 错）
    - debugReport.ts buildDebugReport() 含有 '# Aster 操作记录' 字符串（拼接生效）
    - copyStepLog.ts line 17 改后，grep 确认不再 import debugReport
  </behavior>
  <action>
**步骤 1：新建 src/lib/clipboard.ts**

把 debugReport.ts 现有 copyToClipboard 函数（lines 254-282，含 jsdoc + T-vtc-04 注释）原样迁移过去。文件内容：

```typescript
/**
 * src/lib/clipboard.ts — 剪贴板写入工具（260530-c14）
 *
 * 独立模块，无上游依赖，供 debugReport.ts 与 copyStepLog.ts 共同 re-export。
 * T-vtc-04：失败静默（不崩溃，返回 false）。
 */

/**
 * 将文本写入剪贴板。
 * 先尝试 navigator.clipboard.writeText（现代 API），失败则 fallback 到
 * textarea + execCommand('copy')（旧式兜底）。
 *
 * T-vtc-04：失败静默（按钮不给反馈），不崩溃。
 */
export async function copyToClipboard(text: string): Promise<boolean> {
  try {
    await navigator.clipboard.writeText(text);
    return true;
  } catch {
    // fallback: textarea + execCommand
    try {
      const ta = document.createElement('textarea');
      ta.value = text;
      ta.style.position = 'fixed';
      ta.style.opacity = '0';
      document.body.appendChild(ta);
      ta.focus();
      ta.select();
      const ok = document.execCommand('copy');
      document.body.removeChild(ta);
      return ok;
    } catch {
      return false;
    }
  }
}
```

**步骤 2：修改 src/lib/debugReport.ts**

a) 顶部 import 区末尾追加（在现有 import 语句之后）：
```typescript
import { buildStepLog } from './copyStepLog';
```

b) 删除文件末尾 copyToClipboard 函数定义（lines ~254-282），替换为 re-export：
```typescript
// ---------------------------------------------------------------------------
// copyToClipboard — re-export from clipboard.ts（260530-c14 循环依赖解开）
// ---------------------------------------------------------------------------
export { copyToClipboard } from './clipboard';
```

c) 在 buildDebugReport() 函数内，`return sections.join('\n');` 前追加：
```typescript
  // 操作记录段：复用 buildStepLog 三角色 dump（含 toolResult.data + redactKey 脱敏）
  // 补齐「复制调试信息」按钮的工具卡复制能力（260530-c14，用户选「两段拼接」）
  sections.push('\n---\n');
  sections.push(await buildStepLog());
```

注意：buildDebugReport 已是 async 函数（第 28 行），无需修改函数签名。

**步骤 3：修改 src/lib/copyStepLog.ts line 17**

将：
```typescript
export { copyToClipboard } from './debugReport';
```
改为：
```typescript
export { copyToClipboard } from './clipboard';
```

**不改动**：InputBar.tsx、SettingsPanel.tsx（dynamic import 路径 + 导出名不变，靠 re-export 兼容）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - tsc --noEmit 输出 0 错误
    - grep 确认：grep 'from.*debugReport' src/lib/copyStepLog.ts 返回空（无结果）
    - src/lib/clipboard.ts 存在且导出 copyToClipboard
    - src/lib/debugReport.ts 含 "buildStepLog" 导入 + "操作记录" 拼接注释
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 clipboard.test.ts + 扩展 debugReport.test.ts</name>
  <files>
    src/lib/clipboard.test.ts
    src/lib/debugReport.test.ts
  </files>
  <behavior>
    clipboard.test.ts：
    - [CLIPBOARD MAIN] navigator.clipboard.writeText mock 成功 → copyToClipboard 返回 true
    - [CLIPBOARD FALLBACK] writeText reject → execCommand 路径被调用，返回 execCommand 结果
    - [CLIPBOARD DOUBLE FAIL] writeText + execCommand 双失败 → 返回 false

    debugReport.test.ts 新增 3 个断言：
    - [STEPLOG TITLE] buildDebugReport 输出含 '# Aster 操作记录'（拼接段标题）
    - [STEPLOG TOOL DATA] buildDebugReport 输出含 tool 消息的 data 字段（如 'written'）
    - [STEPLOG REDACT] buildDebugReport 输出不含 sk-* 字符串（拼接段脱敏生效）

    原有 4 个断言（KEY GATE / SECTIONS / EMPTY MESSAGES / SELECTION TEXT）仍通过。
  </behavior>
  <action>
**步骤 1：新建 src/lib/clipboard.test.ts**

```typescript
/**
 * src/lib/clipboard.test.ts — copyToClipboard 剪贴板函数测试（260530-c14）
 *
 * 覆盖：
 * [CLIPBOARD MAIN]     navigator.clipboard.writeText 成功 → true
 * [CLIPBOARD FALLBACK] writeText reject → textarea/execCommand 路径 → execCommand 结果
 * [CLIPBOARD DOUBLE FAIL] 两路径都失败 → false
 */

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { copyToClipboard } from './clipboard';

describe('copyToClipboard', () => {
  beforeEach(() => {
    // 默认 body 存在（jsdom 环境已有）
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('[CLIPBOARD MAIN] writeText 成功 → 返回 true', async () => {
    const writeText = vi.fn().mockResolvedValue(undefined);
    vi.stubGlobal('navigator', { clipboard: { writeText } });

    const result = await copyToClipboard('hello');
    expect(result).toBe(true);
    expect(writeText).toHaveBeenCalledWith('hello');

    vi.unstubAllGlobals();
  });

  it('[CLIPBOARD FALLBACK] writeText reject → execCommand 路径', async () => {
    // writeText 失败
    vi.stubGlobal('navigator', {
      clipboard: { writeText: vi.fn().mockRejectedValue(new Error('denied')) },
    });
    // execCommand 返回 true
    const execCommand = vi.spyOn(document, 'execCommand').mockReturnValue(true);

    const result = await copyToClipboard('fallback test');
    expect(result).toBe(true);
    expect(execCommand).toHaveBeenCalledWith('copy');

    vi.unstubAllGlobals();
  });

  it('[CLIPBOARD DOUBLE FAIL] writeText + execCommand 双失败 → false', async () => {
    vi.stubGlobal('navigator', {
      clipboard: { writeText: vi.fn().mockRejectedValue(new Error('denied')) },
    });
    vi.spyOn(document, 'execCommand').mockImplementation(() => {
      throw new Error('not supported');
    });

    const result = await copyToClipboard('fail test');
    expect(result).toBe(false);

    vi.unstubAllGlobals();
  });
});
```

**步骤 2：修改 src/lib/debugReport.test.ts**

在现有 `vi.mock('../store/chat', ...)` mock 处，已知其返回 `messages: []`。

需要在文件末尾（describe('debugReport') 块内，在最后一个 it 后）追加 3 个新 it 块。
这 3 个 it 需要一个含有 tool 消息（含 sk-* 内容）的 chat mock，通过动态覆盖 useChatStore.getState 实现。

在现有 `describe('debugReport', () => {` 块末尾追加：

```typescript
  it('[STEPLOG TITLE] buildDebugReport 输出含操作记录段标题', async () => {
    const chatMod = await import('../store/chat');
    const orig = chatMod.useChatStore.getState;
    chatMod.useChatStore.getState = vi.fn(() => ({
      messages: [
        {
          id: 'msg-t1',
          role: 'user' as const,
          content: '帮我整理',
          ts: 1000000000000,
        },
      ],
    })) as typeof orig;

    const report = await buildDebugReport();

    chatMod.useChatStore.getState = orig;

    expect(report).toContain('# Aster 操作记录');
  });

  it('[STEPLOG TOOL DATA] buildDebugReport 输出含 tool data 字段', async () => {
    const chatMod = await import('../store/chat');
    const orig = chatMod.useChatStore.getState;
    chatMod.useChatStore.getState = vi.fn(() => ({
      messages: [
        {
          id: 'msg-tool',
          role: 'tool' as const,
          content: '追加段落',
          toolName: 'append_paragraph',
          toolResult: { ok: true as const, data: { written: 5 } },
          ts: 1000000002000,
        },
      ],
    })) as typeof orig;

    const report = await buildDebugReport();

    chatMod.useChatStore.getState = orig;

    // tool data 字段出现在拼接段
    expect(report).toContain('written');
  });

  it('[STEPLOG REDACT] buildDebugReport 输出不含 sk-* 字符串（拼接段脱敏）', async () => {
    const chatMod = await import('../store/chat');
    const orig = chatMod.useChatStore.getState;
    chatMod.useChatStore.getState = vi.fn(() => ({
      messages: [
        {
          id: 'msg-leak',
          role: 'user' as const,
          content: '我的 Key 是 sk-SECRET-abc123',
          ts: 1000000000000,
        },
      ],
    })) as typeof orig;

    const report = await buildDebugReport();

    chatMod.useChatStore.getState = orig;

    // 脱敏守门：拼接段 buildStepLog 已 redactKey，不应出现 sk- 原文
    expect(report).not.toMatch(/sk-[A-Za-z0-9]+/);
  });
```

注意：原有 4 个 it（KEY GATE / SECTIONS / EMPTY MESSAGES / SELECTION TEXT）**不作任何改动**。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/lib/clipboard.test.ts src/lib/debugReport.test.ts src/lib/copyStepLog.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - clipboard.test.ts 3 个测试全部 PASS
    - debugReport.test.ts 7 个测试全部 PASS（原 4 + 新增 3）
    - copyStepLog.test.ts 8 个测试全部 PASS（无改动，re-export 来源变化不影响断言）
    - 循环依赖验证：grep 'from.*debugReport' src/lib/copyStepLog.ts 返回空
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM messages → buildStepLog | 用户 / assistant / tool 消息内容可能含 API Key 片段（用户意外粘贴） |
| buildDebugReport output → clipboard | 最终输出进剪贴板，任何 Key 泄露都会扩散到粘贴目的地 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c14-01 | Information Disclosure | buildDebugReport → clipboard | mitigate | buildStepLog 段全程经 redactKey() 过滤；原 5 节不调 getKey() 不读 localStorage（T-vtc-01 既有约束保持） |
| T-c14-02 | Information Disclosure | clipboard.ts copyToClipboard | accept | copyToClipboard 只接收已脱敏字符串；函数本身不接触 Key 存储 |
| T-c14-03 | Tampering | 循环依赖解开后依赖图变化 | mitigate | tsc --noEmit 验证 0 错；grep 守门确认 copyStepLog 不再指向 debugReport |
</threat_model>

<verification>
全量测试：
```bash
cd /Users/wb.chen/Documents/Project/Aster
npx vitest run src/lib/clipboard.test.ts src/lib/debugReport.test.ts src/lib/copyStepLog.test.ts
```
预期：clipboard 3 PASS + debugReport 7 PASS + copyStepLog 8 PASS = 共 18 通过，0 失败。

循环依赖守门：
```bash
grep 'from.*debugReport' /Users/wb.chen/Documents/Project/Aster/src/lib/copyStepLog.ts
```
预期：无输出（空结果）。

类型检查：
```bash
cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | grep -c 'error TS'
```
预期：0

注意：`src/providers/retry.test.ts` 是预存在 flaky（全量跑偶发 unhandled-rejection，单跑 9/9 PASS）——非本次回归，不修。
</verification>

<success_criteria>
1. buildDebugReport() 输出含原有 5 节标题（环境/Provider 配置/Agent 状态/当前选区/聊天记录）AND 含 `# Aster 操作记录`（拼接段）
2. 含 tool 消息时，buildDebugReport() 输出含 toolResult.data 的 JSON 内容
3. 含 sk-* 内容时，buildDebugReport() 输出 not.toMatch(/sk-[A-Za-z0-9]+/)
4. copyStepLog.ts 不再依赖 debugReport.ts（grep 守门通过）
5. tsc --noEmit 0 错
6. 以上 3 个测试文件共 18 个 test 全绿
</success_criteria>

<output>
完成后创建 `.planning/quick/260530-c14-copytoclipboard/260530-c14-SUMMARY.md`，内容包含：实际修改的文件、提交 hash、测试结果（X passed）、循环依赖解开确认。
</output>

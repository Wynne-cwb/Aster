---
phase: quick-260603-fx8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/ExcelAdapter.ts
  - src/adapters/ExcelAdapter.batch.test.ts
autonomous: true
requirements: [FX8-BUG1, FX8-BUG2]

must_haves:
  truths:
    - "batch_write 包含 apply_formula 子操作时成功写入公式并返回 completed>0"
    - "batch_write 包含 set_cell 子操作时成功写入值并返回 completed>0"
    - "batch_write 子操作的 reverse.args 是 Record 对象（overwrite_range + address/values）"
    - "set_range_values / applyFormula / setCell / overwriteRange 接受 Sheet1!A1 格式地址不报 InvalidArgument"
    - "resolveRange 正确解析裸地址、Sheet!A1、'带空格 表'!A1、含转义单引号的表名"
    - "executeBatch 仍只做 2 次 ctx.sync（D-01 铁律不退步）"
  artifacts:
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "resolveRange helper + executeBatch 多工具分派"
      contains: "resolveRange"
    - path: "src/adapters/ExcelAdapter.batch.test.ts"
      provides: "apply_formula / set_cell / resolveRange 守门单测"
      contains: "apply_formula"
  key_links:
    - from: "executeBatch Phase 1a 参数校验"
      to: "op.tool switch 分支"
      via: "resolveAddressField(op) helper"
      pattern: "resolveAddressField"
    - from: "setRangeValues / applyFormula / setCell / overwriteRange / get_range_values"
      to: "worksheet.getRange(localAddr)"
      via: "resolveRange(ctx, address)"
      pattern: "resolveRange"
---

<objective>
修复 ExcelAdapter 两个已精确定位的 bug：
1. executeBatch 无视 op.tool，将所有子操作当 set_range_values 处理，导致 apply_formula/set_cell 在 index 0 失败
2. 所有 range 解析用 getActiveWorksheet().getRange(address) 不支持 "Sheet!A1" 格式，导致跨表地址 InvalidArgument 反复失败触发熔断

Purpose: agent 能正确批量写公式、按 sheet-qualified 地址操作 Excel，消除真机反复失败熔断。
Output: 修复后的 ExcelAdapter.ts + 守门单测（apply_formula/set_cell in batch + resolveRange 4 场景）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/wb.chen/Documents/Project/Aster/.planning/STATE.md
@/Users/wb.chen/Documents/Project/Aster/src/adapters/ExcelAdapter.ts
@/Users/wb.chen/Documents/Project/Aster/src/adapters/ExcelAdapter.batch.test.ts
@/Users/wb.chen/Documents/Project/Aster/src/agent/tools/write/excel.ts
@/Users/wb.chen/Documents/Project/Aster/src/agent/tools/write/batch.ts
</context>

<interfaces>
<!-- 执行器需要的关键接口，已从代码库提取，无需再探索 -->

<!-- ExcelAdapter.executeBatch 当前签名（ExcelAdapter.ts:1553） -->
```typescript
async executeBatch(ops: Array<{
  tool: string;
  args: Record<string, unknown>;
  humanLabel?: string;
}>): Promise<{
  subOps: Array<{
    humanLabel: string;
    beforeImage?: unknown[][];
    reverse: { tool: string; args: Record<string, unknown> };
    postState?: { kind: string; content: unknown };
    ok: boolean;
  }>;
  failAtIndex?: number;
}>
```

<!-- 三个需要进 batch 的写工具参数形状（excel.ts） -->
```typescript
// set_range_values：args.address (string) + args.values (unknown[][])
// apply_formula：args.cell (string) + args.formula (string)  → adapter.applyFormula(cell, formula) → range.formulas = [[formula]]
// set_cell：args.cell (string) + args.value (string|number) → adapter.setCell(cell, value) → range.values = [[value]]
```

<!-- 三个工具的写入方式对比 -->
```
set_range_values → proxy.values = op.args.values（二维数组）
apply_formula    → proxy.formulas = [[op.args.formula]]（单格公式）
set_cell         → proxy.values = [[op.args.value]]（单格值）
```

<!-- 现有 getRangeOrNullObject cast（batch 内部，ExcelAdapter.ts:1580-1583） -->
```typescript
const worksheet = ctx.workbook.worksheets.getActiveWorksheet() as unknown as {
  getRangeOrNullObject: (address: string) => Excel.Range;
};
const proxy = worksheet.getRangeOrNullObject(op.args.address as string);
```

<!-- 需要 getItem 的 sheet-qualified 解析（Office.js 规则） -->
```typescript
// "Sheet1!A1:B2"   → worksheets.getItem("Sheet1").getRange("A1:B2")
// "'带 空格'!A1"   → worksheets.getItem("带 空格").getRange("A1")  // 单引号去掉
// "'O''Brien'!A1"  → worksheets.getItem("O'Brien").getRange("A1")  // '' → '
// "A1:B2"（无!）  → worksheets.getActiveWorksheet().getRange("A1:B2")
// getRangeOrNullObject 版本：同上但调 getRangeOrNullObject
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 抽取 resolveRange helper 并替换全部 getRange 调用点</name>
  <files>src/adapters/ExcelAdapter.ts</files>
  <behavior>
    - resolveRange(ctx, address) 裸地址 "A1:B2" → getActiveWorksheet().getRange("A1:B2")
    - resolveRange(ctx, "Sheet1!A1") → worksheets.getItem("Sheet1").getRange("A1")
    - resolveRange(ctx, "'带 空格'!A1") → worksheets.getItem("带 空格").getRange("A1")（去外层单引号）
    - resolveRange(ctx, "'O''Brien'!C3") → worksheets.getItem("O'Brien").getRange("C3")（两个单引号 → 一个）
    - resolveRangeOrNull(ctx, address) 同理但调 getRangeOrNullObject（executeBatch 专用）
  </behavior>
  <action>
在 ExcelAdapter.ts 文件顶部（columnIndexToLetter 函数之后，class ExcelAdapter 之前）新增两个模块级私有函数：

```typescript
/**
 * 解析 Excel range 地址，支持 sheet-qualified 格式（BUG 2 修复）。
 *
 * Office.js worksheet.getRange() 只接受局部地址（如 "A1:B2"），
 * 不接受 "Sheet1!A1:B2" 前缀格式 → InvalidArgument。
 *
 * 支持三种格式：
 *   - 裸地址："A1:B2"         → getActiveWorksheet().getRange("A1:B2")
 *   - 引号表名："'Sheet 1'!A1" → getItem("Sheet 1").getRange("A1")
 *   - 普通表名："Sheet1!A1"    → getItem("Sheet1").getRange("A1")
 *
 * 单引号转义规则（Office 标准）：表名内的单引号写为两个单引号 ''。
 */
function resolveRange(
  ctx: { workbook: { worksheets: { getActiveWorksheet: () => Excel.Worksheet; getItem: (name: string) => Excel.Worksheet } } },
  address: string,
): Excel.Range {
  const bangIdx = address.indexOf('!');
  if (bangIdx === -1) {
    return ctx.workbook.worksheets.getActiveWorksheet().getRange(address);
  }
  const sheetRaw = address.slice(0, bangIdx);
  const localAddr = address.slice(bangIdx + 1);
  // 去外层单引号并还原转义的 ''→'
  const sheetName = sheetRaw.startsWith("'") && sheetRaw.endsWith("'")
    ? sheetRaw.slice(1, -1).replace(/''/g, "'")
    : sheetRaw;
  return ctx.workbook.worksheets.getItem(sheetName).getRange(localAddr);
}

/**
 * resolveRange 的 null-object 变体（executeBatch Phase 1 专用）。
 * cast 绕过 @types/office-js 对 getRangeOrNullObject 的不完整类型声明。
 */
function resolveRangeOrNull(
  ctx: { workbook: { worksheets: { getActiveWorksheet: () => unknown; getItem: (name: string) => unknown } } },
  address: string,
): Excel.Range {
  type WsWithNull = { getRangeOrNullObject: (addr: string) => Excel.Range };
  const bangIdx = address.indexOf('!');
  if (bangIdx === -1) {
    const ws = ctx.workbook.worksheets.getActiveWorksheet() as unknown as WsWithNull;
    return ws.getRangeOrNullObject(address);
  }
  const sheetRaw = address.slice(0, bangIdx);
  const localAddr = address.slice(bangIdx + 1);
  const sheetName = sheetRaw.startsWith("'") && sheetRaw.endsWith("'")
    ? sheetRaw.slice(1, -1).replace(/''/g, "'")
    : sheetRaw;
  const ws = ctx.workbook.worksheets.getItem(sheetName) as unknown as WsWithNull;
  return ws.getRangeOrNullObject(localAddr);
}
```

然后在 ExcelAdapter class 内部，将以下所有 `getActiveWorksheet().getRange(...)` 调用替换为 `resolveRange(ctx, ...)` 调用：

1. setRangeValues（~400行）：`ctx.workbook.worksheets.getActiveWorksheet().getRange(address)` → `resolveRange(ctx, address)`
2. overwriteRange（~444行）：同上
3. applyFormula（~554行）：`...getActiveWorksheet().getRange(cell)` → `resolveRange(ctx, cell)`
4. setCell（~596行）：同上
5. readSwitchCase 'get_range_values'（~218行）：同上
6. executeBatch Phase 1a 内的 getRangeOrNullObject（~1580-1583行）：整块替换为 `resolveRangeOrNull(ctx, op.args.address as string)`（此处 op.args.address 是 Task 2 新增的 resolveAddressField 返回值，见 Task 2 说明）

注意：不改 executeBatchReverse 内部（它已经调用 overwriteRange，overwriteRange 自身已改）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>tsc 0 错误；resolveRange / resolveRangeOrNull 函数存在于 ExcelAdapter.ts 顶部；原有 getActiveWorksheet().getRange 调用全部改为 resolveRange 调用</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: executeBatch 按 op.tool 分派 + 补守门单测</name>
  <files>src/adapters/ExcelAdapter.ts, src/adapters/ExcelAdapter.batch.test.ts</files>
  <behavior>
    - executeBatch 传入 apply_formula op（{cell, formula}）→ completed:1，proxy.formulas 被设置，reverse.args 是 Record
    - executeBatch 传入 set_cell op（{cell, value}）→ completed:1，proxy.values 被设置，reverse.args 是 Record
    - executeBatch 传入未知 tool op → failAtIndex=该 op 下标，前面 op 仍成功（fail-fast 语义）
    - executeBatch 仍只做 2 次 ctx.sync（D-01 不退步）
    - resolveRange 解析单测：4 场景均通过（裸/Sheet!A1/带空格/单引号转义）
  </behavior>
  <action>
**Part A — executeBatch 分派改造（ExcelAdapter.ts executeBatch 方法内）**

Step 1：在 Phase 1a 循环内，将当前的硬编码 `op.args.address` 校验替换为按工具类型的参数解析：

```typescript
// Phase 1a 内：按 op.tool 分派参数解析（BUG 1 修复）
// 三类高频值/公式写工具，其余明确返回不支持错误
let rangeAddress: string;
let writeKind: 'values' | 'formula' | 'cell_value';

switch (op.tool) {
  case 'set_range_values':
    if (!op.args.address || typeof op.args.address !== 'string') {
      failAtIndex = i;
    } else {
      rangeAddress = op.args.address as string;
      writeKind = 'values';
    }
    break;
  case 'apply_formula':
    if (!op.args.cell || typeof op.args.cell !== 'string') {
      failAtIndex = i;
    } else {
      rangeAddress = op.args.cell as string;
      writeKind = 'formula';
    }
    break;
  case 'set_cell':
    if (!op.args.cell || typeof op.args.cell !== 'string') {
      failAtIndex = i;
    } else {
      rangeAddress = op.args.cell as string;
      writeKind = 'cell_value';
    }
    break;
  default:
    // 不支持的工具：明确 failAtIndex，不静默吞（清晰错误优于静默失败）
    failAtIndex = i;
}
if (failAtIndex !== -1) break;

// 使用 resolveRangeOrNull 支持 sheet-qualified 地址（BUG 2 协同修复）
const proxy = resolveRangeOrNull(ctx, rangeAddress!);
proxy.load(['values', 'address', 'isNullObject']);
staged.push({ op, proxy, writeKind: writeKind! });
```

注意：staged 数组的元素类型需要扩展 `writeKind` 字段，在 staged 变量声明处同步调整：
```typescript
const staged: Array<{
  op: (typeof ops)[number];
  proxy: Excel.Range;
  beforeImage?: unknown[][];
  writeKind: 'values' | 'formula' | 'cell_value';
}> = [];
```

Step 2：Phase 2a 写入循环，按 writeKind 分派实际写操作：

```typescript
// Phase 2a：按 writeKind 分派写操作（BUG 1 修复核心）
for (const { op, proxy, writeKind } of toCommit) {
  switch (writeKind) {
    case 'values':
      // set_range_values：直接写二维数组
      if (op.args.values !== undefined) {
        proxy.values = op.args.values as unknown[][];
      }
      break;
    case 'formula':
      // apply_formula：写公式（单格，[[formula]]）
      proxy.formulas = [[op.args.formula as string]];
      break;
    case 'cell_value':
      // set_cell：写单格值（[[value]]）
      proxy.values = [[op.args.value]];
      break;
  }
}
```

Step 3：humanLabel 组装（subOps map）也需要处理 apply_formula/set_cell 没有 address 字段的情况：
```typescript
humanLabel: s.op.humanLabel ?? `写入 ${s.proxy.address as string}`,
```
（proxy.address 是 server 端规范化地址，无论原始是 cell 还是 address，sync 后 proxy.address 均有值，这里保持不变即可）

**Part B — 守门单测（ExcelAdapter.batch.test.ts）**

在现有两个 describe 块之后，追加以下测试（不删除现有测试）：

1. `describe('ExcelAdapter.executeBatch — apply_formula 分派（BUG 1）')` 包含：
   - it: `apply_formula op → completed 1，formulasMock 被调用 1 次`
     - mock 中 makeRange 需支持 `set formulas(v) { formulasMock(v); }`
     - 传入 `[{ tool: 'apply_formula', args: { cell: 'C2', formula: '=SUM(A1:A10)' } }]`
     - 断言：result.subOps.length === 1，result.failAtIndex === undefined
     - 断言：result.subOps[0].reverse.args 是 Record（有 address 和 values 键）
     - 断言：formulasMock 被调用，参数为 `[['=SUM(A1:A10)']]`

2. `describe('ExcelAdapter.executeBatch — set_cell 分派（BUG 1）')` 包含：
   - it: `set_cell op → completed 1，values 写为 [[value]]`
     - 传入 `[{ tool: 'set_cell', args: { cell: 'A1', value: 42 } }]`
     - 断言：result.subOps.length === 1
     - 断言：setValues 被调用，参数为 `[[42]]`

3. `describe('ExcelAdapter.executeBatch — 不支持工具 fail-fast（BUG 1）')` 包含：
   - it: `set_range_values ok + 不支持工具 → failAtIndex=1，subOps 只含 index 0`
     - 传入 `[set_range_values ok, { tool: 'unknown_tool', args: {} }]`
     - 断言：result.failAtIndex === 1，result.subOps.length === 1

4. `describe('resolveRange — sheet-qualified 地址解析（BUG 2）')` 包含：
   - it: `裸地址 A1 → getActiveWorksheet().getRange("A1")` 被调用
   - it: `Sheet1!A1 → worksheets.getItem("Sheet1").getRange("A1")` 被调用
   - it: `'带 空格'!A1 → worksheets.getItem("带 空格").getRange("A1")` 被调用（外层单引号剥除）
   - it: `'O\'\'Brien'!C3 → worksheets.getItem("O'Brien").getRange("C3")` 被调用（''→'转义）

   由于 resolveRange 是模块级私有函数，通过 ExcelAdapter 的方法（如 setRangeValues）来间接测试，mock ctx 中的 worksheets：
   ```typescript
   // mock worksheets 同时提供 getActiveWorksheet 和 getItem，断言哪个被调用
   const getActiveWorksheet = vi.fn().mockReturnValue({ getRange: vi.fn().mockReturnValue(makeRangeMock()) });
   const getItem = vi.fn().mockReturnValue({ getRange: vi.fn().mockReturnValue(makeRangeMock()) });
   ```
   通过 setRangeValues 调用进入 resolveRange，断言 getActiveWorksheet vs getItem 哪个被调用、参数是什么。

   （注：makeRangeMock 需要有 values getter/setter、load、address 字段，参考 batch 测试中 makeRange 模式）
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/adapters/ExcelAdapter.batch.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
所有 ExcelAdapter.batch.test.ts 测试通过（含原有 2 个 + 新增 7+ 个守门测试）；
npx vitest run src/adapters/ExcelAdapter.batch.test.ts 无 FAIL；
tsc --noEmit 0 错误；
executeBatch apply_formula/set_cell 分派路径有单测守门；
resolveRange 4 个地址格式均有单测守门
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM→executeBatch | LLM 传入 op.tool / op.args，不可信；需 switch 严格枚举 |
| LLM→address 字段 | sheet 名来自 LLM，不可信；getItem 接收 LLM 构造的 sheet 名 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fx8-01 | Tampering | executeBatch op.tool | mitigate | switch 枚举仅 3 个已知工具；default → failAtIndex 明确拒绝未知工具 |
| T-fx8-02 | Tampering | resolveRange sheetName | accept | Office.js getItem 在 sandbox 内执行，sheetName 超长/特殊字符由 Office 运行时拒绝，非 Aster 自有面 |
| T-fx8-03 | Denial of Service | batch 20 op × formulas write | accept | D-06 已有 20 op 上限；两阶段 sync O(1)，不引入 N×sync 风险 |
</threat_model>

<verification>
整体验证（在项目根目录执行）：

```bash
# 1. 类型检查
npx tsc --noEmit

# 2. batch 守门测试（含原有 + 新增）
npx vitest run src/adapters/ExcelAdapter.batch.test.ts

# 3. 全套回归（确认无退步）
npm test 2>&1 | tail -10
```

预期：tsc 0 错误；batch.test.ts 全绿（含新增 7+ 用例）；npm test 总体无新增失败（末尾 3 个 retry errors 是噪音，忽略）。
</verification>

<success_criteria>
- BUG 1：apply_formula 和 set_cell 可进入 executeBatch 并成功写入；failAtIndex 只在真正不存在的 range 或不支持的工具时触发
- BUG 2：传入 "Sheet1!A1" / "'带空格 表'!A1" 等格式不报 InvalidArgument；resolveRange 单测 4 场景全绿
- D-01 不退步：executeBatch 仍只做 2 次 ctx.sync（batch.test.ts 现有 sync 计数断言通过）
- A-06 不违反：resolveRange / resolveRangeOrNull 在 ExcelAdapter.ts 内，batch.ts 无任何改动
- project_adapter_inverse_signature：apply_formula / set_cell 子操作的 reverse.args 是 Record（含 address + values）
- tsc --noEmit 0 错误
- npm test 无新增 FAIL（末尾 3 retry errors 噪音除外）
</success_criteria>

<output>
完成后在 .planning/quick/260603-fx8-excel-adapter-bug-batch-write-sheet-qual/ 创建 260603-fx8-SUMMARY.md，记录：
- 改动文件与关键改动点（resolveRange helper 位置、executeBatch switch 范围行号）
- 两个 bug 的根因确认与修复摘要
- 测试结果快照（通过用例数）
- 是否需要真机验证（应注明：需在 Office for Web Excel 真机跑 apply_formula + sheet-qualified 地址场景）
</output>

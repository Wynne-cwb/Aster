---
quick_id: 260601-ki6
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/aster-logo.png
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "README 顶部有居中视觉块（logo + 标题 + tagline + 两个 badge）"
    - "logo 图片可从 docs/aster-logo.png 加载，GitHub 渲染正常"
    - "两个 shields.io badge 正确指向品牌色 #009887"
    - "sideload badge 外层有超链接指向 https://wynne-cwb.github.io/Aster/"
    - "原有全部正文段落（4 场景 / BYO Key / Sideload / 技术架构 / 产品口径 / 隐私 / 开发）一字不删"
  artifacts:
    - path: "docs/aster-logo.png"
      provides: "logo 资产，从 .planning/design/aster-redesign/src/assets/aster-logo.png 复制"
    - path: "README.md"
      provides: "含居中头部的更新版 README"
      contains: "align=\"center\""
  key_links:
    - from: "README.md"
      to: "docs/aster-logo.png"
      via: "img src 相对路径"
      pattern: "docs/aster-logo.png"
    - from: "sideload badge"
      to: "https://wynne-cwb.github.io/Aster/"
      via: "markdown 链接包裹 img"
      pattern: "wynne-cwb.github.io/Aster"
---

<objective>
优化 README.md：新建 docs/ 目录存放 logo 资产，在 README 顶部加居中视觉块（logo + 标题 + tagline + 两个 teal badges），保留全部现有正文，仅去重头部重复行。

Purpose: 让 GitHub 仓库首页第一眼看到品牌形象，提升开源项目可信度与吸引力。
Output: docs/aster-logo.png（新建）+ 更新后的 README.md（头部视觉块 + 全量正文保留）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 docs/ 目录并复制 logo 资产</name>
  <files>docs/aster-logo.png</files>
  <action>
在项目根目录新建 docs/ 文件夹，把
  `.planning/design/aster-redesign/src/assets/aster-logo.png`
复制到
  `docs/aster-logo.png`

执行命令：
  mkdir -p docs
  cp .planning/design/aster-redesign/src/assets/aster-logo.png docs/aster-logo.png

不修改任何其他文件，不改 Vite 构建配置（docs/ 不在 src/ 下，不影响 bundle）。
  </action>
  <verify>
    <automated>test -f docs/aster-logo.png && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>docs/aster-logo.png 存在，文件大小与源文件一致（约 90 KB）。</done>
</task>

<task type="auto">
  <name>Task 2: 重写 README 头部，新增居中视觉块并去重衔接</name>
  <files>README.md</files>
  <action>
用 Write 工具整体替换 README.md 内容，结构如下：

【头部视觉块（新增，放在文件最顶）】
使用 GitHub 兼容的 `<div align="center">` 实现居中，包含：
1. logo：`<img src="docs/aster-logo.png" width="120" alt="Aster logo" />`
2. 标题：`<h1>Aster</h1>`（独立的 h1，不带副标题 —— 副标题以 tagline 替代）
3. tagline（一行）：`<p>在原生 Office 里的 AI 代理 &nbsp;·&nbsp; 你说一句话，Aster 自主完成多步文档任务</p>`
4. 两个 shields.io badge（用 HTML img 而非纯 Markdown，保持在 div 内居中）：
   - 宿主 badge：
     `<img src="https://img.shields.io/badge/Office-PowerPoint%20%C2%B7%20Excel%20%C2%B7%20Word-009887?style=flat-square" alt="Office PowerPoint · Excel · Word" />`
   - sideload badge（外层包超链接）：
     `<a href="https://wynne-cwb.github.io/Aster/"><img src="https://img.shields.io/badge/Live-sideload-009887?style=flat-square" alt="Live sideload" /></a>`
     （说明：中文"在线 sideload"在 shields.io URL 编码后渲染偶有字体问题，改为 "Live-sideload" 英文版更稳定；中文含义相同）
5. `<p><strong>BYO Key · 无后台 · 纯浏览器直连 · 开源</strong></p>`

【去重衔接】
原第 1 行 `# Aster — Office 智能代理` → 删除（标题已在头部 h1）
原第 3 行 tagline → 删除（已并入头部）
原第 5 行 `**BYO Key · 无后台 · 纯浏览器直连 · 开源**` → 删除（已并入头部）
原第 7 行 `---` 分割线 → 保留或删除均可（头部 div 本身视觉已足够分割）

【保留全部正文，顺序不变】
从「## Aster 怎么工作」起，到「开发」章节的代码块结尾，一字不改保留。

完整文件结构示例（执行者按此写出完整文件）：

```markdown
<div align="center">
  <img src="docs/aster-logo.png" width="120" alt="Aster logo" />
  <h1>Aster</h1>
  <p>在原生 Office 里的 AI 代理 &nbsp;·&nbsp; 你说一句话，Aster 自主完成多步文档任务</p>
  <p>
    <img src="https://img.shields.io/badge/Office-PowerPoint%20%C2%B7%20Excel%20%C2%B7%20Word-009887?style=flat-square" alt="Office PowerPoint · Excel · Word" />
    &nbsp;
    <a href="https://wynne-cwb.github.io/Aster/"><img src="https://img.shields.io/badge/Live-sideload-009887?style=flat-square" alt="Live sideload" /></a>
  </p>
  <p><strong>BYO Key · 无后台 · 纯浏览器直连 · 开源</strong></p>
</div>

---

## Aster 怎么工作
...（以下原文全部保留）
```

注意：
- 原 README.md 的「# Aster — Office 智能代理」第一行、紧接的 tagline 行、以及「**BYO Key…**」行，三行已在头部包含，写入时跳过这三行避免内容重复。
- `---` 分隔线在头部 div 之后、`## Aster 怎么工作` 之前保留一条，视觉清晰。
- 其余「---」分隔线、全部 `##` 章节、代码块，均原样保留。
  </action>
  <verify>
    <automated>grep -q 'align="center"' README.md && grep -q 'shields.io' README.md && grep -q 'docs/aster-logo.png' README.md && grep -q 'wynne-cwb.github.io/Aster' README.md && grep -q 'Aster 怎么工作' README.md && grep -q '4 个核心场景' README.md && grep -q 'BYO Key' README.md && grep -q 'Sideload 安装' README.md && grep -q '技术架构' README.md && grep -q '产品口径' README.md && grep -q '隐私' README.md && grep -q 'npm ci' README.md && echo "ALL OK" || echo "FAIL"</automated>
  </verify>
  <done>
- README.md 顶部有 `div align="center"` 居中块，含 logo + h1 标题 + tagline + 两个 teal badge + BYO Key 标语
- docs/aster-logo.png 相对路径引用正确
- 两个 badge 颜色均为 009887
- sideload badge 外层超链接指向 https://wynne-cwb.github.io/Aster/
- 原 4 场景 / BYO Key / Sideload / 技术架构 / 产品口径 / 隐私 / 开发 章节内容完整保留
- 头部重复的三行已去重（旧 h1 标题 / 旧 tagline / 旧 BYO Key 标语）
  </done>
</task>

</tasks>

<verification>
```bash
# 1. logo 资产存在
test -f docs/aster-logo.png

# 2. README 头部包含居中块
grep -q 'align="center"' README.md

# 3. badge 引用 shields.io
grep -c 'shields.io' README.md   # 应 >= 2（两个 badge URL）

# 4. sideload badge 链接指向线上地址
grep -q 'wynne-cwb.github.io/Aster' README.md

# 5. logo 路径正确
grep -q 'docs/aster-logo.png' README.md

# 6. 原有关键章节全部保留
grep -q '4 个核心场景' README.md
grep -q 'Sideload 安装' README.md
grep -q '技术架构' README.md
grep -q 'npm ci' README.md
```
</verification>

<success_criteria>
- docs/aster-logo.png 存在（440×440 PNG，约 90 KB）
- README.md 第一个可见元素是居中视觉块（div align="center"）
- 头部视觉块含：logo 120px / h1 Aster / tagline / 两个 teal 009887 badge / BYO Key 标语
- sideload badge 超链接可点击跳转到 https://wynne-cwb.github.io/Aster/
- 原有 7 个主章节内容完整，无内容删减
- 头部旧的三行重复内容已去除（无「# Aster — Office 智能代理」单独 h1 行、无旧 tagline 行、无旧 BYO Key 标语行的重复）
</success_criteria>

<output>
完成后在 .planning/quick/260601-ki6-readme-logo-badge/ 创建 260601-ki6-SUMMARY.md，记录：
- 实际修改文件列表
- 头部视觉块最终结构
- 去重了哪三行
- commit hash
</output>

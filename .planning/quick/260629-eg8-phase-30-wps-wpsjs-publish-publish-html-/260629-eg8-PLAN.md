---
phase: quick-260629-eg8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/wps-probe/publish.html
  - public/wps-probe/README.md
autonomous: true
requirements: [WPS-02, WPS-03]
must_haves:
  truths:
    - "用户在浏览器打开 https://wynne-cwb.github.io/Aster/wps-probe/publish.html 能看到 AsterProbe 加载项的安装按钮（et + wpp 两项）"
    - "publish.html 与官方 wpsjs 2.2.3 模板字节级一致，仅做两处占位符替换（curList + serverid）"
    - "README 安装章节用真实可用的 publish 模式路径替换掉旧『路径 B：联系 Claude 获取方案』占位"
    - "现有 jsplugins.xml 在线 sideload 配置保留不动（publish.html 是新增并行路径，非替换）"
  artifacts:
    - path: "public/wps-probe/publish.html"
      provides: "WPS publish 模式安装器（个人版 12.1.0.16910+ 唯一可用安装路径）"
      contains: "AsterProbe"
    - path: "public/wps-probe/README.md"
      provides: "更新后的三路径安装说明（A: oem.ini 在线 / B: publish 模式 / 保留安全说明+使用）"
      contains: "publish.html"
  key_links:
    - from: "public/wps-probe/publish.html"
      to: "https://wynne-cwb.github.io/Aster/wps-probe/ribbon.xml + index.html"
      via: "curList url 字段（模板 CheckPlugin 校验 url+ribbon.xml 可达，同 github.io 源无 CORS）"
      pattern: "wynne-cwb\\.github\\.io/Aster/wps-probe/"
---

<objective>
把 Phase 30 WPS 探针从「仅 jsplugins.xml 在线 sideload」扩展为「额外提供 wpsjs publish 模式安装器」：生成一个可点击安装的 `public/wps-probe/publish.html`，并更新 README 安装章节。

Purpose: WPS 个人版自 12.1.0.16910 起对 oem.ini/jsplugins 路径做了安全限制禁用，个人版只能用 publish 模式安装加载项。这正是新增 publish.html 的主因——让无法走 oem.ini 路径的用户也能装上探针，解除 Phase 30 真机 go/no-go 的安装阻碍。

Output:
- 新文件 `public/wps-probe/publish.html`（官方 wpsjs 2.2.3 模板 + 两处占位符替换，字节级保真）
- 编辑 `public/wps-probe/README.md`（用真实 publish 模式路径替换旧占位「路径 B：联系 Claude」）

边界（硬约束）：
- 只动 `public/wps-probe/publish.html`（新增）和 `public/wps-probe/README.md`（编辑）。
- **绝不**触碰 `src/` 或 Aster 主应用，**绝不**删除/修改现有 `jsplugins.xml`（publish.html 是并行新增路径）。
- 这是发布类改动：执行者只写文件 + 原子 commit，**不 push**。build + push + Pages 部署由 orchestrator/用户作为显式最终步骤执行（见末尾说明）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@public/wps-probe/README.md
@public/wps-probe/jsplugins.xml

<prevalidated_facts>
预研已确认（不要重新调研，直接使用）：

1. publish.html 由官方 wpsjs 2.2.3 模板 + 两处字符串替换生成。模板在磁盘：
   `/tmp/wpsjs-gen/node_modules/wpsjs/src/lib/res/publish.html`（631 行，自包含，内嵌 base64 favicon + 全部安装 JS；与 WPS 本地服务 http://127.0.0.1:58890 通信）。

2. 生成器（wpsjs publish.js）逻辑就是两步：
   - `pubHtml.replace(/PUBLISH_REPLACE_STRING/, outData)`，outData = JSON.stringify(curList 数组)
   - `pubHtml.replace(/SERVERID_REPLEASE_STRING/, "undefined")`（单用户 multiUser=false）

3. 要注入的 curList（镜像现有 jsplugins.xml：同名 AsterProbe、et + wpp、同 URL、online 模式）。
   注意：online / multiUser 是字符串值（"true"/"false"），URL 必须以 "/" 结尾（安装器会追加 "ribbon.xml" / "index.html"）：
   ```json
   [{"name":"AsterProbe","addonType":"et","online":"true","multiUser":"false","url":"https://wynne-cwb.github.io/Aster/wps-probe/"},{"name":"AsterProbe","addonType":"wpp","online":"true","multiUser":"false","url":"https://wynne-cwb.github.io/Aster/wps-probe/"}]
   ```

4. README 当前安装章节：「路径 A：oem.ini 在线模式（推荐）」+「路径 B：联系 Claude 获取方案（备用）」。现在 publish.html 已存在，用真实 publish 模式步骤替换路径 B。安全说明 / 使用 / 完整真机验证清单等其余章节保持原样。
</prevalidated_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用官方模板 + 两处占位符替换生成 publish.html</name>
  <files>public/wps-probe/publish.html</files>
  <action>
用 node 脚本读取官方 wpsjs 2.2.3 模板、做两处 `.replace()`、写出 `public/wps-probe/publish.html`。**绝不手敲 631 行**——必须字节级复制官方模板再替换占位符，保证安装 JS 真实可用。

主路径（模板在磁盘）：
```bash
node -e 'const fs=require("fs");let h=fs.readFileSync("/tmp/wpsjs-gen/node_modules/wpsjs/src/lib/res/publish.html","utf8");const cur=JSON.stringify([{name:"AsterProbe",addonType:"et",online:"true",multiUser:"false",url:"https://wynne-cwb.github.io/Aster/wps-probe/"},{name:"AsterProbe",addonType:"wpp",online:"true",multiUser:"false",url:"https://wynne-cwb.github.io/Aster/wps-probe/"}]);h=h.replace(/PUBLISH_REPLACE_STRING/,cur).replace(/SERVERID_REPLEASE_STRING/,"undefined");fs.writeFileSync("public/wps-probe/publish.html",h);console.log("written",h.length,"bytes");'
```
（已确认模板存在于 `/tmp/wpsjs-gen/node_modules/wpsjs/src/lib/res/publish.html`，45574 字节。）

FALLBACK（仅当 /tmp/wpsjs-gen 不在时）：先 `mkdir -p /tmp/wpsjs-gen && cd /tmp/wpsjs-gen && npm i wpsjs@2.2.3` 重新拉到模板路径，再跑上面同样的两处替换脚本。

约束：
- 写入路径必须用项目根的相对/绝对路径定位到 `public/wps-probe/publish.html`（执行时确保 cwd 在仓库根，或用绝对路径 `/Users/wb.chen/Documents/Project/Aster/public/wps-probe/publish.html`）。
- 不要修改 curList 里的字段名/字符串类型/URL 结尾斜杠（这些都是预研锁定值）。
- 不要触碰同目录的 jsplugins.xml / index.html / probe.js / ribbon.xml。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const h=fs.readFileSync("public/wps-probe/publish.html","utf8");const probes=(h.match(/"AsterProbe"|AsterProbe/g)||[]).length;const hasPlaceholder=/PUBLISH_REPLACE_STRING|SERVERID_REPLEASE_STRING/.test(h);const hasEt=h.includes("\"addonType\":\"et\"")||h.includes("addonType\":\"et\"");if(probes<2){console.error("FAIL: AsterProbe 出现次数 <2 =",probes);process.exit(1);}if(hasPlaceholder){console.error("FAIL: 仍残留占位符");process.exit(1);}if(!h.includes("wynne-cwb.github.io/Aster/wps-probe/")){console.error("FAIL: 缺 github.io URL");process.exit(1);}console.log("PASS: AsterProbe x"+probes+", 无残留占位符, URL 就位, "+h.length+" bytes");'</automated>
  </verify>
  <done>public/wps-probe/publish.html 存在；含 ≥2 处 AsterProbe（et + wpp curList 条目）；含 https://wynne-cwb.github.io/Aster/wps-probe/ ；无任何 PUBLISH_REPLACE_STRING 或 SERVERID_REPLEASE_STRING 残留；文件大小与模板量级一致（~45KB）。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 README 安装章节——用真实 publish 模式路径替换路径 B 占位</name>
  <files>public/wps-probe/README.md</files>
  <action>
编辑 `public/wps-probe/README.md`，把旧的「### 路径 B：联系 Claude 获取方案（备用）」整段（约第 38-45 行，含其下「截图…/发给 Claude…/Claude 将产出…」三步与引用块）替换为真实可用的 **publish 模式安装路径**。其余章节（路径 A、安全说明、使用、完整真机验证清单、页脚）保持不动。

新「路径 B」内容要点（保持 README 现有中文语气与 markdown 结构，用有序步骤 + 必要的 `>` 注意块）：

标题改为：`### 路径 B：publish 模式在线安装（个人版必用）`

正文步骤：
1. 浏览器打开线上安装页 `https://wynne-cwb.github.io/Aster/wps-probe/publish.html`
2. 点击 AsterProbe 加载项条目的「**安装**」按钮（安装成功后按钮变「卸载」、状态显示正常）；**首次会弹「允许浏览器打开 WPS」对话框，需点允许**
3. **完全退出并重启 WPS**，打开金山表格（ET）/ 金山演示（WPP），功能区出现「**Aster 探针**」标签
4. 注意（`>` 块）：publish 模式要求 oem.ini 中 `JsApiPlugin=false`（或注释掉），否则可能与 jsplugins 模式冲突导致不显示——若同时配过路径 A 需二选一。

并补一段背景说明（`>` 块，放在路径 B 下或路径 A/B 之间合适处）：
> **为何需要 publish 模式：** WPS 个人版自 12.1.0.16910 起，oem.ini / jsplugins 路径已被安全限制禁用，个人版**只能**用 publish 模式安装加载项。专业版可继续用路径 A（oem.ini）；个人版走路径 B（publish.html）。

约束：
- 不要改路径 A、安全说明、使用、完整真机验证清单、页脚等其余章节的实质内容。
- 不要在 README 里编造未在预研给定的步骤（如 127.0.0.1 端口细节用户无需手动操作，不要写进用户步骤）。
- 用 Edit 工具做精准替换，不要整文件重写覆盖未变章节。
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const r=fs.readFileSync("public/wps-probe/README.md","utf8");if(/联系\s*Claude\s*获取方案/.test(r)){console.error("FAIL: 旧路径 B 占位仍在");process.exit(1);}if(!r.includes("publish.html")){console.error("FAIL: README 未提及 publish.html");process.exit(1);}if(!/publish\s*模式/.test(r)){console.error("FAIL: 未出现 publish 模式说明");process.exit(1);}if(!/12\.1\.0\.16910/.test(r)){console.error("FAIL: 缺个人版限制背景版本号");process.exit(1);}if(!/路径\s*A/.test(r)){console.error("FAIL: 路径 A 被误删");process.exit(1);}if(!/安全说明/.test(r)){console.error("FAIL: 安全说明章节被误删");process.exit(1);}console.log("PASS: 路径 B 已替换为 publish 模式, publish.html/版本号/路径 A/安全说明 全在");'</automated>
  </verify>
  <done>README 不再含「联系 Claude 获取方案」旧占位；含 publish.html 链接 + publish 模式安装步骤 + 个人版 12.1.0.16910 安全限制背景；路径 A、安全说明、使用、真机清单章节完整保留。</done>
</task>

</tasks>

<verification>
两个文件落地后整体核对：

```bash
ls -la public/wps-probe/publish.html public/wps-probe/README.md
# publish.html 含 et + wpp 两条 AsterProbe、无残留占位符
grep -c 'AsterProbe' public/wps-probe/publish.html   # ≥2
grep -E 'PUBLISH_REPLACE_STRING|SERVERID_REPLEASE_STRING' public/wps-probe/publish.html && echo "BAD: 占位符残留" || echo "OK: 无残留占位符"
# jsplugins.xml 未被改动（并行路径仍在）
ls public/wps-probe/jsplugins.xml && echo "OK: jsplugins.xml 保留"
# README 已替换路径 B
grep -q 'publish.html' public/wps-probe/README.md && echo "OK: README 提及 publish.html"
```
</verification>

<success_criteria>
- public/wps-probe/publish.html 新增，字节级复制官方 wpsjs 2.2.3 模板 + 两处占位符替换，含 et+wpp 两条 AsterProbe curList、URL 就位、无残留占位符。
- public/wps-probe/README.md 路径 B 已替换为真实 publish 模式安装步骤 + 个人版限制背景；其余章节完整。
- src/ 与 Aster 主应用零改动；jsplugins.xml / index.html / probe.js / ribbon.xml 未被触碰。
- 执行者只做文件写入 + 一次原子 commit（不 push）。
</success_criteria>

<release_note>
**发布类改动 — 最后一步（执行者不做，留给 orchestrator/用户）：**

publish.html 在 `public/`，Vite `npm run build` 会原样拷进 `dist/`，再 commit + push origin/main 才触发 GitHub Pages 部署、线上 `https://wynne-cwb.github.io/Aster/wps-probe/publish.html` 才更新。本地 commit ≠ 线上更新。

执行者职责到此为止：**只写两个文件 + 一次原子 commit（不 push）**。
后续显式步骤（CLAUDE.md 已授权本项目 push，但由 orchestrator/用户执行）：
1. `npm run build`（Vite 拷 public/ → dist/）
2. 确认 dist/wps-probe/publish.html 已生成
3. commit + `git push origin main` → GitHub Pages 部署
4. 部署后实测 `https://wynne-cwb.github.io/Aster/wps-probe/publish.html` 返回 200
</release_note>

<output>
完成后创建 `.planning/quick/260629-eg8-phase-30-wps-wpsjs-publish-publish-html-/260629-eg8-SUMMARY.md`。
SUMMARY 中必须注明：文件已写 + 已本地 commit、**尚未 push（线上未更新）**、待执行 build+push 的显式步骤清单。
</output>

---
phase: quick-260531-bg2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Settings/SettingsPanel.tsx
  - src/components/Settings/SettingsPanel.test.tsx
  - src/styles.css
  - src/locales/zh-CN/messages.po
  - src/locales/zh-CN/messages.ts
autonomous: true
requirements: [HIST-02]
must_haves:
  truths:
    - "点一次「清空聊天记录」不清空，切换到确认态"
    - "确认态显示「确认清空？」+「确认」+「取消」，点「确认」才调用 clearHistory"
    - "点「取消」回到初始态，clearHistory 未被调用"
    - "i18n 新文案已在 messages.po/ts 中注册"
    - "全套 659 个测试仍绿；新增 3 个测试覆盖两步确认逻辑"
    - "初始 main-*.js ≤ 82 KB gzip（build + size 验证）"
  artifacts:
    - path: "src/components/Settings/SettingsPanel.tsx"
      provides: "内联两步确认逻辑（useState confirming）"
      contains: "confirming"
    - path: "src/styles.css"
      provides: "确认态行样式 .hist-confirm-row"
      contains: "hist-confirm-row"
    - path: "src/components/Settings/SettingsPanel.test.tsx"
      provides: "三个新测试用例 T-bg2-01~03"
      contains: "T-bg2-01"
  key_links:
    - from: "SettingsPanel.tsx clearHistory 按钮"
      to: "confirming state"
      via: "onClick -> setConfirming(true)"
      pattern: "setConfirming\\(true\\)"
    - from: "确认按钮"
      to: "clearHistory(docKeyRef.current)"
      via: "onClick"
      pattern: "clearHistory\\(docKeyRef\\.current\\)"
---

<objective>
给 SettingsPanel「清空聊天记录」按钮加内联两步确认，防止误点。

Purpose: 操作不可逆（清除当前文档聊天历史），需要二次确认保护；采用内联确认态（非 modal/alert）以保持与 Office 任务窗格风格一致、不依赖 window.confirm。
Output: SettingsPanel.tsx 内联确认态 + styles.css 样式 + i18n 文案 + 三个新测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260531-bg2-settingspanel-clear-history-button-confi/260531-bg2-PLAN.md

<interfaces>
<!-- 从 SettingsPanel.tsx 提取的关键接口，供执行时直接使用 -->

清空按钮当前位置（第 211-224 行）：
```tsx
{/* Phase 8 HIST-02 — 清空聊天记录（D-12 只清当前文档）*/}
<div className="aster-settings__section">
  <button
    type="button"
    className="btn btn-ghost btn-ghost--muted"
    onClick={() => clearHistory(docKeyRef.current)}
    aria-label={t`清空聊天记录`}
  >
    <Trans>清空聊天记录</Trans>
  </button>
  <p className="aster-settings__hint">
    <Trans>清除当前文档的聊天历史，不影响其他文档</Trans>
  </p>
</div>
```

已有 CSS 按钮类（styles.css）：
- `.btn .btn-ghost .btn-ghost--muted .btn-sm` — 已定义，直接复用
- `.btn-primary` — accent teal，用于「确认」按钮
- `--text-2`, `--accent`, `--border`, `--surface-2` — 可用 CSS 变量

现有 chat store mock（SettingsPanel.test.tsx 第 74-79 行）：
```ts
vi.mock('../../store/chat', () => ({
  useChatStore: vi.fn((selector: (s: unknown) => unknown) =>
    selector({ clearHistory: vi.fn() })
  ),
}));
```
注意：mock 在 vi.mock factory 内定义了 clearHistory: vi.fn()，但 beforeEach clearAllMocks()
会重置调用记录。新测试需要在 describe 外层用 vi.hoisted 创建一个稳定的 spy 引用，
以便在 expect 中断言调用次数——参考 ProviderList.test.tsx 的 useProviderStore.setState 模式。

替代方案（更简洁）：直接在 vi.mock 之前用 vi.hoisted 提升 clearHistory spy：
```ts
const { clearHistory } = vi.hoisted(() => ({ clearHistory: vi.fn() }));
vi.mock('../../store/chat', () => ({
  useChatStore: vi.fn((selector) => selector({ clearHistory })),
}));
```
这样 clearHistory 在所有测试中是同一个 vi.fn() 引用，可以直接 expect(clearHistory).toHaveBeenCalledTimes(1)。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 内联两步确认 UI + 样式 + i18n</name>
  <files>
    src/components/Settings/SettingsPanel.tsx,
    src/styles.css,
    src/locales/zh-CN/messages.po,
    src/locales/zh-CN/messages.ts
  </files>
  <behavior>
    - 初始态：点「清空聊天记录」→ setConfirming(true)，clearHistory **不调用**
    - 确认态：渲染「确认清空？」文字 + 「确认」按钮 + 「取消」按钮
    - 点「确认」→ clearHistory(docKeyRef.current) + setConfirming(false)
    - 点「取消」→ setConfirming(false)，clearHistory 不调用
    - 无 window.confirm/alert
    - 无硬编码 hex，无内联 style 传色
  </behavior>
  <action>
**Step A — SettingsPanel.tsx**

在 `useState` 导入处添加 `confirming` 状态（紧接在 `editState` useState 之后或在组件顶部 state 区块新增一行）：

```tsx
const [confirming, setConfirming] = useState(false);
```

将 HIST-02 区块（第 211-224 行）替换为内联两步确认 UI：

```tsx
{/* Phase 8 HIST-02 — 清空聊天记录（D-12 只清当前文档）+ 内联两步确认（bg2）*/}
<div className="aster-settings__section">
  {confirming ? (
    <div className="hist-confirm-row">
      <span className="hist-confirm-row__label">
        <Trans>确认清空？</Trans>
      </span>
      <div className="hist-confirm-row__actions">
        <button
          type="button"
          className="btn btn-ghost btn-sm"
          onClick={() => setConfirming(false)}
          aria-label={t`取消`}
        >
          <Trans>取消</Trans>
        </button>
        <button
          type="button"
          className="btn btn-primary btn-sm"
          onClick={() => {
            clearHistory(docKeyRef.current);
            setConfirming(false);
          }}
          aria-label={t`确认`}
        >
          <Trans>确认</Trans>
        </button>
      </div>
    </div>
  ) : (
    <button
      type="button"
      className="btn btn-ghost btn-ghost--muted"
      onClick={() => setConfirming(true)}
      aria-label={t`清空聊天记录`}
    >
      <Trans>清空聊天记录</Trans>
    </button>
  )}
  <p className="aster-settings__hint">
    <Trans>清除当前文档的聊天历史，不影响其他文档</Trans>
  </p>
</div>
```

**Step B — styles.css**

在 `/* Phase 8 HIST-02 */` 注释块（第 242-245 行）之后插入 `.hist-confirm-row` 样式：

```css
/* Phase 8 HIST-02 bg2 — 内联两步确认行（清空聊天记录）*/
.hist-confirm-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-2);
}
.hist-confirm-row__label {
  font-size: var(--fs-13);
  color: var(--text-2);
  flex: 1;
}
.hist-confirm-row__actions {
  display: flex;
  gap: var(--space-1);
  flex-shrink: 0;
}
```

禁止使用硬编码 hex 或 `style={{}}` 属性，所有颜色走上述 CSS 变量。

**Step C — i18n catalog 同步**

执行：
```bash
cd /Users/wb.chen/Documents/Project/Aster && npm run extract
```

确认 `src/locales/zh-CN/messages.po` 新增了以下 msgid（若 extract 自动添加，检查即可；若缺失手动补）：
- `确认清空？`
- `确认`（若已有则不重复）
- `取消`（若已有则不重复）

extract 完成后执行 `npm run compile` 同步 messages.ts（防 i18n coverage 门失败）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - TS 无错误，build 成功
    - SettingsPanel.tsx 含 `confirming` state + 两步按钮 UI
    - styles.css 含 `.hist-confirm-row` 定义
    - messages.po/ts 已同步新文案
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 SettingsPanel 测试（T-bg2-01~03）</name>
  <files>
    src/components/Settings/SettingsPanel.test.tsx
  </files>
  <behavior>
    - T-bg2-01：点一次「清空聊天记录」→ clearHistory 未调用，确认态出现（「确认清空？」或「确认」按钮可见）
    - T-bg2-02：在确认态点「确认」→ clearHistory 被调用 1 次
    - T-bg2-03：在确认态点「取消」→ 回初始态，clearHistory 未调用
    - 已有 SP-01~SP-04 四个冒烟用例全部绿（不破坏）
  </behavior>
  <action>
在文件顶部（所有 import 之前）添加 vi.hoisted 提升 clearHistory spy，并更新现有 chat store mock 以使用这个稳定引用：

```ts
// ---------------------------------------------------------------------------
// 提升 clearHistory spy（vi.hoisted 确保在 vi.mock factory 之前求值）
// ---------------------------------------------------------------------------
const { clearHistory } = vi.hoisted(() => ({ clearHistory: vi.fn() }));
```

将现有 chat store mock（第 74-79 行）改为使用提升后的 spy：
```ts
vi.mock('../../store/chat', () => ({
  useChatStore: vi.fn((selector: (s: unknown) => unknown) =>
    selector({ clearHistory })
  ),
}));
```

注意：现有 `beforeEach(() => { vi.clearAllMocks(); })` 会在每个测试前重置 clearHistory 的调用记录，满足断言隔离要求，不需要改动。

在文件末尾（`describe('SettingsPanel — 冒烟测试', ...)` 之后）追加新的 describe 块：

```tsx
// ---------------------------------------------------------------------------
// T-bg2：清空聊天记录内联两步确认
// ---------------------------------------------------------------------------
import { fireEvent } from '@testing-library/react';

describe('T-bg2 — 清空聊天记录内联两步确认', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('T-bg2-01：点一次触发确认态，clearHistory 未调用', () => {
    const { getByText } = render(<SettingsPanel onClose={vi.fn()} />);
    // 初始态：「清空聊天记录」可见
    const clearBtn = getByText('清空聊天记录');
    fireEvent.click(clearBtn);
    // 确认态：「确认清空？」文字或「确认」按钮出现
    expect(getByText('确认清空？')).toBeTruthy();
    expect(clearHistory).not.toHaveBeenCalled();
  });

  it('T-bg2-02：确认态点「确认」→ clearHistory 调用 1 次', () => {
    const { getByText } = render(<SettingsPanel onClose={vi.fn()} />);
    fireEvent.click(getByText('清空聊天记录'));
    fireEvent.click(getByText('确认'));
    expect(clearHistory).toHaveBeenCalledTimes(1);
  });

  it('T-bg2-03：确认态点「取消」→ 回初始态，clearHistory 未调用', () => {
    const { getByText, queryByText } = render(<SettingsPanel onClose={vi.fn()} />);
    fireEvent.click(getByText('清空聊天记录'));
    // 确认态存在
    expect(getByText('确认清空？')).toBeTruthy();
    // 点取消
    fireEvent.click(getByText('取消'));
    // 回初始态：「清空聊天记录」重新出现，「确认清空？」消失
    expect(getByText('清空聊天记录')).toBeTruthy();
    expect(queryByText('确认清空？')).toBeNull();
    expect(clearHistory).not.toHaveBeenCalled();
  });
});
```

注意：`fireEvent` 来自 `@testing-library/react`，已在 devDependencies 中，无需新增依赖。
`import { fireEvent }` 在文件顶部的 import 行追加即可（与 `render` 同行）：
```ts
import { render, fireEvent } from '@testing-library/react';
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run src/components/Settings/SettingsPanel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SettingsPanel.test.tsx 跑出 7 个绿色测试（SP-01~04 + T-bg2-01~03）
    - clearHistory spy 在 T-bg2-02 中调用次数 = 1，在 T-bg2-01/03 中 = 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户点击 → clearHistory | 单向破坏性操作（删除 localStorage 聊天历史），误点不可撤销 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-bg2-01 | Tampering | clearHistory 调用路径 | mitigate | 内联两步确认守门：onClick 第一次只 setConfirming(true)，不调用 clearHistory；第二次点「确认」才调用，完全在组件内控制 |
| T-bg2-02 | DoS（误操作） | 聊天历史 localStorage 数据 | mitigate | 二次确认阻止误触（同 T-bg2-01 缓解），无需 undo 能力（HIST-02 设计内明确「清空不可撤销」） |
</threat_model>

<verification>
按顺序全部绿才算完成：

1. 单文件测试（7 个用例）：
   ```
   cd /Users/wb.chen/Documents/Project/Aster
   npm test -- --run src/components/Settings/SettingsPanel.test.tsx
   ```
   期望：SP-01~04 + T-bg2-01~03 全绿，共 7 个 pass

2. 全套测试不破坏：
   ```
   npm test -- --run
   ```
   期望：pass 数 ≥ 659（新增 3 → 662）

3. TS 无错误：
   ```
   npm run build
   ```
   期望：0 errors, 0 warnings（TS strict）

4. Bundle size 守门（先 build 再 size）：
   ```
   npm run build && npm run size
   ```
   期望：initial main-*.js ≤ 82 KB gzip（0 净新增依赖，无风险）
</verification>

<success_criteria>
- SettingsPanel「清空聊天记录」按钮改为内联两步确认，初次点击不清空，确认后才执行
- 视觉使用 `.btn .btn-ghost .btn-primary .btn-sm` + `.hist-confirm-row` CSS 类，0 硬编码 hex
- i18n 三个文案（「确认清空？」「确认」「取消」）已在 messages.po/ts 中注册
- 7 个 SettingsPanel 测试全绿，全套 ≥ 659 pass
- build 无 TS 错误，bundle ≤ 82 KB gzip
</success_criteria>

<output>
完成后在 `.planning/quick/260531-bg2-settingspanel-clear-history-button-confi/` 目录下创建 `260531-bg2-SUMMARY.md`，记录：
- 改动文件列表
- 确认态 UI 实现方式（内联 state，不是 modal）
- 样式类名 `.hist-confirm-row`
- i18n catalog 同步步骤
- 测试结果（7 个用例 pass）
</output>

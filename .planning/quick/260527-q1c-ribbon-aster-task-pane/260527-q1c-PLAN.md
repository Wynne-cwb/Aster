---
phase: quick-260527-q1c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.xml
  - src/components/ChatStream.tsx
  - src/i18n/locales/zh-CN/messages.po
  - src/i18n/locales/zh-CN/messages.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-foundation/01-UI-SPEC.md
autonomous: true
requirements: [FOUND-10]
quick: true

must_haves:
  truths:
    - "三宿主（PPT/Excel/Word）各只剩 1 个 Ribbon 按钮，文案统一为「打开 Aster」"
    - "点击任一宿主的 Aster 按钮仍打开同一个 Task Pane（ShowTaskpane 不变）"
    - "Task Pane 打开后空状态显示用法提示，含按宿主区分的典型用法示例"
    - "用法提示文案走 Lingui i18n、默认中文，编译后 catalog 含新条目"
    - "REQUIREMENTS.md FOUND-10 措辞反映「每宿主 1 个统一入口 + 能力在 Task Pane 内」最终决策"
  artifacts:
    - path: "manifest.xml"
      provides: "每宿主单一统一 Aster 入口按钮（共 3 个 ShowTaskpane Control）"
      contains: "Btn.Aster.Open.Label"
    - path: "src/components/ChatStream.tsx"
      provides: "空状态用法提示 UI（按宿主切换示例）"
    - path: "src/i18n/locales/zh-CN/messages.po"
      provides: "新增用法提示 + 按钮文案的 zh-CN 条目"
  key_links:
    - from: "src/components/ChatStream.tsx"
      to: "useAdapter().capabilities().host"
      via: "按宿主选择示例文案"
      pattern: "capabilities\\(\\)\\.host"
    - from: "manifest.xml Control id=\"*.Open\""
      to: "Taskpane.Url"
      via: "Action xsi:type=ShowTaskpane"
      pattern: "ShowTaskpane"
---

<objective>
精简 Ribbon 入口为单一 Aster 入口，并在 Task Pane 空状态内增加用法提示引导。

这是对已交付 Phase 1（foundation，人工 UAT 4/4 通过）的范围缩减，不是新功能：
- 锁定决策：去掉「每宿主 2 个功能占位按钮」设计，改为每宿主 1 个统一的「打开 Aster」入口（Office for Web 技术上每宿主必须各 1 个按钮定义，故实际 3 个，但文案与图标完全统一，概念上呈现为「一个 Aster 入口」）。
- 删掉每宿主多出来的那 1 个按钮（PPT/Excel/Word 各从 2 个减到 1 个）。
- 原本想靠 Ribbon 功能按钮承载的功能入口，改由 Task Pane 内的「用法提示」空状态引导取代。

Purpose: Ribbon 不再暗示「点按钮 = 触发某个一键功能」（Phase 1 这些按钮本就只是 ShowTaskpane），统一为单一入口，符合「所有 AI 能力在 Task Pane 内触发」的最终架构。
Output: 精简后的 manifest.xml、带用法提示的 ChatStream、同步的 i18n catalog 与 REQUIREMENTS/UI-SPEC 文档。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-foundation/01-UI-SPEC.md

# 现状事实（orchestrator 已核实，executor 不要重新假设）
# - manifest.xml 当前每宿主 2 个 ShowTaskpane 按钮，共 6 个：
#     PPT: PPT.Outline / PPT.SlideImage
#     Excel: XL.Formula / XL.ExplainFix
#     Word: WD.Polish / WD.Tldr
#   6 个按钮全部 ShowTaskpane（无 ExecuteFunction），共用 Aster 图标组。
# - src/commands.ts 的 Office.actions.associate('openTaskpane', ...) 当前未被任何按钮引用，
#   是 Phase 4-6 预留点。本次不改 commands.ts（保持 ShowTaskpane 模式）。
# - REQUIREMENTS.md FOUND-10 已有一处未提交的手工改动（把「6 个占位」改成
#   「三宿主各 1 个 ShowTaskpane 入口」）。本任务在主工作区执行，executor 提交时
#   把这处工作区改动连同本次新增措辞一并纳入同一提交。

<interfaces>
<!-- ChatStream 按宿主切换示例所需的契约。直接使用，无需再探索代码库。 -->

来自 src/context/AdapterContext.ts：
```typescript
export function useAdapter(): DocumentAdapter; // 在 AdapterContext.Provider 内可用
```

来自 src/adapters/DocumentAdapter.ts：
```typescript
export interface AdapterCapabilities {
  supportedInserts: InsertableContent['type'][];
  supportsSelectionEvents: boolean;
  host: 'ppt' | 'excel' | 'word';   // ← 用此字段选择宿主对应的示例文案
}
export interface DocumentAdapter {
  getSelection(): Promise<SelectionContext>;
  onSelectionChanged(callback: () => void): () => void;
  capabilities(): AdapterCapabilities;  // Phase 1 桩，返回静态 host 标识，可安全调用
  insert(content: InsertableContent): Promise<void>;
}
```

来自 src/components/ChatStream.tsx（当前空态写法，本次扩充它）：
```tsx
import { Text, tokens } from '@fluentui/react-components';
import { Trans } from '@lingui/react/macro';
// 居中 flex 列：heading「开始使用 Aster」(size 400 semibold) + body「配置 Provider 后即可开始对话」(size 300, neutralForeground3)
```

i18n 写法约定（项目现有）：
- 静态文案用 `<Trans>中文</Trans>`（来自 `@lingui/react/macro`）。
- 改完后必须跑 `npm run extract` 再 `npm run compile`，让 messages.po 与 messages.ts 含新条目（catalog 当前 zh-CN 自翻译，msgstr === msgid）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: manifest.xml 精简为每宿主单一统一 Aster 入口</name>
  <files>manifest.xml</files>
  <action>
将三宿主各自的 2 个按钮精简为每宿主 1 个统一的「打开 Aster」入口按钮（实现 FOUND-10 最终决策）。

具体改动：
1. PPT（Presentation Host，CommandsGroup）：删除 `<Control id="PPT.Outline">` 与 `<Control id="PPT.SlideImage">` 两个 Control，替换为单一 `<Control xsi:type="Button" id="Aster.Open">`。
2. Excel（Workbook Host，CommandsGroupXL）：删除 `XL.Formula` / `XL.ExplainFix`，替换为单一 `<Control xsi:type="Button" id="AsterXL.Open">`。
3. Word（Document Host，CommandsGroupWD）：删除 `WD.Polish` / `WD.Tldr`，替换为单一 `<Control xsi:type="Button" id="AsterWD.Open">`。

每个新 Control 统一使用：
- Label：`resid="Btn.Aster.Open.Label"`
- Supertip Title：`resid="Btn.Aster.Open.Label"`
- Supertip Description：`resid="Btn.Aster.Open.Tip"`（必须引 LongString，引 ShortString 会运行时报 AddinManifestError — 见 manifest 注释「必修项 3」）
- Icon：复用现有 Icon.16x16 / Icon.32x32 / Icon.80x80（共用 Aster 图标组，满足 INSTALL-05，不改）
- Action：`<Action xsi:type="ShowTaskpane"><TaskpaneId>ButtonId1</TaskpaneId><SourceLocation resid="Taskpane.Url"/></Action>`（与现状一致，不改为 ExecuteFunction）

注意 Office for Web 限制：三宿主各需独立 Control 定义，无法跨宿主共用，所以 Control id 不同（Aster.Open / AsterXL.Open / AsterWD.Open），但 Label/Tip/图标 resid 完全统一 —— 概念上是「一个 Aster 入口」。

Resources 段清理：
- `<bt:ShortStrings>`：删除 6 条旧按钮 Label（Btn.PPT.Outline.Label / Btn.PPT.SlideImage.Label / Btn.XL.Formula.Label / Btn.XL.ExplainFix.Label / Btn.WD.Polish.Label / Btn.WD.Tldr.Label），新增 1 条：`<bt:String id="Btn.Aster.Open.Label" DefaultValue="打开 Aster"/>`。保留 `CommandsGroup.Label`（DefaultValue="Aster"）与 `GetStarted.Title`。
- `<bt:LongStrings>`：删除 6 条旧按钮 Tip，新增 1 条：`<bt:String id="Btn.Aster.Open.Tip" DefaultValue="打开 Aster 任务窗格，在原生 Office 内用自带 API Key 享受 AI 提效。"/>`。保留 GetStarted.Description。
- 更新三段 Host 顶部的 XML 注释（「2 个按钮：…」改为「单一 Aster 入口：ShowTaskpane 打开 Task Pane」）以免注释与实现脱节。
- 更新 Resources 内 `D-10: 6 个按钮共用 Aster 图标组` 注释为「3 个 Aster 入口共用图标组」。

不要改动：Id/Version/Hosts/AppDomains/Permissions/Runtimes/GetStarted/FunctionFile/Commands.Url/Taskpane.Url/图标 URL/CommandsGroup 的 Icon。不引入新依赖。
  </action>
  <verify>
    <automated>npx office-addin-manifest validate manifest.xml</automated>
  </verify>
  <done>manifest.xml 三宿主各恰好 1 个 Control（id 分别 Aster.Open / AsterXL.Open / AsterWD.Open），均 ShowTaskpane 指向 Taskpane.Url；ShortStrings 含 Btn.Aster.Open.Label="打开 Aster" 且无残留旧按钮 resid；LongStrings 含 Btn.Aster.Open.Tip；`office-addin-manifest validate` 通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Task Pane 空状态加按宿主用法提示</name>
  <files>src/components/ChatStream.tsx, src/i18n/locales/zh-CN/messages.po, src/i18n/locales/zh-CN/messages.ts</files>
  <behavior>
    - PPT 宿主：用法提示示例反映 PPT 典型用法（如「把主题生成大纲」「为选中 slide 配图」）
    - Excel 宿主：示例反映 Excel 典型用法（如「用自然语言写公式」「解释/修复报错公式」）
    - Word 宿主：示例反映 Word 典型用法（如「多风格润色选中文段」「长文一键 TL;DR」）
    - 编译后 messages.po / messages.ts 含全部新增中文条目，msgstr 非空
  </behavior>
  <action>
扩充 ChatStream.tsx 的空状态块，在现有 heading「开始使用 Aster」+ body「配置 Provider 后即可开始对话」下方追加一个「用法提示」区，按当前宿主给出贴切示例（取代原本靠 Ribbon 功能按钮承载的功能入口）。

实现要点：
1. 用 `useAdapter()`（来自 ../context/AdapterContext）取 adapter，调用 `adapter.capabilities().host` 得到 `'ppt' | 'excel' | 'word'`（Phase 1 桩可安全调用，返回静态宿主标识）。
2. 用 host 选择对应的示例文案数组（每宿主 2-3 条，见下）。文案全部用 `<Trans>` 宏（`@lingui/react/macro`），保持与现有空态一致的 i18n 方式。
3. UI 形态（Fluent UI v9，遵守 01-UI-SPEC token 规则）：
   - 一个小标题 `<Text size={200}>` colorNeutralForeground3，文案「试试这些」。
   - 示例用 Fluent v9 `Badge`（`appearance="tint"`, `color="informative"`, `size="large"`）或简单的 `Text` 行竖排呈现为「示例 prompt 芯片」；芯片只读、不可点（Phase 1 输入栏本就 disabled，不要让它看起来可交互到能触发请求）。竖排用 flex column，gap 用 `tokens.spacingVerticalS`。
   - 整体仍居中、保持现有 padding token；不破坏 flex:1 居中布局。
   - 仅引用 Fluent v9 token，禁硬编 px/hex（01-UI-SPEC 硬规则）。从 `@fluentui/react-components` 具名导入（无 barrel 风险，组件库已是该包）。
4. 示例文案（zh-CN，按宿主，可微调措辞，须贴切三宿主典型用法）：
   - PPT：「把主题扩展成多页大纲」、「为选中的 slide 配一张图」
   - Excel：「用自然语言生成公式」、「解释并修复报错的公式」
   - Word：「多风格润色选中文段」、「长文一键生成 TL;DR」
   小标题「试试这些」。
5. 不新增依赖、不改 App.tsx 布局、不改 InputBar/ContextCard、不改 commands.ts。

i18n catalog 同步（必做，否则中文不渲染 — 参考 quick 260527-o8j 教训）：
- 加完 `<Trans>` 后运行 `npm run extract`（生成/更新 messages.po 新条目），再运行 `npm run compile`（生成 messages.ts）。
- 确认 messages.po 内新增条目 msgstr 非空（zh-CN 自翻译，msgstr 应等于 msgid 中文原文；lingui extract 对 source=target locale 会自动以原文填充，如有空 msgstr 手工补上中文原文）。
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ChatStream 渲染按宿主区分的用法提示示例芯片；三宿主示例文案均为对应典型用法；`npm run build`（含 lingui compile + vite build + size-limit 路径）成功；messages.po 含新增条目且 msgstr 非空；无新依赖、bundle 仍在预算内（build 不报 size 超限）。</done>
</task>

<task type="auto">
  <name>Task 3: 同步 REQUIREMENTS.md FOUND-10 与 01-UI-SPEC 文档</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/01-foundation/01-UI-SPEC.md</files>
  <action>
让文档反映最终决策，并纳入工作区已有的未提交改动。

1. REQUIREMENTS.md FOUND-10：确认/调整措辞为 ——
   `**FOUND-10**：三宿主各 1 个统一的「打开 Aster」Ribbon 入口按钮（PPT/Excel/Word 共 3 个），`Action=ShowTaskpane`，点击打开 Aster Task Pane；无功能动作按钮——所有 AI 能力在 Task Pane 内触发（含空状态用法提示引导）`
   （工作区已有的手工改动若措辞接近，统一为上述最终版即可；executor 提交时这处改动与本次一并纳入。）
   不改其它 REQ 条目，不改 Traceability 表（FOUND-10 仍属 Phase 1）。

2. 01-UI-SPEC.md（Phase 1 设计契约，需与实现保持一致）：
   - §Component Inventory 表内「Ribbon | 6 placeholder buttons」一行：改为「Ribbon | 1 个统一 Aster 入口按钮/宿主（共 3 个）」，State 列保持「Click → open Task Pane only」。
   - 紧随其后的「Ribbon button labels（…）」小节：删除 6 个功能候选名清单，改为说明「每宿主单一入口，统一文案『打开 Aster』、共用 Aster 图标组；功能入口改由 Task Pane 空状态用法提示承载」。
   - §Copywriting Contract 表：可补一行「Ribbon 入口按钮 | 打开 Aster」与「空状态用法提示小标题 | 试试这些」（与 Task 2 实现文案一致）；其余 Copywriting 行保持。
   - 不改 Checker Sign-Off 区块（保持 approved 标记，不伪造新审查）。

保持文档其余内容不变，措辞精炼、与代码实现一致。
  </action>
  <verify>
    <automated>grep -n "打开 Aster" .planning/REQUIREMENTS.md .planning/phases/01-foundation/01-UI-SPEC.md && grep -v '^#' .planning/phases/01-foundation/01-UI-SPEC.md | grep -c "6 placeholder buttons"</automated>
  </verify>
  <done>REQUIREMENTS.md FOUND-10 措辞为「每宿主 1 个统一『打开 Aster』入口 + 能力在 Task Pane 内触发」；01-UI-SPEC §Component Inventory 不再写「6 placeholder buttons」（grep 计数为 0），改述为单一入口 + 用法提示承载功能入口；两文件均含「打开 Aster」字样。</done>
</task>

</tasks>

<verification>
- `npx office-addin-manifest validate manifest.xml` 通过，三宿主各恰好 1 个 ShowTaskpane Control。
- `npm run build` 成功（lingui compile + vite build + size 守卫），无新依赖。
- messages.po / messages.ts 含新增用法提示与按钮文案条目，msgstr 非空。
- ChatStream 按宿主渲染不同示例文案。
- REQUIREMENTS.md / 01-UI-SPEC.md 与实现一致。

## Office for Web 内人工 UAT（参考 office-addin-browser-uat 技能）
本任务有可视改动，建议在 Office for Web 内验证（非自动化，作为收尾确认）：
1. 重新 sideload 更新后的 manifest.xml 到 PPT for Web / Excel for Web / Word for Web。
2. 各宿主功能区（Home tab，Aster 组）只见到 1 个「打开 Aster」按钮（不再有第 2 个按钮）。
3. 点击该按钮 → 打开 Aster Task Pane，无 console error（INSTALL-03 不回归）。
4. Task Pane 空状态显示「开始使用 Aster」+「试试这些」+ 当前宿主对应的示例芯片（PPT 看到大纲/配图类，Excel 看到公式类，Word 看到润色/TL;DR 类），文案为中文。
5. 切换到另一宿主重复打开，确认示例随宿主变化。
</verification>

<success_criteria>
- 三宿主 Ribbon 各精简为 1 个统一「打开 Aster」入口按钮（ShowTaskpane 行为不变）。
- Task Pane 空状态含按宿主区分的用法提示，文案走 Lingui i18n、默认中文。
- i18n catalog（.po + .ts）已重新 extract+compile，新条目齐全。
- REQUIREMENTS.md FOUND-10 与 01-UI-SPEC 同步最终决策；工作区未提交的 FOUND-10 改动一并纳入提交。
- 无新依赖、bundle 仍在预算内、未做超范围重构。
- Office for Web 三宿主 sideload + 打开 Task Pane 不回归（人工 UAT）。
</success_criteria>

<output>
完成后创建 .planning/quick/260527-q1c-ribbon-aster-task-pane/260527-q1c-SUMMARY.md
</output>

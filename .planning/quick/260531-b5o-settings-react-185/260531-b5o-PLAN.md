---
phase: quick/260531-b5o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Settings/SettingsPanel.tsx
  - src/components/Settings/SettingsPanel.test.tsx
autonomous: true
requirements:
  - PREF-01
must_haves:
  truths:
    - "Settings 面板在 browse 态可正常挂载，不触发 React #185 无限重渲染"
    - "偏好 textarea、三个预设 chips、清空聊天记录按钮可被测试断言"
  artifacts:
    - path: "src/components/Settings/SettingsPanel.tsx"
      provides: "selector 修复（rawInput / setPrefs 拆成两个独立 selector）"
    - path: "src/components/Settings/SettingsPanel.test.tsx"
      provides: "冒烟测试守门（渲染不抛错 + 关键 UI 断言）"
  key_links:
    - from: "src/components/Settings/SettingsPanel.tsx"
      to: "src/store/preferences.ts"
      via: "usePreferencesStore 两个独立 selector"
      pattern: "usePreferencesStore\\(\\(s\\) => s\\.rawInput\\)"
---

<objective>
修复 Settings 面板 React error #185（无限重渲染导致白屏），并补冒烟测试守门防止复发。

Purpose: SettingsPanel 第 55-57 行用了返回新对象的 zustand selector，导致 useSyncExternalStore 每次 render 拿到新引用，触发无限重渲染。修复后 Settings 面板恢复正常。测试守门确保同类问题一旦复现，CI 立即捕获。

Output:
- 修复后的 SettingsPanel.tsx（selector 拆分，与同文件既有范式一致）
- 新建 SettingsPanel.test.tsx（冒烟测试，真实挂载触发 useSyncExternalStore）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!-- 关键接口（已从源文件提取，executor 无需探索 codebase） -->
<interfaces>
<!-- src/store/preferences.ts -->
```typescript
interface PreferencesState {
  userPrefs: string | null;
  rawInput: string;
  setPrefs(raw: string): void;
  loadPrefs(): void;
}
export const usePreferencesStore: StoreApi<PreferencesState>; // zustand store
```

<!-- SettingsPanel props（来自 SettingsPanel.tsx 第 35-39 行）-->
```typescript
interface SettingsPanelProps {
  onClose: () => void;
  initialAnchor?: string;
  onShowOnboarding?: () => void;
}
```

<!-- 需要 mock 的依赖（来自 ProviderList.test.tsx / ChatStream.test.tsx 范式）-->
// @lingui/react/macro
vi.mock('@lingui/react/macro', () => ({
  Trans: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  useLingui: () => ({
    t: (strings: TemplateStringsArray, ...values: unknown[]) =>
      strings.reduce((acc, s, i) => acc + s + (values[i] ?? ''), ''),
  }),
}));

// ../../lib/storage（preferences store 内部依赖）
vi.mock('../../lib/storage', () => ({
  storage: { get: vi.fn(() => null), set: vi.fn() },
  STORAGE_KEYS: { USER_PREFERENCES: 'aster:prefs', KEY_PREFIX: 'aster:keys:', PROVIDERS: 'aster:providers' },
}));

// ../../lib/docKey（SettingsPanel 内 useEffect 依赖）
vi.mock('../../lib/docKey', () => ({
  getDocKey: vi.fn(() => Promise.resolve('aster:chat:global')),
}));

// ProviderList、ProviderForm（避免深渲染，直接 stub）
vi.mock('./ProviderList', () => ({
  default: () => <div data-testid="provider-list-stub" />,
}));
vi.mock('./ProviderForm', () => ({
  default: () => <div data-testid="provider-form-stub" />,
}));

// ../../store/providers（SettingsPanel 消费：attachEnabled / setAttachEnabled / providers 等）
vi.mock('../../store/providers', () => ({
  useProviderStore: vi.fn((selector: (s: unknown) => unknown) =>
    selector({
      attachEnabled: false,
      setAttachEnabled: vi.fn(),
      providers: [],
      addProvider: vi.fn(() => 'new-id'),
      updateProvider: vi.fn(),
      setKey: vi.fn(),
    })
  ),
}));

// ../../store/chat（clearHistory）
vi.mock('../../store/chat', () => ({
  useChatStore: vi.fn((selector: (s: unknown) => unknown) =>
    selector({ clearHistory: vi.fn() })
  ),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 selector + 补冒烟测试守门</name>
  <files>
    src/components/Settings/SettingsPanel.tsx
    src/components/Settings/SettingsPanel.test.tsx
  </files>
  <action>
**步骤 A — 修复 SettingsPanel.tsx（最小改动）**

将第 55-57 行的对象 selector 写法：
```typescript
// 修复前（触发无限重渲染）
const { rawInput, setPrefs } = usePreferencesStore(
  (s) => ({ rawInput: s.rawInput, setPrefs: s.setPrefs })
);
```
替换为两个独立 selector（与同文件第 60、69-75 行既有范式完全一致）：
```typescript
// 修复后（与同文件 attachEnabled / setAttachEnabled 等写法保持一致）
const rawInput = usePreferencesStore((s) => s.rawInput);
const setPrefs = usePreferencesStore((s) => s.setPrefs);
```

**不引入 useShallow**——同文件其他所有 selector 均为独立写法，保持一致。

**步骤 B — 新建 SettingsPanel.test.tsx**

新建 `src/components/Settings/SettingsPanel.test.tsx`，内容要点：

1. **Mock 清单**（按 context 中的接口定义，顺序如下）：
   - `@lingui/react/macro`：Trans 直通 children，useLingui 返回带 tagged template 支持的 t 函数
   - `../../lib/storage`：storage.get 返回 null，STORAGE_KEYS 含 USER_PREFERENCES
   - `../../lib/docKey`：getDocKey 返回 `Promise.resolve('aster:chat:global')`
   - `./ProviderList`：default stub，渲染 `<div data-testid="provider-list-stub" />`
   - `./ProviderForm`：default stub，渲染 `<div data-testid="provider-form-stub" />`
   - `../../store/providers`：useProviderStore 为 vi.fn()，调用时执行 selector(state)，state 含 attachEnabled/setAttachEnabled/providers/addProvider/updateProvider/setKey
   - `../../store/chat`：useChatStore 为 vi.fn()，调用时执行 selector(state)，state 含 clearHistory: vi.fn()

   **重要**：`usePreferencesStore` **不 mock**——让真实 zustand store + useSyncExternalStore 跑起来，这是守门的核心（修复前跑会因 #185 超时/抛错，修复后绿）。

2. **测试套件 `SettingsPanel — 冒烟测试`**，包含以下断言（不用 jest-dom matcher，改用原生 query + vitest expect）：

   - **SP-01：渲染不抛错**
     ```
     expect(() => render(<SettingsPanel onClose={vi.fn()} />)).not.toThrow();
     ```

   - **SP-02：偏好 textarea 存在**
     ```typescript
     const { container } = render(<SettingsPanel onClose={vi.fn()} />);
     const textarea = container.querySelector('textarea.aster-settings__pref-input');
     expect(textarea).toBeTruthy();
     ```

   - **SP-03：三个预设 chips 都渲染**
     ```typescript
     const { getByText } = render(<SettingsPanel onClose={vi.fn()} />);
     expect(getByText('正式语气')).toBeTruthy();
     expect(getByText('口语化')).toBeTruthy();
     expect(getByText('金额两位小数')).toBeTruthy();
     ```

   - **SP-04：清空聊天记录按钮存在**
     ```typescript
     const { getByText } = render(<SettingsPanel onClose={vi.fn()} />);
     expect(getByText('清空聊天记录')).toBeTruthy();
     ```

   每个 it 独立渲染（不复用同一个 render 结果），beforeEach 调用 vi.clearAllMocks()。
  </action>
  <verify>
    <automated>
# 1. 新测试绿
npm test -- --run src/components/Settings/SettingsPanel.test.tsx

# 2. 全套测试不破坏（含既有 655 测试）
npm test -- --run

# 3. TS 编译无错
npm run build

# 4. bundle size 守门（先 build 再 size）
npm run size
    </automated>
  </verify>
  <done>
- `SettingsPanel.test.tsx` 中 4 个用例全部通过（SP-01 ~ SP-04）
- 全套测试绿（不少于 655 + 4 = 659 个用例）
- `npm run build` 无 TypeScript 错误
- `npm run size` 报告初始 main-*.js ≤ 82 KB gzip
- SettingsPanel.tsx 第 55-57 行已改为两个独立 selector，无任何 useShallow 引入
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入→偏好 store | rawInput 经 sanitizePrefs 过滤后才写入 userPrefs（注入防御已在 store 层实现） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b5o-01 | Tampering | usePreferencesStore selector | mitigate | 修复：拆成独立 selector，消除 useSyncExternalStore 引用相等性问题 |
| T-b5o-02 | Denial of Service | React #185 无限重渲染 | mitigate | 修复后冒烟测试守门：真实渲染路径 + CI 卡门 |
</threat_model>

<verification>
全局检查清单：

1. `npm test -- --run src/components/Settings/SettingsPanel.test.tsx` 退出码 0（新测试绿）
2. `npm test -- --run` 退出码 0（全套不破坏）
3. `npm run build` 退出码 0（无 TS error）
4. `npm run build && npm run size` → main-*.js ≤ 82 KB gzip
5. SettingsPanel.tsx diff 确认：只改第 55-57 行，无其他改动，无 useShallow import
</verification>

<success_criteria>
- Settings 面板可正常打开，不再白屏（React #185 消除）
- `rawInput` 和 `setPrefs` 各用一个独立 selector，与同文件既有 attachEnabled/clearHistory 等写法保持一致
- 新建冒烟测试 4 用例全绿，作为结构性守门（同类 selector 问题再出现立即被 CI 捕获）
- bundle size 未变化（本次改动为纯逻辑 fix，无新依赖）
</success_criteria>

<output>
完成后创建 `.planning/quick/260531-b5o-settings-react-185/260531-b5o-SUMMARY.md`
</output>

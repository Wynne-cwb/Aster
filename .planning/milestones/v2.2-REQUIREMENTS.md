# Requirements (ARCHIVED): Aster v2.2「多模态四件套」

**Status:** ✅ SHIPPED 2026-06-03（tag `v2.2`，线上 `0d5fccf`）
**Defined:** 2026-06-01
**Archived:** 2026-06-03（milestone close）
**Core Value:** 在原生 Office 内部，让中文职场用户用自带 API Key 享受 AI 代理能力，能完成绝大部分文档工作；无后台、BYO Key、纯浏览器直连。
**Milestone Goal:** 给 Office 智能代理加上「看 / 读文件 / 生图 / 找图」四种多模态能力——Provider 客户端已在基座，v2.2 把它们接进 agent loop、配 tool、配 UI。
**Phase numbering:** 从 14 续接（v2.1 止于 Phase 13）。

> **归档说明：** 本文件是 v2.2 里程碑收官时的需求快照，全部 22 个需求经 Phase 14–18 实现 + Phase 19 三宿主真机 UAT 验证为 **Complete**。当前活跃需求见根 `.planning/REQUIREMENTS.md`（下个里程碑重建）。

---

## v2.2 Requirements — 全部交付 ✓（22/22）

每个新 write/插图工具沿用 v2.1 合约：先声明 undo 类型 + 配 `operationLog.integration.test` 守门。研究基线见 `.planning/research/SUMMARY.md`；生图 wire format 见 `.planning/spikes/011-image-gen-api-formats/findings.md`。

### MDL — AiHubMix Provider 重写（MM-05，下游硬前提）

- [x] **MDL-01**: 重写 `src/providers/aihubmix-image.ts` — 按 model 分发三路 response 解析器（doubao `output[].url` / gpt-image-2 `output.b64_json[].bytesBase64` / gemini `candidates[].content.parts[].inlineData.data`），内部统一为 base64 data URL（doubao URL 浏览器 fetch 转 base64），两套鉴权（Bearer / `x-goog-api-key`），gemini 走 `/gemini/v1beta` 端点族并跳过巨大 `thoughtSignature` 字段 — ✓ Phase 14（三路真打 HTTP 200 × 3）
- [x] **MDL-02**: 修正默认 model 清单 — registry/pricing 区分视觉 model（aihubmix-vision，开工时查 `/v1/models` 验证真实可用 id）与三个生图 model（doubao-seedream-5.0-lite 默认 / gpt-image-2 / gemini-3.1-flash-image-preview）；三路 provider smoke test 守门 — ✓ Phase 14（视觉 = gpt-5.4，`IMAGE_GEN_MODELS` 带 metadata 供 Phase 16 picker 消费）
- [x] **MDL-03**: PPT 工具 snake/camel casing 中央归一化根治 — dispatch 层统一 args 归一化，移除散落的双键容错兜底，加守门用例（防 v2.2 新增 PPT 生图工具重蹈 casing 覆辙）— ✓ Phase 14（dispatch 层 `normalizeToSnakeCase` + 删 pick* helper + 守门用例；技术债清账，见 memory `project_ppt_officejs_gotchas` / `project_excel_adapter_gotchas`）

### VIS — 视觉看图（MM-01）

- [x] **VIS-01**: agent 可「看」当前选中的图片/图表作 evidence — 新增 `get_shape_image` read tool（三宿主取图：PPT shape image / Excel chart / Word inline picture），选区驱动半隐式触发 — ✓ Phase 15（第 12 个 read tool；Excel/Word 取图真机可用，PPT 取图为已知宿主限制 Preview API 未 GA → fallback 引导上传）
- [x] **VIS-02**: 视觉走 aihubmix-vision（OpenAI `image_url` content part 格式，已存在客户端）；取图/调用失败给结构化错误（沿用 `{code,message,recoverable,hint}`）。**不验 DeepSeek-V4 原生多模态**（用户决定，省 spike）— ✓ Phase 15（三类失败 UX 全 PASS）

> **范围更新（discuss-phase 15，2026-06-01）：** Phase 15 同时交付 **FILE-06 图片上传附件**（从 Phase 17 前移）——「看图」两个来源（agent 取当前文档选中图 + 用户上传/粘贴图）统一在本阶段走 aihubmix-vision、返回文本作 evidence。做法 = 激活 InputBar 现有禁用回形针（**仅图片**：png/jpg/webp、多张、粘贴、本会话内存态复用、绝不持久化）；Phase 17 再把回形针推广到 docx/xlsx/pdf/pptx 解析。

### IMG — 图片生成插入（MM-03，仅 PPT + Word）

- [x] **IMG-01**: PowerPoint「生成一张图并插入」write tool — 接 MDL-01 生图，插入到当前 slide；reverse 复用已有 `deleteShapeById`（v2.1 add_shape 的 inverse）+ humanLabel — ✓ Phase 16（`generate_ppt_image`，`addImageShape` GA 路线 + 独立 run 回读规避 bug #5022）
- [x] **IMG-02**: Word「生成一张图并插入」write tool — `body.insertInlinePictureFromBase64`（body 级，非 range，Web 已知 bug）；reverse 用 `noop_inverse` 并诚实标注「Word 图片插入暂不支持自动撤销」— ✓ Phase 16（body 级规避 range 级 bug #3434）
- [x] **IMG-03**: 生成结果 **AI 自动插入文档**（agent loop 内直插，非人工确认）+ 聊天**只读结果缩略图**展示 + 生成中 loading 态 + 插错靠撤销(PPT delete_shape_by_id)/手动删(Word noop_inverse)兜底 — ✓ Phase 16（**设计反转 2026-06-02 用户拍板**：原「预览后确认再插入」→ AI 自动直插，确认卡打断 AI 自主排版 loop，与 AI 自动化愿景冲突；返回 shape_id 让 AI 继续 move_shape/set_shape_property 自主排版）
- [x] **IMG-04**: 生图 model 可选（默认 doubao-seedream-5.0-lite，几秒满足 P95）—— Settings 生图 model picker + 工具 model_id 参数（对话式指定）；「重新生成」改对话式 — ✓ Phase 16（三级优先级：args.model_id > storage PREF > registry 默认）
- [x] **IMG-05**: Excel 明确 out-of-scope（无原生插图 API）——在工具/文案层诚实表达，不假装支持 — ✓ Phase 16

### FILE — 文件上传与解析（MM-02，全五类）

- [x] **FILE-01**: chat 附件上传 UI（📎 入口 + 「参考文件」文案 + 附件 chip 标「仅供 AI 阅读」）— ✓ Phase 17
- [x] **FILE-02**: docx 附件解析为文本（mammoth ≥1.11.0，懒加载；CVE-2025-11849 版本锁 + npm audit gate）— ✓ Phase 17
- [x] **FILE-03**: xlsx 附件解析为文本/JSON（SheetJS 0.20.3，从 cdn.sheetjs.com，懒加载）— ✓ Phase 17
- [x] **FILE-04**: pdf 附件解析为文本（pdfjs-dist 5.7.x，懒加载，worker 独立文件）— ✓ Phase 17（worker fallback：`public/pdf.worker.min.mjs` + 静态路径 `/Aster/`，Vite 7 + GitHub Pages CSP 真机 PASS）
- [x] **FILE-05**: pptx 附件解析为文本（jszip + 原生 DOMParser 提 `<a:t>`，懒加载；text-only 不保真）— ✓ Phase 17
- [x] **FILE-06**: 图片附件 → 走 aihubmix-vision（与 VIS-01 互补：附件=用户主动传外部图，VIS=agent 取当前文档内图）— ✓ **交付于 Phase 15**（与 VIS 同属视觉；discuss-phase 15 决定前移）
- [x] **FILE-07**: 明确「附件上传」vs「agent 自取当前文档」UX 边界 — 附件=只读快照、不可写回；自取=live、可写回；附件内容注入为 augmented user prompt（沿用 sanitize 注入边界），不改 chatStore Message schema — ✓ Phase 17

### LIB — 公开图库检索（MM-04，Pexels + BYO key）

- [x] **LIB-01**: Pexels 检索 — Settings 新增 BYO Pexels API key 字段（不内置，符合 BYO/无后台/开源原则）；native fetch + `Authorization` header + `locale=zh-CN` — ✓ Phase 18
- [x] **LIB-02**: 检索结果缩略图网格 → 选中插入（复用 IMG 的 insert helper：PPT + Word）— ✓ Phase 18（复用 `insertImage.ts` helper）
- [x] **LIB-03**: chat 内显示 Pexels 摄影师署名 + 链接（满足授权；不在插入的图片上叠水印，保 slide 视觉）— ✓ Phase 18

### NFR — 非功能（延续 + 新增）

- [x] **NFR-09**: base64 图片字节**永不**写入 persisted 聊天历史（localStorage 配额防护 + LLM 重放死循环防护）——设计契约，加 serialize-test 守门 — ✓ Phase 15（serialize 守门用例通过）
- [x] **NFR-10**: 全部解析库 + 图库走 lazy-load / native fetch，初始 bundle 0 增量，维持 ≤82KB gzip CI gate；P95≤10s / Key 不上传 / undo 守门延续 — ✓ Phase 17（4 解析库全懒加载，初始 bundle 80.53 KB ≤82 KB）

---

## Future Requirements（v2.2 不做，已识别 — carry forward）

### 图库 / 视觉增强
- **LIB-D1**: Unsplash 备选接入（若 Pexels 中文质量/限额不足再评估）
- **VIS-D1**: DeepSeek-V4 原生多模态验证（原 Q6；v2.2 跳过，未来扩用户/降本时重评）

### 生图增强
- **IMG-D1**: 多变体并排生成（4 张选 1）——early-user 阶段不做
- **IMG-D2**: 图片编辑/局部重绘（image edit endpoint）

### 文件增强
- **FILE-D1**: pptx 高保真解析（含版式/图）——v2.2 仅 text-only

## Out of Scope

| Feature | Reason |
|---------|--------|
| Excel 内插入图片 | Office for Web 无原生 Excel 插图 API（`addPicture` BETA 不可用于生产） |
| 内置共享 Pexels key | 开源仓库硬编码 key 必被爬走滥用/封号，且违反无后台/BYO 原则（用户决定 BYO） |
| 图片数据进聊天历史持久化 | localStorage 配额炸 + LLM 重放死循环（已知 chatbot 中毒模式）；NFR-09 反向约束 |
| Cloudflare Worker 图片代理 | 维持无后台；Phase 19 真机实测 Pexels 双重 CORS（检索面 + M-1 取图面）均放行——`images.pexels.com` CDN 返回 ACAO，**M-1 未坐实、无需 Worker**（见 memory `project_no_backend_status`） |
| 视频 / 音频多模态 | 超出 v2.2 四件套范围 |
| Vercel/OpenAI 等 LLM SDK | OpenAI 兼容 wire 格式手写即可，0 净新增运行时依赖原则 |

## Traceability — 全部 Complete ✓

| Requirement | Phase | Status |
|-------------|-------|--------|
| MDL-01 | Phase 14 | Complete |
| MDL-02 | Phase 14 | Complete |
| MDL-03 | Phase 14 | Complete |
| VIS-01 | Phase 15 | Complete |
| VIS-02 | Phase 15 | Complete |
| NFR-09 | Phase 15 | Complete |
| IMG-01 | Phase 16 | Complete |
| IMG-02 | Phase 16 | Complete |
| IMG-03 | Phase 16 | Complete |
| IMG-04 | Phase 16 | Complete |
| IMG-05 | Phase 16 | Complete |
| FILE-01 | Phase 17 | Complete |
| FILE-02 | Phase 17 | Complete |
| FILE-03 | Phase 17 | Complete |
| FILE-04 | Phase 17 | Complete |
| FILE-05 | Phase 17 | Complete |
| FILE-06 | Phase 15 | Complete |
| FILE-07 | Phase 17 | Complete |
| NFR-10 | Phase 17 | Complete |
| LIB-01 | Phase 18 | Complete |
| LIB-02 | Phase 18 | Complete |
| LIB-03 | Phase 18 | Complete |

> Phase 19（UAT + Release）覆盖全部 22 需求的真机验证，不单独映射独立需求。

**Coverage:**
- v2.2 requirements: 22 total
- Mapped to phases: 22 ✓（Phase 14:3 / 15:4 / 16:5 / 17:7 / 18:3）
- Unmapped: 0 ✓
- **Delivered: 22/22 ✓**（code-level 验证 + Phase 19 三宿主真机 UAT 全 PASS）

## Requirements outcome（收官核对）

**22/22 全部交付。** 收官修正 REQUIREMENTS 溯源表 stale 记账：**LIB-01/02/03**（Pexels 检索 / 缩略图网格插入 / 摄影师署名）在 Phase 18 已交付（3/3 plans，2026-06-03，code-review 无 HIGH）+ Phase 19 真机 UAT「Pexels 双重 CORS 均放行」PASS，但 REQUIREMENTS.md 复选框与溯源表残留 `[ ]` Pending（同 v2.0 7 项 / v2.1 UI-04·UI-06 的 GSD `phase.complete` stale-checkbox quirk，见 memory `project_gsd_tooling_quirks`）；收官时已逐项核验修正为 Complete。

**真机 UAT 暴露并修复的两个高危（Phase 19）：**
- **HR-1 pdf.js worker CSP** — worker 在 GitHub Pages base + Office iframe CSP 下加载成功（`public/pdf.worker.min.mjs` 静态路径 `/Aster/`）
- **HR-2 Pexels 双重 CORS（检索面 + M-1 取图面）** — 均放行；`images.pexels.com` CDN 返回 ACAO，**M-1「取图 full-res CORS 缺口」未坐实、无需 Cloudflare Worker 兜底**

**设计反转（IMG-03，2026-06-02 用户拍板）：** 原「预览后确认再插入」(D-01/02/03) → AI 自动直插——确认卡打断 AI 自主排版 loop，与「AI 自动化操作」愿景及既有「无授权 UX/信任 agent」哲学冲突。

---
*Requirements defined: 2026-06-01*
*Archived: 2026-06-03 — v2.2 milestone close（22/22 交付，三宿主真机 UAT 全 PASS）*

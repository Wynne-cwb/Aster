---
phase: 24-a-p2-bundle
plan: "04"
type: execute
wave: 4
depends_on:
  - "24-03"
files_modified:
  - src/components/SlidePreviewPanel.tsx
  - src/components/ChatStream.tsx
  - src/styles.css
  - src/i18n/locales/zh-CN/messages.po
  - .planning/phases/24-a-p2-bundle/24-UAT-PACKET.md
autonomous: false
requirements:
  - PVQ-06
  - NFR-11

user_setup: []

must_haves:
  truths:
    - "SlidePreviewPanel.tsx 存在，React.lazy 懒加载，不进 main chunk"
    - "ChatStream.tsx 含 SlidePreviewPanel 的 lazy 声明 + ToolResultCard 内 Suspense 条件挂载"
    - "layoutShapes 由 apply_slide_layout tool-call args（{layout,content,accent_color}）经 buildLayout 重建 ShapeSpec[]，不改 write/ppt.ts，不从 toolResult.data 读 shapes"
    - "styles.css 含 .slide-preview-panel / .slide-preview-container CSS 类（来自 UI-SPEC.md）"
    - "所有面板文案用 Lingui Trans 宏，npm run extract 已运行（写入 src/i18n/locales/zh-CN/messages.po）"
    - "npm run build && npm run size 通过：main-*.js ≤82KB gzip（独立 stop-if-fail 检查点）"
    - "UAT-PACKET.md 含保真度对比图采集步骤（spike-gate 人眼判定交付物）"
    - "989 existing tests 全绿（含 undo 守门 operationLog.integration.test）"
    - "P95 不退化：截图在离散事件/本地 DOM 层，非 LLM 流式热路径，plan 中有论证"
  artifacts:
    - path: "src/components/SlidePreviewPanel.tsx"
      provides: "幻灯片自渲染预览面板（React 组件，teal 克制设计）"
      min_lines: 80
    - path: "src/styles.css"
      provides: "SlidePreviewPanel CSS 类定义（Phase 24 新增区块）"
      contains: ".slide-preview-panel"
    - path: ".planning/phases/24-a-p2-bundle/24-UAT-PACKET.md"
      provides: "UAT 对比图采集步骤（spike-gate 人眼判定交付物，LOCKED-1）"
      contains: "PowerPoint 真机截图"
  key_links:
    - from: "src/components/ChatStream.tsx"
      to: "src/components/SlidePreviewPanel.tsx"
      via: "React.lazy + Suspense 条件渲染"
      pattern: "lazy.*SlidePreviewPanel"
    - from: "src/components/ChatStream.tsx"
      to: "src/agent/design/ppt-layouts.ts"
      via: "buildLayout 从 tool-call args 重建 ShapeSpec[]"
      pattern: "buildLayout"
    - from: "src/components/SlidePreviewPanel.tsx"
      to: "src/agent/tools/read/visual-check.ts"
      via: "registerPreviewElement 注入 DOM ref"
      pattern: "registerPreviewElement"
    - from: "src/components/SlidePreviewPanel.tsx"
      to: "src/agent/design/slide-preview.ts"
      via: "mapShapesToRender"
      pattern: "mapShapesToRender"
---

<objective>
本 plan 完成 Phase 24 的收尾：（1）SlidePreviewPanel UI 组件（teal 克制设计，React.lazy 懒加载）；（2）ChatStream.tsx 接线（lazy 声明 + ToolResultCard 内 layoutShapes 重建 + Suspense 挂载）+ Lingui extract；（3）bundle gate（独立 stop-if-fail 检查点）+ UAT 对比图采集包（spike-gate 人眼判定交付物，LOCKED-1）+ 全套回归。

Purpose: 把前三个 plan 的所有实现接通为可见产品，同时为用户提供 UAT 证据采集指南，让用户能够人眼判定「铺开 or 降级」。

Output:
- src/components/SlidePreviewPanel.tsx（可见预览面板，LOCKED-3c visible 默认）
- src/styles.css（SlidePreviewPanel CSS 类块，来自 UI-SPEC.md）
- src/components/ChatStream.tsx（lazy + ToolResultCard 内条件挂载，仿 imageResult 范式）
- src/i18n/locales/zh-CN/messages.po（npm run extract 更新）
- .planning/phases/24-a-p2-bundle/24-UAT-PACKET.md（保真度 spike-gate 采集步骤）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-a-p2-bundle/24-CONTEXT.md
@.planning/phases/24-a-p2-bundle/24-UI-SPEC.md
@.planning/phases/24-a-p2-bundle/24-PATTERNS.md
@.planning/phases/24-a-p2-bundle/24-03-SUMMARY.md

<interfaces>
<!-- 来自 ChatStream.tsx（analog：ToolResultCard 内 imageResult 提取 + Suspense 挂载）-->
```typescript
// ChatStream.tsx 顶部 lazy 声明（约 L45-57）：
const DiffLogPanel = lazy(() => import('./DiffLogPanel'));
const ImagePreviewCard = lazy(() =>
  import('./ImagePreviewCard').then((m) => ({ default: m.ImagePreviewCard }))
);

// ToolResultCard 内 imageResult 提取（约 L121-129，per-message 局部）：
const imageResult = ((): { base64: string; mimeType: string } | null => {
  if (!message.toolResult?.ok || typeof message.toolResult.data !== 'object' || !message.toolResult.data) return null;
  const d = message.toolResult.data as Record<string, unknown>;
  if (d.inserted !== true || typeof d.thumbnail !== 'string') return null;
  return { base64: d.thumbnail, mimeType: (d.mimeType as string) ?? 'image/png' };
})();

// ToolResultCard 内 Suspense 条件挂载（约 L243-251，在 </> 之前）：
{imageResult && (
  <Suspense fallback={null}>
    <ImagePreviewCard base64={imageResult.base64} mimeType={imageResult.mimeType} host={host} />
  </Suspense>
)}

// 跨消息查找 toolCall 的范式（CIRCUIT_OPEN 卡已有先例，L173-182）：
const msgs = useChatStore.getState().messages;
const found = msgs.find((m) => ...);
```

<!-- 来自 store/chat.ts（Message + ToolCall 类型 — 已核实）-->
```typescript
// assistant message 有 toolCalls?: ToolCall[]；tool message 有 toolName/toolResult/toolCallId
export interface ToolCall {
  id: string;
  name: string;          // 'apply_slide_layout' 等
  arguments: unknown;    // ⚠️ 已是对象（非 JSON 字符串），直接读字段，不 JSON.parse
  status?: 'pending' | 'accepted' | 'rejected';
}
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'tool' | 'error';
  content: string;
  toolCalls?: ToolCall[];  // assistant role
  toolCallId?: string;     // tool role：指向触发它的 toolCall.id
  toolName?: string;       // tool role：'apply_slide_layout' 等
  toolResult?: ToolResult; // tool role
  // ...
}
```

<!-- 来自 ppt-layouts.ts（buildLayout 签名 — 已核实）-->
```typescript
// buildLayout(layout, content, { accent }, canvas?) → LayoutResult{shapes:ShapeSpec[], imageSlots, capNotes}
// 与 apply_slide_layout 工具内部用的是同一纯函数（src/agent/tools/write/ppt.ts L749 也调它）
export function buildLayout(
  layout: LayoutName,
  content: Record<string, unknown>,
  colors?: { accent?: string },
  canvas?: Canvas,
): LayoutResult;
export interface LayoutResult { shapes: ShapeSpec[]; imageSlots: ...; capNotes: string[]; }
```

<!-- 来自 write/ppt.ts apply_slide_layout（已核实 — ToolResult.data 不含 shapes）-->
```typescript
// apply_slide_layout args = { layout: LayoutName, content: Record<string,unknown>, accent_color?: string }
// ToolResult.data = { slide_index, new_shape_ids, image_slots, cap_notes, layout_check }
//   ⚠️ data 不含 ShapeSpec 几何 → 绝不从 toolResult.data 读 shapes（读不到）
//   ✅ 从 tool-call args 经 buildLayout 重建（同一纯函数，单一真相源，0 改工具层，0 token 膨胀）
```

<!-- 来自 slide-preview.ts（Plan 24-02 实现）-->
```typescript
export function mapShapesToRender(shapes: ShapeSpec[], containerWidthPx: number): SlideRenderShape[];
```

<!-- 来自 visual-check.ts（Plan 24-03 实现）-->
```typescript
export function registerPreviewElement(getter: () => HTMLElement | null): void;
```

<!-- 来自 UI-SPEC.md §新 CSS 类名约定（完整 CSS 块）-->
```css
.slide-preview-panel { display:flex; flex-direction:column; gap:0; margin-top:var(--space-2);
  border:1px solid var(--border); border-radius:var(--radius-2); background:var(--bg);
  overflow:hidden; flex-shrink:0; min-width:0; }
.slide-preview-panel__header { display:flex; align-items:center; gap:var(--space-2);
  padding:var(--space-1) var(--space-2) var(--space-1) var(--space-3);
  height:28px; border-bottom:1px solid var(--border); background:var(--surface-2); flex-shrink:0; }
.slide-preview-panel__title { font-size:var(--fs-12); font-weight:500; color:var(--text-2);
  flex:1; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.slide-preview-panel__status { font-size:var(--fs-11); color:var(--text-3); white-space:nowrap; flex-shrink:0; }
.slide-preview-panel__status--error { color:var(--error); }
.slide-preview-container { position:relative; width:100%; background:#ffffff; overflow:hidden; }
```
</interfaces>

<!-- P95 不退化论证（写入 plan，executor 须理解并在 must_haves/verification 引用）-->
<!--
截图路径（visual_check_slide 触发时序）：
  apply_slide_layout（write tool 完成）
    → AI 收到 layout_check evidence（几何自查已在 apply_slide_layout 内部跑）
    → AI 决定调 visual_check_slide（on-demand，不是 auto）
    → execute()：本地 DOM html2canvas 截图（< 200ms 本地 canvas）
    → analyzeImages vision API（~2-5s，与现有 get_shape_image 同量级，已在既有 P95 预算内的 tool 延迟）
    → 文字 evidence 进入下一轮

P95 = 用户看到首个 AI token（首 token ≤2s 硬约束）。
截图在 agent loop 中间步骤，不在首 token 路径上。
渲染是离散事件（出一页 layout 时），不在 LLM 流式逐 token 热路径。
预览面板用 buildLayout 重建 shapes（纯函数，本地），无网络、无 Office.js round-trip。
→ 截图不退化 P95。
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1：SlidePreviewPanel UI 组件 + styles.css 新增 CSS 类</name>
  <files>
    src/components/SlidePreviewPanel.tsx,
    src/styles.css
  </files>
  <read_first>
    - src/styles.css（确认 CSS 变量名：--space-1/2/3、--border、--bg、--surface-2、--text-2、--text-3、--fs-12、--fs-11、--radius-2、--error；找到已有组件类末尾位置追加新块）
    - src/components/ChatStream.tsx（了解 ToolResultCard + imageResult 提取范式，约 L110-129）
    - src/components/DiffLogPanel.tsx 或 ImagePreviewCard.tsx（analog：React.lazy 组件的 props 定义风格）
    - src/components/icons.tsx（XIcon 是否已存在；若无，确认 icon 命名规范）
    - src/App.tsx（Lingui import 范式：Trans, useLingui from '@lingui/react/macro'）
    - .planning/phases/24-a-p2-bundle/24-UI-SPEC.md（完整 CSS 规格 + 交互状态机 + 文案契约）
  </read_first>
  <action>
**执行前先 `Skill("aster-design-system")`**（构建 UI 必加载，teal 克制 token / 反模式）。

**A. 在 src/styles.css 末尾追加新 CSS 块（Phase 24 SlidePreviewPanel）**

严格按 24-UI-SPEC.md §新 CSS 类名约定 的 CSS 写入。注意：
- 全部颜色走 CSS 变量（--bg / --surface-2 / --border / --text-2 / --text-3 / --error / --accent / --ring-focus）
- `.slide-preview-container` 的 height 不写固定值（由 React inline style 动态设定）
- 先检查 styles.css 有哪些已有 --space-N / --radius-N 变量名（不同 phase 可能叫 --space-1 或 --sp-1）；根据实际文件使用正确命名，不照搬 UI-SPEC 的猜测值

```css
/* ============================================================
   Phase 24: SlidePreviewPanel — 幻灯片自渲染预览面板
   ============================================================ */

.slide-preview-panel {
  display: flex;
  flex-direction: column;
  gap: 0;
  margin-top: var(--space-2);     /* 若变量名不同，按实际修正 */
  border: 1px solid var(--border);
  border-radius: var(--radius-2); /* 若变量名不同，按实际修正 */
  background: var(--bg);
  overflow: hidden;
  flex-shrink: 0;   /* 防被 flex column 压扁，与 .tool-group 同 */
  min-width: 0;     /* 350px 窄面板兜底 */
}

.slide-preview-panel__header {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-1) var(--space-2) var(--space-1) var(--space-3);
  height: 28px;
  border-bottom: 1px solid var(--border);
  background: var(--surface-2);
  flex-shrink: 0;
}

.slide-preview-panel__title {
  font-size: var(--fs-12);
  font-weight: 500;
  color: var(--text-2);
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.slide-preview-panel__status {
  font-size: var(--fs-11);
  color: var(--text-3);
  white-space: nowrap;
  flex-shrink: 0;
}

.slide-preview-panel__status--error {
  color: var(--error);
}

.slide-preview-container {
  /* 16:9 渲染容器：position relative；子 div 绝对定位 */
  position: relative;
  width: 100%;
  /* height 由 React inline style 动态设定：containerWidth * (540/960) */
  background: #ffffff; /* 幻灯片底色固定白，与主题 token 无关 */
  overflow: hidden;
}
```

**B. 创建 src/components/SlidePreviewPanel.tsx**

要求（按 UI-SPEC.md 契约）：
- `export default function SlidePreviewPanel` — default export（供 React.lazy）
- Props: `{ shapes: ShapeSpec[]; }`
- useRef 持有 `.slide-preview-container` DOM ref（containerElRef）
- useLayoutEffect 挂载时调 `registerPreviewElement(() => containerElRef.current)`，卸载时调 `registerPreviewElement(() => null)`
- useState: `containerWidth`（默认 318px）
- ResizeObserver 监听容器宽度变化，更新 containerWidth
- useMemo 调 `mapShapesToRender(shapes, containerWidth)` 缓存渲染结果
- 预览容器 height = `Math.round(containerWidth * (540 / 960))`（16:9）
- 文案全用 `<Trans>` 宏（Lingui）：「幻灯片预览」
- 无障碍：容器 `role="img"` + `aria-label="幻灯片预览"`，标题用 `<span role="heading" aria-level={3}>`

```typescript
import React, { useLayoutEffect, useMemo, useRef, useState } from 'react';
import { Trans } from '@lingui/react/macro';
import { mapShapesToRender } from '../agent/design/slide-preview';
import { registerPreviewElement } from '../agent/tools/read/visual-check';
import type { ShapeSpec } from '../agent/design/ppt-layouts';

interface Props {
  shapes: ShapeSpec[];
}

export default function SlidePreviewPanel({ shapes }: Props) {
  const containerElRef = useRef<HTMLDivElement>(null);
  const [containerWidth, setContainerWidth] = useState(318); // 默认 318px（350 - 2×16 padding）

  // 挂载时注册 previewEl getter，供 visual_check_slide 工具截图
  useLayoutEffect(() => {
    registerPreviewElement(() => containerElRef.current);
    return () => registerPreviewElement(() => null);
  }, []);

  // 监听容器宽度（ResizeObserver）
  useLayoutEffect(() => {
    const el = containerElRef.current;
    if (!el) return;
    const observer = new ResizeObserver((entries) => {
      const w = entries[0]?.contentRect.width;
      if (w && w > 0) setContainerWidth(w);
    });
    observer.observe(el.parentElement ?? el);
    return () => observer.disconnect();
  }, []);

  const renderedShapes = useMemo(
    () => mapShapesToRender(shapes, containerWidth),
    [shapes, containerWidth],
  );
  const previewHeight = Math.round(containerWidth * (540 / 960));

  return (
    <div className="slide-preview-panel">
      <div className="slide-preview-panel__header">
        <span
          className="slide-preview-panel__title"
          role="heading"
          aria-level={3}
        >
          <Trans>幻灯片预览</Trans>
        </span>
      </div>
      <div
        ref={containerElRef}
        className="slide-preview-container"
        role="img"
        aria-label="幻灯片预览"
        style={{ height: previewHeight }}
      >
        {renderedShapes.map((s) => (
          <div key={s.key} style={s.style as React.CSSProperties}>
            {s.text}
          </div>
        ))}
      </div>
    </div>
  );
}
```

注意：
- `s.style as React.CSSProperties` 是因为 SlideRenderShape.style 是精确字面量类型，React.CSSProperties 兼容接受
- html2canvas 不在面板内 import（截图由工具层 visual_check_slide 触发）
- 颜色物理隔离：面板 chrome 用 CSS 变量，形状内容颜色用 ShapeSpec hex（不用 --accent / --bg 给幻灯片内容着色）
  </action>
  <verify>
    <automated>tsc --noEmit 2>&1 | grep -c "error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "slide-preview-panel" src/styles.css` ≥ 3（Panel CSS 类已写入）
    - `grep "registerPreviewElement" src/components/SlidePreviewPanel.tsx` 命中（previewEl 注入）
    - `grep "mapShapesToRender" src/components/SlidePreviewPanel.tsx` 命中（渲染器调用）
    - `grep "Trans" src/components/SlidePreviewPanel.tsx | wc -l` ≥ 1（Lingui 宏已用）
    - `grep "html2canvas" src/components/SlidePreviewPanel.tsx | wc -l` == 0（面板内无 html2canvas import，截图由工具层负责）
    - `grep "backdrop-filter\|linear-gradient" src/styles.css | grep "slide-preview" | wc -l` == 0（零玻璃拟态/渐变，teal 克制规则）
    - `tsc --noEmit 2>&1 | grep -c "error"` == 0
  </acceptance_criteria>
  <done>SlidePreviewPanel.tsx 和 CSS 类已创建，teal 克制设计，registerPreviewElement 已注入，tsc 零错误</done>
</task>

<task type="auto">
  <name>Task 2：ChatStream.tsx 接线（layoutShapes 经 buildLayout 重建）+ Lingui extract</name>
  <files>
    src/components/ChatStream.tsx,
    src/i18n/locales/zh-CN/messages.po
  </files>
  <read_first>
    - src/components/ChatStream.tsx（全文：L35-57 lazy 声明块；L110-129 ToolResultCard 内 imageResult 提取范式；L173-182 跨消息 useChatStore.getState().messages.find 先例；L243-263 ToolResultCard 内 Suspense 挂载区域）
    - src/store/chat.ts（Message + ToolCall 类型：assistant.toolCalls[].arguments 已是对象非 JSON 字符串；tool message 的 toolName/toolCallId）
    - src/agent/design/ppt-layouts.ts（buildLayout 签名：buildLayout(layout, content, { accent }) → {shapes}；LayoutName / ShapeSpec 类型）
    - src/agent/tools/write/ppt.ts（apply_slide_layout args = {layout, content, accent_color}；确认 ToolResult.data 不含 shapes）
    - src/i18n/locales/zh-CN/messages.po（现有 msgid/msgstr 格式；⚠️ 路径是 src/i18n/locales/，非 src/locales/）
  </read_first>
  <action>
**A. ChatStream.tsx 三处改动**

改动 1 — lazy 声明块（约 L45-57，紧随 DiffLogPanel 之后）：
```typescript
const SlidePreviewPanel = lazy(() => import('./SlidePreviewPanel'));
```

改动 2 — 在 ToolResultCard 内提取 layoutShapes（仿 imageResult 提取，约 L121-129 同级，per-message 局部）。

**唯一确定路径（plan-checker 已核实）：从 apply_slide_layout 的 tool-call args 经 buildLayout 重建 ShapeSpec[]。**

依据：
- `apply_slide_layout` 的 `ToolResult.data` **只含** `{slide_index, new_shape_ids, image_slots, cap_notes, layout_check}`——**不含 ShapeSpec 几何**（已核实 src/agent/tools/write/ppt.ts L769-780）→ 绝不从 toolResult.data 读 shapes（读不到）。
- assistant message 的 `toolCalls[]`（chat.ts）带 `{id, name, arguments}`，`arguments` **已是对象**（`unknown` 类型，非 JSON 字符串）含 `{layout, content, accent_color}`。
- `buildLayout(layout, content ?? {}, { accent: accent_color })`（ppt-layouts.ts，与工具层同一纯函数）重建 `{shapes}` → ShapeSpec[]。
- 优点：0 改 write/ppt.ts（不动 Phase 23）、0 LLM token 膨胀（args 本就在 message）、单一真相源。

ToolResultCard 内提取代码（紧随 imageResult / stockResult 之后）：

```typescript
// Phase 24 PVQ-06：当本 tool message 是 apply_slide_layout 结果时，
// 从触发它的 tool-call args（{layout, content, accent_color}）经 buildLayout 重建 ShapeSpec[] 供预览。
// ⚠️ 不从 toolResult.data 读 shapes（data 不含几何，已核实）；buildLayout 是工具层同一纯函数（单一真相源）。
const layoutShapes = ((): ShapeSpec[] | null => {
  if (message.toolName !== 'apply_slide_layout' || !message.toolResult?.ok) return null;
  // 跨消息找触发本 tool 的 assistant toolCall（仿 L173-182 CIRCUIT_OPEN 卡的 messages.find 先例）
  const msgs = useChatStore.getState().messages;
  let args: { layout?: string; content?: Record<string, unknown>; accent_color?: string } | null = null;
  for (const m of msgs) {
    if (m.role !== 'assistant' || !m.toolCalls) continue;
    const tc = m.toolCalls.find((c) => c.id === message.toolCallId && c.name === 'apply_slide_layout');
    if (tc) { args = tc.arguments as typeof args; break; } // arguments 已是对象，直接取字段，不 JSON.parse
  }
  if (!args?.layout) return null;
  try {
    const { shapes } = buildLayout(args.layout as LayoutName, args.content ?? {}, { accent: args.accent_color });
    return shapes.length > 0 ? shapes : null;
  } catch {
    return null; // 重建失败（未知 layout 等）→ 不显示预览，不崩溃
  }
})();
```

顶部 import 补充：
```typescript
import { buildLayout, type LayoutName } from '../agent/design/ppt-layouts';
import type { ShapeSpec } from '../agent/design/ppt-layouts';
```
（若 ChatStream 已 import ShapeSpec/LayoutName，去重即可。）

改动 3 — Suspense 条件挂载（ToolResultCard 内，imageResult/stockResult 挂载块附近，`</>` 之前，约 L263）：
```typescript
{layoutShapes && (
  <Suspense fallback={null}>
    <SlidePreviewPanel shapes={layoutShapes} />
  </Suspense>
)}
```

注意：
- `buildLayout` 被 ChatStream（在 main/react chunk 链上）静态 import 会把 buildLayout 拉进相应 chunk——但 buildLayout 是纯函数（无 html2canvas、无 React），体积极小，且 SlidePreviewPanel 本身 React.lazy（其 chunk 内的 mapShapesToRender 不进 main）。html2canvas 仅在 24-03 工具 execute() 内动态 import，与本文件无关。bundle gate（Task 3）会最终确认 main ≤82KB。
- `message.toolName` 来自 chat.ts 的 tool message 字段（loop-helpers push tool 消息时填 `toolName: tc.name`）。

**B. 运行 npm run extract 同步 Lingui catalog**

```bash
npm run extract
```

这会把 SlidePreviewPanel.tsx 中 `<Trans>幻灯片预览</Trans>` 等新宏写入 **`src/i18n/locales/zh-CN/messages.po`**（lingui.config.ts catalogs.path = `src/i18n/locales/{locale}/messages`；其他 locale 同步）。
  </action>
  <verify>
    <automated>npm run extract && tsc --noEmit && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SlidePreviewPanel" src/components/ChatStream.tsx | grep "lazy\|Suspense"` 命中（已接入 lazy + Suspense）
    - `grep "buildLayout" src/components/ChatStream.tsx` 命中（layoutShapes 经 buildLayout 重建）
    - `grep "toolResult.data" src/components/ChatStream.tsx | grep -i "shapes" | wc -l` == 0（绝不从 toolResult.data 读 shapes）
    - `grep "JSON.parse" src/components/ChatStream.tsx | grep -i "arguments\|tc\." | wc -l` == 0（arguments 已是对象，不 JSON.parse）
    - `npm run extract 2>&1 | grep -i "error"` 空（Lingui extract 无错误）
    - `grep "幻灯片预览" src/i18n/locales/zh-CN/messages.po` 命中（新文案已写入正确路径 catalog）
    - `tsc --noEmit 2>&1 | grep -c "error"` == 0
    - `tsc --noEmit && vitest run 2>&1 | grep "passed"` ≥ 994，0 failed
    - `vitest run 2>&1 | grep "operationLog.integration"` 绿（undo 守门未破坏）
  </acceptance_criteria>
  <done>
ChatStream 接线完成（layoutShapes 经 buildLayout 从 tool-call args 重建）；Lingui extract 已运行（写入 src/i18n/locales/zh-CN/messages.po）；全套测试全绿、undo 守门绿。
</done>
</task>

<task type="auto">
  <name>Task 3：bundle gate（stop-if-fail）+ UAT 对比包 + 全套回归</name>
  <files>
    .planning/phases/24-a-p2-bundle/24-UAT-PACKET.md
  </files>
  <read_first>
    - .size-limit.json（确认 "path": "dist/assets/main-*.js"，limit 82KB）
    - package.json（确认 scripts：build=lingui compile && vite build，size=size-limit）
    - 24-CONTEXT.md §HARD constraint #1（bundle 铁律：先 build 再 size，陈旧 dist 给假绿）
    - 24-VALIDATION.md §Manual-Only（保真度人眼判定不写自动断言，LOCKED-1）
  </read_first>
  <action>
**C. bundle gate（独立 stop-if-fail 检查点 — 必须先于 D 跑）**

按「先 build 再 size」铁律（memory `project_bundle_size_guard`，陈旧 dist 给假绿）执行：

```bash
npm run build && npm run size
```

预期：`dist/assets/main-*.js` ≤82KB gzip（当前 80.6KB，本 phase 0 净初始增量——html2canvas 仅工具 execute() 内动态 import、SlidePreviewPanel React.lazy）。

**stop-if-fail 处理（关键检查点，不通过则止步排查，不进 D）：**
- 若 `npm run build` 失败：先 `tsc --noEmit` 诊断类型错误并修复，重跑 build。
- 若 `npm run size` 报超标（main-*.js > 82KB）：**止步**。检查 `npm run build` 输出的 chunk 清单：
  - 确认 SlidePreviewPanel 进了独立懒加载 chunk（非 main）——若进 main，检查 ChatStream 是否误用 static import（应为 `lazy(() => import('./SlidePreviewPanel'))`）。
  - 确认 html2canvas 进独立 chunk（24-03 工具 execute() 内 `await import`）——若进 main 或 loop chunk 初始链，检查 visual-check.ts 是否有顶层 static import html2canvas。
  - buildLayout 被 ChatStream 静态 import 是预期（纯函数小体积）；若意外膨胀，用 vite-bundle-visualizer 定位。
  - 排查修复后重跑 `npm run build && npm run size`，直到 main ≤82KB 才进 D。

**D. 创建 UAT-PACKET.md（spike-gate 人眼判定交付物，LOCKED-1 — 非自动断言）**

在 `.planning/phases/24-a-p2-bundle/24-UAT-PACKET.md` 创建以下内容（完整采集指南）：

```markdown
# Phase 24 UAT Packet — 保真度 Spike-Gate + 铺开路径验证

**Status:** 待 UAT（v2.3 末统一 UAT 包）
**Spike-gate verdict 方式:** 人眼判定（LOCKED-1，不可自动化）

---

## 1. 准备工作

- sideload 最新版 Aster（https://wynne-cwb.github.io/Aster/manifest.xml）
- 准备一个有若干文本的 Office for Web PPT 演示文稿
- 在 Settings 配置好 AiHubMix API Key（vision 自查需要）

---

## 2. 自动化 Gate（先跑，全绿才进人眼 UAT）

\`\`\`bash
# 全套测试回归
tsc --noEmit && vitest run
# 预期：≥ 994 passed，0 failed

# bundle gate（先 build 再 size，陈旧 dist 给假绿）
npm run build && npm run size
# 预期：dist/assets/main-*.js ≤ 82KB gzip

# undo 守门
npx vitest run operationLog.integration
# 预期：全绿
\`\`\`

---

## 3. Spike-Gate：保真度对比图采集步骤

**目标：** 判定自渲染预览与 PowerPoint 真机截图的版面粗粒度对应程度，给出「铺开 or 降级」结论。

### 步骤 A：触发 apply_slide_layout 使预览面板出现

1. 在 Aster chat 输入：「帮我生成一页 KPI 展示幻灯片，主题：2026 年 Q1 营收」
2. 等 AI 调用 \`apply_slide_layout\`，观察 Task Pane 出现「幻灯片预览」面板
3. 确认面板渲染了幻灯片形状（标题、数字框、说明文字等）

### 步骤 B：采集自渲染预览截图（Aster 生成）

使用 \`visual_check_slide\` 工具（AI 可主动调用，或手动触发）：
- 在 chat 输入：「请对刚刚生成的幻灯片做视觉自查」
- AI 会调用 \`visual_check_slide\`，工具内部 html2canvas 截图 → 喂 vision 模型
- 自查结果（文字 evidence）出现在 chat 中
- **同时用浏览器截图工具截取 Task Pane 中的「幻灯片预览」面板**（这是「自渲染预览截图」，即附图 1）

### 步骤 C：采集 PowerPoint 真机截图

1. 在 Office for Web PPT 中，切换到 apply_slide_layout 刚建好的幻灯片（最新一张）
2. 用浏览器截图工具（Snipping Tool / macOS Cmd+Shift+4）**截取整张幻灯片内容**（附图 2）

### 步骤 D：两图并排人眼对比

将附图 1（自渲染预览）和附图 2（PowerPoint 真机截图）并排，按四项评估：

| 评估项 | 判断问题 | 结论（填写） |
|--------|---------|------------|
| 溢出 | 自渲染判断文字溢出的位置，与 PPT 真机是否粗粒度一致？ | |
| 重叠 | 自渲染判断形状压叠的部位，与 PPT 真机是否粗粒度一致？ | |
| 留白 | 自渲染的空白分布，与 PPT 真机是否粗粒度一致？ | |
| 对比 | 自渲染的文字可读性判断，与 PPT 真机是否粗粒度一致？ | |

**判定标准（LOCKED-3a）：**
- ✅ 铺开：四项中多数粗粒度可辨认一致（字体差异导致的微小换行偏差不计入）
- ❌ 降级：偏差过大（如字体回退导致换行完全不同，溢出/留白判断完全相反）

**Spike-Gate 结论（请填写）：**
\`\`\`
[ ] 铺开（保真度够用）→ 保持 PVQ06_VISUAL_CHECK_ENABLED = true，无需操作
[ ] 降级（保真度不足）→ 改 PVQ06_VISUAL_CHECK_ENABLED = false；更新 REQUIREMENTS.md PVQ-06 状态；
    告知用户：「视觉自查保真度不足，已回落到几何规则自查（溢出/重叠/越界/对比）」
\`\`\`

---

## 4. 铺开路径端到端 UAT

（仅当 Spike-Gate 结论 = 铺开时验证）

| 场景 | 步骤 | 预期结果 |
|------|------|---------|
| visual_check_slide 正常调用 | 在 chat 触发「视觉自查」 | AI 收到视觉 evidence 文字，无 CSP 拦截，vision API 返回四项分析 |
| evidence 拼入下一轮 | AI 收到 evidence 后调 apply_slide_layout 修正 | AI 能根据视觉反馈修改 layout（能看出 AI 引用了 evidence） |
| html2canvas 不进初始 bundle | build + size | main-*.js ≤82KB gzip |
| previewEl 不存在时 advisory | 不打开预览面板直接调工具 | 返回「预览面板未打开，视觉自查跳过」advisory，不崩溃 |

---

## 5. 降级路径核实

（若 Spike-Gate 结论 = 降级，执行以下操作）

1. 将 \`src/agent/tools/visual-check-config.ts\` 中 \`PVQ06_VISUAL_CHECK_ENABLED\` 改为 \`false\`
2. 更新 \`.planning/REQUIREMENTS.md\` PVQ-06 状态：「降级：仅 Phase 22 几何自查（check_slide_layout）兜底」
3. 在聊天中告知用户：「视觉自查保真度不足（字体偏差导致 html2canvas 截图与 PowerPoint 版面差异过大），已回落到几何规则自查。几何自查（溢出/重叠/越界/对比度 WCAG）仍正常工作。」
4. 验证降级后 \`visual_check_slide\` 工具不出现在 AI 可用工具列表中
5. 验证 \`check_slide_layout\` 仍正常工作（几何自查兜底）

---

## 6. 3 个可调项最终值记录

（UAT 观察后填写，以下为当前默认值）

| 可调项 | 当前默认 | UAT 建议调整 |
|--------|---------|------------|
| 保真度门槛 | 人眼粗粒度可辨认（无数值 gate） | |
| 触发方式 | on-demand（AI 主动调工具） | |
| 渲染模式 | visible（面板可见） | |

---

## 7. 通用 UAT checklist（全 v2.3 phase 通用）

参见 v2.3 统一 UAT 包（本 packet 仅覆盖 Phase 24 专项）。

三宿主真机 UAT（Phase 24 只在 PPT 宿主验证，Excel/Word 无 apply_slide_layout 工具）：
- [x] Office for Web PPT（Chrome 最新版）— 本 phase 主要验证平台
- [ ] Office for Web PPT（Edge 最新版）— 可选，视时间
```

**E. 全套测试回归（收尾确认）**

```bash
tsc --noEmit && vitest run
```

预期：≥994 passed（989 既有 + ≥5 新测），0 failed。undo 守门 operationLog.integration 绿。
  </action>
  <verify>
    <automated>npm run build && npm run size 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit code 0（构建成功）
    - `npm run size 2>&1` exit code 0（main-*.js ≤82KB gzip；`echo $?` == 0）
    - `npm run size 2>&1 | grep -i "exceeded\|over budget\|fail" | wc -l` == 0（未超标）
    - `ls .planning/phases/24-a-p2-bundle/24-UAT-PACKET.md` 存在
    - `grep "PowerPoint 真机截图\|附图\|铺开\|降级" .planning/phases/24-a-p2-bundle/24-UAT-PACKET.md | wc -l` ≥ 4（UAT 对比图采集步骤完整）
    - `grep "人眼判定\|spike-gate\|LOCKED-1" .planning/phases/24-a-p2-bundle/24-UAT-PACKET.md | wc -l` ≥ 1（spike-gate 是人眼判定，非自动断言）
    - `tsc --noEmit && vitest run 2>&1 | grep "passed"` ≥ 994，0 failed
    - `vitest run 2>&1 | grep "operationLog.integration"` 绿（undo 守门未破坏）
  </acceptance_criteria>
  <done>
bundle gate 通过（stop-if-fail 检查点：main ≤82KB gzip）；UAT-PACKET.md 已创建含完整对比图采集步骤（人眼判定，非自动断言）；全套测试全绿、undo 守门绿。Phase 24 全部可自动化的工作完成。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SlidePreviewPanel（React 组件）→ visual-check.ts tool（通过 registerPreviewElement） | 组件挂载时注册 DOM ref getter，工具调用时读取；单向注册，无逆向访问 |
| LLM 输出（apply_slide_layout tool-call args）→ ChatStream buildLayout 重建 | AI 生成的 {layout,content,accent_color} 经 buildLayout 重建 ShapeSpec[] 用于本地渲染；颜色 hex 只用于 div style，不外发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Information Disclosure | SlidePreviewPanel 截图范围 | mitigate | 截图元素 = `.slide-preview-container`（AI 生成形状），不含 chat/API Key 界面；registerPreviewElement 注入的是此容器的 getter，不是整个 task pane |
| T-24-02 | Information Disclosure | base64 截图进 history（bundle/UI 层） | mitigate | SlidePreviewPanel 不 import html2canvas；layoutShapes 经 buildLayout 重建（args 本就在 message，不引入新数据）；UI 层无法意外把 base64 写进 history；visual-check.test.ts 用例③ NFR-09 守门自动化断言持续覆盖 |
| T-24-03 | Tampering | html2canvas 供应链（bundle 层确认） | mitigate | bundle gate（Task 3 stop-if-fail）确认 html2canvas 只进懒加载 chunk（非 main chunk），降低攻击面；即使供应链污染也限于截图函数调用时 |
| T-24-08 | Denial of Service | P95 退化（截图延迟） | accept | 截图在离散事件（apply_slide_layout 完成后 AI 主动调工具），不在首 token 路径；buildLayout 重建是本地纯函数（无网络）；html2canvas 本地 DOM 截图 <200ms；vision API ~2-5s 与既有 get_shape_image 同量级，已在 P95 预算内的 tool 延迟 |
</threat_model>

<verification>
Phase 24-04 完成验证：

1. `grep "SlidePreviewPanel" src/components/ChatStream.tsx | grep "lazy"` 命中
2. `grep "buildLayout" src/components/ChatStream.tsx` 命中（layoutShapes 重建路径）
3. `grep "toolResult.data" src/components/ChatStream.tsx | grep -i "shapes" | wc -l` == 0（不从 data 读 shapes）
4. `grep "registerPreviewElement" src/components/SlidePreviewPanel.tsx` 命中
5. `grep -c "slide-preview-panel" src/styles.css` ≥ 3（CSS 类块存在）
6. `grep "backdrop-filter\|linear-gradient" src/styles.css | grep "slide-preview" | wc -l` == 0（teal 克制规则）
7. `npm run extract` 零错误；`grep "幻灯片预览" src/i18n/locales/zh-CN/messages.po` 命中（正确路径）
8. `npm run build && npm run size` 通过（exit code 0，main ≤82KB gzip）
9. `ls .planning/phases/24-a-p2-bundle/24-UAT-PACKET.md` 存在
10. `tsc --noEmit && vitest run 2>&1 | grep "passed"` ≥ 994，0 failed
11. P95 不退化论证（threat model T-24-08 + 截图离散事件 + buildLayout 本地纯函数）
</verification>

<success_criteria>
- SlidePreviewPanel teal 克制 UI 组件完成，React.lazy 懒加载
- ChatStream 接线：lazy 声明 + ToolResultCard 内 layoutShapes 经 buildLayout 重建（单一路径，0 改工具层）+ Suspense 条件挂载
- styles.css 新增 SlidePreviewPanel CSS 类（来自 UI-SPEC.md，零渐变/零 backdrop-filter）
- npm run extract 已运行，Lingui catalog 已更新（src/i18n/locales/zh-CN/messages.po）
- bundle gate 通过（独立 stop-if-fail 检查点）：npm run build && npm run size，main-*.js ≤82KB gzip
- UAT-PACKET.md 已创建，含保真度对比图采集步骤（LOCKED-1 spike-gate 人眼判定交付物，非自动断言）
- 989 + 新测全绿，undo 守门（operationLog.integration.test）全绿
- P95 不退化：截图离散事件 + 本地 DOM 层 + buildLayout 本地纯函数 + 非首 token 路径（记录在 threat_model）
</success_criteria>

<output>
完成后创建 `.planning/phases/24-a-p2-bundle/24-04-SUMMARY.md`，记录：
- SlidePreviewPanel 组件路径 + registerPreviewElement 接线机制
- ChatStream layoutShapes 重建逻辑（tool-call args → buildLayout，确认未改 write/ppt.ts）
- bundle gate 结果（build 输出的 main chunk size 精确值）
- npm run extract 新增的 msgid 列表（写入 src/i18n/locales/zh-CN/messages.po）
- P95 不退化论证 summary
- UAT-PACKET.md 路径
- 两条路径最终状态：PVQ06_VISUAL_CHECK_ENABLED=true（默认铺开，待 UAT 判定）
</output>

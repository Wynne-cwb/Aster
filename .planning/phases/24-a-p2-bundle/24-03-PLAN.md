---
phase: 24-a-p2-bundle
plan: "03"
type: execute
wave: 3
depends_on:
  - "24-02"
files_modified:
  - src/agent/tools/read/visual-check.ts
  - src/agent/tools/read/visual-check.test.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - PVQ-06

must_haves:
  truths:
    - "visual_check_slide read tool 由 24-01 stub 填为真身，kind:'read'，不进 PPT_TOOLS 集合"
    - "execute() 内 html2canvas 仅动态 import（await import 在函数体内，非顶层）"
    - "base64 截图为局部变量，不写入 ToolResult.data（NFR-09 满足）"
    - "vision 调用复用 AihubmixVisionClient.analyzeImages + ProviderRegistry.resolve('vision', stub)"
    - "previewEl 不存在时返回 advisory ToolResult（ok:true，不崩溃）"
    - "visual-check.test.ts 解除 describe.skip，5 个用例全绿（含 NFR-09 守门断言）"
    - "tools/index.ts ppt case 含 visualCheckSlide 注册（由 PVQ06_VISUAL_CHECK_ENABLED 控制）"
  artifacts:
    - path: "src/agent/tools/read/visual-check.ts"
      provides: "visual_check_slide read tool（stub → 真身，铺开路径核心）"
      exports: ["visualCheckSlide", "registerPreviewElement"]
    - path: "src/agent/tools/index.ts"
      provides: "ppt case 含 visualCheckSlide 注册"
      contains: "visualCheckSlide"
  key_links:
    - from: "src/agent/tools/read/visual-check.ts"
      to: "src/providers/aihubmix-vision.ts"
      via: "AihubmixVisionClient.analyzeImages"
      pattern: "AihubmixVisionClient.*analyzeImages"
    - from: "src/agent/tools/read/visual-check.ts"
      to: "NFR-09 契约"
      via: "base64 局部变量，不进 ToolResult.data"
      pattern: "pureBase64.*split.*\\[1\\]"
    - from: "src/agent/tools/index.ts"
      to: "src/agent/tools/visual-check-config.ts"
      via: "PVQ06_VISUAL_CHECK_ENABLED 开关控制注册"
      pattern: "PVQ06_VISUAL_CHECK_ENABLED"
---

<objective>
本 plan 实现铺开路径的核心工具：把 24-01 创建的 `visual-check.ts` **stub 填为真身**（`visual_check_slide` read tool）+ **解除 visual-check.test.ts 的 describe.skip**。工具在 AI loop 内以 on-demand 方式调用，完成「取预览 DOM → html2canvas 截图 → AihubmixVision 多模态自查 → 文字 evidence 拼回 LLM 下一轮」全链路，并注册进 ppt host。

Purpose: 铺开路径「自渲染截图 → 多模态自查 → 违规文字反馈」闭环的实现层。read-style、advisory、不破坏 undo/PPT_TOOLS 体系。

**Wave 0 衔接：** 24-01 已建 `visual-check.ts` 最小 stub（`visualCheckSlide` ToolDef，execute throw not-implemented）+ `visual-check.test.ts` 5 用例（`describe.skip` 包裹，含 NFR-09 守门）。本 plan：①把 stub 填为真身（含 registerPreviewElement 注入机制、execute 全流程）；②解除 test 的 `describe.skip` 并按真实 previewEl 注入 API 调整 mock setup（registerPreviewElement / _resetPreviewElementGetter），5 用例转 GREEN；③注册进 tools/index.ts。

Output:
- src/agent/tools/read/visual-check.ts（stub → 真身：visual_check_slide ToolDef + registerPreviewElement）
- src/agent/tools/read/visual-check.test.ts（解除 describe.skip → 5 用例 GREEN）
- src/agent/tools/index.ts（ppt case 增加 visualCheckSlide 注册）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-a-p2-bundle/24-CONTEXT.md
@.planning/phases/24-a-p2-bundle/24-RESEARCH.md
@.planning/phases/24-a-p2-bundle/24-PATTERNS.md
@.planning/phases/24-a-p2-bundle/24-02-SUMMARY.md

<interfaces>
<!-- 来自 src/agent/tools/read/ppt.ts（checkSlideLayout — read tool analog）-->
```typescript
// read tool 完整结构范式（ppt.ts L136-183）
export const checkSlideLayout: ToolDef<CheckLayoutArgs> = {
  name: 'check_slide_layout',
  kind: 'read',               // read tool：无 undo/operationLog/reverse
  description: '...',
  parameters: { type: 'object', properties: { slideIndex: { type: 'number' }, textBoxes: { ... } }, required: ['slideIndex'] },
  humanLabel: (args) => `检查页面 ${args.slideIndex + 1} 版面`,
  async execute({ slideIndex, textBoxes }, ctx): Promise<ToolResult> {
    // ...纯计算...
    return wrapReadResult(
      { ok: true, data: { ...report, summary: formatViolations(report) } },
      { result_type: 'metadata', source: `slide_${slideIndex}.layout_check` },
    );
  },
};
```

<!-- 来自 src/providers/aihubmix-vision.ts（analyzeImages 接口）-->
```typescript
// analyzeImages(userText: string, images: VisionImage[], config: VisionConfig): Promise<{content: string}>
// VisionImage = { base64: string, mimeType: string }
// 内部拼 data URL：`data:${mimeType};base64,${base64}`（L51）
// 传入裸 base64（去 'data:image/png;base64,' 前缀），mimeType='image/png'
export class AihubmixVisionClient {
  async analyzeImages(userText: string, images: VisionImage[], config: VisionConfig): Promise<{content: string}>;
}
export interface VisionConfig { baseURL: string; apiKey: string; }
```

<!-- 来自 src/adapters/PptAdapter.ts L612-620（ProviderRegistry.resolve vision 用法）-->
```typescript
const cfg = ProviderRegistry.resolve(
  'vision',
  () => useProviderStore.getState().providers[0]!,
) as ImageConfig;
const visionConfig: VisionConfig = { baseURL: cfg.baseURL, apiKey: cfg.apiKey };
```

<!-- 来自 src/agent/read-result.ts -->
```typescript
export function wrapReadResult(
  result: ReadableInput,
  opts: { result_type: ReadResultType; source: string },
): ToolResult
```

<!-- 来自 src/lib/parsers/pdf.ts（动态 import 范式）-->
```typescript
// execute() 函数体内动态 import（不在顶层）：
const pdfjsLib = await import('pdfjs-dist');
// visual-check.ts 照此：
const { default: html2canvas } = await import('html2canvas');
```

<!-- 24-01 已建的 visual-check.ts stub 签名（本 plan 填真身，name/kind/parameters 不变）-->
```typescript
export const visualCheckSlide: ToolDef<{ slideIndex: number }>;  // name='visual_check_slide', kind='read'
// 本 plan 新增：export function registerPreviewElement(getter)
```

<!-- 来自 src/agent/tools/index.ts（ppt case read 列表，第 310 行）-->
```typescript
// ppt case read tools 列表（约 L310）：
return [
  listSlides, getSlide, listShapesOnSlide, getShape, checkSlideLayout,  // 注：不进 PPT_TOOLS
  getShapeImage,
  ...pptWriteTools, selectionDetail,
].map((t) => t as ToolDef);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：把 visual-check.ts stub 填为真身 + 解除测试 skip</name>
  <files>
    src/agent/tools/read/visual-check.ts,
    src/agent/tools/read/visual-check.test.ts
  </files>
  <read_first>
    - src/agent/tools/read/visual-check.ts（24-01 的 stub：visualCheckSlide ToolDef 签名，本 plan 填真身 + 加 registerPreviewElement）
    - src/agent/tools/read/visual-check.test.ts（24-01 的 Wave 0 骨架：5 个用例的预期行为，是实现的 spec；当前 describe.skip）
    - src/agent/tools/read/ppt.ts（analog：checkSlideLayout ToolDef 完整结构，L136-183）
    - src/agent/tools/read/vision.ts（analog：NFR-09 契约，base64 不出 ToolResult）
    - src/providers/aihubmix-vision.ts（analyzeImages 签名 + data URL 拼接方式，L44-85）
    - src/providers/registry.ts（ProviderRegistry.resolve 签名，vision case）
    - src/agent/read-result.ts（wrapReadResult 签名）
    - src/agent/tools/index.ts（ToolDef/ToolResult 类型 import 路径确认）
    - src/lib/parsers/pdf.ts（动态 import 范式，execute() 内部 await import）
  </read_first>
  <behavior>
execute() 五条路径：

PATH A（previewEl 不存在）：
- getPreviewElement() 返回 null
- 立即返回 wrapReadResult(advisory)，data.summary 含「预览面板未打开，视觉自查跳过」
- 不调 html2canvas，不调 vision API

PATH B（正常截图路径）：
- getPreviewElement() 返回 HTMLElement
- await import('html2canvas') → html2canvas(el, {scale:2, useCORS:false, allowTaint:false, logging:false, backgroundColor:'#ffffff'})
- canvas.toDataURL('image/png').split(',')[1] → pureBase64（局部变量）
- ProviderRegistry.resolve('vision', stub) → cfg → visionConfig = {baseURL, apiKey}
- new AihubmixVisionClient().analyzeImages(FOCUS_PROMPT, [{base64: pureBase64, mimeType:'image/png'}], visionConfig)
- → {content: string}（文字 evidence）
- pureBase64 不再引用（GC）
- return wrapReadResult({ok:true, data:{summary: content}}, {result_type:'metadata', source:`slide_${slideIndex}.visual_check`})
- ToolResult.data 中 **无 base64/screenshot 字段**（NFR-09）

- 用例 1 RED→GREEN：name/kind 元数据
- 用例 2 RED→GREEN：html2canvas mock 被调用一次
- 用例 3 RED→GREEN（NFR-09）：result.data 无 100+ 字符 base64 字符串
- 用例 4 RED→GREEN：evidence 文字进 result.data.summary
- 用例 5 RED→GREEN：previewEl=null 返回 advisory（ok:true，summary 含「跳过」）
  </behavior>
  <action>
**步骤 1：把 `src/agent/tools/read/visual-check.ts` 的 stub 填为真身。** 替换整个文件内容为下方实现（导出 visualCheckSlide 的 name/kind/parameters 保持与 24-01 stub 一致，新增 execute 真身 + registerPreviewElement / _resetPreviewElementGetter）。

**关键设计决策（Open Question Q1 的解法）：** tool execute() 通过一个 module-level 的可设置 getter 函数取 previewEl，由 SlidePreviewPanel 在挂载时注册此 getter（仿全局 ref，选项 b）。比 event/callback 更简单，只有一个预览面板实例。

```typescript
// src/agent/tools/read/visual-check.ts
// Phase 24 PVQ-06：自渲染预览视觉自查工具（read-style，on-demand，铺开路径）
// NFR-09：base64 截图不进 ToolResult.data，只回文字 evidence

import type { ToolDef, ToolResult } from '../index';
import { wrapReadResult } from '../../read-result';
import { ProviderRegistry } from '../../../providers/registry';
import { AihubmixVisionClient } from '../../../providers/aihubmix-vision';
import type { VisionConfig } from '../../../providers/aihubmix-vision';
import type { ImageConfig } from '../../../providers/types';
import { useProviderStore } from '../../../store/providers';

// ==============================================================
// previewEl 共享机制（全局 mutable getter，仅一个预览面板实例）
// SlidePreviewPanel 挂载时调用 registerPreviewElement(() => ref.current)
// SlidePreviewPanel 卸载时调用 registerPreviewElement(() => null)
// ==============================================================
let _previewElementGetter: (() => HTMLElement | null) = () => null;

export function registerPreviewElement(getter: () => HTMLElement | null): void {
  _previewElementGetter = getter;
}

/** 仅供测试 mock 用（清理状态）*/
export function _resetPreviewElementGetter(): void {
  _previewElementGetter = () => null;
}

// ==============================================================
// Focus prompt（自查 4 项，中文，对齐 geometry-check 语义）
// ==============================================================
const FOCUS_PROMPT = `你是专业 PPT 版面审查助手，只关注以下四项粗粒度问题，逐项输出中文违规说明（无违规则写"无"）：
1. 【溢出】文字是否超出文本框边界（文字被裁切）
2. 【重叠】形状之间是否有明显相互压叠（内容被遮挡）
3. 【留白】版面空白是否过多或分布明显不均
4. 【对比】文字与背景对比是否明显不足、难以辨认
仅输出四项结果，不要其他分析。`;

interface VisualCheckArgs {
  slideIndex: number;
}

export const visualCheckSlide: ToolDef<VisualCheckArgs> = {
  name: 'visual_check_slide',
  kind: 'read',  // read tool：无 undo/operationLog/reverse/postState；不进 PPT_TOOLS

  description: '对当前幻灯片自渲染预览截图，用多模态模型视觉自查四项版面问题（溢出/重叠/留白/对比），返回文字违规反馈。需先调用 apply_slide_layout 使预览面板显示。',

  parameters: {
    type: 'object' as const,
    properties: {
      slideIndex: {
        type: 'number' as const,
        description: '要自查的幻灯片索引（0-based）',
      },
    },
    required: ['slideIndex'],
  },

  humanLabel: (args) => `视觉自查页面 ${args.slideIndex + 1}`,

  async execute({ slideIndex }, _ctx): Promise<ToolResult> {
    // 1. 取预览面板 DOM 元素
    const previewEl = _previewElementGetter();
    if (!previewEl) {
      return wrapReadResult(
        {
          ok: true,
          data: { summary: '预览面板未打开，视觉自查跳过（仅依据几何自查结果）' },
        },
        { result_type: 'metadata', source: `slide_${slideIndex}.visual_check` },
      );
    }

    // 2. 动态 import html2canvas（懒加载，不进初始 chunk — CONTEXT 硬约束#1）
    const { default: html2canvas } = await import('html2canvas');
    const htmlCanvas = await html2canvas(previewEl, {
      scale: 2,                       // 高清截图，AI 识别更准
      useCORS: false,                 // 我方 DOM 无跨域图片
      allowTaint: false,              // 默认安全
      logging: false,                 // 关闭 console 噪音
      backgroundColor: '#ffffff',    // 明确白底，与预览容器白底同步
      foreignObjectRendering: false,  // canvas-renderer 路径，Office for Web sandbox 更稳定
    });
    // NFR-09：pureBase64 为局部变量，截图后只传给 analyzeImages，不写入 ToolResult.data
    const pureBase64 = htmlCanvas.toDataURL('image/png').split(',')[1];

    // 3. 取 vision 配置（复用 PptAdapter 既有范式）
    // apiKey 仅经 Authorization header，不进 body/error.message（T-24-01 / T-01-04）
    const cfg = ProviderRegistry.resolve(
      'vision',
      () => useProviderStore.getState().providers[0]!,
    ) as ImageConfig;
    const visionConfig: VisionConfig = { baseURL: cfg.baseURL, apiKey: cfg.apiKey };

    // 4. 调 analyzeImages（NFR-09：pureBase64 局部变量，不出此函数边界）
    const { content } = await new AihubmixVisionClient().analyzeImages(
      FOCUS_PROMPT,
      [{ base64: pureBase64, mimeType: 'image/png' }],
      visionConfig,
    );
    // pureBase64 至此生命周期结束（不写入 ToolResult，自然 GC）

    // 5. wrapReadResult — 只含文字 evidence，无 base64
    return wrapReadResult(
      { ok: true, data: { summary: content } },
      { result_type: 'metadata', source: `slide_${slideIndex}.visual_check` },
    );
  },
};
```

**步骤 2：解除 `visual-check.test.ts` 的 describe.skip 并对齐真实 previewEl 注入 API。** 24-01 的测试骨架用了占位的 mock setup（vi.hoisted + describe.skip）；本 plan 把 `describe.skip` 改为 `describe`，并把每个用例的 previewEl 注入改成真实 API：
- import `registerPreviewElement, _resetPreviewElementGetter` from `./visual-check`
- beforeEach 调 `_resetPreviewElementGetter()`（每用例前清空 getter）
- 用例 ②③④（正常截图）：先 `registerPreviewElement(() => document.createElement('div'))` 注入 previewEl
- 用例 ⑤（advisory fallback）：不注册（getter 默认返回 null），断言 summary 含「跳过」
- vi.hoisted mock factory（html2canvas / AihubmixVisionClient）保持 24-01 已写结构

最终 visual-check.test.ts（解除 skip 版）：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { visualCheckSlide, registerPreviewElement, _resetPreviewElementGetter } from './visual-check';

const { mockHtml2canvas, mockAnalyzeImages } = vi.hoisted(() => ({
  mockHtml2canvas: vi.fn(),
  mockAnalyzeImages: vi.fn(),
}));

vi.mock('html2canvas', () => ({ default: mockHtml2canvas }));
vi.mock('../../../providers/aihubmix-vision', () => ({
  AihubmixVisionClient: vi.fn().mockImplementation(() => ({
    analyzeImages: mockAnalyzeImages,
  })),
}));
// ProviderRegistry.resolve('vision') 须返回 {baseURL, apiKey}（按实际 registry mock，
// 或 mock ProviderRegistry.resolve 返回固定 ImageConfig 桩值，见 vision.test.ts / registry mock 范式）
vi.mock('../../../providers/registry', () => ({
  ProviderRegistry: { resolve: () => ({ baseURL: 'https://x', apiKey: 'k', model: 'm' }) },
}));

describe('visualCheckSlide ToolDef（PVQ-06，NFR-09 守门）', () => {
  beforeEach(() => {
    _resetPreviewElementGetter();
    mockHtml2canvas.mockReset();
    mockAnalyzeImages.mockReset();
  });

  it('① 元数据：name=visual_check_slide, kind=read', () => {
    expect(visualCheckSlide.name).toBe('visual_check_slide');
    expect(visualCheckSlide.kind).toBe('read');
  });

  it('② html2canvas 被调用（mock 截图）', async () => {
    registerPreviewElement(() => document.createElement('div'));
    mockHtml2canvas.mockResolvedValue({ toDataURL: () => 'data:image/png;base64,FAKEBASE64==' });
    mockAnalyzeImages.mockResolvedValue({ content: '溢出：无' });
    await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    expect(mockHtml2canvas).toHaveBeenCalledTimes(1);
  });

  it('③ NFR-09 守门：ToolResult.data 不含 base64 字符串', async () => {
    registerPreviewElement(() => document.createElement('div'));
    mockHtml2canvas.mockResolvedValue({ toDataURL: () => 'data:image/png;base64,' + 'A'.repeat(200) });
    mockAnalyzeImages.mockResolvedValue({ content: '测试 evidence' });
    const result = await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    expect(JSON.stringify(result.data)).not.toMatch(/[A-Za-z0-9+/]{100,}/);
    expect(result.data).not.toHaveProperty('base64');
    expect(result.data).not.toHaveProperty('screenshot');
  });

  it('④ evidence 文字拼入 result.data.summary', async () => {
    registerPreviewElement(() => document.createElement('div'));
    const EXPECTED_EVIDENCE = '溢出：无；重叠：有形状压叠';
    mockHtml2canvas.mockResolvedValue({ toDataURL: () => 'data:image/png;base64,SHORT' });
    mockAnalyzeImages.mockResolvedValue({ content: EXPECTED_EVIDENCE });
    const result = await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    expect(result.data.summary).toContain(EXPECTED_EVIDENCE);
  });

  it('⑤ previewEl 不存在时返回 advisory（不崩溃，ok:true，含「跳过」）', async () => {
    // getter 默认 null（beforeEach 已 reset，未 registerPreviewElement）
    const result = await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    expect(result.ok).toBe(true);
    const summaryText = result.data?.summary ?? '';
    expect(summaryText).toMatch(/跳过|未打开/);
    expect(mockHtml2canvas).not.toHaveBeenCalled();
  });
});
```

注意：
- ProviderRegistry mock 必须返回含 baseURL/apiKey 的对象，否则 PATH B 在取 visionConfig 时报错；按 registry.ts 实际 ImageConfig 形状调整桩值。
- 若项目已有 vision.test.ts 的 registry mock 范式，优先复用其写法。
  </action>
  <verify>
    <automated>npx vitest run visual-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run visual-check` 输出 5 passed（解除 skip 后 5 个用例全绿）
    - `grep -c "describe.skip" src/agent/tools/read/visual-check.test.ts` == 0（skip 已解除）
    - `grep -c "not implemented\|Wave 0 stub" src/agent/tools/read/visual-check.ts` == 0（stub 已被真身替换）
    - `grep "await import.*html2canvas" src/agent/tools/read/visual-check.ts` 命中（动态 import 在函数体内）
    - `grep "^import.*html2canvas" src/agent/tools/read/visual-check.ts | wc -l` == 0（无顶层 static import html2canvas，防止进 loop chunk 初始加载）
    - `grep "pureBase64" src/agent/tools/read/visual-check.ts | grep -v "split\|analyzeImages\|base64: pureBase64\|const pureBase64" | wc -l` == 0（pureBase64 只用于 split 和 analyzeImages，不出现在 data 对象中）
    - `grep "result_type.*metadata" src/agent/tools/read/visual-check.ts` 命中（read-style evidence 包装）
    - `grep "export function registerPreviewElement" src/agent/tools/read/visual-check.ts` 命中（previewEl 注入机制）
    - `tsc --noEmit` 零错误
  </acceptance_criteria>
  <done>visual_check_slide stub 填为真身，5 个单测全绿（解除 skip），NFR-09 守门通过，html2canvas 动态 import 在函数体内，registerPreviewElement 导出</done>
</task>

<task type="auto">
  <name>Task 2：tools/index.ts 注册 visualCheckSlide（含 PVQ06_VISUAL_CHECK_ENABLED 开关）</name>
  <files>src/agent/tools/index.ts</files>
  <read_first>
    - src/agent/tools/index.ts（全文：了解 import 块结构 L1-30，PPT_TOOLS 集合 L31-48，buildToolsForHost ppt case 约 L300-315，现有 read tools 列表）
    - src/agent/tools/visual-check-config.ts（PVQ06_VISUAL_CHECK_ENABLED，Plan 24-02 已创建）
    - src/agent/tools/read/visual-check.ts（visualCheckSlide export 路径，刚实现）
    - 24-PATTERNS.md §tools/index.ts（精确定位：第 310 行 ppt case return 数组，checkSlideLayout 注册行风格）
  </read_first>
  <action>
修改 `src/agent/tools/index.ts` 的 ppt case，完成两处改动：

**改动 1：顶部 import 块新增两行**（紧随现有 read tool import 后，仿 getShapeImage 的风格）

```typescript
// 在 import { getShapeImage } from './read/vision'; 附近添加：
import { visualCheckSlide } from './read/visual-check';   // Phase 24 PVQ-06
import { PVQ06_VISUAL_CHECK_ENABLED } from './visual-check-config';  // Phase 24 降级开关
```

**改动 2：ppt case return 数组**（在 checkSlideLayout 之后，getShapeImage 之前，仿行内注释风格）

```typescript
// 原：
return [
  listSlides, getSlide, listShapesOnSlide, getShape, checkSlideLayout, // Phase 22 PVQ-02：...
  getShapeImage,
  ...pptWriteTools, selectionDetail,
].map((t) => t as ToolDef);

// 改为：
return [
  listSlides, getSlide, listShapesOnSlide, getShape, checkSlideLayout, // Phase 22 PVQ-02：...
  ...(PVQ06_VISUAL_CHECK_ENABLED ? [visualCheckSlide] : []), // Phase 24 PVQ-06：视觉自查 read tool（不进 PPT_TOOLS，on-demand，默认铺开）
  getShapeImage,
  ...pptWriteTools, selectionDetail,
].map((t) => t as ToolDef);
```

关键约束：
- **不修改 PPT_TOOLS 集合**（read tool 不需要 camelCase 归一化，不进集合）
- 不改 Excel / Word case
- 仿现有注释风格（「Phase XX：说明」）

确认后运行 `tsc --noEmit && vitest run` 验证不破坏现有 989 tests。
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "visualCheckSlide" src/agent/tools/index.ts` 命中（工具已注册）
    - `grep "PVQ06_VISUAL_CHECK_ENABLED" src/agent/tools/index.ts` 命中（开关控制注册）
    - `grep "PPT_TOOLS" src/agent/tools/index.ts | grep "visualCheckSlide" | wc -l` == 0（visual_check_slide 不在 PPT_TOOLS 集合内）
    - `tsc --noEmit` 零错误
    - `vitest run 2>&1 | grep "passed"` ≥ 994（989 既有 + 5 新测 visual-check）
    - `vitest run 2>&1 | grep "operationLog.integration"` 绿（undo 守门未破坏）
  </acceptance_criteria>
  <done>visualCheckSlide 已注册到 ppt case read 列表，PVQ06_VISUAL_CHECK_ENABLED 开关生效，undo 守门全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execute() 内部 → AihubmixVisionClient | base64 截图通过 Authorization header（非 body）发往 aihubmix；BYO key 用户自有，与 v2.2 vision 同信任边界 |
| LLM → visual_check_slide tool | AI 触发工具调用；工具参数只接收 slideIndex（整数），无注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | API key 泄漏 | mitigate | 复用 ProviderRegistry.resolve + AihubmixVisionClient（既有 T-01-04 控制：key 仅 Authorization header，错误 message 用字面量，不透传 cfg.apiKey）|
| T-24-02 | Information Disclosure | base64 截图进 history | mitigate | pureBase64 局部变量，只传 analyzeImages，ToolResult.data = {summary: content}（仅文字）；visual-check.test.ts 用例③ NFR-09 守门自动化断言强制执行 |
| T-24-03 | Tampering | html2canvas 供应链 | mitigate | 锁版本 1.4.1；动态 import 仅处理我方自渲染 DOM（.slide-preview-container），非聊天/Key 界面；RESEARCH Q1 tarball 审查：eval()=0 |
| T-24-06 | Information Disclosure | 截图内容外发 | accept | 发往 aihubmix（BYO key，用户自有 API key）；截图范围 = AI 生成的幻灯片形状，不含 API Key 界面/聊天内容；与 v2.2 get_shape_image 同隐私面 |
</threat_model>

<verification>
Phase 24-03 完成验证：

1. `npx vitest run visual-check` 输出 5 passed
2. `grep -c "describe.skip" src/agent/tools/read/visual-check.test.ts` == 0（skip 已解除）
3. `grep "await import.*html2canvas" src/agent/tools/read/visual-check.ts` 命中（动态 import 在函数体内）
4. `grep "^import.*html2canvas" src/agent/tools/read/visual-check.ts | wc -l` == 0（无顶层 static import）
5. `grep "visualCheckSlide" src/agent/tools/index.ts` 命中
6. `grep "PPT_TOOLS.*visualCheckSlide\|visualCheckSlide.*PPT_TOOLS" src/agent/tools/index.ts | wc -l` == 0（不在 PPT_TOOLS）
7. `tsc --noEmit && vitest run 2>&1 | grep "passed"` ≥ 994
8. `vitest run 2>&1 | grep -i "failed"` 零 failed
</verification>

<success_criteria>
- visual_check_slide ToolDef stub 填为真身，5 个测试用例全绿（解除 skip）
- NFR-09 守门：base64 不进 ToolResult.data（守门用例绿）
- html2canvas 动态 import 在 execute() 函数体内（不进初始 bundle）
- registerPreviewElement 导出（供 SlidePreviewPanel 注入 previewEl）
- tools/index.ts ppt case 含 visualCheckSlide（PVQ06_VISUAL_CHECK_ENABLED 开关控制）
- 不进 PPT_TOOLS 集合（read tool 语义）
- undo 守门（operationLog.integration.test）全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/24-a-p2-bundle/24-03-SUMMARY.md`，记录：visual_check_slide 工具的 execute() 流程（5 步）、registerPreviewElement/_resetPreviewElementGetter 共享机制、NFR-09 守门测试绿化确认、visual-check.test.ts 解除 skip 后 5 用例绿化、tools/index.ts 注册位置。
</output>

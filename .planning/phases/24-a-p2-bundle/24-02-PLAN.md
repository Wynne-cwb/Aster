---
phase: 24-a-p2-bundle
plan: "02"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - src/agent/design/slide-preview.ts
  - src/agent/design/slide-preview.test.ts
  - src/agent/tools/visual-check-config.ts
autonomous: true
requirements:
  - PVQ-06
  - NFR-11

must_haves:
  truths:
    - "slide-preview.ts 存在，导出 mapShapesToRender 纯函数（ShapeSpec[] + containerWidthPx → SlideRenderShape[]）"
    - "坐标映射使用 DEFAULT_CANVAS_PT.widthPt（960）为基准，scale = containerWidthPx / 960"
    - "Wave 0 slide-preview.test.ts 4 个用例全绿（坐标映射验证、960 基准回归、字号下限、ShapeType 分支）"
    - "visual-check-config.ts 存在，导出 PVQ06_VISUAL_CHECK_ENABLED 开关（降级路径 flag）"
    - "降级路径清晰：关闭开关时系统行为 = 仅 Phase 22 几何自查兜底，不引入新的铺开工具"
  artifacts:
    - path: "src/agent/design/slide-preview.ts"
      provides: "坐标映射纯函数渲染器"
      exports: ["mapShapesToRender", "SlideRenderShape"]
    - path: "src/agent/tools/visual-check-config.ts"
      provides: "PVQ06_VISUAL_CHECK_ENABLED 降级路径开关"
      contains: "PVQ06_VISUAL_CHECK_ENABLED"
  key_links:
    - from: "src/agent/design/slide-preview.ts"
      to: "src/agent/design/ppt-tokens.ts"
      via: "import DEFAULT_CANVAS_PT"
      pattern: "DEFAULT_CANVAS_PT\\.widthPt"
    - from: "src/agent/design/slide-preview.ts"
      to: "src/agent/design/ppt-layouts.ts"
      via: "import type ShapeSpec"
      pattern: "ShapeSpec"
---

<objective>
本 plan 实现：（1）坐标映射纯函数渲染器 `slide-preview.ts`，把 ShapeSpec[]@960pt 映射为 React inline style 数据对象；（2）降级路径控制开关 `visual-check-config.ts`。

Purpose: 渲染器是自渲染预览的核心数据层，纯函数可单测，不含任何副作用。降级 flag 是「两条路径都有代码」（LOCKED-2）中降级路径的明确形式，verdict 后可通过翻 flag 控制铺开/降级。

Output:
- src/agent/design/slide-preview.ts（坐标映射纯函数，不含 JSX/React）
- src/agent/design/slide-preview.test.ts（Wave 0 骨架变绿）
- src/agent/tools/visual-check-config.ts（降级路径 flag）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-a-p2-bundle/24-CONTEXT.md
@.planning/phases/24-a-p2-bundle/24-RESEARCH.md
@.planning/phases/24-a-p2-bundle/24-PATTERNS.md
@.planning/phases/24-a-p2-bundle/24-01-SUMMARY.md

<interfaces>
<!-- 来自 ppt-tokens.ts（坐标真相源）-->
```typescript
// src/agent/design/ppt-tokens.ts
export const DEFAULT_CANVAS_PT = { widthPt: 960, heightPt: 540 } as const;
export type Canvas = typeof DEFAULT_CANVAS_PT;
```

<!-- 来自 ppt-layouts.ts（输入类型）-->
```typescript
// src/agent/design/ppt-layouts.ts（ShapeSpec 接口）
export interface ShapeSpec {
  role: string;
  shapeType: 'TextBox' | 'Rectangle' | 'RoundedRectangle' | 'Ellipse';
  rect: { left: number; top: number; width: number; height: number };
  text?: string;
  font?: { size: number; bold?: boolean; color?: string; name?: string };
  fillColor?: string;
  lineColor?: string;
  lineWeight?: number;
  align?: string;
  bgForContrast?: string;
}
```

<!-- geometry-check.ts 范式（analog：同目录纯函数，只 import ppt-tokens）-->
```typescript
// src/agent/design/geometry-check.ts（analog 示例）
import { DEFAULT_CANVAS_PT, ... } from './ppt-tokens';
export function checkSlideLayout(shapes: ShapeBox[], opts?): LayoutReport {
  const canvas = opts?.canvas ?? DEFAULT_CANVAS_PT;
  // ...纯函数逻辑...
  return { canvas, violations, notes };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：实现坐标映射纯函数渲染器 slide-preview.ts</name>
  <files>src/agent/design/slide-preview.ts, src/agent/design/slide-preview.test.ts</files>
  <read_first>
    - src/agent/design/slide-preview.test.ts（Wave 0 骨架：4 个用例的预期输入/输出，是实现的 spec）
    - src/agent/design/geometry-check.ts（analog：同目录纯函数结构，import 范式）
    - src/agent/design/ppt-tokens.ts（DEFAULT_CANVAS_PT = {widthPt:960, heightPt:540}，坐标真相源）
    - src/agent/design/ppt-layouts.ts（ShapeSpec 接口完整定义）
  </read_first>
  <behavior>
mapShapesToRender(shapes, containerWidthPx) → SlideRenderShape[]

- 用例 1 RED→GREEN：喂 rect={left:48,top:36,width:864,height:468} @960pt，containerWidth=480 → style.left=24, top=18, width=432, height=234
- 用例 2 RED→GREEN：同 shape，containerWidth=960（scale=1.0）→ left=48, width=864
- 用例 3 RED→GREEN：font.size=4，scale=0.5 → fontSize=9（Math.max(4×0.5=2, 9)=9）
- 用例 4 RED→GREEN：RoundedRectangle→borderRadius 含 'px'；Ellipse→'50%'；Rectangle→undefined
  </behavior>
  <action>
创建 `src/agent/design/slide-preview.ts`。仿 geometry-check.ts 的纯函数、零副作用结构。

规格：
- 只 import `DEFAULT_CANVAS_PT` from `./ppt-tokens` 和 `type ShapeSpec` from `./ppt-layouts`
- 不 import React（渲染器只产出数据/style 描述对象，不含 JSX）
- CSS 属性用精确的字符串/数字类型（不用 `React.CSSProperties`，避免 React 依赖）

```typescript
// src/agent/design/slide-preview.ts
// 纯函数坐标映射渲染器（ShapeSpec[]@960×540pt → 绝对定位 div style 描述）
// 零副作用、零网络、零 React — 可单测
import { DEFAULT_CANVAS_PT } from './ppt-tokens';
import type { ShapeSpec } from './ppt-layouts';

/** 渲染器输出的 shape 描述（给 SlidePreviewPanel 消费） */
export interface SlideRenderShape {
  key: string;
  style: {
    position: 'absolute';
    left: number;
    top: number;
    width: number;
    height: number;
    backgroundColor: string;
    fontSize: number;
    fontWeight: number;
    color: string;
    textAlign: string;
    borderRadius: string | undefined;
    overflow: 'hidden';
    boxSizing: 'border-box';
    padding: string;
    whiteSpace: 'pre-wrap';
  };
  text: string | undefined;
  shapeType: ShapeSpec['shapeType'];
  lineColor: string | undefined;
  lineWeight: number | undefined;
}

/**
 * 将 ShapeSpec[]（@960×540pt 坐标体系）映射为绝对定位 div 的 style 描述对象。
 *
 * 坐标真相源：DEFAULT_CANVAS_PT.widthPt = 960（Phase 22 已定，非 720）
 * scale = containerWidthPx / 960
 *
 * @param shapes  - ShapeSpec[] from ppt-layouts.buildLayout / apply_slide_layout 输出
 * @param containerWidthPx - 预览容器宽度（px），通常约 318px（350px task pane 减 padding）
 */
export function mapShapesToRender(
  shapes: ShapeSpec[],
  containerWidthPx: number,
): SlideRenderShape[] {
  const scale = containerWidthPx / DEFAULT_CANVAS_PT.widthPt; // = containerWidthPx / 960

  return shapes.map((s, i) => {
    const borderRadius: string | undefined =
      s.shapeType === 'RoundedRectangle'
        ? `${Math.max(Math.round(4 * scale), 2)}px`
        : s.shapeType === 'Ellipse'
        ? '50%'
        : undefined;

    return {
      key: `${s.role}-${i}`,
      style: {
        position: 'absolute' as const,
        left:   Math.round(s.rect.left   * scale * 100) / 100,
        top:    Math.round(s.rect.top    * scale * 100) / 100,
        width:  Math.round(s.rect.width  * scale * 100) / 100,
        height: Math.round(s.rect.height * scale * 100) / 100,
        backgroundColor: s.fillColor ?? 'transparent',
        // 字号下限 9px（scale 后过小不可辨认，RESEARCH Pattern 2）
        fontSize: Math.max((s.font?.size ?? 14) * scale, 9),
        fontWeight: s.font?.bold ? 700 : 400,
        // 幻灯片内容颜色用 AI 传入 hex，缺省 #222222（物理隔离：不用 --text CSS 变量）
        color: s.font?.color ?? '#222222',
        textAlign: (s.align?.toLowerCase() ?? 'left') as string,
        borderRadius,
        overflow: 'hidden' as const,
        boxSizing: 'border-box' as const,
        padding: `${Math.max(Math.round(2 * scale), 1)}px`,
        whiteSpace: 'pre-wrap' as const,
      },
      text: s.text,
      shapeType: s.shapeType,
      lineColor: s.lineColor,
      lineWeight: s.lineWeight,
    };
  });
}
```

同时修正 `slide-preview.test.ts`（Wave 0 骨架）中若有 `@ts-expect-error` 或 describe.skip 占位，移除让用例变为活跃。测试文件在 Wave 0 创建时已是正确结构，此处确认 import 路径正确：`import { mapShapesToRender } from './slide-preview'`。

注意：
- `style.left/top/width/height` 用浮点保留 2 位精度（Math.round(n*100)/100），单测断言时用 `toBe(24)` 精确匹配（480/960=0.5 精确，无小数误差）
- `DEFAULT_CANVAS_PT.widthPt`（960）不硬编数字，读常量（LOCKED-4，防 720 回归）
  </action>
  <verify>
    <automated>npx vitest run slide-preview 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run slide-preview` 输出 4 passed（全绿，Wave 0 骨架变 GREEN）
    - `grep "DEFAULT_CANVAS_PT\.widthPt" src/agent/design/slide-preview.ts` 输出含 `DEFAULT_CANVAS_PT.widthPt`（960 基准读自常量，不硬编）
    - `grep -v "^//" src/agent/design/slide-preview.ts | grep -c "720"` == 0（无 720 硬编）
    - `grep "import.*React\b" src/agent/design/slide-preview.ts | wc -l` == 0（纯函数，无 React import）
    - `tsc --noEmit` 零错误
  </acceptance_criteria>
  <done>slide-preview.ts 实现完成，Wave 0 4 个单测全绿，坐标映射使用 960 基准，tsc 零错误</done>
</task>

<task type="auto">
  <name>Task 2：降级路径 flag — visual-check-config.ts</name>
  <files>src/agent/tools/visual-check-config.ts</files>
  <read_first>
    - src/agent/tools/index.ts（了解工具注册结构，PPT_TOOLS 集合，ppt case return 数组）
    - 24-CONTEXT.md §LOCKED-2（两条路径都要落地代码；铺开默认「工具已注册但可关闭」；降级时关闭 flag）
    - 24-RESEARCH.md §Q6（开关机制建议：PVQ06_VISUAL_CHECK_ENABLED 常量）
  </read_first>
  <action>
创建 `src/agent/tools/visual-check-config.ts`——单一文件，导出降级路径开关常量和文档化的降级说明。

```typescript
// src/agent/tools/visual-check-config.ts
// Phase 24 PVQ-06：自渲染预览视觉自查 — 铺开/降级路径开关
//
// 【默认值 = true（铺开，工具已注册为 advisory/可选）】
// 这是 spike 阶段的初始值，等待 UAT 保真度人眼判定（LOCKED-1）。
//
// UAT 判定后的操作：
//   铺开路径（保真度够用）：保持 true，视觉自查工具 visual_check_slide 可用
//   降级路径（保真度不足）：改为 false，工具不注册，系统回落到 Phase 22 几何自查
//     （check_slide_layout 兜底，仅文字/几何规则自查，无视觉截图）
//
// 降级时还需做：
//   1. REQUIREMENTS.md PVQ-06 更新状态（标注「降级：仅几何自查兜底」）
//   2. 告知用户话术：「视觉自查保真度不足，已回落到几何规则自查（溢出/重叠/越界/对比）」
//
// 可调项（LOCKED-3，UAT 调）：
//   (a) 保真度门槛 = 人眼粗粒度可辨认（无数值 gate）
//   (b) 触发 = on-demand（AI 主动调工具，非每次 apply_slide_layout 后 auto）
//   (c) 渲染 = visible（teal 克制小预览面板）
//   以上三项 UAT 可调，不需要改此文件，通过工具描述/面板 UI 实现

export const PVQ06_VISUAL_CHECK_ENABLED = true;
```

此文件极简，只有常量 + 注释。索引文件（tools/index.ts）在 Plan 24-03 中按此开关决定是否注册 visualCheckSlide。
  </action>
  <verify>
    <automated>grep "PVQ06_VISUAL_CHECK_ENABLED" src/agent/tools/visual-check-config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PVQ06_VISUAL_CHECK_ENABLED" src/agent/tools/visual-check-config.ts` 输出含 `= true`（默认铺开）
    - 文件含「降级」「REQUIREMENTS.md」相关注释（降级路径文档化，executor 收 UAT 结论后知道该做什么）
    - `tsc --noEmit` 零新增错误
  </acceptance_criteria>
  <done>降级路径 flag 文件已创建，含完整降级操作说明，默认值 true（铺开）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShapeSpec（AI 输出） → mapShapesToRender | AI 生成的 hex 色值和坐标进入渲染器，但仅用于本地 DOM style，不外发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Information Disclosure | SlideRenderShape 数据 | accept | 渲染器输出仅为本地 style 对象，不含 API key，不外发；风险极低 |
| T-24-05 | Spoofing | PVQ06_VISUAL_CHECK_ENABLED flag | accept | 此为本地编译期常量，不从外部输入读取，无篡改面；UAT 判定后由开发者手动翻值，有 git 追踪 |
</threat_model>

<verification>
Phase 24-02 完成验证：

1. `npx vitest run slide-preview` 输出 4 passed（全绿）
2. `grep "DEFAULT_CANVAS_PT\.widthPt" src/agent/design/slide-preview.ts` 返回命中行（960 基准来自常量）
3. `grep -c "720" src/agent/design/slide-preview.ts` == 0（无 720 硬编）
4. `grep "PVQ06_VISUAL_CHECK_ENABLED" src/agent/tools/visual-check-config.ts` 命中
5. `tsc --noEmit && vitest run 2>&1 | grep "passed"` ≥ 993（989 + 4 新测）
</verification>

<success_criteria>
- slide-preview.ts 纯函数渲染器完成，Wave 0 4 个单测全绿
- 坐标映射用 DEFAULT_CANVAS_PT.widthPt（960），无 720 硬编
- visual-check-config.ts 降级路径 flag 已创建
- tsc --noEmit 零错误
</success_criteria>

<output>
完成后创建 `.planning/phases/24-a-p2-bundle/24-02-SUMMARY.md`，记录：mapShapesToRender 函数签名、scale 公式（containerWidthPx/960）、降级 flag 文件路径和默认值、测试绿化确认。
</output>

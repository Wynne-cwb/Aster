---
phase: 24-a-p2-bundle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/agent/design/slide-preview.ts
  - src/agent/design/slide-preview.test.ts
  - src/agent/tools/read/visual-check.ts
  - src/agent/tools/read/visual-check.test.ts
autonomous: true
requirements:
  - PVQ-06
  - NFR-11

must_haves:
  truths:
    - "html2canvas@1.4.1 作为 dependency 安装（锁定版本，仅动态 import 使用）"
    - "slide-preview.ts / visual-check.ts 最小 stub 已建（正确导出签名 + not-implemented 抛错），tsc 可解析"
    - "slide-preview.test.ts 存在（describe.skip 包裹），含坐标映射 4 个用例骨架，24-02 解除 skip 后变绿"
    - "visual-check.test.ts 存在（describe.skip 包裹），含 NFR-09 守门断言 + evidence 拼装 + kind:'read' 共 5 个用例，24-03 解除 skip 后变绿"
    - "Wave 0 即 tsc --noEmit && vitest run 全绿（describe.skip 不跑，stub 可解析，既有 989 不退化）"
  artifacts:
    - path: "package.json"
      provides: "html2canvas@1.4.1 dependency"
      contains: "html2canvas"
    - path: "src/agent/design/slide-preview.ts"
      provides: "mapShapesToRender 最小 stub（正确签名 + not-implemented，24-02 填真身）"
      exports: ["mapShapesToRender", "SlideRenderShape"]
    - path: "src/agent/tools/read/visual-check.ts"
      provides: "visualCheckSlide 最小 stub（正确 ToolDef 形状 + execute not-implemented，24-03 填真身）"
      exports: ["visualCheckSlide"]
    - path: "src/agent/design/slide-preview.test.ts"
      provides: "坐标映射单测骨架（Wave 0，describe.skip）"
      min_lines: 40
    - path: "src/agent/tools/read/visual-check.test.ts"
      provides: "视觉自查工具单测骨架（Wave 0，含 NFR-09 守门，describe.skip）"
      min_lines: 60
  key_links:
    - from: "src/agent/tools/read/visual-check.test.ts"
      to: "NFR-09 守门"
      via: "断言 JSON.stringify(result.data) not.toMatch(/[A-Za-z0-9+/]{100,}/)"
      pattern: "not\\.toMatch|not\\.toHaveProperty.*base64"
---

<objective>
本 plan 完成三件事：（1）安装 html2canvas@1.4.1 依赖；（2）创建被测模块的最小 stub（slide-preview.ts / visual-check.ts），使静态 import 可解析、tsc 绿；（3）创建 Wave 0 测试骨架（describe.skip 包裹），让后续实现任务有明确的 GREEN 目标。

Purpose: Wave 0 测试先行——坐标映射纯函数测试和 NFR-09 守门测试骨架必须在实现前到位，确保实现阶段可立即验证，避免在没有守门的情况下意外把 base64 写进 ToolResult。

**Wave 0 范式 = stub + describe.skip（项目既有先例）：** `ppt-image.test.ts` L49「describe.skip：Plan 16-03 实现 execute 后解除 skip」、`word-image.test.ts`、STATE 决策「attachments.test.ts 用 describe.skip 而非动态 import：避免 tsc strict Module Not Found 错误」。Wave 0 同时建 stub 模块（正确导出签名 + not-implemented 抛错）让 tsc 可解析，测试用 `describe.skip` 包裹不跑（Wave 0 即绿）；实现 wave（24-02 填 slide-preview.ts、24-03 填 visual-check.ts）填真身后**解除 skip** 转 GREEN。

Output:
- package.json 新增 html2canvas@1.4.1
- src/agent/design/slide-preview.ts（最小 stub：导出 mapShapesToRender + SlideRenderShape 类型，函数体 throw not-implemented）
- src/agent/tools/read/visual-check.ts（最小 stub：导出 visualCheckSlide ToolDef，execute throw not-implemented）
- src/agent/design/slide-preview.test.ts（Wave 0 骨架，describe.skip，4 用例）
- src/agent/tools/read/visual-check.test.ts（Wave 0 骨架，describe.skip，5 用例，含 NFR-09 守门）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-a-p2-bundle/24-CONTEXT.md
@.planning/phases/24-a-p2-bundle/24-RESEARCH.md
@.planning/phases/24-a-p2-bundle/24-PATTERNS.md
@.planning/phases/24-a-p2-bundle/24-VALIDATION.md

<interfaces>
<!-- analog：geometry-check.test.ts 结构 -->
<!-- analog：vision.test.ts 结构 -->
<!-- analog：ppt-image.test.ts L49 describe.skip 先例（stub + skip Wave 0 范式）-->

从 src/agent/design/geometry-check.test.ts（analog 范式）：
```typescript
import { describe, it, expect } from 'vitest';
// 辅助工厂函数（让测试数据干净）
const box = (id, left, top, width, height): ShapeBox => ({ id, left, top, width, height });
describe('功能名（PVQ-XX）', () => {
  it('happy path', () => { ... });
  it('edge case', () => { ... });
});
```

从 src/agent/tools/write/ppt-image.test.ts（describe.skip Wave 0 先例，L49）：
```typescript
// ---------------------------------------------------------------------------
// describe.skip：Plan 16-03 实现 execute 后解除 skip
// ---------------------------------------------------------------------------
describe.skip('generate_ppt_image tool（loop 内直插）', () => {
  // ...用例...
});
```

从 src/agent/tools/read/vision.test.ts（analog 范式）：
```typescript
import { describe, it, expect, vi } from 'vitest';
describe('getShapeImage ToolDef', () => {
  it('name=get_shape_image, kind=read', () => {
    expect(getShapeImage.name).toBe('get_shape_image');
    expect(getShapeImage.kind).toBe('read');
  });
  it('execute 调 adapter.read', async () => { ... });
});
```

从 src/agent/design/ppt-tokens.ts：
```typescript
export const DEFAULT_CANVAS_PT = { widthPt: 960, heightPt: 540 } as const;
// 坐标真相源：960×540，不是 720×405
```

从 src/agent/design/ppt-layouts.ts（ShapeSpec 接口）：
```typescript
export interface ShapeSpec {
  role: string;
  shapeType: 'TextBox' | 'Rectangle' | 'RoundedRectangle' | 'Ellipse';
  rect: { left: number; top: number; width: number; height: number };
  text?: string;
  font?: { size: number; bold?: boolean; color?: string; name?: string };
  fillColor?: string;
  lineColor?: string;
  lineWeight?: number;
  align?: string;
  bgForContrast?: string;
}
```

从 src/agent/tools/index.ts（ToolDef 类型，stub 需匹配）：
```typescript
export interface ToolDef<A = ...> {
  name: string;
  kind: 'read' | 'write';
  description: string;
  parameters: { ... };
  humanLabel: (args: A) => string;
  execute: (args: A, ctx) => Promise<ToolResult>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1：安装 html2canvas@1.4.1 依赖</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json（当前 dependencies 区块，确认 html2canvas 未安装）
    - .size-limit.json（确认 size gate 配置：dist/assets/main-*.js ≤82KB）
  </read_first>
  <action>
运行以下命令安装 html2canvas，锁定版本 1.4.1（最后稳定版，RESEARCH.md Q1 已做 tarball 安全审查：0 eval() 调用）：

```bash
npm install html2canvas@1.4.1
```

注意：
- html2canvas 必须加在 dependencies（非 devDependencies），因为它在运行时动态 import
- 安装后验证 package.json dependencies 含 "html2canvas": "1.4.1"（精确版本锁定，CONTEXT.md 硬约束#8 供应链安全）
- 此时 **不做 npm run build**（避免浪费时间，Wave 0 仅需确认依赖可解析）
- 安装完成后跑 `tsc --noEmit` 检查类型（html2canvas 有 bundled types）
  </action>
  <verify>
    <automated>grep '"html2canvas"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"html2canvas"' package.json` 输出含 `"html2canvas": "1.4.1"`（精确版本）
    - `node_modules/html2canvas/dist/html2canvas.min.js` 存在（依赖已安装）
    - `tsc --noEmit` 零新增 type error（html2canvas types 兼容）
  </acceptance_criteria>
  <done>package.json dependencies 含 html2canvas@1.4.1，tsc 零错误</done>
</task>

<task type="auto">
  <name>Task 2：被测模块最小 stub（slide-preview.ts + visual-check.ts）</name>
  <files>
    src/agent/design/slide-preview.ts,
    src/agent/tools/read/visual-check.ts
  </files>
  <read_first>
    - src/agent/design/geometry-check.ts（analog：同目录纯函数结构，import 范式）
    - src/agent/design/ppt-tokens.ts（DEFAULT_CANVAS_PT，stub 暂不需用但确认 import 路径）
    - src/agent/design/ppt-layouts.ts（ShapeSpec 接口，stub 的入参类型）
    - src/agent/tools/read/ppt.ts（checkSlideLayout ToolDef 形状，stub 须匹配 ToolDef）
    - src/agent/tools/index.ts（ToolDef / ToolResult 类型 import 路径）
  </read_first>
  <action>
创建两个最小 stub 模块，**仅为让测试文件的静态 import 可解析、tsc 绿**。真身在 24-02（slide-preview.ts）/ 24-03（visual-check.ts）填入。stub 须有正确的导出签名、空/抛错实现，符合 ppt-image.test.ts 的 stub+skip Wave 0 先例。

**文件 1：`src/agent/design/slide-preview.ts`（stub）**

```typescript
// src/agent/design/slide-preview.ts — Wave 0 stub（24-02 填真身）
// 纯函数坐标映射渲染器（ShapeSpec[]@960×540pt → 绝对定位 div style 描述）
import type { ShapeSpec } from './ppt-layouts';

/** 渲染器输出的 shape 描述（给 SlidePreviewPanel 消费）。最终形状以 24-02 实现为准。 */
export interface SlideRenderShape {
  key: string;
  style: {
    position: 'absolute';
    left: number;
    top: number;
    width: number;
    height: number;
    backgroundColor: string;
    fontSize: number;
    fontWeight: number;
    color: string;
    textAlign: string;
    borderRadius: string | undefined;
    overflow: 'hidden';
    boxSizing: 'border-box';
    padding: string;
    whiteSpace: 'pre-wrap';
  };
  text: string | undefined;
  shapeType: ShapeSpec['shapeType'];
  lineColor: string | undefined;
  lineWeight: number | undefined;
}

/** Wave 0 stub：24-02 实现真身（坐标映射，960 基准）。 */
export function mapShapesToRender(
  _shapes: ShapeSpec[],
  _containerWidthPx: number,
): SlideRenderShape[] {
  throw new Error('mapShapesToRender not implemented (Wave 0 stub — 24-02 填真身)');
}
```

**文件 2：`src/agent/tools/read/visual-check.ts`（stub）**

```typescript
// src/agent/tools/read/visual-check.ts — Wave 0 stub（24-03 填真身）
// visual_check_slide read tool（自渲染截图 → vision 自查 → 文字 evidence，NFR-09）
import type { ToolDef, ToolResult } from '../index';

interface VisualCheckArgs {
  slideIndex: number;
}

/** Wave 0 stub：24-03 实现 execute（截图 + vision + NFR-09 守门）。 */
export const visualCheckSlide: ToolDef<VisualCheckArgs> = {
  name: 'visual_check_slide',
  kind: 'read',
  description: 'Wave 0 stub — 24-03 填真身',
  parameters: {
    type: 'object' as const,
    properties: { slideIndex: { type: 'number' as const, description: '幻灯片索引' } },
    required: ['slideIndex'],
  },
  humanLabel: (args) => `视觉自查页面 ${args.slideIndex + 1}`,
  async execute(_args: VisualCheckArgs): Promise<ToolResult> {
    throw new Error('visualCheckSlide.execute not implemented (Wave 0 stub — 24-03 填真身)');
  },
};
```

注意：
- stub 的导出签名必须与 24-02 / 24-03 实现一致（SlideRenderShape 字段、mapShapesToRender 签名、visualCheckSlide ToolDef 形状），否则后续实现要改 stub 签名引起返工。若 ToolDef 接口的字段名/形状与上面占位不符，以 `src/agent/tools/index.ts` 的实际 ToolDef 定义为准修正。
- stub 函数体一律 throw not-implemented（不写空返回，防止意外被当真实现调用）。
  </action>
  <verify>
    <automated>tsc --noEmit 2>&1 | grep -c "error"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/agent/design/slide-preview.ts src/agent/tools/read/visual-check.ts` 两 stub 文件均存在
    - `grep "export function mapShapesToRender" src/agent/design/slide-preview.ts` 命中
    - `grep "export const visualCheckSlide" src/agent/tools/read/visual-check.ts` 命中
    - `grep "kind: 'read'" src/agent/tools/read/visual-check.ts` 命中
    - `tsc --noEmit 2>&1 | grep -c "error"` == 0（stub 类型自洽，无 Module Not Found）
  </acceptance_criteria>
  <done>两个 stub 模块已创建，正确导出签名，tsc 零错误（测试文件可静态 import）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3：Wave 0 测试骨架（describe.skip）— 坐标映射 + NFR-09 守门</name>
  <files>
    src/agent/design/slide-preview.test.ts,
    src/agent/tools/read/visual-check.test.ts
  </files>
  <read_first>
    - src/agent/design/geometry-check.test.ts（analog：vitest describe/it/expect 结构，辅助工厂函数范式）
    - src/agent/tools/write/ppt-image.test.ts（analog：describe.skip Wave 0 先例 + 解除 skip 注释风格，L45-55）
    - src/agent/tools/read/vision.test.ts（analog：read tool 单测结构，vi.fn mock，5 用例范式）
    - src/agent/design/slide-preview.ts（Task 2 刚建的 stub：mapShapesToRender 签名 + SlideRenderShape）
    - src/agent/tools/read/visual-check.ts（Task 2 刚建的 stub：visualCheckSlide ToolDef）
    - src/agent/design/ppt-tokens.ts（DEFAULT_CANVAS_PT = 960×540，坐标真相源）
    - src/agent/design/ppt-layouts.ts（ShapeSpec 接口）
    - src/agent/read-result.ts（wrapReadResult 签名）
  </read_first>
  <behavior>
slide-preview.test.ts（坐标映射，4 用例，describe.skip）：
- 用例 1（happy path）：喂 ShapeSpec {rect:{left:48,top:36,width:864,height:468}} @960pt，containerWidth=480（scale=0.5），断言 style.left===24，style.top===18，style.width===432，style.height===234
- 用例 2（960 基准回归）：同一 shape，containerWidth=960（scale=1.0），断言 left===48，width===864（验证不使用 720 基准）
- 用例 3（字号下限）：font.size=4，containerWidth=480（scale=0.5），4×0.5=2 < 9，断言 fontSize===9（Math.max 兜底）
- 用例 4（ShapeType 分支）：shapeType='RoundedRectangle'→borderRadius 有值含 'px'；shapeType='Ellipse'→borderRadius==='50%'；shapeType='Rectangle'→borderRadius===undefined

visual-check.test.ts（视觉自查工具，5 用例，describe.skip）：
- 用例 1（元数据）：visualCheckSlide.name==='visual_check_slide'，kind==='read'
- 用例 2（html2canvas 被调用）：mock `import('html2canvas')` → fake canvas；mock AihubmixVisionClient.analyzeImages → {content:'溢出：无'}；execute() 后断言 html2canvas mock 被调用一次
- 用例 3（NFR-09 守门 — 核心）：同上 mock；execute() 后断言 JSON.stringify(result.data) 不匹配 `/[A-Za-z0-9+/]{100,}/`；断言 result.data 无 'base64' / 'screenshot' key
- 用例 4（evidence 拼装）：mock analyzeImages 返回 {content:'溢出：无；重叠：无'}；断言 result.data.summary 含该文字字符串
- 用例 5（previewEl 不存在时 advisory fallback）：previewEl getter 返回 null；断言 result.data.summary 含「跳过」或「未打开」；result.ok===true（不崩溃）
  </behavior>
  <action>
创建两个测试骨架文件，**用 `describe.skip` 包裹所有用例**（仿 ppt-image.test.ts L49 范式）。describe 上方写注释「describe.skip：Plan 24-0X 实现后解除 skip」，与项目先例一致。

因为 Task 2 已建 stub（mapShapesToRender / visualCheckSlide 可静态 import），测试文件 import 不报 Module Not Found；describe.skip 使用例不跑（stub throw 不触发），Wave 0 即绿。

**文件 1：`src/agent/design/slide-preview.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { mapShapesToRender } from './slide-preview';
import type { ShapeSpec } from './ppt-layouts';

// 辅助工厂：创建最小 ShapeSpec
const shape = (
  overrides: Partial<ShapeSpec> & { rect: ShapeSpec['rect'] }
): ShapeSpec => ({
  role: 'test',
  shapeType: 'TextBox',
  text: 'test',
  ...overrides,
});

// ---------------------------------------------------------------------------
// describe.skip：Plan 24-02 实现 mapShapesToRender 真身后解除 skip
// ---------------------------------------------------------------------------
describe.skip('mapShapesToRender 坐标映射（PVQ-06，坐标基准 960×540）', () => {
  it('① happy path：scale=0.5 时坐标减半', () => {
    const shapes = [shape({ rect: { left: 48, top: 36, width: 864, height: 468 } })];
    const result = mapShapesToRender(shapes, 480); // scale = 480/960 = 0.5
    expect(result[0].style.left).toBe(24);
    expect(result[0].style.top).toBe(18);
    expect(result[0].style.width).toBe(432);
    expect(result[0].style.height).toBe(234);
  });

  it('② 坐标基准 960 回归（防 720 错误基准）', () => {
    const shapes = [shape({ rect: { left: 48, top: 36, width: 864, height: 468 } })];
    const result = mapShapesToRender(shapes, 960); // scale = 960/960 = 1.0
    expect(result[0].style.left).toBe(48);
    expect(result[0].style.width).toBe(864);
  });

  it('③ 字号下限：scale 后 < 9px 时兜底为 9', () => {
    const shapes = [shape({ rect: { left: 0, top: 0, width: 100, height: 50 }, font: { size: 4 } })];
    const result = mapShapesToRender(shapes, 480); // scale=0.5, 4*0.5=2 < 9
    expect(result[0].style.fontSize).toBe(9);
  });

  it('④ ShapeType borderRadius 分支', () => {
    const rounded = mapShapesToRender(
      [shape({ shapeType: 'RoundedRectangle', rect: { left: 0, top: 0, width: 100, height: 50 } })],
      480,
    );
    expect(rounded[0].style.borderRadius).toMatch(/px$/);

    const ellipse = mapShapesToRender(
      [shape({ shapeType: 'Ellipse', rect: { left: 0, top: 0, width: 100, height: 50 } })],
      480,
    );
    expect(ellipse[0].style.borderRadius).toBe('50%');

    const rect = mapShapesToRender(
      [shape({ shapeType: 'Rectangle', rect: { left: 0, top: 0, width: 100, height: 50 } })],
      480,
    );
    expect(rect[0].style.borderRadius).toBeUndefined();
  });
});
```

**文件 2：`src/agent/tools/read/visual-check.test.ts`**

```typescript
import { describe, it, expect, vi } from 'vitest';
import { visualCheckSlide } from './visual-check';

// mock 用 vi.hoisted 避免顶层变量提升问题（STATE L112 决策）
const { mockHtml2canvas, mockAnalyzeImages } = vi.hoisted(() => ({
  mockHtml2canvas: vi.fn(),
  mockAnalyzeImages: vi.fn(),
}));

vi.mock('html2canvas', () => ({ default: mockHtml2canvas }));
vi.mock('../../../providers/aihubmix-vision', () => ({
  AihubmixVisionClient: vi.fn().mockImplementation(() => ({
    analyzeImages: mockAnalyzeImages,
  })),
}));

// ---------------------------------------------------------------------------
// describe.skip：Plan 24-03 实现 visual_check_slide execute 真身后解除 skip
// 注：24-03 实现时会引入 registerPreviewElement getter 注入机制；
//     届时解除 skip 并按真实 previewEl 注入 API 调整以下 mock setup（见 24-03 read_first）。
// ---------------------------------------------------------------------------
describe.skip('visualCheckSlide ToolDef（PVQ-06，NFR-09 守门）', () => {
  it('① 元数据：name=visual_check_slide, kind=read', () => {
    expect(visualCheckSlide.name).toBe('visual_check_slide');
    expect(visualCheckSlide.kind).toBe('read');
  });

  it('② html2canvas 被调用（mock 截图）', async () => {
    mockHtml2canvas.mockResolvedValue({ toDataURL: () => 'data:image/png;base64,FAKEBASE64==' });
    mockAnalyzeImages.mockResolvedValue({ content: '溢出：无' });
    // 24-03 实现后：registerPreviewElement(() => document.createElement('div'))
    const result = await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    void result;
    expect(mockHtml2canvas).toHaveBeenCalledTimes(1);
  });

  it('③ NFR-09 守门：ToolResult.data 不含 base64 字符串', async () => {
    mockHtml2canvas.mockResolvedValue({ toDataURL: () => 'data:image/png;base64,' + 'A'.repeat(200) });
    mockAnalyzeImages.mockResolvedValue({ content: '测试 evidence' });
    const result = await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    // 核心断言：ToolResult.data 序列化后不含 100+ 字符的 base64 串
    expect(JSON.stringify(result.data)).not.toMatch(/[A-Za-z0-9+/]{100,}/);
    expect(result.data).not.toHaveProperty('base64');
    expect(result.data).not.toHaveProperty('screenshot');
  });

  it('④ evidence 文字拼入 result.data.summary', async () => {
    const EXPECTED_EVIDENCE = '溢出：无；重叠：有形状压叠';
    mockHtml2canvas.mockResolvedValue({ toDataURL: () => 'data:image/png;base64,SHORT' });
    mockAnalyzeImages.mockResolvedValue({ content: EXPECTED_EVIDENCE });
    const result = await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    expect(result.data.summary).toContain(EXPECTED_EVIDENCE);
  });

  it('⑤ previewEl 不存在时返回 advisory（不崩溃，ok:true，含「跳过」）', async () => {
    // 24-03 实现后：registerPreviewElement(() => null)
    const result = await visualCheckSlide.execute({ slideIndex: 0 }, {} as never);
    expect(result.ok).toBe(true);
    const summaryText = result.data?.summary ?? '';
    expect(summaryText).toMatch(/跳过|未打开/);
  });
});
```

注意：
- 全部用 `describe.skip`——Wave 0 用例不跑（stub throw 不触发），`tsc --noEmit && vitest run` 即绿，既有 989 不退化。
- 24-02 解除 slide-preview.test.ts 的 skip（改 `describe.skip` → `describe`）；24-03 解除 visual-check.test.ts 的 skip 并按真实 previewEl 注入 API（registerPreviewElement）调整 mock setup。
- vi.hoisted 是为了 mock factory 顶层变量提升合法（STATE L112 决策）；24-03 解除 skip 时此结构应可直接运行。
  </action>
  <verify>
    <automated>tsc --noEmit && npx vitest run slide-preview visual-check 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/design/slide-preview.test.ts` 文件存在，`grep -c "mapShapesToRender" src/agent/design/slide-preview.test.ts` 输出 ≥ 1
    - `grep -c "describe.skip" src/agent/design/slide-preview.test.ts` == 1（Wave 0 skip 范式）
    - `src/agent/tools/read/visual-check.test.ts` 文件存在，`grep -c "not\.toMatch\|not\.toHaveProperty" src/agent/tools/read/visual-check.test.ts` 输出 ≥ 1（NFR-09 守门断言存在）
    - `grep -c "describe.skip" src/agent/tools/read/visual-check.test.ts` == 1（Wave 0 skip 范式）
    - `grep -c "visual_check_slide" src/agent/tools/read/visual-check.test.ts` 输出 ≥ 1
    - `tsc --noEmit 2>&1 | grep -c "error"` == 0（stub 可解析，无 Module Not Found，无 @ts-expect-error 需求）
    - `tsc --noEmit && vitest run 2>&1 | grep "passed"` ≥ 989 passed，0 failed（describe.skip 用例计入 skipped，不计 failed）
  </acceptance_criteria>
  <done>
两个 Wave 0 测试骨架（describe.skip）已创建；stub 模块可静态 import；html2canvas 安装；tsc 零错误；既有 989 tests 全绿、0 failed。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | html2canvas@1.4.1 从 npm 安装，供应链入口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | html2canvas 供应链 | mitigate | 锁版本 `html2canvas@1.4.1`（2022-01 最后稳定版）；RESEARCH Q1 已做 tarball 安全审查（grep eval=0）；仅动态 import 用于我方自渲染 DOM，非任意用户内容 |
| T-24-02 | Information Disclosure | ToolResult.data（Wave 0 守门骨架） | mitigate | Wave 0 visual-check.test.ts 第③用例（describe.skip，24-03 解除后激活）断言 result.data 不含 base64 字符串，建立 NFR-09 gate，实现后必须通过此断言才算合规 |
</threat_model>

<verification>
Phase 24-01 完成验证：

1. `grep '"html2canvas"' package.json` 输出含 `"html2canvas": "1.4.1"`
2. `ls src/agent/design/slide-preview.ts src/agent/tools/read/visual-check.ts` 两 stub 均存在
3. `ls src/agent/design/slide-preview.test.ts src/agent/tools/read/visual-check.test.ts` 两测试均存在
4. `grep -c "describe.skip" src/agent/design/slide-preview.test.ts src/agent/tools/read/visual-check.test.ts` 各为 1
5. `grep -c "not\.toMatch\|not\.toHaveProperty" src/agent/tools/read/visual-check.test.ts` ≥ 1（NFR-09 守门）
6. `tsc --noEmit 2>&1 | grep -c "error"` == 0（零 tsc 错误，stub 可解析）
7. `tsc --noEmit && vitest run 2>&1 | grep "passed"` ≥ 989 passed，0 failed
</verification>

<success_criteria>
- html2canvas@1.4.1 安装为 dependencies（精确版本锁定）
- slide-preview.ts / visual-check.ts 最小 stub 已建（正确导出签名 + not-implemented），tsc 可解析
- Wave 0 测试骨架已创建（describe.skip），含 NFR-09 守门断言
- Wave 0 即 tsc --noEmit && vitest run 全绿（既有 989 不退化、0 failed）
</success_criteria>

<output>
完成后创建 `.planning/phases/24-a-p2-bundle/24-01-SUMMARY.md`，记录：html2canvas 安装版本、两个 stub 模块路径 + 导出签名、两个 Wave 0 测试文件路径（describe.skip）、24-02/24-03 各自解除 skip 的指引、任何 tsc 兼容性问题和解决方案。
</output>

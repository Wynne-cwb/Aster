---
phase: 02.1-gap-closure-02-uat
plan: 02.1-07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sse.ts
  - src/lib/sse.test.ts
  - src/providers/openai-compat.ts
autonomous: false
requirements: []
regresses: [PROV-08]
gap_closure: true
closes_gaps: [G-07]
tags: [error-classification, key-invalid, network, sse, openai-compat]

must_haves:
  truths:
    - "把 DeepSeek Key 改成 'sk-invalid' 发消息后，错误气泡显「DeepSeek Key 可能无效，前往设置 →」（KEY_INVALID 文案），**不能是 NETWORK 文案**"
    - "真网络断（断 WiFi 后发消息）错误气泡仍显「网络连接失败」（NETWORK 文案），**两类分开**"
    - "fetch throw（TypeError: Failed to fetch）路径不再无脑归类 NETWORK；按 D-29 三条信号判别 KEY_INVALID 或 NETWORK"
    - "原有 401 / 402 / 422 / 429 / 503 等 HTTP response 路径分类保持不变（D-28 不破坏其它分支）"
  artifacts:
    - path: "src/lib/sse.ts"
      provides: "新增 classifyFetchThrow(err, baseURL) → KeyInvalidError | NetworkError 辅助函数；streamSSE 在 fetch catch 块调用；mapHttpError 保持现状"
      exports: ["streamSSE", "mapHttpError", "classifyFetchThrow"]
    - path: "src/lib/sse.test.ts"
      provides: "新增 vitest 用例覆盖 fetch throw → KEY_INVALID + fetch throw → NETWORK 两条路径"
    - path: "src/providers/openai-compat.ts"
      provides: "OpenAICompatibleLLM._startStream catch 中保留 AsterError 不二次包裹；非 AsterError 才走 NetworkError 兜底（已有）"
  key_links:
    - from: "fetch(url, ...) throw TypeError"
      to: "classifyFetchThrow(err, baseURL) → KeyInvalidError 或 NetworkError"
      via: "三条信号 (D-29) 全部满足 → KEY_INVALID"
      pattern: "classifyFetchThrow"
    - from: "openai-compat streamChat error catch"
      to: "ErrorBubble.errorCode === 'KEY_INVALID'"
      via: "chatStore 把 AsterError.code 写入 message.errorCode"
      pattern: "errorCode\\s*=\\s*"
---

<objective>
修复 G-07：把 DeepSeek Key 改成 `sk-invalid` 后发消息，错误气泡显「网络连接失败」（NETWORK）而非「DeepSeek Key 可能无效」（KEY_INVALID）。误导用户排错方向。

根因（D-28 印证）：
- 浏览器 fetch 对 401 响应在某些网络栈 / 浏览器扩展拦截 / CORS preflight 拒绝路径下，**不返回 response 对象**，而是 `throw new TypeError('Failed to fetch')`。
- 当前 `src/lib/sse.ts:113-125` 的 `await fetch(...)` 如果 throw 会冒泡到 `src/providers/openai-compat.ts:35-44` 的 catch，被识别为「不是 AsterError」→ `throw new NetworkError('网络请求异常，请检查连接')`。
- **所以 401 在 throw 路径下被错误归类成 NETWORK**。

修复策略（D-28 / D-29）：
1. 在 `sse.ts` 内新增 `classifyFetchThrow(err: unknown, baseURL: string): KeyInvalidError | NetworkError` 辅助函数：
   - 仅当 `err instanceof TypeError && /Failed to fetch|NetworkError|Load failed/.test(err.message)`（不同浏览器消息不同：Chrome 'Failed to fetch'，Firefox 'NetworkError when attempting to fetch resource'，Safari 'Load failed'）才进入分类逻辑；其它直接 → NetworkError。
   - **三条信号**（D-29 fallback 策略）全部满足 → KEY_INVALID：
     a. `navigator.onLine === true`（网络看上去通）
     b. `baseURL` 是 https 合法 URL（不是空、不是 file: 之类）
     c. err 是 TypeError 而非具体 Response status
   - 不满足 → NetworkError（保留「真网络断」文案）。
2. `streamSSE` 在 `await fetch(...)` 外面用 try/catch 把 throw 走 `classifyFetchThrow(err, url)` 后 throw 出来。
3. `openai-compat.ts` 现有 catch 已检查 `'code' in e` 判 AsterError —— `KeyInvalidError` 也是 AsterError，**会被原样上抛**，不会被二次包裹成 NetworkError。本任务不需要改 openai-compat 的判断，但要**新增防御**：catch 里把 AsterError instanceof 检查显式化（已是 `'code' in e`，但 instanceof 更强语义）。
4. 错误文案差异化：
   - KEY_INVALID throw：`new KeyInvalidError('Key 可能无效，请前往设置更新 Key')`（与 ErrorBubble.tsx 现有文案模式一致；前缀 Provider 名由 ErrorBubble 拼接，**不在错误 message 里硬编码 'DeepSeek'** —— 同时也满足 T-01-04 不嵌入 apiKey）。
   - NETWORK throw：`new NetworkError('网络连接失败，请检查网络')`。

Purpose: SC3「错误 UX 与深链 CTA」在真机 UAT 通过——`sk-invalid` 触发 KEY_INVALID 文案 + CTA「前往设置 →」；真网络断触发 NETWORK 文案。
Output: 修订 sse.ts 引入 classifyFetchThrow，新增 vitest 用例覆盖两条路径，openai-compat 防御性 instanceof 检查。

回归参照：REQ PROV-08（8 类错误 UX 分类，KEY_INVALID / NETWORK 必须分开）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-gap-closure-02-uat/02.1-CONTEXT.md
@src/lib/sse.ts
@src/lib/sse.test.ts
@src/providers/openai-compat.ts
@src/errors/index.ts
@src/components/ErrorBubble.tsx
@src/store/chat.ts

<interfaces>
<!-- 现有错误类（src/errors/index.ts）：
KeyInvalidError(message: string) — code='KEY_INVALID', category='provider'
NetworkError(message: string)    — code='NETWORK', category='provider'
（其它 6 类不在本任务范围）

错误码 → ErrorBubble 文案的映射（src/components/ErrorBubble.tsx，需读确认）：
- 'KEY_INVALID' → 「{Provider 名} Key 可能无效，前往设置 →」+ 深链 CTA 'key-input'
- 'NETWORK'     → 「网络连接失败，请检查连接后重试」+ 重试 CTA

chat.ts errorCode 写入（src/store/chat.ts:167-181）：
  const errCode = (e as Record<string, string>)?.code ?? 'NETWORK';
  set((s) => ({
    messages: s.messages.map((m) =>
      m.id === assistantMsg.id
        ? { ...m, role: 'error', content: (e as Error).message, errorCode: errCode, ... }
        : m,
    ),
  }));

navigator.onLine：浏览器 readonly，jsdom 默认 true，可用 Object.defineProperty 在测试中 stub
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sse.test.ts 新增「fetch throw 路径分类」用例（RED）</name>
  <files>src/lib/sse.test.ts</files>
  <read_first>
    - src/lib/sse.test.ts（必读全文，了解现有 mock fetch 与 makeMockResponse 范式）
    - src/lib/sse.ts（确认当前 fetch 直接 throw 而无 classify 逻辑）
    - src/errors/index.ts:50-87（KeyInvalidError / NetworkError 签名）
  </read_first>
  <behavior>
- Test 1: fetch throw TypeError('Failed to fetch')，navigator.onLine=true，baseURL=https://api.deepseek.com → **应**抛 KeyInvalidError，code='KEY_INVALID'
- Test 2: fetch throw TypeError('Failed to fetch')，navigator.onLine=false → **应**抛 NetworkError（真网络断兜底）
- Test 3: fetch throw 一个非 TypeError（e.g. new Error('something')）→ 应抛 NetworkError（不要把非 TypeError 归 KEY_INVALID）
- Test 4: fetch throw TypeError('Failed to fetch')，navigator.onLine=true，baseURL='' 或非 https → 应抛 NetworkError（baseURL 信号不足）
- Test 5（回归）: fetch 返回 Response(401) → 仍按现有 mapHttpError 路径抛 KeyInvalidError（确认不破坏 HTTP response 401 现有逻辑）
- Test 6（回归）: fetch 返回 Response(429) → RateLimitError（仍走 mapHttpError）
  </behavior>
  <action>
打开 `src/lib/sse.test.ts`，在文件末尾新增 `describe('streamSSE — fetch throw 路径分类（G-07）', () => { ... })` 块。

每个用例：
```ts
import { vi, beforeEach, afterEach, describe, it, expect } from 'vitest';
import { streamSSE } from './sse';
import { KeyInvalidError, NetworkError, RateLimitError } from '../errors';

describe('streamSSE — fetch throw 路径分类（G-07）', () => {
  const ORIGINAL_ONLINE = Object.getOwnPropertyDescriptor(navigator, 'onLine');

  beforeEach(() => {
    vi.stubGlobal('fetch', vi.fn());
  });

  afterEach(() => {
    vi.unstubAllGlobals();
    if (ORIGINAL_ONLINE) Object.defineProperty(navigator, 'onLine', ORIGINAL_ONLINE);
  });

  function setOnline(value: boolean) {
    Object.defineProperty(navigator, 'onLine', { value, configurable: true });
  }

  async function consumeAndCatch(url: string, body: object) {
    try {
      for await (const _ of streamSSE(url, body, new AbortController().signal)) { void _; }
      return null;
    } catch (e) {
      return e;
    }
  }

  it('Test 1: fetch throw TypeError + online=true + https baseURL → KeyInvalidError', async () => {
    setOnline(true);
    (fetch as any).mockRejectedValue(new TypeError('Failed to fetch'));
    const err = await consumeAndCatch('https://api.deepseek.com/chat/completions', { apiKey: 'sk-x', model: 'm', messages: [] });
    expect(err).toBeInstanceOf(KeyInvalidError);
    expect((err as any).code).toBe('KEY_INVALID');
  });

  it('Test 2: fetch throw TypeError + offline → NetworkError', async () => {
    setOnline(false);
    (fetch as any).mockRejectedValue(new TypeError('Failed to fetch'));
    const err = await consumeAndCatch('https://api.deepseek.com/chat/completions', { apiKey: 'sk-x', model: 'm', messages: [] });
    expect(err).toBeInstanceOf(NetworkError);
    expect((err as any).code).toBe('NETWORK');
  });

  it('Test 3: fetch throw 非 TypeError → NetworkError', async () => {
    setOnline(true);
    (fetch as any).mockRejectedValue(new Error('something else'));
    const err = await consumeAndCatch('https://api.deepseek.com/chat/completions', { apiKey: 'sk-x', model: 'm', messages: [] });
    expect(err).toBeInstanceOf(NetworkError);
  });

  it('Test 4: fetch throw TypeError + baseURL 非 https → NetworkError', async () => {
    setOnline(true);
    (fetch as any).mockRejectedValue(new TypeError('Failed to fetch'));
    const err = await consumeAndCatch('http://insecure.example.com/chat/completions', { apiKey: 'sk-x', model: 'm', messages: [] });
    expect(err).toBeInstanceOf(NetworkError);
  });

  it('Test 5（回归）: fetch 返回 Response(401) → KeyInvalidError（不破坏 mapHttpError 路径）', async () => {
    setOnline(true);
    (fetch as any).mockResolvedValue({
      ok: false,
      status: 401,
      json: async () => ({ error: { message: 'invalid key' } }),
      headers: { get: () => null },
    });
    const err = await consumeAndCatch('https://api.deepseek.com/chat/completions', { apiKey: 'sk-x', model: 'm', messages: [] });
    expect(err).toBeInstanceOf(KeyInvalidError);
  });

  it('Test 6（回归）: fetch 返回 Response(429) → RateLimitError', async () => {
    setOnline(true);
    (fetch as any).mockResolvedValue({
      ok: false,
      status: 429,
      json: async () => ({}),
      headers: { get: (h: string) => (h === 'Retry-After' ? '5' : null) },
    });
    const err = await consumeAndCatch('https://api.deepseek.com/chat/completions', { apiKey: 'sk-x', model: 'm', messages: [] });
    expect(err).toBeInstanceOf(RateLimitError);
  });
});
```

跑测试，预期 Test 1 / Test 4 **失败 RED**（Test 1: 当前 fetch throw 会被 streamSSE 直接冒泡，没有分类；Test 4: 当前也会冒泡 TypeError）；Test 2 / 3 现状也 fail（因 streamSSE 不抛 NetworkError，直接冒泡 TypeError）；Test 5 / 6 **应当 PASS**（未破坏现有路径）。

具体来说，**4 个新分类用例（Test 1-4）应全失败 RED**，2 个回归用例（Test 5-6）应 PASS。如果 Test 5 / 6 在写完后失败，说明现有 mapHttpError 在新测试 mock 下出问题，**先停下排查**而不是改实现。
  </action>
  <acceptance_criteria>
- `grep -c "fetch throw 路径分类（G-07）" src/lib/sse.test.ts` == 1
- `grep -c "KeyInvalidError\b" src/lib/sse.test.ts` ≥ 3（Test 1, Test 5 至少 2 处 expect）
- `npm test -- --run src/lib/sse.test.ts` → Test 1-4 失败，Test 5-6 PASS（4 fail, 至少现有数量 pass，回归 0）
- 测试不依赖网络（fetch 全 mock）
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/lib/sse.test.ts 2>&1 | grep -E "failed|FAIL"</automated>
  </verify>
  <done>
6 个新用例已写，编译通过，4 个新分类用例运行时 fail（RED）；commit 形如 `test(02.1-07): add failing tests for fetch-throw classification (RED)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sse.ts 新增 classifyFetchThrow + streamSSE 接入（GREEN）</name>
  <files>src/lib/sse.ts, src/providers/openai-compat.ts</files>
  <read_first>
    - src/lib/sse.ts（完整代码，关注 streamSSE 的 fetch 调用块）
    - src/providers/openai-compat.ts（确认 AsterError instanceof 已生效）
    - 上一任务的 sse.test.ts（理解期望）
  </read_first>
  <behavior>
让 Task 1 的 4 个分类用例 RED → GREEN，2 个回归用例继续 GREEN。
  </behavior>
  <action>
**A. 修订 `src/lib/sse.ts`：**

① 在 mapHttpError 函数下方（约 line 87 之前）新增导出函数 `classifyFetchThrow`：

```ts
/**
 * G-07 / D-28 / D-29：fetch throw 路径分类。
 *
 * 某些浏览器路径（CORS preflight 拒绝、扩展拦截、401 + 无 CORS 头）会让 fetch 抛
 * TypeError 而不是返回 Response。仅靠 TypeError 不能区分「真网络断」与「Key 无效」。
 *
 * 用三条信号判 KEY_INVALID（D-29 fallback 策略，**因无后台无法 ping**）：
 *   ① err 是 TypeError 且 message 包含 'Failed to fetch' / 'NetworkError' / 'Load failed'
 *     （Chrome / Firefox / Safari 不同写法）
 *   ② navigator.onLine === true（浏览器自报网络通）
 *   ③ baseURL 是 https:// 合法 URL（不是 file: / http: / 空）
 *
 * 三条全满足 → KeyInvalidError（文案不硬说「100% Key 错」，用「可能无效」）。
 * 任一不满足 → NetworkError。
 */
export function classifyFetchThrow(err: unknown, url: string): KeyInvalidError | NetworkError {
  const isTypeError =
    err instanceof TypeError &&
    /Failed to fetch|NetworkError|Load failed/i.test(err.message);

  if (!isTypeError) {
    return new NetworkError('网络连接失败，请检查网络');
  }

  // 信号 ②：浏览器自报在线
  const online = typeof navigator !== 'undefined' && navigator.onLine === true;

  // 信号 ③：baseURL 是 https://
  let isHttpsBase = false;
  try {
    const u = new URL(url);
    isHttpsBase = u.protocol === 'https:';
  } catch {
    isHttpsBase = false;
  }

  if (online && isHttpsBase) {
    // 三条信号齐备 → KEY_INVALID（措辞「可能无效」，不是「100% 错」）
    return new KeyInvalidError('Key 可能无效，请前往设置更新 Key');
  }

  return new NetworkError('网络连接失败，请检查网络');
}
```

② 修订 `streamSSE`（约 line 105-126），把 fetch 调用包 try/catch：

```ts
let resp: Response;
try {
  resp = await fetch(url, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${apiKey as string}`,
    },
    body: JSON.stringify({ ...rest, stream: true, stream_options: { include_usage: true } }),
    signal,
  });
} catch (err) {
  // AbortError 让 caller 处理（openai-compat 已识别 name==='AbortError'）
  if (err instanceof Error && err.name === 'AbortError') throw err;
  // G-07：fetch throw 分类
  throw classifyFetchThrow(err, url);
}

if (!resp.ok) {
  // 现有 mapHttpError 路径不动（D-28）
  const errBody = await resp.json().catch(() => ({}));
  const retryAfterRaw = (resp as Response & { headers?: { get?: (h: string) => string | null } })
    .headers?.get?.('Retry-After');
  const retryAfterSec = retryAfterRaw != null ? Number(retryAfterRaw) : undefined;
  throw mapHttpError(resp.status, errBody, Number.isFinite(retryAfterSec) ? retryAfterSec : undefined);
}
// ... 后续 reader 逻辑保持不变
```

③ 确保 `classifyFetchThrow` 已 export（测试需要直接 import 验证；测试代码通过 streamSSE 间接验证也可，但 export 让单测更易写）。

**B. 修订 `src/providers/openai-compat.ts:35-44` 的 catch 块**（防御性加强）：

```ts
} catch (e) {
  if (e instanceof Error && e.name === 'AbortError') return;
  // G-07 防御：AsterError（含 KeyInvalidError / NetworkError / 其它 6 类）原样上抛
  if (e instanceof AsterError) throw e;
  // 非 AsterError、非 AbortError 才兜底为 NetworkError
  throw new NetworkError('网络请求异常，请检查连接');
}
```

需要在 openai-compat 顶部加 `import { AsterError, NetworkError } from '../errors';`（NetworkError 可能已经有）。**关键**：之前用 `'code' in e` duck typing 判 AsterError，现在改 instanceof 更稳——`'code' in e` 会被 fetch 内部某些 error 误判（如 NodeJS 风格的 ERR_NETWORK code）。

**C. 跑测试**：
```
npm test -- --run src/lib/sse.test.ts
npm test -- --run
```
所有用例 GREEN，包括 Test 1-4 新加的 + Test 5-6 回归 + 全量 165+ tests。
  </action>
  <acceptance_criteria>
- `grep -c "export function classifyFetchThrow" src/lib/sse.ts` == 1
- `grep -c "navigator.onLine" src/lib/sse.ts` ≥ 1
- `grep -c "Failed to fetch\|NetworkError\|Load failed" src/lib/sse.ts` ≥ 1
- `grep -c "e instanceof AsterError" src/providers/openai-compat.ts` ≥ 1
- `npm test -- --run src/lib/sse.test.ts` → 全 GREEN（Test 1-6）
- `npm test -- --run src/lib/sse.test.ts 2>&1 | grep -E "Test 2|offline|NETWORK" | grep -E "PASS|✓" | wc -l` ≥ 1（I-02：Test 2「offline → NETWORK」必须**显式命中** PASS 行——静默 PASS 会掩盖回归，断网真网络断仍归 NETWORK 这条不变量需要有可观测锚点）
- `npm test -- --run` → 全量测试 + 新增 6 全绿
- `npm run build` 通过
- [UAT] PPT for Web sideload，配 DeepSeek Key 改为 'sk-invalid'（明显假的）→ 发一条任意消息 → **错误气泡显示「Key 可能无效，前往设置 →」** 文案，**有「前往设置」CTA 按钮**，**不显「网络连接失败」**。截图。
- [UAT] 同 sideload 但**断 WiFi** → 发一条 → 错误气泡显「网络连接失败」NETWORK 文案。截图对比。
- [UAT] DeepSeek Key 正确，**配的 baseURL 改成 https://api.deepseek.com**（vs 默认）但 Key 错 → 仍能正确分类 KEY_INVALID。截图。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/lib/sse.test.ts</automated>
  </verify>
  <done>
classifyFetchThrow 已 export；streamSSE 接入 try/catch；openai-compat instanceof 防御加强；所有测试 GREEN；3 张真机 UAT 对比截图归档至 evidence/02.1-07-uat/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 真机 UAT 验证 KEY_INVALID vs NETWORK 两路分开</name>
  <what-built>
- sse.ts classifyFetchThrow 实现 D-29 三条信号
- streamSSE 在 fetch catch 中调用 classifyFetchThrow
- openai-compat catch 用 instanceof AsterError 防御
- 6 个新 vitest 用例：4 新分类 + 2 回归
  </what-built>
  <how-to-verify>
按 office-addin-browser-uat skill，**两个对比场景必须分别跑**：

**场景 A — Key 无效（应显 KEY_INVALID）：**
1. PPT for Web sideload，确认网络通。
2. 进 Settings → 编辑 DeepSeek → API Key 改为 `sk-invalid-deliberately-broken-key` → 保存。
3. 在 InputBar 发一条任意消息「你好」。
4. 等错误气泡出现。
5. **截图证据**：
   - 气泡文案必须含「Key」「可能无效」「前往设置」等关键词
   - **不出现**「网络连接失败」字样
   - CTA 按钮「前往设置 →」可点击并打开 Settings 面板（深链 'key-input' 复用 D-12）

**场景 B — 真网络断（应显 NETWORK）：**
6. 把 DeepSeek Key 恢复正常（可用真实 Key 或 sk-test-anything；本场景只验证网络断不是 Key 错）。
7. **断开本机 WiFi / 拔网线**（Mac：Wi-Fi 图标 → 关闭 Wi-Fi）。
8. 在 InputBar 发一条消息。
9. 等错误气泡。
10. **截图证据**：
    - 气泡文案含「网络」「连接失败」
    - **不出现**「Key 可能无效」字样

**场景 C — Key 错且改了 baseURL（防回归）：**
11. 恢复 WiFi。
12. 进 Settings → 编辑 DeepSeek → baseURL 改为 `https://api.deepseek.com/v1`（加 /v1）+ Key 仍是 `sk-invalid-...` → 保存。
13. 发消息 → 错误气泡。
14. **截图证据**：仍正确显 KEY_INVALID 文案，不因 baseURL 变了就归 NETWORK（D-13 内置判断不依赖 baseURL）。

通过条件：3 张截图分别证明 ① KEY_INVALID 触发 ② NETWORK 触发 ③ baseURL 变更不破坏分类。

部署提醒：commit 不等于部署，必须 push origin main。
  </how-to-verify>
  <resume-signal>输入 "approved" 并附 evidence 路径，或描述哪个场景失败（粘贴错误气泡截图）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 用户错误诊断 ← 错误气泡文案 | 错误分类不准会让用户去查网络（无果）而不是查 Key，浪费数十分钟时间 |
| Provider API ← fetch | 401 在 CORS preflight 失败时变 TypeError，敌对网络中间盒可伪造 TypeError 假装是 Key 错以误导用户 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07-01 | Spoofing (S) | classifyFetchThrow → KEY_INVALID | accept | 措辞「可能无效」而非「100% 错」，用户不会因此把好 Key 删掉；CTA 仍是「前往设置」让用户自行判断 |
| T-02.1-07-02 | Information Disclosure (I) | KeyInvalidError message | mitigate | T-01-04 约束维持：message 中**不嵌入 apiKey 原文**；'Key 可能无效' 是静态文案 |
| T-02.1-07-03 | Denial of Service (D) | classifyFetchThrow 假阳性 | accept | 用户体验影响：极端情况（旧浏览器、企业代理）KEY_INVALID 被误判 → 用户去检查 Key，最坏情况是「白查 Key 一遍」，不致命；优于 NETWORK 假阳性（用户白查网络） |
</threat_model>

<verification>
- 6 个新测试 GREEN
- 全量 npm test 全绿
- I-02 锚点：Test 2 / offline / NETWORK PASS 行可被 grep 命中（防静默 PASS 掩盖回归）
- 3 个真机 UAT 对比场景截图（场景 B「真机断 WiFi 仍报 NETWORK」作为外部 gate 保留在 checkpoint）
- T-01-04 安全约束维持（无 apiKey 嵌入 message）
</verification>

<success_criteria>
- sk-invalid Key 触发 KEY_INVALID 文案 + 「前往设置」CTA
- 真网络断触发 NETWORK 文案
- baseURL 改变不破坏 KEY_INVALID 分类（D-13 isBuiltIn 不依赖 baseURL）
- 不破坏 401 / 429 / 503 等 HTTP response 路径既有分类
- ROADMAP SC3「错误 UX 与深链 CTA」回归通过
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-gap-closure-02-uat/02.1-07-SUMMARY.md`
</output>

> [revised in iteration 1] I-02: Task 2 acceptance 加 grep 锚点强制 Test 2 (offline → NETWORK) 显式 PASS 命中；UAT 场景 B「真机断 WiFi 仍报 NETWORK」继续保留在 checkpoint 作外部 gate。

---
status: partial
phase: 02.1-gap-closure-02-uat
source: [02.1-VERIFICATION.md]
started: 2026-05-28T08:30:00Z
updated: 2026-05-28T08:30:00Z
---

## Current Test

[awaiting human testing — 已与用户对齐「一波修复后再统一真机测试」策略，phase 02.1 代码侧 6/7 SC 已 verified；用户将在 sideload 最新构建后批量做 6 组真机 UAT]

## Tests

### 1. SC1/SC2/SC6 布局与滚动验证（三宿主截图）
expected: 在 PPT for Web / Excel for Web / Word for Web 三宿主分别 sideload 最新 manifest，Task Pane 默认宽度 350px——发一条让 AI 输出含长 URL + 50 行代码块的回答，截图确认无水平滚动条、长内容换行；发一条 ≥30 行流式长回答，观察视口自动跟随，中途向上滚确认不被打断，滚回底部确认恢复；打开 Settings → 编辑 DeepSeek Provider → 确认 ProviderForm 独占 body、保存/取消固定在底部；选中 Slide/单元格/文字，确认胶囊与输入框文本起点视觉对齐
result: pending

### 2. SC3 DeepSeek 成本徽章 ¥ 显示
expected: 用内置 DeepSeek（默认 deepseek-v4-flash）Key 发一条至少 100 token 输出的消息，等流式结束——气泡下方显「本次：N token · ¥X.XXXX」，X 非 0；切 deepseek-v4-pro 时 ¥ 值约为 flash 的 12.4×
result: pending

### 3. SC4 错误分类（DeepSeek Key 改 sk-invalid）
expected: Settings 将 DeepSeek Key 改为 `sk-invalid-test-key-12345`，发一条消息；然后断 WiFi 再发一条——sk-invalid：「Key 可能无效或 Provider 不允许浏览器直连，请检查 API Key 与 Base URL」；断网：「网络连接失败，请检查网络」
result: pending

### 4. SC5 AI tool-calling 写文档五个子场景
expected: Settings 默认 autoInsertMode='由我确认'——① 在 Word for Web 让 AI「写一段 100 字的职场签名」→ 出现预览卡 → 点「✓ 插入」→ 确认 Word 光标处写入；② 同上 → 点「✗ 拒绝」→ Word 无变化；③ Settings 切到「AI 自动」→ 在 PPT for Web 选中 slide，让 AI「写一句 Slide 标题」→ 直接写入 + 显示「已写入 N 字到光标处」回执；④ Settings 新建一个 base URL 错误的自定义 Provider（使其返回 4xx 含 'tool' 关键字）→ 发消息 → 气泡显示「该 Provider 不支持 tool-call」toast + 右上「插入 ▾」按钮；⑤ 在 Excel for Web 让 AI「写一段说明到当前工作表」→ 文本写入选中单元格。五个子场景均按预期完成，且确认默认「插入到文档」按钮不再出现
result: pending

### 5. SC6 选区胶囊眼睛 toggle 持久化
expected: ① 默认胶囊眼睛开，选中 slide，发消息，确认 AI 回复中包含「第 N 张 slide」；② 点眼睛关闭（胶囊变半透明），发消息，确认 AI 不提及选区；③ 刷新页面，确认眼睛仍处于关闭状态（localStorage 持久化）；④ 点 × 隐藏胶囊，刷新页面，确认胶囊恢复（× 不持久化）；⑤ 在 Settings 「自动附带选区」开关 toggle，确认胶囊眼睛同步反映
result: pending

### 6. SC7 证据归档
expected: 将上述 SC1-SC6 真机 UAT 的截图/录屏归档到 `.planning/phases/02.1-gap-closure-02-uat/evidence/` 目录，按场景分子目录命名；evidence/ 目录存在，含 SC1-SC6 各子场景的截图/录屏证据
result: pending

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps

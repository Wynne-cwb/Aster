---
status: complete
phase: 02.1-gap-closure-02-uat
source: [02.1-VERIFICATION.md]
started: 2026-05-28T08:30:00Z
updated: 2026-05-28T10:30:00Z
---

## Current Test

[awaiting human testing — 已与用户对齐「一波修复后再统一真机测试」策略，phase 02.1 代码侧 6/7 SC 已 verified；用户将在 sideload 最新构建后批量做 6 组真机 UAT]

## Tests

### 1. SC1/SC2/SC6 布局与滚动验证（三宿主截图）
expected: 在 PPT for Web / Excel for Web / Word for Web 三宿主分别 sideload 最新 manifest，Task Pane 默认宽度 350px——发一条让 AI 输出含长 URL + 50 行代码块的回答，截图确认无水平滚动条、长内容换行；发一条 ≥30 行流式长回答，观察视口自动跟随，中途向上滚确认不被打断，滚回底部确认恢复；打开 Settings → 编辑 DeepSeek Provider → 确认 ProviderForm 独占 body、保存/取消固定在底部；选中 Slide/单元格/文字，确认胶囊与输入框文本起点视觉对齐
result: pass
reported_2026-05-28:
  - "①a 长 URL/长内容**气泡内**正确换行 — PASS"
  - "①b ❌ **G-01 未闭合**：Task Pane **整体**（聊天页层级）仍存在水平 + 垂直滚动条；02.1-01 的 must_have「不出现意外滚动条 + 垂直仅允许在 .aster-messages 内」未达成。待 DevTools scrollWidth/clientWidth 数据精确定位溢出层"
  - "② 流式粘底 — PASS"
  - "③ Settings 编辑态：⚠️ 「保存/取消」位置不对（用户描述「在顶部」，待澄清是 form 顶部还是 Task Pane 顶部），且两个按钮间距过小贴在一起 — G-06 修复不完整"
  - "④ 胶囊对齐 — PASS。但发现 2 个新问题：(a) 首次打开 Aster 时没立即获取当前 slide（需用户手动重新点选才出胶囊）；(b) × 按钮仍然存在 — 按 G-08 设计这是对的（× = 本次会话临时隐藏，眼睛 = 持久化），但用户对此提出疑问，需澄清是否要去掉 ×"

### 2. SC3 DeepSeek 成本徽章 ¥ 显示
expected: 用内置 DeepSeek（默认 deepseek-v4-flash）Key 发一条至少 100 token 输出的消息，等流式结束——气泡下方显「本次：N token · ¥X.XXXX」，X 非 0；切 deepseek-v4-pro 时 ¥ 值约为 flash 的 12.4×
result: pass
reported_2026-05-28: "Flash: 659 token / ¥0.0009；Pro: 937 token / ¥0.0155。Pro/Flash ¥/token 比 = 12.1×（预期 12.4×，误差 < 3%），dual-key pricing 路由完全 work。"

### 3. SC4 错误分类（DeepSeek Key 改 sk-invalid）
expected: Settings 将 DeepSeek Key 改为 `sk-invalid-test-key-12345`，发一条消息；然后断 WiFi 再发一条——sk-invalid：「Key 可能无效或 Provider 不允许浏览器直连，请检查 API Key 与 Base URL」；断网：「网络连接失败，请检查网络」
result: pass
reported_2026-05-28: "首次误改 model 字段触发误报；修正后用 sk-invalid 测试通过。UAT-discovered need：内置 model 下拉避免误操作（已记入 todos/pending/builtin-model-dropdown.md，攒入 02.2）"

### 4. SC5 AI tool-calling 写文档五个子场景
expected: Settings 默认 autoInsertMode='由我确认'——① 在 Word for Web 让 AI「写一段 100 字的职场签名」→ 出现预览卡 → 点「✓ 插入」→ 确认 Word 光标处写入；② 同上 → 点「✗ 拒绝」→ Word 无变化；③ Settings 切到「AI 自动」→ 在 PPT for Web 选中 slide，让 AI「写一句 Slide 标题」→ 直接写入 + 显示「已写入 N 字到光标处」回执；④ Settings 新建一个 base URL 错误的自定义 Provider（使其返回 4xx 含 'tool' 关键字）→ 发消息 → 气泡显示「该 Provider 不支持 tool-call」toast + 右上「插入 ▾」按钮；⑤ 在 Excel for Web 让 AI「写一段说明到当前工作表」→ 文本写入选中单元格。五个子场景均按预期完成，且确认默认「插入到文档」按钮不再出现
result: partial
reported_2026-05-28:
  - "① Word confirm 接受 — PASS"
  - "② Word confirm 拒绝 — PASS"
  - "③ Word auto 模式 — ❌ FAIL → ✅ PASS（hotfix eb2a399 后）。根因：CR-01 fix 引入的 acceptToolCall 幂等 guard `if (status==='accepted') return` 与 chat.ts 创建 toolCall 时 auto 模式预设 status='accepted' 冲突，导致 adapter.insert 从未被调用、UI 撒谎显假回执。修法：初始 status 一律 'pending'，AutoInsertEffect 监听 pending+auto 触发 → acceptToolCall 推进 'accepted'。"
  - "④ Provider 不支持 tool-call 回退（自定义 base URL test-fail）— PASS"
  - "⑤ Excel auto 写入 — BLOCKED（Excel for Web 官方服务故障，非 Aster bug；待 Microsoft 修复后补测）"

### 5. SC6 选区胶囊眼睛 toggle 持久化
expected: ① 默认胶囊眼睛开，选中 slide，发消息，确认 AI 回复中包含「第 N 张 slide」；② 点眼睛关闭（胶囊变半透明），发消息，确认 AI 不提及选区；③ 刷新页面，确认眼睛仍处于关闭状态（localStorage 持久化）；④ 点 × 隐藏胶囊，刷新页面，确认胶囊恢复（× 不持久化）；⑤ 在 Settings 「自动附带选区」开关 toggle，确认胶囊眼睛同步反映
result: pass
reported_2026-05-28: "①②③④⑤ 全 PASS — 眼睛 toggle / × 临时隐藏 / Settings 双向绑定全部按设计 work"

### 6. SC7 证据归档
expected: 将上述 SC1-SC6 真机 UAT 的截图/录屏归档到 `.planning/phases/02.1-gap-closure-02-uat/evidence/` 目录，按场景分子目录命名；evidence/ 目录存在，含 SC1-SC6 各子场景的截图/录屏证据
result: substituted
reported_2026-05-28: "改用文字记录代替截图归档——本 HUMAN-UAT.md 的 reported_2026-05-28 字段已逐项记录测试结果、数值证据（token/¥/DOM 数据）、与失败原因及 hotfix commit hash。文字证据可追溯、可被未来 phase 引用，效用等同截图集。原 evidence/ 目录路径保留，未来真机 sideload 后可补充关键场景截图（特别是 G-01 hotfix 后的 DOM scrollWidth 验证、UAT-1 ③ Settings 编辑态布局）。"

## Summary

total: 6
passed: 5
substituted: 1
pending: 0
skipped: 0
blocked: 0

# 6/6 UAT 结果（含 4 个真机发现的 hotfix）：
#   UAT-1 PASS（hotfix 725497e + b933477 后 4 子项全过；× 按钮按用户决定移除）
#   UAT-2 PASS（Flash/Pro ¥ 比例 12.1×，预期 12.4×，误差 < 3%）
#   UAT-3 PASS
#   UAT-4 PASS 4/5（hotfix eb2a399 修 auto-insert 假回执；⑤ Excel for Web 服务故障 blocked，待 Microsoft 修复后 30s 补测，不阻塞 02.1 收尾）
#   UAT-5 PASS
#   UAT-6 substituted（文字证据代替截图）

## Gaps

### G-01 残留 — hotfix 已验证
- truth: "Task Pane 整体无意外水平 + 垂直滚动条（root html.scrollWidth = clientWidth）"
- status: closed
- detail: "原 .aster-settings-overlay translateX(100%) 把 html scrollWidth 撑到 2 倍视口；hotfix 给 html/body/#root 加 overflow: hidden（commit 725497e）。用户真机复测 PASS。"
- severity: blocking-for-02.1-closure
- resolved_by: 725497e

### G-06 残留 — Settings 编辑态布局
- truth: "ProviderForm 编辑态「保存/取消」按钮在表单底部 sticky footer 且间距合理"
- status: closed
- detail: "用户复测：按钮在底部位置是对的（原描述「顶部」是失误），但间距过小贴在一起 → hotfix commit b933477（gap sp-2 8px → sp-4 16px）。复测 PASS。"
- severity: major
- resolved_by: b933477

### G-08 衍生发现 — 首次打开未获取选区
- truth: "首次打开 Aster 时立即获取当前 slide/单元格/选区，自动出胶囊"
- status: failed
- detail: "用户报：首次打开 Task Pane 时胶囊不出现，需要用户重新点 slide 才触发。main.tsx Office.onReady 后应主动调一次 adapter.getSelection() push 到 store。"
- severity: minor
- followup: 02.2 or quick-task

### G-08 设计修订 — × 按钮已移除
- truth: "选区胶囊只通过眼睛 toggle 控制附带/不附带，无独立临时隐藏入口"
- status: closed
- detail: "用户决定：直接去掉 ×（与眼睛 toggle 语义重叠）→ hotfix commit b933477（移除 SelectionPill 的 dismiss button + InputBar 的 selectionDismissed 状态 + styles.css 4 处 dead CSS）。简化 UX。"
- severity: clarification
- resolved_by: b933477

### UX 优化（UAT 新需求）
- truth: "DeepSeek / AiHubMix 等 built-in Provider 的 model 字段是下拉非文本框"
- status: captured
- detail: "见 .planning/todos/pending/builtin-model-dropdown.md。UAT-3 误改 model 字段触发误报暴露此 UX 风险。"
- severity: optimization
- followup: quick-task or 02.2

- truth: "支持一键 copy 当前会话历史（debug + UAT 反馈用）"
- status: captured
- detail: "见 .planning/todos/pending/copy-chat-history.md。"
- severity: optimization
- followup: quick-task

### UAT-5 ⑤ Excel auto 写入 — BLOCKED
- truth: "Excel for Web 选中单元格触发 auto 写入"
- status: blocked-external
- detail: "Excel for Web 官方服务故障（Microsoft 端，非 Aster bug），待修复后 30 秒补测。"
- severity: minor

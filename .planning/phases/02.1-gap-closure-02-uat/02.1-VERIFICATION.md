---
phase: 02.1-gap-closure-02-uat
verified: 2026-05-28T16:10:00Z
status: human_needed
score: 6/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "SC1 三宿主真机 sideload：350px 窄面板无意外水平/垂直滚动条"
    expected: "PPT/Excel/Word for Web 三宿主 Task Pane 宽度 350px 时，Task Pane 外层无水平/垂直滚动条；垂直滚动只出现在 .aster-messages；长 URL/代码块/长英文在气泡内换行"
    why_human: "CSS overflow 行为、视觉布局正确性无法用 grep 验证；需要真机 sideload 截图，PLAN 指定三宿主各 4 张证据截图（共 12 张）"
  - test: "SC1 三宿主真机 sideload：选区胶囊与 InputBar 内框左右对齐（视觉 ≤ 3px 误差）"
    expected: "胶囊文本左边界与输入框文本左边界差 ≤ 3px（当前数学校核：差 1px），DevTools 像素量测确认"
    why_human: "亚像素对齐无法程序验证，需真机 DevTools 量测截图"
  - test: "SC1 三宿主真机 sideload：Settings 编辑 Provider 表单保存/取消固定在底部，全局选项不混排"
    expected: "编辑 Provider 表单时 ProviderForm 独占 body（editState.kind=editing），全局选项不可见；footer 贴底即使滚动字段区域"
    why_human: "编辑态状态机与 sticky footer 的视觉正确性需真机操作验证"
  - test: "SC2 流式自动滚到底（G-03）"
    expected: "流式 token 追加时 ChatStream 视口跟随到底；用户向上滚查看历史时不被打断；滚回底部后恢复粘底；发新消息强制滚到底"
    why_human: "DOM 滚动行为（自动滚到底 / stickToBottom 切换）在 jsdom 中有空实现，需真机 sideload 验证真实 scrollTo 调用和视觉效果"
  - test: "SC3 DeepSeek 成本徽章显示 ¥（G-04）"
    expected: "使用内置 DeepSeek 发 ≥100 token 回答时，气泡下方显示「本次：N token · ¥X.XXXX」（X 非 0）；切 deepseek-v4-pro 时 ¥ 显著大于 flash；自定义 Provider 只显「N token」"
    why_human: "成本徽章的 ¥ 渲染依赖真实 SSE 流最末 usage chunk，需真机 API 调用验证 calcCostCny 从 config.model 正确查到单价"
  - test: "SC4 错误分类（G-07）：DeepSeek Key 改 sk-invalid 显示 KEY_INVALID 文案"
    expected: "sk-invalid Key 场景：气泡显「Key 可能无效或 Provider 不允许浏览器直连，请检查 API Key 与 Base URL」；断 WiFi 场景：显「网络连接失败」。两类必须分开"
    why_human: "fetch throw 路径分类（classifyFetchThrow）在真机浏览器中的 TypeError.message 格式与 jsdom 可能不同；需真机环境以 sk-invalid Key 触发 + 断网两个场景"
  - test: "SC5 AI tool-calling 写文档五个子场景（G-05）"
    expected: "① confirm/Word/接受 → Word 写入；② confirm/Word/拒绝 → 无变化；③ auto/PPT/replace_selection → 直接写入 + 回执；④ 不支持 tool-call Provider → 回退菜单；⑤ Excel 宿主写入 OK"
    why_human: "tool-calling 写文档涉及 Office.js adapter.insert 真实调用，无法在 jsdom 验证；需三宿主真机 sideload 并以 DeepSeek 实际触发 insert_to_document tool"
  - test: "SC6 选区胶囊眼睛 toggle + 持久化（G-08）"
    expected: "① 默认眼睛开发消息含选区；② 眼睛闭发消息不含选区；③ 刷新页面眼睛仍闭（持久化）；④ × 隐藏刷新后恢复（不持久化）；⑤ Settings 开关与眼睛双向联动"
    why_human: "localStorage 持久化需真机跨页面刷新验证；Office.js 选区上下文传递需真机宿主确认"
  - test: "SC7 Phase 02 SC1-SC6 全部真机 UAT 回归通过（含截图/录屏证据归档）"
    expected: "按 02-08-SUMMARY SC1-SC6 定义，在 Edge/Chrome 最新两版 × 三宿主全部 PASS，证据归档到 .planning/phases/02.1-gap-closure-02-uat/evidence/ 目录"
    why_human: "本 phase 与用户对齐「一波修复后统一真机 UAT」策略，所有 8 个 plan 的真机 checkpoint 均标记 deferred-to-phase-uat，phase 收尾时由用户做一次批量验证"
---

# Phase 2.1: Gap Closure 02 UAT Verification Report

**Phase Goal:** 修复 Phase 02-08 真机 UAT 留下的 8 条 gap（G-01..G-08），让 Phase 02 的 Success Criteria SC1-SC6 真正通过验收，Phase 02 才能 close。本阶段包含 6 条 bug 修复 + 2 条产品设计变更（D-16 / D-15 修订），不引入新需求。
**Verified:** 2026-05-28T16:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1: 滚动条/对齐回归（G-01/G-02/G-06）— 代码层修复落地 | ✓ VERIFIED | styles.css: overflow-wrap ×6, min-width:0 ×9, white-space:pre-wrap ×2；--inputbar-padding-x token；EditState 状态机 + ProviderForm sticky footer (position:sticky ×1 在 styles.css:955) |
| 2 | SC2: 流式自动滚到底（G-03）— 粘底状态机实现 | ✓ VERIFIED | ChatStream.tsx: stickToBottom useState, prevLengthRef, isAtBottom 阈值 8px, useEffect 依赖 [messages, stickToBottom]；ChatStream.test.tsx：4 个 TDD 用例（170→188 tests）|
| 3 | SC3: DeepSeek 成本徽章显示 ¥（G-04）— 双键查表修复 | ✓ VERIFIED | pricing.ts: calcCostCny(usage, providerId, model) 三参数签名；chat.ts: calcCostCny(usage, config.providerId, config.model)；pricing.test.ts: 14 个用例全绿 |
| 4 | SC4: 错误分类正确（G-07）— classifyFetchThrow 实现 | ✓ VERIFIED | sse.ts: classifyFetchThrow 导出函数，D-29 三条信号；mapHttpError 补 case 403 → KeyInvalidError；sse.test.ts: 6 个 G-07 用例 + 403 覆盖；openai-compat.ts: instanceof AsterError 防御 |
| 5 | SC5: AI tool-calling 写文档（G-05/D-16 修订）— 代码层全实现 | ✓ VERIFIED | openai-compat.ts: INSERT_TO_DOCUMENT_TOOL schema, supportsToolCall 探测；chat.ts: ToolCall 接口, acceptToolCall 幂等检查, text 类型+长度校验；ChatBubble.tsx: ToolCallPreviewCard/AutoInsertEffect(useRef)/FallbackInsertMenu；SettingsPanel: autoInsertMode segmented control；三宿主 adapter position 路由；默认「插入到文档」按钮已移除 |
| 6 | SC6: 选区胶囊眼睛 toggle（G-08/D-15 修订）— 完整实现 | ✓ VERIFIED | SelectionPill.tsx: EyeIcon/EyeOffIcon toggle, is-disabled 半透明；providerStore: setAttachEnabled, attachEnabled 字段；storage.ts: SELECTION_ATTACH_ENABLED key, 旧 key 迁移；InputBar.tsx: handleSend guard attachEnabled 读取；SettingsPanel 双向绑定 |
| 7 | SC7: 02 SC1-SC6 全部真机 UAT 回归通过（截图证据归档） | ? UNCERTAIN | evidence/ 目录不存在；所有 8 个 plan 的真机 UAT checkpoint 均标记 deferred-to-phase-uat，已与用户对齐在 phase 收尾统一执行 |

**Score:** 6/7 truths verified（SC7 为真机 UAT，归入 human_needed）

### Deferred Items

无（所有 8 条 gap 已在本 phase 代码层修复）。

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/styles.css` | G-01/G-02/G-06 CSS 修复 | ✓ VERIFIED | overflow-wrap ×6, min-width:0 ×9, white-space:pre-wrap ×2, --inputbar-padding-x, position:sticky, .aster-provider-form__footer, .aster-settings__global-options |
| `src/components/ChatStream.tsx` | G-03 粘底状态机 | ✓ VERIFIED | stickToBottom, prevLengthRef, isAtBottom, useEffect 依赖 [messages, stickToBottom] |
| `src/components/ChatStream.test.tsx` | G-03 TDD 测试 | ✓ VERIFIED | 4 个粘底用例，@testing-library/react |
| `src/providers/pricing.ts` | G-04 双键查表 | ✓ VERIFIED | calcCostCny(usage, providerId, model) 三参数，providerId namespace 守门 |
| `src/store/chat.ts` | G-04 调用方同步 + G-05 ToolCall 数据流 + CR-02/CR-04 修复 | ✓ VERIFIED | calcCostCny 第三参数 config.model；ToolCall 接口；acceptToolCall 幂等；text 类型+长度校验；safeMsg 安全提示 |
| `src/lib/sse.ts` | G-07 classifyFetchThrow + CR-03 403 | ✓ VERIFIED | classifyFetchThrow 导出，D-29 三条信号；mapHttpError case 403 → KeyInvalidError；streamSSE fetch try/catch |
| `src/providers/openai-compat.ts` | G-05 tool-call schema + provider 探测 | ✓ VERIFIED | INSERT_TO_DOCUMENT_TOOL, supportsToolCall 探测，instanceof AsterError 防御 |
| `src/components/ChatBubble.tsx` | G-05 confirm/auto/fallback UI + CR-01 useRef | ✓ VERIFIED | ToolCallPreviewCard, AutoInsertEffect(useRef(false)), FallbackInsertMenu；默认「插入到文档」按钮已移除 |
| `src/components/Settings/SettingsPanel.tsx` | G-05 autoInsertMode 开关 + G-06 三分区路由 | ✓ VERIFIED | EditState 状态机，autoInsertMode segmented control，编辑态独占 body |
| `src/components/Settings/ProviderForm.tsx` | G-06 sticky footer 三层结构 | ✓ VERIFIED | __header/__body/__footer BEM 结构 |
| `src/components/Settings/ProviderList.tsx` | G-06 状态上抛，WR-03 修复 | ✓ VERIFIED | 不含嵌套 ProviderForm；onEdit/onCreate props；useEffect 依赖数组补全 |
| `src/components/SelectionPill.tsx` | G-08 眼睛 toggle | ✓ VERIFIED | EyeIcon/EyeOffIcon，attachEnabled，is-disabled 半透明，onDismiss |
| `src/components/icons.tsx` | G-08 EyeIcon/EyeOffIcon | ✓ VERIFIED | EyeIcon 和 EyeOffIcon 均存在 |
| `src/components/InputBar.tsx` | G-08 handleSend guard | ✓ VERIFIED | attachEnabled guard，selectionDismissed session-only |
| `src/store/providers.ts` | G-08 attachEnabled 持久化，WR-02 内置 Provider 合并，WR-07 addProvider 返回 id | ✓ VERIFIED | setAttachEnabled, hydrateFromStorage 合并 BUILT_IN_PROVIDERS, addProvider 返回 id |
| `src/lib/storage.ts` | G-08 SELECTION_ATTACH_ENABLED key + 旧 key 迁移注释 | ✓ VERIFIED | SELECTION_ATTACH_ENABLED 存在，旧 key deprecated 注释 |
| `src/adapters/PptAdapter.ts` | G-05 position 路由，WR-01 两次 sync | ✓ VERIFIED | cursor/replace_selection/append_end switch；append_end 降为两次 sync |
| `src/adapters/ExcelAdapter.ts` | G-05 position 路由，WR-06 getUsedRange(false) | ✓ VERIFIED | position 路由；getUsedRange(false) 防空表抛错 |
| `src/adapters/WordAdapter.ts` | G-05 position 路由 | ✓ VERIFIED | cursor/replace_selection/append_end switch |
| `.planning/phases/02.1-gap-closure-02-uat/evidence/` | SC7 真机 UAT 截图归档目录 | ? MISSING | 目录不存在；所有截图类 checkpoint 均 deferred-to-phase-uat（已与用户对齐） |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ChatStream.tsx useEffect | messages store delta | 依赖 [messages, stickToBottom]（整体引用） | ✓ WIRED | 流式 delta 每次 set 生成新数组引用，触发 effect |
| calcCostCny | pricing.ts PROVIDER_PRICING | (usage, providerId, model) 三参数，model 二级查表 | ✓ WIRED | chat.ts:164-170 传 config.model |
| openai-compat.ts | INSERT_TO_DOCUMENT_TOOL | shouldAttachTools 条件挂载 | ✓ WIRED | supportsToolCall !== false 时附带 tools |
| streamSSE | ToolCallEnd | tool_calls delta 累积 + flush | ✓ WIRED | sse.ts:315-350 |
| chat.ts sendMessage | acceptToolCall/rejectToolCall | tool_call_end 分支，按 autoInsertMode 设 status | ✓ WIRED | chat.ts tool_call_end 分支完整 |
| AutoInsertEffect | adapter.insert | useRef(false) guard + acceptToolCall | ✓ WIRED | CR-01 修复后 hasTriggeredRef 跨 render 持久 |
| classifyFetchThrow | streamSSE fetch catch | AbortError 透传，其余走分类 | ✓ WIRED | sse.ts:238-241 |
| mapHttpError case 403 | KeyInvalidError | switch case 403 | ✓ WIRED | sse.ts:112-113 |
| InputBar.handleSend | attachEnabled guard | getState().attachEnabled 非订阅读取 | ✓ WIRED | InputBar.tsx:51-54 |
| SettingsPanel EditState | ProviderForm 独占 / 全局选项隐藏 | editState.kind === 'browse' 条件渲染 | ✓ WIRED | SettingsPanel.tsx:124 |
| hydrateFromStorage | BUILT_IN_PROVIDERS 合并 | id 主键 merge，强制 isBuiltIn=true | ✓ WIRED | providers.ts:179-194 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| CostBadge | costCny | calcCostCny(usage, providerId, model) ← SSEUsage ← stream | 依赖真实 SSE 末尾 usage chunk | ✓ FLOWING（代码路径完整；真机 API 调用需 human 验证） |
| ChatBubble ToolCallPreviewCard | toolCalls[].status | chat.ts sendMessage tool_call_end 分支 | 依赖真实模型返回 tool_calls | ✓ FLOWING（代码路径完整；真机 DeepSeek 调用需 human 验证） |
| SelectionPill | attachEnabled | providerStore ← SELECTION_ATTACH_ENABLED localStorage | localStorage read/write | ✓ FLOWING |
| ChatStream | messages | chatStore.messages ← sendMessage delta | 真实 SSE delta | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 190 tests 通过 | `npm test -- --run` | 190 passed, 0 fail（3 个预存在的 unhandled rejection warning 来自 retry.test.ts，与本 phase 无关）| ✓ PASS |
| Build 通过 gzip < 1MB | `npm run build` | main-*.js: 75.31 KB gzip（预算 1MB，使用 ~7.5%） | ✓ PASS |
| overflow-wrap 修复 ≥5 处 | `grep -c "overflow-wrap" src/styles.css` | 6 | ✓ PASS |
| min-width:0 修复 ≥4 处 | `grep -c "min-width: 0" src/styles.css` | 9 | ✓ PASS |
| white-space:pre-wrap ≥1 处 | `grep -c "white-space: pre-wrap" src/styles.css` | 2 | ✓ PASS |
| calcCostCny 三参数签名 | `grep -n "calcCostCny" src/store/chat.ts` | config.model 第三参数存在 | ✓ PASS |
| case 403 补充 | `grep -n "case 403" src/lib/sse.ts` | KeyInvalidError 已挂载 | ✓ PASS |
| hasTriggeredRef 使用 useRef | `grep -n "hasTriggeredRef" src/components/ChatBubble.tsx` | useRef(false) | ✓ PASS |
| acceptToolCall 幂等检查 | `grep -n "tc.status.*accepted.*return" src/store/chat.ts` | 存在幂等检查 | ✓ PASS |
| CR-02 安全提示 | `grep -n "请求遇到未知错误" src/store/chat.ts` | safeMsg 固定提示存在 | ✓ PASS |
| text 类型+长度校验 | `grep -n "typeof parsedArgs.text\|100_000" src/store/chat.ts` | 两行均存在 | ✓ PASS |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|---------|
| PANE-02 | 流式逐字渲染 + 自动滚到底 | ✓ SATISFIED | ChatStream 粘底状态机 |
| PANE-04 | 插入到文档 | ✓ SATISFIED | tool-calling D-16 修订；三宿主 position 路由 |
| COST-01 | 内置 Provider 显示 ¥ 成本徽章 | ✓ SATISFIED | calcCostCny 双键查表修复 |
| COST-02 | 成本徽章格式「N token · ¥X」 | ✓ SATISFIED（代码路径验证） | CostBadge 渲染路径，真机需 human 验证 |
| KEY-04 | API Key 无效时 KEY_INVALID 错误 | ✓ SATISFIED | classifyFetchThrow + mapHttpError 401/403 |
| PROV-08 | 错误 UX 正确分类 | ✓ SATISFIED | classifyFetchThrow D-29 三条信号 |
| NFR-03 | UI 回应性/无意外滚动条 | ✓ SATISFIED（代码层） | CSS overflow 修复，真机需 human 验证 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/ChatBubble.tsx` | 169 | `console.warn(..., err)` 打印完整 err 对象（IN-04 遗留） | ℹ Info | 可能暴露 Office.js 错误细节到 DevTools，不含 API Key，IN-04 已标注但未在 REVIEW-FIX 中修复 |
| `src/styles.css` | 354/358 | `.aster-send:disabled` 两个重复规则块（IN-02 遗留） | ℹ Info | 无功能影响，IN-02 已标注但未在 REVIEW-FIX 中修复 |
| `src/components/icons.tsx` | ~105 | AlertIcon path `0-3.42` 疑似缺少空格（IN-03 遗留） | ℹ Info | 大多数浏览器可正确解析，EdgeHTML 旧版本可能图标变形，IN-03 已标注但未在 REVIEW-FIX 中修复 |

说明：上述三条为 REVIEW.md 中标记为 Info（非 BLOCKER/WARNING）的遗留项，REVIEW-FIX.md 的 `findings_in_scope: 12` 覆盖 CR × 4 + WR × 8（不含 Info 4 条），属已知且接受的技术债。

### Human Verification Required

本 phase 与用户已对齐「一波修复后再统一真机测试」策略。SC1-SC6 的代码/自动化部分已 100% 落地；SC7 及各 SC 的真机宿主截图验证属于 UAT 层面，需要用户执行。

#### 1. SC1/SC2/SC6 布局与滚动验证（三宿主截图）

**测试：** 在 PPT for Web / Excel for Web / Word for Web 三宿主分别 sideload 最新 manifest，Task Pane 默认宽度 350px：
- 发一条让 AI 输出含长 URL + 50 行代码块的回答，截图确认无水平滚动条、长内容换行
- 发一条 ≥30 行流式长回答，观察视口自动跟随；中途向上滚确认不被打断；滚回底部确认恢复
- 打开 Settings → 编辑 DeepSeek Provider → 确认 ProviderForm 独占 body、保存/取消固定在底部
- 选中 Slide/单元格/文字，确认胶囊与输入框文本起点视觉对齐

**Expected：** 无意外滚动条；视口粘底行为正确；Settings 布局三分区清晰；胶囊对齐 ≤ 3px 误差

**Why human：** CSS 视觉正确性、Office WebView 中的 DOM 滚动行为无法程序验证

#### 2. SC3 DeepSeek 成本徽章 ¥ 显示

**测试：** 用内置 DeepSeek（默认 deepseek-v4-flash）Key 发一条至少 100 token 输出的消息，等流式结束

**Expected：** 气泡下方显「本次：N token · ¥X.XXXX」，X 非 0；切 deepseek-v4-pro 时 ¥ 值约为 flash 的 12.4×

**Why human：** ¥ 显示依赖真实 SSE 末尾 usage chunk，需真实 API Key 调用

#### 3. SC4 错误分类（DeepSeek Key 改 sk-invalid）

**测试：** Settings 将 DeepSeek Key 改为 `sk-invalid-test-key-12345`，发一条消息；然后断 WiFi 再发一条

**Expected：** sk-invalid：「Key 可能无效或 Provider 不允许浏览器直连，请检查 API Key 与 Base URL」；断网：「网络连接失败，请检查网络」

**Why human：** classifyFetchThrow 的 TypeError.message 格式在真机 Chrome/Edge 中与 jsdom 不同

#### 4. SC5 AI tool-calling 写文档五个子场景

**测试（Settings 默认 autoInsertMode='由我确认'）：**
- ① 在 Word for Web，让 AI 「写一段 100 字的职场签名」→ 出现预览卡 → 点「✓ 插入」→ 确认 Word 光标处写入
- ② 同上 → 点「✗ 拒绝」→ Word 无变化
- ③ Settings 切到「AI 自动」→ 在 PPT for Web 选中 slide，让 AI 「写一句 Slide 标题」→ 直接写入 + 显示「已写入 N 字到光标处」回执
- ④ Settings 新建一个 base URL 错误的自定义 Provider（使其返回 4xx 含 'tool' 关键字）→ 发消息 → 气泡显示「该 Provider 不支持 tool-call」toast + 右上「插入 ▾」按钮
- ⑤ 在 Excel for Web，让 AI 「写一段说明到当前工作表」→ 文本写入选中单元格

**Expected：** 五个子场景均按预期行为完成，无错误；确认默认「插入到文档」按钮不再出现

**Why human：** Office.js adapter.insert 真实写入需宿主环境；tool-calling 需真实 DeepSeek API

#### 5. SC6 选区胶囊眼睛 toggle 持久化

**测试：**
- ① 默认胶囊眼睛开，选中 slide，发消息，确认 AI 回复中包含「第 N 张 slide」
- ② 点眼睛关闭（胶囊变半透明），发消息，确认 AI 不提及选区
- ③ 刷新页面，确认眼睛仍处于关闭状态（localStorage 持久化）
- ④ 点 × 隐藏胶囊，刷新页面，确认胶囊恢复（× 不持久化）
- ⑤ 在 Settings 「自动附带选区」开关 toggle，确认胶囊眼睛同步反映

**Expected：** 所有 5 个场景行为正确

**Why human：** localStorage 跨刷新持久化需真机验证；Office.js 选区传递需宿主环境

#### 6. SC7 证据归档

**测试：** 将上述 SC1-SC6 真机 UAT 的截图/录屏归档到 `.planning/phases/02.1-gap-closure-02-uat/evidence/` 目录，按场景分子目录命名

**Expected：** evidence/ 目录存在，含 SC1-SC6 各子场景的截图/录屏证据

**Why human：** 需要用户手动执行并提交截图

---

## Gaps Summary

本次验证未发现 BLOCKER 级 gap。所有 6 条 SC（SC1-SC6）的代码实现均已在代码库中验证存在、有实质内容、且正确接线：

- **G-01（SC1 滚动条）：** 10 处 CSS 修改已验证（overflow-wrap ×6, min-width:0 ×9, white-space:pre-wrap ×2）
- **G-02（SC1 对齐）：** --inputbar-padding-x token 已验证，数学校核差 1px
- **G-03（SC2 流式滚动）：** stickToBottom 状态机已验证，4 个 TDD 用例全绿
- **G-04（SC3 成本徽章）：** calcCostCny 三参数签名已验证，14 个测试全绿
- **G-05（SC5 tool-calling）：** 完整 SSE 解析 + Provider 探测 + confirm/auto UI + adapter 路由已验证，188 tests
- **G-06（SC1 Settings 布局）：** EditState 状态机 + sticky footer 已验证
- **G-07（SC4 错误分类）：** classifyFetchThrow + mapHttpError 403 已验证，6 个 G-07 用例
- **G-08（SC6 眼睛 toggle）：** 完整 toggle + 持久化 + handleSend guard 已验证

REVIEW.md（4 BLOCKER + 8 WARNING）所有 12 条已在 REVIEW-FIX.md 记录修复（commit aedc4ed..fbeb9c2），190 tests 全绿，build 75 KB gzip。

**唯一阻塞点：** SC7 真机三宿主 UAT 截图证据尚未归档（evidence/ 目录不存在）。这是已与用户对齐的 `deferred-to-phase-uat` 策略，代码层 SC1-SC6 全部就绪，等待用户执行一次批量真机验收。

---

_Verified: 2026-05-28T16:10:00Z_
_Verifier: Claude (gsd-verifier)_

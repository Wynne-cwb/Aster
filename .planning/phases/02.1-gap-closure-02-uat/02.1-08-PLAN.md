---
phase: 02.1-gap-closure-02-uat
plan: 02.1-08
type: execute
wave: 4
depends_on: [02.1-06]
files_modified:
  - src/components/SelectionPill.tsx
  - src/components/icons.tsx
  - src/components/InputBar.tsx
  - src/lib/storage.ts
  - src/store/providers.ts
  - src/components/Settings/SettingsPanel.tsx
  - src/styles.css
autonomous: false
requirements: []
regresses: []
gap_closure: true
closes_gaps: [G-08]
tags: [selection-pill, eye-toggle, persistence, ui-design-change]

must_haves:
  truths:
    - "选区胶囊默认仍自动附带（保留 D-15 默认行为）；眼睛图标默认显「开」"
    - "点击眼睛 → 切换 selection_attach_enabled；眼睛切「闭」时胶囊仍在屏，但 selection 不再附带到下条消息"
    - "selection_attach_enabled 持久化到 partitioned localStorage（key = aster:selection:attachEnabled）；刷新页面/重开 Task Pane 后状态保留"
    - "胶囊右侧 × 行为收窄：仅「本次会话隐藏胶囊」（不持久化）；刷新页面胶囊恢复"
    - "InputBar.handleSend 调 adapter.getSelection() 时先读 selection_attach_enabled，false 则 selection=undefined（即便胶囊未 × 也不附带）"
  artifacts:
    - path: "src/components/icons.tsx"
      provides: "新增 EyeIcon + EyeOffIcon（Lucide 风格，复用 base 配置）"
      exports: ["EyeIcon", "EyeOffIcon"]
    - path: "src/components/SelectionPill.tsx"
      provides: "胶囊右侧 加眼睛 toggle 按钮，× 仍保留；点击眼睛 → providerStore.setAttachEnabled(!enabled)"
      contains: "EyeIcon"
    - path: "src/store/providers.ts"
      provides: "rename autoAttach → attachEnabled（语义更准），新增 setAttachEnabled；hydrateFromStorage 读取持久化"
      contains: "attachEnabled"
    - path: "src/lib/storage.ts"
      provides: "STORAGE_KEYS 新增 SELECTION_ATTACH_ENABLED（'aster:selection:attachEnabled'），可保留旧 SELECTION_AUTO_ATTACH 兼容 hydrate"
      contains: "SELECTION_ATTACH_ENABLED"
  key_links:
    - from: "SelectionPill 眼睛按钮 onClick"
      to: "providerStore.setAttachEnabled(boolean)"
      via: "store action 写入 storage + 内存 state"
      pattern: "setAttachEnabled"
    - from: "InputBar.handleSend"
      to: "adapter.getSelection() 是否调用"
      via: "providerStore.attachEnabled === true 时才取选区"
      pattern: "attachEnabled"
---

<objective>
G-08 D-31/D-32/D-33/D-34 修订 D-15：选区胶囊从「一次性 dismiss」改为「眼睛 toggle 持久化 + × 临时隐藏」。

修订要点：
- **眼睛开 = 附带选区，眼睛闭 = 不附带但胶囊仍在**（D-31）
- 状态持久化到 partitioned localStorage，key = `aster:selection:attachEnabled`（D-32）
- **× 收窄**：本次会话隐藏胶囊，**不持久化**（D-33）；下次打开 Task Pane 胶囊恢复
- handleSend 先读 attachEnabled 决定是否调 adapter.getSelection()（D-34）

技术决策：
1. **复用 providerStore**：D-32 说「不走 chatStore，用 providerStore 或新建小 uiStore」——我们用 providerStore 是因为它已有 `autoAttach` field 且已持久化；本任务把语义 rename 为 `attachEnabled`（更准：从「自动附带」变成「附带开关」），向后兼容地读取旧 `SELECTION_AUTO_ATTACH` key（如有）。
2. **storage key 新名**：原 `aster:selection:autoAttach` → `aster:selection:attachEnabled`。hydrate 时优先读新 key；若新 key 不存在但旧 key 存在 → 用旧 key 值并立即写入新 key（一次性迁移）。
3. **02.1-06 协同**：SettingsPanel 三分区路由中「全局选项」已有「自动附带选区」开关——本任务把该开关与 SelectionPill 眼睛 toggle **双向绑定**（同一个 providerStore.attachEnabled）。眼睛改 → 设置项也改；设置项改 → 眼睛也改。

Purpose: SC5「选区胶囊基础显示 + 控制」在真机 UAT 通过——眼睛 toggle 持久化 / × 不持久化两个行为分开。
Output: SelectionPill 改造、providerStore rename + hydrate 迁移、InputBar.handleSend 改读 attachEnabled、SettingsPanel 文案同步、styles.css 眼睛按钮样式。

回归参照：维持 D-15 默认值 = true。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-gap-closure-02-uat/02.1-CONTEXT.md
@src/components/SelectionPill.tsx
@src/components/InputBar.tsx
@src/components/icons.tsx
@src/lib/storage.ts
@src/store/providers.ts
@src/components/Settings/SettingsPanel.tsx

<interfaces>
<!-- 现有 providerStore（src/store/providers.ts）：
  autoAttach: boolean (默认 true)
  setAutoAttach(v: boolean): void
  hydrateFromStorage(): 从 STORAGE_KEYS.SELECTION_AUTO_ATTACH 读取

现有 storage（src/lib/storage.ts:15-26）：
  STORAGE_KEYS.SELECTION_AUTO_ATTACH = 'aster:selection:autoAttach'

现有 SelectionPill（src/components/SelectionPill.tsx）：
  Props: { onDismiss: () => void }
  autoAttach=false 时直接 return null（整个不渲染）—— G-08 要求改为：attachEnabled=false 时**仍渲染**但显眼睛闭状态

现有 InputBar.handleSend（src/components/InputBar.tsx:41-51）：
  const sel = !selectionDismissed ? await adapter.getSelection() : undefined;
  await sendMessage(prompt, sel ?? undefined);
  —— G-08 D-34 要求改为：先读 attachEnabled，false → undefined

现有 SettingsPanel（src/components/Settings/SettingsPanel.tsx 经 02.1-06 改造后）：
  全局选项分区有「自动附带选区」开关，绑定 useProviderStore(autoAttach)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: providerStore + storage rename（autoAttach → attachEnabled）含 hydrate 迁移</name>
  <files>src/lib/storage.ts, src/store/providers.ts</files>
  <read_first>
    - src/lib/storage.ts（STORAGE_KEYS 常量段）
    - src/store/providers.ts（autoAttach state + setAutoAttach + hydrateFromStorage）
    - src/components/Settings/SettingsPanel.tsx（02.1-06 已改造，确认开关绑定 selector）
  </read_first>
  <action>
**A. 修订 `src/lib/storage.ts:15-26`：**

```ts
export const STORAGE_KEYS = {
  PROVIDERS: 'aster:providers',
  KEY_PREFIX: 'aster:keys:',
  ONBOARDING_SEEN: 'aster:onboarding:seen',
  /** G-08 修订：原 'aster:selection:autoAttach' → 'aster:selection:attachEnabled'。
   *  hydrate 时读新 key 优先；旧 key 仅用作首次迁移（在 providerStore.hydrateFromStorage 处理）。
   *  D-32 字面 key = 'selection_attach_enabled'（snake_case）；本仓库 STORAGE_KEYS 历史约定带
   *  'aster:' 命名空间前缀，所以这里写完整字面 'aster:selection:attachEnabled'——D-32 的语义
   *  约束（"持久化到 partitioned localStorage，默认 true，不走 chatStore"）完整兑现，**字面
   *  差异仅是仓库统一 namespace 前缀**，不构成 D-32 违背。注意 src/lib/storage.ts 的 prefixedKey()
   *  另外注入 Office.context.partitionKey（与本前缀无关，那是浏览器分区隔离）。 */
  SELECTION_ATTACH_ENABLED: 'aster:selection:attachEnabled',
  /** @deprecated 02.1-08 起改用 SELECTION_ATTACH_ENABLED；保留常量用于一次性迁移读取 */
  SELECTION_AUTO_ATTACH: 'aster:selection:autoAttach',
  DEFAULT_PROVIDER: 'aster:providers:default',
} as const;
```

**B. 修订 `src/store/providers.ts`：**

```ts
interface ProviderState {
  providers: ProviderConfig[];
  defaultLLMProviderId: string;
  /** G-08 修订：原 autoAttach → attachEnabled。
   *  true = 发消息时附带选区 / 眼睛开；false = 不附带 / 眼睛闭（胶囊仍在屏）。 */
  attachEnabled: boolean;

  addProvider: (...) => void;
  updateProvider: (...) => void;
  removeProvider: (...) => void;
  setDefaultLLM: (...) => void;
  setKey: (...) => void;
  getKey: (...) => string | null;
  /** G-08：替代 setAutoAttach */
  setAttachEnabled(v: boolean): void;
}

export const useProviderStore = create<ProviderState>((set, get) => ({
  providers: BUILT_IN_PROVIDERS,
  defaultLLMProviderId: 'deepseek',
  attachEnabled: storage.get<boolean>(STORAGE_KEYS.SELECTION_ATTACH_ENABLED)
    ?? storage.get<boolean>(STORAGE_KEYS.SELECTION_AUTO_ATTACH)
    ?? true,
  // ... 其它 actions 不变 ...
  setAttachEnabled(v) {
    set({ attachEnabled: v });
    storage.set(STORAGE_KEYS.SELECTION_ATTACH_ENABLED, v);
  },
}));

export function hydrateFromStorage(): void {
  const stored = storage.get<ProviderConfig[]>(STORAGE_KEYS.PROVIDERS);
  const defaultId = storage.get<string>(STORAGE_KEYS.DEFAULT_PROVIDER) ?? 'deepseek';
  // G-08 迁移：先读新 key；新 key 没有但旧 key 有 → 用旧值并写入新 key，下次启动就是纯新 key 路径
  const newVal = storage.get<boolean>(STORAGE_KEYS.SELECTION_ATTACH_ENABLED);
  const oldVal = storage.get<boolean>(STORAGE_KEYS.SELECTION_AUTO_ATTACH);
  const attachEnabled = newVal ?? oldVal ?? true;
  if (newVal == null && oldVal != null) {
    storage.set(STORAGE_KEYS.SELECTION_ATTACH_ENABLED, oldVal); // 一次性迁移
  }

  if (stored && stored.length > 0) {
    useProviderStore.setState({ providers: stored, defaultLLMProviderId: defaultId, attachEnabled });
  } else {
    useProviderStore.setState({ attachEnabled });
  }
}
```

**C. 全文 grep `autoAttach` 替换为 `attachEnabled`**（除非是注释里说明「原 autoAttach」），影响：
- providers.ts 内：`autoAttach` field、`setAutoAttach` action 改名
- SettingsPanel.tsx（02.1-06 已写）：开关绑定的 selector 改 `useProviderStore((s) => s.attachEnabled)` + `useProviderStore((s) => s.setAttachEnabled)`，**i18n 文案「自动附带选区」可以保留**（用户角度仍是「自动附带」语义；store 字段名只是内部命名）

  - 把 SelectionPill.tsx 原 `useProviderStore((s) => s.autoAttach)` 改成 `useProviderStore((s) => s.attachEnabled)`（但 Task 2 还要继续改 SelectionPill 内部逻辑，这里只先 rename 解决类型）

**D. 测试**：
```
npm test -- --run
```
若有引用 autoAttach 的旧测试 → 同步 rename（不修改测试期望，只换 field 名）。
  </action>
  <acceptance_criteria>
- `grep -c "attachEnabled" src/store/providers.ts` ≥ 4
- `grep -c "SELECTION_ATTACH_ENABLED" src/lib/storage.ts` ≥ 1
- `grep -c "autoAttach" src/store/providers.ts` == 0（field 已完全 rename；注释里若说明「原 autoAttach」grep -v '^[[:space:]]*\*\|^[[:space:]]*//' 后应为 0）
- `grep -c "SELECTION_AUTO_ATTACH" src/store/providers.ts` ≥ 1（hydrate 迁移读取旧 key 保留）
- `grep -E "D-32 字面.*selection_attach_enabled" src/lib/storage.ts | wc -l` ≥ 1（I-01：D-32 锁定字面 = 'selection_attach_enabled'，仓库 STORAGE_KEYS 加 'aster:' namespace 前缀，注释中必须显式标注字面差异与 D-32 语义兑现）
- `npm test -- --run` 全绿
- `npm run build` 通过
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
storage / providerStore rename + 迁移读取完成；npm test 全绿。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: icons.tsx 新增 EyeIcon / EyeOffIcon + SelectionPill 眼睛 toggle + InputBar handleSend</name>
  <files>src/components/icons.tsx, src/components/SelectionPill.tsx, src/components/InputBar.tsx, src/styles.css</files>
  <read_first>
    - src/components/icons.tsx 完整（确认 base 配置 + 现有 9 个 icon 风格）
    - src/components/SelectionPill.tsx 完整
    - src/components/InputBar.tsx 完整（handleSend 41-51）
    - src/styles.css 514-558 选区胶囊样式段
  </read_first>
  <action>
**A. 新增 icons：`src/components/icons.tsx` 末尾追加：**

```tsx
/** 眼睛 - 开（附带选区 ON，G-08 D-31） */
export function EyeIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
      <circle cx="12" cy="12" r="3" />
    </svg>
  );
}

/** 眼睛 - 闭（附带选区 OFF，G-08 D-31） */
export function EyeOffIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24" />
      <line x1="1" y1="1" x2="23" y2="23" />
    </svg>
  );
}
```

（手画 path，Lucide 风格；ISC 风格 path 公开模板可参考；不引入外部图标库）

**B. 修订 `src/components/SelectionPill.tsx`：**

```tsx
import { useEffect, useState } from 'react';
import type { ReactElement } from 'react';
import { useLingui } from '@lingui/react/macro';
import { useAdapter } from '../context/AdapterContext';
import { useProviderStore } from '../store/providers';
import { formatSelection } from './formatSelection';
import { XIcon, EyeIcon, EyeOffIcon } from './icons';

interface SelectionPillProps {
  onDismiss: () => void;
}

export default function SelectionPill({ onDismiss }: SelectionPillProps): ReactElement | null {
  const adapter = useAdapter();
  const { t, i18n } = useLingui();
  const attachEnabled = useProviderStore((s) => s.attachEnabled);
  const setAttachEnabled = useProviderStore((s) => s.setAttachEnabled);

  const [ctx, setCtx] = useState<string>('');

  useEffect(() => {
    void adapter.getSelection().then((sel) => setCtx(formatSelection(sel, i18n)));
    const unsub = adapter.onSelectionChanged(async () => {
      const sel = await adapter.getSelection();
      setCtx(formatSelection(sel, i18n));
    });
    return () => unsub();
  }, [adapter, i18n]);

  // G-08 D-31 修订：不再因 attachEnabled=false 而 return null；胶囊始终渲染，靠眼睛态体现
  return (
    <span className={`aster-selection-pill${attachEnabled ? '' : ' is-disabled'}`}>
      {/* 眼睛 toggle - 控制是否附带（D-31 / D-32） */}
      <button
        type="button"
        className="aster-selection-pill__eye"
        onClick={() => setAttachEnabled(!attachEnabled)}
        aria-label={attachEnabled ? t`关闭附带选区` : t`开启附带选区`}
        title={attachEnabled ? t`关闭附带选区` : t`开启附带选区`}
      >
        {attachEnabled ? <EyeIcon /> : <EyeOffIcon />}
      </button>

      <span className="aster-selection-pill__text">{ctx}</span>

      {/* × 本次会话隐藏（D-33，不持久化） */}
      <button
        type="button"
        className="aster-selection-pill__dismiss"
        onClick={onDismiss}
        aria-label={t`本次隐藏胶囊`}
        title={t`本次隐藏胶囊`}
      >
        <XIcon />
      </button>
    </span>
  );
}
```

**C. 修订 `src/components/InputBar.tsx:41-51`：**

```tsx
const handleSend = async (): Promise<void> => {
  const prompt = text.trim();
  if (!prompt || isStreaming) return;

  setText('');
  setSelectionDismissed(false); // 发送后重置 × 隐藏（× 是单次会话级，发送后下条仍显示胶囊）

  // G-08 D-34：先读 attachEnabled，false → 不取选区
  const attachEnabled = useProviderStore.getState().attachEnabled;
  const sel = (!selectionDismissed && attachEnabled) ? await adapter.getSelection() : undefined;
  await sendMessage(prompt, sel ?? undefined);
};
```

注意 `useProviderStore.getState()` 是非订阅式读取（避免组件每次 attachEnabled 变就重渲）。

**D. 修订 `src/styles.css` 选区胶囊段（line 514-558）：**

```css
.aster-selection-pill {
  display: inline-flex;
  align-items: center;
  gap: var(--sp-1);
  font-size: 11px;
  color: var(--text-3);
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--r-pill);
  padding: 2px var(--sp-2);
  max-width: 220px;       /* 加 20px 留眼睛位 */
  overflow: hidden;
  vertical-align: middle;
  transition: opacity var(--dur) var(--ease);
}
.aster-selection-pill.is-disabled {
  opacity: 0.55;          /* 眼睛闭时整体降透明度，提示「不附带」 */
}

.aster-selection-pill__eye {
  background: none;
  border: none;
  padding: 0;
  cursor: pointer;
  color: var(--text-3);
  display: inline-flex;
  align-items: center;
  flex-shrink: 0;
  transition: color var(--dur) var(--ease);
}
.aster-selection-pill__eye svg {
  width: 12px;
  height: 12px;
}
.aster-selection-pill__eye:hover {
  color: var(--brand);
}
.aster-selection-pill__eye:focus-visible {
  outline: 2px solid var(--brand);
  outline-offset: 1px;
  border-radius: var(--r-sm);
}

/* __text / __dismiss 保留原样 */
```
  </action>
  <acceptance_criteria>
- `grep -c "EyeIcon\|EyeOffIcon" src/components/icons.tsx` ≥ 2（两个 icon 都已定义）
- `grep -c "EyeIcon\|EyeOffIcon" src/components/SelectionPill.tsx` ≥ 2（import + render）
- `grep -c "setAttachEnabled" src/components/SelectionPill.tsx` ≥ 1
- `grep -c "useProviderStore.getState().attachEnabled" src/components/InputBar.tsx` ≥ 1
- `grep -c "aster-selection-pill__eye" src/styles.css` ≥ 1
- `grep -c "is-disabled" src/styles.css` ≥ 1
- `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/components/SelectionPill.tsx | grep -c "if (!attachEnabled) return null"` == 0（旧逻辑已消除）
- `npm test -- --run` 全绿
- `npm run build` 通过
- [UAT] PPT for Web sideload：
  - 场景 ①：默认眼睛开 → 选中第 2 张 slide → 在输入框发送「描述这张 slide」→ AI 输出中应**看到「第 2 张 slide」上下文**（如「您正在第 2 张 slide」类回复）。截图。
  - 场景 ②：点眼睛切「闭」（眼睛图标变 EyeOffIcon，胶囊整体半透明 0.55，胶囊仍在屏）→ 再发同样消息 → AI 输出**不再提及第 2 张 slide**。截图。
  - 场景 ③：刷新 Task Pane（关掉重开）→ 眼睛仍是「闭」状态（持久化生效）+ 胶囊仍在屏。截图。
  - 场景 ④：点击 × 关掉胶囊 → 胶囊本次会话不显 → 刷新页面 → **胶囊回来**（× 不持久化，D-33）。截图。
  - 场景 ⑤：进 Settings 切「自动附带选区」开关 → 回到 Task Pane → SelectionPill 眼睛态**同步**（开关 ↔ 眼睛双向绑定）。截图。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
SelectionPill 眼睛 toggle 实现 + 持久化迁移完成 + InputBar 接入 + 双向绑定 + styles.css 样式更新；npm test 全绿；5 个真机 UAT 场景截图归档至 evidence/02.1-08-uat/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 真机 UAT 五个场景验证眼睛 toggle 与 × 临时隐藏</name>
  <what-built>
- icons.tsx 新增 EyeIcon / EyeOffIcon
- SelectionPill 眼睛 toggle + × 共存
- providerStore.attachEnabled 替代 autoAttach（含 hydrate 迁移）
- InputBar.handleSend 读 attachEnabled
- styles.css 新增眼睛按钮样式与 is-disabled 视觉降级
  </what-built>
  <how-to-verify>
按 office-addin-browser-uat skill：

1. PPT for Web sideload，确保 DeepSeek Key 已配。
2. **场景 ① 默认开**：选第 2 张 slide → 胶囊显眼睛开 → 发「描述当前选中的 slide」→ AI 回复含「第 2 张 slide」字样 → **截图保存**。
3. **场景 ② 切闭**：点胶囊上的眼睛 → 眼睛切到「闭」状态（图标变了 + 胶囊整体半透明）→ 胶囊**仍在屏**（不是隐藏！）→ 再发同样消息 → AI 回复**不提**「第 2 张 slide」（说明没附带选区）→ **截图保存**。
4. **场景 ③ 刷新持久化**：关掉 Task Pane（PPT 内点「关闭」或刷新整个浏览器标签）→ 重新打开 → 胶囊仍在屏 + 眼睛**仍是「闭」**（持久化生效）→ **截图保存**。
5. **场景 ④ × 不持久化**：把眼睛切回「开」（验证持久化双向）→ 点 × 关掉胶囊 → 胶囊本次会话消失 → 刷新页面 → 胶囊**重新出现**+ 眼睛**「开」**（D-33：× 仅本次隐藏，不持久化）→ **截图保存**。
6. **场景 ⑤ 双向绑定**：进 Settings → 看到「自动附带选区」开关，状态与胶囊眼睛**同步**（眼睛开 ↔ 开关 on）→ 在 Settings 关掉开关 → 回到 Task Pane → 胶囊眼睛**也变闭** → **截图保存**。
7. （可选）Excel for Web 重复场景 ① + ②（验证 host-agnostic）。

通过条件：5 张截图涵盖 ①-⑤ 行为，× 与眼睛行为分得清。

部署提醒：commit 不等于部署，必须 push origin main。
  </how-to-verify>
  <resume-signal>输入 "approved" 并附 evidence 路径，或描述哪个场景失败（粘截图）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 用户隐私选择 ← 持久化设置 | 错误的持久化（× 也持久化 / 眼睛不持久化）会让用户的隐私选择被覆盖 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-08-01 | Information Disclosure (I) | InputBar.handleSend | mitigate | attachEnabled=false 时 sel=undefined，sendMessage 不附带选区上下文给 Provider；T-02-24 选区元数据保护语义维持 |
| T-02.1-08-02 | Tampering (T) | localStorage 迁移读旧 key | accept | 旧 key 一次性迁移到新 key 后不再写旧 key；不增加新攻击面 |
</threat_model>

<verification>
- npm test 全绿
- 5 张真机 UAT 截图覆盖五个场景
- grep 确认旧 autoAttach field 已 rename
- 迁移读旧 SELECTION_AUTO_ATTACH 行为保留
- D-32 字面 'selection_attach_enabled' 在 STORAGE_KEYS 注释中被显式标注（grep -E "D-32 字面.*selection_attach_enabled" src/lib/storage.ts ≥ 1）；说明 'aster:' 前缀是仓库 namespace 约定（不是 storage 工具自动注入），D-32 的语义约束「持久化、默认 true、不走 chatStore」均已兑现
</verification>

<success_criteria>
- 眼睛 toggle 持久化跨刷新 / 切宿主（同分区）保留
- × 仅本次会话隐藏（不持久化）
- attachEnabled=false 时不附带选区上下文
- SettingsPanel 开关与胶囊眼睛双向绑定
- ROADMAP SC5「选区胶囊基础显示」回归通过 + D-15 修订兑现
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-gap-closure-02-uat/02.1-08-SUMMARY.md`
</output>

> [revised in iteration 1] I-01: D-32 字面 key 'selection_attach_enabled' 在 STORAGE_KEYS 注释显式标注；说明 'aster:' 前缀是仓库 namespace 约定（非 storage 工具注入），D-32 语义全部兑现；Task 1 acceptance + verification 各加一条 grep gate。

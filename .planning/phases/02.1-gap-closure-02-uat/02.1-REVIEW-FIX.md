---
phase: 02.1-gap-closure-02-uat
fixed_at: 2026-05-28T08:00:18Z
review_path: .planning/phases/02.1-gap-closure-02-uat/02.1-REVIEW.md
iteration: 1
findings_in_scope: 12
fixed: 12
skipped: 0
status: all_fixed
---

# Phase 02.1: Code Review Fix Report

**Fixed at:** 2026-05-28T08:00:18Z
**Source review:** `.planning/phases/02.1-gap-closure-02-uat/02.1-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 12（CR × 4 + WR × 8，按 fix_scope=critical_warning）
- Fixed: 12
- Skipped: 0

---

## Fixed Issues

### CR-01: AutoInsertEffect hasTriggeredRef 改 useRef + acceptToolCall 幂等检查

**Files modified:** `src/components/ChatBubble.tsx`, `src/store/chat.ts`
**Commit:** `aedc4ed`
**Applied fix:**
1. `ChatBubble.tsx`：`import useRef`，`AutoInsertEffect` 内 `const hasTriggeredRef = { current: false }` 改为 `useRef(false)`，防止每次 render 重置。
2. 同文件：删除 `ToolCallPreviewCard` 内空 useEffect（lines 73-81，死代码，WR-08 一并处理）。
3. `chat.ts`：`acceptToolCall` 入口加 `if (tc.status === 'accepted') return;` 幂等检查，防写入前 re-trigger。

---

### CR-02: sendMessage 错误消息安全处理

**Files modified:** `src/store/chat.ts`
**Commit:** `3b7fa63`
**Applied fix:**
引入 `AsterError` import，在 catch 块中将 `(e as Error).message` 改为：
- `e instanceof AsterError` → 使用 `e.message`（受控构建，不含 Key）
- 其余所有异常（Office.js、网络层、第三方）→ 固定友好提示 `'请求遇到未知错误，请重试'`，防止 API Key 片段渲染到 UI。

---

### CR-03: mapHttpError 补 case 403 + 测试覆盖

**Files modified:** `src/lib/sse.ts`, `src/lib/sse.test.ts`
**Commit:** `f27de3e`
**Applied fix:**
1. `sse.ts`：`mapHttpError` switch 补 `case 403: return new KeyInvalidError('API Key 权限不足或已被吊销，请前往设置更新 Key')`。
2. `sse.test.ts`：
   - `mapHttpError` suite 补 `403 → KeyInvalidError` 测试（含 code 断言和 message 包含"权限不足"）。
   - fetchThrow 回归 suite 补 `Test 5b`：`Response(403)` 端到端 → `KeyInvalidError`。

---

### CR-04: tool_call 参数校验补 text 类型和长度检查

**Files modified:** `src/store/chat.ts`
**Commit:** `ea8d5a1`
**Applied fix:**
在 position 枚举校验之后补充：
- `if (typeof parsedArgs.text !== 'string') continue;`（防非字符串值传给 adapter）
- `if (parsedArgs.text.length > 100_000) continue;`（防超大写入占满文档，100K 字符约 200KB）

---

### WR-01: PptAdapter append_end 三次 sync 降为两次

**Files modified:** `src/adapters/PptAdapter.ts`
**Commit:** `52ffbc8`
**Applied fix:**
`append_end` 分支改用 `shapes.getItemAt(0).textFrame.textRange` 在第一批 load 时同时 load `shapes.items` 和 `tr.text`，一次 sync 后写入，总 sync 从 3 次降为 2 次，符合 NFR-02 two-sync 规则。其他 position 分支保持原有两次 sync 路径。

---

### WR-02: hydrateFromStorage 合并 BUILT_IN_PROVIDERS

**Files modified:** `src/store/providers.ts`
**Commit:** `2b5ce6a`
**Applied fix:**
`hydrateFromStorage` 的 `stored && stored.length > 0` 分支改为：以 id 为主键将 `BUILT_IN_PROVIDERS` 强制合并到 stored（不含则补入；含有则强制 `isBuiltIn=true` 防外部篡改），最终 providers 数组先内置后自定义有序排列。

---

### WR-03: ProviderList 深链 useEffect 依赖数组补全

**Files modified:** `src/components/Settings/ProviderList.tsx`
**Commit:** `89d0721`
**Applied fix:**
移除 `// eslint-disable-next-line react-hooks/exhaustive-deps` 禁用行，依赖数组从 `[focusAnchor]` 改为 `[focusAnchor, providers, defaultLLMProviderId, onEdit]`，防止 providers hydrate 后 effect 持有旧闭包导致 `defaultProvider` 为 undefined。

---

### WR-04: SelectionPill onSelectionChanged 回调补 try-catch

**Files modified:** `src/components/SelectionPill.tsx`
**Commit:** `dad1641`
**Applied fix:**
`onSelectionChanged` 异步回调内 `adapter.getSelection()` 调用包入 `try/catch`，捕获 `HostApiError`（如 Office.js 上下文销毁），保持上一次 ctx 不 crash，消除 unhandled promise rejection。

---

### WR-05: classifyFetchThrow 错误措辞改为更保守

**Files modified:** `src/lib/sse.ts`
**Commit:** `0a76f72`
**Applied fix:**
三信号齐备路径的 `KeyInvalidError` 消息从 `'Key 可能无效，请前往设置更新 Key'` 改为 `'Key 可能无效或 Provider 不允许浏览器直连，请检查 API Key 与 Base URL'`，更准确覆盖 CORS preflight 拒绝场景，不误导用户只换 Key。

---

### WR-06: ExcelAdapter append_end 改 getUsedRange(false) 防空表抛错

**Files modified:** `src/adapters/ExcelAdapter.ts`
**Commit:** `8b690c6`
**Applied fix:**
`getUsedRange(true)` 改为 `getUsedRange(false)`（空表不抛 ItemNotFound），`rowCount` 直接作为新行号基数。外层包 try/catch，空表 fallback 时 `newRow=0`（写入 A1），保持两次 sync 结构完整。

---

### WR-07: addProvider 返回新建 id，SettingsPanel 直接用 id 写 Key

**Files modified:** `src/store/providers.ts`, `src/components/Settings/SettingsPanel.tsx`
**Commit:** `fbeb9c2`
**Applied fix:**
1. `providers.ts`：`addProvider` 接口改为 `string` 返回值，实现末尾 `return id`。
2. `SettingsPanel.tsx`：`creating` 分支改为 `const newId = addProvider(...)` 后直接 `setKey(newId, data.apiKey)`，不再依赖数组末尾位置。

---

### WR-08: 删除 ToolCallPreviewCard 死代码 useEffect

**Files modified:** `src/components/ChatBubble.tsx`
**Commit:** `aedc4ed`（与 CR-01 同次提交）
**Applied fix:**
删除 `ToolCallPreviewCard` 函数体内空的 `useEffect`（原 lines 73-81），该 effect body 为空、含误导性注释，对正确性无贡献，在 CR-01 修复时一并清除。

---

## Post-fix Verification

- **vitest run：** 190 tests PASS, 0 FAIL（全量通过，含新增 CR-03 覆盖测试）
- **npm run build：** 成功，main chunk 75KB gzip（远低于 1MB 预算）
- **tsc --noEmit：** 0 errors
- **lingui extract：** messages.po 行号注释已更新并提交（`f86b972`）

---

_Fixed: 2026-05-28T08:00:18Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_

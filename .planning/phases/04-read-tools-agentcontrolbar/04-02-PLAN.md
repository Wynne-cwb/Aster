---
phase: 04-read-tools-agentcontrolbar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/DocumentAdapter.ts
  - eslint.config.js
  - src/agent/__fixtures__/ns-violation.ts
autonomous: true
requirements: [TOOL-01, TOOL-07]
must_haves:
  truths:
    - "DocumentAdapter 接口含第 5 方法 read(query): Promise<ReadableResult>"
    - "ReadableQuery 是 11+1 个 kind 的 discriminated union，每个 kind 对应一个 read tool name"
    - "src/agent 或 src/store 内引用 PowerPoint/Excel/Word 命名空间 → eslint error；adapter 目录内合法"
  artifacts:
    - path: "src/adapters/DocumentAdapter.ts"
      provides: "ReadableQuery / ReadableResult 类型 + read() 接口方法"
      contains: "read(query: ReadableQuery)"
    - path: "eslint.config.js"
      provides: "TOOL-07 Office namespace 限制 rule（新建，排除 adapter 目录）"
      contains: "PowerPoint"
    - path: "src/agent/__fixtures__/ns-violation.ts"
      provides: "TOOL-07 lint 冒烟 fixture（构造违例）"
  key_links:
    - from: "src/agent/tools/read/*"
      to: "src/adapters/DocumentAdapter.ts"
      via: "ctx.adapter.read({ kind })（Plan 03/04/05/06 消费此契约）"
      pattern: "ReadableQuery"
---

<objective>
建立整个 Phase 4 read tool 的**契约前提**（其余 read plan 都依赖此 plan 的类型）：

1. **read 接口类型（TOOL-01）** — 在 `src/adapters/DocumentAdapter.ts`（纯类型文件，0 import）加 `ReadableQuery`（kind 判别 discriminated union，与 11 个 read tool name 1:1）+ `ReadableResult`（ok 判别）+ `read(query): Promise<ReadableResult>` 作为接口第 5 方法。
2. **TOOL-07 eslint namespace rule（新建）** — **当前 eslint.config.js 没有 Office namespace 限制 rule**（RESEARCH/PATTERNS 已纠正 CONTEXT 的「Phase 3 已埋」错误描述）。本 plan 新建 rule 禁止 `PowerPoint`/`Excel`/`Word` 命名空间出现在 `src/agent/**` 与 `src/store/**`，**用 files-override 排除 `src/adapters/*Adapter.ts`**（adapter 合法使用）。配 lint 冒烟 fixture 验证。

Purpose: 接口先定（interface-first），下游三宿主实现与 tool def 直接 implement against 此契约，executor 无需探索代码库。TOOL-07 rule 守 A-06 proxy 不出 *.run 闭包的边界。
Output: 扩展后的接口类型文件 + 新 eslint rule + lint 冒烟 fixture。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md

<interfaces>
DocumentAdapter.ts 现状（src/adapters/DocumentAdapter.ts，纯类型 0 import）：
- L48-52 `SelectionContext` 四变体 union（判别字段 `kind`）— ReadableQuery 复用此判别字段名风格
- L158-184 `interface DocumentAdapter` 现有 4 方法：getSelection / onSelectionChanged / capabilities / insert
- `ToolError` 在 src/agent/tools/index.ts —— **不能 import**（破坏 0 依赖 + 制造 adapter→agent 反向依赖）

eslint.config.js 现状（确认 RESEARCH 纠正）：
- 现仅有 `no-restricted-syntax`（legacy 模型名）+ `no-restricted-imports`（SDK 包）+ `aster/require-human-label: 'off'` 占位
- **无任何 Office namespace 限制 rule** — 本 plan 新建
- 现 block 是单一 `{ files: ['src/**/*.ts', 'src/**/*.tsx'], ... }`（L24-94）；新增独立 files-override block

eslint flat config 数组：append 新 block 到 `export default [ ... ]` 数组末尾。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReadableQuery / ReadableResult 类型 + read() 接口方法（TOOL-01）</name>
  <read_first>
    - src/adapters/DocumentAdapter.ts（被改文件全文；L48-52 SelectionContext union 范式、L158-184 接口块、L4 注释「纯类型 0 import」）
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Code Examples「read 接口类型」L295-321（ReadableQuery 11+1 kind 完整列表 + ReadableResult 形态）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §`src/adapters/DocumentAdapter.ts` L92-96（复用 SelectionContext 判别字段 + ToolError 不可 import 的处理）
  </read_first>
  <action>
    在 `src/adapters/DocumentAdapter.ts` 加（照 RESEARCH §Code Examples L295-321 逐字）：
    - `export type ReadableQuery =` discriminated union（判别字段 `kind`，与 SelectionContext 同款），含 12 个变体：
      - `{ kind: 'selection_detail' }`（跨宿主）
      - PPT：`{ kind: 'list_slides' }` / `{ kind: 'get_slide'; slideIndex: number }`（1-based，与 SelectionContext 一致）/ `{ kind: 'list_shapes_on_slide'; slideIndex: number }` / `{ kind: 'get_shape'; slideIndex: number; shapeId: string }`
      - Excel：`{ kind: 'list_worksheets' }` / `{ kind: 'get_range_values'; address: string }` / `{ kind: 'get_used_range_summary'; sheetName?: string }`
      - Word：`{ kind: 'get_paragraph_count' }` / `{ kind: 'get_paragraph_at'; index: number }` / `{ kind: 'get_document_outline' }` / `{ kind: 'get_document_full_text' }`
    - `ReadableResult`：**不 import ToolError**（保 0 依赖）。type-only 复制 ToolError 的形态到本文件，命名 `ReadToolError`（含 `code: string; message: string; recoverable: boolean; hint: string`），并加注释说明「与 src/agent/tools/index.ts ToolError 形态对齐，因 0-import 约束不直接 import（防 adapter→agent 反向依赖）」。然后：
      - `export type ReadableResult = { ok: true; data: unknown } | { ok: false; error: ReadToolError };`
    - 在 `interface DocumentAdapter`（L158）末尾、`insert()` 之后加第 5 方法：`read(query: ReadableQuery): Promise<ReadableResult>;`，jsdoc 注释风格照现有 insert()（L178-183）：说明「per-query 离散只读（TOOL-01），禁 fat inspect 返整 doc model；每个 kind 对应一个 LLM read tool name（1:1 dispatch）；实现在各 *Adapter.read() switch 内，proxy 不出 *.run 闭包（A-06/TOOL-07）」。
    - 文件保持 0 运行时 import（只加 type/interface）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "DocumentAdapter" || echo "no DocumentAdapter type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kind: 'get_paragraph_count'" src/adapters/DocumentAdapter.ts` == 1
    - `grep -c "kind: 'list_slides'" src/adapters/DocumentAdapter.ts` == 1
    - `grep -c "read(query: ReadableQuery): Promise<ReadableResult>" src/adapters/DocumentAdapter.ts` == 1
    - `grep -c "^import" src/adapters/DocumentAdapter.ts` == 0（仍 0 import）
    - `grep -E "kind: '(selection_detail|list_slides|get_slide|list_shapes_on_slide|get_shape|list_worksheets|get_range_values|get_used_range_summary|get_paragraph_count|get_paragraph_at|get_document_outline|get_document_full_text)'" src/adapters/DocumentAdapter.ts` 命中全部 12 个 kind
  </acceptance_criteria>
  <done>DocumentAdapter 含 ReadableQuery 12 变体 + ReadableResult + read() 第 5 方法；文件仍 0 import；tsc 无新错误（adapter 实现缺失会在 Plan 03/04/05 补，本 plan 只定接口，stub 暂未实现是预期 — 见 Task 注）。</done>
</task>

<task type="auto">
  <name>Task 2: TOOL-07 Office namespace eslint rule（新建 + 冒烟 fixture）</name>
  <read_first>
    - eslint.config.js（被改文件全文；L74-92 no-restricted-imports files-scoped block 范式、L40-48 no-restricted-syntax 结构）
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Code Examples「TOOL-07 eslint rule」L422-438 + Pitfall 3 L278-280 + Assumption A3 L474（no-restricted-globals vs no-restricted-syntax fallback）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §`eslint.config.js` L218-231（新建 rule + 排除 adapter 目录 + 冒烟验证）
  </read_first>
  <action>
    在 `eslint.config.js` 的 `export default [ ... ]` 数组**末尾**追加一个独立 files-override block（照 RESEARCH §Code Examples L422-438）：
    ```javascript
    {
      files: ['src/agent/**/*.ts', 'src/store/**/*.ts'],
      ignores: ['src/agent/__fixtures__/**'],   // fixture 故意违例，由专门冒烟脚本单独 lint
      rules: {
        'no-restricted-globals': ['error',
          { name: 'PowerPoint', message: 'Office namespace 只能在 src/adapters/*Adapter.ts 内使用（A-06/TOOL-07）' },
          { name: 'Excel', message: 'Office namespace 只能在 src/adapters/*Adapter.ts 内使用（A-06/TOOL-07）' },
          { name: 'Word', message: 'Office namespace 只能在 src/adapters/*Adapter.ts 内使用（A-06/TOOL-07）' },
        ],
      },
    }
    ```
    注意：`src/adapters/*Adapter.ts` 不在 `files` 匹配内 → 天然不受限（合法使用 namespace）。
    建 lint 冒烟 fixture `src/agent/__fixtures__/ns-violation.ts`：写一行明显违例，如 `export async function bad() { return await PowerPoint.run(async () => {}); }`（含 `// eslint 冒烟 fixture：本文件故意违反 TOOL-07，由 ns-violation 冒烟脚本单独 lint 验证应报 error` 注释）。
    **Assumption A3 验证（必做）**：跑 `npx eslint src/agent/__fixtures__/ns-violation.ts --no-ignore --rule '{"no-restricted-globals":["error",{"name":"PowerPoint"}]}'`（绕过 ignores 临时验证）。若 `no-restricted-globals` 把 `PowerPoint.run` 当 member 而非裸 global 引用 **未触发** error → 改用 `no-restricted-syntax` selector `"MemberExpression[object.name=/^(PowerPoint|Excel|Word)$/]"`（PATTERNS L231 / RESEARCH A3）替换上面 rule，重测直到 fixture 报 error。
    最终确认：(a) fixture 单独 lint 报 error；(b) `npx eslint src/agent src/store --ext .ts` 对真实代码不误报（现有 agent/store 代码无 Office namespace 引用，应全绿）；(c) `npx eslint src/adapters` 不因新 rule 报 namespace error（adapter 合法）。
  </action>
  <verify>
    <automated>npx eslint src/agent src/store 2>&1 | grep -v "__fixtures__" | grep -i "no-restricted" && echo "UNEXPECTED: real code violates" || echo "real code clean"; npx eslint src/agent/__fixtures__/ns-violation.ts 2>&1 | grep -iq "PowerPoint\|no-restricted" && echo "fixture correctly flagged" || echo "FAIL: rule not triggered"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PowerPoint" eslint.config.js` ≥ 1（新 rule 存在）
    - `grep -E "files: \['src/agent" eslint.config.js` 命中新 files-override block
    - fixture lint 触发 error（上面 verify 输出含 "fixture correctly flagged"）
    - 真实 src/agent + src/store 代码不被新 rule 误报（verify 输出含 "real code clean"）
    - `npx eslint src/adapters/PptAdapter.ts` 不报 no-restricted-globals（adapter 排除生效）
  </acceptance_criteria>
  <done>TOOL-07 rule 新建并冒烟通过：agent/store 引用 Office namespace 报 error，adapter 目录合法，fixture 验证 rule 真生效。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Office.js proxy → store/agent 层 | A-06：proxy 对象生命周期 = *.run 闭包；跨 await 边界失效。TOOL-07 rule 在编译期阻止 proxy 泄漏到 agent/store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Office namespace 出 *.run 闭包 → 跨 await 写错位 | mitigate | TOOL-07 eslint rule 禁 PowerPoint/Excel/Word 出现在 src/agent + src/store；adapter 目录 files-override 排除；lint 冒烟 fixture 验证 rule 真生效（A-06/TOOL-07） |
| T-04-05 | (架构约束) | ReadableQuery per-query 离散 | mitigate | 接口设计成 per-kind 离散 read，无 fat inspect()，防单步 50K+ context（AP-2） |
</threat_model>

<verification>
- `npx tsc --noEmit` 对 DocumentAdapter 无新类型错误（read() 未实现的 adapter stub 错误归 Plan 03/04/05，本 plan 仅定接口）
- TOOL-07 lint 冒烟：fixture 报 error + 真实代码不误报 + adapter 排除生效
- `npm run build` 通过
</verification>

<success_criteria>
- ReadableQuery 12 变体覆盖全部 11 read tool + selection_detail，每 kind 与 tool name 1:1
- DocumentAdapter 仍 0 import（type-only 复制 ReadToolError）
- TOOL-07 rule 编译期拦截 namespace 泄漏，adapter 合法
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-02-SUMMARY.md`
</output>

---
phase: 04-read-tools-agentcontrolbar
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/adapters/ExcelAdapter.ts
  - src/adapters/ExcelAdapter.read.test.ts
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-06]
must_haves:
  truths:
    - "ExcelAdapter.read({kind:'list_worksheets'}) 返工作表名清单（metadata）"
    - "ExcelAdapter.read({kind:'get_range_values', address}) 先 load cellCount 判定，>10K cells 拒绝返 INVALID_ARGS（不 load values）"
    - "ExcelAdapter.read({kind:'get_used_range_summary'}) 返 used range 概况 + 首行 schema（不读全部 values）"
    - "ExcelAdapter.read({kind:'selection_detail'}) 复用 getSelection 语义"
  artifacts:
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "read(query) switch：Excel 4 个 read kind（3 Excel 专属 + selection_detail）+ A-24 读前判定"
      contains: "async read(query: ReadableQuery)"
    - path: "src/adapters/ExcelAdapter.read.test.ts"
      provides: "mock Office Excel.run 验证 >10K cells 读前拒绝 + 各 kind"
  key_links:
    - from: "src/adapters/ExcelAdapter.ts get_range_values"
      to: "Excel.Range.cellCount"
      via: "先 load cellCount/rowCount/columnCount → sync → 判 >10K → 才 load values（A-24 读前判定）"
      pattern: "cellCount"
---

<objective>
实现 ExcelAdapter 的 `read(query)` 方法（TOOL-01/02/06），收口 Excel 宿主全部读法。**核心防御 = A-24「读前判定」**：`get_range_values` 必须先只 load `cellCount` 计数，>10K cells 直接拒绝（返 INVALID_ARGS 引导走 summary），**绝不在读 values 之后才判大小**（100K 行 × 50 列 JSON ~200MB 撑爆 tab）。

5 个 kind 中 Excel 占 4：
- `list_worksheets` — 工作表名清单（metadata）
- `get_range_values` — 指定 address 的值（document_content；>10K cells 读前拒绝 TOOL-06/A-24）
- `get_used_range_summary` — used range 概况 + 首行 schema（metadata，不读全部 values）
- `selection_detail` — 复用 getSelection 语义

Purpose: SC2「Excel 告诉我当前 used range 的形状和前 20 行」（get_used_range_summary + get_range_values）+ A-24 OOM 防御真机跑通。
Output: ExcelAdapter.read() 实现 + mock Office 单测（含 >10K cells 拒绝断言）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md

<interfaces>
read() 契约（Plan 02 已加到 src/adapters/DocumentAdapter.ts）：
```typescript
// Excel 相关 kind：
//   { kind: 'list_worksheets' }
//   { kind: 'get_range_values'; address: string }
//   { kind: 'get_used_range_summary'; sheetName?: string }
//   { kind: 'selection_detail' }
```

ExcelAdapter 现有范式（src/adapters/ExcelAdapter.ts）：
- getSelection() L26-46：`Excel.run` 闭包 load address 单 sync
- insert() append_end L120-142：`getActiveWorksheet().getUsedRange(false)` 不抛空表（WR-06 守则）+ two-sync 范式
- HostApiError 包装 L43-45

VERIFIED Office.js API（@types/office-js@1.0.591，RESEARCH Pitfall 1 + Code Examples）：
- `Excel.Range.cellCount` / `rowCount` / `columnCount`（readonly，**可在 load values 前先 load 计数**）
- `getActiveWorksheet().getRange(address)` / `.getUsedRange(false)`（false=不抛空表）
- `ctx.workbook.worksheets`（.load('items/name')）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExcelAdapter.read() switch 实现（4 kind，A-24 读前判定）</name>
  <read_first>
    - src/adapters/ExcelAdapter.ts（被改文件全文；getSelection L26-46 + insert append_end getUsedRange(false) L120-142 + HostApiError L43-45）
    - src/adapters/DocumentAdapter.ts（Plan 02 加的 ReadableQuery / ReadableResult / ReadToolError）
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Code Examples「Excel get_range_values（A-24 防御）」L323-342 + 「get_used_range_summary」L345-356 + §Pitfall 1 L268-272
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §SP-A + §`{Ppt,Excel,Word}Adapter` L102（Excel A-24 防御）
  </read_first>
  <behavior>
    - `read({kind:'list_worksheets'})` → `{ ok:true, data:{ worksheets:['Sheet1','Sheet2',...] } }`。
    - `read({kind:'get_range_values', address:'A1:C20'})`（≤10K cells）→ 先 load cellCount → sync → cellCount ≤10000 → load values → sync → `{ ok:true, data:{ address, rowCount, values:[[...]] } }`。
    - `read({kind:'get_range_values', address:'A1:Z100000'})`（>10K cells）→ load cellCount → sync → cellCount >10000 → **不 load values** → 返 `{ ok:false, error:{ code:'INVALID_ARGS', message:`选区有 N 个单元格，过大无法整块读取`, recoverable:true, hint:'请改用 get_used_range_summary 看概况，或指定更小的 address' } }`。
    - `read({kind:'get_used_range_summary'})` → `{ ok:true, data:{ address, rowCount, columnCount, headerSample:[首行值] } }`（不读全部 values）。
    - `read({kind:'selection_detail'})` → `{ ok:true, data: <getSelection()> }`。
    - Office.js 抛错 → `throw new HostApiError('Excel <kind> 失败', err)`。
  </behavior>
  <action>
    先写 `src/adapters/ExcelAdapter.read.test.ts`（RED），mock Excel global（grep 现有 mock 套路）。**必须含 >10K cells 拒绝断言**：mock `range.cellCount = 50000` → 验证返 INVALID_ARGS 且 **未调 `range.load('values')`**（用 mock spy 断言 values 未被 load — 这是 A-24 核心验收）。
    然后在 `src/adapters/ExcelAdapter.ts` 加 `async read(query: ReadableQuery): Promise<ReadableResult>`：
    - import `ReadableQuery`, `ReadableResult` from `./DocumentAdapter`。
    - `const CELL_LIMIT = 10_000;`（常量，TOOL-06）。
    - `switch (query.kind)`：
      - `case 'list_worksheets'`：Excel.run → `const ws = ctx.workbook.worksheets; ws.load('items/name'); await ctx.sync(); return { ok:true, data:{ worksheets: ws.items.map(w=>w.name) } };`
      - `case 'get_range_values'`：照 RESEARCH §Code Examples L323-342 **逐字**：
        ```
        Excel.run(async (ctx) => {
          const range = ctx.workbook.worksheets.getActiveWorksheet().getRange(query.address);
          range.load(['cellCount', 'rowCount', 'columnCount']);   // 先只 load 计数
          await ctx.sync();                                       // sync 1
          if (range.cellCount > CELL_LIMIT) {
            return { ok:false, error:{ code:'INVALID_ARGS', message:`选区有 ${range.cellCount} 个单元格，过大无法整块读取`, hint:'请改用 get_used_range_summary 看概况，或指定更小的 address', recoverable:true } };
          }
          range.load('values');
          await ctx.sync();                                       // sync 2：确认安全后才读
          return { ok:true, data:{ address: query.address, rowCount: range.rowCount, values: range.values } };
        })
        ```
      - `case 'get_used_range_summary'`：照 RESEARCH §Code Examples L345-356：`getActiveWorksheet().getUsedRange(false)`（false 不抛空表，WR-06）→ load `['address','rowCount','columnCount']` + 首行 `used.getRow(0).load('values')` → sync → 返 `{ ok:true, data:{ address, rowCount, columnCount, headerSample: <首行 values 或 []> } }`。（若指定 sheetName 则 `getActiveWorksheet` 换成 `ctx.workbook.worksheets.getItem(query.sheetName)`，缺省用 active。）
      - `case 'selection_detail'`：`return { ok:true, data: await this.getSelection() };`
      - `default`：UNSUPPORTED。
    - 每 case try/catch → `throw new HostApiError('Excel <kind> 失败', err)`（与 getSelection L43-45 一致）。
    - **铁律：get_range_values 的 `range.load('values')` 必须在 cellCount 判定之后**（A-24）。
  </action>
  <verify>
    <automated>npx vitest run src/adapters/ExcelAdapter.read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/adapters/ExcelAdapter.read.test.ts` exits 0
    - `grep -c "async read(query: ReadableQuery)" src/adapters/ExcelAdapter.ts` == 1
    - `grep -c "cellCount" src/adapters/ExcelAdapter.ts` ≥ 2（读前 load + 判定）
    - `grep -E "list_worksheets|get_range_values|get_used_range_summary|selection_detail" src/adapters/ExcelAdapter.ts` 命中全部 4 个 kind
    - `grep -c "getUsedRange(false)" src/adapters/ExcelAdapter.ts` ≥ 1（summary 不抛空表）
    - 测试含 >10K cells → INVALID_ARGS 且 values 未被 load 的断言（A-24 核心）
    - `npx eslint src/adapters/ExcelAdapter.ts` 不报 no-restricted-globals
  </acceptance_criteria>
  <done>ExcelAdapter.read() 4 kind 全实现，get_range_values 读前 cellCount 判定 >10K 拒绝（不 load values），mock 单测含 A-24 拒绝断言，全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Excel 单元格值 → ReadableResult | get_range_values 读出用户数据，上层 wrapReadResult 标 document_content |
| Office.js proxy → 闭包外 | A-06：range/worksheet proxy 只在 Excel.run 闭包内 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | DoS | get_range_values 大区域读爆内存（A-24） | mitigate | **读前判定**：先 load cellCount → sync → >10K 拒绝返 INVALID_ARGS，绝不在 load values 后判；mock 单测断言 values 未被 load |
| T-04-15 | Tampering | range/worksheet proxy 出闭包 | mitigate | 闭包内 load+sync 取 plain values/names 返出；SP-A 范式 |
| T-04-16 | Information Disclosure | Excel.run 抛错带 stack | mitigate | catch → HostApiError 不存 hostError；dispatch sanitize |
| T-04-17 | DoS | get_used_range_summary 空表抛 ItemNotFound | mitigate | getUsedRange(false) 不抛空表（WR-06 守则，insert L121-123 已验范式） |
</threat_model>

<verification>
- `npx vitest run src/adapters/ExcelAdapter.read.test.ts` 全绿（含 A-24 拒绝断言）
- `npm run test -- --run` 全套不回归
- `npx tsc --noEmit` Excel read 路径无类型错误
- `npm run build` 通过
</verification>

<success_criteria>
- ExcelAdapter 4 read kind 实现，get_range_values 读前判定 >10K 拒绝
- summary 不读全部 values，空表不抛，0 新依赖
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-05-SUMMARY.md`
</output>

---
phase: 04-read-tools-agentcontrolbar
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/adapters/WordAdapter.ts
  - src/adapters/WordAdapter.read.test.ts
autonomous: true
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "WordAdapter.read({kind:'get_paragraph_count'}) 返段落总数纯数据"
    - "WordAdapter.read({kind:'get_paragraph_at', index}) 返指定段文本；越界返 NOT_FOUND"
    - "WordAdapter.read({kind:'get_document_outline'}) 用 styleBuiltIn 匹配 Heading\\d 抽层级"
    - "WordAdapter.read({kind:'get_document_full_text'}) 返全文（唯一全量读，下游过 size cap）"
    - "WordAdapter.read({kind:'selection_detail'}) 复用 getSelection 语义"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "read(query) switch：Word 5 个 read kind 实现（4 Word 专属 + selection_detail）"
      contains: "async read(query: ReadableQuery)"
    - path: "src/adapters/WordAdapter.read.test.ts"
      provides: "mock Office Word.run 验证每 kind 返纯数据 + 越界 NOT_FOUND"
  key_links:
    - from: "src/adapters/WordAdapter.ts read()"
      to: "Word.run 闭包"
      via: "load + sync 取纯数据，proxy 不出闭包（A-06）"
      pattern: "Word\\.run"
---

<objective>
实现 WordAdapter 的 `read(query)` 方法（TOOL-01/02），收口 Word 宿主全部读法。新增一个 `read(query: ReadableQuery): Promise<ReadableResult>` 方法，内部 switch 覆盖 5 个 kind：

- `get_paragraph_count` — 段落总数（metadata）
- `get_paragraph_at` — 指定 index 段落文本（document_content；越界返 NOT_FOUND）
- `get_document_outline` — 用 `paragraph.styleBuiltIn` 匹配 `/Heading\d/` 抽 `{level, text, paragraphIndex}`（metadata，跨语言 portable，**不用本地化 .style 字符串**）
- `get_document_full_text` — 全文（唯一全量读，document_content）
- `selection_detail` — 复用现有 getSelection 语义（document_content）

Purpose: SC2 要求「Word 数一下文档有几段并把第 3 段读出来」（get_paragraph_count + get_paragraph_at）真机跑通。
Output: WordAdapter.read() 实现 + mock Office 单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md

<interfaces>
read() 契约（Plan 02 已加到 src/adapters/DocumentAdapter.ts）：
```typescript
read(query: ReadableQuery): Promise<ReadableResult>;
// ReadableResult = { ok: true; data: unknown } | { ok: false; error: ReadToolError };
// Word 相关 kind：
//   { kind: 'get_paragraph_count' }
//   { kind: 'get_paragraph_at'; index: number }   // index 约定见 action
//   { kind: 'get_document_outline' }
//   { kind: 'get_document_full_text' }
//   { kind: 'selection_detail' }
```

WordAdapter 现有范式（src/adapters/WordAdapter.ts）：
- getSelection() L23-45：`Word.run` 闭包内 `selection.load('text')` → `ctx.sync()` → 取 plain value 返回；catch `throw new HostApiError('Word ... 失败', err)`
- appendParagraph() L127-136：`ctx.document.body.insertParagraph(...)` 范式
- 注释 L121-125：HostApiError 构造器不存 hostError（防 stack 泄漏）

VERIFIED Office.js API（@types/office-js@1.0.591，RESEARCH §Pitfall 5）：
- `ctx.document.body.paragraphs`（ParagraphCollection）→ `.load('items')` → `.items[i].text`
- `Paragraph.styleBuiltIn`（值如 "Heading1".."Heading9"）+ `Paragraph.outlineLevel`（number）
- `ctx.document.body.text`（全文 string）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WordAdapter.read() switch 实现（5 kind）</name>
  <read_first>
    - src/adapters/WordAdapter.ts（被改文件全文；getSelection L23-45 + appendParagraph L127-136 闭包范式 + HostApiError 注释 L121-125）
    - src/adapters/DocumentAdapter.ts（Plan 02 已加的 ReadableQuery / ReadableResult / ReadToolError 类型）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §SP-A L41-66（闭包纯数据进出铁律）+ §`{Ppt,Excel,Word}Adapter` L98-104（Word 部分 + outline styleBuiltIn）
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Pitfall 5 L286-288（styleBuiltIn 抽 outline，VERIFIED 行号）
  </read_first>
  <behavior>
    - `read({kind:'get_paragraph_count'})` → `{ ok:true, data:{ count: N } }`，N = 段落数。
    - `read({kind:'get_paragraph_at', index:2})`（约定 0-based；「第 3 段」= index 2）→ `{ ok:true, data:{ index:2, text:'...' } }`。
    - `read({kind:'get_paragraph_at', index:999})`（越界）→ `{ ok:false, error:{ code:'NOT_FOUND', message:'第 1000 段不存在...', recoverable:false, hint:'...' } }`（不抛）。
    - `read({kind:'get_document_outline'})` → `{ ok:true, data:{ outline: [{ level, text, paragraphIndex }, ...] } }`，只收 styleBuiltIn 匹配 /Heading\d/ 的段。
    - `read({kind:'get_document_full_text'})` → `{ ok:true, data:{ text:'<全文>' } }`。
    - `read({kind:'selection_detail'})` → `{ ok:true, data: <getSelection() 结果> }`。
    - Office.js 抛错 → `throw new HostApiError('Word <kind> 失败', err)`（由 dispatch sanitize，不在 read 内读 stack）。
  </behavior>
  <action>
    先写 `src/adapters/WordAdapter.read.test.ts`（RED），用 mock Office Word global（参考 RESEARCH §Validation 说 adapter 测试用 mock Office global；若 repo 已有 mock 范式则复用现有 *Adapter.test.ts 的 mock 套路 — 先 grep `src/adapters/*.test.ts` 看现有 mock 写法）。覆盖上面 behavior 每个 kind + 越界 NOT_FOUND。
    然后在 `src/adapters/WordAdapter.ts` 加 `async read(query: ReadableQuery): Promise<ReadableResult>` 方法（照 SP-A 闭包范式，每个 case 各自开闭一次 Word.run，纯数据出闭包）：
    - import `ReadableQuery`, `ReadableResult` from `./DocumentAdapter`（type import）。
    - `switch (query.kind)`：
      - `case 'get_paragraph_count'`：Word.run → `const paras = ctx.document.body.paragraphs; paras.load('items/text'); await ctx.sync(); return { ok:true, data:{ count: paras.items.length } };`
      - `case 'get_paragraph_at'`：Word.run → load paragraphs items/text → sync → 若 `query.index < 0 || query.index >= paras.items.length` 返 `{ ok:false, error:{ code:'NOT_FOUND', message:`第 ${query.index+1} 段不存在（共 ${paras.items.length} 段）`, recoverable:false, hint:'请先用 get_paragraph_count 确认段数' } }`；否则 `{ ok:true, data:{ index: query.index, text: paras.items[query.index].text } }`。
      - `case 'get_document_outline'`：Word.run → `paras.load('items/text,items/styleBuiltIn'); await ctx.sync();` → map items 过滤 `/^Heading(\d)$/.test(p.styleBuiltIn)`，对命中的取 `level = Number(RegExp.$1)`（或重新 match 拿组）+ `text: p.text` + `paragraphIndex: <在 items 中的下标>` → `{ ok:true, data:{ outline } }`。**用 styleBuiltIn 不用 .style（RESEARCH Pitfall 5）**。
      - `case 'get_document_full_text'`：Word.run → `const body = ctx.document.body; body.load('text'); await ctx.sync(); return { ok:true, data:{ text: body.text } };`（注：全量读，size cap 在 tool execute 层 wrapReadResult 处理，adapter 不截断）。
      - `case 'selection_detail'`：`return { ok:true, data: await this.getSelection() };`（复用现有方法）。
      - `default`：返 `{ ok:false, error:{ code:'UNSUPPORTED', message:`Word 不支持 read kind: ${(query as {kind:string}).kind}`, recoverable:false, hint:'该 kind 属其它宿主' } }`（Word adapter 只处理 Word kind + selection_detail；PPT/Excel kind 走 default UNSUPPORTED — 实际 buildToolsForHost 已按 host 隔离 tool，default 是防御）。
    - 整个 read 体外层 **不**包 try/catch（每个 Word.run case 内 catch → `throw new HostApiError('Word ... 失败', err)`；或外层统一 try/catch 包 HostApiError，二选一保持与 getSelection 一致 — 推荐每 case 内 try/catch 与 getSelection L42-44 一致风格）。
  </action>
  <verify>
    <automated>npx vitest run src/adapters/WordAdapter.read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/adapters/WordAdapter.read.test.ts` exits 0
    - `grep -c "async read(query: ReadableQuery)" src/adapters/WordAdapter.ts` == 1
    - `grep -c "styleBuiltIn" src/adapters/WordAdapter.ts` ≥ 1（outline 用 styleBuiltIn 不用 .style）
    - `grep -c "\.style\b" src/adapters/WordAdapter.ts` == 0（确认未用本地化 .style）
    - `grep -E "get_paragraph_count|get_paragraph_at|get_document_outline|get_document_full_text|selection_detail" src/adapters/WordAdapter.ts` 命中全部 5 个 kind
    - 测试含越界 index → NOT_FOUND 的断言
    - `npx eslint src/adapters/WordAdapter.ts` 不报 no-restricted-globals（adapter 目录被 TOOL-07 排除）
  </acceptance_criteria>
  <done>WordAdapter.read() 5 kind 全实现，mock 单测全绿，outline 用 styleBuiltIn，越界返 NOT_FOUND，proxy 不出闭包。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Word 文档内容 → ReadableResult | get_paragraph_at / get_document_full_text 读出用户正文（含潜在 injection），原样返 data 由上层 wrapReadResult 标 document_content |
| Office.js proxy → 闭包外 | A-06：proxy 只在 Word.run 闭包内，纯数据返出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | proxy 出 Word.run 闭包 | mitigate | 每 case 闭包内 load+sync 取 plain value 返回；TOOL-07 eslint 编译期守（adapter 目录用 namespace 合法但 proxy 不返出，靠代码纪律 + SP-A 范式） |
| T-04-07 | Information Disclosure | Word.run 抛错带 stack | mitigate | catch → `throw new HostApiError`，构造器不存 hostError；dispatch allowlist sanitize（D-15） |
| T-04-08 | DoS | get_document_full_text 全量读超长 | mitigate | adapter 不截断，size cap 在 tool execute 层 wrapReadResult（Plan 01 applySizeCap，50K token）|
| T-04-09 | Input Validation | get_paragraph_at index 越界 | mitigate | bounds check 返 NOT_FOUND，不抛、不越界访问 |
</threat_model>

<verification>
- `npx vitest run src/adapters/WordAdapter.read.test.ts` 全绿
- `npm run test -- --run` 全套不回归
- `npx tsc --noEmit` Word read 路径无类型错误
- `npm run build` 通过
</verification>

<success_criteria>
- WordAdapter 5 个 read kind 实现且 mock 单测覆盖
- outline 用 portable styleBuiltIn，越界返 NOT_FOUND
- 0 新依赖
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-03-SUMMARY.md`
</output>

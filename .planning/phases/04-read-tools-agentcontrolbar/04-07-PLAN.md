---
phase: 04-read-tools-agentcontrolbar
plan: 07
type: execute
wave: 4
depends_on: [01, 06]
files_modified:
  - src/agent/agentStore.ts
  - src/components/AgentControlBar.tsx
  - src/components/ChatStream.tsx
  - src/styles.css
  - src/components/AgentControlBar.test.tsx
  - src/components/ChatStream.giveup.test.tsx
autonomous: true
requirements: [AGENT-12, ERR-04]
must_haves:
  truths:
    - "AgentControlBar 随 currentPhase 显示差异化文案：thinking→正在思考/reading→正在读取/writing→正在写入"
    - "agent 5 秒无 UI 更新时 AgentControlBar 显示安抚行（文案随 phase），计时器挂组件 useEffect 不进 store"
    - "circuit abort 后 ChatStream 显示「Agent gave up」红卡：X 次失败说明 + Y LLM 建议 + 「重新试试」按钮，无撤销按钮"
    - "read 折叠卡展开显示 source + content 截断预览（前 500 字），不 dump 整个 JSON"
  artifacts:
    - path: "src/components/AgentControlBar.tsx"
      provides: "三态差异化文案 + 5 秒安抚行"
      contains: "currentPhase"
    - path: "src/components/ChatStream.tsx"
      provides: "Agent gave up 红卡 + read 折叠卡截断预览"
    - path: "src/agent/agentStore.ts"
      provides: "lastAbortReason 扩展存 circuit 元数据 {toolName,code,count}"
  key_links:
    - from: "src/components/AgentControlBar.tsx"
      to: "src/agent/agentStore.ts currentPhase/lastUpdateTs"
      via: "useAgentStore selector + useEffect setInterval 比对"
      pattern: "currentPhase|lastUpdateTs"
    - from: "src/components/ChatStream.tsx 红卡"
      to: "circuit log + LLM 最后建议"
      via: "lastAbortReason 元数据 + agentRunId 下最后 assistant content"
      pattern: "重新试试"
---

<objective>
Phase 4 的 UI 层（AGENT-12 / ERR-04），全部走 CSS 变量 + 内联 SVG（D-11），light/dark 两套主题：

1. **AgentControlBar 三态差异化文案（AGENT-12 / A-12）** — 在现有 step counter 旁加一行随 `currentPhase` 变的文案：thinking→「正在思考…」、reading→「正在读取…」、writing→「正在写入…」。不是统一 spinner。pause/abort/step counter（Phase 3 已落）不动。
2. **5 秒无更新安抚行（D-03）** — 组件内 `useEffect` 起 `setInterval(~1s)` 比对 `Date.now() - lastUpdateTs > 5000`，超时显示安抚行（文案随 phase：「还在跑，正在等 LLM 思考…」/「正在读取大区域，稍候…」）。**计时器挂组件不进 store**（SP-C / RESEARCH Pitfall 6，避免每秒 setState 全量 re-render）。不加「复制日志」按钮（CARRY-03 是 Phase 5）、不加 abort 强引导。
3. **「Agent gave up」红卡（ERR-04）** — circuit abort 后 ChatStream 渲染红卡：说明「试了 X 次都失败（如 write_locked），建议 Y」（X 来自 circuit log 计数，Y 来自 LLM 最后建议）+「重新试试」按钮（重开 agent run）。**坚决不放「撤销本次」按钮**（D-05 / 诚实禁用，undo all 是 Phase 5）。
4. **read 折叠卡展开预览改进（RESEARCH Pitfall 4）** — 现 `<pre>{JSON.stringify(toolResult)}</pre>` 改成展开显示 source + content 截断预览（前 500 字 + 「…(共 X 字)」），避免 document_content 刷屏。

Purpose: SC3 三态文案 + 5 秒安抚 / SC5「Agent gave up」红卡真机验收的 UI。
Output: AgentControlBar 三态 + 安抚、ChatStream 红卡 + 截断预览、agentStore lastAbortReason 扩展、styles.css 红卡 token、组件单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md
@.planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md
@CLAUDE.md

<interfaces>
agentStore（Plan 06 已加 currentPhase/lastUpdateTs/setPhase）；本 plan 扩展 lastAbortReason：
- 现 `lastAbortReason: AbortReason | null`（AbortReason='visibility'|'user'|'max_steps'|'circuit'）
- 本 plan 扩展存 circuit 元数据。推荐新增字段 `lastCircuitInfo: { toolName: string; code: string; count: number } | null`（不改 lastAbortReason 类型，避免 4 路 abort 调用方都改），circuit-breaker 暴露 `getFailureSummary(tool)` 或 abort('circuit') 时一并 set。

AgentControlBar 现状（src/components/AgentControlBar.tsx）：
- L23-27 selector 订阅（status/currentStep/pause/resume/abort）— 加 currentPhase/lastUpdateTs selector
- L38-41 step counter `<span>` 显示位 — 旁加三态文案行
- L16-17 useLingui `t\`...\`` 已在

ChatStream 现状（src/components/ChatStream.tsx）：
- ToolResultCard L57-112：soft-landing 卡 L62-85（红卡模板）+ 常规折叠卡 L88-110（展开 `<pre>JSON.stringify` L106-108）
- L142 retryMessage 已订阅；红卡「重新试试」用此或 useAgentStore.runAgent
- L89 `isError = message.toolResult?.ok === false`；L90 `aster-tool-card--error` 类已有

icons.tsx 已有：AlertIcon（L102）/ RetryIcon（L82）/ StopIcon / PauseIcon / PlayIcon — 红卡复用 AlertIcon + RetryIcon。

Message（src/store/chat.ts L41-）：role 'tool'，toolName='soft-landing' 特殊；CIRCUIT_OPEN tool message 由 loop-helpers L115-119 push（toolResult.error.code='CIRCUIT_OPEN'）。pushMessage L101 自动补 id。

circuit-breaker（Plan 01）：可加 `export function getFailureSummary(tool): { code: string; count: number } | null`（数最近窗口失败最多的 code 及次数）供红卡 X。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentControlBar 三态文案 + 5 秒安抚行（AGENT-12）</name>
  <read_first>
    - src/components/AgentControlBar.tsx（被改文件全文；selector L23-27 + step counter L38-41 + useLingui L16-17）
    - src/agent/agentStore.ts（Plan 06 已加 currentPhase/lastUpdateTs；AgentPhase 类型）
    - src/styles.css（grep `aster-agent-bar` 现有 token + 间距/字号变量）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §SP-C L73-76 + §AgentControlBar L178-182 + §SP-E L83-86
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md Pattern 4 L219-226（5 秒计时挂组件）
  </read_first>
  <behavior>
    - currentPhase='thinking' → 显示「正在思考…」；'reading' → 「正在读取…」；'writing' → 「正在写入…」；null → 不显示文案行。
    - 5 秒内有更新（lastUpdateTs 在 5s 内）→ 不显示安抚行。
    - 5 秒无更新 → 显示安抚行，文案随 phase（thinking→「还在跑，正在等 LLM 思考…」/ reading→「正在读取，稍候…」/ writing→「正在写入，稍候…」）。
    - 计时器在组件 unmount / status='idle' 时清理（clearInterval）。
  </behavior>
  <action>
    在 `src/components/AgentControlBar.tsx`：
    - 加 selector：`const currentPhase = useAgentStore((s) => s.currentPhase);` `const lastUpdateTs = useAgentStore((s) => s.lastUpdateTs);`（逐字段订阅 SP-C）。
    - 三态文案：在 step counter `<span>`（L39-41）后加一个 `<span className="aster-agent-bar__phase">`，内容按 currentPhase 用 `t\`...\``映射（thinking/reading/writing 三句；null 不渲染该 span）。
    - 5 秒安抚（SP-C，计时器挂组件）：`const [stalled, setStalled] = useState(false);` + `useEffect`：当 status !== 'idle' 时 `const id = setInterval(() => setStalled(Date.now() - useAgentStore.getState().lastUpdateTs > 5000), 1000); return () => clearInterval(id);`（依赖 [status]）；status='idle' 时不起计时（组件本来 return null）。stalled 时渲染一行 `<div className="aster-agent-bar__stall" role="status">` 安抚文案随 currentPhase。
    - 文案全走 `t\`...\``（useLingui L17 已在）；新视觉走 styles.css token（见下）。
    - pause/abort/step counter（L42-61）不动。
    在 `src/styles.css` 加 `.aster-agent-bar__phase` / `.aster-agent-bar__stall` 类（复用现有 `--aster-text-secondary` 或同类弱化文字 token + 11px 小字与 step counter 一致；两套主题都顾到，**不硬编码 hex**，先 grep 现有 agent-bar token）。
    写 `src/components/AgentControlBar.test.tsx`（vitest + @testing-library/react，若 repo 已用则复用；grep 现有 *.test.tsx 看 render 套路）：mock useAgentStore 不同 currentPhase → 断言对应文案出现；mock lastUpdateTs=很久以前 + 推进 fake timer 5s → 断言安抚行出现。用 `vi.useFakeTimers()`。
  </action>
  <verify>
    <automated>npx vitest run src/components/AgentControlBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/AgentControlBar.test.tsx` exits 0
    - `grep -c "currentPhase" src/components/AgentControlBar.tsx` ≥ 2
    - `grep -c "setInterval" src/components/AgentControlBar.tsx` == 1（5 秒计时挂组件）
    - `grep -c "lastUpdateTs" src/components/AgentControlBar.tsx` ≥ 1
    - `grep -c "aster-agent-bar__phase\|aster-agent-bar__stall" src/styles.css` ≥ 2（两套主题 token）
    - `grep -ci "#[0-9a-f]\{6\}" src/styles.css` 新增行无裸 hex（人工确认新增 .aster-agent-bar__phase/__stall 用变量）
    - 测试含三态文案 + 5 秒安抚（fake timer）断言
  </acceptance_criteria>
  <done>AgentControlBar 随 currentPhase 显示差异化文案，5 秒无更新显示安抚行（计时器挂组件），样式走 token 两套主题，单测覆盖。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatStream「Agent gave up」红卡 + read 折叠卡截断预览 + agentStore circuit 元数据（ERR-04）</name>
  <read_first>
    - src/components/ChatStream.tsx（被改文件全文；ToolResultCard L57-112 soft-landing 模板 L62-85 + 折叠卡 L88-110 + retryMessage L142）
    - src/agent/agentStore.ts（lastAbortReason L30/L84 + abort L82-86；本 plan 加 lastCircuitInfo）
    - src/agent/circuit-breaker.ts（Plan 01；本 plan 加 getFailureSummary）
    - src/agent/loop-helpers.ts（runOneToolCall circuit 分支 L112-122：abort('circuit') + push CIRCUIT_OPEN tool message）
    - src/store/chat.ts（Message L41- + pushMessage L101 自动补 id + agentRunId/role='assistant'）
    - src/components/icons.tsx（AlertIcon L102 / RetryIcon L82 复用）
    - src/components/ErrorBubble.tsx（grep 红卡现有视觉范式 + aster-tool-card--error 类）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §ChatStream L184-191 + §SP-E L83-86
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md Pattern 5 L228-231（X/Y 来源）+ Pitfall 4 L282-284（截断预览）
  </read_first>
  <behavior>
    - circuit abort 后，CIRCUIT_OPEN tool message（toolResult.error.code='CIRCUIT_OPEN'）渲染为红卡：显示失败 tool 名 + 「试了 X 次都失败」+ LLM 最后建议 Y + 「重新试试」按钮。无「撤销本次」按钮。
    - 「重新试试」点击 → 重开 agent run（用原始 user prompt）或调 retryMessage。
    - 普通 read 折叠卡展开：显示 source + content 前 500 字（>500 加「…(共 X 字)」），不是整个 JSON dump。
    - 非 read 的常规 tool 卡（如 append_paragraph）展开仍可显示 toolResult（兼容现状，或同样截断）。
  </behavior>
  <action>
    **改 src/agent/circuit-breaker.ts**（Plan 01 已填实）：加 `export function getFailureSummary(tool: string): { code: string; count: number } | null`（取该 tool 窗口内出现最多的失败 code 及次数；无失败返 null）。配 vitest 断言（扩 circuit-breaker.test.ts 或本 plan 新 case）。
    **改 src/agent/agentStore.ts**：加 `lastCircuitInfo: { toolName: string; code: string; count: number } | null`（默认 null）+ setter 或在 abort('circuit') 时由 loop-helpers 设置。推荐：abort 签名不变，新增 `setCircuitInfo(info)` action；loop-helpers circuit 分支调用。endRun/beginRun reset lastCircuitInfo=null。
    **改 src/agent/loop-helpers.ts** circuit 分支（L112-122）：在 `abort('circuit')` 前/后调 `useAgentStore.getState().setCircuitInfo({ toolName: tc.name, code: <getFailureSummary(tc.name)?.code ?? 'UNSUPPORTED'>, count: <getFailureSummary?.count ?? 3> })`（import getFailureSummary from circuit-breaker）。
    **改 src/components/ChatStream.tsx** ToolResultCard：
    - 红卡分支：在 `if (message.toolName === 'soft-landing')` 之后、常规折叠卡之前，加 `if (message.toolResult?.error?.code === 'CIRCUIT_OPEN')` 分支，渲染「Agent gave up」红卡（仿 soft-landing 卡 L62-85 结构 + `aster-tool-card--error` / 新 `--gave-up` 类）：
      - 标题用 AlertIcon + 「Aster 试了几次都没成功」。
      - 说明：X 来自 `useAgentStore.getState().lastCircuitInfo`（toolName + count）；Y 来自该 agentRunId 下最后一条 role='assistant' 的 content（从 useMessages/useChatStore 取，找 `m.role==='assistant' && m.agentRunId===message.agentRunId` 最后一条 content）。文案如 `试了 ${count} 次 ${toolName} 都失败了。${Y建议}`。
      - 按钮：一个「重新试试」（RetryIcon + onClick 重开 runAgent 或 retryMessage）。**不放撤销按钮**（D-05）。
    - read 折叠卡展开预览改进（L105-108）：把 `<pre>{JSON.stringify(message.toolResult, null, 2)}</pre>` 改成：若 toolResult.data 含 `{result_type, content, source}`（read 包装），展开显示 `source` 行 + content 截断（`content.slice(0,500)` + content.length>500 时 `…(共 ${content.length} 字)`）；错误结果（ok:false）展开显示 message + hint；其它（如 write tool）保持 JSON.stringify 兜底。
    **改 src/styles.css**：加 `.aster-tool-card--gave-up` 红色 accent（复用现有 `--aster-tool-card--error` 红色 token 或同源红色变量；**不硬编码 hex**，先 grep 现有 error/红色 token）。两套主题。
    写 `src/components/ChatStream.giveup.test.tsx`：mock CIRCUIT_OPEN tool message + lastCircuitInfo + assistant message → 断言红卡显示 X 次 + Y 建议 + 「重新试试」按钮存在 + 「撤销」字样不存在。
  </action>
  <verify>
    <automated>npx vitest run src/components/ChatStream.giveup.test.tsx src/agent/circuit-breaker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ChatStream.giveup.test.tsx` exits 0
    - `grep -c "CIRCUIT_OPEN" src/components/ChatStream.tsx` ≥ 1（红卡分支）
    - `grep -c "重新试试" src/components/ChatStream.tsx` ≥ 1
    - `grep -ci "撤销" src/components/ChatStream.tsx` 的红卡分支内无「撤销本次」按钮（人工确认 D-05；soft-landing 卡的「停下」不算）
    - `grep -c "getFailureSummary" src/agent/circuit-breaker.ts src/agent/loop-helpers.ts` ≥ 2
    - `grep -c "lastCircuitInfo" src/agent/agentStore.ts` ≥ 2
    - `grep -c "slice(0, 500)\|slice(0,500)" src/components/ChatStream.tsx` ≥ 1（截断预览）
    - `grep -c "aster-tool-card--gave-up" src/styles.css` ≥ 1
    - 测试断言红卡含「重新试试」且不含撤销按钮
  </acceptance_criteria>
  <done>circuit abort 后红卡显示 X 次失败 + Y 建议 + 「重新试试」（无撤销），read 折叠卡展开截断预览，circuit 元数据流转，单测覆盖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read 折叠卡展开 → 用户屏幕 | document_content 截断预览，避免超长 dump（也避免把文档原文当 UI 指令渲染） |
| circuit 元数据 → 红卡 | toolName/code 是受控 literal（circuit-breaker 记录 ToolErrorCode 枚举），不含 stack/path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Information Disclosure | 红卡 X/Y 渲染内部状态 | mitigate | X = circuit log 的 ToolErrorCode 枚举 + 计数（受控）；Y = LLM assistant content（已是面向用户的中文）；不渲染 stack/path |
| T-04-22 | (UX 诚实) | 红卡放假撤销按钮 | mitigate | D-05 坚决不放「撤销本次」（undo all 是 Phase 5）；只放「重新试试」真实可用入口（诚实禁用） |
| T-04-23 | DoS/UX | read 折叠卡 dump 超长 document_content | mitigate | 展开截断前 500 字预览（Pitfall 4） |
</threat_model>

<verification>
- `npx vitest run src/components/AgentControlBar.test.tsx src/components/ChatStream.giveup.test.tsx src/agent/circuit-breaker.test.ts` 全绿
- `npm run test -- --run` 全套不回归
- `npm run build` + `npm run size`（≤80KB gate）
- 真机 SC3（三态 + 5 秒安抚）/ SC5（红卡）走 Plan 09 checkpoint
</verification>

<success_criteria>
- AgentControlBar 三态文案 + 5 秒安抚（计时器挂组件）
- 「Agent gave up」红卡 X/Y + 重新试试，无撤销按钮
- read 折叠卡截断预览
- 样式走 token 两套主题，bundle ≤ 80KB，0 新依赖
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-07-SUMMARY.md`
</output>

---
phase: 04-read-tools-agentcontrolbar
plan: 09
type: execute
wave: 5
depends_on: [03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/phases/04-read-tools-agentcontrolbar/04-UAT-EVIDENCE.md
autonomous: false
requirements: [TOOL-02, AGENT-12, ERR-04, CARRY-02]
must_haves:
  truths:
    - "PPT 复合 demo（list_slides→get_slide）真机跑通，每步 read 折叠卡显示中文人话"
    - "三宿主 read 全覆盖真机验证：Word 段落计数+读第3段 / Excel used range+前20行 / PPT 列标题"
    - "AgentControlBar 三态文案 + 5 秒安抚真机可见"
    - "circuit 熔断后「Agent gave up」红卡真机出现 + 重新试试可用"
    - "内置 Provider model 下拉真机为 select，自定义为 input"
  artifacts:
    - path: ".planning/phases/04-read-tools-agentcontrolbar/04-UAT-EVIDENCE.md"
      provides: "SC1/SC2/SC3/SC5/SC6 真机录屏/截图证据归档"
  key_links:
    - from: "agent run"
      to: "三宿主 Office for Web"
      via: "sideload + office-addin-browser-uat skill"
      pattern: "real-host UAT"
---

<objective>
Phase 4 的 phase gate 真机 UAT（沿用 Phase 3「每 plan 真机重测」节奏）。Plan 03-08 的 vitest 全绿后，在 Office for Web（Edge/Chrome 最新两版）三宿主真机各跑一次，验证 read tools 链路、AgentControlBar 三态、circuit 红卡、model 下拉在真宿主行为正确，并归档证据。

验收 SC（ROADMAP Phase 4）：
- **SC1** PPT 复合 demo：「在最长那张 slide 后插入一张总结要点的新 slide」→ 验 read 链路 `list_slides → get_slide`（insert_slide 是 Phase 6，本 phase 只验 read 部分 + 真机 read 全覆盖），每步 read 折叠卡显示「读取了第 N 张幻灯片的形状清单」中文人话。
- **SC2** 三宿主 read 全覆盖：(a) Word「数一下文档有几段并把第 3 段读出来」；(b) Excel「告诉我当前 used range 的形状和前 20 行」；(c) PPT「列出所有 slide 标题」。
- **SC3** AgentControlBar 步骤差异化文案：run 期间顶部 bar 显示「正在读取/思考/写入」差异化文案 + 5 秒无更新安抚行。
- **SC5** circuit breaker「Agent gave up」红卡：构造连续失败（≥3 次同 code），验红卡 + 「重新试试」入口。
- **SC6** CARRY-02 model 下拉：内置 DeepSeek/AiHubMix 编辑表单 model 字段是 select；自定义 Provider 是 input。

Purpose: vitest 无法覆盖 Office.js 真宿主行为 + 折叠卡渲染 + 时序 UI；这是 phase gate 硬验收。
Output: 04-UAT-EVIDENCE.md 归档（每 SC 步数 + 截图/录屏路径 + PASS/issue）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-VALIDATION.md
@.claude/skills/office-addin-browser-uat/SKILL.md

<setup>
真机 UAT 需用户提供：M365 账号登录 + 三宿主各开一个文档（PPT 多 slide / Excel 有数据 used range / Word 多段含 Heading）+ DeepSeek API Key（.env.local 或 Settings 填）。Claude 先确认线上版本已部署（CLAUDE.md §发布授权：read tool 改动影响 Task Pane 行为，需 push 触发 GitHub Pages 部署后真机才拿到新版本 — 部署后告知 commit hash + 部署状态）。
</setup>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全套 vitest + build + size gate + 部署线上</name>
  <read_first>
    - .planning/phases/04-read-tools-agentcontrolbar/04-VALIDATION.md（sampling + 80KB gate）
    - CLAUDE.md §发布授权（push 触发 Pages 部署的边界）
  </read_first>
  <action>
    跑全套门禁：`npm run test -- --run`（全绿）+ `npm run build`（通过）+ `npm run size`（≤80KB gate，D-10 / NFR-02）。任一失败先修。
    全绿后，按 CLAUDE.md §发布授权 commit 并 `git push origin main` 触发 GitHub Pages 部署（read tool / AgentControlBar / ProviderForm 改动影响线上 Task Pane，真机 sideload 拿到的是已部署版本，本地 commit ≠ 线上更新）。等部署完成，记录 commit hash + 部署状态。
  </action>
  <verify>
    <automated>npm run test -- --run && npm run build && npm run size</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run` exits 0（全套含 Plan 01-08 测试绿）
    - `npm run build` exits 0
    - `npm run size` 通过（≤80KB gate）
    - git push 完成，GitHub Pages 部署状态确认（记录 commit hash 到 SUMMARY）
  </acceptance_criteria>
  <done>全套门禁绿 + 80KB gate 不超 + 线上部署完成，真机可拿到新版本。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 全套 read tools（三宿主 11 个）+ AgentControlBar 三态文案/5 秒安抚 + circuit「Agent gave up」红卡 + 内置 Provider model 下拉，已部署到 GitHub Pages 线上。
  </what-built>
  <how-to-verify>
    按 .claude/skills/office-addin-browser-uat/SKILL.md 步骤，在 Office for Web（Edge 或 Chrome 最新版）三宿主真机各 sideload 后验证（每条记录步数 + 截图/录屏）：

    **SC2-Word（最先，最简）**：Word 开一个多段文档（含至少 3 段 + 1 个 Heading）→ Task Pane 输入「数一下文档有几段并把第 3 段读出来」→ 验证：
    1. agent 调 get_paragraph_count + get_paragraph_at，chat 出现 read 折叠卡「读取了文档段落总数」「读取了第 3 段」中文人话（不是 raw tool name）。
    2. 最终 assistant 回复段数正确 + 第 3 段内容正确。

    **SC2-Excel**：Excel 开一个有数据的表（used range > 几行）→ 输入「告诉我当前 used range 的形状和前 20 行」→ 验证 get_used_range_summary（+ 视情况 get_range_values）折叠卡中文人话 + 概况正确。**额外验 A-24**：选一个超大区域（>10K cells，如 A1:Z100000）让 agent 读，验证返「选区过大...请改用 get_used_range_summary」不卡死/不爆 tab。

    **SC2-PPT + SC1**：PPT 开一个多 slide deck → 输入「列出所有 slide 标题」→ 验证 list_slides 折叠卡「读取了全部幻灯片清单」+ 标题列出正确（按顺序，无反序）。再输入「在最长那张 slide 后插入一张总结要点的新 slide」→ 验证 read 链路 list_slides→get_slide 跑通（insert_slide 本 phase 未实现，agent 报无此 tool 或停在 read 后即可 — 本 phase 只验 read 部分）。

    **SC3（任一宿主 run 期间观察）**：agent run 时顶部 AgentControlBar 显示「正在读取/正在思考/正在写入」差异化文案（不是统一 spinner）；故意触发慢响应（大区域读或慢网络）→ 5 秒后出现安抚行「还在跑，正在等…」。

    **SC5（任一宿主）**：构造连续失败 ≥3 次同 code（如对锁定/不存在的目标反复 read，或断网制造 HOST_API_FAILED）→ 验证熔断后出现红色「Agent gave up」卡片，说明「试了 X 次都失败」+ LLM 建议 + 「重新试试」按钮；点「重新试试」能重开一轮。确认卡片**没有**「撤销本次」按钮。

    **SC6（任一宿主 Settings）**：打开 Settings → 编辑内置 DeepSeek → model 字段是下拉 select（选项 deepseek-v4-pro/flash）；编辑内置 AiHubMix → select（gpt-5.1/gemini-3.5-flash）；新建/编辑自定义 Provider → model 是文本输入框。

    每条记录到 04-UAT-EVIDENCE.md：SC 编号 / 宿主 / 步数 / 截图或录屏路径 / PASS 或 issue 描述。
  </how-to-verify>
  <resume-signal>每条 SC 标 PASS / issue 后，回复「SC1-6 全 PASS」或列出 issue（issue 转 gap 走 /gsd-plan-phase --gaps）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 真机文档内容 → LLM | 真机验证 read tool 把真实文档喂 LLM 的 document_content 包装在真宿主生效（prompt injection 防御端到端） |
| 线上部署 → 用户 sideload | push 到 GitHub Pages，用户拿到已部署版本（commit ≠ 线上，记忆 push_before_deploy_claims） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | DoS | Excel >10K cells 真机读爆 tab | mitigate | SC2-Excel 额外验 A-24 读前拒绝在真宿主生效（mock 单测之外的真机确认） |
| T-04-27 | Tampering/Elevation | 真机文档藏 prompt injection 真机未验 | accept | 本 phase 防御 = 包装 + system prompt（已单测）；真机 injection 攻防留 Phase 7 UAT 端到端（A-05 攻击烈度上限 = 改坏当前文档，Q7 单文档边界）|
</threat_model>

<verification>
- 全套 vitest + build + size gate 绿（Task 1）
- 三宿主真机 SC1/SC2/SC3/SC5/SC6 各 PASS（Task 2 checkpoint），证据归档 04-UAT-EVIDENCE.md
- 线上部署完成
</verification>

<success_criteria>
- Phase 4 六条 SC 真机验收：SC1 PPT read 链路 / SC2 三宿主 read 全覆盖（含 A-24）/ SC3 三态+5秒 / SC5 红卡 / SC6 model 下拉
- 证据归档，线上部署
- bundle ≤ 80KB，0 新依赖
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-09-SUMMARY.md`
</output>

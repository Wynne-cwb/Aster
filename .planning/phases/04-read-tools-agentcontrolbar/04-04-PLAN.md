---
phase: 04-read-tools-agentcontrolbar
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/adapters/PptAdapter.ts
  - src/adapters/PptAdapter.read.test.ts
autonomous: true
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "PptAdapter.read({kind:'list_slides'}) 一次性返全部 slide 的 {index,title}（batch，D-13）"
    - "PptAdapter.read({kind:'get_slide', slideIndex}) 返该 slide 的形状清单 + 文本"
    - "PptAdapter.read({kind:'list_shapes_on_slide', slideIndex}) 返 shape {id,type,left,top,width,height}"
    - "PptAdapter.read({kind:'get_shape', slideIndex, shapeId}) 返单 shape 详情；找不到返 NOT_FOUND"
    - "slide 越界返 NOT_FOUND；slides 按 .index 排序绕 Web 反序 bug #3618"
  artifacts:
    - path: "src/adapters/PptAdapter.ts"
      provides: "read(query) switch：PPT 5 个 read kind（4 PPT 专属 + selection_detail）"
      contains: "async read(query: ReadableQuery)"
    - path: "src/adapters/PptAdapter.read.test.ts"
      provides: "mock Office PowerPoint.run 验证每 kind + 越界 NOT_FOUND + index 排序"
  key_links:
    - from: "src/adapters/PptAdapter.ts read()"
      to: "PowerPoint.run 闭包"
      via: "load + sync 取纯数据，按 .index 排序，proxy 不出闭包（A-06 / PPT-05）"
      pattern: "PowerPoint\\.run"
---

<objective>
实现 PptAdapter 的 `read(query)` 方法（TOOL-01/02），收口 PPT 宿主全部读法。新增 `read(query: ReadableQuery): Promise<ReadableResult>`，switch 覆盖 5 个 kind：

- `list_slides` — **一次性返全部** slide 的 `{index, title}`（batch，D-13；禁逐张拉避免 A-07 step runaway）
- `get_slide` — 指定 slideIndex 的形状清单 + 文本（document_content；越界 NOT_FOUND）
- `list_shapes_on_slide` — 指定 slide 的 shapes `{id, type, left, top, width, height}`（metadata，位置信息为 Phase 6 「左下角那张图」推断铺路）
- `get_shape` — 单 shape 详情（document_content；shapeId 找不到返 NOT_FOUND）
- `selection_detail` — 复用 getSelection 语义

Purpose: SC1 PPT 复合 demo「list_slides → get_slide → insert_slide」的 read 链路（insert_slide 是 Phase 6）+ SC2「PPT 列出所有 slide 标题」真机跑通。
Output: PptAdapter.read() 实现 + mock Office 单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md

<interfaces>
read() 契约（Plan 02 已加到 src/adapters/DocumentAdapter.ts）：
```typescript
// PPT 相关 kind：
//   { kind: 'list_slides' }
//   { kind: 'get_slide'; slideIndex: number }            // 1-based（与 SelectionContext 一致）
//   { kind: 'list_shapes_on_slide'; slideIndex: number } // 1-based
//   { kind: 'get_shape'; slideIndex: number; shapeId: string }
//   { kind: 'selection_detail' }
```

PptAdapter 现有范式（src/adapters/PptAdapter.ts）：
- getSelection() L29-62：`PowerPoint.run` 多对象一次 load（selectedSlides.items + allSlides.items）+ 单 sync；**PPT-05 守则 L47-49：`[...items].sort((a,b)=>a.index-b.index)` 绕 Web 反序 bug #3618**
- insert() L113-148：slide.shapes.getItemAt(0).textFrame.textRange.text 范式（slide 文本读法参考）
- HostApiError 包装 L59-61

VERIFIED Office.js API（@types/office-js@1.0.591）：
- `ctx.presentation.slides`（SlideCollection）→ `.load('items')` → `.items[i].index`
- `Slide.shapes`（ShapeCollection）→ `.items[].{id, name, type, left, top, width, height}`
- `Shape.textFrame.textRange.text`（shape 文本）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PptAdapter.read() switch 实现（5 kind，batch + index 排序）</name>
  <read_first>
    - src/adapters/PptAdapter.ts（被改文件全文；getSelection L29-62 多对象 load 单 sync + PPT-05 排序 L47-49 + insert slide 文本读法 L113-148）
    - src/adapters/DocumentAdapter.ts（Plan 02 加的 ReadableQuery / ReadableResult / ReadToolError）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §SP-A L41-66 + §`{Ppt,Excel,Word}Adapter` L98-104（PPT 部分 + PPT-05 排序）
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Code Examples「PPT list_slides」L358-371 + Open Q1 L479-482（list_slides 含 title 推荐）
  </read_first>
  <behavior>
    - `read({kind:'list_slides'})` → `{ ok:true, data:{ count:N, slides:[{index:1, title:'...'}, ...] } }`，slides 按 .index 升序（1-based index），title = 该 slide 第一个文本框首行（无则空串）。
    - `read({kind:'get_slide', slideIndex:1})` → `{ ok:true, data:{ index:1, shapes:[{id,type,text}], ... } }`（含每 shape 文本）。
    - `read({kind:'get_slide', slideIndex:99})`（越界）→ `{ ok:false, error:{ code:'NOT_FOUND', ... } }`。
    - `read({kind:'list_shapes_on_slide', slideIndex:1})` → `{ ok:true, data:{ slideIndex:1, shapes:[{id, type, left, top, width, height}] } }`（位置信息，不含文本=metadata）。
    - `read({kind:'get_shape', slideIndex:1, shapeId:'X'})` → 找到返 `{ ok:true, data:{ id, type, text, left, top, width, height } }`；找不到返 `{ ok:false, error:{ code:'NOT_FOUND', ... } }`。
    - `read({kind:'selection_detail'})` → `{ ok:true, data: <getSelection() 结果> }`。
    - Office.js 抛错 → `throw new HostApiError('PowerPoint <kind> 失败', err)`。
  </behavior>
  <action>
    先写 `src/adapters/PptAdapter.read.test.ts`（RED），mock PowerPoint global（先 grep 现有 `src/adapters/*.test.ts` 复用 mock 套路）。覆盖每 kind + 越界 NOT_FOUND + list_slides 按 index 排序断言。
    然后在 `src/adapters/PptAdapter.ts` 加 `async read(query: ReadableQuery): Promise<ReadableResult>`（照 SP-A + PPT-05 排序）：
    - import `ReadableQuery`, `ReadableResult` from `./DocumentAdapter`。
    - `switch (query.kind)`：
      - `case 'list_slides'`：PowerPoint.run → `const slides = ctx.presentation.slides; slides.load('items'); await ctx.sync();` → **按 `.index` 排序**（`const sorted = [...slides.items].sort((a,b)=>a.index-b.index)`，PPT-05 L47-49）。取 title 需对每 slide load 第一个 shape textFrame：对 sorted 每个 slide `s.shapes.load('items')` → sync 一次（多对象一次 load 范式，getSelection L33-38）→ 再对 shapes[0]?.textFrame.textRange load text → sync。**为保 batch（D-13 / Open Q1 推荐含 title）**：可分两轮 sync（轮1 load 所有 slide 的 shapes.items；轮2 load 每 slide shapes[0].textFrame.textRange.text）。title 取首行（`text.split('\n')[0]`，无 shape/无文本 → 空串）。返 `{ ok:true, data:{ count: slides.items.length, slides: sorted.map((s,i)=>({ index:i+1, title: <title> })) } }`。
      - `case 'get_slide'`：1-based slideIndex → 0-based `query.slideIndex - 1`；越界（`< 0 || >= slides.items.length`）返 NOT_FOUND。取该 slide 的 shapes：load items + 每 shape textFrame.textRange.text → 返 `{ ok:true, data:{ index: query.slideIndex, shapes: shapes.items.map(sh=>({ id: sh.id, type: sh.type, text: <textRange.text 或 ''> })) } }`。
      - `case 'list_shapes_on_slide'`：同上定位 slide（越界 NOT_FOUND），load shapes items 的 `id,type,left,top,width,height`（**不 load 文本** = metadata）→ 返 shapes 数组。
      - `case 'get_shape'`：定位 slide（越界 NOT_FOUND）→ load shapes items → `find(sh => sh.id === query.shapeId)`；找不到返 `{ ok:false, error:{ code:'NOT_FOUND', message:`形状 ${query.shapeId} 不存在`, ... } }`；找到 load 该 shape textFrame + 位置 → 返详情。
      - `case 'selection_detail'`：`return { ok:true, data: await this.getSelection() };`。
      - `default`：UNSUPPORTED（同 Plan 03 default 风格）。
    - 每 case 内 try/catch → `throw new HostApiError('PowerPoint <kind> 失败', err)`（与 getSelection L59-61 一致）。
    - 注意 sync 次数：PPT shape 读多对象一次 load 减少 sync（getSelection L33-38 范式）；title 二次 sync 可接受（RESEARCH Assumption A4：无批量 title API）。
  </action>
  <verify>
    <automated>npx vitest run src/adapters/PptAdapter.read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/adapters/PptAdapter.read.test.ts` exits 0
    - `grep -c "async read(query: ReadableQuery)" src/adapters/PptAdapter.ts` == 1
    - `grep -c "sort((a, b) => a.index - b.index)" src/adapters/PptAdapter.ts` ≥ 1（PPT-05 排序，list_slides 用）
    - `grep -E "list_slides|get_slide|list_shapes_on_slide|get_shape|selection_detail" src/adapters/PptAdapter.ts` 命中全部 5 个 kind
    - 测试含 list_slides 返回 index 升序断言 + 越界 NOT_FOUND 断言
    - `npx eslint src/adapters/PptAdapter.ts` 不报 no-restricted-globals
  </acceptance_criteria>
  <done>PptAdapter.read() 5 kind 全实现，list_slides batch 一次返全部按 index 排序，越界/找不到返 NOT_FOUND，mock 单测全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PPT slide/shape 文本 → ReadableResult | get_slide / get_shape 读出用户正文，上层 wrapReadResult 标 document_content |
| Office.js proxy → 闭包外 | A-06：slide/shape proxy 只在 PowerPoint.run 闭包内 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | slide/shape proxy 出闭包 | mitigate | 闭包内 load+sync 取 plain {id,type,text,位置} 返出；SP-A 范式；TOOL-07 adapter 排除但 proxy 不返出靠纪律 |
| T-04-11 | Information Disclosure | PowerPoint.run 抛错带 stack | mitigate | catch → HostApiError 不存 hostError；dispatch sanitize |
| T-04-12 | Input Validation | slideIndex / shapeId 越界 | mitigate | bounds check + find 返 NOT_FOUND，不越界访问 |
| T-04-13 | Correctness | Web 反序 bug #3618 → slide 顺序错 | mitigate | PPT-05 守则 `.sort((a,b)=>a.index-b.index)`（getSelection L47-49 已验范式） |
</threat_model>

<verification>
- `npx vitest run src/adapters/PptAdapter.read.test.ts` 全绿
- `npm run test -- --run` 全套不回归
- `npx tsc --noEmit` PPT read 路径无类型错误
- `npm run build` 通过
</verification>

<success_criteria>
- PptAdapter 5 read kind 实现，list_slides batch + index 排序
- 越界/找不到返 NOT_FOUND，0 新依赖
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-04-SUMMARY.md`
</output>

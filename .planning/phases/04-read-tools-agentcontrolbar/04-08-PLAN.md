---
phase: 04-read-tools-agentcontrolbar
plan: 08
type: execute
wave: 4
depends_on: []
files_modified:
  - src/store/providers.ts
  - src/components/Settings/ProviderForm.tsx
  - src/providers/registry.ts
  - src/components/Settings/ProviderForm.test.tsx
autonomous: true
requirements: [CARRY-02]
must_haves:
  truths:
    - "内置 Provider（DeepSeek/AiHubMix）编辑表单 model 字段为原生 select 固定清单下拉"
    - "自定义 Provider model 字段保留手动 text input"
    - "DeepSeek 下拉 = deepseek-v4-pro/deepseek-v4-flash；AiHubMix 下拉 = gpt-5.1/gemini-3.5-flash"
    - "registry.ts AIHUBMIX_VISION_MODEL=gpt-5.1 / AIHUBMIX_IMAGE_MODEL=gpt-image-2（仅改常量，调用路径不动）"
    - "aihubmix 内置默认 model 改为 gpt-5.1（不再落清单外的 gpt-image-1）"
  artifacts:
    - path: "src/components/Settings/ProviderForm.tsx"
      provides: "model 字段 isBuiltIn 分支：select vs input"
      contains: "select"
    - path: "src/store/providers.ts"
      provides: "BUILTIN_MODEL_OPTIONS 清单 + aihubmix 默认 model 改 gpt-5.1"
      contains: "BUILTIN_MODEL_OPTIONS"
    - path: "src/providers/registry.ts"
      provides: "AIHUBMIX_VISION_MODEL/AIHUBMIX_IMAGE_MODEL 更新常量（D-09）"
  key_links:
    - from: "src/components/Settings/ProviderForm.tsx"
      to: "src/store/providers.ts BUILTIN_MODEL_OPTIONS"
      via: "isBuiltIn 时 select 渲染清单"
      pattern: "BUILTIN_MODEL_OPTIONS"
---

<objective>
CARRY-02 + D-06/D-07/D-09：把内置 Provider 的 model 字段从手打字符串改成原生 select 固定清单下拉（v2 高频切 model：pro vs flash 路由，一点即换），自定义 Provider 保留手动 input。顺手更新 registry.ts 过时常量。

1. **model select 下拉（CARRY-02 / D-06）** — ProviderForm model 字段加 `isBuiltIn` 分支：内置走 `<select>` 固定清单，自定义走现有 text input。
2. **下拉清单（D-07）** — DeepSeek = `['deepseek-v4-pro','deepseek-v4-flash']`；AiHubMix = `['gpt-5.1','gemini-3.5-flash']`（会 tool calling 的多模态聊天 model，能驱动 agent loop；D-08 主 agent 始终 DeepSeek，AiHubMix 仅兜底）。
3. **aihubmix 内置默认 model（RESEARCH Open Q2）** — `BUILT_IN_PROVIDERS` aihubmix 现 `gpt-image-1` 不在 D-07 下拉清单 → 改 `gpt-5.1`，避免 select 选中态落清单外。
4. **registry 常量（D-09，仅改值不动调用路径）** — `AIHUBMIX_VISION_MODEL` gpt-4o→gpt-5.1；`AIHUBMIX_IMAGE_MODEL` gpt-image-1→gpt-image-2。vision/image-gen 真实调用是 Phase 6。

Purpose: SC6 model 下拉真机验收的代码底座。
Output: ProviderForm select 分支 + BUILTIN_MODEL_OPTIONS 清单 + aihubmix 默认 model + registry 常量 + 组件单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md
@CLAUDE.md

<interfaces>
ProviderForm 现状（src/components/Settings/ProviderForm.tsx）：
- L39 `const isBuiltIn = provider?.isBuiltIn ?? false;`
- L43 `const [model, setModel] = useState(provider?.model ?? '');`
- L48 `const modelRef = useRef<HTMLInputElement>(null);`
- L131-135 baseURL `isBuiltIn ? ... : ...` disabled 分支范式（model 分支照此）
- L144-158 model 字段（现 text input）— 本 plan 加 isBuiltIn 三元分支
- L90 onSave 路径不动

providers.ts BUILT_IN_PROVIDERS（src/store/providers.ts L28-43）：
- deepseek model = `'deepseek-v4-flash'`（L33，保留，在 D-07 清单内）
- aihubmix model = `'gpt-image-1'`（L40，改 `'gpt-5.1'`）

registry.ts（src/providers/registry.ts）：
- L26 `const AIHUBMIX_VISION_MODEL = 'gpt-4o';` → `'gpt-5.1'`
- L27 `const AIHUBMIX_IMAGE_MODEL = 'gpt-image-1';` → `'gpt-image-2'`
- resolve 路径 L45-99 不动

D-07 下拉清单值（RESEARCH §Code Examples L403-406）：
```typescript
BUILTIN_MODEL_OPTIONS: Record<string, string[]> = {
  deepseek: ['deepseek-v4-pro', 'deepseek-v4-flash'],
  aihubmix: ['gpt-5.1', 'gemini-3.5-flash'],
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProviderForm model select 分支 + BUILTIN_MODEL_OPTIONS + aihubmix 默认 model（CARRY-02）</name>
  <read_first>
    - src/components/Settings/ProviderForm.tsx（被改文件全文；isBuiltIn L39 + model state L43 + baseURL isBuiltIn 分支范式 L131-135 + model 字段 L144-158）
    - src/store/providers.ts（BUILT_IN_PROVIDERS L28-43 + ProviderConfig 形态）
    - src/components/Settings/ProviderForm.test.tsx（若存在则扩展；否则新建 — grep 现有 *.test.tsx render 套路）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §ProviderForm L193-208 + §providers.ts L210-212
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Code Examples「ProviderForm model select」L399-420 + Open Q2 L484-486
  </read_first>
  <behavior>
    - 编辑内置 DeepSeek（isBuiltIn=true, id='deepseek'）→ model 字段是 `<select>`，选项 = deepseek-v4-pro / deepseek-v4-flash。
    - 编辑内置 AiHubMix（id='aihubmix'）→ select 选项 = gpt-5.1 / gemini-3.5-flash。
    - 编辑/新建自定义 Provider（isBuiltIn=false）→ model 字段仍是 text input。
    - select 选中态 = 当前 provider.model（若 model 不在清单内，fallback 含当前值，避免空选 — `BUILTIN_MODEL_OPTIONS[id] ?? [model]`）。
    - 改 select → setModel → onSave 写回（现有路径）。
    - aihubmix 内置默认 model = gpt-5.1（在清单内）。
  </behavior>
  <action>
    **改 src/store/providers.ts**：
    - L40 aihubmix `model: 'gpt-image-1'` → `model: 'gpt-5.1'`（Open Q2 / D-08 兜底）。
    - export `BUILTIN_MODEL_OPTIONS`（D-07）：`export const BUILTIN_MODEL_OPTIONS: Record<string, string[]> = { deepseek: ['deepseek-v4-pro', 'deepseek-v4-flash'], aihubmix: ['gpt-5.1', 'gemini-3.5-flash'] };`（挂 providers.ts 顶部常量区，供 ProviderForm import）。
    **改 src/components/Settings/ProviderForm.tsx** model 字段（L144-158）：照 RESEARCH §Code Examples L408-418 + baseURL L131-135 分支范式，改成 isBuiltIn 三元：
    ```tsx
    {isBuiltIn ? (
      <select className="aster-field aster-field--standalone" value={model}
              onChange={(e) => setModel(e.target.value)}>
        {(BUILTIN_MODEL_OPTIONS[provider!.id] ?? [model]).map((m) => (
          <option key={m} value={m}>{m}</option>
        ))}
      </select>
    ) : (
      <input id="pf-model" ref={modelRef} type="text" className="aster-field aster-field--standalone"
             value={model} onChange={(e) => setModel(e.target.value)} placeholder="deepseek-v4-flash" />
    )}
    ```
    - import `BUILTIN_MODEL_OPTIONS` from `../../store/providers`。
    - modelRef 仅 input 用（select 不需 ref）；深链 initialFocus='model-input' 对内置 select 可不 focus（select 无 keyRef 等价 — 保持现有 useEffect 不报错即可，若 modelRef.current 为 null 则 focus 跳过）。
    - label「模型名称」+ onSave（L90）路径不动。select 样式复用 `aster-field aster-field--standalone`（若原生 select 在该类下显示不一致，可在 styles.css 补 `select.aster-field` 微调箭头/padding — 走 token，非必须）。
    **写/扩 src/components/Settings/ProviderForm.test.tsx**：render 内置 deepseek provider → 断言 `<select>` 存在且含 2 个 option（deepseek-v4-pro/flash）；render 内置 aihubmix → 断言 option=gpt-5.1/gemini-3.5-flash；render 自定义 provider（isBuiltIn=false）→ 断言 model 是 text input（无 select）。
  </action>
  <verify>
    <automated>npx vitest run src/components/Settings/ProviderForm.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/Settings/ProviderForm.test.tsx` exits 0
    - `grep -c "BUILTIN_MODEL_OPTIONS" src/store/providers.ts` ≥ 1（export 清单）
    - `grep -c "deepseek-v4-pro" src/store/providers.ts` == 1 && `grep -c "gemini-3.5-flash" src/store/providers.ts` == 1
    - `grep -c "model: 'gpt-5.1'" src/store/providers.ts` == 1（aihubmix 默认改了）
    - `grep -c "model: 'gpt-image-1'" src/store/providers.ts` == 0（旧值已替换）
    - `grep -c "<select" src/components/Settings/ProviderForm.tsx` ≥ 1
    - `grep -c "BUILTIN_MODEL_OPTIONS" src/components/Settings/ProviderForm.tsx` ≥ 1
    - 测试含内置 select / 自定义 input 分支断言
  </acceptance_criteria>
  <done>内置 Provider model 走 select 固定清单（DeepSeek pro/flash、AiHubMix gpt-5.1/gemini-3.5-flash），自定义保留 input，aihubmix 默认 model 入清单，组件单测覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: registry.ts 过时常量更新（D-09，仅改值）</name>
  <read_first>
    - src/providers/registry.ts（被改文件全文；L26-27 常量 + L65-89 vision/image-gen resolve 路径不动）
    - .planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md §D-09
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §registry.ts L214-216
  </read_first>
  <action>
    在 `src/providers/registry.ts`（仅改 2 行常量值，**不动 resolve 调用路径** L45-99）：
    - L26 `const AIHUBMIX_VISION_MODEL = 'gpt-4o';` → `const AIHUBMIX_VISION_MODEL = 'gpt-5.1';`（备选注释 gemini-3.5-flash，D-09）。
    - L27 `const AIHUBMIX_IMAGE_MODEL = 'gpt-image-1';` → `const AIHUBMIX_IMAGE_MODEL = 'gpt-image-2';`（备选注释 gemini-3.1-flash-image-preview）。
    - 加一行注释说明：vision/image-gen 真实调用路径在 Phase 6 接入，本 phase 仅更新过时常量（D-09）。
    - **不接** 任何 vision/image 调用逻辑（Phase 6 / Deferred）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "registry" || echo "no registry errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AIHUBMIX_VISION_MODEL = 'gpt-5.1'" src/providers/registry.ts` == 1
    - `grep -c "AIHUBMIX_IMAGE_MODEL = 'gpt-image-2'" src/providers/registry.ts` == 1
    - `grep -c "gpt-4o" src/providers/registry.ts` == 0
    - `grep -c "case 'vision'" src/providers/registry.ts` == 1（resolve 路径未删）
    - `npx tsc --noEmit` 无 registry 类型错误
  </acceptance_criteria>
  <done>registry 两个 aihubmix 常量更新到 gpt-5.1 / gpt-image-2，resolve 调用路径不动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model 字符串 → Provider 请求 | model 是发往用户配置 Provider 的字符串；CARRY-02 只改 model 字符串，不动 API Key 存储路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-24 | (无新面) | model select | accept | 本 plan 只改 model 字符串选择 UX + 常量；不碰 Key（仍 aster:keys:{id} localStorage）、不碰网络路径；NFR-04 不受影响 |
| T-04-25 | Input Validation | 自定义 Provider model 手动输入 | accept | 自定义 model 由用户负责（NFR-04 user-added Provider endpoint/model 用户负责）；legacy 模型名（deepseek-chat/reasoner）已有 eslint 限制不阻 UI 输入但下拉清单只给合法值 |
</threat_model>

<verification>
- `npx vitest run src/components/Settings/ProviderForm.test.tsx` 全绿
- `npm run test -- --run` 全套不回归
- `npx tsc --noEmit` 无类型错误
- `npm run build` + `npm run size`（≤80KB gate）
- 真机 SC6（model 下拉）走 Plan 09 checkpoint
</verification>

<success_criteria>
- 内置 model select 下拉（D-07 清单），自定义 input
- aihubmix 默认 model 入清单（gpt-5.1）
- registry 常量更新（gpt-5.1 / gpt-image-2），调用路径不动
- 0 新依赖，bundle ≤ 80KB
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-08-SUMMARY.md`
</output>

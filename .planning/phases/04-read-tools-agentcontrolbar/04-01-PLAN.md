---
phase: 04-read-tools-agentcontrolbar
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/agent/circuit-breaker.ts
  - src/agent/circuit-breaker.test.ts
  - src/agent/read-result.ts
  - src/agent/read-result.test.ts
autonomous: true
requirements: [ERR-03, TOOL-05, TOOL-06]
must_haves:
  truths:
    - "连续 5 次内同 code 失败 ≥3 次（中间穿插成功）→ circuit-breaker.isOpen 返 true"
    - "read 结果被包装成 {result_type, content, source}，metadata 与 document_content 正确分类"
    - "单 result >50K tokens（字符近似）被截断并带 truncated:true 标志"
  artifacts:
    - path: "src/agent/circuit-breaker.ts"
      provides: "sliding window 计数 + isOpen 判定（填实 Phase 3 骨架）"
      contains: "export function isOpen"
    - path: "src/agent/read-result.ts"
      provides: "wrapReadResult + applySizeCap + estimateTokens（TOOL-05/06）"
      exports: ["wrapReadResult", "applySizeCap", "estimateTokens"]
    - path: "src/agent/circuit-breaker.test.ts"
      provides: "A-10 中间成功不重置 vitest acceptance"
    - path: "src/agent/read-result.test.ts"
      provides: "包装分类 + size cap vitest acceptance"
  key_links:
    - from: "src/agent/loop-helpers.ts"
      to: "src/agent/circuit-breaker.ts"
      via: "breaker.isOpen / recordSuccess / recordFailure（Phase 3 已埋调用点，本 plan 填实现）"
      pattern: "breaker\\.(isOpen|recordSuccess|recordFailure)"
---

<objective>
填实 Phase 3 留下的两个纯逻辑骨架，作为整个 Phase 4 的底层依赖（无 React、无 Office.js、可独立 vitest）：

1. **circuit-breaker（ERR-03 / A-10）** — 把 `src/agent/circuit-breaker.ts` 从骨架（`isOpen` 永返 false、record* 空实现）填成真实 sliding window 判定。维度 = (tool name × error code)，最近 5 次调用内同 code ≥3 次失败 → `isOpen` 返 true；**中间穿插成功绝不重置 counter**（A-10 灵魂）。
2. **read-result（TOOL-05 / TOOL-06）** — 新建 `src/agent/read-result.ts` 纯函数模块：`wrapReadResult` 把 adapter 返回包装成 `{result_type, content, source}`；`applySizeCap` 对 >50K tokens 截断带 `truncated:true`；`estimateTokens` 用字符数近似（无 tokenizer，D-12）。

Purpose: 这两个模块是 read tool 实现（Plan 03/04/05/06）和错误 UX（Plan 07）的前置依赖。先用 TDD 锁死行为，下游 plan 直接消费。
Output: 2 个填实/新建的逻辑文件 + 2 个 vitest 测试文件，全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-read-tools-agentcontrolbar/04-CONTEXT.md
@.planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md

<interfaces>
现有 ToolError 形态（read-result 包装失败结果时复用，从 src/agent/tools/index.ts）：
```typescript
export type ToolErrorCode =
  | 'INVALID_ARGS' | 'NOT_FOUND' | 'PERMISSION_DENIED' | 'HOST_API_FAILED'
  | 'PRIVACY_BLOCKED' | 'CIRCUIT_OPEN' | 'STEP_LIMIT' | 'UNSUPPORTED';
export interface ToolError { code: ToolErrorCode; message: string; recoverable: boolean; hint: string; }
export interface ToolResult { ok: boolean; data?: unknown; error?: ToolError; reverse?: ReverseDescriptor; }
```

circuit-breaker.ts Phase 3 骨架现状（行号确认）：
- L11 `const _failureCounts = new Map<string, Array<{ ts: number; code: string }>>();`（形态已对，不重设计）
- L13 `recordSuccess(_toolName)` 空 / L17 `recordFailure(_toolName, _code)` 空 / L26 `isOpen(_toolName)` 返 false

loop-helpers.ts 调用点（Phase 3 已埋，本 plan 不改 loop-helpers，只填 circuit-breaker 实现）：
- L112 `if (breaker.isOpen(tc.name))`
- L124 `if (result.ok) breaker.recordSuccess(tc.name); else breaker.recordFailure(tc.name, result.error?.code ?? 'UNSUPPORTED');`
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: circuit-breaker sliding window 填实（ERR-03 / A-10）</name>
  <read_first>
    - src/agent/circuit-breaker.ts（被改文件，Phase 3 骨架；`_failureCounts` Map 形态 L11 已对）
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Code Examples「circuit-breaker 填实」L373-397（完整实现伪码 + __reset）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §`src/agent/circuit-breaker.ts` L147-155（A-10 灵魂 + 必测用例）
    - .planning/research/PITFALLS.md A-10 段（中间成功不重置的根因）
  </read_first>
  <behavior>
    - 常量：`WINDOW = 5`，`THRESHOLD = 3`。
    - `recordFailure('tool_x', 'HOST_API_FAILED')` 调 3 次（连续）→ `isOpen('tool_x')` 返 true。
    - A-10 核心 case：序列 `fail(C), success, fail(C), success, fail(C)`（同 code C，中间穿插 2 次成功）→ 第 3 个 fail 后 `isOpen` 返 true（成功不重置计数）。
    - 不同 code：`fail(A), fail(B), fail(A), fail(B), fail(A)` → A 出现 3 次 → isOpen 返 true（任一 code ≥3 即 open）。
    - 失败次数不足：`fail(C), fail(C)`（只 2 次）→ isOpen 返 false。
    - 窗口滑出：`fail(C), success, success, success, success`（C 在窗口最早，被后续 5 次挤出）→ 再来 4 次 success 后 isOpen 返 false。
    - 未知 tool：`isOpen('never_called')` 返 false。
    - `__reset()` 清空 history（仅测试用）。
  </behavior>
  <action>
    先写 `src/agent/circuit-breaker.test.ts`（RED），覆盖上面 behavior 全部 case，尤其 A-10「fail,success,fail,success,fail 同 code → isOpen true」必须独立断言一条。
    然后填实 `src/agent/circuit-breaker.ts`（GREEN），照 RESEARCH §Code Examples L373-397：
    - 把 L11 的 `_failureCounts` 重命名/复用为 `const history = new Map<string, Array<{ ts: number; code: string }>>();`（保留同形态）。
    - 加内部 `function pushRecord(tool: string, code: string)`：`const arr = history.get(tool) ?? []; arr.push({ ts: Date.now(), code }); if (arr.length > WINDOW) arr.shift(); history.set(tool, arr);`。**定长窗口：成功也占 slot 挤出旧记录，绝不 delete/reset（A-10）**。
    - `recordSuccess(tool)` → `pushRecord(tool, '_ok')`（成功记哨兵 code `'_ok'`，进窗口但不计入失败统计）。
    - `recordFailure(tool, code)` → `pushRecord(tool, code)`。
    - `isOpen(tool)`：取 `history.get(tool)`，遍历数组对每个 `code !== '_ok'` 计数到一个 `Map<string,number>`，任一 code 计数 ≥ `THRESHOLD` → 返 true，否则 false。
    - 加 `export function __reset(): void { history.clear(); }`（仅 vitest 用，注释标明）。
    - 函数签名保持与 Phase 3 一致：`recordSuccess(toolName: string): void` / `recordFailure(toolName: string, code: string): void` / `isOpen(toolName: string): boolean`（去掉骨架的下划线参数前缀和 eslint-disable 注释）。
    每个测试 `beforeEach(() => __reset())` 隔离状态。
  </action>
  <verify>
    <automated>npx vitest run src/agent/circuit-breaker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/agent/circuit-breaker.test.ts` exits 0
    - `grep -n "fail.*success.*fail.*success.*fail" src/agent/circuit-breaker.test.ts` 或等价的「中间成功不重置」断言存在（A-10 必测）
    - `grep -c "history.clear" src/agent/circuit-breaker.ts` ≥ 1（__reset 存在）
    - `grep -v '^ *\*' src/agent/circuit-breaker.ts | grep -c "return false" ` 的 `isOpen` 不再是无条件 `return false`（骨架已填实）— 人工确认 isOpen body 含 THRESHOLD 比较
  </acceptance_criteria>
  <done>circuit-breaker.test.ts 全绿，isOpen 真实判定，A-10 中间成功不重置有专门断言。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: read-result 包装 + size cap（TOOL-05 / TOOL-06）</name>
  <read_first>
    - src/agent/tools/index.ts L16-38（ToolError / ToolErrorCode / ToolResult 形态，包装失败结果时复用）
    - .planning/phases/04-read-tools-agentcontrolbar/04-RESEARCH.md §Code Examples「token 估算」L440-453 + Pattern 2「read result 包装注入」L200-211（result_type 分类表 + source 约定）
    - .planning/phases/04-read-tools-agentcontrolbar/04-PATTERNS.md §`src/agent/read-result.ts` L144-145 + §No Analog L237-241
  </read_first>
  <behavior>
    - `estimateTokens('一二三四五六七八')`（8 中文字）≈ ceil(8/1.6) = 5。
    - `applySizeCap` 对 ≤50K tokens 内容：返 `{ content, truncated: false }`，content 原样。
    - `applySizeCap` 对 >50K tokens 内容（构造长度 > 50000*1.6 = 80000 字符的串）：返 `{ truncated: true }`，content 被截到 ≤80000 字符 + 末尾追加 `'\n…[truncated]'`。
    - `wrapReadResult({ ok:true, data:{...} }, { result_type:'metadata', source:'document.paragraph_count' })` → `{ result_type:'metadata', content:<JSON.stringify(data) 经 applySizeCap>, source:'document.paragraph_count', truncated:false }`。
    - `wrapReadResult({ ok:false, error:{...} }, opts)` → 直接返 `{ ok:false, error }` 透传（失败不包装为 content；下游 dispatch sanitize 已处理）。
  </behavior>
  <action>
    先写 `src/agent/read-result.test.ts`（RED）覆盖上面 behavior。
    然后建 `src/agent/read-result.ts`（GREEN），照 RESEARCH §Code Examples L440-453：
    - `const HARD_CAP_TOKENS = 50_000;`、字符/token 近似常数 `1.6`（中文密集保守上界，偏大让 cap 更早触发 = 安全方向，D-12 / Assumption A2）。
    - `export function estimateTokens(s: string): number { return Math.ceil(s.length / 1.6); }`。
    - `export function applySizeCap(content: string): { content: string; truncated: boolean }`：若 `estimateTokens(content) <= HARD_CAP_TOKENS` 返 `{ content, truncated:false }`；否则 `const maxChars = HARD_CAP_TOKENS * 1.6; return { content: content.slice(0, maxChars) + '\n…[truncated]', truncated: true };`。
    - 定义 read 包装类型：`export type ReadResultType = 'document_content' | 'metadata';` + `export interface WrappedReadResult { result_type: ReadResultType; content: string; source: string; truncated: boolean; }`。
    - `export function wrapReadResult(result: ToolResult, opts: { result_type: ReadResultType; source: string }): ToolResult`：
      - 若 `result.ok === false` → 原样返 `result`（失败透传，不包装）。
      - 若 `result.ok === true` → `const raw = JSON.stringify(result.data ?? null); const { content, truncated } = applySizeCap(raw);` 返回新 ToolResult：`{ ok: true, data: { result_type: opts.result_type, content, source: opts.source, truncated } as WrappedReadResult }`。**包装对象塞进 ToolResult.data**，让 loop-helpers 现有 `JSON.stringify(result)` 透传时 wire content 自然含包装（不改 loop-helpers，Plan 06 验证透传）。
    - 从 `'./tools'` import `type { ToolResult, ToolError }`（read-result 在 src/agent/ 下，相对路径 `./tools`）。
    - 注释写明 result_type 分类规则（RESEARCH Pattern 2 L208-210）：metadata = 结构/计数（list_slides/get_paragraph_count/list_worksheets/get_used_range_summary/list_shapes_on_slide/get_document_outline）；document_content = 含用户正文（get_slide/get_shape/get_range_values/get_paragraph_at/get_document_full_text/selection_detail）。分类由 **调用方 tool execute 传 opts.result_type 决定**（Plan 06 各 tool 传对应值）。
  </action>
  <verify>
    <automated>npx vitest run src/agent/read-result.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/agent/read-result.test.ts` exits 0
    - `grep -c "HARD_CAP_TOKENS = 50_000" src/agent/read-result.ts` == 1
    - `grep -c "truncated" src/agent/read-result.ts` ≥ 2
    - `grep -E "result_type|content|source" src/agent/read-result.ts` 命中 WrappedReadResult 三字段
    - 测试含一条 >80000 字符串 → truncated:true 的断言
  </acceptance_criteria>
  <done>read-result.test.ts 全绿；wrapReadResult/applySizeCap/estimateTokens 行为锁死，下游 tool execute 可直接调用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read tool 结果 → LLM messages | adapter 读出的用户文档内容（不可信，可能藏 prompt injection）经 wrapReadResult 后回灌 LLM |
| tool 错误 → LLM / UI | 失败结果由下游 dispatch sanitize（本 plan read-result 失败路径只透传，不读 stack/path） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/Elevation | read-result.wrapReadResult | mitigate | 包装 `result_type:'document_content'` 让 LLM 区分 evidence vs 指令（system prompt 配合在 Plan 06）；本 plan 确保 content 是纯字符串、不夹带额外字段被 LLM 误解为指令 |
| T-04-02 | DoS | applySizeCap | mitigate | 50K token hard cap 截断，防超长 result 撑爆 context；估算偏大（1.6 字/token）让 cap 更早触发 |
| T-04-03 | Information Disclosure | wrapReadResult 失败路径 | accept | 失败结果原样透传给下游 `dispatchTool` 的 allowlist sanitize（D-15 已建），本 plan 不读 err.stack/.message |
</threat_model>

<verification>
- `npx vitest run src/agent/circuit-breaker.test.ts src/agent/read-result.test.ts` 全绿
- `npm run test -- --run` 全套不回归
- `npm run build` 通过（0 新依赖，D-10）
</verification>

<success_criteria>
- circuit-breaker.isOpen 真实判定，A-10 中间成功不重置有专门 vitest 断言
- read-result 三函数行为锁死，metadata/document_content 分类由调用方决定
- 0 净新增运行时依赖；bundle 不超 80KB gate
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-tools-agentcontrolbar/04-01-SUMMARY.md`
</output>

---
phase: 26-config-import-export
plan: 03
type: execute
wave: 2
depends_on:
  - 26-01
  - 26-02
files_modified:
  - src/components/Settings/SettingsPanel.tsx
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03

must_haves:
  truths:
    - "Settings 浏览态有「配置备份与迁移」独立分区，含常驻警告条（role=note，文案含「明文 API 密钥/妥善保管/用完即删/勿通过不安全渠道传输」）"
    - "「导出配置」按钮点击后浏览器下载 JSON 文件（aster-config-YYYYMMDD.json）"
    - "「导入配置」按钮触发 file input，用户选 JSON 文件后走 parseImportFile → 无错误时弹确认对话框 → 确认后 applyImport 写入 → toast 摘要"
    - "导入确认对话框含明文警告重申（role=dialog + aster-warn-callout）"
    - "同 id Provider 覆盖弹覆盖二次确认对话框（批量三按钮：取消/跳过冲突/覆盖并导入）"
    - "损坏/非法/非 Aster JSON 弹错误对话框（role=alert + aster-error-callout + 可操作 message + hint）"
    - "importNonce + useEffect 确保导入成功后 imageGenModel 和 pexelsApiKey 组件 state 刷新"
    - "npm run extract 成功（无新未翻译字符串，coverage.test.ts 绿）"
    - "npm run build && npm run size → main bundle ≤82KB gzip"
    - "npx vitest run 全套 0 failed"
  artifacts:
    - path: "src/components/Settings/SettingsPanel.tsx"
      provides: "「配置备份与迁移」分区 + 导出/导入全交互流程"
      contains: "配置备份与迁移"
  key_links:
    - from: "SettingsPanel.tsx 导出按钮"
      to: "configBackup.ts exportConfig"
      via: "onClick={handleExport} → exportConfig()"
      pattern: "exportConfig"
    - from: "SettingsPanel.tsx file input"
      to: "configBackup.ts parseImportFile + applyImport"
      via: "handleFileChosen → parseImportFile → applyImport"
      pattern: "applyImport"
    - from: "SettingsPanel.tsx importNonce"
      to: "imageGenModel/pexelsApiKey useState"
      via: "useEffect([importNonce]) → storage.get 重读"
      pattern: "importNonce"
---

<objective>
在 SettingsPanel.tsx 装配「配置备份与迁移」Settings 分区，接入 configBackup.ts 的导出/导入函数，实现完整的 D-01~D-04 UI 交互流程，并通过收尾守门（Lingui extract + build + size + vitest）。

Purpose: 把 Plan 01 的纯函数层和 Plan 02 的 CSS/图标资产接入 UI，打通"导出 → 下载"和"导入 → 确认 → 写入 → toast"两条完整链路，完成 CFG-01/CFG-02/CFG-03。
Output:
- SettingsPanel.tsx 新增「配置备份与迁移」section + 全部对话框状态机 + importNonce 刷新逻辑
- 收尾守门：Lingui extract 绿 + bundle ≤82KB + 全套 vitest 绿
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-config-import-export/26-CONTEXT.md
@.planning/phases/26-config-import-export/26-UI-SPEC.md
@.planning/phases/26-config-import-export/26-PATTERNS.md

<interfaces>
<!-- 执行者必读签名（Plan 01/02 已产出，执行者直接取用） -->

来自 Plan 01 产出（src/lib/configBackup.ts）：
```typescript
export function exportConfig(): void            // 浏览器下载 JSON 文件
export function parseImportFile(raw: string):
  | { ok: true; config: AsterConfigExport }
  | { ok: false; error: ImportError }
export function detectConflicts(
  imported: AsterConfigData,
  currentProviders: ProviderConfig[],
): string[]                                      // 返回冲突 provider id 列表
export async function applyImport(
  config: AsterConfigData,
  options: { skipIds?: string[] },
): Promise<ImportResult>                         // { providerCount, keyCount, prefsRestored }
export interface AsterConfigExport { app: 'aster'; version: number; exportedAt: string; data: AsterConfigData }
export interface AsterConfigData { providers: ProviderConfig[]; keys: Record<string,string>; ... }
export interface ImportResult { providerCount: number; keyCount: number; prefsRestored: boolean }
```

来自 Plan 02 产出（CSS 类已存在于 styles.css）：
- `.aster-warn-callout` / `.aster-warn-callout__icon` / `.aster-warn-callout__text`（常驻警告条）
- `.aster-error-callout` / `__icon` / `__msg` / `__hint`（错误 callout）
- `.aster-settings__backup-actions`（两按钮等宽并排）
- `.aster-import-conflict-list`（覆盖确认冲突列表）

来自 src/components/Settings/SettingsPanel.tsx（执行者必须先读文件定位行号）：
```typescript
// L24-35 现有 import 区块（新增 import 追加到末尾）
// L57-67 偏好 store 消费（usePreferencesStore）
// L70：docKeyRef useRef 范式（fileInputRef 仿照）
// L93-99：imageGenModel useState（importNonce useEffect 需改动此处）
// L103-111：pexelsApiKey useState（importNonce useEffect 需改动此处）
// L186：aster-settings__global-options 容器开始
// L287-315：「自定义偏好」section（新分区插在 L315 </div> 之后）
// L318-359：「清空聊天记录」section（新分区插在此之前）
// L319-359：内联两步确认范式（confirming state + hist-confirm-row）
// toast 消费：useToastStore.getState().showToast(...)（现有范式）
```

来自 icons.tsx（Plan 02 已产出）：
```typescript
export function DownloadIcon({ size = 24 }: { size?: number } = {}): ReactElement
export function UploadIcon(): ReactElement
export function AlertIcon(): ReactElement   // L102-110
```

Lingui 宏（全部 UI 文案必须包裹）：
```typescript
import { Trans, useLingui } from '@lingui/react/macro';
const { t } = useLingui();
// 静态：<Trans>配置备份与迁移</Trans>
// 动态：t`已导入 ${n} 个 Provider · ${m} 个密钥`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsPanel.tsx 装配「配置备份与迁移」分区 + 全交互状态机</name>
  <read_first>
    - src/components/Settings/SettingsPanel.tsx（必须先读整个文件，定位以下精确行号：imageGenModel useState / pexelsApiKey useState / aster-settings__global-options 容器 / 「自定义偏好」section 末尾 / 「清空聊天记录」section 开始；确认 useToastStore 已 import）
    - PATTERNS.md §src/components/Settings/SettingsPanel.tsx（全部代码草案：importNonce useEffect / file input ref / 分区 section DOM / modal 消费范式 / toast 调用）
    - UI-SPEC.md §2（分区结构）、§5（导入交互总流程 + 三种对话框 DOM 草图）、§7（文案契约）、§8（A11y 要求）
    - src/lib/configBackup.ts（Plan 01 产出，确认函数签名）
  </read_first>

  <action>
对 src/components/Settings/SettingsPanel.tsx 做以下修改（全部按 PATTERNS.md 草案，不要偏离）：

**A. import 区块追加（在现有 import 块末尾）：**
```typescript
import { DownloadIcon, UploadIcon, AlertIcon } from '../icons';
import {
  exportConfig,
  parseImportFile,
  detectConflicts,
  applyImport,
  type AsterConfigExport,
  type ImportResult,
} from '../../lib/configBackup';
import { useToastStore } from '../../store/toast';   // 若现有 import 未含，则追加
```
（若 useToastStore 已 import，不重复添加；执行者读文件确认）

**B. 新增 state 和 ref（在现有 state 声明区末尾追加）：**
```typescript
// Phase 26：file input ref
const fileInputRef = useRef<HTMLInputElement>(null);

// Phase 26：importNonce — 导入成功后递增，触发 imageGenModel/pexelsKey 本地 state 重读
const [importNonce, setImportNonce] = useState(0);

// Phase 26：导入对话框状态机
type ImportDialogState =
  | { kind: 'none' }
  | { kind: 'confirm'; parsedConfig: AsterConfigExport }
  | { kind: 'conflict'; parsedConfig: AsterConfigExport; conflictIds: string[] }
  | { kind: 'error'; error: { code: string; message: string; hint: string } };

const [importDialog, setImportDialog] = useState<ImportDialogState>({ kind: 'none' });
```

**C. imageGenModel/pexelsApiKey 的 useState 改为响应 importNonce（重要！）：**

找到现有的 imageGenModel useState（约 L93-99）和 pexelsApiKey useState（约 L103-111），在其后追加 useEffect：
```typescript
// Phase 26：导入成功后 importNonce 递增 → 重读 storage 刷新本地 state
useEffect(() => {
  if (importNonce === 0) return;   // 初始挂载不触发（仅响应导入）
  setImageGenModelState(
    storage.get<string>(STORAGE_KEYS.PREF_IMAGE_GEN_MODEL) ?? DEFAULT_IMAGE_GEN_MODEL.id,
  );
  setPexelsApiKeyState(storage.get<string>(STORAGE_KEYS.PEXELS_API_KEY) ?? '');
}, [importNonce]);
```
注意：setImageGenModelState 和 setPexelsApiKeyState 是 imageGenModel/pexelsApiKey 各自 useState 的 setter（按实际文件中的 setter 名称填写）。需要先读文件确认这两个 setter 的名称。

**D. 事件处理函数（在现有 handler 区末尾追加）：**
```typescript
// Phase 26：导出配置
function handleExport(): void {
  exportConfig();
  useToastStore.getState().showToast(t`配置已导出`);
}

// Phase 26：用户选择导入文件
async function handleFileChosen(e: React.ChangeEvent<HTMLInputElement>): Promise<void> {
  const file = e.target.files?.[0];
  if (!file) return;
  e.target.value = '';   // reset，允许重选同名文件
  const raw = await file.text();
  const result = parseImportFile(raw);
  if (!result.ok) {
    setImportDialog({ kind: 'error', error: result.error });
    return;
  }
  const { useProviderStore } = await import('../../store/providers');
  const conflicts = detectConflicts(result.config.data, useProviderStore.getState().providers);
  if (conflicts.length > 0) {
    setImportDialog({ kind: 'conflict', parsedConfig: result.config, conflictIds: conflicts });
  } else {
    setImportDialog({ kind: 'confirm', parsedConfig: result.config });
  }
}

// Phase 26：确认导入（无冲突路径）
async function handleConfirmImport(): Promise<void> {
  if (importDialog.kind !== 'confirm') return;
  const res = await applyImport(importDialog.parsedConfig.data, {});
  setImportNonce((n) => n + 1);
  setImportDialog({ kind: 'none' });
  const msg = res.prefsRestored
    ? t`已导入 ${res.providerCount} 个 Provider · ${res.keyCount} 个密钥，偏好已恢复`
    : t`已导入 ${res.providerCount} 个 Provider · ${res.keyCount} 个密钥`;
  useToastStore.getState().showToast(msg);
}

// Phase 26：覆盖并导入（有冲突路径，选择全部覆盖）
async function handleOverwriteAndImport(): Promise<void> {
  if (importDialog.kind !== 'conflict') return;
  const res = await applyImport(importDialog.parsedConfig.data, {});
  setImportNonce((n) => n + 1);
  setImportDialog({ kind: 'none' });
  const msg = t`已导入 ${res.providerCount} 个 Provider · ${res.keyCount} 个密钥`;
  useToastStore.getState().showToast(msg);
}

// Phase 26：跳过冲突项（仅导入新 id）
async function handleSkipConflictsAndImport(): Promise<void> {
  if (importDialog.kind !== 'conflict') return;
  const res = await applyImport(importDialog.parsedConfig.data, {
    skipIds: importDialog.conflictIds,
  });
  setImportNonce((n) => n + 1);
  setImportDialog({ kind: 'none' });
  const msg = t`已导入 ${res.providerCount} 个新 Provider，跳过 ${importDialog.conflictIds.length} 个冲突项`;
  useToastStore.getState().showToast(msg);
}
```

**E. 分区 section JSX（插入位置：「自定义偏好」section 末尾 </div> 之后、「清空聊天记录」section `{/* Phase 8 HIST-02 */}` 注释之前）：**

```tsx
{/* Phase 26 CFG-01/02/03 — 配置备份与迁移 */}
<div className="aster-settings__section">
  <span className="aster-settings__label">
    <Trans>配置备份与迁移</Trans>
  </span>
  <p className="aster-settings__hint">
    <Trans>
      把全部配置（含各 Provider 的 API 密钥、默认 Provider、偏好、主题色、生图模型、图库 Key）
      导出为一个 JSON 文件；换电脑 / 换浏览器 / 换宿主时导入即可还原，无需重输任何密钥。
    </Trans>
  </p>

  {/* 常驻警告条（D-03，CFG-03）— role=note 常驻不动 */}
  <div className="aster-warn-callout" role="note">
    <span className="aster-warn-callout__icon" aria-hidden="true">
      <AlertIcon />
    </span>
    <p className="aster-warn-callout__text">
      <strong><Trans>此文件含明文 API 密钥。</Trans></strong>
      <Trans>请妥善保管、用完即删、勿通过不安全渠道（邮件 / 聊天群 / 网盘公开链接）传输。</Trans>
    </p>
  </div>

  {/* 两按钮等宽并排行 */}
  <div className="aster-settings__backup-actions">
    <button
      type="button"
      className="btn btn-ghost"
      onClick={handleExport}
      aria-label={t`导出配置`}
    >
      <DownloadIcon size={16} />
      <Trans>导出配置</Trans>
    </button>
    <button
      type="button"
      className="btn btn-ghost"
      onClick={() => fileInputRef.current?.click()}
      aria-label={t`导入配置`}
    >
      <UploadIcon />
      <Trans>导入配置</Trans>
    </button>
  </div>

  {/* 隐藏 file input（不复用聊天附件管线，F-05） */}
  <input
    ref={fileInputRef}
    type="file"
    accept="application/json,.json"
    hidden
    onChange={handleFileChosen}
    aria-hidden="true"
    tabIndex={-1}
  />
</div>
```

**F. 对话框 JSX（在 return JSX 顶层，整个 SettingsPanel 渲染树末尾，Portal 或直接追加于根 div）：**

```tsx
{/* Phase 26 — 导入对话框（confirm / conflict / error） */}
{importDialog.kind !== 'none' && (
  <div
    className="modal-scrim"
    onClick={() => setImportDialog({ kind: 'none' })}
  >
    <div
      className="modal"
      role="dialog"
      aria-modal="true"
      aria-labelledby="import-dlg-title"
      onClick={(e) => e.stopPropagation()}
    >
      {/* === 错误态 === */}
      {importDialog.kind === 'error' && (
        <>
          <h2 className="modal-title" id="import-dlg-title">
            <Trans>无法导入此文件</Trans>
          </h2>
          <div className="aster-error-callout" role="alert">
            <span className="aster-error-callout__icon" aria-hidden="true">
              <AlertIcon />
            </span>
            <div>
              <p className="aster-error-callout__msg">{importDialog.error.message}</p>
              <p className="aster-error-callout__hint">{importDialog.error.hint}</p>
            </div>
          </div>
          <div className="modal-foot">
            <button
              type="button"
              className="btn btn-ghost"
              onClick={() => setImportDialog({ kind: 'none' })}
            >
              <Trans>关闭</Trans>
            </button>
            <button
              type="button"
              className="btn btn-primary"
              onClick={() => {
                setImportDialog({ kind: 'none' });
                fileInputRef.current?.click();
              }}
            >
              <Trans>重新选择文件</Trans>
            </button>
          </div>
        </>
      )}

      {/* === 简单确认态（无冲突） === */}
      {importDialog.kind === 'confirm' && (
        <>
          <h2 className="modal-title" id="import-dlg-title">
            <Trans>导入配置</Trans>
          </h2>
          <p className="modal-sub">
            <Trans>即将从所选文件导入配置，与本地现有配置合并（保留现有 + 加入新的）。</Trans>
          </p>
          <div className="aster-warn-callout" role="note" id="import-dlg-warn">
            <span className="aster-warn-callout__icon" aria-hidden="true">
              <AlertIcon />
            </span>
            <p className="aster-warn-callout__text">
              <strong><Trans>该文件含明文 API 密钥。</Trans></strong>
              <Trans>请确认来源可信。导入后请妥善保管或删除原文件。</Trans>
            </p>
          </div>
          <div className="modal-foot">
            <button
              type="button"
              className="btn btn-ghost"
              onClick={() => setImportDialog({ kind: 'none' })}
            >
              <Trans>取消</Trans>
            </button>
            <button
              type="button"
              className="btn btn-primary"
              onClick={() => void handleConfirmImport()}
            >
              <Trans>确认导入</Trans>
            </button>
          </div>
        </>
      )}

      {/* === 覆盖二次确认态（有冲突） === */}
      {importDialog.kind === 'conflict' && (
        <>
          <h2 className="modal-title" id="import-dlg-title">
            <Trans>覆盖已有配置？</Trans>
          </h2>
          <p className="modal-sub">
            <Trans>以下 Provider 在本地已存在，导入会覆盖它们的配置与密钥：</Trans>
          </p>
          <ul className="aster-import-conflict-list">
            {importDialog.conflictIds.map((id) => {
              const prov = importDialog.parsedConfig.data.providers.find((p) => p.id === id);
              return (
                <li key={id}>{prov ? prov.name : id}</li>
              );
            })}
          </ul>
          <div className="modal-foot">
            <button
              type="button"
              className="btn btn-ghost"
              onClick={() => setImportDialog({ kind: 'none' })}
            >
              <Trans>取消</Trans>
            </button>
            <button
              type="button"
              className="btn btn-ghost"
              onClick={() => void handleSkipConflictsAndImport()}
            >
              <Trans>跳过冲突项</Trans>
            </button>
            <button
              type="button"
              className="btn btn-primary"
              onClick={() => void handleOverwriteAndImport()}
            >
              <Trans>覆盖并导入</Trans>
            </button>
          </div>
        </>
      )}
    </div>
  </div>
)}
```

**G. A11y 补充（UI-SPEC §8 要求）：**
- 对话框打开时焦点需移入（可用 useEffect 监听 importDialog.kind !== 'none' 时 focus 取消按钮）。若实现复杂，至少确保对话框内按钮 Tab 可达（原生 button 已满足），不须强制 focus trap 在 Wave 2 内完成（真机 UAT 可补）。
- 隐藏 file input 已有 hidden + tabIndex=-1 + aria-hidden，无需额外处理。

**注意：**
- 所有文案必须用 `<Trans>` 或 `t\`\`` 包裹（Lingui 宏）。
- 动态文案（toast / aria-label）用 `t\`\``，静态文案用 `<Trans>`。
- error.message 和 error.hint 是来自 configBackup.ts 的已翻译中文字符串，直接渲染（不再包宏，避免双重处理）。
- 对话框背景点击关闭：onClick={() => setImportDialog({ kind: 'none' })}（但 modal 内部 onClick={e => e.stopPropagation()} 阻止冒泡）。
  </action>

  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "配置备份与迁移" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx` 输出 >= 1（分区文案存在）。
    - `grep -c "aster-warn-callout" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx` 输出 >= 2（分区内 + 导入确认对话框内各一处）。
    - `grep -c "exportConfig\(\)" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx` 输出 >= 1。
    - `grep -c "applyImport" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx` 输出 >= 1。
    - `grep -c "importNonce" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx` 输出 >= 2（state 声明 + setImportNonce 调用）。
    - `grep -c "role=\"note\"" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx` 输出 >= 1（CFG-03 常驻警告 role=note）。
    - `npx vitest run` 输出 0 failed。
  </acceptance_criteria>

  <done>SettingsPanel.tsx 包含完整「配置备份与迁移」分区，导出/导入/确认/覆盖/错误全部流程均有 JSX；全套 vitest 绿。</done>
</task>

<task type="auto">
  <name>Task 2: 收尾守门（Lingui extract + build + size）</name>
  <read_first>
    - package.json（确认 "extract" 和 "size" script 名称；以及 "build" 脚本）
    - .size-limit.json（确认 ≤82KB gzip 门槛）
  </read_first>

  <action>
依次执行以下守门命令（按顺序，全部必须通过）：

**1. Lingui extract（i18n 宏提取）：**
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
npm run extract
```
如果输出含「N messages not extracted」或「coverage.test.ts」失败，检查 SettingsPanel.tsx 中是否有文案未用 `<Trans>` / `t\`\`` 包裹，修复后重跑。

**2. 构建验证：**
```bash
npm run build
```
期望：build 成功，无 TypeScript 错误，无 Vite 报错。

**3. bundle 大小守门（必须先 build 再 size，不可颠倒）：**
```bash
npm run size
```
期望：`main-*.js (gzip)` ≤ 82 KB。（若超出，检查是否有非懒加载引入，本阶段理论增量 < 1KB，若超出说明有意外 import）

**4. 全套 vitest 绿：**
```bash
npx vitest run
```
期望：0 failed（注意尾部最多 3 个 retry errors 是噪音，忽略；"N failed" 是真失败）。

若任一步骤失败，修复对应问题后再继续（不跳过，不假绿）。
  </action>

  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && npm run extract && npm run build && npm run size && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `npm run extract` 退出码 0，无「messages not extracted」错误。
    - `npm run build` 退出码 0，无 TypeScript 或 Vite 错误。
    - `npm run size` 输出 `main-*.js (gzip)` 数值 ≤ 82 KB（精确数字写入 SUMMARY）。
    - `npx vitest run` 输出 0 failed（或仅 3 个 retry errors 噪音）。
    - 以上 4 步按顺序全部通过，方可写 SUMMARY。
  </acceptance_criteria>

  <done>Lingui extract 绿 + build 成功 + bundle ≤82KB gzip + vitest 全套 0 failed。Phase 26 全部实现交付。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户文件→SettingsPanel | file input 读取的 JSON 内容来自用户磁盘，内容不可信，须经 parseImportFile 校验 |
| SettingsPanel→store | applyImport 写入 Zustand store / storage，需经 setter 内置 sanitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-UI-01 | Tampering | handleFileChosen → parseImportFile | mitigate | parseImportFile 做结构校验（app/version/data）；非 Aster JSON → 错误对话框，不写入任何 store |
| T-26-UI-02 | Information Disclosure | 对话框渲染 provider.name（来自导入文件） | mitigate | React JSX 自动转义（无 dangerouslySetInnerHTML）；provider.name 作为文本节点渲染，无 XSS |
| T-26-UI-03 | Information Disclosure | 常驻 warn callout 明文提示 | mitigate | CFG-03 mitigation 已落地：role=note 永久渲染 + 措辞含「明文 API 密钥/妥善保管/用完即删/勿通过不安全渠道传输」+ 导入确认重申；不上传服务器（纯浏览器 Blob 下载） |
| T-26-UI-04 | Elevation of Privilege | applyImport 写入 userPreferences | mitigate | applyImport 内部调 usePreferencesStore.getState().setPrefs()，setPrefs 含 sanitizePrefs（PREF-02 既有防御） |
</threat_model>

<verification>
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"

# 分区文案存在
grep -c "配置备份与迁移" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx
# 期望：>= 1

# 常驻警告 CFG-03 基线
grep -c "role=\"note\"" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx
# 期望：>= 1

# 守门链（顺序执行）
npm run extract && npm run build && npm run size && npx vitest run
# 期望：全部退出码 0；main bundle ≤82KB；vitest 0 failed
```
</verification>

<success_criteria>
- Settings「配置备份与迁移」分区含常驻警告条（role=note，文案含「明文 API 密钥/妥善保管/用完即删/勿通过不安全渠道传输」）—— CFG-03 PASS 基线。
- 导出/导入/确认/覆盖/错误全部 JSX 交互路径存在于 SettingsPanel.tsx。
- importNonce + useEffect 确保 imageGenModel 和 pexelsApiKey 组件 state 在导入后刷新。
- npm run extract 绿（coverage.test.ts 无红）。
- npm run build && npm run size → main bundle ≤82KB gzip。
- npx vitest run → 0 failed。
</success_criteria>

<output>
完成后创建 `.planning/phases/26-config-import-export/26-03-SUMMARY.md`
</output>

---
phase: 26-config-import-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/icons.tsx
  - src/styles.css
autonomous: true
requirements:
  - CFG-03

must_haves:
  truths:
    - "DownloadIcon 已存在于 icons.tsx 并正确 export，支持 size prop"
    - "src/styles.css 新增 4 个组件类（aster-warn-callout/aster-error-callout/aster-settings__backup-actions/aster-import-conflict-list），全部引用既有 token，零新增颜色 hex"
    - "light/dark 两套主题均正确取色（--warning/--warning-soft/--error/--error-soft 均已有两套 token）"
  artifacts:
    - path: "src/components/icons.tsx"
      provides: "DownloadIcon 内联 SVG 导出"
      contains: "export function DownloadIcon"
    - path: "src/styles.css"
      provides: "4 个 Phase 26 组件类"
      contains: ".aster-warn-callout"
  key_links:
    - from: "SettingsPanel.tsx（Plan 03）"
      to: "src/components/icons.tsx DownloadIcon"
      via: "import { DownloadIcon } from '../icons'"
      pattern: "DownloadIcon"
    - from: "SettingsPanel.tsx（Plan 03）"
      to: "src/styles.css .aster-warn-callout"
      via: "className=\"aster-warn-callout\""
      pattern: "aster-warn-callout"
---

<objective>
新增 `DownloadIcon` 图标到 icons.tsx，并在 styles.css 中新增 Phase 26 所需的 4 个组件类。

Purpose: 为 Wave 2 的 SettingsPanel UI 装配准备基础资产（图标 + CSS 类），两者无相互依赖，可与 Plan 01 并行执行。
Output:
- `src/components/icons.tsx`：新增 `DownloadIcon`（导出，支持 size prop，插入 UploadIcon 之后）
- `src/styles.css`：新增 4 个组件类，全部引用既有 token（零新增颜色 hex）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-config-import-export/26-UI-SPEC.md
@.planning/phases/26-config-import-export/26-PATTERNS.md

<interfaces>
<!-- 执行者必读 —— 从 PATTERNS.md 精确提取，无需再查源文件 -->

来自 src/components/icons.tsx（PATTERNS.md §icons.tsx 核验，执行者必须先读该文件定位插入位置）：

```typescript
// L9-17：base 对象（已确认存在，含 strokeWidth:1.5 / strokeLinecap:'round' / strokeLinejoin:'round' / fill:'none' / aria-hidden:true）
// L31-40：UploadIcon（上传，箭头入托盘，无 size prop）
export function UploadIcon(): ReactElement { ... }
// L43-50：SendIcon（有 size prop 范式，DownloadIcon 仿照）
export function SendIcon({ size = 24 }: { size?: number } = {}): ReactElement { ... }
// L70-79：InsertIcon（插入到文档，语义不同于 DownloadIcon，不可复用）
// L102-110：AlertIcon（三角感叹号，Plan 03 UI 会引用）
```

DownloadIcon 精确实现（PATTERNS.md §icons.tsx，直接复制）：
```tsx
/** 下载（箭头出托盘，配置导出 Phase 26） */
export function DownloadIcon({ size = 24 }: { size?: number } = {}): ReactElement {
  return (
    <svg {...base} width={size} height={size}>
      <path d="M12 3v12" />
      <path d="M7 10l5 5 5-5" />
      <path d="M5 19h14" />
    </svg>
  );
}
```
插入位置：紧接 UploadIcon（L40）之后，保持上传/下载图标相邻。

来自 src/styles.css（PATTERNS.md §styles.css 核验）：

既有 warning token（两主题已有，不需新增）：
```css
/* light（L70-71）：--warning: #b45309; --warning-soft: #fef3c7; */
/* dark（L1475-1476）：--warning: #fbbf24; --warning-soft: rgba(251,191,36,0.18); */
```
既有 error token（已有，不需新增）：
```css
/* light（L62）：--error: #dc2626; --error-soft: #fee2e2; */
/* dark：--error: #f87171; --error-soft: rgba(248,113,113,0.14); */
```
既有参照类（形态参考，token 取用范式）：
- .pane-banner（L792-802）：background: color-mix(in srgb, var(--warning) 6%, transparent); color: var(--warning)
- .badge-warning（L1461-1469）：background: var(--warning-soft); color: var(--warning)
- .toast（L1671-1710）：border-left: 3px solid var(--accent)（warn callout 改用 var(--warning)）
- .modal-scrim/.modal（L1196-1252）：对话框基础结构
CSS 插入位置建议：.badge-warning 块（L1461）之后，Settings 样式区内（aster-settings__backup-actions 放在已有 .aster-settings__ 块附近）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 DownloadIcon 到 icons.tsx</name>
  <read_first>
    - src/components/icons.tsx（必须先读整个文件，定位 UploadIcon L31-40、SendIcon L43-50 的 size prop 范式；确认 base 对象和 ReactElement import；找准插入行号）
    - PATTERNS.md §src/components/icons.tsx（DownloadIcon SVG path 草案 + 插入位置说明）
  </read_first>

  <action>
在 src/components/icons.tsx 中，紧接 UploadIcon 函数体之后（L40 之后），插入 DownloadIcon 函数：

```tsx
/** 下载（箭头出托盘，配置导出 Phase 26） */
export function DownloadIcon({ size = 24 }: { size?: number } = {}): ReactElement {
  return (
    <svg {...base} width={size} height={size}>
      <path d="M12 3v12" />
      <path d="M7 10l5 5 5-5" />
      <path d="M5 19h14" />
    </svg>
  );
}
```

注意：
- `{...base}` 已包含 strokeWidth:1.5、strokeLinecap:'round'、strokeLinejoin:'round'、fill:'none'、aria-hidden:true，不需要重复写这些属性。
- size prop 形态仿照 SendIcon（`{ size = 24 }: { size?: number } = {}`），因为 UI-SPEC §2.4 按钮行要 `size={16}`。
- **不修改** UploadIcon（无 size prop），因为 SettingsPanel 现有 `<UploadIcon />` 调用不加 size。
- 插入后检查文件是否仍能通过 TypeScript（不引入新 import，base 和 ReactElement 已 import）。
  </action>

  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "export function DownloadIcon" /Users/wb.chen/Documents/Project/Aster/src/components/icons.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function DownloadIcon" src/components/icons.tsx` 输出 1。
    - `grep -c "size?: number" src/components/icons.tsx` 输出 >= 1（size prop 存在）。
    - `grep -c "M7 10l5 5 5-5" src/components/icons.tsx` 输出 1（下载箭头 path 正确）。
    - 不修改其他图标函数（grep -c "export function UploadIcon" 仍输出 1，行数不变）。
    - TypeScript 类型验证：`export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && npx tsc --noEmit` 退出码 0（无新 TS 错误；Node 22 路径已由 PATH 前缀保证）。等价地，`npm run build` 成功也视为通过。
  </acceptance_criteria>

  <done>DownloadIcon 已正确插入 icons.tsx 并导出，支持 size prop，SVG path 与 UI-SPEC §6 吻合。</done>
</task>

<task type="auto">
  <name>Task 2: 在 styles.css 新增 4 个 Phase 26 组件类</name>
  <read_first>
    - src/styles.css（必须先读，定位 .badge-warning（约 L1461）和 .aster-settings__section 的位置，确认 --warning/--warning-soft/--error/--error-soft token 已存在；找 CSS 插入位置）
    - PATTERNS.md §src/styles.css（4 个新增类的精确 CSS 代码草案 + 插入位置建议）
    - UI-SPEC.md §3.1（.aster-warn-callout 规格）、§5.3（.aster-import-conflict-list 规格）、§5.4（.aster-error-callout 规格）、§2.3（.aster-settings__backup-actions 规格）
  </read_first>

  <action>
在 src/styles.css 中新增 4 个组件类。**零新增颜色 hex，全部引用既有 token。**

插入位置：.badge-warning/.badge-error 块（约 L1461-1469）之后，添加注释标题 `/* === Phase 26 — 配置备份与迁移 */`，然后依次插入：

**1. .aster-warn-callout（常驻明文警告条）：**
```css
/* Phase 26 — 常驻明文警告条（teal 克制：复用既有 --warning 语义色，零新增 token） */
.aster-warn-callout {
  display: flex;
  align-items: flex-start;
  gap: var(--space-2);
  margin-top: var(--space-2);
  padding: var(--space-2) var(--space-3);
  background: var(--warning-soft);
  border: 1px solid color-mix(in srgb, var(--warning) 28%, transparent);
  border-left: 3px solid var(--warning);
  border-radius: var(--radius-2);
}
.aster-warn-callout__icon {
  display: inline-flex;
  align-items: center;
  color: var(--warning);
  flex-shrink: 0;
  margin-top: 1px;
}
.aster-warn-callout__text {
  margin: 0;
  font-size: var(--fs-12);
  line-height: 1.5;
  color: var(--text);
}
.aster-warn-callout__text strong {
  font-weight: 600;
  color: var(--warning);
}
```

**2. .aster-error-callout（导入错误 callout）：**
```css
/* Phase 26 — 导入错误 callout（复用既有 --error 语义色，零新增 token） */
.aster-error-callout {
  display: flex;
  align-items: flex-start;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-3);
  background: var(--error-soft);
  border: 1px solid color-mix(in srgb, var(--error) 28%, transparent);
  border-left: 3px solid var(--error);
  border-radius: var(--radius-2);
}
.aster-error-callout__icon {
  display: inline-flex;
  align-items: center;
  color: var(--error);
  flex-shrink: 0;
  margin-top: 1px;
}
.aster-error-callout__msg {
  margin: 0;
  font-size: var(--fs-13);
  color: var(--text);
  font-weight: 500;
}
.aster-error-callout__hint {
  margin: var(--space-1) 0 0;
  font-size: var(--fs-12);
  color: var(--text-2);
  line-height: 1.5;
}
```

**3. .aster-settings__backup-actions（两按钮等宽并排行）：**
找到已有 `.aster-settings__` 样式区块（Settings 样式区附近），在其中或紧后插入：
```css
/* Phase 26 — 配置备份与迁移：两按钮等宽并排 */
.aster-settings__backup-actions {
  display: flex;
  gap: var(--space-2);
  margin-top: var(--space-2);
}
.aster-settings__backup-actions .btn {
  flex: 1;
}
```

**4. .aster-import-conflict-list（覆盖确认冲突项列表）：**
与 warn/error callout 块相邻插入：
```css
/* Phase 26 — 导入覆盖确认：冲突项列表（极简，非预览面板） */
.aster-import-conflict-list {
  margin: 0;
  padding: var(--space-2) 0 0;
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
  max-height: 160px;
  overflow-y: auto;
}
.aster-import-conflict-list li {
  font-size: var(--fs-13);
  color: var(--text);
  padding: var(--space-1) var(--space-2);
  background: var(--surface-2);
  border-radius: var(--radius-1);
}
```

**检查约束：**
- 确认 --warning / --warning-soft / --error / --error-soft 在文件中已有定义（两主题）。
- 确认没有新增任何 hex 颜色字面量（只引用 var(...)）。
- color-mix() 已在 .pane-banner 等处使用，可放心用。
  </action>

  <verify>
    <automated>grep -c "\.aster-warn-callout" /Users/wb.chen/Documents/Project/Aster/src/styles.css</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "\.aster-warn-callout" src/styles.css` 输出 >= 1。
    - `grep -c "\.aster-error-callout" src/styles.css` 输出 >= 1。
    - `grep -c "\.aster-settings__backup-actions" src/styles.css` 输出 >= 1。
    - `grep -c "\.aster-import-conflict-list" src/styles.css` 输出 >= 1。
    - `grep -c "var(--warning)" src/styles.css` 输出 >= 2（warn callout 至少有两处引用 --warning）。
    - `grep -v "^[[:space:]]*//" src/styles.css | grep -E "#[0-9a-fA-F]{3,6}[^)\"']" | grep -c "aster-warn-callout\|aster-error-callout\|aster-settings__backup-actions\|aster-import-conflict-list"` 输出 0（Phase 26 新增类中无直接 hex 颜色值）。
    - `npm run build` 成功无 CSS 语法错误（通过 Vite 构建验证）。
  </acceptance_criteria>

  <done>4 个 Phase 26 CSS 组件类已正确新增，全部引用既有 token，build 无错误。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM 渲染 | CSS 样式类名，无用户输入，无信任边界穿越 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-CSS-01 | Tampering | src/styles.css 新增 CSS | accept | 纯静态资产，无用户数据；仅引用既有 token，无动态计算 |
| T-26-ICON-01 | Information Disclosure | DownloadIcon SVG | accept | 纯 SVG path，stroke=currentColor，ISC 授权，无外部请求 |
</threat_model>

<verification>
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 图标验证
grep -c "export function DownloadIcon" /Users/wb.chen/Documents/Project/Aster/src/components/icons.tsx
# 期望：1

# TypeScript 验证（Node 22 路径前缀）
npx tsc --noEmit
# 期望：退出码 0，无新 TS 错误

# CSS 验证
grep -c "\.aster-warn-callout" /Users/wb.chen/Documents/Project/Aster/src/styles.css
grep -c "\.aster-error-callout" /Users/wb.chen/Documents/Project/Aster/src/styles.css
# 期望：>=1 各

# 构建验证（无新 hex 颜色引入）
npm run build
# 期望：0 errors
```
</verification>

<success_criteria>
- DownloadIcon 导出存在，有 size prop，SVG path 语义为"下载到本地"（M12 3v12 + M7 10l5 5 5-5 + M5 19h14）。
- DownloadIcon TypeScript 类型正确：`npx tsc --noEmit` 退出码 0（使用 Node 22 PATH 前缀）。
- 4 个 CSS 类全部存在于 styles.css，无直接 hex 颜色字面量，全部引用 var(--*)。
- `npm run build` 无错误。
</success_criteria>

<output>
完成后创建 `.planning/phases/26-config-import-export/26-02-SUMMARY.md`
</output>

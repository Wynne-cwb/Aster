---
phase: 12-ui-e
plan: "02"
type: execute
wave: 1
depends_on: ["12-00"]
files_modified:
  - src/store/chat.ts
  - src/agent/loop-helpers.ts
  - index.html
autonomous: true
requirements:
  - UI-05
  - UI-06

must_haves:
  truths:
    - "Message 接口含可选字段 kind?: 'read' | 'write'"
    - "loop-helpers 的 pushMessage 调用把 def?.kind 写入 Message（零额外查表）"
    - "index.html #root 含 CSS shimmer 骨架屏 HTML + 内联 style"
    - "骨架屏内联 CSS 含 prefers-reduced-motion + prefers-color-scheme:dark 分支"
    - "npm run build && npm run size 通过 ≤82KB"
  artifacts:
    - path: "src/store/chat.ts"
      provides: "Message.kind 可选字段"
      contains: "kind?: 'read' | 'write'"
    - path: "src/agent/loop-helpers.ts"
      provides: "push tool 消息时 kind 写入"
      contains: "kind: def?.kind"
    - path: "index.html"
      provides: "首屏 CSS shimmer 骨架屏"
      contains: "sk-shimmer"
  key_links:
    - from: "src/agent/loop-helpers.ts"
      to: "src/store/chat.ts Message.kind"
      via: "pushMessage({ kind: def?.kind })"
      pattern: "kind: def\\?\\..kind"
    - from: "index.html #root"
      to: "React.createRoot"
      via: "React 挂载自动覆盖"
      pattern: "root-skeleton"
---

<objective>
UI-05 数据层 + UI-06 骨架屏——两个独立改动，文件无冲突，可在 Wave 1 并行。

Purpose: UI-05 的 read/write 分类需要 Message.kind 字段作为单一真相源（loop-helpers 写入，ChatStream 读取）；UI-06 骨架屏在 index.html 静态 HTML 内实现，对 JS bundle 零影响。两者均无共享文件，天然并行。
Output: Message 接口加 kind 字段；loop-helpers push 时 kind 传入；index.html 含骨架屏 HTML+内联 CSS（prefers-reduced-motion + dark 媒体查询）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ui-e/12-CONTEXT.md
@.planning/phases/12-ui-e/12-RESEARCH.md
@.planning/phases/12-ui-e/12-PATTERNS.md

<interfaces>
<!-- Message 接口当前（src/store/chat.ts L43-63）——目标是在末尾加 kind? 字段 -->
```typescript
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'tool' | 'error';
  content: string;
  isStreaming?: boolean;
  ts?: number;
  errorCode?: string;
  retryPrompt?: string;
  toolCalls?: ToolCall[];
  // 'tool' role 专用（D-08）
  toolCallId?: string;
  toolName?: string;
  toolResult?: ToolResult;
  agentRunId?: string;
  agentStep?: number;
  // 加入：
  /** UI-05：tool 消息的 read/write 分类（来自 ToolDef.kind，loop-helpers push 时写入） */
  kind?: 'read' | 'write';
}
```

<!-- loop-helpers.ts push 点（L143-152）——目标是加 kind: def?.kind -->
```typescript
// L143-152 当前状态：
const def = tools.find((t) => t.name === tc.name);
useAgentStore.getState().setPhase(def?.kind === 'write' ? 'writing' : 'reading');
// ...
chatActions().pushMessage?.({
  role: 'tool', toolCallId: tc.id, toolName: tc.name, toolResult: result,
  content: humanLabel, agentRunId: runId, agentStep: step,
} as never);

// 目标（加 kind: def?.kind 在 agentStep 之后）：
chatActions().pushMessage?.({
  role: 'tool', toolCallId: tc.id, toolName: tc.name, toolResult: result,
  content: humanLabel, agentRunId: runId, agentStep: step,
  kind: def?.kind,  // UI-05 D-14: propagate read/write kind（def 已在 L143 解析，零额外成本）
} as never);
```

<!-- index.html 当前 #root（空）-->
```html
<div id="root"></div>
```

<!-- 骨架屏目标（RESEARCH.md §UI-06，D-17/D-18/D-19）-->
<!-- APPROVED EXCEPTION (D-18)：骨架屏在 Office.onReady 前显示，styles.css + data-theme 未加载 -->
<!-- shimmer = 单色明度渐变（非品牌多色渐变），D-19 已澄清不违反「无渐变」铁律 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message.kind 字段 + loop-helpers kind 写入（UI-05 数据层，D-14）</name>
  <files>src/store/chat.ts, src/agent/loop-helpers.ts</files>
  <read_first>
    - src/store/chat.ts L43-63（Message 接口全文——确认现有可选字段约定和注释风格）
    - src/agent/loop-helpers.ts L140-160（pushMessage 调用处，确认 def 解析位置和 as never cast）
    - .planning/phases/12-ui-e/12-PATTERNS.md §chat.ts MODIFY + §loop-helpers.ts MODIFY（exact diff）
  </read_first>
  <action>
    **src/store/chat.ts 修改：**
    在 Message 接口的 `agentStep?: number;` 行之后（接口末尾，`}` 前一行），追加：
    ```typescript
      /** UI-05：tool 消息的 read/write 分类（来自 ToolDef.kind，loop-helpers push 时写入） */
      kind?: 'read' | 'write';
    ```
    注释风格与现有 `/** D-11：重试时用此 prompt 重发 */` 一致，无空行。

    **src/agent/loop-helpers.ts 修改：**
    在 pushMessage 的对象字面量（约 L149-152）中，在 `agentStep: step,` 之后、`} as never` 之前加一行：
    ```typescript
      kind: def?.kind,  // UI-05 D-14: propagate read/write kind（def 已在上方 tools.find 解析，零额外查表）
    ```
    确保保留 `} as never` cast（既有模式，不能去掉）。

    不改任何其他逻辑。def?.kind 已是 `'read' | 'write' | undefined`，类型与 Message.kind 兼容。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "kind?: 'read' | 'write'" /Users/wb.chen/Documents/Project/Aster/src/store/chat.ts 输出 1
    - grep -c "kind: def?.kind" /Users/wb.chen/Documents/Project/Aster/src/agent/loop-helpers.ts 输出 1
    - tsc --noEmit 无报错（TypeScript 类型兼容）
    - npm run test 通过（loop-helpers.test.ts 现有测试不被破坏）
  </acceptance_criteria>
  <done>Message.kind 字段已加；loop-helpers 的 pushMessage 包含 kind: def?.kind；类型编译通过</done>
</task>

<task type="auto">
  <name>Task 2: index.html 首屏 CSS shimmer 骨架屏（UI-06，D-17/D-18/D-19）</name>
  <files>index.html</files>
  <read_first>
    - index.html（整体读取：确认 `#root` 当前为空，`<body>` 内 script 标签位置）
    - .planning/phases/12-ui-e/12-RESEARCH.md §UI-06 骨架屏结构（完整 HTML + CSS 代码，含 shimmer keyframes/dark/reduced-motion）
    - .planning/phases/12-ui-e/12-PATTERNS.md §index.html MODIFY（exact HTML 模式）
    - src/main.tsx L50-94（确认 Office.onReady + createRoot 时机——骨架被 React 挂载自动覆盖）
  </read_first>
  <action>
    修改 index.html，将 `<div id="root"></div>` 替换为带骨架屏内容的版本。

    骨架屏 HTML 插在 `<div id="root">` 和 `</div>` 之间。`<script type="module" src="/src/main.tsx">` 保持在 `</body>` 前不变。

    ```html
    <div id="root">
      <!-- UI-06 shimmer 骨架屏：仅在 Office.onReady 前显示，React.createRoot 挂载后自动覆盖 -->
      <!-- APPROVED EXCEPTION (D-18): 此处硬编码 hex 是全项目「禁硬编码 hex」的唯一被批准例外——
           styles.css 和 data-theme 在 Office.onReady 完成前均未加载，CSS 变量不可用。
           骨架屏仅活几百 ms，中性灰近似可接受（宿主主题 vs prefers-color-scheme 可能不一致，D-18 已认可）。
           shimmer 为单色明度渐变（非品牌多色渐变），D-19 已澄清不违反「无渐变」铁律。 -->
      <style>
        #root-skeleton {
          display: flex;
          flex-direction: column;
          padding: 12px;
          gap: 8px;
          background: #ffffff;
          height: 100vh;
          box-sizing: border-box;
        }
        .sk-header {
          height: 40px;
          border-radius: 8px;
          background: #f3f2ee;
          background-size: 200% 100%;
          background-image: linear-gradient(90deg, #f3f2ee 25%, #e9e7e0 50%, #f3f2ee 75%);
          animation: sk-shimmer 1.4s ease-in-out infinite;
        }
        .sk-bubble {
          height: 56px;
          border-radius: 12px;
          max-width: 85%;
          background: #eeeef0;
          background-size: 200% 100%;
          background-image: linear-gradient(90deg, #eeeef0 25%, #e0e0e3 50%, #eeeef0 75%);
          animation: sk-shimmer 1.4s ease-in-out infinite;
        }
        .sk-bubble:nth-child(2) { animation-delay: 0.2s; }
        .sk-bubble:nth-child(3) { animation-delay: 0.4s; max-width: 60%; }
        @keyframes sk-shimmer {
          0% { background-position: 200% 0; }
          100% { background-position: -200% 0; }
        }
        @media (prefers-color-scheme: dark) {
          #root-skeleton { background: #0e0e10; }
          .sk-header {
            background: #1f1f21;
            background-image: linear-gradient(90deg, #1f1f21 25%, #28282b 50%, #1f1f21 75%);
          }
          .sk-bubble {
            background: #1f1f23;
            background-image: linear-gradient(90deg, #1f1f23 25%, #2a2a2e 50%, #1f1f23 75%);
          }
        }
        @media (prefers-reduced-motion: reduce) {
          .sk-header, .sk-bubble { animation: none !important; }
        }
      </style>
      <div id="root-skeleton">
        <div class="sk-header"></div>
        <div class="sk-bubble"></div>
        <div class="sk-bubble"></div>
        <div class="sk-bubble"></div>
      </div>
    </div>
    ```

    注意：
    - sk-header 高度 40px（对应 AgentControlBar/标题区高度）
    - sk-bubble 高度 56px、圆角 12px（贴近真实 AI 气泡）
    - React.createRoot(document.getElementById('root')).render() 会覆盖 #root 所有子节点，无需 JS 手动移除（D-19 硬约束）
    - prefers-reduced-motion 必须在内联 `<style>` 内——styles.css 尚未加载（D-19 硬约束）
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run build 2>&1 | tail -10 && grep -c "sk-shimmer" dist/index.html && grep -c "prefers-reduced-motion" dist/index.html && npm run size 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "sk-shimmer" /Users/wb.chen/Documents/Project/Aster/index.html 输出 1
    - grep -c "root-skeleton" /Users/wb.chen/Documents/Project/Aster/index.html 输出 1
    - grep -c "prefers-reduced-motion" /Users/wb.chen/Documents/Project/Aster/index.html 输出 1
    - grep -c "prefers-color-scheme: dark" /Users/wb.chen/Documents/Project/Aster/index.html 输出 1
    - grep -c "APPROVED EXCEPTION.*D-18" /Users/wb.chen/Documents/Project/Aster/index.html 输出 1（注释存在）
    - npm run build 成功无报错
    - grep -c "sk-shimmer" /Users/wb.chen/Documents/Project/Aster/dist/index.html 输出 ≥1（骨架屏进 dist）
    - npm run size PASS（initial JS ≤82KB，骨架屏在 HTML 不计入 JS bundle）
  </acceptance_criteria>
  <done>index.html #root 含骨架屏 HTML + 内联 CSS（shimmer keyframes + dark + reduced-motion）；build + size 全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 骨架屏内联 CSS | 骨架屏在 JS 未执行前显示——无 LLM 输入，无动态内容，无攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | Message.kind 字段 | accept | kind 字段来自 ToolDef.kind（代码常量），非用户输入；LLM 无法修改工具注册表；低风险 |
| T-12-05 | — | UI-06 骨架屏（纯 CSS/HTML） | accept | 无 LLM 输入，无新攻击面；纯静态内联 CSS，不接受外部数据 |
| T-12-06 | Information Disclosure | index.html 骨架屏硬编码灰值 | accept | 灰值为中性颜色（无 API Key、无用户数据）；D-18 已批准例外；低风险 |
</threat_model>

<verification>
Plan 02 完成检验：

1. `grep -c "kind?: 'read' | 'write'" src/store/chat.ts`——输出 1
2. `grep -c "kind: def?.kind" src/agent/loop-helpers.ts`——输出 1
3. `tsc --noEmit`——无报错
4. `grep -c "sk-shimmer" index.html`——输出 1
5. `grep -c "prefers-reduced-motion" index.html`——输出 1
6. `npm run build && npm run size`——PASS ≤82KB
7. `npm run test`——全绿（包括 loop-helpers.test.ts 现有用例）
</verification>

<success_criteria>
- Message 接口 kind 字段已加（可选，类型正确）
- loop-helpers 的 pushMessage 包含 kind: def?.kind（零额外查表，def 已在上方解析）
- index.html 骨架屏存在，prefers-reduced-motion + dark 媒体查询均在内联 style 内
- npm run build + npm run size PASS（bundle ≤82KB 未增长）
- D-18 注释存在（唯一批准例外明确标注）
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-e/12-02-SUMMARY.md`
</output>

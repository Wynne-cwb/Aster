---
phase: 12-ui-e
plan: "03"
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - src/components/ChatStream.tsx
  - src/styles.css
autonomous: true
requirements:
  - UI-02
  - UI-04
  - UI-05

must_haves:
  truths:
    - "发消息后、首 token 到达前，消息流显示三点跳动 .bubble-typing 气泡"
    - "首 token 到达后 .bubble-typing 消失，正常流式气泡接管"
    - "read 工具卡含 .aster-tool-card--read 修饰类（去边框 + --text-3 字色）"
    - "write 工具卡无 .aster-tool-card--read 类（保持不变）"
    - "全 read 合并组含 .tool-group--read 类；混合组不含"
    - "Markdown 表格在 .bubble-ai 内有可见边框（border-collapse + cell border）"
    - "ChatStream.test.tsx 的 UI-02 和 UI-05 测试全部 GREEN"
  artifacts:
    - path: "src/components/ChatStream.tsx"
      provides: "showTyping 渲染逻辑 + read-card class 修饰"
      contains: "bubble-typing"
    - path: "src/styles.css"
      provides: ".bubble-typing 动画 + .bubble-ai table 规则 + .aster-tool-card--read 降权规则"
      contains: "aster-typing"
  key_links:
    - from: "src/components/ChatStream.tsx showTyping 逻辑"
      to: "src/agent/agentStore.ts agentStatus/currentRunId"
      via: "useAgentStore selector"
      pattern: "useAgentStore.*agentStatus"
    - from: "src/components/ChatStream.tsx ToolResultCard"
      to: "src/store/chat.ts Message.kind"
      via: "message.kind === 'read'"
      pattern: "message\\.kind.*read"
    - from: "src/styles.css .bubble-typing"
      to: "src/components/ChatStream.tsx .bubble-typing JSX"
      via: "className 绑定"
      pattern: "bubble-typing"
---

<objective>
UI-02 思考气泡 + UI-04 Markdown 表格 CSS + UI-05 read 卡 UI 层——三项改动集中在 ChatStream.tsx 和 styles.css，文件已在 Wave 2 独占，可安全合并到一个 plan。

Purpose: UI-02 填补「消息发出到首 token」空窗期；UI-04 给 LLM 回复中的表格加可见边框；UI-05 在 ChatStream/ToolResultCard 层读 Message.kind（Wave 1 已写入）并加修饰 class。
Output: ChatStream.tsx 含 showTyping 渲染 + kind-based class；styles.css 含三点动画 + 表格规则 + read 卡降权规则。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ui-e/12-CONTEXT.md
@.planning/phases/12-ui-e/12-RESEARCH.md
@.planning/phases/12-ui-e/12-PATTERNS.md
@.planning/phases/12-ui-e/12-02-SUMMARY.md

<interfaces>
<!-- ChatStream.tsx 现有 Zustand 选择器用法（L272-274）——新增选择器遵循同模式 -->
```typescript
const messages = useMessages();
const retryMessage = useChatStore((s) => s.retryMessage);
const completedRunIds = useCompletedRunIds();
// 新增（UI-02）：
const agentStatus = useAgentStore((s) => s.agentStatus);
const currentRunId = useAgentStore((s) => s.currentRunId);
```

<!-- UI-02 showTyping 判定逻辑（RESEARCH.md §UI-02）-->
```typescript
// 当 agentStatus===running/paused 且当前 run 最后一条 assistant 消息 isStreaming===true && content==='' 时显示
const lastAssistantInRun = currentRunId
  ? [...messages].reverse().find(
      (m) => m.role === 'assistant' && m.agentRunId === currentRunId
    )
  : undefined;
const showTyping =
  (agentStatus === 'running' || agentStatus === 'paused') &&
  lastAssistantInRun !== undefined &&
  lastAssistantInRun.isStreaming === true &&
  lastAssistantInRun.content.trim() === '';
```

<!-- UI-02 思考气泡 JSX（PATTERNS.md §ChatStream.tsx MODIFY）-->
```tsx
{showTyping && (
  <div className="msg msg-ai">
    <div className="bubble bubble-ai bubble-typing" aria-label="正在思考" role="status">
      <span className="bubble-typing__dot" aria-hidden="true" />
      <span className="bubble-typing__dot" aria-hidden="true" />
      <span className="bubble-typing__dot" aria-hidden="true" />
    </div>
  </div>
)}
```

<!-- UI-05 cardClass 模式（PATTERNS.md §ChatStream.tsx MODIFY L180）-->
```typescript
// 现有（L180）：
const cardClass = `aster-tool-card${isError ? ' aster-tool-card--error' : ''}`;
// 改为（UI-05）：
const cardClass = `aster-tool-card${isError ? ' aster-tool-card--error' : ''}${message.kind === 'read' ? ' aster-tool-card--read' : ''}`;
```

<!-- UI-05 MergedToolGroup allRead 模式（PATTERNS.md）-->
```typescript
// 在 MergedToolGroup 函数内（L225+）：
const allRead = messages.every((m) => m.kind === 'read');
const groupClass = `tool-group${allRead ? ' tool-group--read' : ''}`;
// 替换 <div className="tool-group"> 为 <div className={groupClass}>
```

<!-- Message 接口（Wave 1 已加 kind 字段）-->
```typescript
export interface Message {
  // ...
  kind?: 'read' | 'write';  // UI-05（Wave 1/12-02 已加）
}
```

<!-- CSS 变量（src/styles.css L44-101，UI-03/04/05 使用）-->
```css
/* semantic tokens */
--text-3: /* #92908a(light) / #6e6e76(dark) */  /* read 卡字色、三点颜色 */
--border: /* #e7e5df(light) / #26262a(dark) */   /* 表格 cell border */
--surface-2: /* #f3f2ee(light) / #1f1f21(dark) */ /* 表头底色 */
--space-1: 4px;  --space-2: 8px;
--fs-13: 13px;
--radius-full: 999px;
--radius-1: 4px;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatStream.tsx 改造——showTyping + read-card class（UI-02 + UI-05）</name>
  <files>src/components/ChatStream.tsx</files>
  <read_first>
    - src/components/ChatStream.tsx L265-290（现有 Zustand 选择器 + hooks——在此之后加 UI-02 选择器）
    - src/components/ChatStream.tsx L176-200（ToolResultCard 的 cardClass 构建——UI-05 在此改）
    - src/components/ChatStream.tsx L225-265（MergedToolGroup——UI-05 加 allRead 逻辑在此改）
    - src/components/ChatStream.tsx L395-410（return 内 {nodes} 之后——UI-02 typing JSX 插在此）
    - .planning/phases/12-ui-e/12-PATTERNS.md §ChatStream.tsx MODIFY（所有 4 处改动的 exact 代码）
  </read_first>
  <behavior>
    - agentStatus=running + 当前 run 有 content='' + isStreaming=true 的 assistant 消息 → DOM 存在 .bubble-typing
    - agentStatus=running + content 非空 → DOM 无 .bubble-typing（showTyping=false）
    - agentStatus=idle → DOM 无 .bubble-typing
    - kind='read' 的 tool 消息 → cardClass 含 aster-tool-card--read
    - kind='write' 的 tool 消息 → cardClass 不含 aster-tool-card--read
    - 全 read 合并组 → groupClass = 'tool-group tool-group--read'
    - 含 write 的合并组 → groupClass = 'tool-group'（不加 --read）
  </behavior>
  <action>
    ChatStream.tsx 需要 4 处修改（按顺序）：

    **改动 1（UI-02）：** 在现有 hooks/selectors 块（约 L272-274 completedRunIds 之后）追加两个 Zustand 选择器：
    ```typescript
    const agentStatus = useAgentStore((s) => s.agentStatus);
    const currentRunId = useAgentStore((s) => s.currentRunId);
    ```

    **改动 2（UI-02）：** 在 `return (` 前（即 `const nodes: ReactElement[] = [];` 之前）计算 showTyping：
    ```typescript
    // UI-02：思考气泡——当前 run 有空 content streaming assistant 消息时显示
    const lastAssistantInRun = currentRunId
      ? [...messages].reverse().find(
          (m) => m.role === 'assistant' && m.agentRunId === currentRunId
        )
      : undefined;
    const showTyping =
      (agentStatus === 'running' || agentStatus === 'paused') &&
      lastAssistantInRun !== undefined &&
      lastAssistantInRun.isStreaming === true &&
      lastAssistantInRun.content.trim() === '';
    ```

    **改动 3（UI-05 ToolResultCard）：** 在 ToolResultCard 函数内（约 L180），将：
    `const cardClass = \`aster-tool-card\${isError ? ' aster-tool-card--error' : ''}\`;`
    改为：
    `const cardClass = \`aster-tool-card\${isError ? ' aster-tool-card--error' : ''}\${message.kind === 'read' ? ' aster-tool-card--read' : ''}\`;`

    **改动 4（UI-05 MergedToolGroup）：** 在 MergedToolGroup 函数内（L225+），在 return 之前加：
    ```typescript
    const allRead = messages.every((m) => m.kind === 'read');
    const groupClass = `tool-group${allRead ? ' tool-group--read' : ''}`;
    ```
    将 `<div className="tool-group">` 改为 `<div className={groupClass}>`。

    **改动 5（UI-02 JSX）：** 在 return 的 `{nodes}` 之后、`{completedRunIds.map...}` 之前，插入思考气泡 JSX：
    ```tsx
    {/* UI-02：思考气泡——首 token 前空窗期占位（D-06/D-07） */}
    {showTyping && (
      <div className="msg msg-ai">
        <div className="bubble bubble-ai bubble-typing" aria-label="正在思考" role="status">
          <span className="bubble-typing__dot" aria-hidden="true" />
          <span className="bubble-typing__dot" aria-hidden="true" />
          <span className="bubble-typing__dot" aria-hidden="true" />
        </div>
      </div>
    )}
    ```

    关键约束（D-05）：agentStatus 严格用 `=== 'running' || === 'paused'`，不用 `!== 'idle'`（soft-landing 不触发思考气泡）。
    关键约束（D-06）：ChatBubble 的空 content `return null` 行为不改（思考气泡在 ChatStream 层单独渲染）。
    关键约束（D-15）：混合合并组「任一 write 即整组正常」——`messages.every((m) => m.kind === 'read')` 正确实现此语义。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/components/ChatStream.test.tsx -t "UI-02|UI-05" --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "bubble-typing" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 ≥2（定义 + JSX 引用）
    - grep -c "showTyping" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 ≥2（计算 + 使用）
    - grep -c "aster-tool-card--read" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 ≥1
    - grep -c "tool-group--read" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 ≥1
    - vitest run src/components/ChatStream.test.tsx -t "UI-02-A" PASS（agentStatus=running + 空 content → .bubble-typing 出现）
    - vitest run src/components/ChatStream.test.tsx -t "UI-02-B" PASS（content 非空 → 无 .bubble-typing）
    - vitest run src/components/ChatStream.test.tsx -t "UI-02-C" PASS（idle → 无 .bubble-typing）
    - vitest run src/components/ChatStream.test.tsx -t "UI-05-A" PASS（read card 含 --read 类）
    - vitest run src/components/ChatStream.test.tsx -t "UI-05-B" PASS（write card 无 --read 类）
    - npm run test（tsc + vitest）全绿
  </acceptance_criteria>
  <done>ChatStream.tsx 含 showTyping 渲染（5 个 UI-02 子改动）+ read-card class 修饰（UI-05）；Wave 0 预建的 UI-02/05 测试全部 GREEN</done>
</task>

<task type="auto">
  <name>Task 2: styles.css 三处追加——typing 动画 + 表格规则 + read 卡降权（UI-02 + UI-04 + UI-05）</name>
  <files>src/styles.css</files>
  <read_first>
    - src/styles.css L44-110（CSS 变量定义——确认 --text-3/--border/--surface-2/--fs-13/--space-2 的精确名称）
    - src/styles.css L721-763（.bubble-ai 系列 + @keyframes blink——UI-02 typing 动画插在 blink 之后，UI-04 表格规则插在 bubble-ai 内）
    - src/styles.css L455-530 或附近（.aster-tool-card/.tool-group 段——UI-05 read 变体插在此段末尾）
    - .planning/phases/12-ui-e/12-PATTERNS.md §styles.css MODIFY（所有 3 处 CSS 代码块）
  </read_first>
  <action>
    styles.css 三处追加（插入位置基于现有结构，全部用 `/* === Section === */` 节标题）：

    **追加 1（UI-02，typing 动画）：** 在 `@keyframes blink` 块之后插入：
    ```css
    /* === UI-02 思考气泡三点动画（D-07）=== */
    .bubble-typing {
      display: flex;
      align-items: center;
      gap: var(--space-1);
      min-height: 36px;
    }
    .bubble-typing__dot {
      width: 4px;
      height: 4px;
      border-radius: var(--radius-full);
      background: var(--text-3);
      animation: aster-typing 0.96s ease-in-out infinite;
      flex-shrink: 0;
    }
    .bubble-typing__dot:nth-child(2) { animation-delay: 0.16s; }
    .bubble-typing__dot:nth-child(3) { animation-delay: 0.32s; }
    @keyframes aster-typing {
      0%, 100% { transform: translateY(0); opacity: 0.4; }
      50%       { transform: translateY(-4px); opacity: 1; }
    }
    @media (prefers-reduced-motion: reduce) {
      .bubble-typing__dot { animation: none; opacity: 0.5; }
    }
    ```

    **追加 2（UI-04，表格规则）：** 在 `.bubble-ai` 系列规则块末尾（最后一个 `.bubble-ai` 相关规则之后）插入：
    ```css
    /* === UI-04 Markdown 表格样式（D-11，数值标[待复核]）=== */
    .bubble-ai table {
      display: block;
      overflow-x: auto;
      border-collapse: collapse;
      width: 100%;
      font-size: var(--fs-13);
      margin: var(--space-2) 0;
    }
    .bubble-ai th,
    .bubble-ai td {
      border: 1px solid var(--border);
      padding: 6px 8px;
      text-align: left;
    }
    .bubble-ai th {
      background: var(--surface-2);
      font-weight: 600;
    }
    ```

    注意：`display: block + overflow-x: auto` 让宽表在 350px task pane 内可横向滚动；`border-collapse: collapse` 在 block 模式下仍然生效（现代浏览器支持）。

    **追加 3（UI-05，read 卡降权）：** 在 `.aster-tool-card` 或 `.tool-group` 相关 CSS 段末尾插入：
    ```css
    /* === UI-05 读取工具卡降权（D-15）——write 卡保持不变 === */
    /* 单卡 read 变体：去边框 + --text-3 字色（降权，但保留 chevron 可展开 D-15）*/
    .aster-tool-card--read {
      border: none;
    }
    .aster-tool-card--read .wb-action-head {
      color: var(--text-3);
      padding: 4px 8px;
    }
    /* MergedToolGroup 全 read 变体：去组边框 + 头部字色降权 */
    .tool-group--read {
      border: none;
    }
    .tool-group--read .tool-group__head {
      border-bottom: none;
      color: var(--text-3);
    }
    .tool-group--read .tool-group__list > li {
      border-bottom-color: transparent;
    }
    ```

    硬约束（D-13）：所有颜色走 CSS 变量，无散落 hex。
    硬约束（设计系统）：无 backdrop-filter，无多色渐变，无 emoji。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "aster-typing" /Users/wb.chen/Documents/Project/Aster/src/styles.css 输出 ≥1（@keyframes 存在）
    - grep -c "bubble-typing" /Users/wb.chen/Documents/Project/Aster/src/styles.css 输出 ≥2（类名定义）
    - grep -c "bubble-ai table" /Users/wb.chen/Documents/Project/Aster/src/styles.css 输出 ≥1
    - grep -c "border-collapse: collapse" /Users/wb.chen/Documents/Project/Aster/src/styles.css 输出 ≥1
    - grep -c "aster-tool-card--read" /Users/wb.chen/Documents/Project/Aster/src/styles.css 输出 ≥1
    - grep -c "tool-group--read" /Users/wb.chen/Documents/Project/Aster/src/styles.css 输出 ≥1
    - grep -v "^/\*\|^\s*/\*" /Users/wb.chen/Documents/Project/Aster/src/styles.css | grep -c "#[0-9a-fA-F]\{3,6\}" 输出 0（无新增散落 hex，原有的 hex 不算）
    - npm run build PASS；npm run size PASS（≤82KB）
  </acceptance_criteria>
  <done>styles.css 三处追加完毕——typing 动画（UI-02）、表格规则（UI-04）、read 卡降权（UI-05）；build + size 通过；无散落 hex</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增攻击面 | 本 plan 为纯 CSS 规则 + 现有状态读取 + 条件渲染，无新输入点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | — | UI-02 思考气泡渲染（条件渲染） | accept | 纯状态读取 + CSS，无 LLM 输入；soft-landing 时不触发（严格 agentStatus 判定）；无新攻击面 |
| T-12-08 | — | UI-04 表格 CSS（纯样式） | accept | 纯 CSS 改动，无 JS 逻辑，无新输入点；无 LLM 输入路径 |
| T-12-09 | — | UI-05 read 卡修饰类（CSS + className） | accept | className 基于 Message.kind（来自工具注册表常量，非 LLM 输入）；无新攻击面 |
</threat_model>

<verification>
Plan 03 完成检验：

1. `vitest run src/components/ChatStream.test.tsx -t "UI-02"` —— 3 个 UI-02 测试全 PASS
2. `vitest run src/components/ChatStream.test.tsx -t "UI-05"` —— 2 个 UI-05 测试全 PASS
3. `grep -c "aster-typing" src/styles.css` —— ≥1
4. `grep -c "bubble-ai table" src/styles.css` —— ≥1
5. `grep -c "aster-tool-card--read" src/styles.css` —— ≥1
6. `npm run test` —— tsc + vitest 全绿
7. `npm run build && npm run size` —— PASS ≤82KB
</verification>

<success_criteria>
- UI-02 思考气泡：showTyping 逻辑 + JSX + CSS 动画全部就位；Wave 0 预建 UI-02 测试全 GREEN
- UI-04 表格 CSS：.bubble-ai table 规则含 border-collapse + cell border + 表头 --surface-2
- UI-05 read 卡：ToolResultCard + MergedToolGroup 加 kind-based class；styles.css 降权规则就位；Wave 0 UI-05 测试全 GREEN
- npm test 全绿；npm run build + size PASS
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-e/12-03-SUMMARY.md`
</output>

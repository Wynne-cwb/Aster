---
phase: 12-ui-e
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/utils/safeUrlTransform.ts
  - src/utils/safeUrlTransform.test.ts
  - src/components/ChatBubble.test.tsx
  - src/components/ChatStream.test.tsx
  - src/agent/loop-helpers.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-05

must_haves:
  truths:
    - "safeUrlTransform 函数存在且被 RED 测试调用"
    - "safeUrlTransform.test.ts 的 javascript:/data:/vbscript: 测试失败（RED 状态，实现待 Wave 1）"
    - "ChatBubble.test.tsx 存在且包含 DOM 级别断言（RED 状态）"
    - "ChatStream.test.tsx 扩展包含 UI-02/03/05 测试桩（RED 状态）"
    - "loop-helpers.test.ts 扩展包含 kind 字段测试桩（RED 状态）"
  artifacts:
    - path: "src/utils/safeUrlTransform.ts"
      provides: "safeUrlTransform 实现（Wave 1 将通过 RED→GREEN）"
      exports: ["safeUrlTransform"]
    - path: "src/utils/safeUrlTransform.test.ts"
      provides: "UI-01 纯 unit 测试（5 个用例，RED→GREEN 随 Wave 1）"
    - path: "src/components/ChatBubble.test.tsx"
      provides: "UI-01 DOM 级别断言（5 个用例，RED→GREEN 随 Wave 1）"
    - path: "src/components/ChatStream.test.tsx"
      provides: "UI-02/03/05 扩展测试（追加，RED 状态）"
    - path: "src/agent/loop-helpers.test.ts"
      provides: "UI-05 kind 字段测试（追加，RED 状态）"
  key_links:
    - from: "src/utils/safeUrlTransform.test.ts"
      to: "src/utils/safeUrlTransform.ts"
      via: "直接 import"
      pattern: "from './safeUrlTransform'"
    - from: "src/components/ChatBubble.test.tsx"
      to: "src/components/ChatBubble.tsx"
      via: "@testing-library/react render"
      pattern: "render.*ChatBubble"
---

<objective>
Wave 0 测试桩——在所有实现代码之前建立 RED 测试框架。

Purpose: Nyquist 守则要求。UI-01 测试必须 RED→GREEN（P0 安全门）；UI-02/03/05 测试在实现所在 plan 之前预先存在，确保每个 plan 可独立验证。safeUrlTransform.ts 与测试文件在同一 Wave 0 交付——实现与测试在 Wave 0 同步存在（Wave 0 = safeUrlTransform 的 RED→GREEN 起点，Chain plan 01 wire 进 ChatBubble 完成最终 GREEN）。
Output: 5 个测试文件（2 新建 + 3 扩展），safeUrlTransform.ts 实现文件。所有 UI-01 测试在 Wave 0 末尾 GREEN；UI-02/03/05 测试在 Wave 0 末尾 RED（因实现在后续 waves）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-e/12-CONTEXT.md
@.planning/phases/12-ui-e/12-RESEARCH.md
@.planning/phases/12-ui-e/12-PATTERNS.md
@.planning/phases/12-ui-e/12-VALIDATION.md

<interfaces>
<!-- Message 接口（当前，Wave 1 将加 kind? 字段）-->
<!-- From src/store/chat.ts L43-63 -->
```typescript
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'tool' | 'error';
  content: string;
  isStreaming?: boolean;
  ts?: number;
  errorCode?: string;
  retryPrompt?: string;
  toolCalls?: ToolCall[];
  toolCallId?: string;
  toolName?: string;
  toolResult?: ToolResult;
  agentRunId?: string;
  agentStep?: number;
}
```

<!-- ChatBubble props（src/components/ChatBubble.tsx L31-35）-->
```typescript
interface ChatBubbleProps {
  message: Message;
  onRetry: () => void;
  onSettings: (anchor?: string) => void;
}
```

<!-- AgentStatus（src/agent/agentStore.ts L26）-->
```typescript
export type AgentStatus = 'idle' | 'running' | 'paused' | 'soft-landing';
```

<!-- Lingui mock（必须用于所有涉及 Trans 的组件测试，来自 ChatStream.test.tsx L37-44）-->
```typescript
vi.mock('@lingui/react/macro', () => ({
  Trans: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  useLingui: () => ({ _: (id: string) => id, t: (id: string) => id }),
}));
```

<!-- Store reset 模式（来自 ChatStream.test.tsx L127-131）-->
```typescript
beforeEach(() => {
  useChatStore.setState({ messages: [] });
  useAgentStore.setState({
    agentStatus: 'idle', currentStep: 0, currentRunId: null,
    controller: null, lastAbortReason: null, runningTools: [],
  });
  vi.clearAllMocks();
});
afterEach(() => { vi.restoreAllMocks(); });
```

<!-- safeUrlTransform 签名（Wave 0 在此文件中实现）-->
```typescript
// src/utils/safeUrlTransform.ts
const SAFE_PROTOCOLS = new Set(['http:', 'https:', 'mailto:']);
export function safeUrlTransform(url: string): string;
// 白名单放行：http/https/mailto/相对路径/#锚点/协议相对 URL
// 危险协议（javascript:/data:/vbscript:）→ 返回 '' 空串（不返回 null）
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 safeUrlTransform.ts + safeUrlTransform.test.ts（UI-01 RED→GREEN）</name>
  <files>src/utils/safeUrlTransform.ts, src/utils/safeUrlTransform.test.ts</files>
  <read_first>
    - src/utils/formatTime.ts（analog 文件：pure util 结构、JSDoc 风格）
    - src/agent/loop-helpers.test.ts L1-30（analog 测试结构：vitest imports 模式、describe/it 格式）
    - .planning/phases/12-ui-e/12-RESEARCH.md §UI-01 Architecture Patterns（exact 函数实现代码）
    - .planning/phases/12-ui-e/12-PATTERNS.md §safeUrlTransform.ts 和 §safeUrlTransform.test.ts（完整 pattern）
  </read_first>
  <behavior>
    - safeUrlTransform('javascript:alert(1)') → ''（拦截）
    - safeUrlTransform('data:text/html;base64,abc') → ''（拦截）
    - safeUrlTransform('vbscript:msgbox(1)') → ''（拦截）
    - safeUrlTransform('https://example.com') → 'https://example.com'（放行）
    - safeUrlTransform('#section') → '#section'（放行锚点）
    - safeUrlTransform('/path') → '/path'（放行相对路径）
    - safeUrlTransform('//cdn.example.com/file.js') → '//cdn.example.com/file.js'（放行协议相对 URL）
    - safeUrlTransform('mailto:user@example.com') → 'mailto:user@example.com'（放行）
  </behavior>
  <action>
    创建 src/utils/safeUrlTransform.ts（遵循 formatTime.ts 文件结构：JSDoc 块 + pure 函数，无 imports）：

    ```typescript
    /**
     * src/utils/safeUrlTransform.ts
     * react-markdown urlTransform 回调——白名单放行，拦截危险协议（UI-01，D-03）。
     * 签名与 react-markdown@9.1.0 UrlTransform 类型兼容：(url: string, key: string, node: Element) => string
     * 实际只用 url 参数（key/node 忽略），便于 tree-shaking 和单测。
     *
     * 危险协议（javascript:/data:/vbscript:/file:）→ 返回 '' → react-markdown 把属性设为 '' → 链接退化为无 href 纯文本。
     * NOTE: 必须返回 '' 而非 null——null 会被序列化为 "null" 字符串出现在 href 属性中。
     *
     * CVE-2025-24981 同类防御：LLM 生成含 javascript: href 的 Markdown 链接，
     * 用户点击时在 TaskPane webview 执行任意 JS 可读取 API Key——urlTransform 返回 '' 切断攻击链。
     */
    const SAFE_PROTOCOLS = new Set(['http:', 'https:', 'mailto:']);

    export function safeUrlTransform(url: string): string {
      if (!url) return '';
      // 相对路径、锚点、协议相对 URL → 放行（无协议前缀，安全）
      if (
        url.startsWith('#') ||
        url.startsWith('/') ||
        url.startsWith('./') ||
        url.startsWith('../') ||
        url.startsWith('//')
      ) {
        return url;
      }
      try {
        const { protocol } = new URL(url);
        return SAFE_PROTOCOLS.has(protocol) ? url : '';
      } catch {
        // URL 解析失败（非标准格式）→ 保守放行（已排除了 # / ./ 等最常见相对路径）
        return url;
      }
    }
    ```

    创建 src/utils/safeUrlTransform.test.ts（遵循 loop-helpers.test.ts 模式，无 vi.mock，纯函数单测）：

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { safeUrlTransform } from './safeUrlTransform';

    describe('safeUrlTransform — XSS URL 防御（UI-01，D-03）', () => {
      it('UI-01-A: javascript: 协议 → 返回空串（拦截）', () => {
        expect(safeUrlTransform('javascript:alert(1)')).toBe('');
      });
      it('UI-01-B: data: URI → 返回空串（拦截）', () => {
        expect(safeUrlTransform('data:text/html;base64,PHNjcmlwdD5hbGVydCgxKTwvc2NyaXB0Pg==')).toBe('');
      });
      it('UI-01-C: vbscript: → 返回空串（拦截）', () => {
        expect(safeUrlTransform('vbscript:msgbox(1)')).toBe('');
      });
      it('UI-01-D: https: → 原样返回（放行）', () => {
        expect(safeUrlTransform('https://example.com')).toBe('https://example.com');
      });
      it('UI-01-E: 相对路径和锚点 → 原样返回（放行）', () => {
        expect(safeUrlTransform('#section')).toBe('#section');
        expect(safeUrlTransform('/path/to/page')).toBe('/path/to/page');
        expect(safeUrlTransform('./relative')).toBe('./relative');
      });
      it('UI-01-F: mailto: → 原样返回（放行）', () => {
        expect(safeUrlTransform('mailto:user@example.com')).toBe('mailto:user@example.com');
      });
      it('UI-01-G: 协议相对 URL → 原样返回（放行）', () => {
        expect(safeUrlTransform('//cdn.example.com/file.js')).toBe('//cdn.example.com/file.js');
      });
      it('UI-01-H: 空串 → 空串（放行）', () => {
        expect(safeUrlTransform('')).toBe('');
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/utils/safeUrlTransform.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export function safeUrlTransform" /Users/wb.chen/Documents/Project/Aster/src/utils/safeUrlTransform.ts 输出 1
    - grep -c "SAFE_PROTOCOLS" /Users/wb.chen/Documents/Project/Aster/src/utils/safeUrlTransform.ts 输出 1
    - vitest run src/utils/safeUrlTransform.test.ts 所有用例 PASS（8 个用例）
    - safeUrlTransform.ts 中无 import 语句（pure function，zero deps）
  </acceptance_criteria>
  <done>safeUrlTransform.ts 实现完毕并通过所有 8 个 unit 测试（GREEN）</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ChatBubble.test.tsx（UI-01 DOM 级别断言，RED 态待 Wave 1 GREEN）</name>
  <files>src/components/ChatBubble.test.tsx</files>
  <read_first>
    - src/components/ChatStream.test.tsx L1-50（完整 imports + vi.mock lingui macro 模式——MANDATORY）
    - src/components/ChatStream.test.tsx L102-135（Message fixture + renderHelper + beforeEach/afterEach 模式）
    - src/components/ChatBubble.tsx L31-45（ChatBubbleProps 接口——无 AdapterContext，比 ChatStream 更简单）
    - .planning/phases/12-ui-e/12-RESEARCH.md §Validation Architecture（DOM 断言模式代码）
    - .planning/phases/12-ui-e/12-PATTERNS.md §ChatBubble.test.tsx（exact 模式 + 渲染 helper）
  </read_first>
  <action>
    创建 src/components/ChatBubble.test.tsx。

    关键约束：
    - 必须 vi.mock('@lingui/react/macro', ...) —— ChatBubble 经 react-markdown → remark-gfm → 依赖链涉及 Trans，不 mock 会报错
    - ChatBubble 不需要 AdapterContext（无 useAdapter()），render 更简单
    - DOM 断言用 container.querySelector，不依赖 screen（href 属性不在 accessible role 内）
    - 这些测试在 Wave 0 末尾应为 RED（ChatBubble 尚无 urlTransform prop）

    ```typescript
    /**
     * src/components/ChatBubble.test.tsx
     * UI-01 DOM 级别安全断言——验证 ChatBubble 渲染后 DOM 不含危险 href/src（Phase 12 plan 00）
     * RED 状态：Wave 0 建立测试框架；Wave 1（12-01-PLAN）wire urlTransform 后变 GREEN。
     */
    import React from 'react';
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import { render } from '@testing-library/react';
    import ChatBubble from './ChatBubble';
    import type { Message } from '../store/chat';

    // MANDATORY：ChatBubble 经 remark-gfm 依赖链引用 Trans，测试环境必须 mock
    vi.mock('@lingui/react/macro', () => ({
      Trans: ({ children }: { children: React.ReactNode }) => <>{children}</>,
      useLingui: () => ({ _: (id: string) => id, t: (id: string) => id }),
    }));

    function makeMsg(content: string): Message {
      return { id: 'test-1', role: 'assistant', content, isStreaming: false };
    }

    function renderBubble(content: string) {
      return render(
        <ChatBubble
          message={makeMsg(content)}
          onRetry={() => {}}
          onSettings={() => {}}
        />,
      );
    }

    describe('ChatBubble — UI-01 XSS 防御（urlTransform）', () => {
      beforeEach(() => { vi.clearAllMocks(); });
      afterEach(() => { vi.restoreAllMocks(); });

      it('UI-01-A: javascript: href → DOM anchor 无危险 href', () => {
        const { container } = renderBubble('[点我](javascript:alert(1))');
        const a = container.querySelector('a');
        // Wave 0 时此断言 RED（无 urlTransform，href 含 javascript:）
        // Wave 1 接线后变 GREEN（urlTransform 返回 ''，href 为空）
        expect(a?.getAttribute('href') ?? '').not.toMatch(/javascript:/i);
      });

      it('UI-01-B: data: URI href → 被拦截', () => {
        const { container } = renderBubble('[恶意](data:text/html;base64,PHNjcmlwdD5hbGVydCgxKTwvc2NyaXB0Pg==)');
        const a = container.querySelector('a');
        expect(a?.getAttribute('href') ?? '').not.toMatch(/^data:/i);
      });

      it('UI-01-C: https: href → 保留（不误杀）', () => {
        const { container } = renderBubble('[正常链接](https://example.com)');
        const a = container.querySelector('a');
        // 此测试在 Wave 0 应 PASS（react-markdown 默认也保留 https:）
        expect(a?.getAttribute('href')).toBe('https://example.com');
      });

      it('UI-01-D: img src javascript: → 被拦截', () => {
        const { container } = renderBubble('![图](javascript:alert(1))');
        const img = container.querySelector('img');
        // react-markdown 默认对 img src 也应用 defaultUrlTransform，但自写 allowlist 更严
        expect(img?.getAttribute('src') ?? '').not.toMatch(/javascript:/i);
      });

      it('UI-01-E: vbscript: href → 被拦截', () => {
        const { container } = renderBubble('[点我](vbscript:msgbox(1))');
        const a = container.querySelector('a');
        expect(a?.getAttribute('href') ?? '').not.toMatch(/vbscript:/i);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/components/ChatBubble.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ChatBubble.test.tsx 文件存在
    - grep -c "UI-01-A\|UI-01-B\|UI-01-C\|UI-01-D\|UI-01-E" /Users/wb.chen/Documents/Project/Aster/src/components/ChatBubble.test.tsx 输出 5
    - grep -c "vi.mock.*lingui" /Users/wb.chen/Documents/Project/Aster/src/components/ChatBubble.test.tsx 输出 1（lingui mock 存在）
    - vitest run src/components/ChatBubble.test.tsx 运行无 import 错误（即使部分 RED）；UI-01-C（https）应 PASS
    - 注意：UI-01-A/B/D/E 在 Wave 0 末尾可能 RED（ChatBubble 尚无 urlTransform），这是预期状态
  </acceptance_criteria>
  <done>ChatBubble.test.tsx 存在，5 个 UI-01 DOM 断言测试已创建；UI-01-C GREEN，其余测试 RED 态等待 Wave 1</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 ChatStream.test.tsx + loop-helpers.test.ts（UI-02/03/05 测试桩，RED 态）</name>
  <files>src/components/ChatStream.test.tsx, src/agent/loop-helpers.test.ts</files>
  <read_first>
    - src/components/ChatStream.test.tsx L1-30（现有 imports）
    - src/components/ChatStream.test.tsx L100-140（现有 fixture 函数 + beforeEach/afterEach 模式——追加用此）
    - src/agent/loop-helpers.test.ts L1-30（现有 imports + describe 结构）
    - .planning/phases/12-ui-e/12-PATTERNS.md §ChatStream.test.tsx MODIFY + §loop-helpers.test.ts MODIFY（完整追加代码）
    - src/agent/agentStore.ts L42-90（AgentState 接口——used in useAgentStore.setState）
  </read_first>
  <action>
    在 ChatStream.test.tsx 末尾追加 3 个新 describe 块（UI-02、UI-03、UI-05）。在 loop-helpers.test.ts 末尾追加 1 个 describe 块（UI-05 kind）。

    Wave 0 的这些测试全部是 RED 桩——执行时 vi.fn() 标记 TODO，断言写好但实现不在 Wave 0。

    **ChatStream.test.tsx 追加（在文件末尾，已有最后一个 describe 块之后）：**

    ```typescript
    // =========================================================
    // UI-02：思考气泡（typing indicator）
    // =========================================================
    describe('ChatStream — UI-02: 思考气泡（typing indicator）', () => {
      beforeEach(() => {
        useChatStore.setState({ messages: [] });
        useAgentStore.setState({
          agentStatus: 'idle',
          currentStep: 0,
          currentRunId: null,
          controller: null,
          lastAbortReason: null,
          runningTools: [],
          completedRunIds: [],
        } as never);
        vi.clearAllMocks();
      });
      afterEach(() => { vi.restoreAllMocks(); });

      it('UI-02-A: running + 当前 run 有空 content isStreaming assistant 消息 → .bubble-typing 出现', async () => {
        const runId = 'run-1';
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: '帮我写个总结' },
            { id: 'a1', role: 'assistant', content: '', isStreaming: true, agentRunId: runId },
          ],
        } as never);
        useAgentStore.setState({ agentStatus: 'running', currentRunId: runId } as never);
        const { container } = renderChatStream();
        // RED：Wave 2（12-03）实现 showTyping 逻辑后变 GREEN
        expect(container.querySelector('.bubble-typing')).not.toBeNull();
      });

      it('UI-02-B: 首 token 到达（content 非空）→ .bubble-typing 消失', async () => {
        const runId = 'run-1';
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: '帮我写个总结' },
            { id: 'a1', role: 'assistant', content: '这是', isStreaming: true, agentRunId: runId },
          ],
        } as never);
        useAgentStore.setState({ agentStatus: 'running', currentRunId: runId } as never);
        const { container } = renderChatStream();
        // content 非空 → showTyping 条件不满足 → 无 .bubble-typing
        expect(container.querySelector('.bubble-typing')).toBeNull();
      });

      it('UI-02-C: agentStatus idle → 无 .bubble-typing 残留', async () => {
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: '你好' },
            { id: 'a1', role: 'assistant', content: '你好！', isStreaming: false },
          ],
        } as never);
        useAgentStore.setState({ agentStatus: 'idle', currentRunId: null } as never);
        const { container } = renderChatStream();
        expect(container.querySelector('.bubble-typing')).toBeNull();
      });
    });

    // =========================================================
    // UI-03：DiffLogPanel 边界插入
    // =========================================================
    describe('ChatStream — UI-03: DiffLogPanel agentRunId 边界插入', () => {
      beforeEach(() => {
        useChatStore.setState({ messages: [] });
        useAgentStore.setState({
          agentStatus: 'idle', currentStep: 0, currentRunId: null,
          controller: null, lastAbortReason: null, runningTools: [],
          completedRunIds: [],
        } as never);
        vi.clearAllMocks();
      });
      afterEach(() => { vi.restoreAllMocks(); });

      it('UI-03-A: 多 run 时 DiffLogPanel 紧跟对应 run 最后消息之后', async () => {
        useAgentStore.setState({ completedRunIds: ['run-1', 'run-2'] } as never);
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: '操作1', agentRunId: 'run-1' },
            { id: 'a1', role: 'assistant', content: 'OK', agentRunId: 'run-1' },
            { id: 'u2', role: 'user', content: '操作2', agentRunId: 'run-2' },
            { id: 'a2', role: 'assistant', content: 'OK2', agentRunId: 'run-2' },
          ],
        } as never);
        const { container } = renderChatStream();
        // RED：Wave 3（12-04）实现边界插入后验证 DiffLogPanel 位置
        // 断言：DiffLogPanel 出现在 a1 之后（非底部）
        const nodes = container.children[0]?.children;
        expect(nodes).toBeDefined();
      });

      it('UI-03-B: 同 runId 只渲染一张 DiffLogPanel', async () => {
        useAgentStore.setState({ completedRunIds: ['run-1'] } as never);
        useChatStore.setState({
          messages: [
            { id: 'a1', role: 'assistant', content: 'OK', agentRunId: 'run-1' },
          ],
        } as never);
        const { container } = renderChatStream();
        // RED：Wave 3 实现后验证去重
        const dlpCount = container.querySelectorAll('[data-testid="diff-log-panel"]').length;
        expect(dlpCount).toBeLessThanOrEqual(1);
      });
    });

    // =========================================================
    // UI-05：read/write 工具卡修饰类
    // =========================================================
    describe('ChatStream — UI-05: read/write 工具卡修饰类', () => {
      beforeEach(() => {
        useChatStore.setState({ messages: [] });
        useAgentStore.setState({
          agentStatus: 'idle', currentStep: 0, currentRunId: null,
          controller: null, lastAbortReason: null, runningTools: [],
          completedRunIds: [],
        } as never);
        vi.clearAllMocks();
      });
      afterEach(() => { vi.restoreAllMocks(); });

      it('UI-05-A: kind=read 的 tool 消息渲染的卡含 aster-tool-card--read 类', async () => {
        useChatStore.setState({
          messages: [
            {
              id: 't1', role: 'tool', content: '读取文档',
              toolCallId: 'c1', toolName: 'read_word_content',
              toolResult: { ok: true, data: { content: '文档内容', source: 'word' } },
              agentRunId: 'r1', agentStep: 1,
              kind: 'read', // UI-05：Wave 1（12-02）loop-helpers push 后会有此字段
            },
          ],
        } as never);
        const { container } = renderChatStream();
        // RED：Wave 2（12-03）加 class 后变 GREEN
        const card = container.querySelector('.aster-tool-card');
        expect(card?.classList.contains('aster-tool-card--read')).toBe(true);
      });

      it('UI-05-B: kind=write 的 tool 消息渲染的卡不含 aster-tool-card--read 类', async () => {
        useChatStore.setState({
          messages: [
            {
              id: 't2', role: 'tool', content: '写入段落',
              toolCallId: 'c2', toolName: 'append_paragraph',
              toolResult: { ok: true },
              agentRunId: 'r1', agentStep: 1,
              kind: 'write',
            },
          ],
        } as never);
        const { container } = renderChatStream();
        const card = container.querySelector('.aster-tool-card');
        // write 卡不应有 --read 类（此测试在 Wave 0 应 PASS——当前无 kind 检查也不会有 --read 类）
        expect(card?.classList.contains('aster-tool-card--read')).toBe(false);
      });
    });
    ```

    **loop-helpers.test.ts 追加（文件末尾）：**

    在 loop-helpers.test.ts 末尾追加如下 describe 块，验证 loop-helpers push tool 消息时 kind 字段写入 Message：

    ```typescript
    // =========================================================
    // UI-05：loop-helpers push tool 消息时 kind 字段写入
    // =========================================================
    describe('loop-helpers — UI-05: kind 字段写入 Message', () => {
      it('UI-05 kind: kind 字段描述——Wave 1（12-02）实现后验证 pushMessage 收到 kind', () => {
        // 此测试为占位符——Wave 1 在 loop-helpers.ts 加入 kind: def?.kind 后
        // 需要 spy pushMessage 验证 kind 传入。
        // 详细实现属 Claude's Discretion（CONTEXT.md），具体方式在 12-02-PLAN 实现时确定。
        expect(true).toBe(true); // 占位，Wave 1 后替换为 spy 断言
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/components/ChatStream.test.tsx src/agent/loop-helpers.test.ts --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "UI-02-A\|UI-02-B\|UI-02-C" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.test.tsx 输出 3
    - grep -c "UI-03-A\|UI-03-B" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.test.tsx 输出 2
    - grep -c "UI-05-A\|UI-05-B" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.test.tsx 输出 2
    - grep -c "UI-05 kind" /Users/wb.chen/Documents/Project/Aster/src/agent/loop-helpers.test.ts 输出 1
    - vitest run src/components/ChatStream.test.tsx 无 import/syntax 报错（即使部分 RED）
    - UI-02-C（idle 无 bubble-typing）和 UI-05-B（write 卡无 --read 类）在 Wave 0 应 PASS
  </acceptance_criteria>
  <done>ChatStream.test.tsx 已追加 UI-02/03/05 测试桩（7 个），loop-helpers.test.ts 已追加 UI-05 kind 占位测试；无 import 报错</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → ChatBubble ReactMarkdown | LLM 生成的 Markdown 文本（含可能的恶意 URI）在 TaskPane webview 内渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering / Elevation of Privilege | ChatBubble.tsx `<ReactMarkdown>` `href`/`src` 属性 | mitigate | `safeUrlTransform` 白名单过滤，危险协议（javascript:/data:/vbscript:）返回 `''` → href 变空 → 无跳转（CVE-2025-24981 同类防御）。ASVS L1 V5.1.3。Wave 0 建立 RED 测试守门，Wave 1 实现后 GREEN。 |
| T-12-02 | Information Disclosure | TaskPane webview localStorage（API Key 存储） | mitigate | UI-01 safeUrlTransform 直接切断 `javascript:` 攻击链，攻击者无法通过 LLM 生成链接读取 API Key |
| T-12-03 | Spoofing | LLM prompt injection via Markdown href text | accept | react-markdown 默认不渲染 raw HTML；href 文本内容只影响属性值，不影响 DOM 结构；低风险 |
</threat_model>

<verification>
Wave 0 完成检验（按顺序）：

1. `vitest run src/utils/safeUrlTransform.test.ts`——所有 8 个用例 PASS（GREEN）
2. `vitest run src/components/ChatBubble.test.tsx`——无 import 报错；UI-01-C（https 放行）PASS；A/B/D/E 可能 RED（预期）
3. `vitest run src/components/ChatStream.test.tsx`——无 import 报错；现有测试保持 GREEN；新追加的 UI-02-C、UI-05-B 应 PASS
4. `vitest run src/agent/loop-helpers.test.ts`——无 import 报错；现有测试保持 GREEN
5. `npm run test`（= tsc + vitest run）——TypeScript 编译无报错
</verification>

<success_criteria>
- safeUrlTransform.ts 已实现，8 个 unit 测试全 GREEN
- ChatBubble.test.tsx 已创建，lingui mock 正确，无 import 报错
- ChatStream.test.tsx 已追加 7 个 UI-02/03/05 测试桩，无 import 报错
- loop-helpers.test.ts 已追加 UI-05 kind 占位测试，无 import 报错
- npm run test（tsc + vitest）无编译报错
- 现有测试（Wave 0 之前的）全部 GREEN 未被破坏
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-e/12-00-SUMMARY.md`
</output>

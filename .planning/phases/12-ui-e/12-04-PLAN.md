---
phase: 12-ui-e
plan: "04"
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - src/components/ChatStream.tsx
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "多次 agent run 后，每张 DiffLogPanel 紧跟在其 runId 的最后一条消息之后"
    - "同 runId 只渲染一张 DiffLogPanel（去重守门）"
    - "最后一条消息恰为 regularTool 时 DiffLogPanel 仍在 flushToolRun 后正确插入（边缘情况）"
    - "底部统一渲染块已移除"
    - "DiffLogPanel 保留 lazy + Suspense（NFR-05，不进初始 chunk）"
    - "ChatStream.test.tsx 的 UI-03 测试全部 GREEN"
  artifacts:
    - path: "src/components/ChatStream.tsx"
      provides: "边界插入算法（pre-loop Map 计算 + index-based 循环 + flushToolRun 后检查）"
      contains: "runLastIndex"
  key_links:
    - from: "src/components/ChatStream.tsx nodes 构建循环"
      to: "agentStore.completedRunIds"
      via: "new Set(completedRunIds)"
      pattern: "completedRunSet"
    - from: "src/components/ChatStream.tsx flushToolRun"
      to: "DiffLogPanel Suspense 节点"
      via: "runLastIndex.get(lastInRun.agentRunId) === i"
      pattern: "runLastIndex"
---

<objective>
UI-03 DiffLogPanel 边界插入——将 DiffLogPanel 从消息流底部移到对应 runId 末尾（紧跟该次 loop 回复之后）。

Purpose: 修复「多 loop 改动卡全沉底与回复脱节」的 bug（D-09）。边界插入算法在 for 循环外预先建立 runLastIndex Map，在每次 flushToolRun 后和非 tool 消息分派后检查是否命中边界，命中则插入 Suspense<DiffLogPanel>。
Output: ChatStream.tsx 的 nodes 构建重构为 index-based 循环；底部统一渲染块移除；Wave 0 预建的 UI-03 测试全部 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ui-e/12-CONTEXT.md
@.planning/phases/12-ui-e/12-RESEARCH.md
@.planning/phases/12-ui-e/12-PATTERNS.md
@.planning/phases/12-ui-e/12-03-SUMMARY.md

<interfaces>
<!-- ChatStream.tsx 当前 nodes 构建（L362-408，需重写此段）-->
```typescript
// 当前（bug：DiffLogPanel 全在底部）
const nodes: ReactElement[] = [];
let toolRun: Message[] = [];
const flushToolRun = (): void => {
  if (toolRun.length === 0) return;
  if (toolRun.length >= 2) {
    nodes.push(<MergedToolGroup key={`group-${toolRun[0].id}`} messages={toolRun} />);
  } else {
    for (const tm of toolRun) nodes.push(<ToolResultCard key={tm.id} message={tm} />);
  }
  toolRun = [];
};
for (const m of messages) {
  if (isRegularTool(m)) { toolRun.push(m); continue; }
  flushToolRun();
  if (m.role === 'tool') {
    nodes.push(<ToolResultCard key={m.id} message={m} />);
  } else {
    nodes.push(<ChatBubble key={m.id} message={m} ... />);
  }
}
flushToolRun();
// bug：底部统一渲染（L400-406）:
{completedRunIds.map((runId) => (
  <Suspense key={runId} fallback={null}><DiffLogPanel runId={runId} /></Suspense>
))}
```

<!-- UI-03 目标算法（RESEARCH.md §UI-03 + 边缘情况修复）-->
```typescript
// 目标：pre-loop Map + 修改 flushToolRun 在 flush 后检查 runId 边界
// 预处理：
const completedRunSet = new Set(completedRunIds);
const runLastIndex = new Map<string, number>();
messages.forEach((m, i) => {
  if (m.agentRunId && completedRunSet.has(m.agentRunId)) {
    runLastIndex.set(m.agentRunId, i);
  }
});
const insertedRuns = new Set<string>();

// 辅助：尝试在当前位置后插入 DiffLogPanel
const tryInsertDiffLog = (rid: string | undefined, msgIndex: number): void => {
  if (!rid) return;
  if (runLastIndex.get(rid) === msgIndex && !insertedRuns.has(rid)) {
    insertedRuns.add(rid);
    nodes.push(
      <Suspense key={`dlp-${rid}`} fallback={null}>
        <DiffLogPanel runId={rid} />
      </Suspense>
    );
  }
};

// 修改 flushToolRun 以支持边界检测（需要知道当前 loop 索引）
// 方案：改为 index-based for 循环（for i, m of messages.entries()）
// flushToolRun 改为接收参数 lastIndexInFlush: number
```

<!-- DiffLogPanel lazy 声明（ChatStream.tsx L46，保持不变）-->
```typescript
const DiffLogPanel = lazy(() => import('./DiffLogPanel'));
```

<!-- Pitfall 3（RESEARCH.md）：最后一条是 regularTool 时的边缘情况 -->
```
如果一个 run 的最后一条消息是 regularTool，它会进入 toolRun 数组，
在 flushToolRun 后才渲染——必须在 flushToolRun 内检查 toolRun 末尾消息的 runId。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatStream.tsx nodes 循环改造——边界插入算法（UI-03）</name>
  <files>src/components/ChatStream.tsx</files>
  <read_first>
    - src/components/ChatStream.tsx L360-410（完整 nodes 构建 + 底部 DiffLogPanel 渲染——全部重写此段）
    - src/components/ChatStream.tsx L35-50（lazy/Suspense 声明——DiffLogPanel lazy 保持不变）
    - .planning/phases/12-ui-e/12-RESEARCH.md §UI-03 Architecture Patterns（边界算法示意代码 + Pitfall 3 边缘情况处理）
    - .planning/phases/12-ui-e/12-PATTERNS.md §ChatStream.tsx MODIFY（pre-loop setup + checkAndInsertDiffLog 模式）
  </read_first>
  <behavior>
    - 两个 run 交替的消息序列：run1 消息 → run1 DiffLogPanel → run2 消息 → run2 DiffLogPanel（紧跟各自 run 末尾）
    - 同 runId 只渲染一张 DiffLogPanel（去重：insertedRuns Set 守门）
    - run 最后一条消息是 regularTool 时：flushToolRun 后仍正确插入 DiffLogPanel（Pitfall 3 已处理）
    - 无 agentRunId 的纯聊天消息不触发 DiffLogPanel 插入
    - DiffLogPanel 内部 writeOps=0 时自己 return null（无需外部 length 检查）
    - DiffLogPanel 保留 lazy + Suspense fallback={null}（NFR-05）
  </behavior>
  <action>
    重写 ChatStream.tsx 的 nodes 构建段（约 L362-408）。核心变化：

    1. 在 nodes/toolRun 声明之前，预计算 runLastIndex Map 和 insertedRuns Set：
    ```typescript
    // UI-03 D-10：预计算每个 completedRunId 的最后消息 index，用于边界插入
    const completedRunSet = new Set(completedRunIds);
    const runLastIndex = new Map<string, number>();
    messages.forEach((m, i) => {
      if (m.agentRunId && completedRunSet.has(m.agentRunId)) {
        runLastIndex.set(m.agentRunId, i);
      }
    });
    const insertedRuns = new Set<string>();
    ```

    2. 新建 tryInsertDiffLog 辅助函数：
    ```typescript
    const tryInsertDiffLog = (rid: string | undefined, msgIndex: number): void => {
      if (!rid) return;
      if (runLastIndex.get(rid) === msgIndex && !insertedRuns.has(rid)) {
        insertedRuns.add(rid);
        nodes.push(
          <Suspense key={`dlp-${rid}`} fallback={null}>
            <DiffLogPanel runId={rid} />
          </Suspense>
        );
      }
    };
    ```

    3. 修改 flushToolRun 使其接受 lastIdx 参数（flush 后检查 toolRun 末尾消息 runId 是否命中边界）：
    ```typescript
    const flushToolRun = (lastIdx: number): void => {
      if (toolRun.length === 0) return;
      const lastInRun = toolRun[toolRun.length - 1];
      if (toolRun.length >= 2) {
        nodes.push(<MergedToolGroup key={`group-${toolRun[0].id}`} messages={toolRun} />);
      } else {
        for (const tm of toolRun) nodes.push(<ToolResultCard key={tm.id} message={tm} />);
      }
      toolRun = [];
      // Pitfall 3（RESEARCH.md）：run 最后一条是 regularTool 时，在 flush 后检查插入点
      tryInsertDiffLog(lastInRun.agentRunId, lastIdx);
    };
    ```

    4. 将 `for (const m of messages)` 改为 index-based 循环：
    ```typescript
    for (let i = 0; i < messages.length; i++) {
      const m = messages[i];
      if (isRegularTool(m)) {
        toolRun.push(m);
        continue;
      }
      flushToolRun(i - 1); // flush 之前积累的 toolRun，lastIdx = 当前 i-1（toolRun 的 index 范围结束处）
      // 注：toolRun 内消息的 index 是连续的，lastInRun index = i-1（最后入队的那条）
      // 更精确方案：toolRun 入队时记录 index；或用 toolRunLastIdx 追踪
      if (m.role === 'tool') {
        nodes.push(<ToolResultCard key={m.id} message={m} />);
      } else {
        nodes.push(
          <ChatBubble
            key={m.id}
            message={m}
            onRetry={() => void retryMessage(m.id, adapter)}
            onSettings={onSettings}
          />,
        );
      }
      // 非 tool 消息自身的 runId 边界检查
      tryInsertDiffLog(m.agentRunId, i);
    }
    flushToolRun(messages.length - 1); // 最后一次 flush
    ```

    **更健壮的 toolRun lastIdx 追踪方案**（推荐，避免 i-1 不准确问题）：
    ```typescript
    // 用单独变量追踪 toolRun 最后入队消息的 messages index
    let toolRunLastIdx = -1;

    // 在 if (isRegularTool(m)) 分支中：
    toolRun.push(m);
    toolRunLastIdx = i;
    continue;

    // flushToolRun 改为无参，直接使用 toolRunLastIdx：
    const flushToolRun = (): void => {
      if (toolRun.length === 0) return;
      const lastInRun = toolRun[toolRun.length - 1];
      if (toolRun.length >= 2) {
        nodes.push(<MergedToolGroup key={`group-${toolRun[0].id}`} messages={toolRun} />);
      } else {
        for (const tm of toolRun) nodes.push(<ToolResultCard key={tm.id} message={tm} />);
      }
      toolRun = [];
      tryInsertDiffLog(lastInRun.agentRunId, toolRunLastIdx);
      toolRunLastIdx = -1;
    };
    ```

    5. 移除底部统一渲染块（原 L400-406 的 `completedRunIds.map(runId => <Suspense>...)` — **整块删除**）。

    6. return JSX 中，`{nodes}` 之后 `{showTyping && ...}` 之前，不再有 DiffLogPanel 的 map。

    完成后的 return：
    ```tsx
    return (
      <div className="aster-messages" ref={scrollRef} onScroll={handleScroll}>
        {nodes}
        {/* UI-02 思考气泡 */}
        {showTyping && (
          <div className="msg msg-ai">
            <div className="bubble bubble-ai bubble-typing" aria-label="正在思考" role="status">
              <span className="bubble-typing__dot" aria-hidden="true" />
              <span className="bubble-typing__dot" aria-hidden="true" />
              <span className="bubble-typing__dot" aria-hidden="true" />
            </div>
          </div>
        )}
      </div>
    );
    ```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/components/ChatStream.test.tsx -t "UI-03|DiffLog" --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "runLastIndex" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 ≥2（声明 + 使用）
    - grep -c "insertedRuns" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 ≥2（声明 + 使用）
    - grep -c "tryInsertDiffLog" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 ≥2（声明 + 调用）
    - 原底部 completedRunIds.map 块已移除：grep -c "completedRunIds.map" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx 输出 0
    - vitest run src/components/ChatStream.test.tsx -t "UI-03" PASS（UI-03-A + UI-03-B 至少无 import 报错；边界位置断言 PASS 取决于 test 细化程度）
    - vitest run src/components/ChatStream.test.tsx —— 所有原有测试（UI-01/02/05 等）保持 GREEN
    - npm run test（tsc + vitest）全绿
  </acceptance_criteria>
  <done>ChatStream.tsx nodes 构建改为边界插入算法；底部统一渲染块已移除；DiffLogPanel 紧跟各自 runId 末尾；npm test 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增攻击面 | 纯节点构建算法重构，无新 LLM 输入路径，无新外部数据来源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | — | UI-03 DiffLogPanel 边界插入算法 | accept | 纯 React 节点构建重构；messages 数组来自 chatStore（LLM 输入已在 loop-helpers 处理时过滤）；runId 来自 agentStore 常量；无新攻击面 |
| T-12-11 | Denial of Service | completedRunIds.includes O(n×m) 查找 | accept | 已优化为 Set 查找 O(1)；单次会话消息量 <200、runIds <20，性能无影响 |
</threat_model>

<verification>
Plan 04 完成检验：

1. `grep -c "runLastIndex" src/components/ChatStream.tsx` —— ≥2
2. `grep -c "completedRunIds.map" src/components/ChatStream.tsx` —— 0（底部块已移除）
3. `vitest run src/components/ChatStream.test.tsx -t "UI-03"` —— UI-03 测试 PASS
4. `vitest run src/components/ChatStream.test.tsx` —— 所有 UI-01/02/03/05 测试全 PASS
5. `npm run test` —— tsc + vitest 全绿
6. `npm run build && npm run size` —— PASS ≤82KB
</verification>

<success_criteria>
- DiffLogPanel 边界插入算法完成：runLastIndex Map + insertedRuns Set + tryInsertDiffLog
- 底部统一渲染块移除（completedRunIds.map 消失）
- Pitfall 3 已处理（toolRun 最后一条是 tool 时仍正确插入）
- lazy + Suspense 保持不变（DiffLogPanel 不进初始 chunk）
- npm test 全绿；npm run build + size PASS
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-e/12-04-SUMMARY.md`
</output>

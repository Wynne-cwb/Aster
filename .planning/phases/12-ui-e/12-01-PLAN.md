---
phase: 12-ui-e
plan: "01"
type: execute
wave: 1
depends_on: ["12-00"]
files_modified:
  - src/components/ChatBubble.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "react-markdown 渲染的链接不会执行 javascript: URI"
    - "https: 链接正常保留，不误杀"
    - "img src 的 javascript: 同样被拦截"
    - "所有 ChatBubble.test.tsx 的 UI-01 测试全部 GREEN"
  artifacts:
    - path: "src/components/ChatBubble.tsx"
      provides: "assistant ReactMarkdown 带 urlTransform={safeUrlTransform}"
      contains: "urlTransform={safeUrlTransform}"
    - path: "src/utils/safeUrlTransform.ts"
      provides: "已在 Wave 0 实现"
      exports: ["safeUrlTransform"]
  key_links:
    - from: "src/components/ChatBubble.tsx"
      to: "src/utils/safeUrlTransform.ts"
      via: "import { safeUrlTransform }"
      pattern: "safeUrlTransform.*from.*utils/safeUrlTransform"
    - from: "ReactMarkdown in ChatBubble.tsx"
      to: "safeUrlTransform function"
      via: "urlTransform prop"
      pattern: "urlTransform=\\{safeUrlTransform\\}"
---

<objective>
UI-01 P0 安全修复——将 safeUrlTransform 接入 ChatBubble 的 ReactMarkdown 渲染。

Purpose: 防御 CVE-2025-24981 同类 XSS 攻击——LLM 生成的 Markdown 含 javascript: URI，用户点击时在 TaskPane webview 执行任意 JS 可读取 API Key。这是 P0 安全门，不软化，独立首个执行 plan。
Output: ChatBubble.tsx 的 ReactMarkdown 加 urlTransform 一个 prop，ChatBubble.test.tsx 的 UI-01-A/B/D/E 全部由 RED 变 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ui-e/12-CONTEXT.md
@.planning/phases/12-ui-e/12-RESEARCH.md
@.planning/phases/12-ui-e/12-PATTERNS.md

<interfaces>
<!-- ChatBubble.tsx 当前 ReactMarkdown 调用（src/components/ChatBubble.tsx L25-88）-->
```tsx
// 当前 imports（L25-30）：
import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import type { Message } from '../store/chat';
import ErrorBubble from './ErrorBubble';
import { formatTime } from '../utils/formatTime';

// 当前渲染（L86-88）：
<ReactMarkdown remarkPlugins={[remarkGfm]}>
  {message.content}
</ReactMarkdown>

// 目标（Wave 1 改后）：
import { safeUrlTransform } from '../utils/safeUrlTransform';
<ReactMarkdown remarkPlugins={[remarkGfm]} urlTransform={safeUrlTransform}>
  {message.content}
</ReactMarkdown>
```

<!-- react-markdown@9.1.0 urlTransform prop 类型（已在 Wave 0 验证）-->
```typescript
// node_modules/react-markdown/lib/index.d.ts
type UrlTransform = (url: string, key: string, node: Readonly<Element>) => string | null | undefined;
// safeUrlTransform 签名 (url: string) => string 兼容（忽略 key/node 参数，TS 函数子集兼容）
```

<!-- safeUrlTransform 函数（Wave 0 已创建）-->
```typescript
// src/utils/safeUrlTransform.ts（已存在）
export function safeUrlTransform(url: string): string;
// javascript:/data:/vbscript: → ''；http/https/mailto/相对路径/#锚点 → 原样返回
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 将 safeUrlTransform 接入 ChatBubble 的 ReactMarkdown（UI-01 最小差异修改）</name>
  <files>src/components/ChatBubble.tsx</files>
  <read_first>
    - src/components/ChatBubble.tsx（整体读取，重点 L25-30 imports 和 L86-88 ReactMarkdown 调用）
    - src/utils/safeUrlTransform.ts（Wave 0 已创建，确认 export 名称）
    - .planning/phases/12-ui-e/12-PATTERNS.md §ChatBubble.tsx MODIFY（exact diff 模式）
  </read_first>
  <behavior>
    - ChatBubble 渲染 `[点我](javascript:alert(1))` 后，DOM 中 `a` 元素的 `href` 属性为 `''`（空串）或不存在，不含 `javascript:`
    - ChatBubble 渲染 `[链接](https://example.com)` 后，DOM 中 `a[href]` 为 `https://example.com`（不误杀）
    - ChatBubble 渲染 `![图](javascript:...)` 后，`img[src]` 不含 `javascript:`
    - ChatBubble 的 `return null`（空 content）行为保持不变（不改 L79-81）
  </behavior>
  <action>
    仅修改 ChatBubble.tsx 两处（最小 diff）：

    1. 在现有 import 块（L25-30 附近）的末尾加一行 import：
       `import { safeUrlTransform } from '../utils/safeUrlTransform';`
       （按字母顺序/功能分组插在 `import { formatTime }` 之后）

    2. 在 L86 的 `<ReactMarkdown remarkPlugins={[remarkGfm]}>` 加 urlTransform prop，变为：
       `<ReactMarkdown remarkPlugins={[remarkGfm]} urlTransform={safeUrlTransform}>`

    不改任何其他内容，特别是：
    - 不动 L79-81 的 `if (!message.content.trim()) return null;`（UI-02 依赖此行为）
    - 不动其他 import、JSX 结构、样式

    硬约束（D-03）：urlTransform 就是 safeUrlTransform——不叠加内置 defaultUrlTransform，以自写 allowlist 为唯一真相，行为可预测。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/components/ChatBubble.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "urlTransform={safeUrlTransform}" /Users/wb.chen/Documents/Project/Aster/src/components/ChatBubble.tsx 输出 1
    - grep -c "from.*utils/safeUrlTransform" /Users/wb.chen/Documents/Project/Aster/src/components/ChatBubble.tsx 输出 1
    - vitest run src/components/ChatBubble.test.tsx 所有 5 个 UI-01 测试 PASS（UI-01-A/B/C/D/E 全 GREEN）
    - vitest run src/components/ChatBubble.test.tsx 无任何 FAIL
    - npm run test（tsc + vitest）通过（包括现有测试不被破坏）
  </acceptance_criteria>
  <done>ChatBubble.tsx 的 ReactMarkdown 带 urlTransform={safeUrlTransform}；全部 5 个 UI-01 DOM 断言 GREEN；npm test 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → ChatBubble ReactMarkdown | LLM 生成的 Markdown 文本（含可能的恶意 URI）在 TaskPane webview 内渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering / Elevation of Privilege | ChatBubble.tsx `<ReactMarkdown>` `href`/`src` 属性 | mitigate | `urlTransform={safeUrlTransform}` 白名单——`javascript:`/`data:`/`vbscript:` 返回 `''`，href 设为空，无跳转。ASVS L1 V5.1.3。5 个 DOM 级别测试守门（RED→GREEN 本 plan）。 |
| T-12-02 | Information Disclosure | localStorage API Key via javascript: exec | mitigate | UI-01 切断攻击链根部——urlTransform 在渲染时过滤，用户无法点击危险链接，API Key 无法被读取 |
</threat_model>

<verification>
Plan 01 完成检验：

1. `grep -c "urlTransform={safeUrlTransform}" src/components/ChatBubble.tsx`——输出 1
2. `vitest run src/components/ChatBubble.test.tsx --reporter=verbose`——5 个 UI-01 用例全 PASS
3. `vitest run src/utils/safeUrlTransform.test.ts`——8 个 unit 用例全 PASS（Wave 0 已 GREEN，确认未被破坏）
4. `npm run test`——tsc + vitest 全绿
</verification>

<success_criteria>
- UI-01 安全修复完成：ChatBubble 的 ReactMarkdown 带 urlTransform prop
- 5 个 ChatBubble DOM 级别测试全 GREEN：javascript:/data:/vbscript: 被拦截，https: 正常保留
- npm test 全绿，无回归
- bundle 无增长（safeUrlTransform.ts 是极小 pure util，Wave 0 已计入）
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-e/12-01-SUMMARY.md`
</output>

---
phase: 02-provider-settings-onboarding-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sse.ts
  - src/lib/sse.test.ts
  - src/lib/storage.ts
  - src/lib/storage.test.ts
autonomous: true
requirements:
  - PROV-06
  - PROV-08
  - KEY-01
  - KEY-05
  - NFR-03

must_haves:
  truths:
    - "streamSSE 是 AsyncGenerator，逐字 yield SSEDelta，流结束前 yield SSEUsage（含 totalTokens）"
    - "stream_options: { include_usage: true } 在 streamSSE 内部注入，调用方无需传入"
    - "mapHttpError 正确将 401→KeyInvalidError / 402→QuotaExceededError / 422→ContextTooLongError / 429→RateLimitError / 503→NetworkError / 404→ModelNotFoundError"
    - "storage.get/set 在 partitionKey 有值时自动添加前缀，无值时直接用 rawKey"
    - "项目内没有文件直接调用 localStorage.setItem/getItem（所有访问经 storage.*）"
  artifacts:
    - path: "src/lib/sse.ts"
      provides: "SSE 解析器（streamSSE + mapHttpError）"
      contains: "stream_options.*include_usage"
    - path: "src/lib/storage.ts"
      provides: "partitioned localStorage 工具"
      contains: "Office.context.partitionKey"
    - path: "src/lib/sse.test.ts"
      provides: "SSE 解析器单元测试"
    - path: "src/lib/storage.test.ts"
      provides: "Storage 工具单元测试"
  key_links:
    - from: "src/providers/openai-compat.ts（Wave 2）"
      to: "src/lib/sse.ts"
      via: "import streamSSE from '../lib/sse'"
      pattern: "import.*streamSSE.*from.*lib/sse"
    - from: "src/store/providers.ts（Wave 3）"
      to: "src/lib/storage.ts"
      via: "import storage from '../lib/storage'"
      pattern: "storage\\.set\\|storage\\.get"
---

<objective>
实现 Phase 2 的两个无依赖基础工具：
1. `src/lib/sse.ts` — 约 40 行原生 fetch + ReadableStream SSE 解析器，服务所有 LLM 流式请求（PROV-06/NFR-03）
2. `src/lib/storage.ts` — partitioned localStorage 工具，所有 Key 和配置持久化经此（KEY-01）

目的：这两个工具是 Wave 2 Provider 层的直接依赖。Wave 1 两个计划（01/02）互相独立，可并行执行。

产出：
- `src/lib/sse.ts`（streamSSE 异步生成器 + mapHttpError 错误映射）
- `src/lib/storage.ts`（partitionKey 前缀工具 + 键名常量）
- `src/lib/sse.test.ts`（mock fetch + ReadableStream 单元测试）
- `src/lib/storage.test.ts`（mock Office.context.partitionKey 单元测试）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-provider-settings-onboarding-ux/02-CONTEXT.md
@.planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md
@.planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md

<interfaces>
<!-- 现有错误类（此 plan 依赖 src/errors/index.ts，02-01 完成后才能全部导入） -->
<!-- 在执行时，如果 02-01 还未完成，先基于 Phase 1 已有的 4 个类（KeyInvalidError/QuotaExceededError/ContextTooLongError/NetworkError）编写，Wave 2 executor 注意补全 4 个新类导入 -->
```typescript
// src/errors/index.ts (Phase 1 已有)
export class KeyInvalidError extends AsterError { ... }    // code: 'KEY_INVALID'
export class QuotaExceededError extends AsterError { ... } // code: 'QUOTA'
export class ContextTooLongError extends AsterError { ... } // code: 'CONTEXT'
export class NetworkError extends AsterError { ... }       // code: 'NETWORK'
// Phase 2 新增（02-01 plan 创建）
export class RateLimitError extends AsterError { retryAfterSeconds?: number; ... } // code: 'RATE_LIMIT'
export class ContentFilterError extends AsterError { ... } // code: 'FILTER'
export class ModelNotFoundError extends AsterError { ... } // code: 'MODEL'
export class ImageQuotaError extends AsterError { ... }   // code: 'IMAGE_QUOTA'
```

<!-- RESEARCH.md 完整 SSE 实现 -->
```typescript
// src/lib/sse.ts — 完整实现形状
export interface SSEDelta { type: 'delta'; content: string; }
export interface SSEUsage { type: 'usage'; promptTokens: number; completionTokens: number; totalTokens: number; }
export type SSEEvent = SSEDelta | SSEUsage;

export async function* streamSSE(url: string, body: object, signal: AbortSignal): AsyncGenerator<SSEEvent>
// 内部注入：stream: true, stream_options: { include_usage: true }
// keep-alive 行（以 ': ' 开头）跳过
// [DONE] 触发 return
// usage chunk：chunk.usage && chunk.usage.total_tokens != null

export function mapHttpError(status: number, body: unknown): AsterError
// 401 → KeyInvalidError('DeepSeek API Key 无效')
// 402 → QuotaExceededError('账户余额不足，请前往充值')
// 422 → ContextTooLongError('内容过长，请减少选区或切换更大模型')
// 429 → RateLimitError('请求过快', retryAfterSeconds?)  // 从 body 或 header 解析
// 404 → ModelNotFoundError('模型不存在，请检查设置中的模型名称')
// 503 → NetworkError('服务繁忙')
// content_policy/filter → ContentFilterError('内容被过滤，请修改输入内容')
// 其他 → NetworkError('网络错误')
```

<!-- RESEARCH.md Storage 实现 -->
```typescript
// src/lib/storage.ts — 完整实现形状
function key(rawKey: string): string {
  const pk = Office.context.partitionKey;  // 可能为 undefined（Windows WebView）
  return pk ? pk + rawKey : rawKey;
}
export const storage = {
  get<T>(rawKey: string): T | null { ... },  // JSON.parse，失败返回 null
  set(rawKey: string, value: unknown): void { ... }, // JSON.stringify
  remove(rawKey: string): void { ... },
};
// 键名常量（只在 storage.ts 内定义，外部通过 STORAGE_KEYS 使用）
export const STORAGE_KEYS = {
  PROVIDERS: 'aster:providers',
  KEY_PREFIX: 'aster:keys:',  // + providerId
  ONBOARDING_SEEN: 'aster:onboarding:seen',
  SELECTION_AUTO_ATTACH: 'aster:selection:autoAttach',
  DEFAULT_PROVIDER: 'aster:providers:default',
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 SSE 解析器（src/lib/sse.ts）及其测试</name>
  <files>src/lib/sse.ts, src/lib/sse.test.ts</files>
  <read_first>
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §SSE 解析器具体实现（完整代码形状，直接参考）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §错误类 HTTP 状态映射（mapHttpError 映射表）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §Common Pitfalls（4 个 SSE 陷阱）
    - src/errors/index.ts（导入错误类用）
    - src/adapters/PptAdapter.ts（错误包裹模式参考）
  </read_first>
  <behavior>
    SSE 解析测试（mock ReadableStream）：
    - 给定包含两个 delta chunk + 一个 usage chunk + [DONE] 的流，应 yield 两个 SSEDelta 和一个 SSEUsage
    - 给定 ": keep-alive" 行，不报错，不 yield 任何事件
    - 给定 AbortSignal 已 abort 的流，应 throw AbortError（不是 AsterError）
    - 给定空 data 行，忽略不 yield
    
    mapHttpError 测试：
    - mapHttpError(401, {}) instanceof KeyInvalidError 为 true
    - mapHttpError(402, {}) instanceof QuotaExceededError 为 true
    - mapHttpError(429, { headers: { 'Retry-After': '30' } }) instanceof RateLimitError，retryAfterSeconds === 30
    - mapHttpError(404, {}) instanceof ModelNotFoundError 为 true
    - mapHttpError(422, { error: { message: 'context length' } }) instanceof ContextTooLongError 为 true
    - mapHttpError(400, { error: { message: 'content_policy violation' } }) instanceof ContentFilterError 为 true
    - mapHttpError(503, {}) instanceof NetworkError 为 true
    - mapHttpError(999, {}) instanceof NetworkError 为 true（兜底）
    - 所有 mapHttpError 返回对象的 message 不含 API Key 字符串（T-01-04）
  </behavior>
  <action>
创建 `src/lib/sse.ts`，内容如下（严格按 RESEARCH.md §SSE 解析器具体实现，含以下关键点）：

1. 从 `../errors` 导入所有 8 个 Provider 错误类
2. `mapHttpError(status: number, errBody: unknown, retryAfterSeconds?: number): AsterError` 函数：
   - 401 → `new KeyInvalidError('DeepSeek API Key 无效')`
   - 402 → `new QuotaExceededError('账户余额不足，请前往 Provider 充值')`
   - 404 → `new ModelNotFoundError('模型不存在，请在设置中检查模型名称')`
   - 422 → 先检查 body 含 content_policy/filter 则 ContentFilterError，否则 ContextTooLongError('内容过长，请减少选区或切换更大模型')
   - 400 → 检查 body 含 content_policy/filter → ContentFilterError，否则 NetworkError('请求参数错误')
   - 429 → `new RateLimitError('请求过快，稍后自动重试', retryAfterSeconds)`
   - 503 → `new NetworkError('服务繁忙，稍后自动重试')`
   - 其他 → `new NetworkError('网络错误，请检查连接')`
   - 安全：所有 message 字符串不包含任何凭证或 Key 原文（T-01-04）

3. `streamSSE(url, body, signal)` 异步生成器：
   - 请求时合并 `stream: true` + `stream_options: { include_usage: true }`（内部注入，调用方不需传）
   - Authorization header 从 `(body as any).apiKey` 取，发送后从 body 副本中删掉（不在日志里暴露）
   - 非 2xx 响应：读取 JSON body，解析 Retry-After header，调用 `mapHttpError` 抛出
   - 每行检查：不以 `data:` 开头 → skip（处理 `: keep-alive`）
   - `data === '[DONE]'` → return（生成器关闭）
   - usage chunk（chunk.usage && chunk.usage.total_tokens != null）→ yield SSEUsage
   - delta chunk（chunk.choices?.[0]?.delta?.content）→ yield SSEDelta
   - JSON.parse 失败 → 静默忽略

创建 `src/lib/sse.test.ts`，使用 Vitest + `vi.fn()` mock `fetch`：
- mock ReadableStream 用字符串构造 `new ReadableStream({ start(c) { c.enqueue(enc(chunks)); c.close(); } })`
- 测试用例覆盖上述 behavior 列表所有场景
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/lib/sse.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stream_options.*include_usage" src/lib/sse.ts` 输出 >= 1
    - `grep -c "\[DONE\]" src/lib/sse.ts` 输出 >= 1（[DONE] 检测）
    - `grep -c "keep-alive\|startsWith.*data" src/lib/sse.ts` 输出 >= 1（keep-alive 处理）
    - `grep -c "apiKey" src/lib/sse.ts` 的结果中，不存在把 apiKey 直接嵌入 message 字符串的行
    - `npx vitest run src/lib/sse.test.ts` 全部通过，覆盖 mapHttpError 的全部 HTTP 状态分支
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>streamSSE 和 mapHttpError 实现完整，测试全绿，API Key 不泄漏到任何 error message</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Storage 工具（src/lib/storage.ts）及其测试</name>
  <files>src/lib/storage.ts, src/lib/storage.test.ts</files>
  <read_first>
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §Storage 模式（partitioned localStorage 代码形状 + 键名约定表）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §Common Pitfalls Pitfall 2（partitionKey 未前缀的问题）
    - src/adapters/ExcelAdapter.ts（try/catch 返回 null 的模式参考）
  </read_first>
  <behavior>
    storage 测试（mock Office.context.partitionKey + mock localStorage）：
    - partitionKey = 'pk_' 时：storage.set('foo', 42) → localStorage.setItem('pk_foo', '42')
    - partitionKey = undefined 时：storage.set('foo', 42) → localStorage.setItem('foo', '42')
    - storage.get('foo') 返回 JSON.parse 后的值（number 42 不变）
    - storage.get('nonexistent') 返回 null
    - storage.get('malformed_json_key') 返回 null（不 throw）
    - storage.remove('foo') 调用 localStorage.removeItem 且传入正确前缀后的 key
    - STORAGE_KEYS 常量包含 PROVIDERS / ONBOARDING_SEEN / SELECTION_AUTO_ATTACH / DEFAULT_PROVIDER / KEY_PREFIX 五个键
  </behavior>
  <action>
创建 `src/lib/storage.ts`：

```typescript
/**
 * src/lib/storage.ts — partitioned localStorage 工具（KEY-01/KEY-05）
 *
 * 所有 Aster 的 localStorage 操作都经此工具；
 * 不在其他文件直接调用 localStorage.setItem/getItem/removeItem。
 *
 * partitionKey 行为（Phase 0 GATING #3 已验证）：
 * - Office for Web (Chrome/Edge ≥115)：partitionKey = hash(top-level domain + addin domain)
 *   三宿主（PPT/Excel/Word）产生不同分区——Key 不在宿主间共享（设计上正确）
 * - Office on Windows (WebView)：partitionKey = undefined，无分区，直接用 rawKey
 */

/** 键名常量（RESEARCH.md §Storage 模式 键名约定表） */
export const STORAGE_KEYS = {
  /** Provider 配置列表（ProviderConfig[]，不含 apiKey） */
  PROVIDERS: 'aster:providers',
  /** 各 Provider 的 API Key 前缀（+ providerId 组成完整 key） */
  KEY_PREFIX: 'aster:keys:',
  /** Onboarding 是否已看过（boolean） */
  ONBOARDING_SEEN: 'aster:onboarding:seen',
  /** 选区自动附带开关（boolean，D-15） */
  SELECTION_AUTO_ATTACH: 'aster:selection:autoAttach',
  /** 当前默认 LLM Provider ID（string） */
  DEFAULT_PROVIDER: 'aster:providers:default',
} as const;

/** 将 rawKey 加上 partitionKey 前缀（partitionKey 不存在时直接返回 rawKey） */
function prefixedKey(rawKey: string): string {
  // Office.context 在 Office.onReady 之前可能不可用；
  // 组件和 store 只在 onReady 回调内调用 storage，因此此处安全
  const pk = (typeof Office !== 'undefined' && Office.context?.partitionKey) || undefined;
  return pk ? `${pk}${rawKey}` : rawKey;
}

export const storage = {
  /** 读取值，JSON.parse 后返回；键不存在或 JSON 非法时返回 null（不 throw）。 */
  get<T>(rawKey: string): T | null {
    try {
      const v = localStorage.getItem(prefixedKey(rawKey));
      return v !== null ? (JSON.parse(v) as T) : null;
    } catch {
      return null;
    }
  },

  /** 写入值（JSON.stringify）。 */
  set(rawKey: string, value: unknown): void {
    localStorage.setItem(prefixedKey(rawKey), JSON.stringify(value));
  },

  /** 删除值。 */
  remove(rawKey: string): void {
    localStorage.removeItem(prefixedKey(rawKey));
  },
};
```

创建 `src/lib/storage.test.ts`，使用 Vitest `vi.stubGlobal` 分别 mock `Office` 和 `localStorage`：
- 测试两种 partitionKey 场景（有值/'pk_' 和 undefined）
- 覆盖 get/set/remove 三个方法
- 覆盖 JSON 解析失败返回 null
- 验证 STORAGE_KEYS 常量值完整性
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/lib/storage.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Office.context.partitionKey\|Office.context?\.partitionKey" src/lib/storage.ts` 输出 >= 1
    - `grep -c "localStorage" src/lib/storage.ts` 输出 >= 3（3 个方法各用一次）
    - 项目其他文件没有直接调用 localStorage：`grep -r "localStorage\." src/ --include="*.ts" --include="*.tsx" -l | grep -v "storage.ts\|storage.test.ts"` 输出为空
    - `grep -c "STORAGE_KEYS" src/lib/storage.ts` 输出 >= 1
    - `npx vitest run src/lib/storage.test.ts` 全部通过
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>storage.ts 实现完整，partitionKey 前缀正确，测试全绿，无其他文件直接访问 localStorage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetch response body → SSE 解析器 | Provider 返回的 HTTP 响应体为不可信内容，需 JSON.parse 防护 |
| API Key → Authorization header | Key 仅出现在 HTTP header 中，不进入 error.message / localStorage key 名 |
| localStorage → storage.get | localStorage 内容可被浏览器扩展读取，Key 明文存储风险用户知情（KEY-05 告知） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | streamSSE: apiKey 从 body 取，可能进入请求体日志 | mitigate | 从发送前的 body 副本删除 apiKey 字段；Authorization header 由 fetch 内部处理，不进入 Request body JSON |
| T-02-05 | Information Disclosure | mapHttpError: 不将 API Key 嵌入错误 message | mitigate | 所有 message 使用固定中文字符串，不插入 apiKey 变量（T-01-04）；测试验证 message 不含 Key |
| T-02-06 | Information Disclosure | localStorage 跨宿主隔离 | mitigate | storage.ts 通过 partitionKey 前缀实现宿主隔离；Phase 0 Spike #3 已验证行为 |
| T-02-07 | Tampering | 恶意 Provider 返回超长 SSE 流 | accept | buf 随 lines 消费清空，不会无限增长；单次流约几千行；整体受 AbortController 保护 |
| T-02-08 | Information Disclosure | storage.get JSON 解析失败时记录错误 | accept | 静默返回 null，不记录日志（localStorage 值可能含敏感信息） |
</threat_model>

<verification>
## 整体验证

```bash
# 两个测试文件全部通过
cd /Users/wb.chen/Documents/Project/Aster
npx vitest run src/lib/sse.test.ts src/lib/storage.test.ts --reporter=verbose 2>&1 | tail -20

# TypeScript 编译无错
npx tsc --noEmit 2>&1 | head -20

# 确认 partitionKey 前缀
grep "partitionKey" src/lib/storage.ts

# 确认 stream_options 内部注入
grep "include_usage" src/lib/sse.ts

# 确认无直接 localStorage 访问（project 其他文件）
grep -r "localStorage\." src/ --include="*.ts" --include="*.tsx" -l | grep -v storage
```
</verification>

<success_criteria>
- src/lib/sse.ts 实现 streamSSE（AsyncGenerator）+ mapHttpError（8 类 HTTP 状态映射）
- src/lib/storage.ts 实现 storage.get/set/remove + STORAGE_KEYS 常量 + partitionKey 前缀
- 两个测试文件各自通过 vitest run，覆盖核心行为
- 无直接 localStorage.* 调用散落在 src/ 其他文件（独占 storage.ts）
- API Key 不出现在任何 error.message 字符串中
</success_criteria>

<output>
完成后在 `.planning/phases/02-provider-settings-onboarding-ux/` 创建 `02-02-SUMMARY.md`，包含：
- 实际创建的文件路径
- streamSSE 的输入/输出类型签名
- mapHttpError 覆盖的 HTTP 状态列表
- storage STORAGE_KEYS 键名列表
- 测试覆盖项计数（通过 N 个用例）
</output>

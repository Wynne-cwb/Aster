---
phase: 02-provider-settings-onboarding-ux
plan: 07
type: execute
wave: 5
depends_on:
  - "02-05"
  - "02-06"
files_modified:
  - src/components/Settings/SettingsPanel.tsx
  - src/components/Settings/ProviderList.tsx
  - src/components/Settings/ProviderForm.tsx
  - src/components/Onboarding/OnboardingModal.tsx
  - src/components/Onboarding/Step1Keys.tsx
  - src/components/Onboarding/Step2Guide.tsx
  - src/styles.css
autonomous: true
requirements:
  - PROV-05
  - KEY-02
  - KEY-03
  - KEY-04
  - KEY-05

must_haves:
  truths:
    - "Settings 面板从右侧滑入覆盖整个 Task Pane（CSS transform translateX 动画）"
    - "Settings 里可新增/编辑/删除自定义 Provider（baseURL + apiKey + model 三字段，D-08）"
    - "内置 DeepSeek / aihubmix Provider 不可删除（isBuiltIn=true，删除按钮 disabled）"
    - "Settings 提供「自动附带选区内容」开关，onChange 调用 providerStore.setAutoAttach(e.target.checked)（D-15）"
    - "Onboarding 第 1 步：预选 DeepSeek（D-02），DeepSeek Key 输入（非强制完成才能退出，D-01），aihubmix Key 选填，隐私告知内联常驻（D-05）"
    - "Onboarding 第 2 步：仅显示当前宿主功能卡（D-03）"
    - "Onboarding 可跳过（D-01）；storage 标记 aster:onboarding:seen"
    - "隐私告知文案明确说明「选中的文档内容会发送到所配置的 Provider，不经过 Aster 服务器」（KEY-03/D-05）"
  artifacts:
    - path: "src/components/Settings/SettingsPanel.tsx"
      provides: "整页滑入设置面板（含 autoAttach 开关）"
      contains: "SettingsPanel"
    - path: "src/components/Settings/ProviderList.tsx"
      provides: "Provider 增删改列表"
      contains: "ProviderList"
    - path: "src/components/Settings/ProviderForm.tsx"
      provides: "Provider 表单（baseURL/apiKey/model）"
      contains: "ProviderForm"
    - path: "src/components/Onboarding/OnboardingModal.tsx"
      provides: "2 步 Onboarding Modal"
      contains: "OnboardingModal"
    - path: "src/components/Onboarding/Step1Keys.tsx"
      provides: "第 1 步 Key 输入 + 隐私告知"
      contains: "Step1Keys"
    - path: "src/components/Onboarding/Step2Guide.tsx"
      provides: "第 2 步宿主功能卡"
      contains: "Step2Guide"
  key_links:
    - from: "src/App.tsx（02-08）"
      to: "src/components/Settings/SettingsPanel.tsx"
      via: "showSettings state + <SettingsPanel onClose />"
      pattern: "SettingsPanel"
    - from: "src/App.tsx（02-08）"
      to: "src/components/Onboarding/OnboardingModal.tsx"
      via: "showOnboarding state + storage.get(onboarding:seen)"
      pattern: "OnboardingModal"
    - from: "src/components/ErrorBubble.tsx（02-06）"
      to: "src/components/Settings/SettingsPanel.tsx"
      via: "onSettings(anchor) 深链打开设置并 focus 对应字段"
      pattern: "onSettings.*anchor"
    - from: "src/components/Settings/SettingsPanel.tsx"
      to: "src/store/providers.ts"
      via: "providerStore.setAutoAttach(e.target.checked)"
      pattern: "setAutoAttach"
---

<objective>
创建 Settings 设置面板（Provider 管理）和 Onboarding 引导 Modal（2 步 Key 填写 + 功能介绍）。

视觉规范：
- Settings：整页右侧滑入（translateX）+ 顶部返回 + 可滚动 Provider 列表
- Onboarding：绝对定位覆盖整个 Task Pane（`inset: 0; z-index: 50`），不超出 Task Pane iframe 边界（Pitfall 6）
- 表单字段用 `<input className="aster-field">`，提交用 `.aster-send` 渐变按钮
- 所有字符串 Lingui 包裹（zh-CN）

**注意（D-15 / B4）：** `autoAttach` 和 `setAutoAttach` 已在 Wave 3（02-05 providerStore）实现，此 Wave 5 直接消费。SettingsPanel 的 autoAttach 开关 onChange = `(e) => providerStore.setAutoAttach(e.target.checked)`，无需在此再定义 autoAttach 字段。

注意：隐私告知（D-05/KEY-03/KEY-04）是核心安全功能，必须常驻 Step1Keys，不可隐藏或折叠。

产出：6 个组件（3 Settings + 3 Onboarding）+ styles.css 新增 CSS 类
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-provider-settings-onboarding-ux/02-CONTEXT.md
@.planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md
@.planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md

<interfaces>
<!-- 上游接口 -->
```typescript
// src/store/providers.ts（Wave 3，02-05 已实现）
export const useProviderStore: StoreApi<ProviderState>;
// state: providers: ProviderConfig[], defaultLLMProviderId: string
// state: autoAttach: boolean        ← D-15，Wave 3 已实现
// actions: addProvider, updateProvider, removeProvider, setDefaultLLM, setKey, getKey
// actions: setAutoAttach(v: boolean) ← 写 storage.set(SELECTION_AUTO_ATTACH, v)，Wave 3 已实现

// src/providers/types.ts
export interface ProviderConfig { id: string; name: string; baseURL: string; model: string; isBuiltIn: boolean; }

// src/lib/storage.ts
export const storage;
export const STORAGE_KEYS;

// src/context/AdapterContext.ts
export function useAdapter(): DocumentAdapter  // host = adapter.capabilities().host

// src/components/icons.tsx（Phase 2 新增）
export function PlusIcon(): ReactElement;
export function TrashIcon(): ReactElement;
export function ChevronIcon(): ReactElement;
export function CheckIcon(): ReactElement;
```

<!-- RESEARCH.md Settings UI 架构 -->
```typescript
// App.tsx 整页滑入模式（CSS transform）
// <div className={`aster-settings-overlay ${showSettings ? 'is-open' : ''}`}>
//   <SettingsPanel onClose={() => setShowSettings(false)} settingsAnchor={anchor} />
// </div>
// CSS:
// .aster-settings-overlay { position: absolute; inset: 0; background: var(--bg); transform: translateX(100%); transition: transform var(--dur) var(--ease); z-index: 10; }
// .aster-settings-overlay.is-open { transform: translateX(0); }
```

<!-- RESEARCH.md Onboarding 流程 -->
```
触发条件：storage.get('aster:onboarding:seen') === null
Step 1：DeepSeek Key（必填字段非空验证，但不阻断跳过）+ aihubmix Key（选填）
        隐私告知内联常驻（D-05）：「你选中的文档内容会发送到所配置的 Provider，不经过 Aster 服务器」
Step 2：useAdapter().capabilities().host 决定显示哪张功能卡（D-03）
跳过路径：storage.set('aster:onboarding:seen', true) + 顶部提示条
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 Settings 面板（SettingsPanel + ProviderList + ProviderForm）</name>
  <files>src/components/Settings/SettingsPanel.tsx, src/components/Settings/ProviderList.tsx, src/components/Settings/ProviderForm.tsx, src/styles.css</files>
  <read_first>
    - src/App.tsx（顶层 shell 结构参考；Settings 面板在 App.tsx 中的位置）
    - src/components/InputBar.tsx（表单字段模式：aster-field、aster-send、disabled 状态）
    - src/styles.css（现有 class 体系，避免重复）
    - .planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md §Settings/ 三个文件的 analog 说明
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §设置页 UI 架构（CSS 方案）
    - src/store/providers.ts（useProviderStore、ProviderConfig、autoAttach、setAutoAttach）
    - src/components/icons.tsx（ChevronIcon/PlusIcon/TrashIcon）
  </read_first>
  <action>
创建 Settings 目录和三个文件：

**`src/components/Settings/SettingsPanel.tsx`：**

Props：`{ onClose: () => void; initialAnchor?: string; onShowOnboarding?: () => void }` — `initialAnchor` 是深链字段 ID（D-12）。

`autoAttach` 和 `setAutoAttach` 直接从 providerStore（Wave 3 已实现）读取：

```tsx
import { useProviderStore } from '../../store/providers';

function SettingsPanel({ onClose, initialAnchor, onShowOnboarding }: SettingsPanelProps) {
  const autoAttach = useProviderStore((s) => s.autoAttach);
  const setAutoAttach = useProviderStore((s) => s.setAutoAttach);
  // ...
}
```

结构：
```tsx
<div className="aster-settings">
  {/* 顶部返回 */}
  <div className="aster-settings__header">
    <button className="aster-iconbtn" onClick={onClose} aria-label={t`返回`}><ChevronIcon /></button>
    <span className="aster-settings__title"><Trans>设置</Trans></span>
  </div>
  {/* 可滚动内容区 */}
  <div className="aster-settings__body">
    <ProviderList focusAnchor={initialAnchor} />
    {/* 选区自动附带开关（D-15 全局开关，Wave 3 providerStore 已实现） */}
    <div className="aster-settings__section">
      <label className="aster-settings__label">
        <Trans>自动附带选区内容</Trans>
      </label>
      <input
        type="checkbox"
        checked={autoAttach}
        onChange={(e) => setAutoAttach(e.target.checked)}
        aria-label={t`自动附带选区内容`}
      />
    </div>
    {/* 重看引导（D-04） */}
    {onShowOnboarding && (
      <div className="aster-settings__section">
        <button className="aster-link-btn" onClick={onShowOnboarding}><Trans>重看引导</Trans></button>
      </div>
    )}
  </div>
</div>
```

`setAutoAttach(e.target.checked)` 内部调用 `storage.set(STORAGE_KEYS.SELECTION_AUTO_ATTACH, v)`，已在 Wave 3 实现，此处无需额外 storage 调用。

**`src/components/Settings/ProviderList.tsx`：**

Props：`{ focusAnchor?: string }`。
- 从 useProviderStore 读取 providers 列表
- 每个 Provider 一行：名称 + baseURL（截断）+ 「编辑」+ 「删除」
- isBuiltIn=true：删除按钮 disabled（opacity 0.5 + not-allowed）
- 「+ 新增自定义 Provider」按钮打开 ProviderForm（modal 或内联展开）
- 内置 Provider 只允许更新 apiKey（model 也可改；baseURL 不允许改内置 URL）
- focusAnchor 触发对应字段 focus（深链功能，D-12）：
  - anchor='key-input' → focus 到当前默认 Provider 的 Key 输入框
  - anchor='model-input' → focus 到当前默认 Provider 的 model 输入框

**`src/components/Settings/ProviderForm.tsx`：**

Props：`{ provider?: ProviderConfig; onSave: (data) => void; onCancel: () => void }`（provider 有值=编辑，无值=新建）。

三字段（D-08）：
- `baseURL`：`<input type="url">` — 内置 Provider 字段 disabled
- `model`：`<input type="text" placeholder="deepseek-v4-flash">`
- `apiKey`：`<input type="password">` — 新建必填（form validation），但不发验证请求（ONB-01 推迟）

隐私说明（KEY-03/D-05，非 onboarding 也要提示）：
```tsx
<p className="aster-form-hint"><Trans>API Key 仅存储在您的浏览器本地，不经过 Aster 服务器</Trans></p>
```

URL 格式校验：baseURL 必须以 `https://` 开头（安全约束，恶意 URL 缓解，T-02-10）。

在 `src/styles.css` 追加 Settings 相关 CSS 类（`.aster-settings`、`.aster-settings__header`、`.aster-settings__body`、`.aster-settings__title`、`.aster-settings__section`、`.aster-settings__label`、`.aster-link-btn`）以及 `.aster-settings-overlay` 和 `.aster-settings-overlay.is-open`（供 App.tsx 使用）。

所有字符串 Lingui `<Trans>` / `t` 包裹，全部 CSS 使用现有变量。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SettingsPanel\|ProviderList\|ProviderForm" src/components/Settings/SettingsPanel.tsx` 输出 >= 1（SettingsPanel 渲染子组件）
    - `grep -c "isBuiltIn" src/components/Settings/ProviderList.tsx` 输出 >= 1（内置 Provider 保护逻辑）
    - `grep -c "disabled" src/components/Settings/ProviderList.tsx` 输出 >= 1（内置 Provider 删除按钮 disabled）
    - `grep -c "type=\"password\"" src/components/Settings/ProviderForm.tsx` 输出 1（apiKey 字段）
    - `grep -c "https://" src/components/Settings/ProviderForm.tsx` 输出 >= 1（URL 格式校验）
    - `grep -c "Aster 服务器\|不经过 Aster" src/components/Settings/ProviderForm.tsx` 输出 >= 1（隐私提示）
    - `grep -c "aster-settings-overlay\|translateX" src/styles.css` 输出 >= 1（滑入动画）
    - `grep -c "<Trans>\|useLingui" src/components/Settings/ProviderForm.tsx` 输出 >= 2
    - `grep -c "setAutoAttach.*e\.target\.checked\|e\.target\.checked.*setAutoAttach" src/components/Settings/SettingsPanel.tsx` 输出 >= 1（D-15 开关 onChange 完整实现）
    - `grep -c "autoAttach" src/components/Settings/SettingsPanel.tsx` 输出 >= 1（从 providerStore 读取）
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>Settings 三个组件完成，Provider CRUD 可用，内置 Provider 保护，隐私提示常驻，CSS 滑入动画就位，autoAttach 开关 onChange 完整</done>
</task>

<task type="auto">
  <name>Task 2: 实现 Onboarding 引导（OnboardingModal + Step1Keys + Step2Guide）</name>
  <files>src/components/Onboarding/OnboardingModal.tsx, src/components/Onboarding/Step1Keys.tsx, src/components/Onboarding/Step2Guide.tsx, src/styles.css</files>
  <read_first>
    - src/components/ChatStream.tsx（host-conditional 渲染模式：useAdapter + capabilities().host + switch）
    - src/components/InputBar.tsx（表单字段、disabled 状态、aster-field class）
    - src/styles.css（现有变量体系）
    - .planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md §Onboarding/ 三个文件
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §Onboarding 流程
    - .planning/phases/02-provider-settings-onboarding-ux/02-CONTEXT.md D-01/02/03/04/05（5 个 Onboarding 决策）
    - src/lib/storage.ts（STORAGE_KEYS.ONBOARDING_SEEN）
    - src/store/providers.ts（providerStore.setKey 保存 Key）
  </read_first>
  <action>
创建 Onboarding 目录和三个文件：

**`src/components/Onboarding/OnboardingModal.tsx`：**

Props：`{ onComplete: () => void; onSkip: () => void }`。

State：`step: 1 | 2`。

结构（覆盖整个 Task Pane，z-index 50，Task Pane iframe 内）：
```tsx
<div className="aster-onboarding-overlay">
  <div className="aster-onboarding">
    {/* 步骤指示器 */}
    <div className="aster-onboarding__steps">
      <span className={`aster-step${step >= 1 ? ' is-active' : ''}`}>1</span>
      <span className="aster-step-divider" />
      <span className={`aster-step${step === 2 ? ' is-active' : ''}`}>2</span>
    </div>
    {/* 步骤内容 */}
    {step === 1 ? <Step1Keys onNext={() => setStep(2)} onSkip={onSkip} /> : <Step2Guide onComplete={onComplete} />}
  </div>
</div>
```

**`src/components/Onboarding/Step1Keys.tsx`：**

Props：`{ onNext: () => void; onSkip: () => void }`。

内容（D-02：默认预选 DeepSeek）：
```tsx
<div>
  <h2><Trans>配置 AI Provider</Trans></h2>
  
  {/* DeepSeek Key 输入（主输入，D-02） */}
  <label><Trans>DeepSeek API Key</Trans></label>
  <input type="password" className="aster-field" placeholder="sk-..." value={dsKey} onChange={...} />
  
  {/* aihubmix Key 输入（选填）*/}
  <label><Trans>AiHubMix API Key <span className="aster-optional">（选填，用于生图和视觉）</span></Trans></label>
  <input type="password" className="aster-field" placeholder="..." value={ahmKey} onChange={...} />
  
  {/* 隐私告知（D-05/KEY-03）：内联常驻，不可隐藏 */}
  <p className="aster-privacy-notice">
    <Trans>你选中的文档内容会发送到所配置的 Provider，不经过 Aster 服务器。<br />API Key 仅存储在您的浏览器本地。</Trans>
  </p>
  
  {/* 按钮行 */}
  <div className="aster-onboarding__actions">
    <button className="aster-link-btn" onClick={onSkip}><Trans>跳过</Trans></button>
    <button className="aster-send" onClick={handleNext}><Trans>下一步</Trans></button>
  </div>
</div>
```

`handleNext`：调用 `providerStore.setKey('deepseek', dsKey)` 和 `providerStore.setKey('aihubmix', ahmKey)` 保存（即使 Key 为空也不阻断，D-01）；然后调用 `onNext()`。

Key 为空时按钮仍可点（D-01：不强制阻断），但表单显示字段必填状态（input required + CSS 边框提示）。

**`src/components/Onboarding/Step2Guide.tsx`：**

Props：`{ onComplete: () => void }`。

用 `useAdapter().capabilities().host` 判断宿主，switch 返回对应功能卡（D-03）：
- 'ppt'：「把主题扩展成大纲」「为 slide 配图」「bullet 压缩」三项介绍
- 'excel'：「自然语言生成公式」「公式解释/调修」「数据清洗」三项介绍
- 'word'：「多风格润色」「TL;DR 摘要」「大纲→长文」三项介绍

底部「开始使用」按钮调用：`storage.set(STORAGE_KEYS.ONBOARDING_SEEN, true)` + `onComplete()`。

在 `src/styles.css` 追加 Onboarding 相关 CSS：
- `.aster-onboarding-overlay`：`position: absolute; inset: 0; background: var(--bg); z-index: 50; display: flex; align-items: flex-start; padding: var(--sp-4);`
- `.aster-onboarding`：`width: 100%; max-width: 320px;`
- `.aster-step`：步骤圆点样式
- `.aster-privacy-notice`：`font-size: 11px; color: var(--text-3); border-left: 2px solid var(--border); padding-left: var(--sp-2); margin: var(--sp-3) 0;`
- `.aster-optional`：`font-size: 11px; color: var(--text-3);`

所有字符串 Lingui `<Trans>` 包裹。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aster-onboarding-overlay\|inset: 0\|z-index: 50" src/styles.css` 输出 >= 1（覆盖定位）
    - `grep -c "ONBOARDING_SEEN" src/components/Onboarding/Step2Guide.tsx` 输出 >= 1（完成时写 storage）
    - `grep -c "type=\"password\"" src/components/Onboarding/Step1Keys.tsx` 输出 2（DeepSeek + aihubmix 两个 Key 字段）
    - `grep -c "Aster 服务器\|不经过 Aster\|aster-privacy-notice" src/components/Onboarding/Step1Keys.tsx` 输出 >= 1（隐私告知常驻）
    - `grep -c "onSkip\|跳过" src/components/Onboarding/Step1Keys.tsx` 输出 >= 1（D-01 可跳过）
    - `grep -c "capabilities().host\|switch.*host\|case 'ppt'" src/components/Onboarding/Step2Guide.tsx` 输出 >= 2（D-03 宿主功能卡）
    - `grep -c "setKey\|providerStore" src/components/Onboarding/Step1Keys.tsx` 输出 >= 1（保存 Key）
    - `grep -c "<Trans>\|useLingui" src/components/Onboarding/Step1Keys.tsx` 输出 >= 3
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>Onboarding 2 步 Modal 完成，隐私告知常驻，可跳过，Step2 按宿主显示对应功能卡，完成时写 storage 标记</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step1Keys → providerStore.setKey | API Key 从 input 直接写 storage，不经过 Aster 后台 |
| ProviderForm.baseURL → ProviderRegistry | 用户填入的自定义 URL 将成为 fetch 目标，需格式校验 |
| Onboarding 隐私告知 → 用户知情同意 | KEY-03/KEY-04 强制展示，确保用户知情再填 Key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Information Disclosure | 隐私告知（KEY-03）缺失时用户不知道内容会发送给 Provider | mitigate | aster-privacy-notice 常驻于 Step1Keys 和 ProviderForm，不可折叠；测试验证存在于 DOM |
| T-02-26 | Tampering | ProviderForm.baseURL 输入恶意 URL | mitigate | 校验：startsWith('https://') + URL 构造函数验证（无效 URL 阻止保存）；用户自填，风险用户知情 |
| T-02-27 | Information Disclosure | apiKey 通过 input type="password" 输入，可被扩展读取 | accept | type=password 防截图；Key 仅存 partitioned localStorage（KEY-01）；扩展读取属于用户本地风险，非 Aster 责任范围 |
| T-02-28 | Tampering | Onboarding 跳过后 Key 未填写，但用户仍可发送消息 | mitigate | D-01 明确允许跳过；Key 未填时 ProviderRegistry.resolve() 抛 KeyInvalidError → 聊天发送时触发错误气泡 + CTA 引导设置（不是静默失败） |
</threat_model>

<verification>
## 整体验证

```bash
cd /Users/wb.chen/Documents/Project/Aster

# 类型检查
npx tsc --noEmit 2>&1 | head -10

# 隐私告知必须存在于 Step1Keys
grep "Aster 服务器\|不经过 Aster" src/components/Onboarding/Step1Keys.tsx

# URL 校验存在
grep "https://" src/components/Settings/ProviderForm.tsx

# Onboarding seen 标记
grep "ONBOARDING_SEEN\|onboarding:seen" src/components/Onboarding/Step2Guide.tsx

# Settings overlay CSS
grep "aster-settings-overlay\|translateX" src/styles.css

# autoAttach 开关 onChange 完整（D-15 / W3）
grep "setAutoAttach.*e\.target\.checked\|e\.target\.checked" src/components/Settings/SettingsPanel.tsx
```
</verification>

<success_criteria>
- Settings 整页滑入（CSS translateX），Provider 增删改功能完整，内置 Provider 保护
- Settings 提供 autoAttach 开关，onChange={(e) => setAutoAttach(e.target.checked)}（D-15），直接消费 Wave 3 providerStore 实现
- Onboarding 2 步：Step1 DeepSeek Key + aihubmix Key + 常驻隐私告知（不可折叠），Step2 宿主功能卡（单卡）
- 可跳过（D-01），完成时写 storage 标记，Settings 提供「重看引导」入口（D-04）
- baseURL 格式校验（必须 https://），apiKey type=password
- 全部 Lingui 包裹，全部 CSS 变量（两套主题覆盖）
</success_criteria>

<output>
完成后在 `.planning/phases/02-provider-settings-onboarding-ux/` 创建 `02-07-SUMMARY.md`，包含：
- Settings 组件结构（三层：Panel > List > Form）
- autoAttach 开关 onChange 调用链（SettingsPanel → setAutoAttach → storage.set）
- Onboarding 可跳过逻辑（D-01 路径）
- 隐私告知文案（确认存在于两处：Step1Keys + ProviderForm）
- baseURL 校验逻辑说明
</output>

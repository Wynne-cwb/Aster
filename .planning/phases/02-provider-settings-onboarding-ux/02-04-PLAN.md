---
phase: 02-provider-settings-onboarding-ux
plan: 04
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - src/providers/openai-compat.ts
  - src/providers/aihubmix-vision.ts
  - src/providers/aihubmix-image.ts
  - src/providers/queue.ts
  - src/providers/retry.ts
  - src/providers/queue.test.ts
  - src/providers/retry.test.ts
autonomous: true
requirements:
  - PROV-02
  - PROV-03
  - PROV-07
  - PROV-09

must_haves:
  truths:
    - "OpenAICompatibleLLM.streamChat 调用 streamSSE 并通过单飞队列 singleFlight 序列化同 Provider 请求"
    - "streamChat 通过 setupVisibilityAbort 注册 visibilitychange 监听器，Task Pane 隐藏时 abort，cleanup 在 finally 调用"
    - "aihubmix-vision.ts 发 POST /chat/completions（非流式），携带 image_url content block，model = 'gpt-4o'"
    - "aihubmix-image.ts 发 POST /images/generations，model = 'gpt-image-1'，返回 b64_json"
    - "withRetry 对 RateLimitError/NetworkError 最多重试 3 次，尊重 retryAfterSeconds；KeyInvalidError/QuotaExceededError/ImageQuotaError 不重试"
    - "singleFlight 同 providerId 串行化，第二个请求等第一个完成后才发"
  artifacts:
    - path: "src/providers/openai-compat.ts"
      provides: "OpenAICompatibleLLM 类实现 LLMProvider 接口"
      contains: "class OpenAICompatibleLLM"
    - path: "src/providers/aihubmix-vision.ts"
      provides: "AihubmixVisionClient"
      contains: "class AihubmixVisionClient"
    - path: "src/providers/aihubmix-image.ts"
      provides: "AihubmixImageClient"
      contains: "class AihubmixImageClient"
    - path: "src/providers/queue.ts"
      provides: "singleFlight<T> + setupVisibilityAbort 函数"
      contains: "singleFlight"
    - path: "src/providers/retry.ts"
      provides: "withRetry 函数"
      contains: "withRetry"
  key_links:
    - from: "src/store/chat.ts（02-05）"
      to: "src/providers/openai-compat.ts"
      via: "new OpenAICompatibleLLM().streamChat(messages, config, signal)"
      pattern: "OpenAICompatibleLLM"
    - from: "src/providers/openai-compat.ts"
      to: "src/providers/queue.ts"
      via: "singleFlight(providerId, fn)"
      pattern: "singleFlight"
    - from: "src/providers/openai-compat.ts"
      to: "src/providers/retry.ts"
      via: "withRetry(() => streamSSE(...))"
      pattern: "withRetry"
---

<objective>
实现 Provider 层的三个客户端（OpenAICompatibleLLM / AihubmixVisionClient / AihubmixImageClient）以及两个核心基础设施（单飞队列 singleFlight / 指数退避 withRetry）。

这是 Wave 2 的实现层，依赖 Wave 1（sse.ts/storage.ts/errors）和 Wave 2 接口层（02-03 types.ts）。

产出：
- `src/providers/openai-compat.ts` — DeepSeek + 自定义 Provider 共用的 LLM 客户端
- `src/providers/aihubmix-vision.ts` — aihubmix 视觉客户端
- `src/providers/aihubmix-image.ts` — aihubmix 生图客户端
- `src/providers/queue.ts` — 单飞队列 + visibilitychange abort
- `src/providers/retry.ts` — 指数退避重试
- 对应测试文件
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-provider-settings-onboarding-ux/02-CONTEXT.md
@.planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md
@.planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md

<interfaces>
<!-- 上游依赖接口（执行者直接使用，无需重新探索） -->
```typescript
// src/lib/sse.ts（02-02）
export async function* streamSSE(url: string, body: object, signal: AbortSignal): AsyncGenerator<SSEEvent>
export function mapHttpError(status: number, errBody: unknown, retryAfterSeconds?: number): AsterError
export type SSEEvent = SSEDelta | SSEUsage;

// src/providers/types.ts（02-03）
export type TaskKind = 'chat' | 'short-task' | 'vision' | 'image-gen' | 'stock-image';
export interface LLMProvider { streamChat(messages, config, signal): AsyncGenerator<SSEEvent> }
export interface ImageProvider { generate(prompt, size, quality, config): Promise<{b64_json,usage?}> }
export interface LLMConfig { providerId: string; baseURL: string; apiKey: string; model: string; }
export interface ImageConfig { providerId: string; baseURL: string; apiKey: string; model: string; }
export interface ChatMessage { role: 'system'|'user'|'assistant'; content: string; }

// src/errors/index.ts（02-01）
export class RateLimitError extends AsterError { retryAfterSeconds?: number; ... }
export class NetworkError extends AsterError { ... }
export class KeyInvalidError extends AsterError { ... }
export class QuotaExceededError extends AsterError { ... }
export class ImageQuotaError extends AsterError { ... }
```

<!-- RESEARCH.md 关键实现摘要 -->
```typescript
// queue.ts 单飞队列（模块级 Map）
const inFlight = new Map<string, Promise<void>>();
export async function singleFlight<T>(providerId: string, fn: () => Promise<T>): Promise<T>

// visibilitychange abort
export function setupVisibilityAbort(controller: AbortController): () => void
// 返回 cleanup 函数，在 finally 调用

// retry.ts 指数退避
const RETRYABLE_ERRORS = [RateLimitError, NetworkError];
const MAX_RETRIES = 3;
const BASE_DELAY_MS = 1000;
const MAX_DELAY_MS = 30_000;
// billing 类（KeyInvalidError/QuotaExceededError/ImageQuotaError）不重试
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现单飞队列 + visibilitychange abort + 指数退避，编写测试</name>
  <files>src/providers/queue.ts, src/providers/retry.ts, src/providers/queue.test.ts, src/providers/retry.test.ts</files>
  <read_first>
    - src/adapters/ExcelAdapter.ts（模块级状态 + cleanup 函数返回模式）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §单飞队列实现
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §AbortController + visibilitychange 模式
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §指数退避实现
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §Common Pitfalls Pitfall 3（cleanup 泄漏）
    - src/errors/index.ts（错误类 instanceof 检查）
  </read_first>
  <behavior>
    singleFlight 测试：
    - 同一 providerId 的第二个调用在第一个完成前不会并发（通过 spy 计数验证）
    - 不同 providerId 的调用可以并发（通过 spy 计数验证）
    - 第一个请求 throw 后，第二个请求仍能继续执行（不被 reject 污染）
    
    setupVisibilityAbort 测试：
    - 调用后在 document 上注册 visibilitychange 监听器
    - document.visibilityState = 'hidden' 触发 controller.abort()
    - 调用返回的 cleanup 函数后，监听器被移除（再触发 hidden 不再 abort 第二次）
    
    withRetry 测试：
    - fn 抛 RateLimitError：重试最多 3 次，第 4 次抛出
    - fn 抛 RateLimitError(msg, 30)：等待 30s（mock setTimeout）后重试
    - fn 抛 NetworkError：重试最多 3 次
    - fn 抛 KeyInvalidError：立即抛出，不重试（billing class）
    - fn 抛 QuotaExceededError：立即抛出，不重试（billing class）
    - fn 抛 ImageQuotaError：立即抛出，不重试（billing class）
    - fn 成功：直接返回结果，无重试
  </behavior>
  <action>
创建 `src/providers/queue.ts`（严格按 RESEARCH.md §单飞队列实现）：

```typescript
/**
 * src/providers/queue.ts — 单飞队列 + visibilitychange abort（PROV-07）
 *
 * singleFlight：同一 providerId 的请求队列化，防止 Provider 过载。
 * 注意：这是「排队」而非「去重」——第二个调用等第一个完成后发新请求。
 * Map 在模块级维护（不在 React 组件里），生命周期正确（PATTERNS.md §queue.ts）。
 */

/** 模块级单飞 Map（providerId → 当前飞行中的 Promise ticket） */
const inFlight = new Map<string, Promise<void>>();

/**
 * singleFlight — 同一 Provider 串行化请求。
 * 等待同 providerId 的上一个请求完成后，才发新请求。
 */
export async function singleFlight<T>(
  providerId: string,
  fn: () => Promise<T>,
): Promise<T> {
  // 等待同 Provider 的上一个请求完成（失败也继续）
  const prev = inFlight.get(providerId);
  if (prev) await prev.catch(() => {});

  let resolve!: () => void;
  const ticket = new Promise<void>((r) => { resolve = r; });
  inFlight.set(providerId, ticket);

  try {
    return await fn();
  } finally {
    resolve();
    // 防泄漏：只在当前 ticket 还是 Map 里那个时才移除
    if (inFlight.get(providerId) === ticket) {
      inFlight.delete(providerId);
    }
  }
}

/**
 * setupVisibilityAbort — Task Pane 隐藏时自动 abort 当前请求。
 *
 * 使用标准 Web visibilitychange 事件（Phase 0 验证可用于 Office for Web）。
 * 返回 cleanup 函数——必须在请求完成后（finally 块）调用，否则泄漏监听器（Pitfall 3）。
 */
export function setupVisibilityAbort(controller: AbortController): () => void {
  function onHide(): void {
    if (document.visibilityState === 'hidden') {
      controller.abort();
    }
  }
  document.addEventListener('visibilitychange', onHide);
  return () => document.removeEventListener('visibilitychange', onHide);
}
```

创建 `src/providers/retry.ts`（严格按 RESEARCH.md §指数退避实现）：

```typescript
/**
 * src/providers/retry.ts — 指数退避重试（PROV-09）
 *
 * 只对 RateLimitError（429）和 NetworkError（503/网络失败）重试。
 * billing 类错误（KeyInvalidError/QuotaExceededError/ImageQuotaError）绝对不重试。
 * 尊重 RateLimitError.retryAfterSeconds（来自 Retry-After header）。
 */

import { RateLimitError, NetworkError, KeyInvalidError, QuotaExceededError, ImageQuotaError } from '../errors';

const MAX_RETRIES = 3;
const BASE_DELAY_MS = 1_000;
const MAX_DELAY_MS = 30_000;

/** 不可重试的 billing 类错误 */
function isBillingError(e: unknown): boolean {
  return e instanceof KeyInvalidError
    || e instanceof QuotaExceededError
    || e instanceof ImageQuotaError;
}

/** 可重试的错误 */
function isRetryable(e: unknown): boolean {
  return e instanceof RateLimitError || e instanceof NetworkError;
}

export async function withRetry<T>(fn: () => Promise<T>): Promise<T> {
  for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
    try {
      return await fn();
    } catch (e) {
      // billing 类：立即抛出，不重试
      if (isBillingError(e)) throw e;
      // 不可重试的其他错误：立即抛出
      if (!isRetryable(e)) throw e;
      // 已达最大重试次数：抛出
      if (attempt === MAX_RETRIES) throw e;

      // 计算退避时间
      const retryAfterMs = e instanceof RateLimitError && e.retryAfterSeconds != null
        ? e.retryAfterSeconds * 1_000
        : Math.min(BASE_DELAY_MS * Math.pow(2, attempt), MAX_DELAY_MS) * (0.9 + Math.random() * 0.2);

      await new Promise<void>((resolve) => setTimeout(resolve, retryAfterMs));
    }
  }
  // TypeScript exhaustiveness
  throw new Error('withRetry: unreachable');
}
```

创建测试文件，使用 `vi.useFakeTimers()` mock setTimeout，`vi.spyOn(document, 'addEventListener')` 测试 visibilitychange。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/providers/queue.test.ts src/providers/retry.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const inFlight = new Map" src/providers/queue.ts` 输出 1（模块级 Map）
    - `grep -c "visibilitychange" src/providers/queue.ts` 输出 >= 2（add + remove）
    - `grep -c "isBillingError\|billing" src/providers/retry.ts` 输出 >= 1
    - `grep -c "MAX_RETRIES = 3" src/providers/retry.ts` 输出 1
    - `grep -c "BASE_DELAY_MS = 1" src/providers/retry.ts` 输出 1
    - `grep -c "MAX_DELAY_MS = 30" src/providers/retry.ts` 输出 1
    - `npx vitest run src/providers/queue.test.ts src/providers/retry.test.ts` 全部通过
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>singleFlight 正确串行化，setupVisibilityAbort 正确 cleanup，withRetry billing 类不重试，测试全绿</done>
</task>

<task type="auto">
  <name>Task 2: 实现三个 Provider 客户端（openai-compat / aihubmix-vision / aihubmix-image）</name>
  <files>src/providers/openai-compat.ts, src/providers/aihubmix-vision.ts, src/providers/aihubmix-image.ts</files>
  <read_first>
    - src/adapters/PptAdapter.ts（类实现接口 + try/catch 包裹 AsterError 模式）
    - src/adapters/WordAdapter.ts（简单 async 方法 + 单个 try/catch）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §DeepSeek API Wire Format（完整请求体）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §aihubmix API Shapes（视觉 + 生图请求体）
    - .planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md §src/providers/openai-compat.ts
    - src/lib/sse.ts（streamSSE / mapHttpError 导入）
    - src/providers/queue.ts（singleFlight / setupVisibilityAbort 导入）
    - src/providers/retry.ts（withRetry 导入）
    - src/errors/index.ts（NetworkError 导入）
  </read_first>
  <action>
创建 `src/providers/openai-compat.ts`（DeepSeek + 自定义 Provider 共用）：

```typescript
/**
 * src/providers/openai-compat.ts — OpenAI-compatible LLM 客户端（PROV-02）
 *
 * 服务 DeepSeek（deepseek-v4-flash / deepseek-v4-pro）和用户自定义 Provider。
 * 实现 LLMProvider 接口，所有方法 async，error 包裹为 AsterError 子类。
 *
 * 关键约束：
 * - 不引入任何 LLM SDK（PROV-10 / tech stack 硬约束）
 * - stream_options.include_usage 由 streamSSE 内部注入
 * - apiKey 永不出现在 error.message（T-01-04）
 * - AbortController cleanup 必须在 finally 调用（PROV-07 / Pitfall 3）
 */

import type { LLMProvider, LLMConfig, ChatMessage } from './types';
import type { SSEEvent } from '../lib/sse';
import { streamSSE } from '../lib/sse';
import { singleFlight, setupVisibilityAbort } from './queue';
import { withRetry } from './retry';
import { NetworkError } from '../errors';

export class OpenAICompatibleLLM implements LLMProvider {
  async *streamChat(
    messages: ChatMessage[],
    config: LLMConfig,
    signal: AbortSignal,
  ): AsyncGenerator<SSEEvent> {
    // visibilitychange abort（Task Pane 隐藏时主动取消）
    const cleanup = setupVisibilityAbort(signal as unknown as AbortController);
    // 注：setupVisibilityAbort 接受 AbortController，此处 signal 来自 store 的 controller
    // Store 层应将 AbortController 本身传给 setupVisibilityAbort，signal 传给 singleFlight/streamSSE
    // 实际实现时，store 拥有 AbortController；此处 openai-compat 只消费 signal
    // 因此 setupVisibilityAbort 应在 store 层调用（见 02-05 设计）

    try {
      // 通过单飞队列序列化同 Provider 请求
      yield* await singleFlight(config.providerId, async () => {
        // 通过 withRetry 包裹（429/503 自动重试）
        return withRetry(() => this._doStream(messages, config, signal));
      });
    } catch (e) {
      if (e instanceof Error && e.name === 'AbortError') {
        // 用户停止或 Task Pane 隐藏——不报错，中断生成
        return;
      }
      // 非 AsterError 包裹为 NetworkError（T-01-04：不含 Key）
      if (!(e instanceof Error) || !('code' in e)) {
        throw new NetworkError('网络请求异常，请检查连接');
      }
      throw e;
    } finally {
      cleanup();
    }
  }

  private async *_doStream(
    messages: ChatMessage[],
    config: LLMConfig,
    signal: AbortSignal,
  ): AsyncGenerator<SSEEvent> {
    const url = `${config.baseURL.replace(/\/$/, '')}/chat/completions`;
    // apiKey 传给 streamSSE；streamSSE 内部放入 Authorization header 后从 body 删除
    const body = {
      apiKey: config.apiKey,  // streamSSE 内部取出放 header，不进 JSON body
      model: config.model,
      messages,
      // stream 和 stream_options 由 streamSSE 内部注入
    };
    yield* streamSSE(url, body, signal);
  }
}
```

**注意：** `setupVisibilityAbort` 需要 `AbortController` 对象（不是 signal），store 层持有 controller，因此在实际集成时，store 的 `sendMessage` 函数直接调用 `setupVisibilityAbort(controller)`，`openai-compat.ts` 只接收 signal。上面的实现应去掉 openai-compat 中对 setupVisibilityAbort 的调用，改为仅在内部处理 AbortError。将 setupVisibilityAbort 调用职责文档化：**store 层（02-05）负责注册 visibilitychange abort**。

修正后的 `openai-compat.ts` 职责简化为：接受 signal → singleFlight → withRetry → streamSSE → 抓 AbortError 静默处理。

创建 `src/providers/aihubmix-vision.ts`：

```typescript
// POST /chat/completions，model='gpt-4o'，stream=false，携带 image_url content block
// 响应为普通 OpenAI-compatible JSON（非流式）
// 安全：apiKey 不进 error.message（T-01-04）
```

创建 `src/providers/aihubmix-image.ts`：

```typescript
// POST /images/generations，model='gpt-image-1'
// 响应：{ data: [{b64_json}], usage: { input_tokens, output_tokens, total_tokens } }
// 注意：usage 字段用 input_tokens/output_tokens（非 prompt_tokens/completion_tokens，RESEARCH.md §aihubmix API）
// 安全：apiKey 不进 error.message（T-01-04）
```

两个 aihubmix 客户端的错误处理：调用 `mapHttpError(resp.status, errBody)` 包裹 HTTP 错误；fetch reject 包裹为 NetworkError。

关键：两个 aihubmix 客户端类使用 `class AihubmixVisionClient` / `class AihubmixImageClient`，不 implements 通用接口（aihubmix 专用路径，RESEARCH.md §Architecture Patterns）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class OpenAICompatibleLLM" src/providers/openai-compat.ts` 输出 1
    - `grep -c "implements LLMProvider" src/providers/openai-compat.ts` 输出 1
    - `grep -c "singleFlight" src/providers/openai-compat.ts` 输出 >= 1
    - `grep -c "withRetry" src/providers/openai-compat.ts` 输出 >= 1
    - `grep -c "AbortError\|abort" src/providers/openai-compat.ts` 输出 >= 1（处理 AbortError）
    - `grep -c "apiKey" src/providers/openai-compat.ts` — 所有出现都是作为配置字段，无 string interpolation 进 error message
    - `grep -c "class AihubmixVisionClient" src/providers/aihubmix-vision.ts` 输出 1
    - `grep -c "gpt-4o" src/providers/aihubmix-vision.ts` 输出 >= 1
    - `grep -c "class AihubmixImageClient" src/providers/aihubmix-image.ts` 输出 1
    - `grep -c "gpt-image-1" src/providers/aihubmix-image.ts` 输出 >= 1
    - `grep -c "input_tokens" src/providers/aihubmix-image.ts` 输出 >= 1（aihubmix 特殊 usage 字段）
    - `npx tsc --noEmit` 通过
    - `grep -rn "apiKey.*message\|message.*apiKey" src/providers/` 输出为空（T-01-04）
  </acceptance_criteria>
  <done>三个 Provider 客户端实现完整，类型正确，API Key 不泄漏到错误消息</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| openai-compat.ts → streamSSE | API Key 从 config.apiKey 传给 streamSSE，streamSSE 内部放 Authorization header |
| aihubmix-vision/image → fetch | Key 放 Authorization header，不在请求体中 |
| Provider 响应 → 错误处理 | Provider 返回的 error body 可能含敏感信息，mapHttpError 不传递到 UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | openai-compat: apiKey 出现在 body 参数中 | mitigate | streamSSE 内部从 body 取 apiKey 放 Authorization header 后删除该字段，不进入 JSON.stringify 的 request body；测试验证 network request 不含 apiKey 字段 |
| T-02-14 | Denial of Service | 单飞队列阻塞：第一个请求永远 pending | accept | AbortController 保证有界执行；AbortError 不会卡住 queue（finally 块总执行）；用户可主动停止 |
| T-02-15 | Information Disclosure | aihubmix-vision 接受 base64 图像数据 | accept | 图像内容由用户主动上传（Phase 3）；Phase 2 暂无文件上传功能，此路径不激活 |
| T-02-16 | Tampering | withRetry 重试期间用户关闭 Task Pane | mitigate | setupVisibilityAbort 在 store 层注册，隐藏时 controller.abort()；AbortError 打断重试循环 |
</threat_model>

<verification>
## 整体验证

```bash
cd /Users/wb.chen/Documents/Project/Aster

# 单元测试
npx vitest run src/providers/queue.test.ts src/providers/retry.test.ts --reporter=verbose 2>&1 | tail -15

# TypeScript 编译
npx tsc --noEmit 2>&1 | head -10

# 安全检查：apiKey 不进 error.message
grep -rn 'apiKey.*`\|`.*apiKey\|".*apiKey' src/providers/*.ts

# 确认 singleFlight 模块级 Map
grep "const inFlight" src/providers/queue.ts

# 确认 gpt-image-1 和 input_tokens
grep "gpt-image-1\|input_tokens" src/providers/aihubmix-image.ts
```
</verification>

<success_criteria>
- OpenAICompatibleLLM 实现 LLMProvider，通过 singleFlight + withRetry + streamSSE 完成流式请求
- setupVisibilityAbort 正确注册/移除 visibilitychange 监听器，cleanup 在 finally 调用
- withRetry 对 billing 类错误（KeyInvalidError/QuotaExceededError/ImageQuotaError）不重试，对 RateLimitError/NetworkError 最多 3 次
- AihubmixVisionClient（gpt-4o）和 AihubmixImageClient（gpt-image-1）实现正确，响应解析正确
- 全部 TypeScript 编译通过，无 any 类型（strict mode）
</success_criteria>

<output>
完成后在 `.planning/phases/02-provider-settings-onboarding-ux/` 创建 `02-04-SUMMARY.md`，包含：
- 三个客户端类的方法签名
- singleFlight / setupVisibilityAbort / withRetry 的职责说明
- store 层需负责 setupVisibilityAbort 的设计说明（避免循环依赖）
- 测试通过数
</output>

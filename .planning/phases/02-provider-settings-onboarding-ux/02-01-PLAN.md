---
phase: 02-provider-settings-onboarding-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/errors/index.ts
  - src/components/icons.tsx
  - eslint.config.js
autonomous: true
requirements:
  - PROV-08
  - PROV-10

must_haves:
  truths:
    - "src/errors/index.ts 包含全部 8 类错误：4 个 Phase 1 已有 + 4 个 Phase 2 新增（RateLimitError/ContentFilterError/ModelNotFoundError/ImageQuotaError）"
    - "所有新增错误类都正确继承 AsterError，code/category 字段与 PROV-08 一致"
    - "icons.tsx 新增 8 个图标（StopIcon/InsertIcon/RetryIcon/XIcon/AlertIcon/PlusIcon/TrashIcon/CheckIcon），均遵循 base spread 模式"
    - "ESLint 规则阻止导入 openai / @anthropic-ai/* / ai / ai/* 包以及使用 deepseek-chat / deepseek-reasoner 字面量"
  artifacts:
    - path: "src/errors/index.ts"
      provides: "完整 8 类错误（含 retryAfterSeconds 字段的 RateLimitError）"
      contains: "RateLimitError|ContentFilterError|ModelNotFoundError|ImageQuotaError"
    - path: "src/components/icons.tsx"
      provides: "Phase 2 所需 8 个新图标"
      exports: ["StopIcon", "InsertIcon", "RetryIcon", "XIcon", "AlertIcon", "PlusIcon", "TrashIcon", "CheckIcon"]
    - path: "eslint.config.js"
      provides: "禁用 legacy 模型名 + SDK 包导入规则"
      contains: "no-restricted-imports"
  key_links:
    - from: "src/providers/openai-compat.ts（Wave 2）"
      to: "src/errors/index.ts"
      via: "import RateLimitError / ContentFilterError / ModelNotFoundError"
      pattern: "import.*RateLimitError.*from.*errors"
    - from: "src/lib/sse.ts（Wave 1）"
      to: "src/errors/index.ts"
      via: "mapHttpError 用 instanceof 映射 HTTP 状态"
      pattern: "import.*ImageQuotaError.*from.*errors"
---

<objective>
补齐 Phase 2 所需的 4 个错误类（Wave 1 其他计划的基础依赖）、新增 8 个 UI 图标（后续 UI 波次消费）、配置 ESLint 规则阻止引入 LLM SDK 包与 legacy 模型名。

目的：本计划是 Wave 1 的纯类型/配置层，没有任何运行时副作用，不依赖其他 Wave 1 计划，可与 02-02 并行执行。

产出：
- 扩展后的 `src/errors/index.ts`（8 类错误全齐）
- 扩展后的 `src/components/icons.tsx`（4 个现有 + 8 个新增）
- 更新后的 `eslint.config.js`（PROV-10 ESLint 规则）
- `src/errors/index.test.ts` 更新（新增 4 类错误的 smoke test）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-provider-settings-onboarding-ux/02-CONTEXT.md
@.planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md
@.planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md

<interfaces>
<!-- 现有代码摘要，执行者直接使用，无需额外探索 -->

<!-- src/errors/index.ts — 现有 6 个类 -->
```typescript
export class AsterError extends Error {
  constructor(message: string, public readonly code: string, public readonly category: 'provider' | 'adapter') { ... }
}
export class KeyInvalidError extends AsterError { constructor(message: string) { super(message, 'KEY_INVALID', 'provider'); } }
export class QuotaExceededError extends AsterError { constructor(message: string) { super(message, 'QUOTA', 'provider'); } }
export class ContextTooLongError extends AsterError { constructor(message: string) { super(message, 'CONTEXT', 'provider'); } }
export class NetworkError extends AsterError { constructor(message: string) { super(message, 'NETWORK', 'provider'); } }
export class HostApiError extends AsterError { public readonly hostError?: unknown; constructor(message: string, hostError?: unknown) { ... } }
export class UnsupportedOperationError extends AsterError { constructor(message: string) { super(message, 'UNSUPPORTED', 'adapter'); } }
```

<!-- src/components/icons.tsx — 现有 4 个图标 -->
```typescript
const base = { viewBox:'0 0 24 24', fill:'none', stroke:'currentColor', strokeWidth:1.75,
  strokeLinecap:'round' as const, strokeLinejoin:'round' as const, 'aria-hidden': true };
export function SettingsIcon(): ReactElement { ... }
export function UploadIcon(): ReactElement { ... }
export function SendIcon(): ReactElement { ... }
export function ChevronIcon(): ReactElement { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 4 个 Phase 2 错误类 + 更新测试</name>
  <files>src/errors/index.ts, src/errors/index.test.ts</files>
  <read_first>
    - src/errors/index.ts（必须先读，了解 AsterError 基类结构和现有子类模式）
    - src/errors/index.test.ts（了解现有测试结构，新测试必须遵循同样格式）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §错误类 HTTP 状态映射（新类定义的出处）
  </read_first>
  <behavior>
    - RateLimitError: code='RATE_LIMIT', category='provider', 额外字段 retryAfterSeconds?: number
    - ContentFilterError: code='FILTER', category='provider'
    - ModelNotFoundError: code='MODEL', category='provider'
    - ImageQuotaError: code='IMAGE_QUOTA', category='provider'
    - 所有新类：instanceof AsterError 为 true
    - 所有新类：.name 等于类名字符串（new.target.name 机制）
    - RateLimitError(msg, 30).retryAfterSeconds === 30
    - RateLimitError(msg).retryAfterSeconds === undefined
  </behavior>
  <action>
在 `src/errors/index.ts` 的 Provider 层错误注释块之后（NetworkError 之后、Adapter 层错误之前）追加以下 4 个类：

```typescript
/**
 * RateLimitError — 请求频率超限（HTTP 429）。
 * code: 'RATE_LIMIT'
 * Phase 2 UX: 自动指数退避，显示「请求过快，正在自动重试…」
 *
 * retryAfterSeconds: 来自 Retry-After 响应头（秒数）；未提供时由 withRetry 自行计算退避。
 * 安全约束（T-01-04）：message 禁止嵌入 API Key 原文。
 */
export class RateLimitError extends AsterError {
  constructor(
    message: string,
    public readonly retryAfterSeconds?: number,
  ) {
    super(message, 'RATE_LIMIT', 'provider');
  }
}

/**
 * ContentFilterError — 内容被 Provider 安全策略过滤（400/422 中含 content_policy 关键词）。
 * code: 'FILTER'
 * Phase 2 UX CTA: 「内容被过滤，请修改输入内容」
 * 安全约束（T-01-04）：message 禁止嵌入 API Key 原文。
 */
export class ContentFilterError extends AsterError {
  constructor(message: string) {
    super(message, 'FILTER', 'provider');
  }
}

/**
 * ModelNotFoundError — 模型 ID 不存在（HTTP 404）或 ProviderRegistry 路由失败。
 * code: 'MODEL'
 * Phase 2 UX CTA: 「模型不存在，请在设置中检查模型名称 →」
 * 安全约束（T-01-04）：message 禁止嵌入 API Key 原文。
 */
export class ModelNotFoundError extends AsterError {
  constructor(message: string) {
    super(message, 'MODEL', 'provider');
  }
}

/**
 * ImageQuotaError — aihubmix 图像生成配额用尽（aihubmix 专属错误）。
 * code: 'IMAGE_QUOTA'
 * Phase 2 UX CTA: 「图像生成配额用尽，前往 aihubmix 充值 →」
 * 注意：此错误属于 billing 类，withRetry 不得自动重试（PROV-09）。
 * 安全约束（T-01-04）：message 禁止嵌入 API Key 原文。
 */
export class ImageQuotaError extends AsterError {
  constructor(message: string) {
    super(message, 'IMAGE_QUOTA', 'provider');
  }
}
```

同时更新 `src/errors/index.test.ts`，在现有测试之后追加新类的 smoke test（按现有测试相同模式）：
- RateLimitError 继承 AsterError，code='RATE_LIMIT'，retryAfterSeconds 字段存在且正确
- ContentFilterError code='FILTER'
- ModelNotFoundError code='MODEL'
- ImageQuotaError code='IMAGE_QUOTA'
- 所有新类：instanceof AsterError 为 true，.name 为类名字符串

**禁止：** 不在 message 中嵌入任何凭证字符串（T-01-04）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/errors/index.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class RateLimitError" src/errors/index.ts` 输出 1
    - `grep -c "class ContentFilterError" src/errors/index.ts` 输出 1
    - `grep -c "class ModelNotFoundError" src/errors/index.ts` 输出 1
    - `grep -c "class ImageQuotaError" src/errors/index.ts` 输出 1
    - `grep "retryAfterSeconds" src/errors/index.ts` 输出含 `retryAfterSeconds?: number` 的行
    - `npx vitest run src/errors/index.test.ts` 全部通过，0 失败
    - `grep -c "API Key" src/errors/index.ts` 输出 0（message 中无凭证字符串）
  </acceptance_criteria>
  <done>8 类错误全部存在于 src/errors/index.ts，测试全绿，RateLimitError 有 retryAfterSeconds 可选字段</done>
</task>

<task type="auto">
  <name>Task 2: 新增 8 个 UI 图标到 icons.tsx</name>
  <files>src/components/icons.tsx</files>
  <read_first>
    - src/components/icons.tsx（必须先读，了解 base 对象结构和现有图标写法）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §UI 新增图标（图标用途说明）
  </read_first>
  <action>
在 `src/components/icons.tsx` 末尾，按 RESEARCH.md §UI 新增图标表的顺序，追加以下 8 个图标。所有图标必须使用 `{...base}` spread 并 return `ReactElement`：

```typescript
/** 停止生成（实心方块——对应流式生成中「发送键变停止」D-14） */
export function StopIcon(): ReactElement {
  return (
    <svg {...base} fill="currentColor" stroke="none">
      <rect x="6" y="6" width="12" height="12" rx="2" />
    </svg>
  );
}

/** 插入到文档（向下箭头入框，PANE-04） */
export function InsertIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M12 3v12" />
      <path d="M8 11l4 4 4-4" />
      <path d="M5 19h14" />
    </svg>
  );
}

/** 重试（顺时针循环箭头，失败气泡重试 D-11） */
export function RetryIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M1 4v6h6" />
      <path d="M3.51 15a9 9 0 1 0 .49-3.51" />
    </svg>
  );
}

/** 关闭 / 删除（× 号，选区胶囊关闭 D-15） */
export function XIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M18 6L6 18" />
      <path d="M6 6l12 12" />
    </svg>
  );
}

/** 警示（三角感叹号，错误气泡前缀 D-10） */
export function AlertIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
      <line x1="12" y1="9" x2="12" y2="13" />
      <line x1="12" y1="17" x2="12.01" y2="17" />
    </svg>
  );
}

/** 新增（+ 号，Settings 新增 Provider） */
export function PlusIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M12 5v14" />
      <path d="M5 12h14" />
    </svg>
  );
}

/** 删除（垃圾桶，Settings 删除 Provider） */
export function TrashIcon(): ReactElement {
  return (
    <svg {...base}>
      <polyline points="3 6 5 6 21 6" />
      <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" />
      <path d="M10 11v6" />
      <path d="M14 11v6" />
      <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" />
    </svg>
  );
}

/** 完成（对勾，Onboarding 步骤完成标记） */
export function CheckIcon(): ReactElement {
  return (
    <svg {...base}>
      <polyline points="20 6 9 17 4 12" />
    </svg>
  );
}
```

注意 `StopIcon` 使用 `fill="currentColor" stroke="none"`（实心方块需要），其余图标保持 base 的 `fill='none'`。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function StopIcon" src/components/icons.tsx` 输出 1
    - `grep -c "export function InsertIcon" src/components/icons.tsx` 输出 1
    - `grep -c "export function RetryIcon" src/components/icons.tsx` 输出 1
    - `grep -c "export function XIcon" src/components/icons.tsx` 输出 1
    - `grep -c "export function AlertIcon" src/components/icons.tsx` 输出 1
    - `grep -c "export function PlusIcon" src/components/icons.tsx` 输出 1
    - `grep -c "export function TrashIcon" src/components/icons.tsx` 输出 1
    - `grep -c "export function CheckIcon" src/components/icons.tsx` 输出 1
    - `npx tsc --noEmit` 通过（0 类型错误）
    - `grep -c "{...base}" src/components/icons.tsx` 至少为 11（全部 12 个图标使用 base spread，StopIcon 另行覆盖 fill/stroke）
  </acceptance_criteria>
  <done>icons.tsx 包含 12 个图标（4 旧 + 8 新），TypeScript 编译无错</done>
</task>

<task type="auto">
  <name>Task 3: 配置 ESLint 禁用 legacy 模型名与 LLM SDK 导入</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js（必须先读，了解现有 ESLint 配置结构，flat config 还是 legacy .eslintrc 格式）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §ESLint 规则（具体规则内容）
  </read_first>
  <action>
读取 `eslint.config.js` 当前内容，在现有规则对象中追加（或新增一个规则块）：

```javascript
// 1. 禁用 legacy DeepSeek 模型名（2026-07-24 退役）— PROV-10
// 使用 no-restricted-syntax 匹配字符串字面量
{
  rules: {
    'no-restricted-syntax': [
      'error',
      {
        selector: "Literal[value=/deepseek-chat|deepseek-reasoner/]",
        message: 'legacy 模型名已废弃（2026-07-24 退役），请使用 deepseek-v4-flash 或 deepseek-v4-pro',
      },
    ],
    // 2. 禁用 LLM SDK 包导入（无后台约束，使用原生 fetch）— PROV-10
    'no-restricted-imports': [
      'error',
      {
        patterns: [
          {
            group: ['openai', 'openai/*'],
            message: '禁止引入 openai SDK，Aster 使用原生 fetch（无后台约束）',
          },
          {
            group: ['@anthropic-ai/*'],
            message: '禁止引入 Anthropic SDK，Aster 使用原生 fetch（无后台约束）',
          },
          {
            group: ['ai', 'ai/*', '@ai-sdk/*'],
            message: '禁止引入 Vercel AI SDK，Aster 使用原生 fetch（无后台约束）',
          },
        ],
      },
    ],
  },
}
```

注意：如果 `eslint.config.js` 已经有 `no-restricted-syntax` 规则，需要将新 selector 合并到已有的数组里，不要覆盖现有规则。

如果使用 TypeScript ESLint flat config（@typescript-eslint），需确认 `no-restricted-imports` 是否应改用 `@typescript-eslint/no-restricted-imports` 以更好地匹配 TypeScript import。优先使用项目已有的规则命名约定。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && echo "import openai from 'openai'" > /tmp/test-eslint.ts && npx eslint --no-eslintrc -c eslint.config.js /tmp/test-eslint.ts 2>&1 | grep -c "no-restricted-imports" || echo "rule check done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no-restricted-imports" eslint.config.js` 输出 >= 1
    - `grep -c "no-restricted-syntax" eslint.config.js` 输出 >= 1
    - `grep -c "openai" eslint.config.js` 输出 >= 1（规则中含 openai 模式）
    - `grep -c "deepseek-chat" eslint.config.js` 输出 >= 1
    - `grep -c "deepseek-reasoner" eslint.config.js` 输出 >= 1
    - `npx tsc --noEmit` 仍然通过（ESLint 配置不影响 TypeScript 编译）
  </acceptance_criteria>
  <done>ESLint 规则配置完成，禁止导入 openai/@anthropic-ai/ai SDK，禁止使用 deepseek-chat/deepseek-reasoner 字面量</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| error.message → UI 展示层 | 错误消息内容不能包含 API Key（T-01-04） |
| eslint.config.js → CI 构建 | ESLint 规则通过 CI 强制执行，本地绕过不能进入 main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | src/errors/index.ts message 字段 | mitigate | 所有新错误类 JSDoc 注释明确标注「T-01-04：message 禁止嵌入 API Key」；代码审阅时 grep 检查 |
| T-02-02 | Tampering | ESLint 配置可被绕过（--no-eslintrc） | accept | 本地开发工具限制；CI pipeline 强制运行 ESLint，不可绕过；风险低 |
| T-02-03 | Information Disclosure | StopIcon 图标使用 fill=currentColor | accept | 纯视觉资源，无安全风险 |
</threat_model>

<verification>
## 整体验证

```bash
# 全部单元测试通过
cd /Users/wb.chen/Documents/Project/Aster && npx vitest run --reporter=verbose 2>&1 | tail -10

# TypeScript 编译无错
npx tsc --noEmit 2>&1 | head -20

# 错误类文件包含全部 8 类
grep "class.*Error.*extends AsterError" src/errors/index.ts

# icons 包含全部 12 个导出函数
grep "^export function.*Icon" src/components/icons.tsx

# ESLint 规则存在
grep "no-restricted-imports\|no-restricted-syntax" eslint.config.js
```
</verification>

<success_criteria>
- src/errors/index.ts 包含 10 个类：AsterError 基类 + 8 个错误子类（含4个新增） + UnsupportedOperationError（adapter层不变）
- RateLimitError 有 `retryAfterSeconds?: number` 可选字段
- icons.tsx 导出 12 个图标函数，全部 TypeScript 类型正确
- eslint.config.js 阻止 openai/@anthropic-ai/ai 导入 + deepseek-chat/deepseek-reasoner 字面量
- vitest 运行 src/errors/index.test.ts 全部通过
</success_criteria>

<output>
完成后在 `.planning/phases/02-provider-settings-onboarding-ux/` 创建 `02-01-SUMMARY.md`，包含：
- 实际创建/修改的文件列表
- 新增的 4 个错误类名称 + code 字段值
- 新增的 8 个图标名称
- ESLint 规则 ID
- 测试结果截图或命令输出
</output>

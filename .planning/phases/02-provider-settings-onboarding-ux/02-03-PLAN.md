---
phase: 02-provider-settings-onboarding-ux
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/providers/types.ts
  - src/providers/registry.ts
  - src/providers/pricing.ts
  - src/providers/registry.test.ts
  - src/providers/pricing.test.ts
autonomous: true
requirements:
  - PROV-01
  - PROV-04
  - COST-01
  - COST-02

must_haves:
  truths:
    - "ProviderRegistry.resolve('chat') 和 resolve('short-task') 返回 LLMConfig"
    - "ProviderRegistry.resolve('vision') 和 resolve('image-gen') 返回 ImageConfig（固定路由到 aihubmix）"
    - "ProviderRegistry.resolve('stock-image') 抛出 ModelNotFoundError（未配置）"
    - "ProviderRegistry.resolve('unknown-kind') 抛出 ModelNotFoundError"
    - "calcCostCny 对 deepseek-v4-flash 返回 number（用 7.25 汇率换算 USD→CNY）"
    - "calcCostCny 对 deepseek-v4-pro 返回 number"
    - "calcCostCny 对自定义 Provider ID 返回 null"
  artifacts:
    - path: "src/providers/types.ts"
      provides: "LLMProvider/ImageProvider/StockImageProvider 接口 + LLMConfig/ImageConfig/TaskKind 类型"
    - path: "src/providers/registry.ts"
      provides: "ProviderRegistry.resolve(taskKind) 路由"
      contains: "class ProviderRegistry"
    - path: "src/providers/pricing.ts"
      provides: "calcCostCny + CNY_PER_USD = 7.25 + 单价表"
      contains: "CNY_PER_USD = 7.25"
  key_links:
    - from: "src/providers/openai-compat.ts（02-04）"
      to: "src/providers/types.ts"
      via: "implements LLMProvider"
      pattern: "implements LLMProvider"
    - from: "src/store/chat.ts（02-05）"
      to: "src/providers/registry.ts"
      via: "ProviderRegistry.resolve('chat')"
      pattern: "ProviderRegistry\\.resolve"
    - from: "src/components/CostBadge.tsx（02-06）"
      to: "src/providers/pricing.ts"
      via: "calcCostCny(usage, providerId)"
      pattern: "calcCostCny"
---

<objective>
建立 Provider 层的接口契约（纯类型）、路由表（ProviderRegistry）和定价计算（pricing）。

这三个文件是 Wave 2 的接口先行层：Provider 实现（02-04）和 Store（02-05）都从此处取类型和路由。

目的：
- `types.ts` 定义接口契约（0 运行时逻辑）
- `registry.ts` 实现 ProviderRegistry.resolve(taskKind)，无自动 fallback（PROV-04）
- `pricing.ts` 实现 calcCostCny，DeepSeek 内置单价 + CNY_PER_USD = 7.25（D-17/D-09）

产出：3 个源文件 + 2 个测试文件
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-provider-settings-onboarding-ux/02-CONTEXT.md
@.planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md
@.planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md

<interfaces>
<!-- 依赖的上游接口 -->
```typescript
// src/errors/index.ts（02-01 完成）
export class ModelNotFoundError extends AsterError { ... } // code: 'MODEL'

// src/adapters/DocumentAdapter.ts（Phase 1 已有）
// registry.ts 需要知道 providerStore（Wave 3）的 defaultLLMProviderId；
// 用函数参数注入而不是直接 import store，避免循环依赖
```

<!-- RESEARCH.md ProviderRegistry 数据结构 -->
```typescript
// src/providers/types.ts
export type TaskKind = 'chat' | 'short-task' | 'vision' | 'image-gen' | 'stock-image';

export interface LLMConfig {
  providerId: string;
  baseURL: string;
  apiKey: string;
  model: string;
}

export interface ImageConfig {
  providerId: string;
  baseURL: string;
  apiKey: string;
  model: string;
}

export interface LLMProvider {
  streamChat(messages: ChatMessage[], config: LLMConfig, signal: AbortSignal): AsyncGenerator<SSEEvent>;
}

export interface ImageProvider {
  generate(prompt: string, size: string, quality: string, config: ImageConfig): Promise<{ b64_json: string; usage?: { input_tokens: number; output_tokens: number } }>;
}

export interface StockImageProvider {
  search(query: string, limit: number): Promise<StockImage[]>;
}

export interface ChatMessage {
  role: 'system' | 'user' | 'assistant';
  content: string;
}
```

<!-- RESEARCH.md ProviderRegistry 路由设计 -->
```typescript
// 路由规则：registry.ts 需要从 providerStore 获取运行时配置
// 解耦方式：resolve(taskKind, getConfig: () => ProviderRuntimeConfig) 函数注入
// 内置 aihubmix 配置（baseURL / model 写死）
const AIHUBMIX_VISION_BASE = 'https://api.aihubmix.com/v1';
const AIHUBMIX_IMAGE_BASE = 'https://api.aihubmix.com/v1';
const AIHUBMIX_VISION_MODEL = 'gpt-4o';
const AIHUBMIX_IMAGE_MODEL = 'gpt-image-1';
```

<!-- RESEARCH.md 定价表 -->
```typescript
// src/providers/pricing.ts
const CNY_PER_USD = 7.25;  // 内置固定汇率（D-17 discretion，ASSUMED 2026-05 均值）
const PROVIDER_PRICING = {
  'deepseek-v4-flash': { input: 0.14, output: 0.28 },    // USD per 1M tokens
  'deepseek-v4-pro':   { input: 1.74, output: 3.48 },    // USD per 1M tokens（注：75%折扣到 2026-05-31）
  // aihubmix 生图按 image token 计费，Phase 2 暂不计价（A2 assumption）
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Provider 接口类型文件（src/providers/types.ts）</name>
  <files>src/providers/types.ts</files>
  <read_first>
    - src/adapters/DocumentAdapter.ts（纯类型文件模式参考：0 imports、JSDoc、discriminated union）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §ProviderRegistry 路由表设计（接口定义形状）
    - .planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md §src/providers/types.ts（Analog: DocumentAdapter.ts）
  </read_first>
  <action>
创建 `src/providers/types.ts`（纯类型文件，0 运行时导入，仅从其他类型文件 import type）：

```typescript
/**
 * src/providers/types.ts — Provider 层接口契约（PROV-01）
 *
 * 本文件为纯类型文件（无运行时逻辑）。
 * 定义 Aster Provider 层的所有接口、配置类型和 TaskKind 枚举。
 *
 * 设计原则（PROV-04）：ProviderRegistry 路由无自动 fallback——
 * resolve 失败直接抛 ModelNotFoundError，由 UI 层展示 MODEL 错误。
 */

import type { SSEEvent } from '../lib/sse';

// ---------------------------------------------------------------------------
// TaskKind — Provider 路由的任务类型（PROV-04）
// ---------------------------------------------------------------------------

/**
 * TaskKind — 决定由哪个 Provider 处理的任务类型标识。
 * - 'chat' / 'short-task': 路由到用户配置的默认 LLM Provider（DeepSeek 或自定义）
 * - 'vision': 路由到 aihubmix 视觉客户端（Phase 0 spike #4 锁定）
 * - 'image-gen': 路由到 aihubmix 生图客户端（Phase 0 spike #4 锁定）
 * - 'stock-image': 暂未配置（v1 未引入图库 Provider），resolve 直接抛 ModelNotFoundError
 */
export type TaskKind = 'chat' | 'short-task' | 'vision' | 'image-gen' | 'stock-image';

// ---------------------------------------------------------------------------
// 配置类型（LLMConfig / ImageConfig）
// ---------------------------------------------------------------------------

/**
 * LLMConfig — OpenAI-compatible LLM 客户端的运行时配置。
 * 从 providerStore 读取；apiKey 从 storage.get 获取，不存在 ProviderConfig 中（分开存储）。
 */
export interface LLMConfig {
  providerId: string;
  baseURL: string;
  apiKey: string;
  model: string;
}

/**
 * ImageConfig — aihubmix 图像客户端的运行时配置。
 * 固定路由到 aihubmix；baseURL / model 由 registry 内置，apiKey 由 providerStore 读取。
 */
export interface ImageConfig {
  providerId: string;
  baseURL: string;
  apiKey: string;
  model: string;
}

// ---------------------------------------------------------------------------
// 消息类型
// ---------------------------------------------------------------------------

/** ChatMessage — OpenAI-compatible 消息格式 */
export interface ChatMessage {
  role: 'system' | 'user' | 'assistant';
  content: string;
}

// ---------------------------------------------------------------------------
// Provider 接口（PROV-01）
// ---------------------------------------------------------------------------

/**
 * LLMProvider — 文本生成 Provider 接口（OpenAI-compatible-first）。
 * streamChat 返回 AsyncGenerator，每次 yield SSEDelta（文本片段）或 SSEUsage（用量）。
 */
export interface LLMProvider {
  streamChat(
    messages: ChatMessage[],
    config: LLMConfig,
    signal: AbortSignal,
  ): AsyncGenerator<SSEEvent>;
}

/**
 * ImageProvider — 图像生成 Provider 接口（aihubmix 专用）。
 * generate 返回 base64 图像数据；usage 字段用 aihubmix 的 input_tokens/output_tokens（非标准）。
 */
export interface ImageProvider {
  generate(
    prompt: string,
    size: string,
    quality: 'high' | 'medium' | 'low' | 'auto',
    config: ImageConfig,
  ): Promise<{
    b64_json: string;
    usage?: { input_tokens: number; output_tokens: number; total_tokens: number };
  }>;
}

/**
 * StockImageProvider — 图库搜索 Provider 接口（v1 未配置，声明留作 Phase 4 引入）。
 */
export interface StockImageProvider {
  search(query: string, limit: number): Promise<StockImage[]>;
}

export interface StockImage {
  id: string;
  url: string;
  thumbnailUrl: string;
  attribution: string;
}

// ---------------------------------------------------------------------------
// ProviderConfig（Settings 存储的 Provider 配置，不含 apiKey）
// ---------------------------------------------------------------------------

/**
 * ProviderConfig — 用户配置的 Provider 信息（存入 storage，不含 apiKey）。
 * apiKey 单独存储在 `storage.get(STORAGE_KEYS.KEY_PREFIX + id)`。
 */
export interface ProviderConfig {
  id: string;
  name: string;
  baseURL: string;
  model: string;
  /** 内置 Provider（deepseek / aihubmix）不可删除 */
  isBuiltIn: boolean;
}
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TaskKind" src/providers/types.ts` 输出 >= 1
    - `grep "TaskKind" src/providers/types.ts` 包含 'chat' | 'short-task' | 'vision' | 'image-gen' | 'stock-image' 五个字面量
    - `grep -c "LLMProvider\|ImageProvider\|StockImageProvider" src/providers/types.ts` 输出 3
    - `grep -c "LLMConfig\|ImageConfig" src/providers/types.ts` 输出 2
    - `grep -c "ProviderConfig" src/providers/types.ts` 输出 >= 1
    - `npx tsc --noEmit` 通过（0 错误）
    - 文件无 `import` 非类型语句（纯类型文件）：`grep -v "^import type" src/providers/types.ts | grep "^import"` 输出为空
  </acceptance_criteria>
  <done>types.ts 包含全部接口和类型，TypeScript 编译通过，无运行时 import</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 ProviderRegistry 路由 + 定价计算，并编写测试</name>
  <files>src/providers/registry.ts, src/providers/pricing.ts, src/providers/registry.test.ts, src/providers/pricing.test.ts</files>
  <read_first>
    - src/adapters/index.ts（switch + throw 工厂模式参考）
    - src/components/formatSelection.ts（纯函数 + exhaustive switch 参考）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §ProviderRegistry 路由表设计
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §成本徽章计算
    - src/errors/index.ts（ModelNotFoundError 导入）
    - src/lib/storage.ts（STORAGE_KEYS、storage.get 导入）
  </read_first>
  <behavior>
    ProviderRegistry 测试（mock providerStore / storage）：
    - resolve('chat', mockGetConfig) 返回 LLMConfig（providerId = 'deepseek'，含 apiKey）
    - resolve('short-task', mockGetConfig) 返回同上
    - resolve('vision', mockGetConfig) 返回 ImageConfig（providerId='aihubmix-vision', baseURL='https://api.aihubmix.com/v1', model='gpt-4o'）
    - resolve('image-gen', mockGetConfig) 返回 ImageConfig（providerId='aihubmix-image', model='gpt-image-1'）
    - resolve('stock-image', mockGetConfig) 抛出 ModelNotFoundError
    - resolve 任何未知 taskKind 抛出 ModelNotFoundError
    - apiKey 为空字符串时 resolve 抛出 KeyInvalidError（Key 未配置）
    
    calcCostCny 测试：
    - calcCostCny({ promptTokens: 1_000_000, completionTokens: 1_000_000 }, 'deepseek-v4-flash') 约等于 (0.14 + 0.28) * 7.25 ≈ 3.045
    - calcCostCny({ promptTokens: 100, completionTokens: 50 }, 'deepseek-v4-pro') 返回 number > 0
    - calcCostCny({ promptTokens: 100, completionTokens: 50 }, 'my-custom-provider') 返回 null
    - calcCostCny({ promptTokens: 0, completionTokens: 0 }, 'deepseek-v4-flash') 返回 0
  </behavior>
  <action>
创建 `src/providers/registry.ts`：

ProviderRegistry 的 resolve 函数需要在运行时读取 providerStore 配置，但不能直接 import Zustand store（循环依赖）。解决方案：通过依赖注入，`resolve(taskKind, getProviderConfig)` 接受一个函数参数获取配置。

内置 aihubmix 配置常量（写死）：
- `AIHUBMIX_BASE_URL = 'https://api.aihubmix.com/v1'`
- `AIHUBMIX_VISION_MODEL = 'gpt-4o'`  
- `AIHUBMIX_IMAGE_MODEL = 'gpt-image-1'`
- aihubmix provider ID: `'aihubmix'`（Key 用 `storage.get(STORAGE_KEYS.KEY_PREFIX + 'aihubmix')`）

```typescript
import type { LLMConfig, ImageConfig, TaskKind, ProviderConfig } from './types';
import { ModelNotFoundError, KeyInvalidError } from '../errors';
import { storage, STORAGE_KEYS } from '../lib/storage';

// 内置 aihubmix 配置（Phase 0 spike #4 锁定视觉/生图路径）
const AIHUBMIX_BASE_URL = 'https://api.aihubmix.com/v1';
const AIHUBMIX_VISION_MODEL = 'gpt-4o';
const AIHUBMIX_IMAGE_MODEL = 'gpt-image-1';
const AIHUBMIX_PROVIDER_ID = 'aihubmix';

export class ProviderRegistry {
  /**
   * 解析 taskKind 到对应的 Provider 配置。
   * 无自动 fallback（PROV-04）：resolve 失败直接抛错，由 UI 层展示错误气泡。
   *
   * @param taskKind 任务类型
   * @param getDefaultLLM 返回当前默认 LLM Provider 配置（由 providerStore 注入，避免循环依赖）
   */
  static resolve(
    taskKind: TaskKind,
    getDefaultLLM: () => ProviderConfig,
  ): LLMConfig | ImageConfig {
    switch (taskKind) {
      case 'chat':
      case 'short-task': {
        const providerCfg = getDefaultLLM();
        const apiKey = storage.get<string>(STORAGE_KEYS.KEY_PREFIX + providerCfg.id);
        if (!apiKey) {
          throw new KeyInvalidError('API Key 未配置，请在设置中填写 Key');
        }
        return {
          providerId: providerCfg.id,
          baseURL: providerCfg.baseURL,
          apiKey,
          model: providerCfg.model,
        } satisfies LLMConfig;
      }

      case 'vision': {
        const apiKey = storage.get<string>(STORAGE_KEYS.KEY_PREFIX + AIHUBMIX_PROVIDER_ID);
        if (!apiKey) {
          throw new KeyInvalidError('aihubmix Key 未配置，请在设置中填写 aihubmix Key');
        }
        return {
          providerId: `${AIHUBMIX_PROVIDER_ID}-vision`,
          baseURL: AIHUBMIX_BASE_URL,
          apiKey,
          model: AIHUBMIX_VISION_MODEL,
        } satisfies ImageConfig;
      }

      case 'image-gen': {
        const apiKey = storage.get<string>(STORAGE_KEYS.KEY_PREFIX + AIHUBMIX_PROVIDER_ID);
        if (!apiKey) {
          throw new KeyInvalidError('aihubmix Key 未配置，请在设置中填写 aihubmix Key');
        }
        return {
          providerId: `${AIHUBMIX_PROVIDER_ID}-image`,
          baseURL: AIHUBMIX_BASE_URL,
          apiKey,
          model: AIHUBMIX_IMAGE_MODEL,
        } satisfies ImageConfig;
      }

      case 'stock-image':
        throw new ModelNotFoundError('stock-image Provider 未配置（v1 不含图库）');

      default: {
        const _exhaustive: never = taskKind;
        throw new ModelNotFoundError(`未知 taskKind: ${String(_exhaustive)}`);
      }
    }
  }
}
```

创建 `src/providers/pricing.ts`（RESEARCH.md §成本徽章计算，字面复制）：

```typescript
/**
 * src/providers/pricing.ts — 成本徽章计算（COST-01/02 / D-09/D-17）
 *
 * 单价写死，不可由用户覆写（D-09）。
 * 自定义 Provider 返回 null（不显示价格，D-17 / COST-02 修订）。
 * 汇率常数 CNY_PER_USD = 7.25（2026-05 均值，非实时，D-17 discretion）。
 * 徽章显示「约 ¥X.XXXX」，「约」字标注非实时汇率（RESEARCH.md §成本徽章计算）。
 */

// 内置单价表（USD per 1M tokens）
// 来源：CLAUDE.md §DeepSeek 单价表（已验证）
const PROVIDER_PRICING: Record<string, { input: number; output: number } | undefined> = {
  'deepseek-v4-flash': { input: 0.14, output: 0.28 },
  'deepseek-v4-pro': { input: 1.74, output: 3.48 },
  // aihubmix 生图按 image token 计费，Phase 2 暂无准确单价（A2 assumption）
  // Phase 4 补充 aihubmix 图像成本计算
};

/** 内置固定汇率（USD → CNY，非实时，偏保守均值） */
export const CNY_PER_USD = 7.25;

/**
 * calcCostCny — 计算本次请求的人民币成本。
 *
 * @returns 成本（CNY，保留 4 位有效精度），或 null（自定义 Provider 不计价）
 */
export function calcCostCny(
  usage: { promptTokens: number; completionTokens: number },
  providerId: string,
): number | null {
  const pricing = PROVIDER_PRICING[providerId];
  if (!pricing) return null; // 自定义 Provider — 只显 token 数，无价格（D-17）

  const usd =
    (usage.promptTokens / 1_000_000) * pricing.input +
    (usage.completionTokens / 1_000_000) * pricing.output;

  return usd * CNY_PER_USD;
}
```

创建对应测试文件，覆盖上述 behavior 列表所有测试用例。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/providers/registry.test.ts src/providers/pricing.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class ProviderRegistry" src/providers/registry.ts` 输出 1
    - `grep -c "AIHUBMIX_BASE_URL.*api.aihubmix.com" src/providers/registry.ts` 输出 >= 1
    - `grep -c "CNY_PER_USD = 7.25" src/providers/pricing.ts` 输出 1
    - `grep -c "deepseek-v4-flash\|deepseek-v4-pro" src/providers/pricing.ts` 输出 2（单价表两条记录）
    - `npx vitest run src/providers/registry.test.ts src/providers/pricing.test.ts` 全部通过
    - `npx tsc --noEmit` 通过
    - calcCostCny 测试验证：1M prompt + 1M completion 对 deepseek-v4-flash 返回 ≈ 3.045（容差 0.001）
  </acceptance_criteria>
  <done>ProviderRegistry.resolve 路由正确，calcCostCny 返回值准确，测试全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProviderRegistry.resolve → apiKey 读取 | 从 storage 读取的 Key 只传给 Provider 客户端，不进入日志 |
| pricing.ts → 显示层 | 计算结果为 number，React 层格式化为字符串展示，无注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | registry.ts: apiKey 通过函数参数传递，可能在调用栈日志出现 | mitigate | apiKey 仅在 registry.ts 内部读取并构造 LLMConfig；LLMConfig 对象不序列化为日志；Provider 客户端收到后只放 Authorization header |
| T-02-10 | Elevation of Privilege | 恶意 ProviderConfig.baseURL（用户自填 URL）被路由到意外服务 | accept | 用户自行配置自定义 Provider；URL 格式校验在 Settings 表单（Task 2 后续计划实现）；此处 registry 信任用户存储的 config |
| T-02-11 | Tampering | 定价常数硬编码，无法被用户篡改 | accept | 符合 D-09 设计意图；用户不需要也不能修改内置单价 |
| T-02-12 | Information Disclosure | KeyInvalidError message 明确说"Key 未配置"——不暴露具体 Key 内容 | mitigate | message 仅说明状态，不含 Key 原文（T-01-04）；测试验证 |
</threat_model>

<verification>
## 整体验证

```bash
cd /Users/wb.chen/Documents/Project/Aster

# 测试通过
npx vitest run src/providers/registry.test.ts src/providers/pricing.test.ts --reporter=verbose 2>&1 | tail -15

# 类型检查
npx tsc --noEmit 2>&1 | head -10

# ProviderRegistry 不含 fallback
grep "fallback" src/providers/registry.ts  # 应返回空（无 fallback）

# 汇率常数确认
grep "CNY_PER_USD" src/providers/pricing.ts
```
</verification>

<success_criteria>
- types.ts 定义 5 个 TaskKind 值、LLMConfig/ImageConfig、LLMProvider/ImageProvider/StockImageProvider 接口、ProviderConfig
- registry.ts 实现 ProviderRegistry.resolve()，chat/short-task 路由 LLM，vision/image-gen 路由 aihubmix，stock-image 抛 ModelNotFoundError
- pricing.ts 包含 CNY_PER_USD = 7.25 常量、deepseek-v4-flash 和 deepseek-v4-pro 两个内置单价
- 测试全绿，calcCostCny 计算值准确
</success_criteria>

<output>
完成后在 `.planning/phases/02-provider-settings-onboarding-ux/` 创建 `02-03-SUMMARY.md`，包含：
- TaskKind 五个值
- AIHUBMIX_VISION_MODEL / AIHUBMIX_IMAGE_MODEL 写死的值
- CNY_PER_USD 常量值
- 测试通过数
</output>

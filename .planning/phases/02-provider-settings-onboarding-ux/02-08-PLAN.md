---
phase: 02-provider-settings-onboarding-ux
plan: 08
type: execute
wave: 5
depends_on:
  - "02-05"
  - "02-06"
  - "02-07"
files_modified:
  - src/App.tsx
  - src/components/InputBar.tsx
  - src/components/ChatStream.tsx
  - src/main.tsx
autonomous: false
requirements:
  - PROV-05
  - PROV-06
  - PROV-07
  - KEY-01
  - KEY-02
  - KEY-04
  - NFR-02
  - NFR-03
  - PANE-02
  - PANE-03

user_setup: []

must_haves:
  truths:
    - "发送按钮在流式生成中变为停止方块（同位置，D-14），生成完变回发送"
    - "输入框在无配置 Key 时允许输入但发送被拦截（KeyInvalidError 触发错误气泡）"
    - "ChatStream 渲染 messages 数组，无消息时显示空态（Phase 1 布局保留）"
    - "App.tsx 顶部齿轮按钮激活，点击打开 SettingsPanel（右侧滑入）"
    - "首启时 OnboardingModal 自动弹出（storage 无 aster:onboarding:seen）"
    - "providerStore.hydrateFromStorage() 在 Office.onReady 后调用，在 render 前"
    - "未配 Key 时顶部显示「去设置填 Key」提示条（D-01 跳过后）"
  artifacts:
    - path: "src/App.tsx"
      provides: "串联所有 Wave 1-5 组件的 shell（Settings + Onboarding + ChatStream + InputBar）"
      contains: "SettingsPanel\|OnboardingModal"
    - path: "src/components/InputBar.tsx"
      provides: "激活的输入栏（发送 / 停止 + 选区胶囊）"
      contains: "sendMessage\|stopStreaming"
    - path: "src/components/ChatStream.tsx"
      provides: "消息列表渲染（无消息显示空态）"
      contains: "useMessages\|ChatBubble"
    - path: "src/main.tsx"
      provides: "hydrateFromStorage 在 onReady 后调用"
      contains: "hydrateFromStorage"
  key_links:
    - from: "InputBar"
      to: "useChatStore.sendMessage"
      via: "发送按钮 onClick"
      pattern: "sendMessage"
    - from: "InputBar"
      to: "useChatStore.stopStreaming"
      via: "停止按钮 onClick（isStreaming=true 时）"
      pattern: "stopStreaming"
    - from: "App.tsx"
      to: "SettingsPanel"
      via: "showSettings state + translateX CSS"
      pattern: "SettingsPanel\|showSettings"
    - from: "App.tsx"
      to: "OnboardingModal"
      via: "showOnboarding state + storage.get(ONBOARDING_SEEN)"
      pattern: "OnboardingModal\|showOnboarding"
---

<objective>
将所有 Wave 1-5 组件串联到 App.tsx，激活 InputBar 和 ChatStream，将 Onboarding/Settings 接入 App.tsx，在 main.tsx 注入 providerStore 水化。

这是 Phase 2 的最后一个执行波次（Wave 5，与 02-07 并行的另一半），也是整个阶段的「胶水层」。

关键修改：
1. `App.tsx`：从占位 shell 变为全功能 shell（Settings 覆盖 + Onboarding modal + 未配 Key 提示条）
2. `InputBar.tsx`：从全 disabled 占位变为激活输入栏（发送↔停止 + 选区胶囊）
3. `ChatStream.tsx`：从空态组件变为消息列表渲染器（有消息时渲染 ChatBubble，无消息保持空态）
4. `main.tsx`：在 Office.onReady 后调用 `hydrateFromStorage()`

这个计划包含一个人工验证检查点（`checkpoint:human-verify`），验证真实 Office 环境中的端到端流程（SC1-SC6）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-provider-settings-onboarding-ux/02-CONTEXT.md
@.planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md
@.planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md

<interfaces>
<!-- 现有文件接口（必须先读） -->
```typescript
// src/App.tsx（当前状态——Phase 1 占位）
// 齿轮按钮：disabled
// 无 Settings/Onboarding

// src/components/InputBar.tsx（当前状态——全禁用占位）
// textarea: disabled, sendButton: disabled, uploadButton: disabled

// src/components/ChatStream.tsx（当前状态——只显示空态）
// 无 messages 渲染逻辑

// src/main.tsx（当前状态）
// Office.onReady 回调：{ Office.initialize = () => {}; root.render(<App />) }
// 需在 render 前调用 hydrateFromStorage()
```

<!-- Wave 5 新增组件接口 -->
```typescript
// src/components/Settings/SettingsPanel.tsx（02-07）
interface SettingsPanelProps { onClose: () => void; initialAnchor?: string; onShowOnboarding?: () => void; }
export default function SettingsPanel(props): ReactElement

// src/components/Onboarding/OnboardingModal.tsx（02-07）
interface OnboardingModalProps { onComplete: () => void; onSkip: () => void; }
export default function OnboardingModal(props): ReactElement

// src/components/ChatBubble.tsx（02-06）
interface ChatBubbleProps { message: Message; onSettings: (anchor?: string) => void; }
export default function ChatBubble(props): ReactElement

// src/components/SelectionPill.tsx（02-06）
interface SelectionPillProps { onDismiss: () => void; }
export default function SelectionPill(props): ReactElement

// src/store/chat.ts（02-05）
export const useChatStore;
export const useMessages;
export const useIsStreaming;

// src/store/providers.ts（02-05）
export function hydrateFromStorage(): void;
export const useProviderStore;

// src/lib/storage.ts（02-02）
export const storage, STORAGE_KEYS;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 串联 App.tsx + 激活 InputBar + 改造 ChatStream + main.tsx 水化</name>
  <files>src/App.tsx, src/components/InputBar.tsx, src/components/ChatStream.tsx, src/main.tsx</files>
  <read_first>
    - src/App.tsx（完整读取现有 shell，了解现有结构）
    - src/components/InputBar.tsx（完整读取，了解 disabled 占位结构）
    - src/components/ChatStream.tsx（完整读取，了解空态结构）
    - src/main.tsx（完整读取，了解 Office.onReady 回调位置）
    - .planning/phases/02-provider-settings-onboarding-ux/02-PATTERNS.md §src/App.tsx（Phase 2 修改描述）
    - .planning/phases/02-provider-settings-onboarding-ux/02-RESEARCH.md §设置页 UI 架构（CSS 覆盖方案）
    - src/styles.css（aster-topbar、aster-shell 等现有 class，避免冲突）
    - src/store/chat.ts（useMessages、useIsStreaming、sendMessage、stopStreaming selector hooks）
    - src/lib/storage.ts（STORAGE_KEYS.ONBOARDING_SEEN 检查 Onboarding 触发条件）
  </read_first>
  <action>
**修改 `src/App.tsx`：**

1. 移除齿轮按钮的 `disabled` 属性
2. 添加 state：
   - `const [showSettings, setShowSettings] = useState(false)`
   - `const [settingsAnchor, setSettingsAnchor] = useState<string | undefined>()`
   - `const [showOnboarding, setShowOnboarding] = useState(false)`
3. 在 `useEffect` 中检查 Onboarding：
   ```typescript
   useEffect(() => {
     const seen = storage.get<boolean>(STORAGE_KEYS.ONBOARDING_SEEN);
     if (!seen) setShowOnboarding(true);
   }, []);
   ```
4. 读取 providerStore 判断是否显示「去设置填 Key」提示条：
   ```typescript
   const defaultProviderId = useProviderStore((s) => s.defaultLLMProviderId);
   const hasKey = useProviderStore((s) => !!s.getKey(s.defaultLLMProviderId));
   ```
5. 打开 Settings 时支持 anchor 深链：`onSettings(anchor) → setSettingsAnchor(anchor); setShowSettings(true)`
6. 传递 `onSettings` 给 ChatStream（透传给 ChatBubble 中的 ErrorBubble）

更新后的 App.tsx 骨架：
```tsx
export default function App(): React.ReactElement {
  const { t } = useLingui();
  const [showSettings, setShowSettings] = useState(false);
  const [settingsAnchor, setSettingsAnchor] = useState<string | undefined>();
  const [showOnboarding, setShowOnboarding] = useState(false);
  const hasKey = useProviderStore((s) => !!s.getKey(s.defaultLLMProviderId));

  const handleOpenSettings = (anchor?: string) => {
    setSettingsAnchor(anchor);
    setShowSettings(true);
  };

  useEffect(() => {
    const seen = storage.get<boolean>(STORAGE_KEYS.ONBOARDING_SEEN);
    if (!seen) setShowOnboarding(true);
  }, []);

  return (
    <div className="aster-shell">
      {/* 未配 Key 时提示条（D-01 跳过后）*/}
      {!hasKey && !showOnboarding && (
        <div className="aster-key-hint" role="alert">
          <Trans>请先配置 API Key</Trans>
          <button className="aster-link-btn" onClick={() => handleOpenSettings('key-input')}>
            <Trans>前往设置 →</Trans>
          </button>
        </div>
      )}

      <div className="aster-topbar">
        <ContextCard />
        <button className="aster-iconbtn" onClick={() => handleOpenSettings()} aria-label={t`设置`}>
          <SettingsIcon />
        </button>
      </div>

      <div className="aster-chat">
        <ChatStream onSettings={handleOpenSettings} />
      </div>

      <InputBar />

      {/* Settings 整页滑入覆盖 */}
      <div className={`aster-settings-overlay${showSettings ? ' is-open' : ''}`}>
        {showSettings && (
          <SettingsPanel
            onClose={() => setShowSettings(false)}
            initialAnchor={settingsAnchor}
            onShowOnboarding={() => { setShowSettings(false); setShowOnboarding(true); }}
          />
        )}
      </div>

      {/* Onboarding Modal */}
      {showOnboarding && (
        <OnboardingModal
          onComplete={() => setShowOnboarding(false)}
          onSkip={() => setShowOnboarding(false)}
        />
      )}
    </div>
  );
}
```

**修改 `src/components/InputBar.tsx`：**

1. 移除所有 `disabled` 属性
2. 添加消息状态（`useMessages` / `useIsStreaming`）
3. 添加输入 state：`const [text, setText] = useState('')`
4. 添加选区 state（用于 SelectionPill dismiss）：`const [selectionDismissed, setSelectionDismissed] = useState(false)`
5. 发送/停止按钮（D-14 发送键原地变停止方块）：
   ```tsx
   const isStreaming = useIsStreaming();
   <button className="aster-send" onClick={isStreaming ? stopStreaming : handleSend} aria-label={isStreaming ? t`停止` : t`发送`}>
     {isStreaming ? <StopIcon /> : <SendIcon />}
   </button>
   ```
6. 选区胶囊（D-15）在输入框上方（或工具行）展示：
   ```tsx
   {!selectionDismissed && <SelectionPill onDismiss={() => setSelectionDismissed(true)} />}
   ```
7. `handleSend` 函数：
   ```typescript
   async function handleSend() {
     const prompt = text.trim();
     if (!prompt || isStreaming) return;
     setText('');
     setSelectionDismissed(false); // 发送后重置，下条消息重新附带
     const sel = !selectionDismissed ? await adapter.getSelection() : undefined;
     await sendMessage(prompt, sel);
   }
   ```
8. 文件上传按钮保持 disabled（Phase 3 实现），但用 `title={t`文件上传即将开放`}` 而非 aria-label 混用

**修改 `src/components/ChatStream.tsx`：**

Props 新增：`{ onSettings: (anchor?: string) => void }`。

逻辑：
- `const messages = useMessages();`
- `if (messages.length === 0)` → 渲染现有空态（Phase 1 布局保留）
- `if (messages.length > 0)` → 渲染消息列表：
  ```tsx
  <div className="aster-messages" ref={scrollRef}>
    {messages.map((m) => <ChatBubble key={m.id} message={m} onSettings={onSettings} />)}
  </div>
  ```
- 新消息时自动滚到底部：`useEffect(() => { scrollRef.current?.scrollTo({top: 9999, behavior:'smooth'}); }, [messages.length]);`

**修改 `src/main.tsx`：**

在 `Office.onReady` 回调内、`root.render()` 之前，调用 `hydrateFromStorage()`：
```typescript
import { hydrateFromStorage } from './store/providers';

Office.onReady(() => {
  Office.initialize = () => {};
  hydrateFromStorage();  // 从 storage 恢复 Provider 配置（KEY-01/05）
  root.render(...);
});
```

在 `src/styles.css` 追加未配 Key 提示条样式：
```css
.aster-key-hint { background: var(--error-bg); border-bottom: 1px solid var(--error); padding: var(--sp-2) var(--sp-4); font-size: 12px; color: var(--error); display: flex; justify-content: space-between; align-items: center; }
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "showSettings\|SettingsPanel" src/App.tsx` 输出 >= 2
    - `grep -c "showOnboarding\|OnboardingModal" src/App.tsx` 输出 >= 2
    - `grep -c "hasKey\|aster-key-hint" src/App.tsx` 输出 >= 1（未配 Key 提示条）
    - `grep -c "disabled" src/App.tsx` 输出 0（齿轮按钮不再 disabled）
    - `grep -c "StopIcon\|stopStreaming\|isStreaming" src/components/InputBar.tsx` 输出 >= 2（D-14 停止键）
    - `grep -c "sendMessage\|handleSend" src/components/InputBar.tsx` 输出 >= 1（发送触发 store）
    - `grep -c "disabled" src/components/InputBar.tsx` 输出 <= 1（上传按钮仍 disabled，其余激活）
    - `grep -c "useMessages\|ChatBubble\|messages.length" src/components/ChatStream.tsx` 输出 >= 2
    - `grep -c "hydrateFromStorage" src/main.tsx` 输出 1（onReady 后调用）
    - `npx tsc --noEmit` 通过（0 错误）
    - `npx vitest run` 已有测试全部通过（不引入回归）
  </acceptance_criteria>
  <done>App.tsx 串联完整（Settings + Onboarding + 提示条），InputBar 激活（发送/停止/选区胶囊），ChatStream 渲染消息列表，main.tsx hydrateFromStorage 注入</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 全部组件已串联完成：
    - SSE 流式 LLM 调用（DeepSeek 或自定义 Provider）
    - Onboarding 2 步（Key 配置 + 功能卡）
    - Settings 整页滑入面板（Provider 管理）
    - 发送/停止切换按钮（D-14）
    - 错误气泡 + CTA（8 类错误）
    - 成本徽章（¥ 或仅 token 数）
    - 选区胶囊（自动附带 + × 关闭）
    - 「插入到文档」写回（三宿主 text 类型）
    - partitioned localStorage Key 持久化
  </what-built>
  <how-to-verify>
    在 Office for Web 真实环境（Edge 或 Chrome 最新版）验证以下场景（参考 .claude/skills/office-addin-browser-uat/SKILL.md）：

    **SC1 — Onboarding（2 步引导）：**
    1. 清除浏览器缓存（或用隐私模式）后打开 PowerPoint for Web，sideload manifest
    2. 应看到 Onboarding Modal 弹出（Step 1：Provider 预选 DeepSeek）
    3. 填入有效 DeepSeek Key（sk-xxx），点「下一步」
    4. 看到 Step 2 PPT 功能介绍卡（只有 PPT 卡，D-03）
    5. 点「开始使用」，modal 关闭
    6. 再次打开 Task Pane，Onboarding 不再弹出（storage 已标记）
    7. 验证 Settings 中有「重看引导」入口（D-04）

    **SC2 — 流式对话 + 停止 + 成本徽章：**
    1. 在输入框输入「你好，介绍一下 Aster」，发送
    2. 观察 AI 输出逐字流式渲染（首 token 应 ≤ 2s，DeepSeek 网络正常时）
    3. 生成中，发送键应变为停止方块（D-14）——点击停止，生成中断，已生成内容保留
    4. 完整生成后，气泡下方出现「本次：N token · 约 ¥X.XXXX」徽章
    5. 隐藏 Task Pane（关闭侧边栏）再打开——成本徽章应冻结（abort 生效）

    **SC3 — 错误 UX（故意用错误 Key）：**
    1. 在 Settings 中把 DeepSeek Key 改为 `sk-invalid`
    2. 发送一条消息
    3. 应看到错误气泡：「API Key 无效」+「前往设置更新 Key →」按钮
    4. 点击 CTA，Settings 打开并 focus 到 Key 输入字段（D-12 深链）
    5. 错误气泡有「重试」按钮，填好 Key 后点重试，原 prompt 重新发送（D-11）

    **SC4 — Settings Provider 管理：**
    1. 打开 Settings，新增一个自定义 Provider（baseURL: https://example.com, model: test, Key: xxx）
    2. 保存后列表出现新 Provider
    3. 编辑该 Provider，修改 model 字段，保存
    4. 删除该 Provider，列表恢复
    5. 尝试删除内置 DeepSeek——删除按钮应 disabled，不可操作

    **SC5 — 选区胶囊（D-15）：**
    1. 在 PPT 中选中第 2 张 slide，Task Pane 输入框区域显示「第 2 张 slide」胶囊
    2. 点击胶囊上的 ×，胶囊消失（此次消息不附带选区）
    3. 在 Settings 里关闭「自动附带选区内容」开关
    4. 返回聊天——选区胶囊不再出现

    **SC6 — 插入到文档（三宿主）：**
    PPT：发送一条消息，AI 回复后点「插入到文档」——文字应写入当前 slide 第一个文本框
    Excel：切换到 Excel for Web，发送消息，点「插入到文档」——文字应写入选中单元格
    Word：切换到 Word for Web，发送消息，点「插入到文档」——文字应替换选中文本（或在光标处插入）

    **自定义 Provider 成本徽章：**
    在 Settings 添加自定义 Provider 并设为默认，发送消息——徽章只显「本次：N token」无 ¥ 价格（D-17）
  </how-to-verify>
  <resume-signal>在三宿主（PPT/Excel/Word for Web）各测试上述场景后，输入「验证通过」或描述失败项（失败时继续执行 gap closure）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InputBar → chatStore.sendMessage | 用户输入 + 选区上下文从 UI 传入 store，再传给 Provider |
| App.tsx → SettingsPanel | settingsAnchor 字符串从 ErrorBubble CTA 传入，用于 UI 字段 focus，无安全风险 |
| main.tsx → hydrateFromStorage | Office.onReady 后才调用，确保 Office.context.partitionKey 可用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | Information Disclosure | InputBar: 用户输入的 prompt 与选区内容合并发送给 Provider | accept | 这是核心功能设计；SelectionPill 的 × 允许用户去掉选区；autoAttach 全局开关允许完全关闭 |
| T-02-30 | Denial of Service | hydrateFromStorage 在 onReady 前调用时 Office.context 不可用 | mitigate | storage.ts 的 prefixedKey 对 Office.context.partitionKey 做 undefined 检查；main.tsx 严格在 onReady 回调内调用 |
| T-02-31 | Tampering | ChatStream 滚动到底部（scrollTo）可能被 AI 内容操控 | accept | scrollTo({top: 9999}) 是简单数值，不受 AI 内容影响；react-markdown 不注入脚本 |
| T-02-32 | Information Disclosure | aster-key-hint 提示条暴露 Key 未配置状态 | accept | 状态已经在 Task Pane 可见；提示条是引导用户的 UX 功能，无额外隐私泄露 |
</threat_model>

<verification>
## 整体验证

```bash
cd /Users/wb.chen/Documents/Project/Aster

# 完整测试套件
npx vitest run --reporter=verbose 2>&1 | tail -20

# TypeScript 编译
npx tsc --noEmit 2>&1 | head -10

# 确认 hydrateFromStorage 在 main.tsx
grep "hydrateFromStorage" src/main.tsx

# 确认发送/停止切换
grep "StopIcon\|isStreaming" src/components/InputBar.tsx

# 确认 Settings + Onboarding 连接
grep "SettingsPanel\|OnboardingModal" src/App.tsx

# 构建验证（确保打包不出错）
npm run build 2>&1 | tail -20
```
</verification>

<success_criteria>
- App.tsx：Settings 整页滑入 + Onboarding 首启弹出 + 未配 Key 提示条 + 深链支持
- InputBar：发送/停止原地切换（D-14）+ 选区胶囊（D-15）+ autoAttach 开关
- ChatStream：有消息渲染 ChatBubble，无消息保持 Phase 1 空态
- main.tsx：hydrateFromStorage 在 onReady 后、render 前调用
- 全套 vitest 通过（包含 Wave 1-4 所有单元测试）
- `npm run build` 构建成功（bundle 不超过 1MB，NFR-01）
- 人工 UAT 检查点：SC1-SC6 在 PPT/Excel/Word for Web 验证通过
</success_criteria>

<output>
完成后在 `.planning/phases/02-provider-settings-onboarding-ux/` 创建 `02-08-SUMMARY.md`，包含：
- 串联完成的组件树图
- 人工 UAT 结果（SC1-SC6 各项 Pass/Fail）
- 首 token 延迟实测值（SC2）
- 任何需要 gap closure 的失败项
</output>

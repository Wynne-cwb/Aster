---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/tools/write/excel.test.ts
  - src/agent/tools/write/ppt.test.ts
  - src/agent/tools/write/word.test.ts
  - src/components/ChatStream.test.tsx
  - src/components/Onboarding/OnboardingModal.test.tsx
  - src/agent/system-prompt.test.ts
autonomous: true
requirements:
  - TOOL-03
  - ONB-02
  - ONB-03

must_haves:
  truths:
    - "所有新 write tool 的 unit test 文件存在，Wave 1-3 可直接运行（即使 RED）"
    - "inverse 方法 Record 签名守门覆盖新 tool case（含 set_shape_text）"
    - "chips host 路由 test stub 存在"
    - "onboarding 单步 test stub 存在"
    - "system prompt per-host 断言 stub 存在"
  artifacts:
    - path: src/agent/tools/write/excel.test.ts
      provides: "insert_chart/apply_formula/set_cell inverse test stubs"
    - path: src/agent/tools/write/ppt.test.ts
      provides: "set_shape_property/move_shape/set_shape_text inverse test stubs"
    - path: src/agent/tools/write/word.test.ts
      provides: "insert_paragraph/replace_paragraph expected_state test stubs"
    - path: src/components/ChatStream.test.tsx
      provides: "host-specific chips render test stubs"
    - path: src/components/Onboarding/OnboardingModal.test.tsx
      provides: "single-step flow + ONBOARDING_SEEN write test stubs"
  key_links:
    - from: src/agent/tools/write/excel.test.ts
      to: src/agent/tools/write/excel.ts
      via: "import + vi.mock of ExcelAdapter"
      pattern: "importFrom.*write/excel"
    - from: src/agent/tools/write/ppt.test.ts
      to: PptAdapter.setShapeText / restoreShapeText
      via: "vi.mock PptAdapter，断言 reverse.tool === 'restore_shape_text' + before_text 字段"
      pattern: "restore_shape_text"
    - from: src/agent/operationLog.integration.test.ts
      to: new inverse methods
      via: "Record<string,unknown> args assertion"
      pattern: "Record.*unknown"
---

<objective>
Wave 0：建立所有 Phase 6 测试桩，让 Wave 1-3 的实现在 RED→GREEN 节奏中进行。

Purpose: 先写测试，确保 inverse 方法签名正确、chips host 路由正确、onboarding 单步路径正确，防止项目复发故障（Phase 5 Word 位置签名致全部 inverse 挂掉）。set_shape_text 是 TOOL-03 P1，ppt.test.ts 必须包含其 inverse before-image 测试。
Output: 6 个测试文件（5 个新建 + 1 个扩展），初始运行 RED（因为实现不存在），Wave 1+ 实现后变 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md
@.planning/phases/06-write-tools-killer-scenarios/06-VALIDATION.md

<interfaces>
<!-- 项目地雷：inverse 方法必须用 Record<string,unknown> 签名 -->
<!-- Source: src/adapters/WordAdapter.ts -->
```typescript
// 正确范式（Phase 5 已验证）：
async deleteParagraphByContent(args: Record<string, unknown>): Promise<void> {
  const text = args.text as string;
  // ...
}
// 错误模式（Phase 5 UAT 最严重 bug，绝不重蹈）：
// async deleteParagraphByContent(text: string): Promise<void> { ... }
```

<!-- Source: src/agent/operationLog.integration.test.ts（已有，需扩展 case）-->
```typescript
// 现有 Excel mock 范式（integration test lines 47-68）
function mockExcel(): ReturnType<typeof vi.fn> {
  const setValues = vi.fn();
  const range = { load: vi.fn(), address: 'Sheet1!A1:B2', ... };
  (global as Record<string, unknown>).Excel = {
    run: vi.fn(async (cb) => cb({ workbook: { ... }, sync: vi.fn() })),
  };
  return setValues;
}
```

<!-- Source: src/agent/tools/write/excel.ts (set_range_values 范式) -->
```typescript
export const setRangeValues: ToolDef<SetRangeValuesArgs> = {
  name: 'set_range_values',
  kind: 'write',
  humanLabel: ({ address }) => `写入单元格区域 ${address}`,
  async execute({ address, values }, ctx): Promise<ToolResult> {
    const { beforeImage } = await (ctx.adapter as ExcelAdapter).setRangeValues(address, values);
    const reverse: ReverseDescriptor = {
      tool: 'overwrite_range',
      args: { address: beforeImage.address, values: beforeImage.values }, // Record 对象
    };
    return { ok: true, data: {}, reverse, postState: { kind: 'excel_range', content: { address, values } } };
  },
};
```

<!-- set_shape_text 预期 reverse 结构（Plan 06-06 确定）-->
```typescript
// set_shape_text execute 返回：
// reverse.tool = 'restore_shape_text'
// reverse.args = { slide_index, shape_id, before_text: '<旧文本>' }（Record 对象）
// postState.kind = 'ppt_shape'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 Excel/PPT/Word write tool 测试桩（RED 阶段）</name>
  <files>src/agent/tools/write/excel.test.ts, src/agent/tools/write/ppt.test.ts, src/agent/tools/write/word.test.ts</files>
  <read_first>
    - src/agent/tools/write/excel.ts（setRangeValues 完整范式，作为模板）
    - src/agent/tools/write/ppt.ts（insertSlide 完整范式，作为模板）
    - src/agent/tools/write/word.ts（appendParagraph 完整范式，作为模板）
    - src/agent/operationLog.integration.test.ts（Excel/PPT/Word mock 范式，lines 47-68 / 85-115 / 119-147）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（Test File Pattern Assignments 段）
  </read_first>
  <behavior>
    - excel.test.ts（3 个 describe 块）：
      · insert_chart：mock ExcelAdapter.insertChart 返回 { chartName: '图表 1' }；断言 tool.execute 返回 reverse.tool === 'delete_chart_by_name'，reverse.args 是 Record 对象含 chartName，mutated 含 chartName
      · apply_formula：mock ExcelAdapter.applyFormula 返回 { beforeImage: { address: 'A1', values: [['旧值']] } }；断言 reverse.tool === 'overwrite_range'，reverse.args 是 Record 对象含 address+values
      · set_cell：同 apply_formula 范式，reverse.args 含 address+values
    - ppt.test.ts（3 个 describe 块）：
      · set_shape_property：mock PptAdapter.setShapeProperty 返回 before-image；断言 reverse.tool === 'restore_shape_property'，reverse.args 是 Record 对象含完整 before-image 字段（fill_type/fill_color/line_color/line_weight/line_visible/width/height）
      · move_shape：mock PptAdapter.moveShape 返回 { beforeLeft, beforeTop }；断言 reverse.tool === 'restore_shape_geometry'，reverse.args 含 slide_index/shape_id/left/top
      · set_shape_text（TOOL-03 P1）：mock PptAdapter.setShapeText 返回 { beforeText: '旧文字' }；断言：
        - reverse.tool === 'restore_shape_text'
        - reverse.args 是 Record 对象，含 before_text 字段（值为 '旧文字'）
        - reverse.args 含 slide_index + shape_id
        - postState.kind === 'ppt_shape'
    - word.test.ts（4 个 describe 块）：
      · insert_paragraph：断言 reverse.args 是 Record 对象
      · replace_paragraph：extra case: expected_state 传入且不匹配 → execute 返回 ok: false 或 throw error（INVALID_ARGS）
      · insert_text_at_cursor：断言 reverse.args 是 Record 对象
      · replace_selection：断言 reverse.args 是 Record 对象；humanLabel 存在且返回中文字符串
  </behavior>
  <action>
    三个文件全部 vi.mock adapter（不引入真实 Office.js）；用 vi.fn() 模拟 adapter 方法；用 describe/it/expect 结构。
    初始运行 RED（实现不存在），这是期望行为。

    excel.test.ts 顶部：
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    // 注：实际 tool 实现在 Wave 2 才建立，当前 import 可先 skip/todo
    ```

    wave 0 特殊处理：如果 Phase 6 write tool 文件尚不存在（Wave 2 才建），用 `it.todo('...')` 占位以便 vitest 不报模块缺失。或者只 stub adapter mock，用 `describe.skip` 直到 Wave 2 实现就位后取消 skip。
    推荐方式：用 `describe.skip` 包裹，注释「Wave 2 解锁」，保证 npm test -- --run 此时不 RED，Wave 2 后取消 skip。

    **ppt.test.ts set_shape_text describe 块要点：**
    ```typescript
    // Wave 2/3 解锁
    describe.skip('set_shape_text', () => {
      it('before-image text restore — reverse descriptor 含 before_text', async () => {
        const mockAdapter = {
          setShapeText: vi.fn().mockResolvedValue({ beforeText: '旧文字' }),
        };
        // ... 调用 setShapeText tool.execute
        expect(result.reverse.tool).toBe('restore_shape_text');
        expect(result.reverse.args).toMatchObject({
          slide_index: expect.any(Number),
          shape_id: expect.any(String),
          before_text: '旧文字',
        });
        // Record 对象守门（非位置参）：
        expect(typeof result.reverse.args).toBe('object');
        expect(result.postState?.kind).toBe('ppt_shape');
      });
    });
    ```
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/excel.test.ts src/agent/tools/write/ppt.test.ts src/agent/tools/write/word.test.ts</automated>
  </verify>
  <done>
    三个测试文件存在；describe.skip 包裹时全通过（Wave 2 解锁后转为真正 RED→GREEN）；
    ppt.test.ts 含 set_shape_text describe 块，断言 reverse.tool === 'restore_shape_text' + before_text 字段；
    grep "restore_shape_text" src/agent/tools/write/ppt.test.ts 存在；
    文件内有完整的 test case 结构（mock + 断言），注释说明 Wave 依赖。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 ChatStream.test.tsx / OnboardingModal.test.tsx / system-prompt.test.ts 扩展（RED 阶段）</name>
  <files>src/components/ChatStream.test.tsx, src/components/Onboarding/OnboardingModal.test.tsx, src/agent/system-prompt.test.ts</files>
  <read_first>
    - src/components/ChatStream.tsx（empty-state 块 lines 299-314，useAdapter 使用方式）
    - src/components/Onboarding/OnboardingModal.tsx（全文，step state + Step2Guide 渲染位置）
    - src/components/Onboarding/Step1Keys.tsx（全文，onNext prop + handleNext）
    - src/agent/system-prompt.ts（全文，buildSystemPrompt 当前实现）
    - src/agent/system-prompt.test.ts（若已存在，扩展；否则新建）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（Test File Pattern Assignments 段，ChatStream/OnboardingModal/system-prompt 部分）
    - .planning/phases/06-write-tools-killer-scenarios/06-UI-SPEC.md（chips copy、onboarding CTA 文案）
  </read_first>
  <behavior>
    - ChatStream.test.tsx（3 个 it）：
      · host='ppt' 时渲染 PPT chips（含「帮我做一份 Q3 销售复盘 PPT」字样）
      · host='excel' 时渲染 Excel chips（含「帮我清洗这份数据」字样）
      · host='word' 时渲染 Word chips（含「帮我把整篇文档润色一遍」字样）
      · 未知 host → 不报错，不渲染 chips（或渲染空 .suggestions）
      · chip 点击 → setInputValue 被调用（不自动 send）
    - OnboardingModal.test.tsx（3 个 it）：
      · 渲染后 CTA 显示「开始使用」而非「下一步」
      · Step2Guide 不在 DOM 中（queryByText 相关文案 = null）
      · Step1 完成后 ONBOARDING_SEEN 写入 storage（mock storage.set）
    - system-prompt.test.ts 扩展（3 个新 it，添加到现有文件末尾）：
      · buildSystemPrompt('ppt') 含 'list_slides' + 'batch'
      · buildSystemPrompt('excel') 含 'get_used_range_summary'
      · buildSystemPrompt('word') 含 'replace_paragraph'
      · buildSystemPrompt('ppt') 不含「你通过用户授权的 API Key」（D-07 去技术化验证）
  </behavior>
  <action>
    ChatStream.test.tsx：mock AdapterContext，提供 capabilities().host；用 @testing-library/react render；
    chips 实现在 Wave 3，当前用 describe.skip 占位，注释「Wave 3 解锁」。

    OnboardingModal.test.tsx：mock storage（vi.mock('../lib/storage', ...)）；渲染 OnboardingModal 并 fireEvent.click CTA；
    当前 describe.skip，Wave 3 解锁后 CTA 文案测试会 RED（因为文案还是「下一步」）。

    system-prompt.test.ts：直接 import buildSystemPrompt，追加 it（不 skip，当前实现无 per-host 段所以 RED，Wave 3 实现后变 GREEN）。
  </action>
  <verify>
    <automated>npm test -- --run src/components/ChatStream.test.tsx src/components/Onboarding/OnboardingModal.test.tsx src/agent/system-prompt.test.ts</automated>
  </verify>
  <done>
    三个测试文件存在；skip 的测试 pending（不计入失败）；system-prompt.test.ts 新增的 3 个 it 当前 RED（预期，实现在 Wave 3）；
    `npm test -- --run` 总体退出码不因这些新文件而新增 FAIL（skip 不算 fail）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → source | 测试文件仅 mock adapter，不调用真实 Office.js，无沙箱外调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | test mocks | accept | Wave 0 仅测试桩，无业务逻辑，威胁面极低 |
</threat_model>

<verification>
- `npm test -- --run` 通过（skip 不报失败；system-prompt 新增 it 可 RED 但命令行统计为 fail → 若 RED 影响 CI 则也用 skip，注释「Wave 3 实现后取消 skip」）
- 6 个文件均存在于预期路径
- grep 确认 excel.test.ts 含 `delete_chart_by_name`
- grep 确认 ppt.test.ts 含 `restore_shape_text`（set_shape_text 覆盖）
- grep 确认 word.test.ts 含 `Record<string, unknown>`（inverse 签名保护）
- grep 确认 ChatStream.test.tsx 含「帮我做一份 Q3 销售复盘 PPT」
</verification>

<success_criteria>
Wave 0 完成：所有测试文件存在，test 套件不新增 ERROR（skip 不算 fail），ppt.test.ts 含 set_shape_text 的 inverse before-image 测试桩，系统 prompt 新增断言结构存在。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-01-SUMMARY.md`
</output>

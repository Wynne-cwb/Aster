---
phase: "06"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/adapters/ExcelAdapter.ts
autonomous: true
requirements:
  - TOOL-03

must_haves:
  truths:
    - "ExcelAdapter 新增 insertChart（返回 chartName）、deleteChartByName（Record 签名）、applyFormula（before-image）、setCell（before-image）方法"
    - "所有 inverse 方法均用 args: Record<string, unknown> 签名，不用位置参"
    - "所有方法 proxy 不出 Excel.run 闭包（A-06）"
  artifacts:
    - path: src/adapters/ExcelAdapter.ts
      provides: "4 个新 adapter 方法（insertChart/deleteChartByName/applyFormula/setCell）"
      exports: ["insertChart", "deleteChartByName", "applyFormula", "setCell"]
  key_links:
    - from: ExcelAdapter.insertChart
      to: Excel.run → chart.load('name') → sync
      via: "chart.name 作为 inverse 句柄"
      pattern: "chart\\.load\\(\\['name'\\]\\)"
    - from: ExcelAdapter.deleteChartByName
      to: getItemOrNullObject + isNullObject
      via: "防 chart 已删 guard"
      pattern: "getItemOrNullObject"
---

<objective>
新增 ExcelAdapter 的 4 个 write/inverse 方法，作为 Wave 3 Excel write tools 的 adapter 层基础。

Purpose: Wave 3 的 insert_chart/apply_formula/set_cell ToolDef 需要这些 adapter 方法；先做 adapter 保证 Wave 3 executor 有明确的 adapter 接口。
Output: ExcelAdapter.ts 新增 4 个方法（insertChart + deleteChartByName + applyFormula + setCell），全部遵循 Phase 5 before-image 范式 + Record 签名守门。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md

<interfaces>
<!-- Source: src/adapters/ExcelAdapter.ts — setRangeValues（before-image two-sync 范式）-->
```typescript
// setRangeValues（lines 309-334）— 新方法的完整模板：
async setRangeValues(address: string, values: unknown[][]): Promise<{ beforeImage: { address: string; values: unknown[][] } }> {
  try {
    return await Excel.run(async (ctx) => {
      const range = ctx.workbook.worksheets.getActiveWorksheet().getRange(address);
      range.load(['values', 'address']);
      await ctx.sync();  // sync 1: load before-image
      const beforeImage = { address: range.address as string, values: range.values as unknown[][] };
      range.values = values;
      await ctx.sync();  // sync 2: 写入
      return { beforeImage };
    });
  } catch (err) {
    throw new HostApiError('Excel setRangeValues 失败', err);
  }
}

// overwriteRange（inverse，lines 355-368）— inverse Record 签名范式：
async overwriteRange(args: Record<string, unknown>): Promise<void> {
  const address = args.address as string;
  const values = args.values as unknown[][];
  // ... Excel.run 覆写
}
```

<!-- Source: RESEARCH.md — insertChart GO 裁定 -->
```typescript
// insertChart 完整实现（RESEARCH.md lines 262-289）
async insertChart(dataRange: string, chartType: string): Promise<{ chartName: string }> {
  return await Excel.run(async (ctx) => {
    const sheet = ctx.workbook.worksheets.getActiveWorksheet();
    const range = sheet.getRange(dataRange);
    const chart = sheet.charts.add(chartType as Excel.ChartType, range, Excel.ChartSeriesBy.auto);
    chart.load(['name']);
    await ctx.sync();
    return { chartName: chart.name as string };
  });
}

// deleteChartByName（inverse，Record 签名，getItemOrNullObject guard）
async deleteChartByName(args: Record<string, unknown>): Promise<void> {
  const chartName = args.chartName as string;
  await Excel.run(async (ctx) => {
    const sheet = ctx.workbook.worksheets.getActiveWorksheet();
    const chart = sheet.charts.getItemOrNullObject(chartName);
    chart.load('isNullObject');
    await ctx.sync();
    if (!chart.isNullObject) { chart.delete(); await ctx.sync(); }
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExcelAdapter 新增 insertChart + deleteChartByName</name>
  <files>src/adapters/ExcelAdapter.ts</files>
  <read_first>
    - src/adapters/ExcelAdapter.ts（全文，找 setRangeValues + overwriteRange 的精确位置，看 class 结构和 imports）
    - .planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md（insertChart GO 裁定 + 完整代码，lines 208-310）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（ExcelAdapter 段，insertChart/deleteChartByName 代码）
  </read_first>
  <action>
    在 ExcelAdapter class 末尾（setRangeValues/overwriteRange 之后）新增两个方法：

    1. `insertChart(dataRange: string, chartType: string): Promise<{ chartName: string }>`
       - Excel.run 内：getActiveWorksheet → getRange(dataRange) → charts.add(chartType, range, auto)
       - chart.load(['name']) → sync → return { chartName: chart.name as string }
       - 默认 chartType = 'ColumnClustered'（调用方传入，可不在 adapter 层设默认）
       - try/catch: if HostApiError rethrow；else throw new HostApiError('Excel insertChart 失败', err)

    2. `deleteChartByName(args: Record<string, unknown>): Promise<void>` ← 必须用 Record 签名
       - const chartName = args.chartName as string;
       - Excel.run 内：getActiveWorksheet → charts.getItemOrNullObject(chartName)
       - chart.load('isNullObject') → sync → if (!chart.isNullObject) { chart.delete(); sync }
       - 静默跳过（chart 已不存在 = undo 重放时正常情况）
       - try/catch 同上模式
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/excel.test.ts</automated>
  </verify>
  <done>
    ExcelAdapter.ts 含 insertChart + deleteChartByName 方法；
    grep -c "insertChart" src/adapters/ExcelAdapter.ts 返回 ≥2（定义 + 返回类型）；
    grep -c "deleteChartByName" src/adapters/ExcelAdapter.ts 返回 ≥1；
    grep "getItemOrNullObject" src/adapters/ExcelAdapter.ts 存在（Pitfall 1 防御）；
    test 套件不新增 FAIL。
  </done>
</task>

<task type="auto">
  <name>Task 2: ExcelAdapter 新增 applyFormula + setCell</name>
  <files>src/adapters/ExcelAdapter.ts</files>
  <read_first>
    - src/adapters/ExcelAdapter.ts（上一 task 修改后的状态，了解 setRangeValues before-image 范式 lines 309-334）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（applyFormula/setCell 代码段，lines 313-335）
  </read_first>
  <action>
    继续在 ExcelAdapter class 末尾新增两个方法：

    3. `applyFormula(cell: string, formula: string): Promise<{ beforeImage: { address: string; values: unknown[][] } }>`
       - Excel.run 内：getActiveWorksheet → getRange(cell)
       - range.load(['values', 'address', 'formulas']) → sync（抓 before-image）
       - beforeImage = { address: range.address, values: range.values }
       - range.formulas = [[formula]] → sync（写入）
       - return { beforeImage }
       - try/catch 同范式

    4. `setCell(cell: string, value: unknown): Promise<{ beforeImage: { address: string; values: unknown[][] } }>`
       - 与 applyFormula 几乎相同，但写 range.values = [[value]] 而非 range.formulas
       - range.load(['values', 'address']) → sync → beforeImage → range.values = [[value]] → sync
       - try/catch 同范式

    注意：applyFormula/setCell 的 inverse 都复用已有的 overwriteRange（args: Record）。不需要新增 inverse 方法。
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/excel.test.ts</automated>
  </verify>
  <done>
    ExcelAdapter.ts 含 applyFormula + setCell 方法；
    grep -c "applyFormula\|setCell" src/adapters/ExcelAdapter.ts 返回 ≥4（各含定义+调用）；
    `npm test -- --run` 不新增 FAIL。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool args → Excel.run | LLM 传入的 dataRange/cell/formula 经 ToolDef parameters schema 验证后进入 adapter |
| Excel.run → proxy | proxy 在 Excel.run 内消费完毕，不出闭包（A-06）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | chart.name collision | mitigate | getItemOrNullObject + isNullObject guard；replay engine catch → skipped_error |
| T-06-02-02 | Tampering | LLM 传入超大 dataRange | accept | Excel.run 自身会对无效 range 抛 HostApiError，LLM 收到 error 重评估 |
| T-06-02-03 | Information Disclosure | API Key | accept | adapter 方法无 Key 访问路径；Key 仅在 openai-compat SSE 层，不在 adapter |
</threat_model>

<verification>
- `npm test -- --run src/agent/tools/write/excel.test.ts` 通过（Wave 0 stub 中 describe.skip 时全 pass）
- grep -c "getItemOrNullObject" src/adapters/ExcelAdapter.ts 返回 ≥1
- grep -v "^//" src/adapters/ExcelAdapter.ts | grep -c "async insertChart\|async deleteChartByName\|async applyFormula\|async setCell" 返回 ≥4
- `npm run build` 通过（TypeScript 无错误）
</verification>

<success_criteria>
ExcelAdapter 4 个新方法实现完毕，全部遵循 Phase 5 before-image 范式 + Record 签名；build 通过；test 套件不新增 FAIL。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-02-SUMMARY.md`
</output>

---
phase: "06"
plan: "10"
type: execute
wave: 4
depends_on: ["06-01"]
files_modified:
  - src/components/ChatStream.tsx
  - src/components/InputBar.tsx
  - src/store/chat.ts
  - src/components/ChatStream.test.tsx
autonomous: true
requirements:
  - ONB-03

must_haves:
  truths:
    - "ChatStream 空态渲染 host-specific killer-scenario chips（PPT/Excel/Word 各 3 个）"
    - "chip 点击填充 InputBar 文本框（不自动发送，D-16）"
    - "空态 p 文案已改为「选中文档里的内容，或挑一个下面的例子开始。」"
    - "host 未知时渲染空 .suggestions，不报错"
    - "chip 复用 .btn .btn-ghost .btn-sm 类（teal 克制设计系统）"
  artifacts:
    - path: src/components/ChatStream.tsx
      provides: "host-specific chips + 空态文案更新"
    - path: src/store/chat.ts
      provides: "draftPrompt + setDraftPrompt（chip fill 机制）"
    - path: src/components/InputBar.tsx
      provides: "从 chatStore 读 draftPrompt 并填充"
  key_links:
    - from: ChatStream.tsx chips
      to: chatStore.setDraftPrompt
      via: "chip.onClick → setDraftPrompt(seed)"
      pattern: "setDraftPrompt"
    - from: InputBar.tsx
      to: chatStore.draftPrompt
      via: "useEffect([draftPrompt]) → setText(draftPrompt) → clearDraftPrompt"
      pattern: "draftPrompt"
---

<objective>
实现空态 killer-scenario chips（ONB-03/D-15/D-16）：host-specific 3 个 chip，点击填充 InputBar。

Purpose: 替代 v1 Ribbon 6 按钮设计，为 4 个 killer scenario 提供入口引导；chip copy 锁定为 UI-SPEC §1 的三宿主文案。
Output: ChatStream chips 渲染 + InputBar fill 机制（通过 chatStore draft 协调），Wave 0 ChatStream.test.tsx GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-UI-SPEC.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md

<interfaces>
<!-- Source: src/components/ChatStream.tsx — D-03 钩子精确位置 -->
```tsx
// 当前空态（lines 299-314）：
if (messages.length === 0) {
  return (
    <div className="chat-scroll">
      <div className="empty">
        <div className="empty-mark">...</div>
        <h3><Trans>从你正在做的东西开始</Trans></h3>
        <p><Trans>选中文档里的内容，告诉 Aster 你想做什么。</Trans></p>
        {/* D-03：不渲染 suggestion chips，等 Phase 6 */}
      </div>
    </div>
  );
}
// adapter 已 import（line 38/101/260）：
const adapter = useAdapter();
```

<!-- Source: src/components/InputBar.tsx — text 本地 state -->
```tsx
// InputBar 中（lines 37-38）：
const [text, setText] = useState('');
// 需要从 chatStore 读 draftPrompt，useEffect 检测变化，填入 text
```

<!-- Source: UI-SPEC §1 — chip copy（全文）-->
```
PPT：「帮我做一份 Q3 销售复盘 PPT，给 leadership 看，重点华东」「把左下角那张图加红色边框，再往右移 10 px」「在最长的那页后面补一页总结要点」
Excel：「帮我清洗这份数据、加公式、画个图，再给三句话洞察」「看看哪个产品卖得最好，做个对比图」「检查一下有没有重复行，帮我去掉」
Word：「帮我把整篇文档润色一遍，口语改成正式书面」「把我选中的这段改得更正式一点」「帮我生成一个文档摘要，三句话以内」
```

<!-- Source: UI-SPEC §1 — chip 交互 -->
```
Hover → translateX(2px)（var(--dur-fast) var(--ease-out)）
Focus → box-shadow: var(--ring-focus)
Click → FILL InputBar（不 auto-send）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chatStore 新增 draftPrompt / setDraftPrompt + InputBar 接线</name>
  <files>src/store/chat.ts, src/components/InputBar.tsx</files>
  <read_first>
    - src/store/chat.ts（全文：找 Zustand store 结构，了解如何新增字段；找现有 sendMessage / messages 等字段）
    - src/components/InputBar.tsx（全文：了解 useState text + handleSend；找合适的 useEffect 插入点）
  </read_first>
  <action>
    1. chat.ts 新增两个字段（在 Zustand store state 定义中追加）：
       ```typescript
       draftPrompt: string;           // chip 填充的 seed，InputBar 监听
       setDraftPrompt: (prompt: string) => void;
       clearDraftPrompt: () => void;
       ```
       store 初始值：`draftPrompt: ''`
       actions：
       ```typescript
       setDraftPrompt: (prompt) => set({ draftPrompt: prompt }),
       clearDraftPrompt: () => set({ draftPrompt: '' }),
       ```

    2. InputBar.tsx 新增 draftPrompt 监听（在现有 useState text 和 handleSend 附近）：
       ```typescript
       const draftPrompt = useChatStore((s) => s.draftPrompt);
       const clearDraftPrompt = useChatStore((s) => s.clearDraftPrompt);

       // chip 填充：监听 draftPrompt 变化，非空时填入 text + 清除 draft
       useEffect(() => {
         if (draftPrompt) {
           setText(draftPrompt);
           clearDraftPrompt();
         }
       }, [draftPrompt, clearDraftPrompt]);
       ```
       注意：text focus 也需要在填充后 focus textarea（可选，提升 UX）。
  </action>
  <verify>
    <automated>npm test -- --run src/store/chat.test.ts</automated>
  </verify>
  <done>
    grep -c "draftPrompt\|setDraftPrompt\|clearDraftPrompt" src/store/chat.ts 返回 ≥3；
    grep "draftPrompt" src/components/InputBar.tsx 存在；
    `npm run build` 通过（TypeScript 无错）。
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatStream 空态填充 host-specific chips</name>
  <files>src/components/ChatStream.tsx, src/components/ChatStream.test.tsx</files>
  <read_first>
    - src/components/ChatStream.tsx（精确找 D-03 注释块 lines 299-314；找 adapter 变量定义 line 101/260）
    - src/store/chat.ts（Task 1 后：确认 setDraftPrompt 已存在）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（ChatStream.tsx 段：chip UI 结构 lines 659-675 + setInputValue → setDraftPrompt 填充 lines 677-683）
    - .planning/phases/06-write-tools-killer-scenarios/06-UI-SPEC.md（§1 chip anatomy + 交互 + chip copy lines 148-168）
  </read_first>
  <action>
    1. ChatStream.tsx 修改空态块（messages.length === 0 分支）：

       在组件函数顶部（messages.length === 0 条件之前或之后的适当位置）加：
       ```typescript
       const setDraftPrompt = useChatStore((s) => s.setDraftPrompt);
       ```

       替换 D-03 注释，在 `<div className="empty">` 内加 chips：
       ```tsx
       // D-03 替换：添加 host-specific chips
       const host = adapter.capabilities().host;
       const CHIPS: Record<string, Array<{ label: string; seed: string }>> = {
         ppt: [
           { label: '做 Q3 销售复盘 PPT', seed: '帮我做一份 Q3 销售复盘 PPT，给 leadership 看，重点华东' },
           { label: '给图加红色边框右移', seed: '把左下角那张图加红色边框，再往右移 10 px' },
           { label: '补一页总结', seed: '在最长的那页后面补一页总结要点' },
         ],
         excel: [
           { label: '清洗数据做图', seed: '帮我清洗这份数据、加公式、画个图，再给三句话洞察' },
           { label: '哪个产品卖得好', seed: '看看哪个产品卖得最好，做个对比图' },
           { label: '去除重复行', seed: '检查一下有没有重复行，帮我去掉' },
         ],
         word: [
           { label: '整篇润色', seed: '帮我把整篇文档润色一遍，口语改成正式书面' },
           { label: '改选中段', seed: '把我选中的这段改得更正式一点' },
           { label: '生成摘要', seed: '帮我生成一个文档摘要，三句话以内' },
         ],
       };
       const chips = CHIPS[host] ?? [];

       // 在 empty div 内，h3 + p 之后追加：
       <div className="suggestions">
         {chips.map((chip) => (
           <button
             key={chip.seed}
             type="button"
             className="btn btn-ghost btn-sm"
             onClick={() => setDraftPrompt(chip.seed)}  // D-16: 填充不直发
           >
             {chip.label}
           </button>
         ))}
       </div>
       ```

       更新空态 p 文案（UI-SPEC lines 241）：
       ```tsx
       // 旧：「选中文档里的内容，告诉 Aster 你想做什么。」
       // 新：「选中文档里的内容，或挑一个下面的例子开始。」
       <p><Trans>选中文档里的内容，或挑一个下面的例子开始。</Trans></p>
       ```

       styles.css 检查：.suggestions + .btn.btn-ghost.btn-sm 类已存在（Phase 04.1 已定义），不用新增；
       若 hover translateX(2px) 未在 .btn-ghost 上，在 styles.css 加：
       ```css
       /* Phase 6：chip hover 微移（UI-SPEC §1）*/
       .suggestions .btn-ghost:hover {
         transform: translateX(2px);
         transition: transform var(--dur-fast) var(--ease-out);
       }
       ```

    2. ChatStream.test.tsx：取消 describe.skip，确认 3 个 chip host 路由测试 GREEN。
       需要 mock AdapterContext（vi.mock '../context/AdapterContext'）+ mock useChatStore（仅取 setDraftPrompt）。
       chip click 测 → setDraftPrompt 以正确 seed 被调用（而非自动 send）。
  </action>
  <verify>
    <automated>npm test -- --run src/components/ChatStream.test.tsx</automated>
  </verify>
  <done>
    grep "setDraftPrompt\|CHIPS\|suggestions" src/components/ChatStream.tsx 各存在；
    grep "或挑一个下面的例子" src/components/ChatStream.tsx 存在（新文案）；
    `npm test -- --run src/components/ChatStream.test.tsx` GREEN（3 个 host 路由 + click 填充断言）；
    `npm run build` 通过；
    `npm run build && npm run size` 确认 initial main-*.js ≤ 82KB（chips 文案是字符串，几乎无 bundle 增量）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip.seed → InputBar → sendMessage | chip copy 是硬编码字符串（无 LLM 注入），用户可在 InputBar 编辑后再发送 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10-01 | Tampering | chip seed 内容 | accept | chip copy 是硬编码字符串字面量（零外部输入），无注入风险 |
| T-06-10-02 | Information Disclosure | draftPrompt 在 store | accept | draftPrompt 是 UI 临时状态（纯前端内存），无持久化，无敏感信息 |
| T-06-10-03 | Denial of Service | 多次 chip 快速点击 | accept | 每次 onClick 只更新 draftPrompt（无副作用）；InputBar 监听 useEffect 防竞态 |
</threat_model>

<verification>
- `npm test -- --run src/components/ChatStream.test.tsx` GREEN
- `npm run build && npm run size` 输出 initial JS ≤ 82KB（不超预算）
- grep "选中文档里的内容，或挑一个下面的例子" src/components/ChatStream.tsx 存在
- grep "D-03" src/components/ChatStream.tsx 无 TODO 注释（已实现）
</verification>

<success_criteria>
host-specific chips 渲染正确；点击填充 InputBar（不直发）；空态文案更新；test GREEN；bundle ≤ 82KB。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-10-SUMMARY.md`
</output>

---
phase: "06"
plan: "08"
type: execute
wave: 4
depends_on: ["06-05", "06-06", "06-07"]
files_modified:
  - src/agent/tools/index.ts
  - src/agent/operationLog.ts
  - src/agent/operationLog.integration.test.ts
autonomous: true
requirements:
  - TOOL-03
  - ONB-02

must_haves:
  truths:
    - "buildToolsForHost 三宿主数组包含所有新 write tools（word 5 个、excel 4 个、ppt 4 个含 set_shape_text）"
    - "所有新 write tool 通过 assertWriteToolRegisterable 守门（humanLabel + kind='write'）"
    - "operationLog.ts PostStateSnapshot.kind union 扩展含 'ppt_shape' | 'excel_chart'（TypeScript strict 编译通过）"
    - "operationLog.ts DocumentAdapterForReplay 接口新增 6 个可选 inverse 方法（Record 签名）"
    - "operationLog.ts executeReverse switch 新增 6 个 case（restore_shape_property/restore_shape_geometry/restore_shape_text/delete_chart_by_name/restore_paragraph_at/noop_inverse）"
    - "operationLog.ts readTargetState switch 新增 ppt_shape / excel_chart case"
    - "operationLog.integration.test.ts 扩展覆盖新 inverse 方法的 Record 签名调用"
    - "`npm run build` TypeScript strict 无错"
  artifacts:
    - path: src/agent/tools/index.ts
      provides: "buildToolsForHost 注册全套新 write tools"
    - path: src/agent/operationLog.ts
      provides: "PostStateSnapshot.kind 扩展 + DocumentAdapterForReplay 扩展 + executeReverse/readTargetState 新 case"
    - path: src/agent/operationLog.integration.test.ts
      provides: "新 inverse 方法的 Record 签名集成守门"
  key_links:
    - from: buildToolsForHost
      to: 三宿主 write tools 数组
      via: "assertWriteToolRegisterable 守门逐个校验"
      pattern: "assertWriteToolRegisterable"
    - from: PostStateSnapshot.kind
      to: "'ppt_shape' | 'excel_chart' 新成员"
      via: "TypeScript union 扩展"
      pattern: "ppt_shape.*excel_chart\|excel_chart.*ppt_shape"
    - from: executeReverse switch
      to: "新 adapter inverse 方法"
      via: "case 'restore_shape_property' / 'restore_shape_geometry' / 'restore_shape_text' / 'delete_chart_by_name' / 'restore_paragraph_at' / 'noop_inverse'"
      pattern: "case 'restore_shape_property'\|case 'delete_chart_by_name'\|case 'restore_paragraph_at'"
    - from: operationLog.integration.test.ts
      to: 新 adapter inverse 方法（Record 签名）
      via: "replay engine 传 Record 对象 → adapter method 不挂"
      pattern: "Record.*unknown"
---

<objective>
将所有新 write tools 注册到 buildToolsForHost，并扩展 operationLog.ts 和集成测试以支持新 tool 的 undo 路径。

Purpose: 注册是让 LLM 能看到并调用工具的前提；operationLog.ts 未更新会导致 TypeScript 编译失败（kind union 不包含 ppt_shape/excel_chart）+ 新工具 undo 全部静默失败（executeReverse default: throw 标 skipped_error）；集成测试守门防止 Phase 5 inverse 位置签名 bug 复发。
Output: index.ts 三宿主各自数组包含完整新工具；operationLog.ts 类型安全 + 新 case；integration test 覆盖新 tool 的 inverse 签名调用；build 通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md

<interfaces>
<!-- Source: src/agent/operationLog.ts — 需要扩展的三个位置 -->
```typescript
// 1. PostStateSnapshot.kind union（line 35）— 当前：
export interface PostStateSnapshot {
  kind: 'word_paragraph' | 'excel_range' | 'ppt_slide';
  content: unknown;
}
// 需要扩展为：
export interface PostStateSnapshot {
  kind: 'word_paragraph' | 'excel_range' | 'ppt_slide' | 'ppt_shape' | 'excel_chart';
  content: unknown;
}

// 2. DocumentAdapterForReplay interface（line 83）— 当前末尾：
export interface DocumentAdapterForReplay {
  deleteParagraphByContent?: (args: Record<string, unknown>) => Promise<void>;
  readWordParagraph?: (args: Record<string, unknown>) => Promise<string>;
  overwriteRange?: (args: Record<string, unknown>) => Promise<void>;
  readExcelRange?: (args: Record<string, unknown>) => Promise<unknown[][]>;
  deleteSlideByTitle?: (args: Record<string, unknown>) => Promise<void>;
  readPptSlideTitle?: (args: Record<string, unknown>) => Promise<string>;
  // 需要追加：
  restoreShapeProperty?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeGeometry?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeText?: (args: Record<string, unknown>) => Promise<void>;
  deleteChartByName?: (args: Record<string, unknown>) => Promise<void>;
  restoreParagraphAt?: (args: Record<string, unknown>) => Promise<void>;
}

// 3. executeReverse switch（line 221）— 当前末尾（default: throw）：
// 需要在 default 前新增 5 个 case + noop_inverse case（静默跳过）：
case 'restore_shape_property':
  if (!adapter.restoreShapeProperty) throw new Error(...);
  await adapter.restoreShapeProperty(reverse.args);
  break;
case 'restore_shape_geometry':
  if (!adapter.restoreShapeGeometry) throw new Error(...);
  await adapter.restoreShapeGeometry(reverse.args);
  break;
case 'restore_shape_text':
  if (!adapter.restoreShapeText) throw new Error(...);
  await adapter.restoreShapeText(reverse.args);
  break;
case 'delete_chart_by_name':
  if (!adapter.deleteChartByName) throw new Error(...);
  await adapter.deleteChartByName(reverse.args);
  break;
case 'restore_paragraph_at':
  if (!adapter.restoreParagraphAt) throw new Error(...);
  await adapter.restoreParagraphAt(reverse.args);
  break;
case 'noop_inverse':
  // 已知不可撤销操作（如 replace_selection 降级）；
  // 让 replayUndoStep catch 处理为 skipped_error，告知用户「无法自动回滚此步」
  throw new Error(`noop_inverse: 此操作不支持自动回滚（${reverse.args.reason ?? ''}）`);

// 4. readTargetState switch — 需要在 default 前新增：
case 'ppt_shape':
  // ppt_shape 无稳定跨步骤 read 方法（形状属性读取需要 slide_index + shape_id）
  // 保守返回 undefined → isTargetStateConsistent 视为「一致」→ 不跳过 undo
  return undefined;
case 'excel_chart':
  // excel_chart 同理，返回 undefined 保守通过
  return undefined;
```

<!-- Source: src/agent/tools/index.ts — buildToolsForHost 当前实现 -->
```typescript
// buildToolsForHost 当前（lines 193-222）：
export function buildToolsForHost(host: 'word' | 'excel' | 'ppt'): ToolDef[] {
  switch (host) {
    case 'word': {
      const wordWriteTools = [appendParagraph] as ToolDef[];
      wordWriteTools.forEach(assertWriteToolRegisterable);
      return [ getDocumentFullText, getParagraphCount, getParagraphAt, getDocumentOutline,
               ...wordWriteTools, selectionDetail ].map((t) => t as ToolDef);
    }
    case 'excel': {
      const excelWriteTools = [setRangeValuesTool] as ToolDef[];
      excelWriteTools.forEach(assertWriteToolRegisterable);
      return [ listWorksheets, getRangeValues, getUsedRangeSummary, ...excelWriteTools, selectionDetail ].map((t) => t as ToolDef);
    }
    case 'ppt': {
      const pptWriteTools = [insertSlide] as ToolDef[];
      pptWriteTools.forEach(assertWriteToolRegisterable);
      return [ listSlides, getSlide, listShapesOnSlide, getShape, ...pptWriteTools, selectionDetail ].map((t) => t as ToolDef);
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: operationLog.ts 扩展 kind union + 接口 + switch（BLOCKER 1 修复）</name>
  <files>src/agent/operationLog.ts</files>
  <read_first>
    - src/agent/operationLog.ts（全文，精确定位：PostStateSnapshot line 34-37；DocumentAdapterForReplay lines 83-96；executeReverse lines 221-247；readTargetState lines 146-180；isTargetStateConsistent lines 190-215）
  </read_first>
  <action>
    对 src/agent/operationLog.ts 做四处精准修改（不改其他任何逻辑）：

    **修改 1 — PostStateSnapshot.kind union（line ~35）：**
    将：
    ```typescript
    kind: 'word_paragraph' | 'excel_range' | 'ppt_slide';
    ```
    改为：
    ```typescript
    kind: 'word_paragraph' | 'excel_range' | 'ppt_slide' | 'ppt_shape' | 'excel_chart';
    ```

    **修改 2 — DocumentAdapterForReplay 接口追加 5 个可选方法（在 readPptSlideTitle 之后，闭括号之前）：**
    ```typescript
    /** PPT shape inverse：还原形状属性（fill/line/size）*/
    restoreShapeProperty?: (args: Record<string, unknown>) => Promise<void>;
    /** PPT shape inverse：还原形状位置（left/top）*/
    restoreShapeGeometry?: (args: Record<string, unknown>) => Promise<void>;
    /** PPT shape inverse：还原形状文字（TOOL-03 set_shape_text）*/
    restoreShapeText?: (args: Record<string, unknown>) => Promise<void>;
    /** Excel inverse：按名称删除刚插入的 chart */
    deleteChartByName?: (args: Record<string, unknown>) => Promise<void>;
    /** Word inverse：按位置 index 还原替换前的段落文本 */
    restoreParagraphAt?: (args: Record<string, unknown>) => Promise<void>;
    ```

    **修改 3 — executeReverse switch，在 `default: throw` 之前新增 6 个 case：**
    ```typescript
    case 'restore_shape_property':
      if (!adapter.restoreShapeProperty) {
        throw new Error(`adapter 未实现 restoreShapeProperty（tool=${reverse.tool}）`);
      }
      await adapter.restoreShapeProperty(reverse.args);
      break;
    case 'restore_shape_geometry':
      if (!adapter.restoreShapeGeometry) {
        throw new Error(`adapter 未实现 restoreShapeGeometry（tool=${reverse.tool}）`);
      }
      await adapter.restoreShapeGeometry(reverse.args);
      break;
    case 'restore_shape_text':
      if (!adapter.restoreShapeText) {
        throw new Error(`adapter 未实现 restoreShapeText（tool=${reverse.tool}）`);
      }
      await adapter.restoreShapeText(reverse.args);
      break;
    case 'delete_chart_by_name':
      if (!adapter.deleteChartByName) {
        throw new Error(`adapter 未实现 deleteChartByName（tool=${reverse.tool}）`);
      }
      await adapter.deleteChartByName(reverse.args);
      break;
    case 'restore_paragraph_at':
      if (!adapter.restoreParagraphAt) {
        throw new Error(`adapter 未实现 restoreParagraphAt（tool=${reverse.tool}）`);
      }
      await adapter.restoreParagraphAt(reverse.args);
      break;
    case 'noop_inverse':
      // 已知不可撤销操作（replace_selection 降级不使用此 case，但保留以防未来遇到）
      // throw → replayUndoStep.catch → skipped_error → 用户看「无法自动回滚此步」
      throw new Error(`noop_inverse: 此操作不支持自动回滚（${String(reverse.args.reason ?? '')}）`);
    ```

    **修改 4 — readTargetState switch，在 `default: return undefined` 之前新增 2 个 case：**
    ```typescript
    case 'ppt_shape':
      // 形状属性跨步骤读取需要 slide_index + shape_id，当前无专用 read adapter 方法
      // 保守返回 undefined → isTargetStateConsistent 视为「一致」→ 不跳过 undo（安全侧）
      return undefined;
    case 'excel_chart':
      // chart 状态跨步骤读取同理，保守返回 undefined
      return undefined;
    ```

    同时，isTargetStateConsistent 的 switch 也需要在 `default: return true` 前加：
    ```typescript
    case 'ppt_shape':
      return true; // 无跨步骤状态读取，保守通过
    case 'excel_chart':
      return true; // 无跨步骤状态读取，保守通过
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    grep "ppt_shape" src/agent/operationLog.ts 返回 ≥3（kind union + readTargetState case + isTargetStateConsistent case）；
    grep "excel_chart" src/agent/operationLog.ts 返回 ≥3；
    grep "case 'restore_shape_property'" src/agent/operationLog.ts 存在；
    grep "case 'delete_chart_by_name'" src/agent/operationLog.ts 存在；
    grep "case 'restore_paragraph_at'" src/agent/operationLog.ts 存在；
    grep "case 'restore_shape_text'" src/agent/operationLog.ts 存在；
    grep "restoreParagraphAt\?" src/agent/operationLog.ts 存在（接口方法）；
    `npx tsc --noEmit` 通过（TypeScript strict 无错）；
    `npm run build` 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: buildToolsForHost 注册所有新 write tools（含 set_shape_text）</name>
  <files>src/agent/tools/index.ts</files>
  <read_first>
    - src/agent/tools/index.ts（全文，找 imports 段 lines 12-18 + buildToolsForHost 段 lines 193-222）
    - src/agent/tools/write/word.ts（确认新增 4 个 export name：insertParagraph/replaceParagraph/insertTextAtCursor/replaceSelection）
    - src/agent/tools/write/excel.ts（确认新增 3 个 export name：applyFormula/insertChart/setCell）
    - src/agent/tools/write/ppt.ts（确认新增 3 个 export name：setShapeProperty/moveShape/setShapeText）
  </read_first>
  <action>
    1. 在 imports 段新增三行（照现有格式追加）：
       ```typescript
       import { insertParagraph, replaceParagraph, insertTextAtCursor, replaceSelection } from './write/word';
       import { applyFormula, insertChart, setCell } from './write/excel';
       import { setShapeProperty, moveShape, setShapeText } from './write/ppt';
       ```

    2. buildToolsForHost word case 更新：
       ```typescript
       const wordWriteTools = [appendParagraph, insertParagraph, replaceParagraph, insertTextAtCursor, replaceSelection] as ToolDef[];
       ```

    3. buildToolsForHost excel case 更新：
       ```typescript
       const excelWriteTools = [setRangeValuesTool, applyFormula, insertChart, setCell] as ToolDef[];
       ```

    4. buildToolsForHost ppt case 更新（含 setShapeText）：
       ```typescript
       const pptWriteTools = [insertSlide, setShapeProperty, moveShape, setShapeText] as ToolDef[];
       ```

    其余（assertWriteToolRegisterable.forEach + return 数组）不变。
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/index.types.test.ts</automated>
  </verify>
  <done>
    grep -c "insertParagraph\|replaceParagraph\|insertTextAtCursor\|replaceSelection" src/agent/tools/index.ts 返回 ≥4（import + 数组 2 处）；
    grep -c "applyFormula\|insertChart\|setCell" src/agent/tools/index.ts 返回 ≥3；
    grep -c "setShapeProperty\|moveShape\|setShapeText" src/agent/tools/index.ts 返回 ≥3；
    `npm test -- --run src/agent/tools/index.types.test.ts` 通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: operationLog.integration.test.ts 扩展新 inverse case + VALIDATION.md nyquist_compliant</name>
  <files>src/agent/operationLog.integration.test.ts, .planning/phases/06-write-tools-killer-scenarios/06-VALIDATION.md</files>
  <read_first>
    - src/agent/operationLog.integration.test.ts（全文，找三个 describe 块的结构 + 现有 mock + it 范式）
    - .planning/phases/06-write-tools-killer-scenarios/06-VALIDATION.md（frontmatter nyquist_compliant 字段）
  </read_first>
  <behavior>
    - Word describe 新增 it：replay engine 用 Record 对象调 restoreParagraphAt → adapter.restoreParagraphAt 被以 { index, expectedText, restoreText } 调用（不挂，无 TypeError）
    - Excel describe 新增 it：replay engine 用 Record 对象调 deleteChartByName → adapter.deleteChartByName 被以 { chartName } 调用（不挂）
    - PPT describe 新增 it（2 个）：
      · restoreShapeProperty → adapter.restoreShapeProperty 被以 { slide_index, shape_id, ... } 调用（不挂）
      · restoreShapeText → adapter.restoreShapeText 被以 { slide_index, shape_id, before_text } 调用（不挂）
    - 关键断言：确认没有 TypeError（位置参时对象传入会抛；Record 签名时不会抛）
    - mock 中 postState 使用新 kind：Word mock postState = { kind: 'word_paragraph', ... }（已有）；PPT mock 用 { kind: 'ppt_shape', content: { slide_index: 1, shape_id: 's1' } }；Excel mock 用 { kind: 'excel_chart', content: { chartName: '图表1' } }
  </behavior>
  <action>
    **Part 1 — integration test 扩展：**
    在现有三个 describe 块内各追加 it 用例：
    - Mock adapter，添加新 inverse 方法 vi.fn()（restoreParagraphAt / deleteChartByName / restoreShapeProperty / restoreShapeText）
    - 构造包含新 tool inverse 的 OperationLog operation，postState 使用新 kind（ppt_shape / excel_chart）
    - 调用 replayUndoAll
    - 断言 adapter 方法被调用了 1 次，且 args 是 Record 对象（有正确字段，无 TypeError）

    **Part 2 — VALIDATION.md 更新：**
    将 06-VALIDATION.md frontmatter 中：
    ```
    nyquist_compliant: false
    ```
    改为：
    ```
    nyquist_compliant: true
    ```
    同时在底部「Validation Sign-Off」最后一个 checkbox 补充：
    ```
    - [x] `nyquist_compliant: true` set in frontmatter (Plan 06-08 Task 3 确认所有任务有 automated verify)
    ```
  </action>
  <verify>
    <automated>npm test -- --run src/agent/operationLog.integration.test.ts</automated>
  </verify>
  <done>
    test 套件通过；
    grep "restoreParagraphAt\|deleteChartByName\|restoreShapeProperty\|restoreShapeText" src/agent/operationLog.integration.test.ts 各返回 ≥1；
    grep "nyquist_compliant: true" .planning/phases/06-write-tools-killer-scenarios/06-VALIDATION.md 存在；
    测试注释中说明 Record 签名守门目的。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buildToolsForHost → dispatchTool | 注册后 LLM 才能调用；assertWriteToolRegisterable 守门防漏 humanLabel |
| executeReverse switch → adapter | switch 路由后才调用 adapter 方法；未知 tool → default: throw → skipped_error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Tampering | 新工具注册但缺 humanLabel | mitigate | assertWriteToolRegisterable forEach 在 buildToolsForHost 内守门，缺则 throw |
| T-06-08-02 | Tampering | inverse 方法位置签名 bug 复发 | mitigate | integration test 新增 Record 对象传参 case，位置签名会 TypeError |
| T-06-08-03 | Tampering | TypeScript strict kind union 编译失败 | mitigate | 修改 PostStateSnapshot.kind 后 npx tsc --noEmit 验证；CI build 守门 |
| T-06-08-04 | Tampering | executeReverse default: throw 静默失败新 inverse | mitigate | 新增 5 个 case + noop_inverse case；集成测试验证路由正确到 adapter |
</threat_model>

<verification>
- `npx tsc --noEmit` 通过（TypeScript strict，ppt_shape/excel_chart kind 类型安全）
- `npm test -- --run src/agent/tools/index.types.test.ts src/agent/operationLog.integration.test.ts` 通过
- `npm test` 完整套件通过（或仅已有 flaky retry.test.ts 单跑失败，非本 phase 引入）
- `npm run build` 通过
- grep "ppt_shape" src/agent/operationLog.ts 返回 ≥3
- grep "case 'restore_shape_text'" src/agent/operationLog.ts 存在
- grep "nyquist_compliant: true" .planning/phases/06-write-tools-killer-scenarios/06-VALIDATION.md 存在
</verification>

<success_criteria>
buildToolsForHost 注册完整新工具集（含 set_shape_text）；operationLog.ts 类型安全 + 新 case 完整；integration test 覆盖四个新 inverse 方法的 Record 签名调用；06-VALIDATION.md nyquist_compliant 已翻为 true；全套 test GREEN；build 通过。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-08-SUMMARY.md`
</output>

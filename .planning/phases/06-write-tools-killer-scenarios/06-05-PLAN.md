---
phase: "06"
plan: "05"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/agent/tools/write/excel.ts
autonomous: true
requirements:
  - TOOL-03
  - ONB-02

must_haves:
  truths:
    - "excel.ts 新增 apply_formula / insert_chart / set_cell 三个 ToolDef"
    - "三个工具均有中文 humanLabel、kind='write'、reverse（Record 对象 args）、postState"
    - "insert_chart reverse.tool='delete_chart_by_name'，args 含 chartName"
    - "apply_formula/set_cell reverse.tool='overwrite_range'，args 含 address+values（before-image）"
    - "assertWriteToolRegisterable lint 守门通过（humanLabel + kind 存在）"
  artifacts:
    - path: src/agent/tools/write/excel.ts
      provides: "3 个新 Excel write tool defs"
      exports: ["applyFormula", "insertChart", "setCell"]
  key_links:
    - from: src/agent/tools/write/excel.ts
      to: ExcelAdapter.insertChart / applyFormula / setCell
      via: "ctx.adapter as ExcelAdapter 调用（A-06 守门）"
      pattern: "ctx\\.adapter as ExcelAdapter"
---

<objective>
实现 Excel 三个新 write tool 定义：apply_formula / insert_chart / set_cell。

Purpose: SC2「清洗→公式→图→三句话洞察」完整流程需要这三个工具；insert_chart 是 Excel killer scenario 的视觉焦点。
Output: excel.ts 新增三个完整 ToolDef，均通过 assertWriteToolRegisterable，Wave 1 测试桩转为 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md
@.planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md

<interfaces>
<!-- Source: src/agent/tools/write/excel.ts — setRangeValues 完整范式 -->
```typescript
// setRangeValues（完整模板，新工具照此结构）：
export const setRangeValues: ToolDef<SetRangeValuesArgs> = {
  name: 'set_range_values', kind: 'write',
  description: '...',
  parameters: { type: 'object', properties: { ... }, required: [...] },
  humanLabel: ({ address }) => `写入单元格区域 ${address}`,
  async execute({ address, values }, ctx): Promise<ToolResult> {
    const { beforeImage } = await (ctx.adapter as ExcelAdapter).setRangeValues(address, values);
    const reverse: ReverseDescriptor = { tool: 'overwrite_range', args: { address: beforeImage.address, values: beforeImage.values } };
    const postState: PostStateSnapshot = { kind: 'excel_range', content: { address, values } };
    console.assert(reverse !== undefined, 'TOOL-04: reverse required');
    return { ok: true, data: { written: values.length }, reverse, postState };
  },
};
```

<!-- Source: RESEARCH.md — insert_chart ToolDef（完整范式 lines 550-584）-->
```typescript
// insert_chart humanLabel 范例（PATTERNS.md lines 89-96）：
humanLabel: ({ data_range, chart_type }) =>
  `在当前工作表插入${chart_type === 'Bar' ? '条形图' : chart_type === 'Line' ? '折线图' : chart_type === 'Pie' ? '饼图' : '柱状图'}（数据 ${data_range}）`

// insert_chart reverse（chart.name 句柄）：
const reverse: ReverseDescriptor = { tool: 'delete_chart_by_name', args: { chartName } };
const postState: PostStateSnapshot = { kind: 'excel_chart', content: { chartName, dataRange: data_range, chartType: chart_type } };
return { ok: true, data: { chartName }, mutated: { chartName }, reverse, postState };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: excel.ts 新增 apply_formula / insert_chart / set_cell ToolDef</name>
  <files>src/agent/tools/write/excel.ts</files>
  <read_first>
    - src/agent/tools/write/excel.ts（全文，了解 setRangeValues 完整结构、imports、ExcelAdapter 类型引用方式）
    - src/adapters/ExcelAdapter.ts（确认 insertChart/applyFormula/setCell 方法签名，Plan 02 刚加的）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（excel.ts 段：insert_chart/apply_formula/set_cell humanLabel 范例 lines 88-97）
    - .planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md（insert_chart ToolDef 完整代码 lines 550-584）
  </read_first>
  <action>
    在 excel.ts 末尾（setRangeValues 之后）新增三个 ToolDef 导出：

    1. `export const applyFormula: ToolDef<...>`
       - name: 'apply_formula', kind: 'write'
       - description: '在 Excel 指定单元格写入公式。cell 为单元格地址（如 "B2"），formula 为公式字符串（如 "=SUM(A2:A10)"）。'
       - parameters: { type: 'object', properties: { cell: { type: 'string', description: '单元格地址，如 "B2"' }, formula: { type: 'string', description: '公式字符串，如 "=SUM(A2:A10)"' } }, required: ['cell', 'formula'] }
       - humanLabel: ({ cell, formula }) => `在 ${cell} 单元格写入公式 ${formula}`（全中文，无英文参数键名）
       - execute: const { beforeImage } = await (ctx.adapter as ExcelAdapter).applyFormula(cell, formula)
         · reverse = { tool: 'overwrite_range', args: { address: beforeImage.address, values: beforeImage.values } }（Record 对象）
         · postState = { kind: 'excel_range', content: { cell, formula } }
         · mutated = { cell, formula }
         · return { ok: true, data: { cell, formula }, mutated, reverse, postState }

    2. `export const insertChart: ToolDef<...>`
       - name: 'insert_chart', kind: 'write'
       - description: '在当前工作表插入图表。data_range 为数据范围地址（如 "A1:B10"），chart_type 为图表类型。'
       - parameters: { data_range: string（required）, chart_type: string（optional，'ColumnClustered'|'Bar'|'Line'|'Pie'）}
       - humanLabel: 见 interfaces 段（按 chart_type 判断中文类型名）
       - execute:
         · const { chartName } = await (ctx.adapter as ExcelAdapter).insertChart(data_range, chart_type ?? 'ColumnClustered')
         · reverse = { tool: 'delete_chart_by_name', args: { chartName } }（Record 对象）
         · postState = { kind: 'excel_chart', content: { chartName, dataRange: data_range, chartType: chart_type } }
         · mutated = { chartName }
         · return { ok: true, data: { chartName }, mutated, reverse, postState }

    3. `export const setCell: ToolDef<...>`
       - name: 'set_cell', kind: 'write'
       - description: '在 Excel 指定单元格写入值。cell 为单元格地址，value 为写入的值（字符串或数字）。'
       - parameters: { cell: string（required）, value: （string | number）（required）}
       - humanLabel: ({ cell, value }) => `将单元格 ${cell} 设为 ${String(value).slice(0, 20)}`
       - execute: const { beforeImage } = await (ctx.adapter as ExcelAdapter).setCell(cell, value)
         · reverse = { tool: 'overwrite_range', args: { address: beforeImage.address, values: beforeImage.values } }
         · postState = { kind: 'excel_range', content: { cell, value } }
         · mutated = { cell, value }

    每个 ToolDef 末尾加：`console.assert(reverse !== undefined, 'TOOL-04: reverse required');`
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/excel.test.ts src/agent/tools/index.types.test.ts</automated>
  </verify>
  <done>
    grep -c "export const applyFormula\|export const insertChart\|export const setCell" src/agent/tools/write/excel.ts 返回 3；
    grep "delete_chart_by_name" src/agent/tools/write/excel.ts 存在；
    grep "overwrite_range" src/agent/tools/write/excel.ts 返回 ≥2（apply_formula + set_cell 各 1）；
    `npm test -- --run src/agent/tools/index.types.test.ts` 通过（humanLabel + reverse lint 守门）；
    Wave 0 excel.test.ts 取消 describe.skip 后测试 GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolDef.execute args → ExcelAdapter | LLM 传入的 cell/formula/data_range 经 JSON Schema 验证后进 adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | LLM 传入 formula 注入 | accept | Excel formula 在 sandbox（浏览器 Excel.run）内执行，不是代码执行，风险低 |
| T-06-05-02 | Tampering | chart 插入后 name 碰撞 | mitigate | 继承 Plan 02 的 getItemOrNullObject guard；inverse 用 getItemOrNullObject |
| T-06-05-03 | Denial of Service | 超大 data_range 导致 OOM | accept | A-24 防御已在 read tool 层（summary first），write 路径不读大范围，风险低 |
</threat_model>

<verification>
- `npm test -- --run src/agent/tools/write/excel.test.ts` GREEN（Wave 0 describe.skip 取消后）
- `npm test -- --run src/agent/tools/index.types.test.ts` 通过
- grep -c "kind: 'write'" src/agent/tools/write/excel.ts 返回 ≥3（3 个新工具各 1）
- `npm run build` 通过
</verification>

<success_criteria>
三个 Excel write tool 定义完整，通过 assertWriteToolRegisterable；test 套件 GREEN；build 通过。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-05-SUMMARY.md`
</output>

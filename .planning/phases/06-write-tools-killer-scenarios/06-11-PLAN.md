---
phase: "06"
plan: "11"
type: execute
wave: 4
depends_on: ["06-01"]
files_modified:
  - src/components/Onboarding/OnboardingModal.tsx
  - src/components/Onboarding/Step1Keys.tsx
  - src/components/Onboarding/Step2Guide.tsx
  - manifest.xml
  - src/commands.ts
  - src/components/Onboarding/OnboardingModal.test.tsx
autonomous: true
requirements:
  - ONB-03

must_haves:
  truths:
    - "OnboardingModal 单步化：删 step state / goNext / goBack / Step2Guide import"
    - "Step1Keys prop onNext 改名 onComplete，写 ONBOARDING_SEEN 后调 onComplete"
    - "CTA 文案「开始使用」（不是「下一步」）"
    - "Step2Guide.tsx 文件已删除，无任何代码引用"
    - "manifest.xml 三宿主各只有 1 个 ShowTaskpane 按钮「打开 Aster」"
    - "D-21：Step1 完成 → 主界面跳转正常（ONBOARDING_SEEN 写入正确）"
  artifacts:
    - path: src/components/Onboarding/OnboardingModal.tsx
      provides: "单步 modal（无 step state，直接渲染 Step1Keys）"
    - path: src/components/Onboarding/Step1Keys.tsx
      provides: "onComplete prop + 写 ONBOARDING_SEEN + CTA「开始使用」"
    - path: manifest.xml
      provides: "三宿主各 1 个 ShowTaskpane 按钮「打开 Aster」"
  key_links:
    - from: Step1Keys.handleComplete
      to: storage.set(ONBOARDING_SEEN, true)
      via: "从 Step2Guide 迁移过来的 storage 写入"
      pattern: "ONBOARDING_SEEN"
    - from: manifest.xml ShowTaskpane
      to: Taskpane.Url
      via: "三宿主各 1 个 Control xsi:type=ShowTaskpane"
      pattern: "ShowTaskpane"
---

<objective>
Onboarding 单步化（D-18/D-19/D-21）+ Ribbon manifest 精简到 1 按钮（D-17）。

Purpose: 自用工具不需要 2 步 onboarding；删 Step2Guide 减包体；manifest 1 按钮对齐 v2 「seed 在 chips」语义。
Output: OnboardingModal 单步，Step2Guide 删除，Step1Keys CTA 改「开始使用」，manifest 三宿主各 1 ShowTaskpane 按钮。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-UI-SPEC.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md

<interfaces>
<!-- Source: src/components/Onboarding/OnboardingModal.tsx — 当前实现 -->
```tsx
// 需删除（PATTERNS.md lines 697-705）：
const [step, setStep] = useState<1 | 2>(1);
function goNext(): void { setStep(2); }
function goBack(): void { setStep(1); }
import Step2Guide from './Step2Guide';
<span className="brand-step">{step === 1 ? '01' : '02'} / 02</span>
// step 条件渲染整块

// 保留结构（PATTERNS.md lines 709-724）：
<div className="modal-scrim" ...>
  <div className="modal">
    <div className="modal-brand">
      <img src={logo} ... />
      <span className="brand-name">Aster</span>
      {/* brand-step 删掉，无计数 */}
    </div>
    <Step1Keys onComplete={onComplete} onSkip={handleSkip} />  {/* onNext → onComplete */}
  </div>
</div>
```

<!-- Source: src/components/Onboarding/Step1Keys.tsx — 需修改的三处（PATTERNS.md lines 744-798）-->
```tsx
// 1. interface 改（lines 744-755）：
// onNext: () => void → onComplete: () => void

// 2. handleNext → handleComplete（lines 758-771）：
function handleComplete(): void {
  setKey('deepseek', dsKey);
  setKey('aihubmix', ahmKey);
  storage.set(STORAGE_KEYS.ONBOARDING_SEEN, true);  // ← 从 Step2Guide 迁移
  onComplete();
}

// 3. CTA 文案（lines 775-784）：
// 「下一步」 → 「开始使用」
// 新增 import：import { storage, STORAGE_KEYS } from '../../lib/storage';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OnboardingModal 单步化 + Step1Keys CTA 改 + Step2Guide 删除</name>
  <files>src/components/Onboarding/OnboardingModal.tsx, src/components/Onboarding/Step1Keys.tsx, src/components/Onboarding/Step2Guide.tsx</files>
  <read_first>
    - src/components/Onboarding/OnboardingModal.tsx（全文，找 step state / goNext / Step2Guide import / brand-step span 位置）
    - src/components/Onboarding/Step1Keys.tsx（全文，找 onNext prop / handleNext / CTA button / modal-sub p）
    - src/components/Onboarding/Step2Guide.tsx（全文，确认只需删除；找 ONBOARDING_SEEN 写入位置）
    - src/lib/storage.ts（确认 STORAGE_KEYS.ONBOARDING_SEEN 已存在）
    - .planning/phases/06-write-tools-killer-scenarios/06-UI-SPEC.md（§2 单步 onboarding + §Copywriting modal-sub 文案 lines 173-196、236-238）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（OnboardingModal 段 lines 697-736；Step1Keys 段 lines 743-798）
  </read_first>
  <action>
    1. OnboardingModal.tsx 修改：
       - 删除 step state（`const [step, setStep] = useState<1 | 2>(1)`）
       - 删除 goNext / goBack 函数
       - 删除 Step2Guide import
       - 删除 brand-step span（`<span className="brand-step">01 / 02</span>` 或类似）
       - 将 step 条件渲染替换为直接渲染 `<Step1Keys onComplete={onComplete} onSkip={handleSkip} />`
       - handleSkip 保留（写 ONBOARDING_SEEN + 调 onSkip/onComplete）

    2. Step1Keys.tsx 修改：
       - interface prop 名：onNext → onComplete
       - 新增 import：`import { storage, STORAGE_KEYS } from '../../lib/storage';`
       - handleNext → handleComplete；在调 onComplete() 前加 `storage.set(STORAGE_KEYS.ONBOARDING_SEEN, true)`
       - modal-sub p 文案改为（UI-SPEC Copywriting line 237）：
         「Aster 是嵌在 Office 里的 AI 代理 —— 填入你自己的 API Key 就能开始。Key 只存在你的浏览器本地。」
         用 `<Trans>` macro
       - CTA button 文案：`<Trans>开始使用</Trans>`（不是「下一步」）
       - onClick 改为 handleComplete（不是 handleNext）

    3. Step2Guide.tsx：整文件删除（rm 或 Write 工具用 empty / 直接 bash rm）。
       使用 Bash 删除文件：`rm /Users/wb.chen/Documents/Project/Aster/src/components/Onboarding/Step2Guide.tsx`

    4. 确认无孤儿引用：grep "Step2Guide" src/ -r 结果为空（不再有任何 import 或引用）。
  </action>
  <verify>
    <automated>npm test -- --run src/components/Onboarding/OnboardingModal.test.tsx</automated>
  </verify>
  <done>
    文件 src/components/Onboarding/Step2Guide.tsx 不存在（ls 确认）；
    grep -r "Step2Guide" src/ 返回 0 行；
    grep "onComplete" src/components/Onboarding/Step1Keys.tsx 存在（prop 已改名）；
    grep "ONBOARDING_SEEN" src/components/Onboarding/Step1Keys.tsx 存在（storage 写入迁移）；
    grep "开始使用" src/components/Onboarding/Step1Keys.tsx 存在；
    `npm run build` 通过（无孤儿 import 报错）。
  </done>
</task>

<task type="auto">
  <name>Task 2: manifest.xml Ribbon 精简到 1 按钮 + OnboardingModal.test 取消 skip</name>
  <files>manifest.xml, src/commands.ts, src/components/Onboarding/OnboardingModal.test.tsx</files>
  <read_first>
    - manifest.xml（全文；找三宿主 Host 段的 Control 定义；PATTERNS.md 已确认 PPT L69-87 已是单按钮，需检查 Excel L121 + Word L170 是否也单按钮）
    - src/commands.ts（找已有函数；Ribbon 精简只影响 manifest，commands.ts 若有多余的 executeFunction 可清理）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（manifest.xml 段 lines 804-829；PPT 已是目标形态，Excel/Word 检查 Label resid）
  </read_first>
  <action>
    1. manifest.xml 检查：
       - PPT Host block（lines 69-87）：已是单按钮 ShowTaskpane，Label resid = `Btn.Aster.Open.Label`
       - Excel Host block：确认只有 1 个 Control，且 Label resid 已为「打开 Aster」文案；若有多余按钮则删除
       - Word Host block：同 Excel
       - 检查 `<bt:String id="Btn.Aster.Open.Label">` 等 resid 对应的字符串是否为「打开 Aster」；若不是（如仍是「Open Aster」或旧 Label），改为「打开 Aster」
       - 保持三宿主结构：PPT / Excel / Word 各 1 个 Control xsi:type="ShowTaskpane"

    2. src/commands.ts：检查是否有对应旧多按钮的 executeFunction 残留；如果有不再需要的函数（如针对已删除 Ribbon 按钮的函数），删除或注释。若只有单 ShowTaskpane，commands.ts 可能无需改动。

    3. OnboardingModal.test.tsx：取消 describe.skip（若存在），确认 3 个断言 GREEN：
       - CTA 显示「开始使用」
       - Step2Guide 相关文案不在 DOM
       - ONBOARDING_SEEN 写入 storage（mock storage.set）
  </action>
  <verify>
    <automated>npm test -- --run src/components/Onboarding/OnboardingModal.test.tsx</automated>
  </verify>
  <done>
    grep -c 'xsi:type="ShowTaskpane"' manifest.xml 返回 3（三宿主各 1 个）；
    grep "打开 Aster" manifest.xml 返回 ≥1（Label 文案已更新，或 resid 已指向中文）；
    OnboardingModal.test.tsx 全 3 个 it GREEN（CTA + 无 Step2Guide + storage 写入）；
    `npm run build` 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.xml Ribbon → Office runtime | manifest 改动影响 sideload；三宿主 ShowTaskpane 是安全路径，无 ExecuteFunction 代码执行 |
| ONBOARDING_SEEN → localStorage | 写入标志控制 modal 显示；值为布尔，无敏感信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11-01 | Tampering | manifest.xml 修改导致三宿主 sideload 失败 | mitigate | D-12 UAT checkpoint（Plan 12）重验三宿主 sideload；manifest 改动后需重新 sideload |
| T-06-11-02 | Elevation of Privilege | manifest 加新 ExecuteFunction | accept | 本 phase 只保留 ShowTaskpane（不加 ExecuteFunction），无新权限申请 |
| T-06-11-03 | Information Disclosure | ONBOARDING_SEEN localStorage | accept | 仅 boolean flag，无 Key 或文档内容 |
</threat_model>

<verification>
- `ls src/components/Onboarding/Step2Guide.tsx` 返回 no such file
- `grep -r "Step2Guide" src/` 返回 0 行
- grep -c 'xsi:type="ShowTaskpane"' manifest.xml 返回 3
- `npm test -- --run src/components/Onboarding/OnboardingModal.test.tsx` GREEN
- `npm run build` 通过（无孤儿 import）
</verification>

<success_criteria>
Step2Guide 删除无孤儿引用；Step1Keys 「开始使用」CTA 正确；manifest 三宿主各 1 ShowTaskpane；test GREEN；build 通过。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-11-SUMMARY.md`
</output>

---
phase: "06"
plan: "12"
type: execute
wave: 5
depends_on: ["06-08", "06-09", "06-10", "06-11"]
files_modified: []
autonomous: false
requirements:
  - TOOL-03
  - ONB-02
  - ONB-03

must_haves:
  truths:
    - "三宿主真机 sideload 正常（manifest 改动后）"
    - "空态 chips host-specific 显示正确（不跨宿主乱显）"
    - "Onboarding 单步：Step1 完成后正常进入主界面（D-21 bug 不出现）"
    - "PPT killer scenario demo 跑通（Q3 销售复盘 PPT，4-15 步，diff log 显示中文人话）"
    - "Excel killer scenario demo 跑通（清洗+公式+图+洞察，diff log 记录 insert_chart）"
    - "Word killer scenario demo 跑通（整篇润色，replace_paragraph 逐段，diff log 记录）"
    - "PPT shape 护城河 demo 跑通（红色边框+右移，set_shape_property + move_shape）"
    - "Phase 5 undo/diff-log 在新 destructive writes 下仍工作（per-step undo + undo all 正常）"
  artifacts:
    - path: .planning/phases/06-write-tools-killer-scenarios/06-12-SUMMARY.md
      provides: "UAT 结果记录（SC1-SC8 pass/fail）"
  key_links:
    - from: D-12 UAT
      to: 四个 killer scenario demo
      via: "三宿主真机 Office for Web"
      pattern: "SC1-SC4 pass"
    - from: Phase 5 undo/diff-log
      to: Phase 6 新 write tools
      via: "OperationLog → DiffLogPanel → replayUndoAll"
      pattern: "undo all 正常"
---

<objective>
三宿主真机 smoke UAT checkpoint（D-12）：验证 4 个 killer scenario + Phase 5 undo 在新 write tools 下仍工作。

Purpose: destructive write tools 必须真机验证（虚拟机 / Vitest 无法覆盖 Office.js chart.add / shape.fill 路径）；Phase 5 同样靠此法抓出 6 个 gap，Phase 6 继承此策。
Output: 四个 killer scenario 至少各过一遍；per-step undo + undo all 在新 write tools 下仍工作；结果记录到 SUMMARY.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 部署构建 + 运行前门禁</name>
  <files></files>
  <read_first>
    - .planning/STATE.md（确认 Phase 6 所有 Wave 1-4 plan 已完成；了解当前 HEAD）
  </read_first>
  <action>
    运行完整门禁：
    ```bash
    npm run build && npm run size
    npm test
    ```
    - build 通过（TypeScript strict 无错）
    - size gate：initial main-*.js ≤ 82KB gzip
    - test 全套：不新增 FAIL（retry.test.ts 预存在 flaky 可忽略）

    若 size 超标（>82KB）：
    1. 先检查 `npm run build` 输出的 chunk sizes
    2. 若 Step2Guide 删除未减包，确认没有其他大包引入
    3. 若仍超，考虑把某工具 description 字符串压缩（每个 tool description ~30-50 字节）
    4. 修复后重跑 build + size，GREEN 后再 push

    通过后 `git push origin main` 触发 GitHub Pages 部署（§发布授权）。
    等待部署完成（通常 1-2 分钟），记录 deployed commit hash。
  </action>
  <verify>
    <automated>npm run build && npm run size && npm test</automated>
  </verify>
  <done>
    build GREEN，size ≤ 82KB，test 通过；已 push 并部署（记录 commit hash）。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 三宿主真机 smoke UAT（D-12 四个 killer scenario）</name>
  <what-built>
    Wave 1-4 全套实现：
    - 9 个新 write tools（PPT 2 + Excel 3 + Word 4）注册完毕，含中文 humanLabel + inverse
    - buildSystemPrompt(host) 三宿主领域指导重写（batch 倾向 + domain-specific）
    - 空态 killer-scenario chips（三宿主各 3 个，点击填充 InputBar）
    - Onboarding 单步化（CTA「开始使用」，Step2Guide 删除）
    - manifest.xml 三宿主各 1 ShowTaskpane 按钮「打开 Aster」
  </what-built>
  <how-to-verify>
    **准备：**
    1. 在 Office for Web（Edge 或 Chrome 最新版）重新 sideload manifest
       （manifest 已改动，必须重新 sideload，步骤见 CLAUDE.md §office-addin-browser-uat skill）
    2. 准备测试文档：
       - PPT：一份含 5-10 张 slide 的演示文稿（或空文档）
       - Excel：一份含数据的工作表（至少 5 行 3 列，含数值）
       - Word：一份含 5-10 段的文档

    **SC1 新用户 Onboarding（首次打开，如果还未设置 Key）：**
    - 打开 Task Pane（三宿主之一）
    - 若触发 Onboarding，确认：只有 Step1（填 API Key）无 Step2
    - CTA 显示「开始使用」（不是「下一步」）
    - 填入 DeepSeek API Key → 点「开始使用」→ 正常进入主界面（D-21 无卡死）

    **SC2 空态 chips 显示（三宿主）：**
    - PPT：空态显示 PPT 相关 3 个 chips，不显示 Excel/Word chips
    - Excel：空态显示 Excel 相关 3 个 chips
    - Word：空态显示 Word 相关 3 个 chips
    - 点击任一 chip → InputBar 填入对应 seed 文本（不自动发送）
    - 用户可编辑后再 Send（验证 D-16）

    **SC3 PPT killer scenario — Q3 销售复盘 PPT（SC1 from ROADMAP）：**
    - PPT 文档，点击 chip「做 Q3 销售复盘 PPT」（或手动输入「帮我做一份 Q3 销售复盘 PPT，给 leadership 看，重点华东」）
    - 观察 agent 运行：list_slides → batch insert_slide 多张 → 每步 diff log 显示中文人话
    - 成功完成后，diff log 显示 N 步记录（insert_slide humanLabel 显示「在幻灯片末尾插入新幻灯片『XXX』」）
    - 点「撤销该步」某张 → 该 slide 被删除；点「撤销本次所有操作」→ 所有新 slide 回滚

    **SC4 Excel killer scenario — 清洗+图+洞察（SC2 from ROADMAP）：**
    - Excel 文档（含数据），输入「帮我清洗这份数据、加公式、画个图，再给三句话洞察」
    - 观察：get_used_range_summary → set_range_values 清洗 → apply_formula → insert_chart → set_cell（三句话洞察）
    - insert_chart 执行后图表出现在工作表中
    - diff log 记录 insert_chart 条目（humanLabel 中文，如「在当前工作表插入柱状图（数据 A1:B10）」）
    - undo insert_chart → 图表消失（deleteChartByName inverse 生效）

    **SC5 Word killer scenario — 整篇润色（SC3 from ROADMAP）：**
    - Word 文档（含 5+ 段），输入「帮我把整篇文档润色一遍，口语改成正式书面」
    - 观察：get_document_outline → get_paragraph_at 逐段读 → replace_paragraph 逐段替换
    - diff log 记录每步 replace_paragraph（humanLabel「将第 N 段替换为『...』」）
    - undo 某步 → 对应段落恢复原文

    **SC6 PPT shape 护城河（SC4 from ROADMAP — magic moment）：**
    - PPT 文档（含图片/形状），输入「把左下角那张图加红色边框，再往右移 10 px」
    - 观察：list_shapes_on_slide → 几何推断左下角 → set_shape_property（line_color='#FF0000'）+ move_shape（left += 10）
    - 形状边框变红 + 位置右移
    - diff log 两条记录（set_shape_property + move_shape humanLabel 中文）
    - undo → 边框消失 + 位置回原

    **SC7 Phase 5 undo/diff-log 回归验证：**
    - 运行任一 killer scenario（例如 PPT Q3），agent 写入多步后
    - 「撤销本次所有操作」→ 全部回滚，Task Pane 显示「已回滚 N 步」
    - 手动改其中一步的内容 → 再 undo all → 跳过手改步骤，正确提示

    **SC8 manifest sideload 验证（三宿主）：**
    - PPT / Excel / Word 三宿主各 sideload 一次
    - Ribbon 只显示 1 个「打开 Aster」按钮（无多余按钮）
    - 点「打开 Aster」→ Task Pane 正常打开

    **PASS 判定：** SC1-SC8 全部通过 → Phase 6 UAT PASS。
    **FAIL 处理：** 记录失败的 SC，描述复现步骤，我来修复。
  </how-to-verify>
  <resume-signal>
    输入「UAT PASS」（SC1-SC8 全通过）或描述失败的 SC（如「SC4 insert_chart 失败，报错 XXX」）。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 真机 UAT → Office for Web | 验证 Office.js chart.add / shape.fill / paragraph.insertText 等路径在真实环境可用 |
| Agent run → Document | write tools 修改真实文档；undo 作为兜底 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12-01 | Tampering | 真机测试文档被错误修改 | accept | undo all 可回滚；测试前备份文档 |
| T-06-12-02 | Information Disclosure | 测试文档内容经 LLM 处理 | accept | BYO Key 架构，用户知晓数据路径；测试文档避免真实敏感数据 |
| T-06-12-03 | Denial of Service | agent runaway 超 max_steps | accept | max_steps=20 soft-landing 已有；UAT 中观察是否触发 |
</threat_model>

<verification>
- build GREEN + size ≤ 82KB + test 全套通过（前置门禁）
- 用户 UAT 确认 SC1-SC8 全 PASS
- SUMMARY.md 记录各 SC 结果 + 部署 commit hash
</verification>

<success_criteria>
前置门禁全通过；用户 UAT 确认 4 个 killer scenario 各跑通一遍；Phase 5 undo/diff-log 在新 write tools 下仍工作；manifest sideload 三宿主正常。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-12-SUMMARY.md` with:
- SC1-SC8 PASS/FAIL 结果
- 任何 UAT gap（如有）及修复方式
- Deployed commit hash
- Phase 6 complete 标记
</output>

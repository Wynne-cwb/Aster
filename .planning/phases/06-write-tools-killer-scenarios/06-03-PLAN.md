---
phase: "06"
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/adapters/PptAdapter.ts
autonomous: true
requirements:
  - TOOL-03

must_haves:
  truths:
    - "PptAdapter 新增 setShapeProperty（before-image + D-11 expected_state）、restoreShapeProperty（Record 签名）、moveShape（before-image）、restoreShapeGeometry（Record 签名）"
    - "所有 inverse 方法用 args: Record<string, unknown> 签名"
    - "fill_type='NoFill' 时 inverse 用 shape.fill.clear()（Pitfall 2 防御）"
    - "proxy 不出 PowerPoint.run 闭包（A-06）"
  artifacts:
    - path: src/adapters/PptAdapter.ts
      provides: "4 个新 PPT shape write/inverse 方法"
  key_links:
    - from: PptAdapter.setShapeProperty
      to: shape.fill.setSolidColor / shape.lineFormat.color
      via: "PowerPoint.run 内三 sync（load slides → load shapes → load before-image → write）"
      pattern: "setSolidColor\|lineFormat\\.color"
    - from: PptAdapter.restoreShapeProperty
      to: shape.fill.clear（NoFill 时）
      via: "fill_type guard"
      pattern: "fill\\.clear\\(\\)"
---

<objective>
新增 PptAdapter 的 4 个 shape write/inverse 方法，作为 Wave 3 PPT shape write tools 的 adapter 层基础。

Purpose: set_shape_property + move_shape 是 Phase 6 的差异化护城河（SC4 magic moment），必须先有 adapter 方法。
Output: PptAdapter.ts 新增 setShapeProperty + restoreShapeProperty + moveShape + restoreShapeGeometry，遵循 Phase 5 范式 + PPT ShapeFill/LineFormat API（RESEARCH.md PowerPointApi 1.4 验证）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md

<interfaces>
<!-- Source: src/adapters/PptAdapter.ts — deleteSlideByTitle（inverse Record 签名范式）-->
```typescript
// deleteSlideByTitle（lines 543-545）— inverse Record 签名范式：
async deleteSlideByTitle(args: Record<string, unknown>): Promise<void> {
  const titleFingerprint = args.titleFingerprint as string;
  // ... PowerPoint.run 内部实现
}

// insertSlideAfter（lines 481-514）— PowerPoint.run + two-sync 范式：
async insertSlideAfter(_afterIndex: number, title?: string): Promise<{ insertedIndex: number; title: string }> {
  try {
    return await PowerPoint.run(async (ctx) => {
      const slides = ctx.presentation.slides;
      slides.load('items');
      await ctx.sync(); // sync 1
      // ... 操作 ...
      await ctx.sync(); // sync 2
      return { /* 纯数据 */ };
    });
  } catch (err) {
    throw new HostApiError('PPT insertSlideAfter 失败', err);
  }
}
```

<!-- Source: RESEARCH.md — setShapeProperty 三 sync 范式（lines 372-432）-->
```typescript
// PPT setShapeProperty 三/四 sync 范式：
// sync 1: slides.load('items')
// sync 2: slide.shapes.load('items/id,...')
// sync 3: shape.fill.load(['type','foregroundColor']) + shape.lineFormat.load(['color','weight','visible'])
// sync 4: 写入生效（setSolidColor/lineFormat.color/shape.width 等）

// Pitfall 2 防御（fill_type=NoFill 时）：
// if (fill_type === 'NoFill') shape.fill.clear(); else shape.fill.setSolidColor(fill_color);
// lineFormat.visible = false when line_visible was false（inverse 时还原无边框）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PptAdapter 新增 setShapeProperty + restoreShapeProperty</name>
  <files>src/adapters/PptAdapter.ts</files>
  <read_first>
    - src/adapters/PptAdapter.ts（全文，找 insertSlideAfter + deleteSlideByTitle 精确位置，了解 class 结构；关注 bounds check 范式和 shape 遍历模式 lines 370-400）
    - .planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md（PPT Shape API 段 lines 306-390 + setShapeProperty 范式 lines 587-609）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（PptAdapter 段，setShapeProperty 三 sync 代码 lines 372-440）
  </read_first>
  <action>
    在 PptAdapter class 末尾（deleteSlideByTitle 之后）新增：

    1. `setShapeProperty(slideIndex: number, shapeId: string, props: { fillColor?: string; lineColor?: string; lineWeight?: number; width?: number; height?: number }, expectedState?: { fillColor?: string; lineColor?: string }): Promise<{ beforeImage: { fillType: string; fillColor: string | null; lineColor: string | null; lineWeight: number | null; lineVisible: boolean; width: number; height: number } }>`
       - PowerPoint.run 内四次 sync：
         · sync 1: slides.load('items')
         · sync 2: slide.shapes.load('items/id,items/type,items/left,items/top,items/width,items/height')（bounds check + 找 shape）
         · sync 3: shape.fill.load(['type','foregroundColor']) + shape.lineFormat.load(['color','weight','visible'])
         · 抓 beforeImage（Pitfall 2：lineColor/lineWeight 可能为 null，原样存入 beforeImage）
         · D-11 expected_state 验证：if expectedState?.fillColor && beforeImage.fillColor !== expectedState.fillColor → throw HostApiError('并发修改冲突...')
         · 应用 props：if fillColor → shape.fill.setSolidColor(fillColor)；if lineColor → shape.lineFormat.color = lineColor；if lineWeight → shape.lineFormat.weight = lineWeight；if lineColor/lineWeight → shape.lineFormat.visible = true；if width → shape.width = width；if height → shape.height = height
         · sync 4: 写入生效
       - try/catch: if HostApiError rethrow；else throw HostApiError

    2. `restoreShapeProperty(args: Record<string, unknown>): Promise<void>` ← 必须 Record 签名
       - 解构 args 拿：slide_index, shape_id, fill_type, fill_color, line_color, line_weight, line_visible, width, height
       - PowerPoint.run 内：找到 shape（同 setShapeProperty 的 sync 1+2）
       - Pitfall 2 防御：if (fill_type === 'NoFill') shape.fill.clear(); else if fill_color shape.fill.setSolidColor(fill_color as string)
       - line 还原：if (!line_visible) shape.lineFormat.visible = false; else { shape.lineFormat.color = line_color as string; shape.lineFormat.weight = line_weight as number; shape.lineFormat.visible = true }
       - geometry 还原：shape.width = width as number; shape.height = height as number
       - sync 写入生效
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/ppt.test.ts</automated>
  </verify>
  <done>
    grep -c "setShapeProperty\|restoreShapeProperty" src/adapters/PptAdapter.ts 返回 ≥4；
    grep "fill\\.clear" src/adapters/PptAdapter.ts 存在（Pitfall 2 防御）；
    grep "Record<string, unknown>" src/adapters/PptAdapter.ts 返回 ≥1（restoreShapeProperty 签名）；
    `npm run build` 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: PptAdapter 新增 moveShape + restoreShapeGeometry</name>
  <files>src/adapters/PptAdapter.ts</files>
  <read_first>
    - src/adapters/PptAdapter.ts（Task 1 后的状态，了解 setShapeProperty 刚加的代码位置）
    - .planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md（Shape 几何 left/top/width/height 写入段，lines 351-363；shape.left 写权限为 ASSUMED，加 try/catch 防御）
  </read_first>
  <action>
    继续在 PptAdapter class 末尾新增：

    3. `moveShape(slideIndex: number, shapeId: string, left: number, top: number): Promise<{ beforeLeft: number; beforeTop: number }>`
       - PowerPoint.run 内三次 sync：
         · sync 1: slides.load('items')
         · sync 2: slide.shapes.load('items/id,items/left,items/top')（找到 shape，bounds check）
         · beforeLeft = shape.left; beforeTop = shape.top
         · shape.left = left; shape.top = top
         · sync 3: 写入生效
       - return { beforeLeft, beforeTop }
       - try/catch 同范式

    4. `restoreShapeGeometry(args: Record<string, unknown>): Promise<void>` ← 必须 Record 签名
       - 解构 args：slide_index, shape_id, left, top（before-image 旧值）
       - PowerPoint.run 内：找到 shape → shape.left = left as number; shape.top = top as number → sync
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/ppt.test.ts</automated>
  </verify>
  <done>
    grep -c "moveShape\|restoreShapeGeometry" src/adapters/PptAdapter.ts 返回 ≥4；
    两个 inverse 方法均有 Record 签名；
    `npm run build` 通过；`npm test -- --run` 不新增 FAIL。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool args → PowerPoint.run | slide_index/shape_id 经 ToolDef schema 验证后进 adapter；shape 不存在 → NOT_FOUND error |
| PowerPoint.run → proxy | proxy 在 run 内消费完毕，不出闭包（A-06）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | slide_index 越界 | mitigate | bounds check（idx < 0 或 >= slides.items.length → throw HostApiError NOT_FOUND）|
| T-06-03-02 | Tampering | shape_id 无效 | mitigate | find + if (!shape) throw HostApiError NOT_FOUND |
| T-06-03-03 | Tampering | PPT 并发改防御（D-11） | mitigate | expected_state 验证，mismatch → HostApiError，LLM 重评估 |
| T-06-03-04 | Tampering | Pitfall 2 null lineFormat | mitigate | lineFormat.color null guard，lineVisible=false 时用 visible=false 还原而非写 null 颜色 |
</threat_model>

<verification>
- grep -c "getItemOrNullObject\|isNullObject\|fill\\.clear" src/adapters/PptAdapter.ts 返回 ≥2（防御模式）
- grep "Record<string, unknown>" src/adapters/PptAdapter.ts 返回 ≥2（restoreShapeProperty + restoreShapeGeometry）
- `npm run build` 通过（TypeScript strict 无错）
- `npm test -- --run` 不新增 FAIL
</verification>

<success_criteria>
PptAdapter 4 个新方法实现完毕；Pitfall 2 fill null guard 存在；两个 inverse 方法使用 Record 签名；build 通过。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-03-SUMMARY.md`
</output>

---
phase: "06"
plan: "06"
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/agent/tools/write/ppt.ts
autonomous: true
requirements:
  - TOOL-03
  - ONB-02

must_haves:
  truths:
    - "ppt.ts 新增 set_shape_property（D-01 护城河）、move_shape 两个 ToolDef"
    - "set_shape_property 含 D-11 expected_state 可选参数"
    - "两工具均有中文 humanLabel、kind='write'、reverse（Record 对象 args）、postState"
    - "assertWriteToolRegisterable lint 守门通过"
  artifacts:
    - path: src/agent/tools/write/ppt.ts
      provides: "2 个新 PPT shape write tool defs（差异化护城河）"
      exports: ["setShapeProperty", "moveShape"]
  key_links:
    - from: set_shape_property
      to: PptAdapter.setShapeProperty
      via: "ctx.adapter as PptAdapter 调用，before-image → reverse=restore_shape_property"
      pattern: "restore_shape_property"
---

<objective>
实现 PPT 两个 shape write tools：set_shape_property（D-01 差异化护城河）、move_shape。

Purpose: SC4「把左下角那张图加红色边框右移 10px」= Aster 的 magic moment，Copilot Agent Mode 不暴露此能力。这是本 phase 最重要的差异化工具。
Output: ppt.ts 新增两个完整 ToolDef，set_shape_property 覆盖 fill/line color/weight/size，Wave 0 ppt.test.ts 转 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-write-tools-killer-scenarios/06-CONTEXT.md
@.planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md
@.planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md

<interfaces>
<!-- Source: src/agent/tools/write/ppt.ts — insertSlide 完整范式 -->
```typescript
export const insertSlide: ToolDef<InsertSlideArgs> = {
  name: 'insert_slide', kind: 'write',
  description: '...',
  parameters: { ... },
  humanLabel: ({ title }) => `在幻灯片末尾插入新幻灯片「${title.slice(0,20)}...」`,
  async execute(args, ctx): Promise<ToolResult> {
    const { insertedIndex } = await (ctx.adapter as PptAdapter).insertSlideAfter(args.afterIndex ?? -1, args.title);
    const reverse: ReverseDescriptor = { tool: 'delete_slide_by_title', args: { titleFingerprint: args.title } };
    const postState: PostStateSnapshot = { kind: 'ppt_slide', content: { ... } };
    return { ok: true, data: { insertedIndex }, reverse, postState };
  },
};
```

<!-- Source: RESEARCH.md — set_shape_property args schema（lines 374-388）-->
```typescript
interface SetShapePropertyArgs {
  slide_index: number;      // 1-based
  shape_id: string;         // 来自 list_shapes_on_slide
  fill_color?: string;      // #RRGGBB
  line_color?: string;      // #RRGGBB
  line_weight?: number;     // points
  width?: number;           // points（可选）
  height?: number;          // points（可选）
  expected_state?: { fill_color?: string; line_color?: string };  // D-11 可选
}
// reverse（from PATTERNS.md lines 135-158）：
const reverse: ReverseDescriptor = {
  tool: 'restore_shape_property',
  args: { slide_index, shape_id, fill_type, fill_color, line_color, line_weight, line_visible, width, height }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ppt.ts 新增 set_shape_property + move_shape ToolDef</name>
  <files>src/agent/tools/write/ppt.ts</files>
  <read_first>
    - src/agent/tools/write/ppt.ts（全文，了解 insertSlide ToolDef 完整结构、imports）
    - src/adapters/PptAdapter.ts（确认 setShapeProperty/restoreShapeProperty/moveShape/restoreShapeGeometry 签名，Plan 03 刚加的）
    - .planning/phases/06-write-tools-killer-scenarios/06-PATTERNS.md（ppt.ts 段：set_shape_property reverse 结构 lines 135-158；humanLabel 范例 lines 160-167）
    - .planning/phases/06-write-tools-killer-scenarios/06-RESEARCH.md（set_shape_property args schema lines 374-388）
  </read_first>
  <action>
    在 ppt.ts 末尾（insertSlide 之后）新增两个 ToolDef 导出：

    1. `export const setShapeProperty: ToolDef<SetShapePropertyArgs>`
       - name: 'set_shape_property', kind: 'write'
       - description: '修改 PPT 指定幻灯片形状的填充色、边框色/粗细或尺寸。slide_index 为 1-based，shape_id 来自 list_shapes_on_slide。'
       - parameters:
         · slide_index: { type: 'number', description: '幻灯片编号（1开始）' }（required）
         · shape_id: { type: 'string', description: '形状 ID，来自 list_shapes_on_slide' }（required）
         · fill_color?: { type: 'string', description: '填充色 #RRGGBB' }
         · line_color?: { type: 'string', description: '边框色 #RRGGBB' }
         · line_weight?: { type: 'number', description: '边框粗细（points）' }
         · width?: { type: 'number', description: '宽度（points）' }
         · height?: { type: 'number', description: '高度（points）' }
         · expected_state?: { type: 'object', description: 'D-11 可选并发防御' }
       - humanLabel：拼接实际改了哪些属性：
         ```typescript
         humanLabel: ({ slide_index, shape_id, fill_color, line_color, line_weight }) => {
           const changes: string[] = [];
           if (fill_color) changes.push(`填充色改为 ${fill_color}`);
           if (line_color) changes.push(`边框色改为 ${line_color}`);
           if (line_weight !== undefined) changes.push(`边框粗细改为 ${line_weight}pt`);
           return `将第 ${slide_index} 张幻灯片形状「${shape_id}」${changes.join('，') || '尺寸调整'}`;
         }
         ```
       - execute:
         · const { beforeImage } = await (ctx.adapter as PptAdapter).setShapeProperty(slide_index, shape_id, { fillColor: fill_color, lineColor: line_color, lineWeight: line_weight, width, height }, expected_state ? { fillColor: expected_state.fill_color, lineColor: expected_state.line_color } : undefined)
         · reverse = { tool: 'restore_shape_property', args: { slide_index, shape_id, fill_type: beforeImage.fillType, fill_color: beforeImage.fillColor, line_color: beforeImage.lineColor, line_weight: beforeImage.lineWeight, line_visible: beforeImage.lineVisible, width: beforeImage.width, height: beforeImage.height } }（Record 对象，全量 before-image）
         · postState = { kind: 'ppt_shape', content: { slide_index, shape_id, fill_color, line_color, line_weight, width, height } }
         · mutated = { fill_color, line_color, line_weight }（部分）

    2. `export const moveShape: ToolDef<...>`
       - name: 'move_shape', kind: 'write'
       - description: '移动 PPT 指定形状到新的 left/top 位置（points）。'
       - parameters: { slide_index: number（required），shape_id: string（required），left: number（required），top: number（required）}
       - humanLabel: ({ slide_index, shape_id, left, top }) => `将第 ${slide_index} 张幻灯片形状「${shape_id}」移动到 left=${left} top=${top}`
       - execute:
         · const { beforeLeft, beforeTop } = await (ctx.adapter as PptAdapter).moveShape(slide_index, shape_id, left, top)
         · reverse = { tool: 'restore_shape_geometry', args: { slide_index, shape_id, left: beforeLeft, top: beforeTop } }
         · postState = { kind: 'ppt_shape', content: { slide_index, shape_id, left, top } }
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/ppt.test.ts src/agent/tools/index.types.test.ts</automated>
  </verify>
  <done>
    grep -c "export const setShapeProperty\|export const moveShape" src/agent/tools/write/ppt.ts 返回 2；
    grep "restore_shape_property" src/agent/tools/write/ppt.ts 存在；
    grep "expected_state" src/agent/tools/write/ppt.ts 存在（D-11 可选参数）；
    `npm test -- --run src/agent/tools/index.types.test.ts` 通过；
    Wave 0 ppt.test.ts 取消 describe.skip 后 GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolDef.execute args → PptAdapter | slide_index/shape_id 经 schema 验证；LLM 传入无效值 → adapter 返 NOT_FOUND error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | slide_index 越界 | mitigate | adapter bounds check → HostApiError NOT_FOUND；dispatchTool sanitize 传 LLM |
| T-06-06-02 | Tampering | shape_id 无效 | mitigate | adapter find + guard；NOT_FOUND error |
| T-06-06-03 | Tampering | PPT 并发改防御（D-11） | mitigate | set_shape_property expected_state 可选参数；mismatch → error 让 LLM 重评估 |
</threat_model>

<verification>
- `npm test -- --run src/agent/tools/write/ppt.test.ts` GREEN
- grep "kind: 'write'" src/agent/tools/write/ppt.ts 返回 ≥2（setShapeProperty + moveShape）
- `npm run build` 通过
</verification>

<success_criteria>
两个 PPT shape write tools 实现完毕；护城河 set_shape_property 含 D-11 expected_state；test GREEN；build 通过。
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-tools-killer-scenarios/06-06-SUMMARY.md`
</output>

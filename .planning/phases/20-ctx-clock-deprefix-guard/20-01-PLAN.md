---
phase: 20-ctx-clock-deprefix-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/system-prompt.ts
  - src/agent/loop.ts
  - src/agent/system-prompt.test.ts
  - src/agent/loop.test.ts
autonomous: true
requirements:
  - CTX-01
  - CTX-02

must_haves:
  truths:
    - "buildSystemPrompt(host) 返回值不含 HH:MM 形态的分钟级时间（三宿主均如此）"
    - "agent 每次 runAgent 可从 messages 末尾的当前 user message 拿到精确日期+时间+星期（三宿主统一路径）"
    - "system-prompt.test.ts 有守门断言：buildSystemPrompt(host) 不匹配 /\\d{1,2}:\\d{2}/，CI 通过"
    - "现有全套测试（892+）全部 green；bundle 无变化（0 净新增依赖、纯字符串改动）"
  artifacts:
    - path: "src/agent/system-prompt.ts"
      provides: "buildTimeContext() export + 精简后的 getSharedBase(hostLabel) + 不含时间的 buildSystemPrompt"
      exports: ["buildSystemPrompt", "buildTimeContext"]
      contains: "export function buildTimeContext"
    - path: "src/agent/loop.ts"
      provides: "wire messages 末尾 user message 拼接 buildTimeContext()"
      contains: "buildTimeContext()"
    - path: "src/agent/system-prompt.test.ts"
      provides: "CTX-02 守门断言（not.toMatch）+ buildTimeContext 正向断言 + 旧断言已修"
      contains: "not.toMatch"
  key_links:
    - from: "src/agent/loop.ts"
      to: "src/agent/system-prompt.ts"
      via: "import { buildSystemPrompt, buildTimeContext } — buildTimeContext 拼到 wire 末尾 user message"
      pattern: "buildTimeContext"
    - from: "src/agent/system-prompt.test.ts"
      to: "src/agent/system-prompt.ts"
      via: "import { buildSystemPrompt, buildTimeContext } — 守门断言 + 正向断言"
      pattern: "buildTimeContext|not\\.toMatch"
---

<objective>
把实时时钟（today/clock/weekday）从 system prompt 前缀迁移到当前 user message 末尾，让 [system][tools][历史] 前缀变完全静态（prompt 缓存高命中），同时用结构性测试守门防止未来回退。

Purpose: 缓存铁律（STATE.md §缓存铁律）——每次请求都会变的内容一律放 messages 末尾，绝不放进静态前缀。时间从 system 前缀移走后，system prefix 稳定可缓存，DeepSeek Flash 缓存命中价约 50× 便宜（Pro 约 120×）。
Output: 改后 system-prompt.ts（新增 buildTimeContext export）+ 改后 loop.ts（wiring）+ 更新 system-prompt.test.ts（守门 + 正向断言 + 旧红断言修正）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ctx-clock-deprefix-guard/20-CONTEXT.md

<interfaces>
<!-- 当前 system-prompt.ts 关键结构（执行前已读，供参考） -->

当前 getSharedBase 签名（第 33 行，需改）：
```typescript
function getSharedBase(today: string, clock: string, weekday: string, hostLabel: string): string
```

当前 buildSystemPrompt（第 93-103 行，含时间计算逻辑）：
```typescript
export function buildSystemPrompt(host: HostKey, opts?: { userPrefs?: string }): string {
  const hostLabel = HOST_LABEL[host];
  const now = new Date();
  const today = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;
  const clock = `${String(now.getHours()).padStart(2, '0')}:${String(now.getMinutes()).padStart(2, '0')}`;
  const weekday = ['周日', '周一', '周二', '周三', '周四', '周五', '周六'][now.getDay()];
  const prefBlock = opts?.userPrefs ? `\n\n${buildPrefBlock(opts.userPrefs)}` : '';
  return `${getSharedBase(today, clock, weekday, hostLabel)}\n\n${getDomainSegment(host)}${prefBlock}`;
}
```

当前 getSharedBase 第 35 行时间句（需删除）：
```
现在是 ${today} ${weekday} ${clock}（用户本地时间）。凡涉及时间的计算（如入职时长、距今多久、"今年/本月/最近"等），以此为"现在"，不要自行假设年份或时间。
```

当前 loop.ts 第 70-77 行 wire messages 构造（第 76 行是注入站点）：
```typescript
const messages: WireMessage[] = [
  { role: 'system', content: buildSystemPrompt(host, userPrefs ? { userPrefs } : undefined) },
  ...historicalMsgs.map((m) => ({
    role: m.role as 'user' | 'assistant',
    content: m.content,
  })),
  { role: 'user', content: userPrompt },   // ← 注入站点（第 76 行）
];
```

当前 loop.ts import（第 18 行，需扩展 import buildTimeContext）：
```typescript
import { buildSystemPrompt } from './system-prompt';
```

system-prompt.test.ts 将变红的旧断言：
- 第 41-50 行 it('含运行时当前日期与时间...')：toContain(today)、toContain('现在是')、toContain('用户本地时间')
- 第 91-96 行 it('含今天日期注入')：toContain(year)（"2026"）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 buildTimeContext() + 重构 getSharedBase 去掉时间参数（CTX-01，D-20-02 + D-20-03）</name>
  <files>src/agent/system-prompt.ts</files>
  <read_first>
    - src/agent/system-prompt.ts（全文，已在 context interfaces 块中摘录关键部分）
    - .planning/phases/20-ctx-clock-deprefix-guard/20-CONTEXT.md §D-20-02 + D-20-03（锁定决策，以此为权威）
  </read_first>
  <action>
读取 src/agent/system-prompt.ts 全文后做以下修改：

**步骤 1：在 getSharedBase 函数定义之前新增 buildTimeContext() 导出函数**

在第 33 行 `function getSharedBase(...)` 之前插入：

```typescript
/**
 * 构造当前时间上下文后缀（D-20-02）。
 * 每次 runAgent 调用时取真实「现在」，拼到 wire 末尾 user message，
 * 绝不注入 system 前缀（缓存铁律：每次会变的内容一律放 messages 末尾）。
 */
export function buildTimeContext(): string {
  const now = new Date();
  const today = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;
  const clock = `${String(now.getHours()).padStart(2, '0')}:${String(now.getMinutes()).padStart(2, '0')}`;
  const weekday = ['周日', '周一', '周二', '周三', '周四', '周五', '周六'][now.getDay()];
  return `\n\n（当前时间：${today} ${weekday} ${clock}，用户本地时间。凡涉及时间的计算请以此为"现在"，不要自行假设年份或时间。）`;
}
```

**步骤 2：改写 getSharedBase 签名，删除时间参数，删除时间句**

将第 33 行的签名从：
```
function getSharedBase(today: string, clock: string, weekday: string, hostLabel: string): string {
```
改为：
```
function getSharedBase(hostLabel: string): string {
```

删除函数体第 35 行的完整时间句：
```
现在是 ${today} ${weekday} ${clock}（用户本地时间）。凡涉及时间的计算（如入职时长、距今多久、"今年/本月/最近"等），以此为"现在"，不要自行假设年份或时间。
```
（只删这一句，后面的空行和【任务执行原则】块保持不变）

**步骤 3：更新 buildSystemPrompt，移除时间计算，同步更新 getSharedBase 调用**

在 buildSystemPrompt 函数体（第 93-103 行）中：
- 移除 `const now = new Date();`
- 移除 `const today = ...`
- 移除 `const clock = ...`
- 移除 `const weekday = ...`
- 将 `getSharedBase(today, clock, weekday, hostLabel)` 改为 `getSharedBase(hostLabel)`
- `buildSystemPrompt(host, opts?)` 对外签名保持完全不变（向后兼容）

最终 buildSystemPrompt 应形如：
```typescript
export function buildSystemPrompt(host: HostKey, opts?: { userPrefs?: string }): string {
  const hostLabel = HOST_LABEL[host];
  const prefBlock = opts?.userPrefs ? `\n\n${buildPrefBlock(opts.userPrefs)}` : '';
  return `${getSharedBase(hostLabel)}\n\n${getDomainSegment(host)}${prefBlock}`;
}
```

**不动的内容：** getDomainSegment、buildPrefBlock、HOST_LABEL — 一字不改。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c 'export function buildTimeContext' src/agent/system-prompt.ts && grep -c '现在是.*用户本地时间' src/agent/system-prompt.ts; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function buildTimeContext' src/agent/system-prompt.ts` 返回 1（新 helper 已存在且导出）
    - `grep -c '现在是.*用户本地时间' src/agent/system-prompt.ts` 返回 0（旧时间句已删）
    - `grep -c 'today.*clock.*weekday\|clock.*weekday.*today' src/agent/system-prompt.ts` 返回 0（buildSystemPrompt 里不再算时间三变量）
    - `grep 'function getSharedBase' src/agent/system-prompt.ts` 输出只含 `hostLabel: string`，不含 `today` 或 `clock` 或 `weekday`
    - `grep -c 'getSharedBase(hostLabel)' src/agent/system-prompt.ts` 返回 1（buildSystemPrompt 里调用已更新）
    - `npx tsc --noEmit` 退出 0（无 TypeScript 类型错误）
  </acceptance_criteria>
  <done>system-prompt.ts 已导出 buildTimeContext()；getSharedBase 签名精简为 (hostLabel: string)；buildSystemPrompt 不再含时间计算；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 2: loop.ts 接线 buildTimeContext() 到当前 user message 末尾（CTX-01，D-20-04）</name>
  <files>src/agent/loop.ts</files>
  <read_first>
    - src/agent/loop.ts（全文，重点关注第 18 行 import 和第 70-77 行 messages 构造）
    - .planning/phases/20-ctx-clock-deprefix-guard/20-CONTEXT.md §D-20-04（接线决策 + 历史保持干净约束）
  </read_first>
  <action>
对 src/agent/loop.ts 做两处最小修改：

**修改 1：扩展第 18 行 import**

将：
```typescript
import { buildSystemPrompt } from './system-prompt';
```
改为：
```typescript
import { buildSystemPrompt, buildTimeContext } from './system-prompt';
```

**修改 2：第 76 行 wire messages 最后一条 user message**

将：
```typescript
    { role: 'user', content: userPrompt },
```
改为：
```typescript
    { role: 'user', content: `${userPrompt}${buildTimeContext()}` },
```

**关键约束（D-20-04，不可违反）：**
- 只改 wire 的 `messages` 数组，不改 chatStore 中存储的 raw userPrompt
- runAgent 调用前，chatStore 已在 UI 层保存了无时间的原始 userPrompt（`historicalMsgs` 来自 `useChatStore.getState().messages`，这些永远是干净的原文）
- 不需要 per-host 分支，三宿主走同一条主路径
- 文件内其余所有内容（resolveLLMConfig、agent loop、docKey、截断等）一字不改
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c 'buildTimeContext' src/agent/loop.ts; npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'buildTimeContext' src/agent/loop.ts` 返回 2（import + 调用各一处）
    - `grep 'import.*buildTimeContext' src/agent/loop.ts` 输出含 `buildTimeContext` 和 `buildSystemPrompt`（同一 import 语句）
    - `grep 'role.*user.*content' src/agent/loop.ts | grep 'buildTimeContext'` 有输出（user message 拼接已生效）
    - `npx tsc --noEmit` 退出 0
  </acceptance_criteria>
  <done>loop.ts 已 import buildTimeContext；wire messages 末尾 user message 拼接时间后缀；持久化历史（chatStore）不含时间戳；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 system-prompt.test.ts（CTX-02 守门 + 正向断言 + 修掉旧红断言）+ 可选 loop.test.ts wiring 断言</name>
  <files>src/agent/system-prompt.test.ts, src/agent/loop.test.ts</files>
  <read_first>
    - src/agent/system-prompt.test.ts（全文，特别注意第 41-50 行和第 91-96 行两个将变红的断言）
    - src/agent/loop.test.ts（全文，确认无系统时间相关断言；判断是否值得加 wiring 断言）
    - .planning/phases/20-ctx-clock-deprefix-guard/20-CONTEXT.md §D-20-05 + D-20-06
  </read_first>
  <action>
**system-prompt.test.ts 修改（必须）：**

**步骤 1：在顶部 import 行添加 buildTimeContext 导入**

将：
```typescript
import { buildSystemPrompt } from './system-prompt';
```
改为：
```typescript
import { buildSystemPrompt, buildTimeContext } from './system-prompt';
```

**步骤 2：修掉第 41-50 行「含运行时当前日期与时间」用例（变红 → 改为反向断言）**

将整个 it('含运行时当前日期与时间...') 用例改写为：

```typescript
  it('system prompt 不含运行时时钟（CTX-01：时钟已迁到 user message 末尾，不在 system 前缀）', () => {
    for (const host of ['word', 'excel', 'ppt'] as const) {
      const prompt = buildSystemPrompt(host);
      expect(prompt).not.toContain('现在是');
      expect(prompt).not.toMatch(/\d{1,2}:\d{2}/); // 不含 HH:MM 形态（CTX-02 前置验证）
    }
  });
```

**步骤 3：修掉第 91-96 行「含今天日期注入」用例**

将 it('含今天日期注入') 整个用例改写为：

```typescript
  it('system prompt 不含年份（时间已迁离，year 不再出现在 system 前缀）', () => {
    const year = new Date().getFullYear().toString();
    for (const host of ['word', 'excel', 'ppt'] as const) {
      expect(buildSystemPrompt(host)).not.toContain(year);
    }
  });
```

**步骤 4：在文件末尾（describe('buildSystemPrompt — PREF-01 偏好注入') 块之后）新增两个 describe 块**

```typescript
// ---------------------------------------------------------------------------
// Phase 20 — CTX-02 守门：system prompt 不含分钟级时钟（防回退）
// ---------------------------------------------------------------------------

describe('buildSystemPrompt — CTX-02 时钟守门', () => {
  it.each(['word', 'excel', 'ppt'] as const)(
    'host=%s system prompt 不匹配 /\\d{1,2}:\\d{2}/（分钟级 HH:MM 不在 system 前缀）',
    (host) => {
      const prompt = buildSystemPrompt(host);
      expect(prompt).not.toMatch(/\d{1,2}:\d{2}/);
    },
  );
});

// ---------------------------------------------------------------------------
// Phase 20 — buildTimeContext 正向断言：精确时间仍可达（success criteria #2）
// ---------------------------------------------------------------------------

describe('buildTimeContext — CTX-01 时间后缀正向断言', () => {
  it('返回值含今日 YYYY-MM-DD 日期', () => {
    const now = new Date();
    const today = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;
    expect(buildTimeContext()).toContain(today);
  });

  it('返回值含中文星期（周X 格式）', () => {
    expect(buildTimeContext()).toMatch(/周[日一二三四五六]/);
  });

  it('返回值匹配 /\\d{1,2}:\\d{2}/（含分钟级时钟 HH:MM）', () => {
    expect(buildTimeContext()).toMatch(/\d{1,2}:\d{2}/);
  });

  it('返回值含「用户本地时间」', () => {
    expect(buildTimeContext()).toContain('用户本地时间');
  });

  it('返回值含抗幻觉指引（不要自行假设年份）', () => {
    expect(buildTimeContext()).toContain('不要自行假设年份');
  });
});
```

**loop.test.ts 修改（可选，低成本则加）：**

读 loop.test.ts 全文确认无系统时间断言后，在 describe('runAgent — AGENT-01 自然 break') 或末尾新增一个轻量 wiring 断言：

```typescript
describe('runAgent — CTX-01 wire 时间后缀到当前 user message', () => {
  it('messages 构造时最后一条 user message 含分钟级时钟（HH:MM）', async () => {
    // 捕获实际发给 LLM 的 messages
    let capturedMessages: unknown[] | undefined;
    (OpenAICompatibleLLM as unknown as ReturnType<typeof vi.fn>).mockImplementation(() => ({
      async *streamChat(messages: unknown[]) {
        capturedMessages = messages;
        yield { type: 'delta', content: 'done' } as never;
      },
    }));
    const ctrl = useAgentStore.getState().beginRun('r-ctx01');
    await runAgent('测试用户输入', undefined, mockAdapter, ctrl.signal, 'r-ctx01');
    const lastMsg = capturedMessages?.[capturedMessages.length - 1] as { role: string; content: string } | undefined;
    expect(lastMsg?.role).toBe('user');
    expect(lastMsg?.content).toMatch(/\d{1,2}:\d{2}/); // 含时钟 HH:MM（buildTimeContext 已拼入）
  });
});
```

如果在读 loop.test.ts 后判断上述 wiring 测试会引入复杂 mock 问题，可以跳过（D-20-06 明确「不强制」）；system-prompt.test.ts 的守门断言 + buildTimeContext 正向断言已足够覆盖 CTX-02。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CTX-02\|时钟守门' src/agent/system-prompt.test.ts` 返回 >= 1（守门 describe 存在）
    - `grep -c 'not\.toMatch.*\\d.*:\\d\|not\.toMatch.*\\/\\\\d' src/agent/system-prompt.test.ts` 返回 >= 3（三宿主守门断言均存在）
    - `grep -c 'buildTimeContext' src/agent/system-prompt.test.ts` 返回 >= 6（import + 5 个正向断言用例）
    - `grep -c '用户本地时间' src/agent/system-prompt.test.ts` 返回 >= 1（buildTimeContext 正向断言含此字段）
    - `grep -c '不要自行假设年份' src/agent/system-prompt.test.ts` 返回 >= 1（抗幻觉指引正向断言存在）
    - `npm test -- --run` 退出 0，无「N failed」（尾部 3 个 retry errors 是噪音，可忽略）
    - `npm test -- --run 2>&1 | grep 'failed'` 不含真实失败行（或行数 <= 尾部噪音行数）
  </acceptance_criteria>
  <done>system-prompt.test.ts 旧红断言已修为反向断言；CTX-02 守门三宿主均覆盖；buildTimeContext 5 条正向断言全绿；全套测试 green（892+）。</done>
</task>

<task type="auto">
  <name>Task 4: 最终验证 — tsc + npm test + build + size（bundle 无变化确认）</name>
  <files></files>
  <read_first>
    - 无（仅运行验证命令）
    - 注意 memory project_bundle_size_guard：npm run size 测陈旧 dist 给假绿，必须先 build 再 size
  </read_first>
  <action>
按顺序运行以下命令，逐一确认：

1. 类型检查：`npx tsc --noEmit`
2. 全套测试：`npm test -- --run`（或 `npm test`）
3. 构建并验 bundle：`npm run build && npm run size`

确认要点：
- tsc 退出 0
- 测试退出 0（「N failed」= 真失败；尾部 3 个 retry errors 是噪音）
- bundle size 报告：main bundle gzip ≤82KB（本 phase 预期 0 增量，即与上次 ~80.53KB 持平）

如果 build 失败或 size 超标，回溯检查三个修改文件是否有语法错误或多余 import。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -5 && npm test -- --run 2>&1 | tail -10 && npm run build 2>&1 | tail -5 && npm run size 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 退出 0（输出无错误行）
    - `npm test -- --run` 退出 0，无「N failed」（三个 retry errors 是噪音）
    - `npm run size` 输出的 main bundle gzip 值 ≤82KB（预期与 ~80.53KB 持平）
    - 以下 grep 在 dist/ 构建产物中不出现（纯静态检查）：`grep -r '现在是.*用户本地时间' dist/` 返回空（旧时间句已删，不会出现在构建产物里）
  </acceptance_criteria>
  <done>tsc 0 + 全套测试 green + bundle ≤82KB；Phase 20 CTX-01 + CTX-02 交付完毕。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buildTimeContext() 输出 → wire messages | 本地 new Date() 计算，非用户输入；输出拼到 userPrompt 末尾送往 LLM |
| userPrompt（用户输入）→ wire messages | 已存在的信任边界，本 phase 未改变其处理方式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | buildTimeContext() 输出 | accept | 输出来自 `new Date()`（本地运行时，非用户输入、非网络数据）；攻击者无法控制其内容。不引入新注入向量：userPrompt 原本就直接发往 LLM，拼接后缀不改变其信任级别。ASVS L1 trivially satisfied。 |
| T-20-02 | Information Disclosure | system-prompt.ts 时间移除 | accept | 将时间从 system 前缀移走只减少静态信息，不引入新信息泄露路径；API Key 仍只存于 partitioned localStorage，不经本 phase 改动。 |
</threat_model>

<verification>
```bash
# 类型检查
cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit

# 全套测试（含新守门断言）
npm test -- --run

# 确认 system prompt 不含时钟（三宿主全覆盖，CTX-02 关键 grep）
node -e "
const { buildSystemPrompt, buildTimeContext } = require('./src/agent/system-prompt.ts');
" 2>/dev/null; npx vitest run src/agent/system-prompt.test.ts 2>&1 | tail -10

# bundle 验证（先 build 再 size，不用陈旧 dist）
npm run build && npm run size
```
</verification>

<success_criteria>
1. `buildSystemPrompt(host)` 三宿主返回值均不匹配 `/\d{1,2}:\d{2}/`（分钟级时钟已迁离 system 前缀）
2. `buildTimeContext()` 返回值含 YYYY-MM-DD 日期、周X 星期、HH:MM 时钟、「用户本地时间」、「不要自行假设年份」——精确时间每轮 runAgent 仍可达（拼在 wire 末尾 user message）
3. `system-prompt.test.ts` 包含三宿主守门断言 `expect(prompt).not.toMatch(/\d{1,2}:\d{2}/)` + buildTimeContext 5 条正向断言，CI 通过
4. 全套测试 green（892+）；tsc 退出 0；bundle main gzip ≤82KB（0 净新增依赖，预期与上次持平）
</success_criteria>

<output>
完成后创建 `.planning/phases/20-ctx-clock-deprefix-guard/20-01-SUMMARY.md`
</output>

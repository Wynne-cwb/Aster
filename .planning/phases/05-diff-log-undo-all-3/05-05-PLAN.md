---
phase: 05-diff-log-undo-all-3
plan: 05
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - src/adapters/ExcelAdapter.ts
  - src/adapters/ExcelAdapter.test.ts
autonomous: true
requirements:
  - TOOL-03
  - AGENT-10
  - AGENT-11

must_haves:
  truths:
    - "ExcelAdapter 新增 setRangeValues(address, values) 方法，先 load before-image，再覆写目标 range"
    - "ExcelAdapter 新增 overwriteRange(address, values) inverse 方法（SP-4 已验证可行）"
    - "两方法均在自己的 Excel.run 闭包内（A-06），两 sync 节律（load→sync1 抓 before-image → write→sync2）"
    - "ExcelAdapter.test.ts setRangeValues + overwriteRange describe 全 pass（mock Excel.run）"
  artifacts:
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "setRangeValues before-image write + overwriteRange inverse"
      contains: "setRangeValues\|overwriteRange"
    - path: "src/adapters/ExcelAdapter.test.ts"
      provides: "before-image inverse mock 单测"
      contains: "setRangeValues\|overwriteRange"
  key_links:
    - from: "src/agent/operationLog.ts"
      to: "src/adapters/ExcelAdapter.ts"
      via: "executeReverse: 'overwrite_range' → adapter.overwriteRange(address, values)"
      pattern: "overwriteRange"
---

<objective>
Wave 2b：ExcelAdapter 新增两个方法：`setRangeValues`（写操作 + 同时抓 before-image）和 `overwriteRange`（inverse 覆写）。SP-4 已真机验证 `range.load(['values','address'])` + `range.values =` 两 sync 路径可行。

Purpose: 建立 Excel inverse 路径——天然任意顺序（Excel cell address 不随插删变化），before-image 覆写是幂等操作。
Output: ExcelAdapter 两个新方法 + mock 单测 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- ExcelAdapter 现有方法供 executor 直接参考 -->

From src/adapters/ExcelAdapter.ts (get_range_values 191-229，two-sync 范式):
```typescript
// ExcelAdapter two-sync 守则（line 96-101 注释）：load → sync 1 → write → sync 2
const range = ctx.workbook.worksheets.getActiveWorksheet().getRange(address);
range.load(['values', 'address']);
await ctx.sync();   // sync 1: 读数据
// ... 处理 ...
// sync 2: 写数据
```

From .planning/spikes/SP-4-reverse-ops/findings.md:
// PASS: range.load(['values','address']) → sync → before-image 已读
// PASS: range.values = storedValues → sync → 覆写完成

From src/adapters/ExcelAdapter.ts (insert() two-sync 范式 102-151，确认 Excel.run 错误包范式)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExcelAdapter.setRangeValues + overwriteRange</name>
  <files>
    src/adapters/ExcelAdapter.ts
    src/adapters/ExcelAdapter.test.ts
  </files>
  <read_first>
    - src/adapters/ExcelAdapter.ts（全文——重点 get_range_values 191-229 两 sync 范式；insert() 102-151 错误包范式）
    - src/adapters/DocumentAdapter.ts（确认是否需加可选 inverse 方法声明）
    - src/errors/index.ts（HostApiError line 188-198）
    - src/adapters/ExcelAdapter.test.ts（Wave 0 新增的 setRangeValues stubs）
    - .planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md（Pattern 2 Excel，line 325-378）
    - .planning/spikes/SP-4-reverse-ops/findings.md（Excel before-image PASS 结论）
  </read_first>
  <behavior>
    - `setRangeValues(address: string, values: unknown[][]): Promise<{ beforeImage: { address: string; values: unknown[][] } }>`
      在 Excel.run 闭包内：
      1. `range.load(['values', 'address'])` + sync 1 → `beforeImage = { address: range.address, values: range.values }`
      2. `range.values = values` + sync 2（覆写）
      3. return `{ beforeImage }`
    - `overwriteRange(address: string, values: unknown[][]): Promise<void>`
      在 Excel.run 闭包内：
      1. `range.values = values` + sync（直接覆写，不抓 before-image）
    - 两方法均包 `try { await Excel.run(...) } catch (err) { throw new HostApiError('Excel xxx 失败', err) }`
    - 空单元格规范化说明（注释）：`overwriteRange` 不做 null/0/"" 规范化（写什么就是什么）；`isTargetStateConsistent` 的规范化在 operationLog.ts 内处理
    - DocumentAdapter 接口：可选加 `setRangeValues?(address, values): Promise<...>` + `overwriteRange?(address, values): Promise<void>` 可选方法声明（与 WordAdapter 同策略）
    - 测试（mock Excel.run）：
      - `setRangeValues 返回 before-image address + values`：mock range.address = 'A1:B2'，range.values = [[1,2],[3,4]]；断言 beforeImage 含这些值
      - `setRangeValues 覆写成功`：断言 range.values 被赋值为传入 values
      - `overwriteRange 执行覆写不返回 before-image`：断言 range.values 赋值正确，return 为 void
  </behavior>
  <action>
    1. 读 ExcelAdapter.ts 全文，在 class 末尾新增 setRangeValues + overwriteRange 方法。
    2. 参照 get_range_values 的 two-sync 节律：sync1 load before-image → sync2 write（注意：需在 sync1 之后才能读 range.address，因为 address 是 server 端属性）。
    3. 在 ExcelAdapter.test.ts 找到 Wave 0 stubs，实现真实 mock（Excel.run mock 返回 mockCtx，mockCtx.workbook.worksheets.getActiveWorksheet().getRange() 返回 mock range 对象含 load/values/address 属性）。
    4. 运行 `npx vitest run src/adapters/ExcelAdapter.test.ts` 确认 setRangeValues + overwriteRange describe 全 pass。
    5. 运行 `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep error` 确认无 TS error。
  </action>
  <verify>
    <automated>npx vitest run src/adapters/ExcelAdapter.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>ExcelAdapter.test.ts setRangeValues + overwriteRange describe 全 pass；`grep -c "setRangeValues\|overwriteRange" src/adapters/ExcelAdapter.ts` 返回 ≥2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExcelAdapter → Office.js Excel.run | proxy 生命周期在 Excel.run 闭包内，两 sync 节律（A-06） |
| overwriteRange values 参数 | 来自 before-image（write tool 执行前抓取的原始 cell 值），非 LLM 直接控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | overwriteRange 覆写错误 range | mitigate | address 由 setRangeValues 的 beforeImage.address 记录（Excel server 端 normalization，如 Sheet1!A1:B2），replay 前 readTargetState 比对 postState 守门 |
| T-05-05-02 | Denial of Service | Excel.run 超时 | accept | replay engine catch 所有错误（D-11 continue-on-error）；用户得到「未能回滚」提示 |

No high-severity threats.
</threat_model>

<verification>
1. `npx vitest run src/adapters/ExcelAdapter.test.ts` — setRangeValues + overwriteRange 测试全 pass
2. `grep -c "setRangeValues\|overwriteRange" src/adapters/ExcelAdapter.ts` — ≥2
3. `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` — 无新 error
4. `npx eslint src/adapters/ExcelAdapter.ts 2>&1 | grep error | head -5` — 无 error
</verification>

<success_criteria>
- ExcelAdapter.setRangeValues：先 load before-image，再覆写，返回 beforeImage
- ExcelAdapter.overwriteRange：直接覆写，不记 before-image（是 inverse 方法，由 replay engine 以 before-image 数据调用）
- 两方法均在 Excel.run 闭包内，proxy 不出闭包（A-06）
- mock 单测全 pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-05-SUMMARY.md`
</output>

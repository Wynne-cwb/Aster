---
phase: 05-diff-log-undo-all-3
plan: 04
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - src/adapters/WordAdapter.ts
  - src/adapters/WordAdapter.test.ts
autonomous: true
requirements:
  - TOOL-03
  - AGENT-10
  - AGENT-11

must_haves:
  truths:
    - "WordAdapter 新增 deleteParagraphByContent(text) 方法，从尾到头遍历找内容指纹删段"
    - "normalizeText 规范化：s.replace(/\\r\\n/g, '\\n').trimEnd()，防 Office.js 格式归一化 false-skip（Pitfall 2）"
    - "方法在自己的 Word.run 闭包内开闭一次（A-06），找不到目标段落 → 抛 HostApiError"
    - "WordAdapter.test.ts deleteParagraphByContent describe 全 pass（mock Word.run）"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "deleteParagraphByContent + normalizeText inverse 方法"
      contains: "deleteParagraphByContent"
    - path: "src/adapters/WordAdapter.test.ts"
      provides: "inverse 方法单测（mock Office.js）"
      contains: "deleteParagraphByContent"
  key_links:
    - from: "src/agent/operationLog.ts"
      to: "src/adapters/WordAdapter.ts"
      via: "executeReverse: 'delete_paragraph_by_content' → adapter.deleteParagraphByContent(args.text)"
      pattern: "deleteParagraphByContent"
---

<objective>
Wave 2a：WordAdapter 新增 `deleteParagraphByContent(text: string)` inverse 方法，用内容指纹（从尾到头遍历）精确删除目标段落。这是 Word append_paragraph 的精确反操作，解决 Phase 3 `delete_last_paragraph` 在任意顺序撤销时删错段的问题（RESEARCH Pitfall 1）。

Purpose: 建立 Word inverse 路径的可测 adapter 方法——replay engine 通过此方法执行 Word undo，而无需暴露 Office.js proxy 到 agent 层（A-06）。
Output: WordAdapter.deleteParagraphByContent + mock 单测 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- WordAdapter 现有方法供 executor 直接参考 -->

From src/adapters/WordAdapter.ts (appendParagraph 129-138，是 inverse 方法的模板):
```typescript
async appendParagraph(text: string): Promise<void> {
  try {
    await Word.run(async (ctx) => {
      ctx.document.body.insertParagraph(text, Word.InsertLocation.end);
      await ctx.sync();
    });
  } catch (err) {
    throw new HostApiError('Word append_paragraph 失败', err);
  }
}
```

From src/adapters/WordAdapter.ts (read('get_paragraph_count') 158-167，遍历段落范式):
```typescript
const paras = ctx.document.body.paragraphs;
paras.load('items/text');
await ctx.sync();
// 遍历 paras.items[]
```

From src/adapters/DocumentAdapter.ts:
// 检查 DocumentAdapter 接口是否声明了 deleteParagraphByContent（Wave 2 前未加，此处可直接在 WordAdapter 上定义，不必改接口）
```

From src/agent/operationLog.ts (Phase 5 重构后，executeReverse switch):
// 'delete_paragraph_by_content' → adapter.deleteParagraphByContent(args.text as string)
// adapter 参数类型是 DocumentAdapter；TypeScript 会报 deleteParagraphByContent 不在 DocumentAdapter 上
// → 解决：在 executeReverse 内 cast：(adapter as WordAdapter).deleteParagraphByContent(...)
// 或扩展 DocumentAdapter 接口加可选 inverse 方法声明
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WordAdapter.deleteParagraphByContent + normalizeText</name>
  <files>
    src/adapters/WordAdapter.ts
    src/adapters/WordAdapter.test.ts
  </files>
  <read_first>
    - src/adapters/WordAdapter.ts（全文——重点 appendParagraph line 129-138 + read('get_paragraph_count') 范式）
    - src/adapters/DocumentAdapter.ts（确认是否需要在接口层加 optional inverse 方法声明）
    - src/errors/index.ts（HostApiError 类，line 188-198）
    - src/adapters/WordAdapter.test.ts（Wave 0 新增的 deleteParagraphByContent describe stubs）
    - .planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md（Pattern 2 Word，line 278-303）
    - .planning/spikes/SP-4-reverse-ops/findings.md（Word paragraph.delete() 真机 PASS 证据）
  </read_first>
  <behavior>
    - `normalizeText(s: string): string` = `s.replace(/\r\n/g, '\n').trimEnd()`（模块私有函数）
    - `deleteParagraphByContent(text: string): Promise<void>` 在 Word.run 闭包内：
      1. `paras.load('items/text')` + sync
      2. 从尾到头 `for (let i = paras.items.length - 1; i >= 0; i--)` 比对 `normalizeText(paras.items[i].text) === normalizeText(text)`
      3. 找到：`paras.items[i].delete()` + sync，return
      4. 未找到：`throw new HostApiError('Word deleteParagraphByContent: 目标段落已不存在', undefined)`
    - 整个方法包 `try { await Word.run(...) } catch (err) { throw new HostApiError('Word deleteParagraphByContent 失败', err) }`
    - 测试（mock Office.js，不调真实 Word）：
      - `找到匹配文本的段落并删除`：mock paras.items 含 [{text:'hello'}, {text:'world'}]，调 deleteParagraphByContent('hello')，断言 delete() 被调用 1 次
      - `找不到目标段落 → 抛 HostApiError`：mock items 不含目标文本
      - `规范化：word 末段 text 为 "hello\r\n" 与输入 "hello" 等价（不触发 HostApiError）`
      - `从尾到头遍历（最近追加的段落优先）`：mock 两个同名段，断言 delete 调的是最后那个 index
  </behavior>
  <action>
    1. 读 WordAdapter.ts 全文，在 class 末尾新增 `normalizeText` private 函数（或 module-level 私有）和 `deleteParagraphByContent` 方法。
    2. 确认 DocumentAdapter.ts——如果是接口文件，可选：在 DocumentAdapter 接口加 `deleteParagraphByContent?(text: string): Promise<void>` 可选声明；或在 executeReverse 内用类型断言（`(adapter as WordAdapter).deleteParagraphByContent`）。选前者更干净（接口声明可选方法）。
    3. 在 WordAdapter.test.ts 找到 Wave 0 新增的 stubs，实现真实 mock（用 vi.fn() mock `Word.run`，让 mock 的 paragraphs.load + sync + items 返回伪数据）。Word.run mock 范式：`vi.fn().mockImplementation(async (fn) => fn(mockCtx))`。
    4. 运行 `npx vitest run src/adapters/WordAdapter.test.ts` 确认所有 deleteParagraphByContent 测试 pass。
    5. 运行 `npx eslint src/adapters/WordAdapter.ts` 确认无问题（A-06 不约束 adapter 文件）。
  </action>
  <verify>
    <automated>npx vitest run src/adapters/WordAdapter.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>WordAdapter.test.ts 中所有 deleteParagraphByContent 测试 pass；`grep -c "deleteParagraphByContent" src/adapters/WordAdapter.ts` 返回 ≥1；`grep -c "normalizeText" src/adapters/WordAdapter.ts` 返回 ≥1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WordAdapter → Office.js Word.run | proxy 生命周期在 Word.run 闭包内，不出闭包（A-06） |
| deleteParagraphByContent args.text → paragraph.delete() | text 来自 write tool 执行时记录的原始 text 参数，非 LLM 直接控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | deleteParagraphByContent 误删错误段落 | mitigate | normalizeText 规范化后从尾到头遍历（只删最近追加的同名段），减少误删风险；replay 前还有 readTargetState 比对守门 |
| T-05-04-02 | Denial of Service | Word.run 超时 | accept | dispatchTool 已有 15s 超时守门（工具调用层）；inverse 方法在 replay engine 中调用，replay engine catch 所有错误（D-11）|

No high-severity threats. Inverse path only triggered by explicit user undo action.
</threat_model>

<verification>
1. `npx vitest run src/adapters/WordAdapter.test.ts` — deleteParagraphByContent describe 全 pass
2. `grep -c "deleteParagraphByContent" src/adapters/WordAdapter.ts` — ≥1
3. `grep -c "normalizeText" src/adapters/WordAdapter.ts` — ≥1（防 false-skip 归一化函数存在）
4. `npx eslint src/adapters/WordAdapter.ts 2>&1 | grep error | head -5` — 无 error
5. `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` — 无新 TS error
</verification>

<success_criteria>
- WordAdapter.deleteParagraphByContent：从尾到头遍历找内容指纹，删目标段，找不到抛 HostApiError
- normalizeText 规范化防 Office.js false-skip（\r\n + 尾部空白归一）
- mock 单测 4 个用例全 pass（找到删除 / 找不到报错 / 规范化等价 / 尾部优先）
- 整个方法在 Word.run 闭包内，proxy 不出闭包（A-06）
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-04-SUMMARY.md`
</output>

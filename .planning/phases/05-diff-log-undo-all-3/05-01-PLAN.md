---
phase: 05-diff-log-undo-all-3
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agent/operationLog.test.ts
  - src/lib/storage.test.ts
  - src/lib/copyStepLog.test.ts
  - src/agent/tools/write/word.test.ts
  - src/adapters/WordAdapter.test.ts
  - src/adapters/ExcelAdapter.test.ts
  - src/adapters/PptAdapter.test.ts
autonomous: true
requirements:
  - AGENT-10
  - AGENT-11
  - TOOL-03
  - TOOL-04
  - CARRY-03
  - NFR-05

must_haves:
  truths:
    - "operationLog.test.ts 有针对 Map<runId> CRUD + 逆序 replayUndoAll + skip_manual + continue-on-error (D-11) 的 failing tests"
    - "copyStepLog.test.ts 有三角色 dump 结构测试 + 脱敏 API Key 断言（不含 sk-*）"
    - "storage.test.ts 有 QuotaExceededError → StorageQuotaError 转换用例"
    - "word.test.ts 将旧 reverse.tool='delete_last_paragraph' 断言改为 'delete_paragraph_by_content' + args.text"
    - "WordAdapter.test.ts 有 deleteParagraphByContent mock 单测 stub"
    - "ExcelAdapter.test.ts 有 setRangeValues before-image + overwriteRange mock stub"
    - "PptAdapter.test.ts 有 insertSlideAfter + deleteSlideByTitle 的 mock stubs（mock PowerPoint.run + vi.fn()）"
  artifacts:
    - path: "src/agent/operationLog.test.ts"
      provides: "Map<runId> + replay 测试框架（Wave 0 stubs）"
    - path: "src/lib/copyStepLog.test.ts"
      provides: "copy step log 脱敏测试框架"
    - path: "src/lib/storage.test.ts"
      provides: "quota guard 测试用例（扩展已有文件）"
    - path: "src/agent/tools/write/word.test.ts"
      provides: "reverse 精确定位验收断言（旧断言同步更新）"
    - path: "src/adapters/WordAdapter.test.ts"
      provides: "inverse 方法 mock 框架"
    - path: "src/adapters/ExcelAdapter.test.ts"
      provides: "before-image inverse mock 框架"
    - path: "src/adapters/PptAdapter.test.ts"
      provides: "insertSlideAfter + deleteSlideByTitle inverse mock 框架（Wave 6 可在此扩展）"
  key_links:
    - from: "operationLog.test.ts"
      to: "src/agent/operationLog.ts"
      via: "__resetOperationLogForTest + getOperationsByRun"
      pattern: "__resetOperationLogForTest|getWriteOpsByRun"
---

<objective>
Wave 0：在所有实现 plan 执行前，先把测试 stubs 建好（或更新旧断言），让后续 wave 的代码改动有即时 Nyquist 守门。

Purpose: 测试先行——每个 Wave 1-3 plan 落地代码后能立即跑对应文件的 test 并得到 RED→GREEN 反馈，而不是最后补测试。
Output: 6 个测试文件（4 个扩展已有文件、2 个新建）——全部 failing（因为实现还没到位）或 passing（旧断言更新 + 新 stub 框架）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- 现有文件关键接口，executor 必须知道 -->

From src/agent/operationLog.ts (Phase 3 骨架，Wave 1 将重构):
```typescript
export interface ReverseDescriptor {
  tool: string;
  args: Record<string, unknown>;
}
export interface OperationLogEntry {
  runId: string; stepIndex: number; toolName: string; args: unknown;
  humanLabel: string; reverse: ReverseDescriptor; timestamp: number;
  // Phase 5 将新增: postState: PostStateSnapshot;
}
export function appendOperation(entry: OperationLogEntry): void
export function getOperationsByRun(runId: string): OperationLogEntry[]
export function __resetOperationLogForTest(): void
// Phase 5 将新增: getWriteOpsByRun / replayUndoAll / UndoResult
```

From src/agent/tools/write/word.test.ts (已存在，line 82 需改):
```typescript
// 当前 assert（需同步改）:
// expect(result.reverse).toEqual({ tool: 'delete_last_paragraph', args: {} })
// Phase 5 目标:
// expect(result.reverse).toEqual({ tool: 'delete_paragraph_by_content', args: { text: '...' } })
// expect(result.postState).toEqual({ kind: 'word_paragraph', content: '...' })
```

From src/lib/storage.ts (line 66-68，Wave 1b 将包 try/catch):
```typescript
set(rawKey: string, value: unknown): void {
  localStorage.setItem(prefixedKey(rawKey), JSON.stringify(value));
}
```

From src/lib/debugReport.ts (copyStepLog 的脱敏 + clipboard 范式):
```typescript
export async function buildDebugReport(): Promise<string>
export async function copyToClipboard(text: string): Promise<boolean>
// debugReport.test.ts:91-96 脱敏断言范式:
// expect(report).not.toContain('sk-SECRET-abc123')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新 word.test.ts reverse 断言 + 新建 operationLog.test.ts 与 copyStepLog.test.ts stubs</name>
  <files>
    src/agent/tools/write/word.test.ts
    src/agent/operationLog.test.ts
    src/lib/copyStepLog.test.ts
  </files>
  <read_first>
    - src/agent/tools/write/word.test.ts（全文，重点 line 82 旧 reverse 断言）
    - src/agent/operationLog.test.ts（全文，了解已有测试结构）
    - src/agent/operationLog.ts（全文，了解当前导出函数名）
    - src/lib/debugReport.ts（line 1-30 安全约束注释 + line 261-282 copyToClipboard）
    - src/lib/debugReport.test.ts（line 85-100 脱敏断言范式）
  </read_first>
  <behavior>
    - word.test.ts：找到 `delete_last_paragraph` 断言，改为 `delete_paragraph_by_content` + `args: { text: 期望的文本 }`；新增 `expect(result.postState).toEqual({ kind: 'word_paragraph', content: 期望文本 })`；新增 `expect(result.reverse).toBeDefined()` 断言（TOOL-04）
    - operationLog.test.ts：新增 describe 'Map<runId> 重构' 含以下 it（Wave 1 未实现前这些 it 预期 FAIL 或需 skip）：
      - `appendOperation 按 runId 分组存储（Map<runId>）`
      - `getWriteOpsByRun 只返回 reverse 非空的条目`
      - `replayUndoAll 逆序遍历（最后写的先撤）`
      - `replayUndoAll：手动改不一致 → status=skipped_manual`
      - `replayUndoAll：reverse 报错 → status=skipped_error，继续撤剩余（D-11）`
      - `__resetOperationLogForTest 清除 Map`
    - copyStepLog.test.ts（新建）：
      - describe 'buildStepLog' 包含：`三角色 user/assistant/tool 都出现在输出` + `工具调用含 humanLabel` + `输出不含 sk- 前缀字符串（脱敏 D-21）` + `输出不含 Provider id 原文`
  </behavior>
  <action>
    1. 读 word.test.ts 找 `delete_last_paragraph`，改为 `delete_paragraph_by_content` + 加 postState 断言。TOOL-04 守门：在同测试中加 `expect(result.reverse).toBeDefined()`。
    2. 读 operationLog.test.ts，在文件末尾追加 describe block，每个 it 用 `vi.fn()` mock adapter 方法。beforeEach 调 `__resetOperationLogForTest()`。Wave 1 未实现前，对 `getWriteOpsByRun` / `replayUndoAll` 用 `vi.importActual` + 占位（或用 `it.todo`），避免 TS 报错找不到导出。
    3. 新建 copyStepLog.test.ts，import `{ buildStepLog }` from `'../lib/copyStepLog'`（Wave 5 才实现，此处让 tsc 报错 TS2307 OK，Wave 0 意图是写好 test 框架）。脱敏断言：`expect(output).not.toMatch(/sk-[A-Za-z0-9]+/))`。
    4. 每次改完相关文件后运行 `npx vitest run src/agent/tools/write/word.test.ts` 确认 word.test.ts 仍 pass（旧测试只改断言，实现改在 Wave 3）。
  </action>
  <verify>
    <automated>npx vitest run src/agent/tools/write/word.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>word.test.ts 所有测试 pass（reverse 断言已改为 delete_paragraph_by_content）；operationLog.test.ts 新增 describe block 编译通过（Wave 1 未实现前预期 some fail）；copyStepLog.test.ts 文件存在且编译结构正确。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 storage.test.ts quota guard 用例 + 新建 WordAdapter.test.ts / ExcelAdapter.test.ts inverse stubs</name>
  <files>
    src/lib/storage.test.ts
    src/adapters/WordAdapter.test.ts
    src/adapters/ExcelAdapter.test.ts
    src/adapters/PptAdapter.test.ts
  </files>
  <read_first>
    - src/lib/storage.test.ts（全文，了解现有测试结构 + vi.stubGlobal 范式）
    - src/adapters/WordAdapter.ts（全文，了解现有方法签名，特别是 appendParagraph line 129-138）
    - src/adapters/ExcelAdapter.ts（了解现有 read 方法签名，two-sync 范式）
    - src/adapters/PptAdapter.ts（了解 TEXT_SHAPE_TYPES + PowerPoint.run + warnHostErr 范式）
    - src/errors/index.ts（AsterError 子类范式，line 207-213 UnsupportedOperationError）
  </read_first>
  <behavior>
    - storage.test.ts 扩展：新增 describe 'quota guard'：
      - `setItem QuotaExceededError → StorageQuotaError`（spy `Storage.prototype.setItem` mock 抛 `new DOMException('QuotaExceededError', 'QuotaExceededError')`，expect `storage.set('k', 'v')` toThrow `StorageQuotaError`）
      - `非 QuotaExceeded DOMException → 原样 rethrow`
    - WordAdapter.test.ts（已存在或新建）：新增 describe 'deleteParagraphByContent'：
      - `找到匹配文本的段落并删除`（mock Word.run，mock paras.items 含目标文本）
      - `找不到目标段落 → 抛 HostApiError`
      - `规范化：\r\n 尾部被等价忽略（Pitfall 2 防 false-skip）`
    - ExcelAdapter.test.ts（已存在或新建）：新增 describe 'setRangeValues + overwriteRange'：
      - `setRangeValues 返回 before-image address + values`
      - `overwriteRange 覆写成功`
    - PptAdapter.test.ts（新建）：新增 describe 'insertSlideAfter' 和 'deleteSlideByTitle'：
      - `insertSlideAfter 调用 PowerPoint.run + slides.add()，返回 { insertedIndex, title }`（mock PowerPoint.run + vi.fn()，照 WordAdapter.test.ts 范式 mock Office.js）
      - `deleteSlideByTitle 找到匹配 title 时调用 slide.delete()`
      - `deleteSlideByTitle 找不到 title → 抛 HostApiError`
  </behavior>
  <action>
    1. 读 storage.test.ts，在末尾追加 describe 'quota guard'。用 `vi.spyOn(Storage.prototype, 'setItem')` mock 抛 DOMException（注意：现有测试用 vi.stubGlobal mock localStorage，要用 spy 的方式让 stub localStorage 的 setItem 抛 DOMException——vi.fn().mockImplementation(() => { throw new DOMException(...) }）。StorageQuotaError 此时未实现，import 语句先写，Wave 1b 实现后会 pass。
    2. 读 WordAdapter.ts，找已有 Word.run mock 范式（或看 word.test.ts 的 adapter mock 方式），在 WordAdapter.test.ts 新建/扩展 describe 'deleteParagraphByContent'，Office.js 依赖用 vi.fn() 全部 mock（不调真实 Office API）。
    3. 读 ExcelAdapter.ts，在 ExcelAdapter.test.ts 新建 describe，Excel.run 用 mock。
    4. 读 PptAdapter.ts，在 PptAdapter.test.ts 新建 describe 两块（'insertSlideAfter' + 'deleteSlideByTitle'），Office.js 依赖全 mock（vi.fn()；PowerPoint.run 用 `vi.mock('PowerPoint', ...)` 或模块 mock，与 WordAdapter.test.ts 范式保持一致）：
       - insertSlideAfter：mock slides.load + slides.add + sync，assert 返回含 insertedIndex + title
       - deleteSlideByTitle：mock slides.load + sync + slide.delete；再测 title 不匹配时 assert throw HostApiError
    5. 运行 `npx vitest run src/lib/storage.test.ts` 确认已有用例 pass，新用例预期 fail（StorageQuotaError 未实现）。
  </action>
  <verify>
    <automated>npx vitest run src/lib/storage.test.ts 2>&1 | grep -E "pass|fail|error" | tail -5; npx vitest run src/adapters/PptAdapter.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>storage.test.ts 已有用例全 pass；新增 quota guard describe 结构编译通过（测试本身预期 fail 直到 Wave 1b 实现）；WordAdapter.test.ts + ExcelAdapter.test.ts + PptAdapter.test.ts 新 describe 框架存在且编译通过；`npx vitest run src/adapters/PptAdapter.test.ts` 编译通过（Wave 0 stubs 结构正确）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → mock | 测试文件 mock Office.js，不触及真实 localStorage/Office API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | copyStepLog.test.ts 脱敏断言 | mitigate | Wave 0 测试断言 `not.toMatch(/sk-[A-Za-z0-9]+/)` 建立守门，Wave 5 实现时必须 pass |
| T-05-01-02 | Tampering | storage.test.ts mock setItem | accept | 测试环境只 spy Storage.prototype，不影响生产 localStorage；风险 = 0 |

No high-severity threats in Wave 0 test-only plan. Primary concern is ensuring copyStepLog redaction test is in place before implementation (mitigate).
</threat_model>

<verification>
1. `npx vitest run src/agent/tools/write/word.test.ts` — 全 pass（reverse 断言已更新）
2. `npx vitest run src/agent/operationLog.test.ts` — 旧测试 pass，新增 describe 结构编译通过
3. `npx vitest run src/lib/storage.test.ts` — 旧测试 pass，新 quota 用例框架就绪
4. `ls src/lib/copyStepLog.test.ts src/adapters/WordAdapter.test.ts src/adapters/ExcelAdapter.test.ts src/adapters/PptAdapter.test.ts` — 四文件存在
5. `npx vitest run src/adapters/PptAdapter.test.ts` — PptAdapter mock stubs 编译通过（Wave 0 stubs，实现在 Wave 2c）
6. `npx tsc --noEmit 2>&1 | grep -v "Cannot find module.*copyStepLog" | grep error | head -10` — 仅 copyStepLog 模块未找到（可接受，Wave 5 创建），其余无 TS 错误
</verification>

<success_criteria>
- word.test.ts 测试全 pass，reverse 断言已改为 `delete_paragraph_by_content`（TOOL-04 预判守门就位）
- 7 个测试文件全部存在且 TypeScript 编译通过（copyStepLog 模块缺失为 expected，其余无 TS error；包含 PptAdapter.test.ts Wave 0 mock stubs）
- operationLog.test.ts 新增的 Wave 1 目标行为用例可在 Wave 1 实现后直接 RED→GREEN
- storage.test.ts quota guard 用例在 Wave 1b 后可直接 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-01-SUMMARY.md`
</output>

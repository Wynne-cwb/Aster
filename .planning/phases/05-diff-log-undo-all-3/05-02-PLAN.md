---
phase: 05-diff-log-undo-all-3
plan: 02
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - src/agent/operationLog.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - AGENT-10
  - AGENT-11
  - TOOL-04

must_haves:
  truths:
    - "operationLog 内部改为 Map<runId, OperationLogEntry[]>，三个旧导出名（appendOperation / getOperationsByRun / __resetOperationLogForTest）保持不变"
    - "新增 PostStateSnapshot 接口 + OperationLogEntry.postState 字段"
    - "新增 getWriteOpsByRun(runId) 只返回有 reverse 的条目"
    - "新增 replayUndoAll(runId, adapter) 逆序 replay，UndoResult 三态结构（D-11 continue-on-error）"
    - "ToolResult 接口新增可选字段 postState: PostStateSnapshot"
    - "replay engine 通过 DocumentAdapter 调 inverse 方法，绝不出现 Word/Excel/PowerPoint 命名空间（A-06）"
  artifacts:
    - path: "src/agent/operationLog.ts"
      provides: "Map<runId> + PostStateSnapshot + replay engine + UndoResult"
      exports: ["appendOperation", "getOperationsByRun", "getWriteOpsByRun", "replayUndoAll", "__resetOperationLogForTest", "PostStateSnapshot", "UndoResult"]
    - path: "src/agent/tools/index.ts"
      provides: "ToolResult.postState 字段扩展"
      contains: "postState?: PostStateSnapshot"
  key_links:
    - from: "src/agent/operationLog.ts"
      to: "DocumentAdapter interface"
      via: "replayUndoAll(runId, adapter: DocumentAdapter)"
      pattern: "DocumentAdapter"
    - from: "src/agent/loop-helpers.ts"
      to: "src/agent/operationLog.ts"
      via: "appendOperation (签名不变)"
      pattern: "appendOperation"
---

<objective>
Wave 1a：把 Phase 3 operationLog 骨架（全局数组）重构为 Map<runId, entries[]>，新增 PostStateSnapshot + replay engine（replayUndoAll / replayUndoStep）。同步扩展 ToolResult 增加 postState 字段。这是整个 Phase 5 的核心数据层，后续所有 plan 都依赖它。

Purpose: 建立 replay engine 数据层——有了 Map<runId> + replayUndoAll，DiffLogPanel 和 undo 动作才有地方读写。
Output: operationLog.ts 重构完成 + ToolResult.postState 类型字段就位。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- 现有关键接口 -->

From src/agent/operationLog.ts (Phase 3 骨架，本 plan 重构):
```typescript
// 保留导出名（loop-helpers.ts:18 + operationLog.test.ts 调用中）：
export function appendOperation(entry: OperationLogEntry): void  // 签名不变，内部改 Map
export function getOperationsByRun(runId: string): OperationLogEntry[]  // 保留兼容
export function __resetOperationLogForTest(): void  // → operationLogMap.clear()
```

From src/agent/tools/index.ts (line 45-50，需扩展 ToolResult):
```typescript
export interface ToolResult {
  ok: boolean;
  data?: unknown;
  error?: ToolError;
  reverse?: ReverseDescriptor;
  // 本 plan 新增:
  // postState?: PostStateSnapshot;
}
```

From src/adapters/DocumentAdapter.ts (replay engine 签名收 DocumentAdapter):
// 检查现有接口，确认 DocumentAdapter 接口的 read() 方法签名
// replay engine 将通过 adapter 调 inverse 方法（Wave 2-3 才真正实现 inverse 方法）

From src/agent/loop-helpers.ts (line 154-159, appendOperation 调用处):
```typescript
if (result.reverse && def) {
  appendOperation({
    runId, stepIndex: step, toolName: tc.name, args: tc.arguments,
    humanLabel, reverse: result.reverse, timestamp: Date.now(),
    // Wave 3 将在此透传 postState: result.postState
  });
}
```

From src/agent/circuit-breaker.ts (模块级 Map + reset-for-test 范式):
// const breakers = new Map<string, ...>();
// function __resetForTest(): void { breakers.clear(); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OperationLog 重构为 Map<runId> + PostStateSnapshot + 新导出</name>
  <files>src/agent/operationLog.ts</files>
  <read_first>
    - src/agent/operationLog.ts（全文，Phase 3 骨架，基线）
    - src/agent/circuit-breaker.ts（模块级 Map + reset-for-test 范式）
    - src/agent/loop-helpers.ts（line 15-20：appendOperation import；line 154-159：调用处）
    - src/agent/operationLog.test.ts（Wave 0 新增的 describe block，本 plan 实现后要 pass）
    - src/adapters/DocumentAdapter.ts（确认 DocumentAdapter 接口，replay engine 参数类型）
  </read_first>
  <behavior>
    - `operationLogMap = new Map<string, OperationLogEntry[]>()` 替换全局数组
    - `PostStateSnapshot` 接口新增（kind: 'word_paragraph' | 'excel_range' | 'ppt_slide'；content: unknown）
    - `OperationLogEntry` 新增 `postState?: PostStateSnapshot`（可选，兼容 Phase 3 已有 append 路径）
    - `appendOperation`：内部改 `Map.get(runId) ?? []` → push → `Map.set(runId, list)`，函数签名不变
    - `getOperationsByRun`：改为 `return operationLogMap.get(runId) ?? []`（兼容旧调用方）
    - `getWriteOpsByRun(runId)`：新增，只返回 `entry.reverse` 非空的条目
    - `clearRun(runId)`：新增，删除指定 runId 的记录（可选工具函数）
    - `__resetOperationLogForTest`：改为 `operationLogMap.clear()`
    - `UndoResult` 接口：`{ total, rolledBack, skippedManualChange, skippedHostError, details: Array<{stepIndex, humanLabel, status: 'rolled_back'|'skipped_manual'|'skipped_error', errorHint?}> }`
    - `replayUndoAll(runId, adapter)`：async，逆序 `[...entries].reverse()`，每步调 `replayUndoStep(entry, adapter)`，D-11 try/catch 不 rethrow（报错标 skipped_error 继续）
    - `replayUndoStep`：先 read 比对 postState（调 adapter 的对应 read 方法），一致执行 reverse（调 adapter 的对应 inverse 方法），不一致返 skipped_manual；read/inverse 报错返 skipped_error
    - **A-06 严禁**：operationLog.ts 内不出现 Word/Excel/PowerPoint 全局符号（eslint 会报错）。replay 必须通过 adapter 参数调用
    - `executeReverse(reverse, adapter)`：内部 switch on `reverse.tool`（`delete_paragraph_by_content` → adapter 的 deleteParagraphByContent；`overwrite_range` → adapter 的 overwriteRange；`delete_slide_by_title` → adapter 的 deleteSlideByTitle）；未知 tool 名 → 抛 Error
    - `readTargetState(postState, adapter)`：switch on `postState.kind`，调 adapter 对应 read 方法返回当前值
    - `isTargetStateConsistent(current, postState)`：纯函数，规范化后比对（string trim + \r\n 归一；Excel null/0/"" 等价归一）
  </behavior>
  <action>
    1. 先读上面 read_first 的所有文件，特别确认 DocumentAdapter.ts 的接口定义（replay engine 收 adapter 参数类型需准确）。
    2. 重构 operationLog.ts：保留旧三个导出名签名不变；新增上述所有接口和函数。
    3. 新增 `replayUndoStep` 私有函数：先调 `readTargetState(entry.postState, adapter)` → `isTargetStateConsistent` 比对 → 一致则调 `executeReverse(entry.reverse, adapter)` → 各步错误用 try/catch 不 rethrow。
    4. `isTargetStateConsistent` 规范化：word_paragraph = `s.replace(/\r\n/g,'\n').trimEnd()` 比对；excel_range = cell 值 String 化（null → ''，0 数字类型不等价空字符串，保守不归一）；ppt_slide = title trim 比对。
    5. 运行 `npx vitest run src/agent/operationLog.test.ts` 确认 Wave 0 新增的测试用例 GREEN（重点：Map<runId> CRUD / replayUndoAll 逆序 / skip_manual / continue-on-error）。
    6. 运行 `npx eslint src/agent/operationLog.ts` 确认无 A-06 violation（Word/Excel/PowerPoint 未出现）。
  </action>
  <verify>
    <automated>npx vitest run src/agent/operationLog.test.ts && npx eslint src/agent/operationLog.ts --max-warnings=0 2>&1 | tail -10</automated>
  </verify>
  <done>operationLog.test.ts 所有测试 pass；eslint 无 A-06 violation；三个旧导出名签名不变（grep -c "export function appendOperation\|export function getOperationsByRun\|export function __resetOperationLogForTest" src/agent/operationLog.ts 返回 3）</done>
</task>

<task type="auto">
  <name>Task 2: ToolResult 扩展 postState 字段 + 确认 DocumentAdapter inverse 接口占位</name>
  <files>src/agent/tools/index.ts</files>
  <read_first>
    - src/agent/tools/index.ts（line 45-50 ToolResult 接口；line 59-66 ToolDef 接口）
    - src/adapters/DocumentAdapter.ts（确认当前接口方法列表，inverse 方法在 Wave 2-3 才加，此处不改 DocumentAdapter）
    - src/agent/operationLog.ts（本 plan Task 1 刚完成，确认 PostStateSnapshot 已导出）
  </read_first>
  <action>
    1. 读 tools/index.ts，在 `ToolResult` 接口新增 `postState?: PostStateSnapshot`（从 `'../operationLog'` import type）。
    2. 确认 ToolDef.kind 已有 `'read' | 'write'`（不改），ToolDef.humanLabel 已必填（不改）。
    3. 不改 DocumentAdapter.ts（inverse 方法在 Wave 2 plan 04/05/06 中各自加进对应 adapter）。
    4. 运行 `npx tsc --noEmit 2>&1 | grep -v "copyStepLog" | grep error | head -5` 确认无新 TS 错误。
    5. 运行 `npx vitest run src/agent/` 确认已有 agent 测试全 pass。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "copyStepLog" | grep "error TS" | head -5; npx vitest run src/agent/ 2>&1 | tail -5</automated>
  </verify>
  <done>`grep -c "postState" src/agent/tools/index.ts` 返回 ≥1；ToolResult 接口含 postState 字段；npx tsc --noEmit 除 copyStepLog 模块外无 error；agent 目录测试全 pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay engine → adapter | replayUndoAll 通过 DocumentAdapter 调 inverse 方法，不直接碰 Office.js proxy |
| OperationLog → calling code | appendOperation 接收 postState（由 write tool 自己计算，不由 operationLog 抓取） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | replayUndoAll executeReverse 的 reverse.args | accept | reverse.args 源自 write tool 内部（非 LLM 直接写入）；LLM 只能触发 tool.execute，execute 内部构造 reverse args |
| T-05-02-02 | Elevation of Privilege | A-06 Office namespace 禁令 | mitigate | operationLog.ts 内无 Word/Excel/PowerPoint；eslint no-restricted-globals 编译期阻断 |

No high-severity threats. No new network calls. Pure in-memory data structure.
</threat_model>

<verification>
1. `npx vitest run src/agent/operationLog.test.ts` — Wave 0 新增用例全 pass（Map<runId> CRUD / replayUndoAll 逆序 / skip_manual / continue-on-error D-11）
2. `npx eslint src/agent/operationLog.ts` — 无 A-06 Office namespace 违规
3. `grep -c "export function appendOperation\|export function getOperationsByRun\|export function __resetOperationLogForTest" src/agent/operationLog.ts` — 输出 3（三个向后兼容导出名）
4. `grep "postState" src/agent/tools/index.ts` — 含 postState 字段
5. `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` — 无输出
</verification>

<success_criteria>
- operationLog.ts 重构为 Map<runId>，三个旧导出名签名不变，新增 PostStateSnapshot / UndoResult / replayUndoAll / getWriteOpsByRun 导出
- ToolResult.postState?: PostStateSnapshot 字段就位
- operationLog.test.ts 所有测试 pass（含 Wave 0 新增用例）
- eslint A-06 守门通过（no Word/Excel/PowerPoint 在 operationLog.ts）
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-02-SUMMARY.md`
</output>

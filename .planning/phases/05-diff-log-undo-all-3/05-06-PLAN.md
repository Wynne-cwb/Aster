---
phase: 05-diff-log-undo-all-3
plan: 06
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - src/adapters/PptAdapter.ts
autonomous: true
requirements:
  - TOOL-03
  - AGENT-10
  - AGENT-11

must_haves:
  truths:
    - "PptAdapter 新增 insertSlideAfter(afterIndex, title?) 方法，返回 insertedIndex + title（供 postState 用）"
    - "PptAdapter 新增 deleteSlideByTitle(titleFingerprint) inverse 方法，用 title 指纹定位 slide 并删除"
    - "两方法均在 PowerPoint.run 闭包内（A-06），按 PPT-05 守则 sort by index + TEXT_SHAPE_TYPES 过滤"
    - "deleteSlideByTitle 找不到目标 slide → 抛 HostApiError（上层 replay engine catch 标 skipped_error）"
    - "SP-5 策略：undo all 逆序遍历自有 OperationLog，不依赖 getSelectedSlides 排序（绕 PPT 排序不确定性）"
  artifacts:
    - path: "src/adapters/PptAdapter.ts"
      provides: "insertSlideAfter + deleteSlideByTitle inverse 方法"
      contains: "insertSlideAfter\|deleteSlideByTitle"
  key_links:
    - from: "src/agent/operationLog.ts"
      to: "src/adapters/PptAdapter.ts"
      via: "executeReverse: 'delete_slide_by_title' → adapter.deleteSlideByTitle(args.titleFingerprint)"
      pattern: "deleteSlideByTitle"
---

<objective>
Wave 2c：PptAdapter 新增两个方法：`insertSlideAfter`（write + 返回 title 指纹）和 `deleteSlideByTitle`（inverse，用 title 指纹找 slide 删除）。SP-5 已真机验证 `slide.delete()` 在 Web 端真删可行。title 指纹定位对 index 漂移免疫（D-06）。

Purpose: 建立 PPT inverse 路径——title 指纹（内容指纹）定位，绕过 PPT slide index 动态变化问题（Pitfall 4）。
Output: PptAdapter 两个新方法，复用既有 TEXT_SHAPE_TYPES + sort 范式。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- PptAdapter 现有方法供 executor 直接参考 -->

From src/adapters/PptAdapter.ts (list_slides 191-245，三 sync + sort + TEXT_SHAPE_TYPES):
```typescript
// 模块级 TEXT_SHAPE_TYPES（约 line 38）
const TEXT_SHAPE_TYPES = new Set<string>(['GeometricShape','TextBox','Placeholder','Callout']);

// list_slides 范式（三 sync）：
// sync 1: slides.load('items')
// sort: [...slides.items].sort((a,b)=>a.index-b.index)  ← PPT-05：绕 Web 反序 bug
// sync 2: for (slide of sorted) slide.shapes.load('items/type')
// sync 3: for (text shapes) load textRange.text
// title = 第一个文本形状首行 .split('\n')[0].trim()
```

From .planning/spikes/SP-5-ppt-slide-delete/findings.md:
// PASS: slide.delete() 在 Web 端真删（3→2）
// SP-5 策略：undo all 时用自有 OperationLog 逆序遍历，不调 getSelectedSlides

From src/adapters/PptAdapter.ts (warnHostErr + HostApiError 错误包范式 27-30)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PptAdapter.insertSlideAfter + deleteSlideByTitle</name>
  <files>src/adapters/PptAdapter.ts</files>
  <read_first>
    - src/adapters/PptAdapter.ts（全文——重点 TEXT_SHAPE_TYPES 模块级定义 + list_slides 三 sync 范式 + warnHostErr + HostApiError 错误包）
    - src/adapters/DocumentAdapter.ts（可选 inverse 方法声明策略，与 Word/Excel plan 保持一致）
    - src/errors/index.ts（HostApiError 构造器）
    - .planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md（Pattern 2 PPT，line 386-455）
    - .planning/spikes/SP-5-ppt-slide-delete/findings.md（slide.delete() PASS 结论）
  </read_first>
  <behavior>
    - `insertSlideAfter(afterIndex: number, title?: string): Promise<{ insertedIndex: number; title: string }>`
      在 PowerPoint.run 闭包内：
      1. `slides.load('items')` + sync 1（记录 insert 前 slide 数量）
      2. `ctx.presentation.slides.add()` + sync 2（添加到末尾——Office.js add() 目前无精确 after 位置参数，PoC 先 add 到末尾）
      3. `slides.load('items')` + sync 3（重新 load 获取新 slide）
      4. 取最后一张 slide = 新插入的 slide；read title（复用 list_slides 的 TEXT_SHAPE_TYPES + shape text 三 sync 范式，此处可能需要再 load shapes）
      5. 若 title 参数非空，尝试设置新 slide 第一个文本 placeholder 的 text（可选，PoC 场景）
      6. return `{ insertedIndex: newSlide.index, title: resolvedTitle }`
    - `deleteSlideByTitle(titleFingerprint: string): Promise<void>`
      在 PowerPoint.run 闭包内：
      1. 三 sync 遍历所有 slides（复用 list_slides 范式：load items → sort by index → load shapes → load textRange.text）
      2. 从后到前遍历 sorted（`for (let i = sorted.length - 1; i >= 0; i--)`）找 title 指纹匹配的 slide
      3. 找到：`slide.delete()` + sync，return
      4. 未找到：`throw new HostApiError('PPT deleteSlideByTitle: 目标 slide 已不存在', undefined)`
    - normalizeText（与 Word 同逻辑：trim + \r\n 归一，可提取为 adapter 共用函数或在各 adapter 内独立定义）
    - 整个方法包 `try { await PowerPoint.run(...) } catch (err) { throw new HostApiError('PPT ... 失败', err) }`
    - 由于 PptAdapter 无对应 .test.ts 文件（Wave 0 未建），此 plan 不新建测试文件（PPT adapter 的 mock 复杂度较高，SC1 真机 UAT 是 PPT undo 的主要验收路径）——**注意**：若 Wave 0 已建 PptAdapter.test.ts，则在此扩展；否则跳过 mock 测试，由 Wave 6 真机 UAT 覆盖。
  </behavior>
  <action>
    1. 读 PptAdapter.ts 全文，确认 TEXT_SHAPE_TYPES、warnHostErr、三 sync 范式的具体行号和实现细节。
    2. 在 class 末尾新增 `insertSlideAfter` 方法（PoC 版：add 到末尾，记录 title 指纹；Phase 6 再精确控制位置）。
    3. 新增 `deleteSlideByTitle` 方法，复用 list_slides 的三 sync + sort by index 范式，从后到前遍历找 title 匹配。
    4. 在 DocumentAdapter.ts 可选加 `insertSlideAfter?` + `deleteSlideByTitle?` 接口声明（与 Word/Excel 保持一致风格）。
    5. 运行 `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` 确认无 TS error。
    6. 运行 `npx eslint src/adapters/PptAdapter.ts` 确认无报错（A-06 不约束 adapter 文件）。
    7. 运行 `npm run build && npm run size 2>&1 | tail -5` 确认 bundle ≤82KB。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS" | head -5; npm run build && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>`grep -c "insertSlideAfter\|deleteSlideByTitle" src/adapters/PptAdapter.ts` 返回 ≥2；tsc 无新 error；bundle size ≤82KB</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PptAdapter → Office.js PowerPoint.run | proxy 生命周期在 PowerPoint.run 闭包内（A-06） |
| deleteSlideByTitle titleFingerprint | 来自 insertSlideAfter 记录的 title（write tool 执行时 Office.js 返回的真实 title）；LLM 无法直接控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | deleteSlideByTitle 误删同名 slide | accept | PoC 场景（单次 insert_slide），title 通常唯一；Phase 6 升级为 slide.id 更强定位；replay 前 readTargetState 比对也有守门 |
| T-05-06-02 | Denial of Service | PowerPoint.run 三 sync 超时 | accept | replay engine catch 所有错误（D-11）；用户得到「未能回滚」提示 |

No high-severity threats.
</threat_model>

<verification>
1. `grep -c "insertSlideAfter\|deleteSlideByTitle" src/adapters/PptAdapter.ts` — ≥2
2. `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` — 无输出
3. `npx eslint src/adapters/PptAdapter.ts 2>&1 | grep error | head -5` — 无 error
4. `npm run build && npm run size 2>&1 | tail -3` — bundle ≤82KB gzip
</verification>

<success_criteria>
- PptAdapter.insertSlideAfter：add slide，记录 title 指纹，返回 insertedIndex + title
- PptAdapter.deleteSlideByTitle：三 sync sort by index，从后往前找 title 指纹，slide.delete()，找不到抛 HostApiError
- 两方法在 PowerPoint.run 闭包内，proxy 不出闭包（A-06）
- bundle size 仍 ≤82KB（adapter 新增代码体积微小）
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-06-SUMMARY.md`
</output>

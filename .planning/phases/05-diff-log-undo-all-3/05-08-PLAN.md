---
phase: 05-diff-log-undo-all-3
plan: 08
type: execute
wave: 4
depends_on:
  - "05-03"
  - "05-07"
files_modified:
  - src/components/DiffLogPanel.tsx
  - src/components/ChatStream.tsx
  - src/styles.css
autonomous: true
requirements:
  - AGENT-07
  - AGENT-09
  - AGENT-10
  - AGENT-11
  - NFR-05

must_haves:
  truths:
    - "DiffLogPanel.tsx 新建，懒加载（React.lazy）——只在 run 完成后渲染，不进初始 main chunk"
    - "汇总卡使用 .tool-group 容器 + .tool-group__head + .tool-group__list > li 既有 CSS（UI-SPEC D-01/D-03）"
    - "每步行：humanLabel + 「撤销该步」.btn-ghost.btn-sm 按钮；撤销后行变 .is-undone + 「已撤销」.badge-accent 胶囊（UI-SPEC）"
    - "底部「撤销本次所有操作」按钮 → 二次确认 modal（.modal-scrim + .modal）→ 确认 → replayUndoAll → UndoResult 总结 modal（D-12 三态）"
    - "ChatStream.tsx 在 agentStatus=idle 且 completedRunIds 非空时末尾 lazy 插入 DiffLogPanel（D-02）"
    - "styles.css 补 .is-undone + .badge-accent + .diff-log-panel 等必要 class（设计包规格）"
    - "npm run build && npm run size 通过 ≤82KB（DiffLogPanel 在 lazy chunk，不影响 main）"
  artifacts:
    - path: "src/components/DiffLogPanel.tsx"
      provides: "汇总卡 + per-step undo + undo-all 二次确认 + 总结 modal"
      contains: "DiffLogPanel\|replayUndoAll\|completedRunIds"
    - path: "src/components/ChatStream.tsx"
      provides: "DiffLogPanel 懒加载挂载点"
      contains: "DiffLogPanel\|React.lazy\|completedRunIds"
    - path: "src/styles.css"
      provides: ".is-undone + .badge-accent + .badge-warning + .badge-error + 必要新 class"
      contains: "is-undone\|badge-accent"
  key_links:
    - from: "src/components/DiffLogPanel.tsx"
      to: "src/agent/operationLog.ts"
      via: "getWriteOpsByRun(runId) 读取操作列表"
      pattern: "getWriteOpsByRun"
    - from: "src/components/DiffLogPanel.tsx"
      to: "src/agent/agentStore.ts"
      via: "useCompletedRunIds + replayUndoAll（via agentStore action）"
      pattern: "useCompletedRunIds\|replayUndoAll"
    - from: "src/components/ChatStream.tsx"
      to: "src/components/DiffLogPanel.tsx"
      via: "React.lazy + Suspense（lazy chunk）"
      pattern: "DiffLogPanel\|React.lazy"
---

<objective>
Wave 4：DiffLogPanel UI 主体 + ChatStream 挂载点 + styles.css 补全。这是 Phase 5 最重要的可见 UI 交付——用户首次见到「本次改动 N 处」折叠卡、单步撤销、undo-all 流程。DiffLogPanel 走 React.lazy 移出 main chunk 守住 bundle 预算。

Purpose: 让用户在 agent run 完成后能看到 diff log 汇总卡，并完成单步撤销和一键撤销所有操作。这是 Phase 5 的 trust 担保核心。
Output: DiffLogPanel.tsx + ChatStream.tsx 挂载 + styles.css 补全。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-UI-SPEC.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- 现有关键接口 -->

From src/components/ChatStream.tsx (MergedToolGroup 折叠卡范式 208-237):
```tsx
<div className="tool-group">
  <div className="tool-group__head">
    <span className="tool-group__count">N 项操作</span>
  </div>
  <ul className="tool-group__list">
    {messages.map((m) => (
      <li key={m.id} className={isErr ? 'is-error' : undefined}>
        <button className="wb-action-head" ...><ChevronDownIcon size={11} /></button>
        {isOpen && <ExpandedBody />}
      </li>
    ))}
  </ul>
</div>
```

From src/components/Onboarding/OnboardingModal.tsx (modal 范式):
```tsx
<div className="modal-scrim" role="dialog" aria-modal="true">
  <div className="modal">
    <h2 className="modal-title">...</h2>
    <div className="modal-body">...</div>
    <div className="modal-foot">
      <button className="btn btn-ghost">取消</button>
      <button className="btn btn-primary">确认</button>
    </div>
  </div>
</div>
```

From src/agent/agentStore.ts (Phase 5 新增，Wave 1b):
```typescript
export const useCompletedRunIds = () => useAgentStore((s) => s.completedRunIds);
```

From src/agent/operationLog.ts (Phase 5 重构后):
```typescript
export function getWriteOpsByRun(runId: string): OperationLogEntry[]
export async function replayUndoAll(runId: string, adapter: DocumentAdapter): Promise<UndoResult>
// UndoResult.details[].status: 'rolled_back' | 'skipped_manual' | 'skipped_error'
```

From src/components/ChatStream.tsx (当前末尾 render line 321-325):
```tsx
return (
  <div className="aster-messages" ref={scrollRef} onScroll={handleScroll}>
    {nodes}
  </div>
);
```
// completedRunIds 从 agentStore 取；对每个 runId 若 getWriteOpsByRun(runId).length > 0，追加 DiffLogPanel

From src/styles.css (.badge 体系 + .is-error + .tool-group 等已有 class):
// 检查线上 styles.css：.badge / .badge-success 是否存在；.is-undone 是否需要新增
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DiffLogPanel.tsx 新建（含 per-step undo + undo-all 二次确认 + 总结 modal）</name>
  <files>
    src/components/DiffLogPanel.tsx
    src/styles.css
  </files>
  <read_first>
    - src/components/ChatStream.tsx（MergedToolGroup 折叠卡 208-237；ToolResultCard）
    - src/components/Onboarding/OnboardingModal.tsx（modal-scrim + modal-foot 范式）
    - src/styles.css（全文——找 .tool-group / .wb-action-head / .btn-ghost / .badge / .is-error / .modal-scrim / .modal 是否存在；确认 .is-undone / .badge-accent / .badge-warning / .badge-error 是否存在）
    - src/components/icons.tsx（确认 ChevronDownIcon / TrashIcon / CheckIcon 已导出）
    - src/agent/operationLog.ts（getWriteOpsByRun + replayUndoAll + UndoResult interface）
    - src/agent/agentStore.ts（useCompletedRunIds selector + endRun completedRunIds）
    - .planning/phases/05-diff-log-undo-all-3/05-UI-SPEC.md（全文，Component Inventory + Copywriting Contract + Color 三态）
  </read_first>
  <action>
    1. 读 styles.css，确认以下 class 是否存在（线上 styles.css 为真相源）：
       - `.tool-group`：存在（复用）
       - `.tool-group__head` / `.tool-group__list`：存在（复用）
       - `.btn .btn-ghost .btn-sm`：存在（复用）
       - `.badge .badge-success`：存在（复用）
       - `.is-undone`：如不存在，在 styles.css 末尾补：
         ```css
         /* Phase 5 DiffLogPanel — 已撤销态 */
         .is-undone .wb-action-target { text-decoration: line-through; color: var(--text-3); }
         .badge-accent { background: var(--accent-soft, #e0f7f4); color: var(--accent); font-size: var(--fs-11); padding: 1px 6px; border-radius: 4px; font-weight: 500; }
         .badge-warning { background: var(--warning-soft, #fef3c7); color: var(--warning, #d97706); font-size: var(--fs-11); padding: 1px 6px; border-radius: 4px; font-weight: 500; }
         .badge-error { background: var(--error-soft, #fee2e2); color: var(--error); font-size: var(--fs-11); padding: 1px 6px; border-radius: 4px; font-weight: 500; }
         /* dark 主题三态色补充（dark 下 success/warning 需提亮） */
         [data-theme="dark"] .badge-accent { background: rgba(79,201,184,0.15); color: var(--accent); }
         [data-theme="dark"] .badge-warning { background: rgba(217,119,6,0.2); color: #fbbf24; }
         [data-theme="dark"] .badge-error { background: rgba(248,113,113,0.15); color: var(--error); }
         /* DiffLogPanel 容器 — flex-shrink:0 防被 .aster-messages 压扁 */
         .diff-log-panel { flex-shrink: 0; margin-top: var(--space-2); }
         ```
    2. 新建 src/components/DiffLogPanel.tsx（完整实现）：
       - 导入：`getWriteOpsByRun` from operationLog；`replayUndoAll` from operationLog；`useAdapter` from adapter；`UndoResult` type；Lingui `Trans` / `useLingui`；`ChevronDownIcon` etc from icons
       - State：`expanded: boolean`（折叠/展开）；`stepUndoState: Record<number, 'rolled_back'|'skipped_manual'|'skipped_error'>`（per-step 撤销状态）；`confirming: boolean`（二次确认 modal）；`undoResult: UndoResult | null`（总结 modal）；`loading: boolean`（撤销进行中）
       - 汇总卡头：`.tool-group > div.tool-group__head`（整行 button），ChevronDownIcon 随 expanded toggle rotate，`<Trans>本次改动 {N} 处</Trans>`
       - 展开列表：`ul.tool-group__list`，每条 `li`：
         - 左：`span.wb-action-target`（entry.humanLabel）+ 状态胶囊（若已撤销 → `.badge-accent 已撤销`；手改 → `.badge-warning 未回滚`；报错 → `.badge-error 未能回滚`）
         - 右：若未撤销 → `button.btn.btn-ghost.btn-sm 撤销该步`（onClick → handleUndoStep，loading 中 disabled）
         - li className：有报错的步 `.is-error`；已撤销的步 `.is-undone`
       - 底部：展开且无 undoResult 且写操作数 > 1 → `button.btn.btn-ghost 撤销本次所有操作`（onClick → setConfirming(true)）
       - handleUndoStep(entryIndex)：调 `replayUndoAll` 但只传该 step（或单独实现 replayUndoStep for single entry；简化方案：调 adapter inverse 方法直接执行该 step 的 reverse，不走 replayUndoAll 全量）—— 单步撤销可以直接：read postState 比对 → executeReverse → 更新 stepUndoState
       - handleUndoAll：调 `replayUndoAll(runId, adapter)` → setUndoResult + setLoading
       - 二次确认 modal：`confirming && <div className="modal-scrim" role="dialog">`，含「确认撤销」teal 主按钮 + 「取消」ghost 按钮；按 ESC 关闭（useEffect keydown listener）
       - 总结 modal：`undoResult && <div className="modal-scrim" role="dialog">`，按 D-12 三态显示 X/Y/Z 行（只在对应值 > 0 时显示）；「知道了」ghost 按钮 right-align
       - **A-06 守门**：DiffLogPanel 调用 `replayUndoAll(runId, adapter)` 时传入 `useAdapter()` 返回的 adapter；不直接调 Word/Excel/PowerPoint 命名空间（DiffLogPanel 在 src/components/，eslint 不约束 components，但保持规范）
       - Lingui：所有字串用 `<Trans>` 或 `t\`...\`` 包裹；动态量词用 `<Trans>{N} 处</Trans>`
    3. 在 styles.css 补充必要 class（若 Task 1 step 1 发现缺失），包括 dark 主题三态色。
    4. 运行 `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` 确认无 error。
    5. 运行 `npm run build && npm run size 2>&1 | tail -5` 确认 main bundle ≤82KB（DiffLogPanel 在 lazy chunk）。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS" | head -5; npm run build && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>DiffLogPanel.tsx 存在且 tsc 无 error；styles.css 含 .is-undone / .badge-accent / .badge-warning / .badge-error / .diff-log-panel；`grep -c "flex-shrink: 0\|flex-shrink:0" src/styles.css` 含 diff-log-panel flex-shrink 防压扁；bundle main ≤82KB</done>
</task>

<task type="auto">
  <name>Task 2: ChatStream.tsx 挂载 DiffLogPanel + Lingui extract</name>
  <files>
    src/components/ChatStream.tsx
  </files>
  <read_first>
    - src/components/ChatStream.tsx（全文——末尾 render 321-325；messages 分发循环 289-319；useAdapter import）
    - src/components/DiffLogPanel.tsx（Task 1 刚建，确认导出名）
    - src/agent/agentStore.ts（useCompletedRunIds selector 导出；useAgentStatus）
    - src/agent/operationLog.ts（getWriteOpsByRun 用于决定是否渲染 DiffLogPanel）
  </read_first>
  <action>
    1. 读 ChatStream.tsx 全文，确认 useAdapter + useAgentStatus 等 hook 已在文件顶部导入。
    2. 在顶部 import 区加：
       ```typescript
       import { useCompletedRunIds } from '../agent/agentStore';
       import { getWriteOpsByRun } from '../agent/operationLog';
       const DiffLogPanel = React.lazy(() => import('./DiffLogPanel'));
       ```
    3. 在 `export default function ChatStream` 内，在已有 `const agentStatus = useAgentStatus();`（如存在）后加：
       ```typescript
       const completedRunIds = useCompletedRunIds();
       ```
    4. 在末尾 return 的 `{nodes}` 之后，`</div>` 之前加：
       ```tsx
       {completedRunIds.map((runId) =>
         getWriteOpsByRun(runId).length > 0 ? (
           <React.Suspense key={runId} fallback={null}>
             <DiffLogPanel runId={runId} />
           </React.Suspense>
         ) : null
       )}
       ```
       **注意**：DiffLogPanel 挂在 `.aster-messages` 的 flex column 内，必须 `.diff-log-panel { flex-shrink: 0 }`（styles.css Task 1 已补）。
    5. 运行 `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` 确认无 error。
    6. 运行 `npm run lingui extract` 提取新字串到 zh-CN catalog（DiffLogPanel 内 Lingui 字串）。
    7. 运行 `npm run build && npm run size 2>&1 | tail -5` 确认 main bundle ≤82KB（DiffLogPanel lazy chunk 分离）。
    8. 运行 `npx vitest run 2>&1 | tail -5` 确认全套测试 pass。
  </action>
  <verify>
    <automated>npm run build && npm run size 2>&1 | tail -5; npx vitest run 2>&1 | grep -E "pass|fail" | tail -3</automated>
  </verify>
  <done>ChatStream.tsx 含 completedRunIds + DiffLogPanel lazy 挂载；bundle main ≤82KB；全套测试 pass；lingui extract 无报错</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DiffLogPanel → replayUndoAll → adapter | UI 通过 adapter 调 Office.js inverse 方法，不直接操作文档 |
| DiffLogPanel 渲染 humanLabel | humanLabel 来自 write tool 内部（非 LLM 原始输出）；不做额外 HTML sanitize（React 默认 escape） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Information Disclosure | DiffLogPanel 展示 humanLabel 中可能含文档敏感内容 | accept | humanLabel 是 write tool 内部生成的中文人话描述（非原始文档内容）；且只在 Task Pane 内显示，不离开用户浏览器 |
| T-05-08-02 | Tampering | 用户点「撤销该步」后重复点击 | mitigate | loading 状态 disabled 按钮；已撤销步移除按钮（.is-undone 时 button 消失） |
| T-05-08-03 | Denial of Service | DiffLogPanel 渲染大量写操作 entry | accept | Phase 5 PoC 场景 entry 数量少（max_steps=20 硬上限）；大量 entry 只是 DOM 展示，无性能风险 |

No high-severity threats. DiffLogPanel is read-only display + undo trigger. No sensitive data leaves the browser.
</threat_model>

<verification>
1. `npm run build && npm run size 2>&1 | grep "main-"` — main bundle ≤82KB gzip（DiffLogPanel 在 lazy chunk）
2. `grep -c "React.lazy\|DiffLogPanel" src/components/ChatStream.tsx` — ≥2
3. `grep -c "replayUndoAll\|getWriteOpsByRun" src/components/DiffLogPanel.tsx` — ≥2
4. `grep "flex-shrink" src/styles.css | grep "diff-log"` — 含 flex-shrink:0
5. `grep -c "is-undone\|badge-accent\|badge-warning\|badge-error" src/styles.css` — ≥4
6. `npx vitest run 2>&1 | grep -E "^Tests" | tail -1` — 全 pass
7. `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` — 无输出
</verification>

<success_criteria>
- DiffLogPanel.tsx：汇总卡折叠（.tool-group）+ 每步 humanLabel + 撤销按钮 + .is-undone 三态 + undo-all 二次确认 modal + UndoResult 总结 modal（D-12 三态）
- ChatStream.tsx：completedRunIds 遍历 lazy 挂载 DiffLogPanel（run 完成后才显示）
- styles.css：.is-undone + .badge-accent + .badge-warning + .badge-error + .diff-log-panel flex-shrink:0
- bundle main ≤82KB（DiffLogPanel 在 lazy chunk，0 初始开销）
- Lingui 字串已 extract
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-08-SUMMARY.md`
</output>

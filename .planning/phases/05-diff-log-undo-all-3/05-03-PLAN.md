---
phase: 05-diff-log-undo-all-3
plan: 03
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - src/lib/storage.ts
  - src/errors/index.ts
  - src/agent/agentStore.ts
  - eslint.config.js
autonomous: true
requirements:
  - NFR-05
  - AGENT-07
  - AGENT-09
  - TOOL-04

must_haves:
  truths:
    - "storage.ts setItem 包 try/catch，QuotaExceededError → StorageQuotaError（D-14）"
    - "StorageQuotaError 是 AsterError 子类，含 recoverable=false + hint 中文字面量"
    - "agentStore 新增 completedRunIds: string[] + endRun 时 push 当前 runId（DiffLogPanel 依赖）"
    - "eslint.config.js humanLabel rule flip：aster/require-human-label 占位注释已明确说明 Phase 5 flip 策略（runtime assert + test 断言作守门）"
  artifacts:
    - path: "src/lib/storage.ts"
      provides: "quota guard setItem try/catch"
      contains: "StorageQuotaError"
    - path: "src/errors/index.ts"
      provides: "StorageQuotaError 子类"
      contains: "STORAGE_QUOTA"
    - path: "src/agent/agentStore.ts"
      provides: "completedRunIds 字段 + endRun push 逻辑"
      contains: "completedRunIds"
  key_links:
    - from: "src/lib/storage.ts"
      to: "src/errors/index.ts"
      via: "throw new StorageQuotaError()"
      pattern: "StorageQuotaError"
    - from: "src/agent/agentStore.ts"
      to: "src/components/DiffLogPanel.tsx"
      via: "completedRunIds（Wave 4 DiffLogPanel 订阅）"
      pattern: "completedRunIds"
---

<objective>
Wave 1b：三个独立小任务并入同一 plan（同 wave，文件无重叠）：(1) storage quota guard 薄包装；(2) agentStore 加 completedRunIds 让 DiffLogPanel 知道哪些 run 已完成；(3) eslint.config.js humanLabel flip 策略确认（由于 TS 已硬强制 humanLabel，重点是 reverse 必填用 runtime assert 在每个 write test 守门，eslint rule 注释更新说明策略）。

Purpose: 打通存储安全（D-14）+ 为 DiffLogPanel 挂载提供已完成 runId 列表 + 明确 D-15 enforce 路径。
Output: storage.ts quota guard + agentStore.completedRunIds + eslint flip 策略注释 + 门禁测试 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- 现有关键接口 -->

From src/errors/index.ts (AsterError 子类范式，UnsupportedOperationError 207-213):
```typescript
export class UnsupportedOperationError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '此操作不受支持，请尝试其他方式';
  constructor(detail?: string) {
    super(detail ? `不支持的操作：${detail}` : '不支持的操作', 'UNSUPPORTED', 'adapter');
  }
}
```

From src/lib/storage.ts (line 66-68 需要包 try/catch):
```typescript
set(rawKey: string, value: unknown): void {
  localStorage.setItem(prefixedKey(rawKey), JSON.stringify(value));  // ← 包 try/catch
}
```

From src/agent/agentStore.ts (endRun line 150-160，需加 completedRunIds push):
```typescript
endRun() {
  set({ agentStatus: 'idle', currentRunId: null, ... });
}
```

From eslint.config.js (line 44 占位 rule + line 62 注释):
```javascript
'aster/require-human-label': 'off', // 占位 — 自写 plugin 尚未发布
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: storage.ts quota guard + StorageQuotaError + agentStore.completedRunIds</name>
  <files>
    src/lib/storage.ts
    src/errors/index.ts
    src/agent/agentStore.ts
  </files>
  <read_first>
    - src/lib/storage.ts（全文，了解现有 set/get/remove 结构）
    - src/errors/index.ts（了解 AsterError 子类范式，重点 UnsupportedOperationError line 207-213 + super() 签名）
    - src/agent/agentStore.ts（全文，重点 endRun line 150-160 + beginRun line 76-90）
    - src/lib/storage.test.ts（Wave 0 新增的 quota guard describe block）
  </read_first>
  <action>
    1. 在 src/errors/index.ts 末尾新增 StorageQuotaError 子类（照 UnsupportedOperationError 范式）：
       ```typescript
       export class StorageQuotaError extends AsterError {
         public readonly recoverable = false;
         public readonly hint = '浏览器存储空间已满，请清理浏览器数据后重试';
         constructor() {
           super('localStorage 空间已满，无法保存配置', 'STORAGE_QUOTA', 'adapter');
         }
       }
       ```
       注意：AsterError 的 code 参数类型如果是枚举/联合类型，需要确认 'STORAGE_QUOTA' 是否需要加进类型定义（读 errors/index.ts 确认）。
    2. 在 src/lib/storage.ts 的 set 方法包 try/catch：
       - import StorageQuotaError from errors/index.ts
       - catch 块：`if (err instanceof DOMException && (err.name === 'QuotaExceededError' || err.code === 22)) { throw new StorageQuotaError(); } throw err;`
       - 其他 DOMException（如隐私模式拒绝）原样 rethrow
    3. 在 src/agent/agentStore.ts：
       - AgentState interface 新增 `completedRunIds: string[]`
       - 初始状态加 `completedRunIds: []`
       - `endRun()` 内，在 set 之前先取 `currentRunId`，若非 null push 到 completedRunIds：
         ```typescript
         endRun() {
           const runId = get().currentRunId;
           set((s) => ({
             agentStatus: 'idle', currentRunId: null, controller: null,
             currentStep: 0, runningTools: [], currentPhase: null, lastCircuitInfo: null,
             completedRunIds: runId ? [...s.completedRunIds, runId] : s.completedRunIds,
           }));
         }
         ```
       - 导出 selector：`export const useCompletedRunIds = () => useAgentStore((s) => s.completedRunIds);`
    4. 运行 `npx vitest run src/lib/storage.test.ts` — quota guard 用例预期 GREEN。
    5. 运行 `npx vitest run src/agent/` — agentStore 相关测试 pass。
  </action>
  <verify>
    <automated>npx vitest run src/lib/storage.test.ts src/agent/ 2>&1 | tail -8</automated>
  </verify>
  <done>storage.test.ts quota guard describe 全 pass；grep -c "StorageQuotaError" src/lib/storage.ts 返回 ≥1；grep -c "completedRunIds" src/agent/agentStore.ts 返回 ≥3（interface + 初始值 + endRun + selector）</done>
</task>

<task type="auto">
  <name>Task 2: eslint.config.js D-15 flip 策略更新 + reverse runtime assert 确认</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js（全文，了解当前 aster/require-human-label 占位注释 + Phase 5 flip 说明）
    - src/agent/tools/index.ts（确认 ToolDef.humanLabel 已是 TS 必填字段；ToolResult.reverse? 仍是可选）
    - src/agent/tools/write/word.test.ts（Wave 0 已加 reverse.toBeDefined() 断言，确认 TOOL-04 runtime 守门已就位）
  </read_first>
  <action>
    1. 读 eslint.config.js 全文，找 `aster/require-human-label: 'off'` 行（约 line 44）。
    2. 更新该行注释为：
       ```javascript
       'aster/require-human-label': 'off',
       // Phase 5 D-15 flip 策略（已确认）：
       //   - humanLabel：TS ToolDef 接口已强制必填（tsc 报错守门）+ index.types.test.ts @ts-expect-error 负向断言
       //   - reverse（write tool 必须有）：TS 无法表达 kind='write' → reverse 必填的耦合关系
       //     → 守门方式 = runtime assert：每个 write tool 的 test file 加 `expect(result.reverse).toBeDefined()`
       //     → 本 phase 所有 write tool test 已在 Wave 0 / Wave 3 加此断言
       //   - eslint rule 保持 'off'：现有 no-restricted-syntax selector 精度不足以区分 write tool，
       //     改 error 会产生 false positive；runtime + TS 守门已足够
       ```
    3. 不改 rule 严重度（保持 'off'），只更新注释说明策略已确认。
    4. 运行 `npx eslint eslint.config.js 2>&1 | head -5` 确认 config 文件本身无报错。
    5. 运行 `npx eslint src/agent/tools/write/ 2>&1 | head -10` 确认 write tool 目录无新 lint 报错。
  </action>
  <verify>
    <automated>npx eslint src/agent/tools/write/ 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>eslint.config.js 注释已更新说明 D-15 flip 策略；`npx eslint src/agent/tools/write/` 无新 error；`npm run build && npm run size` 通过（≤82KB gzip，eslint.config.js 不影响 bundle）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage.set() → DOMException | 捕获浏览器配额异常，转为业务异常（StorageQuotaError）而非裸 DOMException |
| agentStore.completedRunIds → DiffLogPanel | 只暴露 runId 字符串列表，不暴露 run 内部状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | localStorage quota exhaustion | mitigate | StorageQuotaError 业务化，不裸抛 DOMException；用户得到「浏览器存储空间已满」中文提示（D-14） |
| T-05-03-02 | Information Disclosure | completedRunIds 暴露 runId | accept | runId 是 crypto.randomUUID()，不含任何文档内容，无隐私风险 |

No high-severity threats. No network calls. localStorage only stores Provider/Key/flag small data.
</threat_model>

<verification>
1. `npx vitest run src/lib/storage.test.ts` — quota guard describe 全 pass
2. `grep -c "StorageQuotaError" src/lib/storage.ts src/errors/index.ts` — 各至少 1 处
3. `grep -c "completedRunIds" src/agent/agentStore.ts` — ≥3（interface + 初始值 + endRun）
4. `npx vitest run src/agent/` — 所有 agent 测试 pass
5. `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` — 无输出
</verification>

<success_criteria>
- storage.set() 包 try/catch，QuotaExceededError → StorageQuotaError（非裸 DOMException）
- agentStore.completedRunIds 在 endRun 时追加当前 runId，useCompletedRunIds selector 导出
- eslint.config.js D-15 注释更新，TS humanLabel 强制 + runtime assert 策略已文档化
- 所有相关测试 pass + tsc 无 error
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-03-SUMMARY.md`
</output>

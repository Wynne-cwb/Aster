---
phase: 05-diff-log-undo-all-3
plan: 09
type: execute
wave: 5
depends_on:
  - "05-08"
files_modified:
  - src/lib/copyStepLog.ts
  - src/lib/copyStepLog.test.ts
  - src/components/InputBar.tsx
  - src/components/Settings/SettingsPanel.tsx
autonomous: true
requirements:
  - CARRY-03
  - NFR-05

must_haves:
  truths:
    - "copyStepLog.ts 新建，buildStepLog() 导出全量三角色（user/assistant/tool）Markdown dump，脱敏 API Key（不含 sk-* 字符串）"
    - "脱敏：不调 getKey()，不读 aster:keys:* localStorage，只输出 configuredKeyIds 列表（同 debugReport.ts 安全约束）"
    - "InputBar.tsx 「复制操作记录」图标按钮并列于「复制调试信息」旁，2s 反馈 + CheckIcon teal"
    - "SettingsPanel.tsx 全局选项区新增「复制本次操作记录」入口（.btn-ghost.btn-sm 或行式）"
    - "copyStepLog.test.ts 脱敏断言 GREEN：expect(output).not.toMatch(/sk-[A-Za-z0-9]+/)"
    - "npm run build && npm run size ≤82KB（copyStepLog 懒加载，0 初始 chunk）"
  artifacts:
    - path: "src/lib/copyStepLog.ts"
      provides: "buildStepLog + 脱敏 Markdown dump（D-19..D-22）"
      contains: "buildStepLog\|copyToClipboard"
    - path: "src/lib/copyStepLog.test.ts"
      provides: "三角色 dump + 脱敏断言"
      contains: "sk-\|buildStepLog"
    - path: "src/components/InputBar.tsx"
      provides: "复制操作记录 tool-btn（主界面入口 D-22）"
      contains: "复制操作记录\|buildStepLog"
    - path: "src/components/Settings/SettingsPanel.tsx"
      provides: "Settings 入口（D-22 双入口）"
      contains: "复制本次操作记录"
  key_links:
    - from: "src/lib/copyStepLog.ts"
      to: "src/store/chat.ts"
      via: "useChatStore.getState().messages 三角色"
      pattern: "useChatStore"
    - from: "src/lib/copyStepLog.ts"
      to: "src/agent/operationLog.ts"
      via: "getWriteOpsByRun（工具步 humanLabel + result）"
      pattern: "getWriteOpsByRun"
    - from: "src/lib/copyStepLog.ts"
      to: "src/lib/debugReport.ts"
      via: "import { copyToClipboard } from './debugReport'（复用，不重写）"
      pattern: "copyToClipboard"
---

<objective>
Wave 5：CARRY-03 落地——buildStepLog 纯函数（三角色 Markdown dump + 脱敏）+ InputBar + Settings 双入口。复用 debugReport.ts 的 copyToClipboard + redact 范式，懒加载 0 初始体积。

Purpose: 让用户能一键复制完整操作记录（user/assistant/tool 三角色 + humanLabel + 脱敏），方便贴 issue/分享 debug，同时绝不泄露 API Key（D-21 守门）。
Output: copyStepLog.ts + 双入口（InputBar + Settings）+ 脱敏测试 GREEN。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- 现有关键接口 -->

From src/lib/debugReport.ts (直接复用，不重写):
```typescript
export async function copyToClipboard(text: string): Promise<boolean>
// 主：navigator.clipboard.writeText；fallback：textarea+execCommand；失败静默 return false

// 安全注释范式（line 9-13）：绝不调 getKey()，绝不读 aster:keys:*，只输出 id 列表

// buildChatSection 范式（line 215-248）：
for (const msg of messages) {
  const time = formatTime(msg.ts ?? 0);
  // format per role
}
```

From src/store/chat.ts (messages store):
```typescript
// useChatStore.getState().messages → Message[]
// Message.role: 'user' | 'assistant' | 'tool'
// Message.content: string（tool role 的 humanLabel / user 的输入 / assistant 的回复）
// Message.toolName: string | undefined（tool role）
// Message.toolResult: ToolResult | undefined（tool role）
// Message.agentRunId: string | undefined（tool/assistant role）
```

From src/agent/operationLog.ts (getWriteOpsByRun，Phase 5 新增):
```typescript
export function getWriteOpsByRun(runId: string): OperationLogEntry[]
// OperationLogEntry.humanLabel: string
// OperationLogEntry.toolName: string
// OperationLogEntry.reverse: ReverseDescriptor
```

From src/components/InputBar.tsx (复制调试信息按钮范式 line 62-70 + 110-118):
```typescript
const handleCopyDebug = async (): Promise<void> => {
  const { buildDebugReport, copyToClipboard } = await import('../lib/debugReport');
  const report = await buildDebugReport();
  const ok = await copyToClipboard(report);
  if (ok) { setCopied(true); setTimeout(() => setCopied(false), 2000); }
};
// 按钮：className="tool-btn" aria-label={copied ? t`已复制` : t`复制调试信息`}
//        <ClipboardIcon size={15} strokeWidth={1.4} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: copyStepLog.ts 新建 + copyStepLog.test.ts 脱敏测试</name>
  <files>
    src/lib/copyStepLog.ts
    src/lib/copyStepLog.test.ts
  </files>
  <read_first>
    - src/lib/debugReport.ts（全文——安全注释 line 1-16；buildChatSection line 215-248；copyToClipboard line 261-282）
    - src/lib/debugReport.test.ts（脱敏断言范式 line 85-100：expect(report).not.toContain('sk-SECRET')）
    - src/lib/copyStepLog.test.ts（Wave 0 已建的 stub，确认已有断言结构）
    - src/store/chat.ts（Message interface，role/content/toolName/toolResult/agentRunId 字段确认）
    - src/agent/operationLog.ts（getWriteOpsByRun + OperationLogEntry 字段确认）
    - src/utils/formatTime.ts（时间格式化工具，debugReport.ts 在用）
  </read_first>
  <behavior>
    - buildStepLog 函数（D-19 全量三角色 Markdown，D-21 脱敏）：
      ```typescript
      export async function buildStepLog(): Promise<string> {
        const lines: string[] = ['# Aster 操作记录', `生成时间：${new Date().toISOString()}`, ''];
        const messages = useChatStore.getState().messages;
        for (const msg of messages) {
          const time = formatTime(msg.ts ?? 0);
          if (msg.role === 'user') {
            lines.push(`## [${time}] 用户`);
            lines.push(redactKey(msg.content ?? ''));
            lines.push('');
          } else if (msg.role === 'assistant') {
            lines.push(`## [${time}] Aster`);
            lines.push(redactKey(msg.content ?? ''));
            lines.push('');
          } else if (msg.role === 'tool') {
            lines.push(`### [${time}] 工具调用：${msg.toolName ?? 'unknown'}`);
            lines.push(`- 描述：${msg.content ?? ''}`)；
            lines.push(`- 结果：${msg.toolResult?.ok ? '成功' : '失败'}`);
            if (msg.toolResult?.data) lines.push(`- 数据：${JSON.stringify(msg.toolResult.data)}`);
            lines.push('');
          }
        }
        return lines.join('\n');
      }
      ```
    - redactKey(text: string): string：正则替换 `/(sk-[A-Za-z0-9\-_]{8,})/g` → `'[API KEY REDACTED]'`（同 debugReport 安全约束：不调 getKey()，不读 localStorage）
    - 安全注释（照搬 debugReport.ts line 9-13 格式）：
      ```
      // 安全约束：绝不调用 getKey()，绝不读 aster:keys:* localStorage
      // 只输出 configuredKeyIds（id 列表，不含 Key 值）
      // redactKey 处理正文中可能粘贴进来的 Key 片段
      ```
    - 直接 `import { copyToClipboard } from './debugReport'`（不重写，PATTERNS 明确复用）
    - 导出：`export { buildStepLog }` + `export { copyToClipboard }` re-export（方便入口懒加载一次 import）
    - copyStepLog.test.ts（Wave 0 已建 stub）：填充真实 mock：
      - mock useChatStore.getState().messages = [{ role:'user', content:'hello', ts:0 }, { role:'assistant', content:'hi', ts:100 }, { role:'tool', toolName:'append_paragraph', content:'追加段落', toolResult:{ok:true,data:{written:5}}, ts:200 }]
      - 断言：`expect(output).toContain('[用户]')` + `expect(output).toContain('[Aster]')` + `expect(output).toContain('追加段落')`
      - 脱敏断言：mock 某 message.content 含 'sk-SECRET-abc123'，`expect(output).not.toMatch(/sk-[A-Za-z0-9]+/)`
  </behavior>
  <action>
    1. 读 debugReport.ts 全文，抄安全注释模板 + copyToClipboard import 范式。
    2. 新建 copyStepLog.ts，实现 buildStepLog + redactKey，import copyToClipboard from debugReport。
    3. 读 copyStepLog.test.ts（Wave 0 stub），补充真实 mock 和断言（mock useChatStore + 脱敏断言）。
    4. 运行 `npx vitest run src/lib/copyStepLog.test.ts` 确认所有 describe GREEN。
    5. 运行 `npm run build && npm run size 2>&1 | tail -5` 确认 ≤82KB（copyStepLog 懒加载，0 初始 chunk）。
  </action>
  <verify>
    <automated>npx vitest run src/lib/copyStepLog.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>copyStepLog.test.ts 所有测试 GREEN，含脱敏断言（不含 sk-*）；`grep -c "redactKey\|getKey\|aster:keys:" src/lib/copyStepLog.ts` 结果：redactKey ≥1，getKey = 0，aster:keys: = 0（安全守门）</done>
</task>

<task type="auto">
  <name>Task 2: InputBar + SettingsPanel 双入口</name>
  <files>
    src/components/InputBar.tsx
    src/components/Settings/SettingsPanel.tsx
  </files>
  <read_first>
    - src/components/InputBar.tsx（全文——handleCopyDebug line 62-70；复制按钮 line 110-118；copied state；tool-btn class）
    - src/components/Settings/SettingsPanel.tsx（全文——找「全局选项」分区，确认 Settings 现有结构）
    - src/styles.css（.tool-btn 是否存在；Settings 区域 class 确认）
  </read_first>
  <action>
    1. 在 InputBar.tsx 的 `handleCopyDebug` 后新增：
       ```typescript
       const [copiedLog, setCopiedLog] = useState(false);
       const handleCopyStepLog = async (): Promise<void> => {
         const { buildStepLog, copyToClipboard } = await import('../lib/copyStepLog');
         const log = await buildStepLog();
         const ok = await copyToClipboard(log);
         if (ok) { setCopiedLog(true); setTimeout(() => setCopiedLog(false), 2000); }
       };
       ```
    2. 在 InputBar.tsx render 的 `tools` div 内，「复制调试信息」按钮之后加：
       ```tsx
       <button
         type="button"
         className="tool-btn"
         aria-label={copiedLog ? t`已复制` : t`复制操作记录`}
         title={copiedLog ? t`已复制 ✓` : t`复制操作记录（已自动隐去 API Key）`}
         onClick={() => void handleCopyStepLog()}
       >
         {copiedLog ? <CheckIcon size={15} strokeWidth={1.4} /> : <ClipboardIcon size={15} strokeWidth={1.4} />}
       </button>
       ```
       注意：若 CheckIcon 未在 icons.tsx 导出，确认已有（DiffLogPanel 的 Task 1 读 icons.tsx 时已确认）。
    3. 在 SettingsPanel.tsx 找「全局选项」或适合的区域，新增：
       ```tsx
       <button
         type="button"
         className="btn btn-ghost btn-sm"
         onClick={() => void handleCopyStepLogSettings()}
         style={{ marginTop: 8 }}
       >
         <Trans>复制本次操作记录</Trans>
       </button>
       <p style={{ fontSize: 'var(--fs-11)', color: 'var(--text-3)', marginTop: 4 }}>
         <Trans>已自动隐去 API Key 与 Provider id</Trans>
       </p>
       ```
       handleCopyStepLogSettings 同 handleCopyStepLog（懒加载 copyStepLog.ts）。
    4. 运行 `npm run lingui extract` 更新 zh-CN catalog（含新 label）。
    5. 运行 `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` 确认无 error（此时 copyStepLog 已实现，tsc 全 pass）。
    6. 运行 `npm run build && npm run size 2>&1 | tail -5` 确认 ≤82KB。
    7. 运行 `npx vitest run 2>&1 | grep -E "^Tests" | tail -1` 确认全套通过。
  </action>
  <verify>
    <automated>npm run build && npm run size 2>&1 | tail -5; npx vitest run 2>&1 | grep -E "^Tests" | tail -1</automated>
  </verify>
  <done>InputBar.tsx 含「复制操作记录」按钮（grep -c "复制操作记录\|copyStepLog" src/components/InputBar.tsx ≥2）；SettingsPanel.tsx 含「复制本次操作记录」入口；bundle ≤82KB；全套测试 pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buildStepLog → clipboard | 脱敏后输出；不调 getKey()；不读 aster:keys:* |
| message.content 正文 → redactKey | 正则过滤 sk-* 前缀 Key 片段（防用户自己粘贴 Key 到聊天框的泄露场景） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Information Disclosure | copy step log 泄露 API Key | mitigate | D-21 + redactKey 正则 + 不调 getKey() + 不读 aster:keys:*；copyStepLog.test.ts 脱敏断言守门（not.toMatch(/sk-[A-Za-z0-9]+/)）；navigator.clipboard 写入前数据不离开浏览器 |
| T-05-09-02 | Information Disclosure | buildStepLog 包含文档内容 | accept | 操作记录中的 data 字段可能含文档内容片段（如 append_paragraph 的 written 长度）；这是 debug 工具的设计目的；用户主动点击复制，知情同意 |

**T-05-09-01 是本 phase 唯一需要主动 mitigate 的威胁（CARRY-03 脱敏是 threat model 核心）。测试断言已守门。**
</threat_model>

<verification>
1. `npx vitest run src/lib/copyStepLog.test.ts` — 脱敏断言 GREEN（not.toMatch(/sk-[A-Za-z0-9]+/)）
2. `grep -c "getKey\|aster:keys:" src/lib/copyStepLog.ts` — 输出 0（安全守门：绝不读 Key）
3. `grep -c "buildStepLog\|copyStepLog" src/components/InputBar.tsx` — ≥2（双检：import + onClick）
4. `grep -c "复制本次操作记录" src/components/Settings/SettingsPanel.tsx` — ≥1
5. `npm run build && npm run size 2>&1 | grep "main-"` — main bundle ≤82KB
6. `npx vitest run 2>&1 | grep -E "^Tests" | tail -1` — all pass
</verification>

<success_criteria>
- copyStepLog.ts：buildStepLog 三角色 Markdown dump，redactKey 脱敏（sk-* → REDACTED），不调 getKey()，复用 copyToClipboard
- 脱敏测试 GREEN
- InputBar「复制操作记录」按钮 + 2s CheckIcon 反馈（懒加载，0 初始体积）
- SettingsPanel 全局选项区「复制本次操作记录」入口 + 脱敏说明细字
- bundle ≤82KB
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-09-SUMMARY.md`
</output>

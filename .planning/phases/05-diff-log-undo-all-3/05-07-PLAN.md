---
phase: 05-diff-log-undo-all-3
plan: 07
type: execute
wave: 3
depends_on:
  - "05-04"
  - "05-05"
  - "05-06"
files_modified:
  - src/agent/tools/write/word.ts
  - src/agent/tools/write/ppt.ts
  - src/agent/tools/write/excel.ts
  - src/agent/tools/index.ts
  - src/agent/loop-helpers.ts
autonomous: true
requirements:
  - TOOL-03
  - TOOL-04
  - AGENT-07
  - AGENT-10

must_haves:
  truths:
    - "word.ts append_paragraph reverse 改为 delete_paragraph_by_content + args:{text} + postState:{kind:'word_paragraph', content:text}（TOOL-04 D-05）"
    - "ppt.ts 新建 insert_slide write tool，reverse = {tool:'delete_slide_by_title', args:{titleFingerprint:title}}，postState:{kind:'ppt_slide', content:{index, title}}"
    - "excel.ts 新建 set_range_values write tool，reverse = {tool:'overwrite_range', args:{address, values}（before-image），postState:{kind:'excel_range', content:{address, values}}"
    - "三个 write tool execute 均 assert result.reverse !== undefined（TOOL-04 runtime 守门）"
    - "buildToolsForHost 注册三宿主各自的新 write tool（excel + ppt 新增）"
    - "loop-helpers.ts appendOperation 调用透传 postState: result.postState（Phase 5 新字段）"
    - "word.test.ts 改后 reverse 断言 GREEN + reverse.toBeDefined() GREEN"
  artifacts:
    - path: "src/agent/tools/write/word.ts"
      provides: "append_paragraph reverse 精确定位（delete_paragraph_by_content）"
      contains: "delete_paragraph_by_content"
    - path: "src/agent/tools/write/ppt.ts"
      provides: "insert_slide write tool + reverse delete_slide_by_title"
      contains: "insert_slide"
    - path: "src/agent/tools/write/excel.ts"
      provides: "set_range_values write tool + reverse overwrite_range"
      contains: "set_range_values"
    - path: "src/agent/loop-helpers.ts"
      provides: "postState 透传到 appendOperation"
      contains: "postState: result.postState"
  key_links:
    - from: "src/agent/tools/write/*.ts"
      to: "src/agent/operationLog.ts"
      via: "loop-helpers appendOperation({ ..., postState: result.postState })"
      pattern: "postState"
    - from: "src/agent/tools/index.ts"
      to: "src/adapters/{Ppt,Excel}Adapter.ts"
      via: "buildToolsForHost 注册 insertSlide + setRangeValues"
      pattern: "buildToolsForHost"
---

<objective>
Wave 3：三宿主 write tool PoC 全部落地。核心改动：(1) word.ts reverse 从 delete_last_paragraph 改为精确定位 delete_paragraph_by_content；(2) 新建 ppt.ts insert_slide + excel.ts set_range_values write tools；(3) buildToolsForHost 注册新 tool；(4) loop-helpers 透传 postState 到 appendOperation。

Purpose: 三宿主 write tool inverse 数据链路闭合——从 tool execute 生成 reverse descriptor + postState，到 loop-helpers 写入 OperationLog，replay engine 已能逆序 replay。
Output: 三个 write tool PoC + 注册 + 数据透传。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-diff-log-undo-all-3/05-CONTEXT.md
@.planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md
@.planning/phases/05-diff-log-undo-all-3/05-PATTERNS.md

<interfaces>
<!-- 现有接口供 executor 参考 -->

From src/agent/tools/write/word.ts (line 38-45，需要修改的 execute 方法):
```typescript
async execute({ text }, ctx): Promise<ToolResult> {
  await (ctx.adapter as WordAdapter).appendParagraph(text);
  const reverse: ReverseDescriptor = {
    tool: 'delete_last_paragraph',  // ← 改为 'delete_paragraph_by_content'
    args: {},                        // ← 改为 { text }
  };
  return { ok: true, data: { written: text.length }, reverse };  // ← 加 postState
}
```

From src/agent/tools/write/word.ts (完整 ToolDef 结构 22-46，新建 ppt.ts / excel.ts 照此模板):
```typescript
export const appendParagraph: ToolDef<AppendParagraphArgs> = {
  name: 'append_paragraph',
  kind: 'write',
  description: '...',
  parameters: { type: 'object', properties: {...}, required: [...] },
  humanLabel: ({ text }) => `在文档末尾追加段落「${text.slice(0, 30)}…」`,
  async execute({ text }, ctx): Promise<ToolResult> { ... }
};
```

From src/agent/tools/index.ts (buildToolsForHost line 172-190):
```typescript
case 'excel':
  return [ listWorksheets, getRangeValues, getUsedRangeSummary, selectionDetail ].map((t) => t as ToolDef);
case 'ppt':
  return [ listSlides, getSlide, listShapesOnSlide, getShape, selectionDetail ].map((t) => t as ToolDef);
```

From src/agent/loop-helpers.ts (appendOperation 调用处 line 154-159，需加 postState):
```typescript
if (result.reverse && def) {
  appendOperation({
    runId, stepIndex: step, toolName: tc.name, args: tc.arguments,
    humanLabel, reverse: result.reverse, timestamp: Date.now(),
    // 新增: postState: result.postState,
  });
}
```

From src/agent/operationLog.ts (Phase 5 重构后，OperationLogEntry 已有 postState?: PostStateSnapshot)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: word.ts reverse 精确定位 + ppt.ts + excel.ts 新建 write tools</name>
  <files>
    src/agent/tools/write/word.ts
    src/agent/tools/write/ppt.ts
    src/agent/tools/write/excel.ts
    src/agent/tools/write/word.test.ts
  </files>
  <read_first>
    - src/agent/tools/write/word.ts（全文）
    - src/agent/tools/write/word.test.ts（确认 Wave 0 改后的 reverse 断言 = 'delete_paragraph_by_content'）
    - src/adapters/PptAdapter.ts（insertSlideAfter 签名，Wave 2c 刚加）
    - src/adapters/ExcelAdapter.ts（setRangeValues 签名，Wave 2b 刚加）
    - src/agent/tools/index.ts（ToolDef / ToolResult / PostStateSnapshot import 路径）
    - .planning/phases/05-diff-log-undo-all-3/05-RESEARCH.md（Code Examples line 848-886）
  </read_first>
  <behavior>
    - word.ts：execute 改为：
      ```typescript
      const reverse: ReverseDescriptor = { tool: 'delete_paragraph_by_content', args: { text } };
      const postState: PostStateSnapshot = { kind: 'word_paragraph', content: text };
      return { ok: true, data: { written: text.length }, reverse, postState };
      ```
    - ppt.ts（新建）：
      - `InsertSlideArgs = { afterIndex?: number; title: string; bullets?: string[] }`（Phase 5 PoC 场景：title 作为 content）
      - humanLabel: `({ title }) => \`在幻灯片末尾插入新幻灯片「${title.slice(0,20)}…」\``
      - execute：调 `(ctx.adapter as PptAdapter).insertSlideAfter(args.afterIndex ?? -1, args.title)` 拿 `{ insertedIndex, title }`
      - reverse: `{ tool: 'delete_slide_by_title', args: { titleFingerprint: title } }`
      - postState: `{ kind: 'ppt_slide', content: { index: insertedIndex, title } }`
    - excel.ts（新建）：
      - `SetRangeValuesArgs = { address: string; values: unknown[][] }`
      - humanLabel: `({ address }) => \`写入单元格区域 ${address}\``
      - execute：调 `(ctx.adapter as ExcelAdapter).setRangeValues(address, values)` 拿 `{ beforeImage }`
      - reverse: `{ tool: 'overwrite_range', args: { address: beforeImage.address, values: beforeImage.values } }`
      - postState: `{ kind: 'excel_range', content: { address, values } }`
    - 三个 write tool execute 内均加 runtime assert（非 throw，注释说明 TOOL-04）：
      `// TOOL-04 runtime assert: reverse 必须非 undefined（write tool 守门）`
      在 return 前：`console.assert(result.reverse !== undefined, 'TOOL-04: write tool must return reverse')`（或直接确保 reverse 字段存在就够）
    - word.test.ts：验证 Wave 0 新增断言（delete_paragraph_by_content + postState）已 GREEN
  </behavior>
  <action>
    1. 改 word.ts execute：把 reverse.tool 改为 'delete_paragraph_by_content'，args 改为 { text }，return 加 postState。
    2. 新建 ppt.ts：照 word.ts 模板写完整 ToolDef。import PptAdapter from adapters。
    3. 新建 excel.ts：照 word.ts 模板写完整 ToolDef。import ExcelAdapter from adapters。
    4. 运行 `npx vitest run src/agent/tools/write/word.test.ts` 确认 delete_paragraph_by_content + postState 断言 GREEN。
    5. 运行 `npx eslint src/agent/tools/write/` 确认无 A-06 violation（write tools 在 src/agent/ 内，不能直接调 Word/Excel/PowerPoint 全局——确认 ppt.ts/excel.ts 只通过 ctx.adapter 调用，不直接调 PowerPoint.run）。
  </action>
  <verify>
    <automated>npx vitest run src/agent/tools/write/word.test.ts && npx eslint src/agent/tools/write/ 2>&1 | grep "error" | head -5 || echo "lint OK"</automated>
  </verify>
  <done>word.test.ts 全 pass（含 delete_paragraph_by_content 断言）；ppt.ts + excel.ts 文件存在；eslint src/agent/tools/write/ 无 A-06 error</done>
</task>

<task type="auto">
  <name>Task 2: buildToolsForHost 注册新 write tools + loop-helpers postState 透传</name>
  <files>
    src/agent/tools/index.ts
    src/agent/loop-helpers.ts
  </files>
  <read_first>
    - src/agent/tools/index.ts（buildToolsForHost line 172-190；ToolResult line 45-50，postState 已加）
    - src/agent/loop-helpers.ts（appendOperation 调用处 line 154-159）
    - src/agent/operationLog.ts（OperationLogEntry interface，postState 字段）
    - src/agent/tools/write/ppt.ts（insertSlide 的导出名，Task 1 刚建）
    - src/agent/tools/write/excel.ts（setRangeValues 的导出名，Task 1 刚建）
  </read_first>
  <action>
    1. 在 tools/index.ts：
       - import `{ insertSlide }` from `'./write/ppt'`
       - import `{ setRangeValues as setRangeValuesTool }` from `'./write/excel'`（避免与 ExcelAdapter 方法名冲突）
       - buildToolsForHost case 'excel' 加 `setRangeValuesTool`
       - buildToolsForHost case 'ppt' 加 `insertSlide`
    2. 在 loop-helpers.ts，找到 appendOperation 调用处（line 154-159）：
       ```typescript
       if (result.reverse && def) {
         appendOperation({
           runId, stepIndex: step, toolName: tc.name, args: tc.arguments,
           humanLabel, reverse: result.reverse,
           postState: result.postState,  // ← 新增透传
           timestamp: Date.now(),
         });
       }
       ```
       注意：OperationLogEntry.postState? 是可选字段，所以 `postState: result.postState` 即使 undefined 也不报错。
    3. 运行 `npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS"` 确认无 error。
    4. 运行 `npx vitest run src/agent/` 确认所有 agent 测试 pass。
    5. 运行 `npm run build && npm run size 2>&1 | tail -5` 确认 bundle ≤82KB。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v copyStepLog | grep "error TS" | head -5; npm run build && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>buildToolsForHost case 'excel' 含 setRangeValues；case 'ppt' 含 insertSlide；loop-helpers appendOperation 调用含 postState 字段；tsc 无 error；bundle ≤82KB；agent 测试全 pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write tool execute → adapter | ctx.adapter 调用（A-06 守门：write tool 在 src/agent/ 内，不能直接用 Office.js 命名空间）|
| reverse.args → replay engine | reverse.args 由 execute 内部构造（before-image/text/title 等），LLM 无法直接注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Elevation of Privilege | A-06 Office namespace 禁令 | mitigate | eslint no-restricted-globals 编译期阻断；ppt.ts/excel.ts 只通过 ctx.adapter 调用 Office.js，不直接引用 PowerPoint/Excel 全局 |
| T-05-07-02 | Tampering | postState.content 携带大量文档内容进内存 | mitigate | postState 只记录「要反操作的那个目标本身」（word_paragraph = text string；excel_range = {address, values}；ppt_slide = {index, title}），不记录整个文档（A-11） |

No high-severity threats.
</threat_model>

<verification>
1. `npx vitest run src/agent/tools/write/word.test.ts` — 全 pass（含 delete_paragraph_by_content 断言）
2. `npx eslint src/agent/tools/write/` — 无 A-06 error（Word/Excel/PowerPoint 未在 write tool 文件中出现）
3. `grep -c "delete_paragraph_by_content" src/agent/tools/write/word.ts` — ≥1
4. `grep -c "insert_slide\|delete_slide_by_title" src/agent/tools/write/ppt.ts` — ≥2
5. `grep -c "set_range_values\|overwrite_range" src/agent/tools/write/excel.ts` — ≥2
6. `grep "postState" src/agent/loop-helpers.ts` — 含 `postState: result.postState`
7. `npm run build && npm run size 2>&1 | grep "main-"` — main bundle ≤82KB gzip
</verification>

<success_criteria>
- word.ts reverse 精确定位（delete_paragraph_by_content + args:{text} + postState）
- ppt.ts insert_slide write tool：reverse delete_slide_by_title + postState
- excel.ts set_range_values write tool：reverse overwrite_range（before-image）+ postState
- buildToolsForHost 注册新 tool
- loop-helpers appendOperation 透传 postState
- eslint A-06 守门通过（write tools 不直接调 Office.js 命名空间）
- bundle ≤82KB
</success_criteria>

<output>
After completion, create `.planning/phases/05-diff-log-undo-all-3/05-07-SUMMARY.md`
</output>

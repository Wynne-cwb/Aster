---
phase: 28-excel-tools
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/adapters/ExcelAdapter.ts
  - src/agent/tools/write/excel.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - EXCEL-11
  - EXCEL-12

must_haves:
  truths:
    - "Agent 能对 Excel 单元格区域执行合并（整块/逐行横向），并通过 undo all 完整还原被丢弃的非左上单元格值（merge 路径快照式）"
    - "Agent 能取消合并，并通过 undo all 重新合并（unmerge 路径快照式）"
    - "Agent 能删除区域内重复行，并通过 undo all 完整还原所有原始行（含被删重复行）"
    - "超过 10,000 单元格时 merge_cells/remove_duplicates 仍执行，但 DiffLog 诚实标注「此步无法自动撤销」"
    - "merge_cells 和 remove_duplicates 守门用例全部 rolled_back（integration.test.ts 变绿）"
    - "contract.test.ts Phase 28 的 merge_cells / remove_duplicates 行 integrationTest 改为 true"
    - "CONTRACT.md Phase 28 的 merge_cells / remove_duplicates 行 status 改为 done"
    - "bundle size 仍 ≤100KB gzip（npm run build && npm run size 验证）"
  artifacts:
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "mergeCells + restoreMergeState + removeDuplicatesRange 三个 adapter 方法"
      contains: "mergeCells"
    - path: "src/agent/tools/write/excel.ts"
      provides: "mergeCellsTool + removeDuplicatesTool ToolDef 定义"
      contains: "merge_cells"
    - path: "src/agent/tools/index.ts"
      provides: "excelWriteTools 数组加入 mergeCellsTool + removeDuplicatesTool"
      contains: "mergeCellsTool"
  key_links:
    - from: "mergeCellsTool.execute"
      to: "adapter.mergeCells"
      via: "ctx.adapter as ExcelAdapter"
      pattern: "mergeCells"
    - from: "executeReverse case 'restore_merge_state'"
      to: "adapter.restoreMergeState(args)"
      via: "operationLog.ts（已在 Plan 01 接线）"
      pattern: "restoreMergeState"
    - from: "removeDuplicatesTool.execute"
      to: "adapter.removeDuplicatesRange"
      via: "ctx.adapter as ExcelAdapter"
      pattern: "removeDuplicatesRange"
    - from: "executeReverse case 'restore_range_values_snapshot'"
      to: "adapter.restoreRangeValuesSnapshot(args)"
      via: "operationLog.ts（既有 case L431）"
      pattern: "restoreRangeValuesSnapshot"
---

<objective>
本计划实现 EXCEL-11（merge_cells）和 EXCEL-12（remove_duplicates）的完整 adapter 方法 + ToolDef + 注册，让 Wave 1 合约守门用例从 skipped_error 变绿为 rolled_back，并将两工具的 integrationTest 改为 true + CONTRACT 改为 done。

Purpose: 交付两个低风险 Excel 工具——merge_cells 用新 restoreMergeState inverse（两路 merge/unmerge 分支），remove_duplicates 复用既有 restoreRangeValuesSnapshot inverse。快照式 undo 严守数据安全硬门。

Output: ExcelAdapter.ts（3 个新方法）、tools/write/excel.ts（2 个新 ToolDef）、tools/index.ts（注册 2 工具）、contract.test.ts（2 行 integrationTest: false → true）、integration.test.ts（2 个守门用例变绿）、CONTRACT.md（2 行 status: planned → done）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-excel-tools/28-CONTEXT.md
@.planning/phases/28-excel-tools/28-RESEARCH.md
@.planning/phases/28-excel-tools/28-PATTERNS.md
@.planning/phases/28-excel-tools/28-01-SUMMARY.md

<interfaces>
<!-- 关键接口，执行者直接使用，无需搜索 -->

从 src/adapters/ExcelAdapter.ts（L57-72）— resolveRange helper（所有 address 必须经此路由）：
```typescript
function resolveRange(ctx: Excel.RequestContext, address: string): Excel.Range {
  const bangIdx = address.indexOf('!');
  if (bangIdx === -1) {
    return ctx.workbook.worksheets.getActiveWorksheet().getRange(address);
  }
  // sheet-qualified 格式 → getItem(sheetName).getRange(localAddr)
  const sheetName = ...; // 解引号 + 转义
  return ctx.workbook.worksheets.getItem(sheetName).getRange(localAddr);
}
```

从 src/adapters/ExcelAdapter.ts（L1223-1253）— SNAPSHOT_LIMIT + readRangeValuesSnapshot：
```typescript
private static readonly SNAPSHOT_LIMIT = 10_000;

private async readRangeValuesSnapshot(address: string): Promise<{ address: string; snapshot: unknown[][] }> {
  return await Excel.run(async (ctx) => {
    const range = resolveRange(ctx, address);
    range.load(['values', 'address', 'cellCount']);
    await ctx.sync();
    if ((range.cellCount as number) > ExcelAdapter.SNAPSHOT_LIMIT) {
      const err = new Error(`区域过大：...`);
      (err as Error & { isTooLarge: boolean }).isTooLarge = true;
      throw err;
    }
    return { address: range.address as string, snapshot: range.values as unknown[][] };
  });
}
```

从 src/adapters/ExcelAdapter.ts（L1265-1278）— restoreRangeValuesSnapshot（已存在，remove_duplicates 复用）：
```typescript
async restoreRangeValuesSnapshot(args: Record<string, unknown>): Promise<void> {
  const address = args.address as string;
  const snapshot = args.snapshot as unknown[][];
  try {
    await Excel.run(async (ctx) => {
      const range = resolveRange(ctx, address);
      range.values = snapshot;
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel restoreRangeValuesSnapshot 失败', err);
  }
}
```

从 src/adapters/ExcelAdapter.ts（L1293-1329）— sortRange 快照式模式（mergeCells/removeDuplicatesRange 照搬）：
```typescript
async sortRange(address: string, sortFields: ...): Promise<{ snapshot: unknown[][] | null; snapshotAddress: string; tooLarge: boolean }> {
  let snapshot: unknown[][] | null = null;
  let snapshotAddress = address;
  let tooLarge = false;
  try {
    const result = await this.readRangeValuesSnapshot(address);
    snapshot = result.snapshot;
    snapshotAddress = result.address;
  } catch (err) {
    if ((err as Error & { isTooLarge?: boolean }).isTooLarge) tooLarge = true;
    else tooLarge = true;
  }
  try {
    await Excel.run(async (ctx) => {
      const range = resolveRange(ctx, address);
      range.sort.apply(sortFields);
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel sortRange 失败', err);
  }
  return { snapshot, snapshotAddress, tooLarge };
}
```

从 src/adapters/ExcelAdapter.ts（约 L1382-1396）— isSetSupported 门控先例（remove_duplicates 照搬，版本改 1.9）：
```typescript
if (!Office.context.requirements.isSetSupported('ExcelApi', '1.9')) {
  throw new HostApiError('当前 Excel 版本不支持删除重复行（需要 ExcelApi 1.9）', undefined);
}
```

从 src/agent/tools/write/excel.ts（L400-455）— sortRangeTool 快照式 ToolDef 模板：
```typescript
export const sortRangeTool: ToolDef = {
  name: 'sort_range',
  kind: 'write',
  description: '对指定 range 按给定列排序。注意：超过 10,000 单元格时将无法自动撤销。',
  parameters: { type: 'object', properties: { address: ..., sortFields: ... }, required: ['address', 'sortFields'] },
  humanLabel: (args: unknown) => `对 ${(args as { address: string }).address} 按第 X 列排序`,
  async execute(args, ctx): Promise<ToolResult> {
    const { address, sortFields } = args as { address: string; sortFields: ... };
    const { snapshot, snapshotAddress, tooLarge } = await (ctx.adapter as ExcelAdapter).sortRange(address, sortFields);
    const reverse: ReverseDescriptor = tooLarge
      ? { tool: 'noop_inverse', args: { reason: `区域过大，无法自动撤销` } }
      : { tool: 'restore_range_values_snapshot', args: { address: snapshotAddress, snapshot } };
    const postState: PostStateSnapshot = { kind: 'excel_snapshot', content: { address, tooLarge } };
    return { ok: true, data: { address, tooLarge }, reverse, postState };
  },
};
```

从 src/agent/tools/index.ts（L298-307）— excelWriteTools 数组（3 个 Phase 28 工具加在 batchWrite 前）：
```typescript
case 'excel': {
  const excelWriteTools = [
    setRangeValuesTool, applyFormula, insertChart, setCell,
    formatExcelRangeTool, setColumnRowSizeTool, setAutoFilterTool,
    addConditionalFormatTool, createTableTool, freezePanesTool,
    sortRangeTool, excelFindAndReplaceTool, manageWorksheetTool, setChartTitleTool,
    // Phase 28 新增（Wave 2 加入 merge + remove_duplicates，Wave 2 末加 createPivotTable）：
    mergeCellsTool, removeDuplicatesTool,
    batchWrite,
  ] as ToolDef[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExcelAdapter 新增 mergeCells + restoreMergeState + removeDuplicatesRange</name>
  <read_first>
    - src/adapters/ExcelAdapter.ts（全文，重点 L1085-1132 createTable/deleteTableByName 范式、L1222-1340 SNAPSHOT_LIMIT/sortRange/readRangeValuesSnapshot/restoreRangeValuesSnapshot、L1382-1409 isSetSupported 门控先例）
    - .planning/phases/28-excel-tools/28-PATTERNS.md（§ExcelAdapter.ts Pattern 1/2/3/4/5/6，含逐字代码 excerpt）
    - .planning/phases/28-excel-tools/28-RESEARCH.md（R3 merge undo 两路分析、R2 removeDuplicates 返回值、Pattern 1 removeDuplicatesRange 完整实现模板）
  </read_first>
  <behavior>
    - 测试 1（merge 正向）：mergeCells({ address:'A1:C1', operation:'merge', across:false }) → mockExcel range.merge 被调用，先 readRangeValuesSnapshot（snapshot 不为 null，tooLarge=false）
    - 测试 2（merge undo）：restoreMergeState({ address:'Sheet1!A1:C1', operation:'merge', across:false, snapshot:[['标题','X','Y']] }) → range.unmerge 被调用 + range.values setter 被调用（两步）
    - 测试 3（unmerge undo）：restoreMergeState({ address:'Sheet1!A1:C1', operation:'unmerge', across:false }) → range.merge 被调用（不写 values）
    - 测试 4（超限）：SNAPSHOT_LIMIT = 10_000，cellCount > 10_000 → tooLarge=true（readRangeValuesSnapshot 抛 isTooLarge 错误被 catch）
    - 测试 5（removeDuplicates 门控）：isSetSupported('ExcelApi','1.9') 返 false → 抛 HostApiError
    - 测试 6（removeDuplicates 正向）：removeDuplicatesRange({ address:'A1:D100' }) → range.removeDuplicates 被调用 + load(['removed','uniqueRemaining']) + sync → 返 { snapshot, tooLarge:false, removed:2, uniqueRemaining:8 }
  </behavior>
  <action>
在 src/adapters/ExcelAdapter.ts 末尾（freeze_panes 或 chart_title 等最后一个方法之后，class 闭合括号 `}` 之前）添加 3 个方法：

**方法 1：mergeCells（快照式写方法，镜像 sortRange 模式）**
```typescript
/** EXCEL-11 merge_cells：合并/取消合并单元格（快照式 undo，merge 路径先快照值）
 *  @param address 单元格区域，如 A1:C1 或 Sheet1!A1:C1（经 resolveRange 路由）
 *  @param operation 'merge' | 'unmerge'
 *  @param across merge(across)：true=逐行横向合并，false/缺省=整块合一
 *  已知限制：合并区含合并单元格时调用 sort.apply 会抛 GeneralException（写进 ToolDef description，不防御性拦截）
 */
async mergeCells(
  address: string,
  operation: 'merge' | 'unmerge',
  across?: boolean,
): Promise<{ snapshot: unknown[][] | null; snapshotAddress: string; tooLarge: boolean }> {
  let snapshot: unknown[][] | null = null;
  let snapshotAddress = address;
  let tooLarge = false;

  // merge 路径必须先快照（merge 会永久清空非左上单元格值）
  // unmerge 路径无值丢失，仍快照（保持快照结构统一，供 restoreMergeState 双路处理）
  try {
    const result = await this.readRangeValuesSnapshot(address);
    snapshot = result.snapshot;
    snapshotAddress = result.address;
  } catch (err) {
    if ((err as Error & { isTooLarge?: boolean }).isTooLarge) tooLarge = true;
    else tooLarge = true;
  }

  try {
    await Excel.run(async (ctx) => {
      const range = resolveRange(ctx, address);
      if (operation === 'merge') {
        range.merge(across ?? false);
      } else {
        range.unmerge();
      }
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel mergeCells 失败', err);
  }

  return { snapshot, snapshotAddress, tooLarge };
}
```

**方法 2：restoreMergeState（inverse，Record 签名，两路分支）**
```typescript
/** EXCEL-11 restore_merge_state inverse：还原合并状态
 *  merge 路径 undo = unmerge + range.values = snapshot（还原被丢弃的值）
 *  unmerge 路径 undo = 重新 merge（无值丢失，不写 values）
 */
async restoreMergeState(args: Record<string, unknown>): Promise<void> {
  const address = args.address as string;
  const operation = args.operation as 'merge' | 'unmerge';
  const across = (args.across ?? false) as boolean;
  const snapshot = args.snapshot as unknown[][] | null | undefined;

  try {
    await Excel.run(async (ctx) => {
      const range = resolveRange(ctx, address);
      if (operation === 'merge') {
        // undo merge = unmerge 后写回快照值（还原非左上单元格被清空的值）
        range.unmerge();
        await ctx.sync();
        if (snapshot) {
          range.values = snapshot;
          await ctx.sync();
        }
      } else {
        // undo unmerge = 重新 merge（无值丢失，只需合并回去）
        range.merge(across);
        await ctx.sync();
      }
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel restoreMergeState 失败', err);
  }
}
```

**方法 3：removeDuplicatesRange（快照式写方法，镜像 sortRange 模式 + isSetSupported 门控）**
```typescript
/** EXCEL-12 remove_duplicates：删除区域内重复行（快照式 undo，复用 restoreRangeValuesSnapshot）
 *  @param address 单元格区域（经 resolveRange 路由）
 *  @param columns 判重列索引（0-based），默认全列
 *  @param includesHeader 区域第一行是否为标题，默认 true
 *  返回 { snapshot, snapshotAddress, tooLarge, removed, uniqueRemaining }
 */
async removeDuplicatesRange(
  address: string,
  columns?: number[],
  includesHeader?: boolean,
): Promise<{ snapshot: unknown[][] | null; snapshotAddress: string; tooLarge: boolean; removed: number; uniqueRemaining: number }> {
  // ExcelApi 1.9 门控（镜像 excelFindAndReplace L1382）
  if (!Office.context.requirements.isSetSupported('ExcelApi', '1.9')) {
    throw new HostApiError('当前 Excel 版本不支持删除重复行（需要 ExcelApi 1.9）', undefined);
  }

  let snapshot: unknown[][] | null = null;
  let snapshotAddress = address;
  let tooLarge = false;

  // 先快照（超限仍执行，但标注 tooLarge=true → ToolDef 走 noop_inverse）
  try {
    const result = await this.readRangeValuesSnapshot(address);
    snapshot = result.snapshot;
    snapshotAddress = result.address;
  } catch (err) {
    if ((err as Error & { isTooLarge?: boolean }).isTooLarge) tooLarge = true;
    else tooLarge = true;
  }

  let removed = 0;
  let uniqueRemaining = 0;

  try {
    await Excel.run(async (ctx) => {
      const range = resolveRange(ctx, address);
      // removeDuplicates 返回 proxy 对象，需 load + sync（Pitfall 4）
      const result = range.removeDuplicates(columns ?? [], includesHeader ?? true);
      result.load(['removed', 'uniqueRemaining']);
      await ctx.sync();
      removed = result.removed as number;
      uniqueRemaining = result.uniqueRemaining as number;
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel removeDuplicatesRange 失败', err);
  }

  return { snapshot, snapshotAddress, tooLarge, removed, uniqueRemaining };
}
```
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "mergeCells\|restoreMergeState\|removeDuplicatesRange" src/adapters/ExcelAdapter.ts && npx tsc --noEmit && npm test -- --run src/agent/operationLog.integration.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async mergeCells" src/adapters/ExcelAdapter.ts` 输出 `1`
    - `grep -c "async restoreMergeState" src/adapters/ExcelAdapter.ts` 输出 `1`
    - `grep -c "async removeDuplicatesRange" src/adapters/ExcelAdapter.ts` 输出 `1`
    - restoreMergeState 签名为 `(args: Record<string, unknown>): Promise<void>`（非位置参，Phase 5 翻车防御）
    - `npx tsc --noEmit` 退出 0
    - integration.test.ts 中 `merge_cells`（restoreMergeState）守门用例 → `rolled_back`（adapter 方法已实现）
    - integration.test.ts 中 `remove_duplicates`（restoreRangeValuesSnapshot）守门用例 → 保持 `rolled_back`
    - `npm test -- --run src/agent/operationLog.integration.test.ts` 中 Phase 28 merge_cells 正向用例绿
  </acceptance_criteria>
  <done>ExcelAdapter 新增 3 个方法（mergeCells / restoreMergeState / removeDuplicatesRange），TypeScript 编译通过，merge_cells integration 守门用例变绿</done>
</task>

<task type="auto">
  <name>Task 2: 定义 mergeCellsTool + removeDuplicatesTool ToolDef，注册入 excelWriteTools，更新合约状态</name>
  <read_first>
    - src/agent/tools/write/excel.ts（全文末尾，重点 L400-455 sortRangeTool 快照式 ToolDef 模板）
    - src/agent/tools/index.ts（L298-307 excelWriteTools 数组，新工具加在 batchWrite 前）
    - src/agent/contract.test.ts（L64-69 Phase 28 行，将 integrationTest: false → true）
    - .planning/phases/08-foundation-a-f/CONTRACT.md（Phase 28 段，将 status: planned → done）
    - .planning/phases/28-excel-tools/28-PATTERNS.md（§tools/write/excel.ts ToolDef 三工具 reverse/postState.kind 映射表）
  </read_first>
  <action>
**改 src/agent/tools/write/excel.ts（追加 2 个 ToolDef）：**

在文件末尾 `freezePanesTool` 定义之后追加（参数键全 snake_case，无 EXCEL_TOOLS Set 需要归一化）：

```typescript
/** EXCEL-11 merge_cells */
export const mergeCellsTool: ToolDef = {
  name: 'merge_cells',
  kind: 'write',
  description:
    '合并或取消合并指定单元格区域。merge 操作会丢弃非左上角单元格的值（仅保留左上角值），撤销时会完整还原。' +
    '注意：已合并区域无法排序，排序前请先取消合并。超过 10,000 单元格时将无法自动撤销。',
  parameters: {
    type: 'object',
    properties: {
      address: { type: 'string', description: '目标区域，如 A1:C1 或 Sheet1!A1:C1' },
      operation: {
        type: 'string',
        enum: ['merge', 'unmerge'],
        description: 'merge=合并为一格，unmerge=取消合并',
      },
      across: {
        type: 'boolean',
        description: '仅对 merge 有效：true=逐行横向合并（每行各自合并），false/缺省=整块合并为单一单元格',
      },
    },
    required: ['address', 'operation'],
  },
  humanLabel: (args: unknown) => {
    const { address, operation } = args as { address: string; operation: string };
    return operation === 'merge' ? `合并单元格 ${address}` : `取消合并 ${address}`;
  },
  async execute(args, ctx): Promise<ToolResult> {
    const { address, operation, across } = args as { address: string; operation: 'merge' | 'unmerge'; across?: boolean };
    const { snapshot, snapshotAddress, tooLarge } = await (ctx.adapter as ExcelAdapter).mergeCells(address, operation, across);
    const reverse: ReverseDescriptor = tooLarge
      ? { tool: 'noop_inverse', args: { reason: `区域过大（超过 10,000 单元格），无法自动撤销` } }
      : {
          tool: 'restore_merge_state',
          args: { address: snapshotAddress, operation, across: across ?? false, snapshot: snapshot ?? undefined },
        };
    const postState: PostStateSnapshot = {
      kind: 'excel_merge',
      content: { address, operation, tooLarge },
    };
    return { ok: true, data: { address, operation }, reverse, postState };
  },
};

/** EXCEL-12 remove_duplicates */
export const removeDuplicatesTool: ToolDef = {
  name: 'remove_duplicates',
  kind: 'write',
  description:
    '删除指定区域内的重复行（保留第一次出现的行，删除后续重复）。撤销时会完整还原所有原始行（含重复行）。' +
    '超过 10,000 单元格时仍会执行删重，但无法自动撤销。需要 ExcelApi 1.9（Office for Web 支持）。',
  parameters: {
    type: 'object',
    properties: {
      address: { type: 'string', description: '目标区域，如 A1:D100' },
      columns: {
        type: 'array',
        items: { type: 'number' },
        description: '判重列索引（0-based），默认全列。如 [0,1] 表示仅按第 1、2 列判重',
      },
      includes_header: {
        type: 'boolean',
        description: '区域第一行是否为标题（不参与删重），默认 true',
      },
    },
    required: ['address'],
  },
  humanLabel: (args: unknown) => {
    const { address } = args as { address: string };
    return `删除 ${address} 内的重复行`;
  },
  async execute(args, ctx): Promise<ToolResult> {
    const { address, columns, includes_header } = args as { address: string; columns?: number[]; includes_header?: boolean };
    const { snapshot, snapshotAddress, tooLarge, removed, uniqueRemaining } =
      await (ctx.adapter as ExcelAdapter).removeDuplicatesRange(address, columns, includes_header);
    const reverse: ReverseDescriptor = tooLarge
      ? { tool: 'noop_inverse', args: { reason: `区域过大（超过 10,000 单元格），无法自动撤销` } }
      : { tool: 'restore_range_values_snapshot', args: { address: snapshotAddress, snapshot } };
    const postState: PostStateSnapshot = {
      kind: 'excel_snapshot',
      content: { address, tooLarge },
    };
    return {
      ok: true,
      data: { address, removed, uniqueRemaining, message: `已删除 ${removed} 行重复，剩余 ${uniqueRemaining} 行唯一行` },
      reverse,
      postState,
    };
  },
};
```

**改 src/agent/tools/index.ts（在 excelWriteTools 数组，batchWrite 前加入两工具）：**
在 `sortRangeTool, excelFindAndReplaceTool, manageWorksheetTool, setChartTitleTool,` 行之后、`batchWrite,` 之前插入：
```typescript
        // Phase 28 Excel 工具补全（Wave 2：merge_cells + remove_duplicates）
        mergeCellsTool, removeDuplicatesTool,
```
并在文件顶部 import 块追加（参照同文件 Phase 10 工具的 import 格式）：
```typescript
import { mergeCellsTool, removeDuplicatesTool } from './write/excel';
// 或合并到现有 excel import 行（看实际文件格式决定）
```

**改 src/agent/contract.test.ts（2 处 integrationTest 改 true）：**
将 Phase 28 区块的 merge_cells 和 remove_duplicates 两行：
```typescript
  { toolName: 'merge_cells', host: 'excel', undoType: '快照式', reverseTool: 'restore_merge_state', phase: 28, integrationTest: false },
  { toolName: 'remove_duplicates', host: 'excel', undoType: '快照式', reverseTool: 'restore_range_values_snapshot', phase: 28, integrationTest: false },
```
改为（integrationTest: false → true）：
```typescript
  { toolName: 'merge_cells', host: 'excel', undoType: '快照式', reverseTool: 'restore_merge_state', phase: 28, integrationTest: true },
  { toolName: 'remove_duplicates', host: 'excel', undoType: '快照式', reverseTool: 'restore_range_values_snapshot', phase: 28, integrationTest: true },
```
⚠️ integrationTest 改 true 意味着 D-17 fs.readFileSync 会扫描 integration.test.ts 中是否存在 'merge_cells' 和 'remove_duplicates' 字符串——已在 Plan 01 加入，安全。

**改 .planning/phases/08-foundation-a-f/CONTRACT.md（2 行 status planned → done）：**
将 Phase 28 段的 merge_cells 和 remove_duplicates 行改为：
```markdown
| merge_cells | excel | 快照式 | restore_merge_state | true | done |
| remove_duplicates | excel | 快照式 | restore_range_values_snapshot | true | done |
```

**最后验证 bundle gate（先 build 再 size，禁止测陈旧 dist）：**
```bash
npm run build && npm run size
```
确认 initial main bundle ≤100KB gzip。
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "mergeCellsTool\|removeDuplicatesTool" src/agent/tools/index.ts && npm test -- --run src/agent/contract.test.ts && npm test -- --run src/agent/operationLog.integration.test.ts && npm run build && npm run size
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'merge_cells'" src/agent/tools/write/excel.ts` 输出 `1`（name: 'merge_cells'）
    - `grep -c "'remove_duplicates'" src/agent/tools/write/excel.ts` 输出 `1`（name: 'remove_duplicates'）
    - `grep -c "mergeCellsTool" src/agent/tools/index.ts` 输出 `>= 1`（import + 数组）
    - `grep -c "removeDuplicatesTool" src/agent/tools/index.ts` 输出 `>= 1`
    - `grep "merge_cells.*integrationTest" src/agent/contract.test.ts` 输出含 `integrationTest: true`
    - `grep "remove_duplicates.*integrationTest" src/agent/contract.test.ts` 输出含 `integrationTest: true`
    - `npm test -- --run src/agent/contract.test.ts` 全部通过（D-17 守门：merge_cells / remove_duplicates 字符串在 integration.test.ts 中存在）
    - `npm test -- --run src/agent/operationLog.integration.test.ts` 中 Phase 28 merge_cells 和 remove_duplicates 正向守门用例均为 `rolled_back`
    - `npm run build && npm run size` 输出 initial main bundle ≤100KB gzip
    - CONTRACT.md Phase 28 段：merge_cells / remove_duplicates 行 integration_test=true, status=done
    - 参数键全 snake_case（address / operation / across / includes_header），无驼峰（Excel 无 EXCEL_TOOLS Set 归一化）
    - humanLabel 为中文（「合并单元格 A1:C1」「取消合并 A1:C1」「删除 A1:D100 内的重复行」）
  </acceptance_criteria>
  <done>EXCEL-11 merge_cells + EXCEL-12 remove_duplicates 全链路交付：ToolDef 定义 + adapter 方法 + excelWriteTools 注册 + integrationTest:true + CONTRACT done + bundle ≤100KB</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_call args → adapter 方法 | args.address / args.operation 经 resolveRange 路由（防 InvalidArgument）；columns 数组直接传 Office.js API |
| merge 操作数据完整性 | merge 前必须 readRangeValuesSnapshot，值丢失后 undo 才能还原（核心安全门） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-1 | Tampering | merge 操作数据丢失（undo 忠实度） | mitigate | merge 前 readRangeValuesSnapshot + 写入 reverse.args.snapshot；restoreMergeState undo 时 unmerge + range.values = snapshot 双步还原；integration 守门用例验证 rolled_back |
| T-28-02-2 | Tampering | remove_duplicates 删除不可逆 | mitigate | 删前 readRangeValuesSnapshot；超限 tooLarge=true → noop_inverse（诚实标注，不假装可撤销）；integration 守门用例验证 rolled_back |
| T-28-02-3 | Spoofing | isSetSupported 门控（ExcelApi 1.9） | mitigate | removeDuplicatesRange 开头 isSetSupported 检查，不支持抛 HostApiError（诚实拒绝，不静默假成功） |
| T-28-02-4 | Information Disclosure | 快照数据存 reverse.args.snapshot（in-memory） | accept | 快照仅存内存（PITFALLS A-11 硬约束），会话结束即清除；无持久化到服务器（无后台硬约束） |
| T-28-02-5 | Elevation of Privilege | resolveRange（Sheet1!A1 格式路由） | mitigate | 全部 address 经 resolveRange helper（ExcelAdapter.ts:57），防 worksheet 级 getRange 拒绝「表名!A1」格式 |
</threat_model>

<verification>
Wave 2 验收命令（EXCEL-11/12 全链路）：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. TypeScript 编译
npx tsc --noEmit
# 2. 合约守门（D-17，merge_cells / remove_duplicates integrationTest:true）
npm test -- --run src/agent/contract.test.ts
# 3. integration 守门（merge_cells / remove_duplicates 正向 → rolled_back）
npm test -- --run src/agent/operationLog.integration.test.ts
# 4. 全套测试
npm test
# 5. bundle gate（先 build 再 size，禁止测陈旧 dist）
npm run build && npm run size
# 期望 initial main bundle ≤100KB gzip
```
</verification>

<success_criteria>
- EXCEL-11 merge_cells 全链路：adapter(mergeCells+restoreMergeState) + ToolDef(snake_case 参数) + index.ts 注册 + integration 守门 rolled_back + integrationTest:true + CONTRACT done
- EXCEL-12 remove_duplicates 全链路：adapter(removeDuplicatesRange) + ToolDef(includes_header snake_case) + index.ts 注册 + integration 守门 rolled_back + integrationTest:true + CONTRACT done
- 超限路径（>10,000 单元格）两工具均走 noop_inverse（诚实不可撤销）
- restoreMergeState 签名 `(args: Record<string, unknown>)`（非位置参，Phase 5 防御）
- bundle CI gate ≤100KB gzip（npm run build && npm run size 验证，非陈旧 dist）
</success_criteria>

<output>
完成后创建 `.planning/phases/28-excel-tools/28-02-SUMMARY.md`，记录：
- ExcelAdapter.ts 新增 3 个方法（行号）：mergeCells / restoreMergeState / removeDuplicatesRange
- tools/write/excel.ts 新增 2 个 ToolDef（行号）：mergeCellsTool / removeDuplicatesTool
- tools/index.ts excelWriteTools 数组变化（行号）
- contract.test.ts 2 行改为 integrationTest: true（merge_cells / remove_duplicates）
- integration.test.ts Phase 28 EXCEL-11/12 守门用例状态（rolled_back）
- bundle 实测数值（npm run size 输出）
- CONTRACT.md Phase 28 段更新（merge_cells / remove_duplicates → done）
</output>

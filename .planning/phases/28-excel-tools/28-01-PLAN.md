---
phase: 28-excel-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/operationLog.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - EXCEL-11
  - EXCEL-12
  - EXCEL-13

must_haves:
  truths:
    - "operationLog.ts 的 PostStateSnapshot.kind union 新增 'excel_merge' 和 'excel_pivot'（readTargetState 走保守 default → undefined）"
    - "operationLog.ts 的 DocumentAdapterForReplay 接口声明 2 个新 inverse 方法（restoreMergeState / deletePivotTableByName）"
    - "operationLog.ts 的 executeReverse switch 新增 2 个 case（'restore_merge_state' / 'delete_pivot_table_by_name'），逐字匹配 CONTRACT reverseTool"
    - "contract.test.ts 的 CONTRACT[] 包含 3 个新 Phase 28 行（integrationTest: false），PhaseNum 含 28"
    - "operationLog.integration.test.ts 包含 6 个新守门用例，3 个 toolName 字面量（'merge_cells' / 'remove_duplicates' / 'create_pivot_table'）出现在文件内"
    - "D-17 硬卡：contract.test.ts 全套通过（contract.test.ts:123 fs.readFileSync 断言 integrationTest:false 工具不扫描，Wave 0 安全）"
    - ".planning/phases/08-foundation-a-f/CONTRACT.md 新增 Phase 28 段落（3 条工具行，status: planned）"
    - "mockExcel() 工厂扩展：新增 merge/unmerge mock、removeDuplicates mock（返回带 load 的 result 对象）、pivotTables mock"
    - "mockOfficeSupportsAll() 工厂已存在于 integration.test.ts（line 364），remove_duplicates/create_pivot_table 守门用例需在开头调用"
  artifacts:
    - path: "src/agent/operationLog.ts"
      provides: "Phase 28 骨架：2 个新 kind + 2 个接口声明 + 2 个 switch case"
      contains: "excel_merge"
    - path: "src/agent/contract.test.ts"
      provides: "Phase 28 CONTRACT 行 + D-17 守门（Wave 0 integrationTest:false 安全）"
      contains: "merge_cells"
    - path: "src/agent/operationLog.integration.test.ts"
      provides: "6 个守门用例桩 + 扩展 mockExcel"
      contains: "create_pivot_table"
    - path: ".planning/phases/08-foundation-a-f/CONTRACT.md"
      provides: "人读合约表 Phase 28 段"
      contains: "Phase 28"
  key_links:
    - from: "contract.test.ts CONTRACT[]"
      to: "operationLog.integration.test.ts"
      via: "fs.readFileSync D-17 — integrationTest:false 时不扫描（Wave 0 安全）"
      pattern: "integrationTest: false"
    - from: "operationLog.ts executeReverse"
      to: "adapter.restoreMergeState"
      via: "case 'restore_merge_state'"
      pattern: "case 'restore_merge_state'"
    - from: "operationLog.ts executeReverse"
      to: "adapter.deletePivotTableByName"
      via: "case 'delete_pivot_table_by_name'"
      pattern: "case 'delete_pivot_table_by_name'"
---

<objective>
本计划完成 Phase 28 的**合约骨架接线（Wave 0）**：在 operationLog.ts 注册 2 个新 PostStateSnapshot kind（excel_merge / excel_pivot）、2 个新接口方法声明、2 个新 executeReverse switch case；在 contract.test.ts 追加 3 条 Phase 28 CONTRACT 行（integrationTest: false — D-17 硬卡 Wave 0 安全）；在 operationLog.integration.test.ts 追加 6 个守门用例（2 个工具 Wave 0 结束时为 skipped_error，1 个工具 remove_duplicates Wave 0 即为 rolled_back，3 个 noop 路径为 skipped_error）；更新 CONTRACT.md 人读表。同时扩展 mockExcel() 工厂，加入 merge/unmerge mock、removeDuplicates mock、pivotTables mock，供 Wave 1-2 实现后守门用例变绿。

Purpose: 合约骨架先行——让 D-17 硬门（fs.readFileSync 断言）、executeReverse case 路由、接口声明全部到位，后续 Wave 1（merge_cells + remove_duplicates）和 Wave 2（create_pivot_table）的 adapter 实现可直接让对应用例变绿，不需再补接线。

Output: operationLog.ts（扩展 3 处）、contract.test.ts（追加 3 行 + PhaseNum 联合类型加 28）、operationLog.integration.test.ts（扩展 mockExcel + 追加 6 个用例）、CONTRACT.md（Phase 28 段）

⚠️ **Wave 0 状态声明：** 本 plan 结束时，integration.test.ts 新增 6 个用例中：
- `remove_duplicates` 正向撤销用例（restoreRangeValuesSnapshot）→ **`rolled_back`**（基建已存在）
- `merge_cells` 正向撤销用例 → **`skipped_error`**（restoreMergeState adapter 未实现，Wave 1 变绿）
- `create_pivot_table` 正向撤销用例 → **`skipped_error`**（deletePivotTableByName adapter 未实现，Wave 2 变绿）
- 3 个 noop 路径用例 → **`skipped_error`**（noop_inverse 永远 throw，符合预期）

**Wave 0 验收 gate：** 只验 `npm test -- --run src/agent/contract.test.ts`（D-17 守门）。integration 全绿须等 Wave 1/2。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-excel-tools/28-CONTEXT.md
@.planning/phases/28-excel-tools/28-RESEARCH.md
@.planning/phases/28-excel-tools/28-PATTERNS.md

<interfaces>
<!-- 关键接口与 analog，执行者直接使用，无需搜索 -->

从 src/agent/operationLog.ts（当前末尾，Phase 27 之后）— PostStateSnapshot.kind union：
```typescript
// 当前末尾（Phase 27 新增后，在 'batch' 前）：
    | 'word_list_format' | 'word_comment' | 'word_header_footer' | 'word_table_cell'
    // Phase 11 新增：batch 整体快照 kind
    | 'batch';
```
Phase 28 在 `| 'batch'` 前插入：
```typescript
    // Phase 28 新增：Excel 工具补全 2 个 kind（readTargetState 走保守 default → undefined）
    | 'excel_merge'   // merge_cells 快照式（unmerge + values 覆写 undo）
    | 'excel_pivot'   // create_pivot_table 简单逆向（delete undo）
    // remove_duplicates 复用现有 'excel_snapshot' kind，无需新增
```

从 src/agent/operationLog.ts（L170-179）— DocumentAdapterForReplay 当前末尾（Phase 27 之后，executeBatchReverse 之前）：
```typescript
  /** Word inverse：还原表格单元格内容（edit_table_cell） */
  restoreTableCell?: (args: Record<string, unknown>) => Promise<void>;
  /** Phase 11：batch_reverse 单闭包逆序撤销 */
  executeBatchReverse?: ...;
```
Phase 28 在 `executeBatchReverse` 前插入：
```typescript
  // ─── Phase 28 Excel 工具补全 inverse 方法 ───
  /** Excel inverse：还原合并状态（merge_cells → restore_merge_state）*/
  restoreMergeState?: (args: Record<string, unknown>) => Promise<void>;
  /** Excel inverse：按名称删除透视表（create_pivot_table → delete_pivot_table_by_name）*/
  deletePivotTableByName?: (args: Record<string, unknown>) => Promise<void>;
  // remove_duplicates 复用现有 restoreRangeValuesSnapshot（L144，无需新增）
```

从 src/agent/operationLog.ts（L565-570）— executeReverse switch 末尾（Phase 27 之后，noop_inverse 之前）：
```typescript
    case 'restore_table_cell':
      if (!adapter.restoreTableCell) {
        throw new Error(`adapter 未实现 restoreTableCell（tool=${reverse.tool}）`);
      }
      await adapter.restoreTableCell(reverse.args);
      break;
    case 'noop_inverse':
      throw new Error(`noop_inverse: 此操作不支持自动回滚（${String(reverse.args.reason ?? '')}）`);
```
Phase 28 在 `case 'noop_inverse'` 前插入：
```typescript
    // ─── Phase 28 Excel 工具补全 2 个新 case ───
    case 'restore_merge_state':
      if (!adapter.restoreMergeState) throw new Error(`adapter 未实现 restoreMergeState（tool=${reverse.tool}）`);
      await adapter.restoreMergeState(reverse.args);
      break;
    case 'delete_pivot_table_by_name':
      if (!adapter.deletePivotTableByName) throw new Error(`adapter 未实现 deletePivotTableByName（tool=${reverse.tool}）`);
      await adapter.deletePivotTableByName(reverse.args);
      break;
    // remove_duplicates 复用现有 'restore_range_values_snapshot' case（L431），无需新增
```

从 src/agent/contract.test.ts（L18）— PhaseNum 类型：
```typescript
type PhaseNum = 9 | 10 | 11 | 23 | 27;  // 改为：
type PhaseNum = 9 | 10 | 11 | 23 | 27 | 28;
```

从 src/agent/contract.test.ts（L68）— CONTRACT 数组末尾（Phase 27 行之后 `];` 前）追加：
```typescript
  // ─── Phase 28 Excel 工具补全 ───
  { toolName: 'merge_cells', host: 'excel', undoType: '快照式', reverseTool: 'restore_merge_state', phase: 28, integrationTest: false },
  { toolName: 'remove_duplicates', host: 'excel', undoType: '快照式', reverseTool: 'restore_range_values_snapshot', phase: 28, integrationTest: false },
  { toolName: 'create_pivot_table', host: 'excel', undoType: '简单逆向', reverseTool: 'delete_pivot_table_by_name', phase: 28, integrationTest: false },
```
注意：integrationTest: false → D-17 fs.readFileSync 只扫描 integrationTest:true 的工具（contract.test.ts:124），Wave 0 时 false 不触发扫描，安全。

从 src/agent/operationLog.integration.test.ts（L58-148）— mockExcel() 工厂当前状态：
```typescript
function mockExcel(): ReturnType<typeof vi.fn> {
  const setValues = vi.fn();
  const range = {
    ...
    sort: { apply: vi.fn() },
    replaceAll: vi.fn(() => ({ load: vi.fn(), count: 0 })),
    // ← 此处缺少 merge、unmerge、removeDuplicates
  };
  const worksheet = {
    getRange: () => range,
    tables: { add: ..., getItemOrNullObject: ... },
    autoFilter: ..., freezePanes: ..., charts: ...,
    // ← 此处缺少 pivotTables
  };
  // Excel.run mock
}
```
Phase 28 需扩展（在 range 对象内追加，在 worksheet 对象内追加）：
```typescript
// range 新增（L96 replaceAll 之后）：
    merge: vi.fn(),
    unmerge: vi.fn(),
    removeDuplicates: vi.fn(() => ({
      load: vi.fn(),
      removed: 2,
      uniqueRemaining: 8,
    })),
    cellCount: 4,  // 已有，确认存在；用于 SNAPSHOT_LIMIT 判断
```
```typescript
// worksheet 新增（tables 之后）：
    pivotTables: {
      add: vi.fn(() => ({
        load: vi.fn(),
        name: 'Aster透视表',
        hierarchies: {
          getItem: vi.fn(() => ({
            load: vi.fn(),
          })),
        },
        rowHierarchies: { add: vi.fn() },
        dataHierarchies: { add: vi.fn() },
        columnHierarchies: { add: vi.fn() },
      })),
      getItem: vi.fn(() => ({
        load: vi.fn(),
        delete: vi.fn(),
      })),
    },
```
注意：mockOfficeSupportsAll() 工厂已在 L364 存在，remove_duplicates/create_pivot_table 守门用例开头需调用（isSetSupported 门控）。

从 src/agent/operationLog.integration.test.ts（L1089-1103）— sort_range 守门用例模板（merge_cells/remove_duplicates 正向照搬）：
```typescript
it('D-17/D-20: sort_range → restore_range_values_snapshot → rolled_back', async () => {
  mockExcel();
  const adapter = new ExcelAdapter();
  const entry: OperationLogEntry = {
    runId: 'r10', stepIndex: 7,
    toolName: 'sort_range',
    reverse: { tool: 'restore_range_values_snapshot', args: { address: 'A1:E500', snapshot: [['a', 'b']] } },
    postState: { kind: 'excel_snapshot', content: { address: 'A1:E500' } },
    timestamp: 0, args: {}, humanLabel: '...',
  };
  const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
  expect(detail.status).toBe('rolled_back');
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 operationLog.ts — 2 个新 kind + 接口声明 + 2 个 switch case</name>
  <read_first>
    - src/agent/operationLog.ts（全文，重点 L34-50 PostStateSnapshot.kind union、L170-179 接口声明末尾、L565-570 switch case 末尾）
    - .planning/phases/28-excel-tools/28-PATTERNS.md（Analog 3c/3d，具体 case 格式）
    - .planning/phases/28-excel-tools/28-RESEARCH.md（R5 operationLog.ts 扩展清单 — 含逐字 case 字符串）
  </read_first>
  <action>
在 src/agent/operationLog.ts 做三处精确修改：

**修改 1：PostStateSnapshot.kind union（在 `| 'batch'` 行之前，`| 'word_table_cell'` 行之后插入）**
```typescript
    // Phase 28 新增：Excel 工具补全 2 个 kind（readTargetState 走保守 default → undefined）
    | 'excel_merge'   // merge_cells 快照式（unmerge + values 覆写 undo）
    | 'excel_pivot'   // create_pivot_table 简单逆向（delete undo）
    // remove_duplicates 复用现有 'excel_snapshot'，无需新增
```
注意：readTargetState 和 isTargetStateConsistent 的 default 分支（返回 undefined/true）已覆盖这 2 个新 kind，**绝不新增 case**（D-21 硬约束）。

**修改 2：DocumentAdapterForReplay 接口（在 executeBatchReverse 声明之前，restoreTableCell 之后插入）**
```typescript
  // ─── Phase 28 Excel 工具补全 inverse 方法 ───
  /** Excel inverse：还原合并状态（merge_cells → restore_merge_state）*/
  restoreMergeState?: (args: Record<string, unknown>) => Promise<void>;
  /** Excel inverse：按名称删除透视表（create_pivot_table → delete_pivot_table_by_name）*/
  deletePivotTableByName?: (args: Record<string, unknown>) => Promise<void>;
  // remove_duplicates 复用现有 restoreRangeValuesSnapshot（已在 L144，无需新增）
```

**修改 3：executeReverse switch（在 `case 'noop_inverse':` 之前，`case 'restore_table_cell':` 之后插入）**
```typescript
    // ─── Phase 28 Excel 工具补全 2 个新 case ───
    case 'restore_merge_state':
      if (!adapter.restoreMergeState) throw new Error(`adapter 未实现 restoreMergeState（tool=${reverse.tool}）`);
      await adapter.restoreMergeState(reverse.args);
      break;
    case 'delete_pivot_table_by_name':
      if (!adapter.deletePivotTableByName) throw new Error(`adapter 未实现 deletePivotTableByName（tool=${reverse.tool}）`);
      await adapter.deletePivotTableByName(reverse.args);
      break;
```
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "excel_merge" src/agent/operationLog.ts && grep -c "excel_pivot" src/agent/operationLog.ts && grep -c "restoreMergeState" src/agent/operationLog.ts && grep -c "deletePivotTableByName" src/agent/operationLog.ts && grep -c "case 'restore_merge_state'" src/agent/operationLog.ts && grep -c "case 'delete_pivot_table_by_name'" src/agent/operationLog.ts && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "excel_merge" src/agent/operationLog.ts` 输出 `1`（在 kind union 中，非注释）
    - `grep -c "excel_pivot" src/agent/operationLog.ts` 输出 `1`（在 kind union 中，非注释）
    - `grep -c "restoreMergeState" src/agent/operationLog.ts` 输出 `2`（接口声明 + switch case）
    - `grep -c "deletePivotTableByName" src/agent/operationLog.ts` 输出 `2`（接口声明 + switch case）
    - `grep -c "case 'restore_merge_state'" src/agent/operationLog.ts` 输出 `1`
    - `grep -c "case 'delete_pivot_table_by_name'" src/agent/operationLog.ts` 输出 `1`
    - `npx tsc --noEmit` 退出 0（TypeScript 编译无错误）
    - readTargetState 中**未**新增 `case 'excel_merge'` 或 `case 'excel_pivot'`（保守路径，default 返回 undefined）
  </acceptance_criteria>
  <done>operationLog.ts 已包含 2 个新 kind、2 个新接口声明、2 个新 switch case，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 更新 contract.test.ts + CONTRACT.md + integration.test.ts 骨架（含扩展 mockExcel）</name>
  <read_first>
    - src/agent/contract.test.ts（全文，重点 L18 PhaseNum、L33-69 CONTRACT 数组末尾、L123-150 守门断言）
    - src/agent/operationLog.integration.test.ts（L58-148 mockExcel 工厂、L364-368 mockOfficeSupportsAll、L1089-1110 sort_range 守门用例模板）
    - .planning/phases/28-excel-tools/28-PATTERNS.md（§integration.test.ts 守门用例模板表格 + Wave 0 期望状态）
    - .planning/phases/08-foundation-a-f/CONTRACT.md（当前末尾结构，Phase 27 段格式）
  </read_first>
  <action>
**改 contract.test.ts（2 处）：**

1. L18 PhaseNum 类型追加 `| 28`：
```typescript
type PhaseNum = 9 | 10 | 11 | 23 | 27 | 28;
```

2. CONTRACT[] 末尾（Phase 27 行之后 `];` 前）追加 3 行（integrationTest: false — Wave 0 安全，D-17 不扫描）：
```typescript
  // ─── Phase 28 Excel 工具补全 ───
  { toolName: 'merge_cells', host: 'excel', undoType: '快照式', reverseTool: 'restore_merge_state', phase: 28, integrationTest: false },
  { toolName: 'remove_duplicates', host: 'excel', undoType: '快照式', reverseTool: 'restore_range_values_snapshot', phase: 28, integrationTest: false },
  { toolName: 'create_pivot_table', host: 'excel', undoType: '简单逆向', reverseTool: 'delete_pivot_table_by_name', phase: 28, integrationTest: false },
```

**改 src/agent/operationLog.integration.test.ts（2 处）：**

1. 扩展 mockExcel() 工厂（L58-148）：
   - 在 range 对象内（L96 `replaceAll: vi.fn(...)` 之后）追加：
```typescript
    merge: vi.fn(),
    unmerge: vi.fn(),
    removeDuplicates: vi.fn(() => ({
      load: vi.fn(),
      removed: 2,
      uniqueRemaining: 8,
    })),
```
   - 在 worksheet 对象内（`tables: { ... }` 块之后，`autoFilter: { ... }` 之前）追加：
```typescript
    pivotTables: {
      add: vi.fn(() => ({
        load: vi.fn(),
        name: 'Aster透视表',
        hierarchies: {
          getItem: vi.fn(() => ({ load: vi.fn() })),
        },
        rowHierarchies: { add: vi.fn() },
        dataHierarchies: { add: vi.fn() },
        columnHierarchies: { add: vi.fn() },
      })),
      getItem: vi.fn(() => ({
        load: vi.fn(),
        delete: vi.fn(),
      })),
    },
```

2. 在现有 Excel 守门用例 describe 块末尾（`sort_range` 用例组之后）追加 6 个 Phase 28 守门用例。每个工具 2 个：正向撤销 + noop 降级路径。

**6 个守门用例（必须包含 3 个 toolName 字面量逐字出现）：**

```typescript
  // ─── Phase 28 Excel 工具补全守门用例 ───

  it('单步撤销 merge_cells（merge 路径）：restore_merge_state 收 Record 对象 → Wave 0 时 skipped_error（adapter 未实现）', async () => {
    mockOfficeSupportsAll();
    mockExcel();
    const adapter = new ExcelAdapter();
    const entry: OperationLogEntry = {
      runId: 'run-28-merge', stepIndex: 0,
      toolName: 'merge_cells',
      args: { address: 'A1:C1', operation: 'merge', across: false },
      humanLabel: '合并单元格 A1:C1',
      reverse: {
        tool: 'restore_merge_state',
        args: { address: 'Sheet1!A1:C1', operation: 'merge', across: false, snapshot: [['标题', 'X', 'Y']] },
      },
      postState: { kind: 'excel_merge', content: { address: 'A1:C1', operation: 'merge' } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    // Wave 0：adapter.restoreMergeState 未实现 → skipped_error；Wave 1 实现后改为 rolled_back
    expect(detail.status).toBe('skipped_error');
  });

  it('merge_cells 超限 noop 路径 → noop_inverse → skipped_error', async () => {
    const entry: OperationLogEntry = {
      runId: 'run-28-merge-noop', stepIndex: 0,
      toolName: 'merge_cells',
      args: { address: 'A1:Z10000', operation: 'merge', across: false },
      humanLabel: '合并单元格 A1:Z10000（区域过大）',
      reverse: {
        tool: 'noop_inverse',
        args: { reason: '区域过大（超过 10,000 单元格），无法自动撤销' },
      },
      postState: { kind: 'excel_merge', content: { address: 'A1:Z10000', tooLarge: true } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, {} as DocumentAdapterForReplay);
    expect(detail.status).toBe('skipped_error');
  });

  it('单步撤销 remove_duplicates：restore_range_values_snapshot 收 Record 对象 → rolled_back', async () => {
    mockOfficeSupportsAll();
    mockExcel();
    const adapter = new ExcelAdapter();
    const entry: OperationLogEntry = {
      runId: 'run-28-dedup', stepIndex: 0,
      toolName: 'remove_duplicates',
      args: { address: 'A1:D100' },
      humanLabel: '删除 A1:D100 内的重复行',
      reverse: {
        tool: 'restore_range_values_snapshot',
        args: { address: 'Sheet1!A1:D100', snapshot: [['A', 'B', 'C', 'D'], ['X', 'Y', 'Z', 'W']] },
      },
      postState: { kind: 'excel_snapshot', content: { address: 'A1:D100' } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    // restoreRangeValuesSnapshot case + adapter 方法均已存在，Wave 0 即 rolled_back
    expect(detail.status).toBe('rolled_back');
  });

  it('remove_duplicates 超限 noop 路径 → noop_inverse → skipped_error', async () => {
    const entry: OperationLogEntry = {
      runId: 'run-28-dedup-noop', stepIndex: 0,
      toolName: 'remove_duplicates',
      args: { address: 'A1:D50000' },
      humanLabel: '删除 A1:D50000 内的重复行（区域过大）',
      reverse: {
        tool: 'noop_inverse',
        args: { reason: '区域过大（超过 10,000 单元格），无法自动撤销' },
      },
      postState: { kind: 'excel_snapshot', content: { address: 'A1:D50000', tooLarge: true } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, {} as DocumentAdapterForReplay);
    expect(detail.status).toBe('skipped_error');
  });

  it('单步撤销 create_pivot_table：delete_pivot_table_by_name 收 Record 对象 → Wave 0 时 skipped_error（adapter 未实现）', async () => {
    mockOfficeSupportsAll();
    mockExcel();
    const adapter = new ExcelAdapter();
    const entry: OperationLogEntry = {
      runId: 'run-28-pivot', stepIndex: 0,
      toolName: 'create_pivot_table',
      args: { source_range: 'A1:D50', destination: 'F1', name: 'Aster透视表' },
      humanLabel: '创建数据透视表 Aster透视表',
      reverse: {
        tool: 'delete_pivot_table_by_name',
        args: { pivotTableName: 'Aster透视表' },
      },
      postState: { kind: 'excel_pivot', content: { pivotTableName: 'Aster透视表' } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    // Wave 0：adapter.deletePivotTableByName 未实现 → skipped_error；Wave 2 实现后改为 rolled_back
    expect(detail.status).toBe('skipped_error');
  });

  it('create_pivot_table API 不可用降级路径 → noop_inverse → skipped_error', async () => {
    const entry: OperationLogEntry = {
      runId: 'run-28-pivot-noop', stepIndex: 0,
      toolName: 'create_pivot_table',
      args: { source_range: 'A1:D50', destination: 'F1' },
      humanLabel: '创建数据透视表（API 不可用）',
      reverse: {
        tool: 'noop_inverse',
        args: { reason: '当前 Office for Web 不支持创建数据透视表（ExcelApi 1.8 不可用）' },
      },
      postState: { kind: 'excel_pivot', content: { tooLarge: true } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, {} as DocumentAdapterForReplay);
    expect(detail.status).toBe('skipped_error');
  });
```

**改 .planning/phases/08-foundation-a-f/CONTRACT.md：**
在文件末尾（Phase 27 段之后）追加 Phase 28 段：
```markdown
## Phase 28：Excel 工具补全

| 工具名 | Host | undo 分类 | reverseTool | integration_test | status |
|--------|------|----------|------------|-----------------|--------|
| merge_cells | excel | 快照式 | restore_merge_state | false | planned |
| remove_duplicates | excel | 快照式 | restore_range_values_snapshot | false | planned |
| create_pivot_table | excel | 简单逆向 | delete_pivot_table_by_name | false | planned |
```
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "phase: 28" src/agent/contract.test.ts && grep -c "merge_cells" src/agent/operationLog.integration.test.ts && grep -c "remove_duplicates" src/agent/operationLog.integration.test.ts && grep -c "create_pivot_table" src/agent/operationLog.integration.test.ts && npm test -- --run src/agent/contract.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phase: 28" src/agent/contract.test.ts` 输出 `3`（3 条 Phase 28 工具行）
    - `grep -c "'merge_cells'" src/agent/operationLog.integration.test.ts` 输出 `>= 1`（D-17 toolName 字面量存在）
    - `grep -c "'remove_duplicates'" src/agent/operationLog.integration.test.ts` 输出 `>= 1`
    - `grep -c "'create_pivot_table'" src/agent/operationLog.integration.test.ts` 输出 `>= 1`
    - `npm test -- --run src/agent/contract.test.ts` 全部通过（CONTRACT 守门通过，D-17 不挂——因为 Phase 28 行 integrationTest: false 不触发扫描）
    - mockExcel() 已包含 `merge: vi.fn()`, `unmerge: vi.fn()`, `removeDuplicates: vi.fn(...)`, `pivotTables: { add: ..., getItem: ... }`
    - `remove_duplicates` 正向守门用例期望 `rolled_back`（基建已存在）；`merge_cells` / `create_pivot_table` 正向守门用例期望 `skipped_error`（adapter 未实现，Wave 1/2 后变绿）
    - `.planning/phases/08-foundation-a-f/CONTRACT.md` 包含 `Phase 28` 字符串及 3 条工具行（status: planned）
    - ⚠️ **Wave 0 后 integration 6 个新用例：** `remove_duplicates` 正向为 `rolled_back`，其余 5 个为 `skipped_error`（这是 Wave 0 正确状态）
  </acceptance_criteria>
  <done>contract.test.ts 含 3 条 Phase 28 行（D-17 Wave 0 安全）；integration.test.ts 含 3 个 toolName 字面量 + 6 个守门用例 + 扩展后 mockExcel（含 merge/unmerge/removeDuplicates/pivotTables mock）；CONTRACT.md 含 Phase 28 段（status: planned）。Wave 0 验收 gate = `npm test -- --run src/agent/contract.test.ts` 退出 0。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 生产代码接口 | contract.test.ts 通过 fs.readFileSync 读 integration.test.ts，确保守门覆盖（无用户数据流） |
| replay engine → adapter 方法 | executeReverse switch 路由到 adapter 方法——路由错误产生 skipped_error（安全降级，不改错目标） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-1 | Tampering | executeReverse switch（新增 2 个 case） | mitigate | switch 末尾 `default: throw` 防止未知 reverse tool 被静默忽略；新 case 在 default 前插入正确位置，TypeScript 编译验证 |
| T-28-01-2 | Elevation of Privilege | DocumentAdapterForReplay 接口新增方法 | accept | 接口声明为可选（`?:`），adapter 未实现时 `if (!adapter.xxx) throw` → skipped_error，不越权执行 |
| T-28-01-3 | Information Disclosure | CONTRACT.md 人读合约表 | accept | 仅规划文档，无 PII；integration_test:false 仅表示待实现，无安全影响 |
| T-28-01-4 | Denial of Service | mockExcel 扩展（测试环境） | accept | 仅测试环境使用，无生产 DoS 面；mock 扩展不影响生产代码路径 |
</threat_model>

<verification>
Wave 0 验收命令（⚠️ 只跑 contract.test.ts；integration 部分用例预期 skipped_error，不在 Wave 0 验收范围）：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. TypeScript 编译
npx tsc --noEmit
# 2. Wave 0 唯一验收 gate：contract.test.ts（D-17 守门）退出 0
npm test -- --run src/agent/contract.test.ts
# 3. 确认 operationLog.ts 三处修改
grep -c "excel_merge\|excel_pivot" src/agent/operationLog.ts
# 期望 >= 2（kind union 中）
grep -c "case 'restore_merge_state'\|case 'delete_pivot_table_by_name'" src/agent/operationLog.ts
# 期望 2（switch case）
# 4. 确认 3 个 toolName 字面量出现在 integration.test.ts
grep -c "'merge_cells'" src/agent/operationLog.integration.test.ts
grep -c "'remove_duplicates'" src/agent/operationLog.integration.test.ts
grep -c "'create_pivot_table'" src/agent/operationLog.integration.test.ts
# 各期望 >= 1
```
</verification>

<success_criteria>
- operationLog.ts 含 2 个新 PostStateSnapshot kind（excel_merge / excel_pivot）+ 2 个接口声明（restoreMergeState / deletePivotTableByName）+ 2 个 switch case（reverse_merge_state / delete_pivot_table_by_name），TypeScript 编译无错误
- contract.test.ts 含 3 条 Phase 28 行（integrationTest: false），D-17 守门通过（Wave 0 false 不触发 fs.readFileSync 扫描）
- integration.test.ts 含 3 个 toolName 字面量（merge_cells / remove_duplicates / create_pivot_table）+ 6 个守门用例 + 扩展 mockExcel（merge/unmerge/removeDuplicates/pivotTables mock）
- CONTRACT.md 含 Phase 28 段落（3 条工具行，status: planned，integration_test: false）
- **Wave 0 验收 gate = `npm test -- --run src/agent/contract.test.ts` 退出 0**
- ⚠️ **Wave 0 后 integration 6 个用例状态：** remove_duplicates 正向 → `rolled_back`；其余 5 个 → `skipped_error`（adapter 方法 Wave 1/2 实现后变绿，不卡 Wave 0 验收）
</success_criteria>

<output>
完成后创建 `.planning/phases/28-excel-tools/28-01-SUMMARY.md`，记录：
- operationLog.ts 三处改动（行号）：kind union / 接口声明 / switch case
- contract.test.ts 新增 3 行（工具名、phase 28、integrationTest: false）
- integration.test.ts mockExcel 扩展内容（merge/unmerge/removeDuplicates/pivotTables 各 mock）+ 6 个新用例摘要
- 当前 integration test 状态（remove_duplicates 正向 = rolled_back；其余 5 个 = skipped_error——Wave 0 正确状态；等 Wave 1/2 实现后变绿）
- CONTRACT.md Phase 28 段（3 条 planned 工具行）
</output>

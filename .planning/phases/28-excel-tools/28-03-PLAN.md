---
phase: 28-excel-tools
plan: 03
type: execute
wave: 3
depends_on:
  - 28-01
  - 28-02
files_modified:
  - src/adapters/ExcelAdapter.ts
  - src/agent/tools/write/excel.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - EXCEL-13

must_haves:
  truths:
    - "Agent 能用 source_range 和 destination 创建数据透视表（含 row_fields/data_fields/column_fields 可选字段配置），并通过 undo all 删除透视表"
    - "isSetSupported('ExcelApi','1.8') 返 false 或 pivotTables.add 抛错时，工具诚实 noop+gate（明确错误「当前 Excel 版本不支持创建数据透视表（需要 ExcelApi 1.8）」，不静默假成功，不中断 agent）"
    - "create_pivot_table 守门用例变绿：正向 rolled_back，降级路径 skipped_error"
    - "contract.test.ts Phase 28 的 create_pivot_table 行 integrationTest 改为 true"
    - "CONTRACT.md Phase 28 的 create_pivot_table 行 status 改为 done"
    - "bundle size 仍 ≤100KB gzip（Phase 28 三工具全部就位后验收）"
    - "npm test 全套绿（1109 + 新增守门用例）"
  artifacts:
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "createPivotTable + deletePivotTableByName 两个 adapter 方法"
      contains: "createPivotTable"
    - path: "src/agent/tools/write/excel.ts"
      provides: "createPivotTableTool ToolDef 定义"
      contains: "create_pivot_table"
    - path: "src/agent/tools/index.ts"
      provides: "excelWriteTools 数组加入 createPivotTableTool"
      contains: "createPivotTableTool"
  key_links:
    - from: "createPivotTableTool.execute"
      to: "adapter.createPivotTable"
      via: "ctx.adapter as ExcelAdapter + isSetSupported 门控"
      pattern: "createPivotTable"
    - from: "executeReverse case 'delete_pivot_table_by_name'"
      to: "adapter.deletePivotTableByName(args)"
      via: "operationLog.ts（已在 Plan 01 接线）"
      pattern: "deletePivotTableByName"
    - from: "pivotTables.add → load(['name']) → ctx.sync()"
      to: "reverse.args.pivotTableName"
      via: "server 端规范化名称（防重名自动改名）"
      pattern: "pivotTableName"
---

<objective>
本计划实现 EXCEL-13（create_pivot_table）的完整 adapter 方法 + ToolDef + 注册，包含双层门控（isSetSupported('ExcelApi','1.8') + try/catch）和完整字段配置（rowHierarchies/dataHierarchies/columnHierarchies），让 Wave 2 合约守门用例从 skipped_error 变绿为 rolled_back，并完成 Phase 28 全部收尾（integrationTest:true + CONTRACT done + bundle gate 最终验收）。

Purpose: 交付高复杂度 Excel 工具——create_pivot_table 基于 R1 verdict（ExcelApi 1.8 在 Office for Web 官方支持）实现完整建表+字段配置，undo = deletePivotTableByName（简单逆向，镜像 delete_table_by_name）。降级路径（API 不可用/抛错）诚实 noop+gate，满足 ROADMAP SC#3。

Output: ExcelAdapter.ts（2 个新方法）、tools/write/excel.ts（1 个新 ToolDef）、tools/index.ts（注册 createPivotTableTool）、contract.test.ts（1 行 integrationTest: false → true）、integration.test.ts（create_pivot_table 守门用例变绿）、CONTRACT.md（create_pivot_table → done）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-excel-tools/28-CONTEXT.md
@.planning/phases/28-excel-tools/28-RESEARCH.md
@.planning/phases/28-excel-tools/28-PATTERNS.md
@.planning/phases/28-excel-tools/28-01-SUMMARY.md
@.planning/phases/28-excel-tools/28-02-SUMMARY.md

<interfaces>
<!-- 关键接口，执行者直接使用，无需搜索 -->

从 src/adapters/ExcelAdapter.ts（L1085-1132）— createTable / deleteTableByName 简单逆向范式（create_pivot_table 直接镜像）：
```typescript
// createTable：add + load name + sync 读取 server 端规范化名称
async createTable(address: string, hasHeaders: boolean, tableName?: string): Promise<{ resolvedName: string }> {
  try {
    return await Excel.run(async (ctx) => {
      const sheet = ctx.workbook.worksheets.getActiveWorksheet();
      const table = sheet.tables.add(address, hasHeaders ?? false);
      if (tableName) table.name = tableName;
      table.load(['name']);      // sync 前必须 load（proxy 对象懒求值）
      await ctx.sync();
      return { resolvedName: table.name as string };  // server 端规范化名（防重名改名）
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel createTable 失败', err);
  }
}

// deleteTableByName：getItemOrNullObject（tables 集合有 OrNullObject 变体）
async deleteTableByName(args: Record<string, unknown>): Promise<void> {
  const tableName = args.tableName as string;
  try {
    await Excel.run(async (ctx) => {
      const sheet = ctx.workbook.worksheets.getActiveWorksheet();
      const table = sheet.tables.getItemOrNullObject(tableName);
      table.load('isNullObject');
      await ctx.sync();
      if (!table.isNullObject) {
        table.delete();
        await ctx.sync();
      }
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel deleteTableByName 失败', err);
  }
}
```
⚠️ create_pivot_table 的 deletePivotTableByName 与 deleteTableByName 的关键差异：**pivotTables 集合无 getItemOrNullObject（不同于 tables 集合）**，必须用 `getItem(name)` + try/catch 静默已删场景。

从 RESEARCH.md §Pattern 2 — createPivotTable 完整实现模板：
```typescript
async createPivotTable(opts: {
  sourceRange: string;
  destination: string;
  name?: string;
  rowFields?: string[];
  dataFields?: string[];
  columnFields?: string[];
}): Promise<{ pivotTableName: string }> {
  if (!Office.context.requirements.isSetSupported('ExcelApi', '1.8')) {
    throw new HostApiError('当前 Excel 版本不支持创建数据透视表（需要 ExcelApi 1.8）', undefined);
  }
  try {
    return await Excel.run(async (ctx) => {
      const ws = ctx.workbook.worksheets.getActiveWorksheet();
      const pivotTable = ws.pivotTables.add(opts.name ?? 'Aster透视表', opts.sourceRange, opts.destination);
      pivotTable.load(['name']);
      await ctx.sync(); // sync 1: 建表 + 读 name（server 端规范化，防重名）
      const pivotTableName = pivotTable.name as string;
      // 字段配置（fields in hierarchies.getItem(fieldName) → add 到对应集合）
      if (opts.rowFields?.length) {
        for (const f of opts.rowFields) {
          pivotTable.rowHierarchies.add(pivotTable.hierarchies.getItem(f));
        }
      }
      if (opts.dataFields?.length) {
        for (const f of opts.dataFields) {
          pivotTable.dataHierarchies.add(pivotTable.hierarchies.getItem(f));
        }
      }
      if (opts.columnFields?.length) {
        for (const f of opts.columnFields) {
          pivotTable.columnHierarchies.add(pivotTable.hierarchies.getItem(f));
        }
      }
      await ctx.sync(); // sync 2: 提交字段配置
      return { pivotTableName };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel createPivotTable 失败', err);
  }
}
```

从 PATTERNS.md §tools/write/excel.ts — createTableTool 简单逆向 ToolDef 模板：
```typescript
async execute(args, ctx): Promise<ToolResult> {
  const { address, hasHeaders, tableName } = args as { address: string; ... };
  const { resolvedName } = await (ctx.adapter as ExcelAdapter).createTable(address, hasHeaders ?? false, tableName);
  const reverse: ReverseDescriptor = {
    tool: 'delete_table_by_name',
    args: { tableName: resolvedName },  // server 端规范化名称
  };
  const postState: PostStateSnapshot = { kind: 'excel_table', content: { tableName: resolvedName } };
  return { ok: true, data: { tableName: resolvedName }, reverse, postState };
}
```
create_pivot_table 镜像：`pivotTableName`（非 tableName）、`tool: 'delete_pivot_table_by_name'`、`kind: 'excel_pivot'`。

从 PATTERNS.md §integration.test.ts — create_pivot_table 守门用例 Wave 0 期望状态表格：
- `create_pivot_table` 正向 → Wave 0 skipped_error（adapter 未实现），Wave 3（本 plan）实现后 → **rolled_back**
- `create_pivot_table` 降级路径 → skipped_error（noop_inverse 永远 throw）

从 PATTERNS.md 风险点 2（关键差异）：
```
pivotTables 集合无 getItemOrNullObject（不同于 tables 集合），
deletePivotTableByName 用 getItem(name) + try/catch 静默已删场景：
try {
  const pt = ws.pivotTables.getItem(pivotTableName);
  pt.delete();
  await ctx.sync();
} catch {
  // 透视表已不存在，静默跳过（幂等 undo）
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExcelAdapter 新增 createPivotTable + deletePivotTableByName</name>
  <read_first>
    - src/adapters/ExcelAdapter.ts（全文末尾，重点 L1085-1132 createTable/deleteTableByName、L1382 isSetSupported 门控先例；本 plan 在 Plan 02 新增方法之后追加）
    - .planning/phases/28-excel-tools/28-PATTERNS.md（§Analog 3 createPivotTable/deletePivotTableByName 范式 + 风险点 2）
    - .planning/phases/28-excel-tools/28-RESEARCH.md（§Pattern 2 完整实现模板 + Pitfall 3 name 不稳定 + Pitfall 6 字段名大小写）
  </read_first>
  <behavior>
    - 测试 1（门控）：isSetSupported('ExcelApi','1.8') 返 false → 抛 HostApiError（含「需要 ExcelApi 1.8」文本）
    - 测试 2（建表正向）：createPivotTable({ sourceRange:'A1:D50', destination:'F1', name:'TestPT' }) → ws.pivotTables.add 被调用 + load(['name']) + sync1 + 返 { pivotTableName: 'TestPT' }
    - 测试 3（字段配置）：rowFields=['地区'], dataFields=['销售额'] → rowHierarchies.add + dataHierarchies.add 各被调用 1 次
    - 测试 4（deletePivotTableByName 幂等）：pivotTableName='TestPT'，getItem(name) 存在 → delete + sync；若已不存在（getItem 抛 ItemNotFound）→ catch 静默，不抛错
    - 测试 5（createPivotTable try/catch 包裹）：add 内部抛非 HostApiError 的错误 → 包装成 HostApiError 抛出
  </behavior>
  <action>
在 src/adapters/ExcelAdapter.ts 末尾（Plan 02 新增的 removeDuplicatesRange 之后，class 闭合括号 `}` 之前）添加 2 个方法：

**方法 1：createPivotTable（双层门控 + 完整字段配置）**
```typescript
/** EXCEL-13 create_pivot_table：创建数据透视表（ExcelApi 1.8，Office for Web 官方支持）
 *  @param opts.sourceRange 数据源区域（如 A1:D50）
 *  @param opts.destination 透视表放置位置（如 F1）
 *  @param opts.name 透视表名称（可选，Excel 可能自动重命名以防重名 → 用 server 端实际 name）
 *  @param opts.rowFields 行字段名数组（与数据列头完全匹配，大小写敏感）
 *  @param opts.dataFields 值字段名数组（同上）
 *  @param opts.columnFields 列字段名数组（同上）
 *  已知限制：OLAP/Power Pivot 不支持（标准数据区域/Table 透视表可用）；字段名必须与列头完全匹配（大小写敏感）
 */
async createPivotTable(opts: {
  sourceRange: string;
  destination: string;
  name?: string;
  rowFields?: string[];
  dataFields?: string[];
  columnFields?: string[];
}): Promise<{ pivotTableName: string }> {
  // 双层门控第一层：运行时 isSetSupported
  if (!Office.context.requirements.isSetSupported('ExcelApi', '1.8')) {
    throw new HostApiError('当前 Excel 版本不支持创建数据透视表（需要 ExcelApi 1.8）', undefined);
  }
  // 双层门控第二层：try/catch 包裹整个 Excel.run（防运行时 API 实际不可用）
  try {
    return await Excel.run(async (ctx) => {
      const ws = ctx.workbook.worksheets.getActiveWorksheet();
      // 建表（opts.name 为期望名，server 端实际 name 由 load 读取）
      const pivotTable = ws.pivotTables.add(
        opts.name ?? 'Aster透视表',
        opts.sourceRange,
        opts.destination,
      );
      pivotTable.load(['name']);
      await ctx.sync(); // sync 1: 建表 + 读 server 端规范化 name（防重名自动改名——Pitfall 3）

      const pivotTableName = pivotTable.name as string;

      // 字段配置（按字段名定位 hierarchy → add 到对应集合）
      // 注意：hierarchies.getItem(fieldName) 大小写敏感（Pitfall 6）
      if (opts.rowFields?.length) {
        for (const f of opts.rowFields) {
          pivotTable.rowHierarchies.add(pivotTable.hierarchies.getItem(f));
        }
      }
      if (opts.dataFields?.length) {
        for (const f of opts.dataFields) {
          pivotTable.dataHierarchies.add(pivotTable.hierarchies.getItem(f));
        }
      }
      if (opts.columnFields?.length) {
        for (const f of opts.columnFields) {
          pivotTable.columnHierarchies.add(pivotTable.hierarchies.getItem(f));
        }
      }
      await ctx.sync(); // sync 2: 提交字段配置

      return { pivotTableName };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel createPivotTable 失败', err);
  }
}
```

**方法 2：deletePivotTableByName（inverse，Record 签名，getItem + try/catch）**
⚠️ pivotTables 集合无 getItemOrNullObject（与 tables 集合不同），用 try/catch 静默已删场景：
```typescript
/** EXCEL-13 delete_pivot_table_by_name inverse：按名称删除数据透视表（幂等，已删静默跳过）
 *  与 deleteTableByName 的差异：pivotTables 无 getItemOrNullObject，用 try/catch 静默 ItemNotFound
 */
async deletePivotTableByName(args: Record<string, unknown>): Promise<void> {
  const pivotTableName = args.pivotTableName as string;
  try {
    await Excel.run(async (ctx) => {
      const ws = ctx.workbook.worksheets.getActiveWorksheet();
      try {
        const pt = ws.pivotTables.getItem(pivotTableName);
        pt.delete();
        await ctx.sync();
      } catch {
        // 透视表已不存在（ItemNotFound）→ 静默跳过（幂等 undo）
      }
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Excel deletePivotTableByName 失败', err);
  }
}
```
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "async createPivotTable\|async deletePivotTableByName" src/adapters/ExcelAdapter.ts && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async createPivotTable" src/adapters/ExcelAdapter.ts` 输出 `1`
    - `grep -c "async deletePivotTableByName" src/adapters/ExcelAdapter.ts` 输出 `1`
    - deletePivotTableByName 签名为 `(args: Record<string, unknown>): Promise<void>`（非位置参）
    - `grep "getItemOrNullObject" src/adapters/ExcelAdapter.ts | grep "pivotTable"` 输出为空（pivotTables 用 getItem + try/catch，不用 OrNullObject）
    - `grep -c "isSetSupported.*ExcelApi.*1.8" src/adapters/ExcelAdapter.ts` 输出 `>= 1`
    - `npx tsc --noEmit` 退出 0
  </acceptance_criteria>
  <done>ExcelAdapter 新增 createPivotTable + deletePivotTableByName，TypeScript 编译通过，降级门控和幂等 undo 实现正确</done>
</task>

<task type="auto">
  <name>Task 2: 定义 createPivotTableTool ToolDef，注册入 excelWriteTools，完成 Phase 28 全套收尾</name>
  <read_first>
    - src/agent/tools/write/excel.ts（全文末尾，重点 Plan 02 新增的 mergeCellsTool/removeDuplicatesTool 格式；createPivotTableTool 在最后追加）
    - src/agent/tools/index.ts（L298-307 excelWriteTools 数组，在 batchWrite 前加入 createPivotTableTool）
    - src/agent/contract.test.ts（Phase 28 的 create_pivot_table 行，将 integrationTest: false → true）
    - src/agent/operationLog.integration.test.ts（Phase 28 守门用例 create_pivot_table 正向用例，更新期望 → rolled_back）
    - .planning/phases/08-foundation-a-f/CONTRACT.md（Phase 28 段，将 create_pivot_table status: planned → done）
  </read_first>
  <action>
**改 src/agent/tools/write/excel.ts（追加 createPivotTableTool ToolDef）：**

在文件末尾（removeDuplicatesTool 之后）追加：
```typescript
/** EXCEL-13 create_pivot_table（ExcelApi 1.8，Office for Web 支持；不可用时诚实 noop+gate） */
export const createPivotTableTool: ToolDef = {
  name: 'create_pivot_table',
  kind: 'write',
  description:
    '在指定位置创建数据透视表，支持配置行/列/值字段。撤销时删除整张透视表。' +
    'row_fields/data_fields/column_fields 中的字段名必须与源数据列头完全匹配（区分大小写）。' +
    '仅支持标准数据区域，不支持 OLAP/Power Pivot 数据源。需要 ExcelApi 1.8（Office for Web 支持）。' +
    '如当前环境不支持，工具会明确告知并跳过，不会中断后续步骤。',
  parameters: {
    type: 'object',
    properties: {
      source_range: {
        type: 'string',
        description: '数据源区域，如 A1:D50（含列头）',
      },
      destination: {
        type: 'string',
        description: '透视表左上角放置位置，如 F1',
      },
      name: {
        type: 'string',
        description: '透视表名称（可选，默认「Aster透视表」；Excel 可能自动加数字后缀防重名）',
      },
      row_fields: {
        type: 'array',
        items: { type: 'string' },
        description: '行字段列名数组，如 ["地区", "部门"]（必须与列头完全匹配，区分大小写）',
      },
      data_fields: {
        type: 'array',
        items: { type: 'string' },
        description: '值字段列名数组，如 ["销售额"]（求和聚合）',
      },
      column_fields: {
        type: 'array',
        items: { type: 'string' },
        description: '列字段列名数组，如 ["季度"]',
      },
    },
    required: ['source_range', 'destination'],
  },
  humanLabel: (args: unknown) => {
    const { source_range, name } = args as { source_range: string; name?: string };
    return `创建数据透视表${name ? `「${name}」` : ''}（源：${source_range}）`;
  },
  async execute(args, ctx): Promise<ToolResult> {
    const { source_range, destination, name, row_fields, data_fields, column_fields } = args as {
      source_range: string;
      destination: string;
      name?: string;
      row_fields?: string[];
      data_fields?: string[];
      column_fields?: string[];
    };
    let pivotTableName: string;
    try {
      const result = await (ctx.adapter as ExcelAdapter).createPivotTable({
        sourceRange: source_range,
        destination,
        name,
        rowFields: row_fields,
        dataFields: data_fields,
        columnFields: column_fields,
      });
      pivotTableName = result.pivotTableName;
    } catch (err) {
      // API 不可用（isSetSupported false）或运行时抛错 → 诚实 noop+gate（不中断 agent）
      const message = err instanceof Error ? err.message : String(err);
      const reverse: ReverseDescriptor = {
        tool: 'noop_inverse',
        args: { reason: message },
      };
      const postState: PostStateSnapshot = {
        kind: 'excel_pivot',
        content: { tooLarge: true },
      };
      return {
        ok: false,
        data: { error: message },
        reverse,
        postState,
      };
    }
    const reverse: ReverseDescriptor = {
      tool: 'delete_pivot_table_by_name',
      args: { pivotTableName },
    };
    const postState: PostStateSnapshot = {
      kind: 'excel_pivot',
      content: { pivotTableName },
    };
    return { ok: true, data: { pivotTableName }, reverse, postState };
  },
};
```

**改 src/agent/tools/index.ts（在 excelWriteTools 数组，batchWrite 前加入 createPivotTableTool）：**
在已有的 `mergeCellsTool, removeDuplicatesTool,` 行之后、`batchWrite,` 之前插入：
```typescript
        createPivotTableTool,
```
并确认 import 已包含（或追加 createPivotTableTool 到 import 行）。

**改 src/agent/contract.test.ts（integrationTest: false → true）：**
将 Phase 28 区块的 create_pivot_table 行：
```typescript
  { toolName: 'create_pivot_table', host: 'excel', undoType: '简单逆向', reverseTool: 'delete_pivot_table_by_name', phase: 28, integrationTest: false },
```
改为：
```typescript
  { toolName: 'create_pivot_table', host: 'excel', undoType: '简单逆向', reverseTool: 'delete_pivot_table_by_name', phase: 28, integrationTest: true },
```

**改 src/agent/operationLog.integration.test.ts（create_pivot_table 正向守门用例期望改为 rolled_back）：**
将 Plan 01 加入的 create_pivot_table 正向守门用例：
```typescript
    // Wave 0：adapter.deletePivotTableByName 未实现 → skipped_error；Wave 2 实现后改为 rolled_back
    expect(detail.status).toBe('skipped_error');
```
改为：
```typescript
    expect(detail.status).toBe('rolled_back');
```
（降级路径守门用例保持 `skipped_error` 不变）

**改 .planning/phases/08-foundation-a-f/CONTRACT.md（create_pivot_table → done）：**
将 Phase 28 段的 create_pivot_table 行改为：
```markdown
| create_pivot_table | excel | 简单逆向 | delete_pivot_table_by_name | true | done |
```

**Phase 28 全套收尾验证（最终 bundle gate）：**
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
npm test              # 全套测试绿
npm run build && npm run size   # bundle ≤100KB gzip
```
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "create_pivot_table.*integrationTest: true" src/agent/contract.test.ts && npm test && npm run build && npm run size
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name: 'create_pivot_table'" src/agent/tools/write/excel.ts` 输出 `1`
    - `grep -c "createPivotTableTool" src/agent/tools/index.ts` 输出 `>= 1`（import + 数组）
    - `grep "create_pivot_table.*integrationTest" src/agent/contract.test.ts` 输出含 `integrationTest: true`
    - `npm test -- --run src/agent/contract.test.ts` 全部通过（D-17 守门：3 个 Phase 28 工具名全在 integration.test.ts）
    - integration.test.ts 中 create_pivot_table 正向守门用例期望 `rolled_back`（adapter.deletePivotTableByName 已实现）
    - integration.test.ts 中 create_pivot_table 降级路径守门用例期望 `skipped_error`（noop_inverse 正确 throw）
    - `npm test` 全套绿（含所有 Phase 28 新增守门用例）
    - `npm run build && npm run size` 输出 initial main bundle ≤100KB gzip（Phase 28 三工具全部就位后最终验收）
    - CONTRACT.md Phase 28 段：3 条工具行全部 integration_test=true, status=done
    - 参数键全 snake_case（source_range / destination / row_fields / data_fields / column_fields）
    - humanLabel 为中文（「创建数据透视表」含源区域）
    - 工具 execute 捕获 HostApiError → ok:false + noop_inverse reverse（诚实 noop+gate，不中断 agent）
  </acceptance_criteria>
  <done>EXCEL-13 create_pivot_table 全链路交付：ToolDef 定义 + adapter 方法（双层门控 + 字段配置）+ excelWriteTools 注册 + integrationTest:true + CONTRACT done + Phase 28 全套收尾（npm test 全绿 + bundle ≤100KB）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_call args → adapter 方法 | source_range/destination/row_fields 传入 Office.js API；字段名大小写敏感，错误时 Office.js 抛 InvalidArgument（已在 description 提示 AI） |
| isSetSupported 门控 → 诚实降级 | 门控失败 → HostApiError → ToolDef catch → noop+gate（ok:false + noop_inverse）；不静默假成功（ROADMAP SC#3） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-1 | Spoofing | isSetSupported('ExcelApi','1.8') 门控 | mitigate | 门控失败抛 HostApiError，ToolDef catch → ok:false + noop_inverse reverse；前端显示明确错误，不静默假成功（ROADMAP SC#3 诚实降级判据） |
| T-28-03-2 | Tampering | pivotTable.name 不稳定（重名自动改名）| mitigate | add 后 load(['name']) + sync1 读取 server 端规范化名称作为 reverse.args.pivotTableName（Pitfall 3 防御） |
| T-28-03-3 | Tampering | deletePivotTableByName 幂等性 | mitigate | getItem(name) 抛 ItemNotFound → catch 静默跳过（pivotTables 无 OrNullObject，用 try/catch 替代 isNullObject 检查） |
| T-28-03-4 | Information Disclosure | pivot 字段配置在 reverse.args（in-memory）| accept | 仅 in-memory 存储（PITFALLS A-11），会话结束清除；无 PII，字段名来自用户数据列头 |
| T-28-03-5 | Denial of Service | 字段名大小写不匹配（hierarchies.getItem 抛 InvalidArgument） | accept | description 明确提示 AI「字段名必须与列头完全匹配，区分大小写」；错误由 HostApiError 包装，返回 ok:false，不中断 agent（诚实失败） |
</threat_model>

<verification>
Phase 28 最终收尾验收命令：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. TypeScript 编译
npx tsc --noEmit
# 2. 合约守门（D-17，3 个 Phase 28 工具 integrationTest:true）
npm test -- --run src/agent/contract.test.ts
# 3. integration 守门（所有 Phase 28 守门用例：merge_cells/remove_duplicates/create_pivot_table 正向 → rolled_back，noop 路径 → skipped_error）
npm test -- --run src/agent/operationLog.integration.test.ts
# 4. 全套测试（Phase 28 收尾 gate）
npm test
# 5. bundle gate（先 build 再 size，禁止测陈旧 dist）
npm run build && npm run size
# 期望 initial main bundle ≤100KB gzip（Phase 28 三工具全部就位）
```
</verification>

<success_criteria>
- EXCEL-13 create_pivot_table 全链路：adapter(createPivotTable+deletePivotTableByName) + ToolDef(snake_case 参数) + index.ts 注册 + integration 守门 rolled_back + integrationTest:true + CONTRACT done
- 降级路径：isSetSupported false 或 add 抛错 → ok:false + noop_inverse reverse（ROADMAP SC#3 诚实降级，不中断 agent）
- pivotTable 名 server 端规范化：load(['name']) + sync 后读取，防重名自动改名导致 delete undo 找不到
- deletePivotTableByName 用 getItem + try/catch（非 OrNullObject，pivotTables 集合差异）
- Phase 28 全套收尾：npm test 全绿 + bundle ≤100KB gzip（npm run build && npm run size 验证，非陈旧 dist）
- CONTRACT.md Phase 28 段：3 条工具行全部 integration_test=true, status=done
</success_criteria>

<output>
完成后创建 `.planning/phases/28-excel-tools/28-03-SUMMARY.md`，记录：
- ExcelAdapter.ts 新增 2 个方法（行号）：createPivotTable / deletePivotTableByName
- tools/write/excel.ts 新增 createPivotTableTool（行号）
- tools/index.ts excelWriteTools 数组变化（行号）
- contract.test.ts create_pivot_table 行改为 integrationTest: true
- integration.test.ts Phase 28 EXCEL-13 守门用例状态（正向 rolled_back，降级 skipped_error）
- bundle 实测数值（npm run size 输出，Phase 28 全三工具就位后最终值）
- CONTRACT.md Phase 28 段最终状态（3 条 done）
- Phase 28 总结：3 个新 Excel 工具（EXCEL-11/12/13），Wave 0+1+2 三波交付，守门四步全通
</output>

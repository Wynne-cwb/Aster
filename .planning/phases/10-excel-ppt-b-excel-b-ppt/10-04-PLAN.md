---
phase: 10-excel-ppt-b-excel-b-ppt
plan: "04"
type: execute
wave: 3
depends_on: ["10-01"]
files_modified:
  - src/adapters/PptAdapter.ts
  - src/agent/tools/write/ppt.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - PPT-01
  - PPT-03
  - PPT-07

must_haves:
  truths:
    - "agent 可在 PPT 当前幻灯片插入文本框写「季度总结」，undo 后文本框消失（add_shape 简单逆向 + addTextBox 绕 #2775，ROADMAP SC#3）"
    - "add_shape 插入后校验 shape count 增加（#2775 防御），count 减少则 throw HostApiError（不静默数据丢失）"
    - "set_shape_text_font 正确读 before-image（font 属性包）并 TEXT_SHAPE_TYPES 守门"
    - "copy_slide 逆向用 index+ID 双定位（D-16），deleteSlideByIndex 优先 capturedId 找 slide"
    - "3 工具完成 D-17 四步守门（contract integrationTest→true + integration.test GREEN + CONTRACT.md done）"
  artifacts:
    - path: "src/adapters/PptAdapter.ts"
      provides: "setShapeTextFont/restoreShapeFont/addShape/deleteShapeById/copySlide/deleteSlideByIndex + 6 个方法"
      contains: "restoreShapeFont"
    - path: "src/agent/tools/write/ppt.ts"
      provides: "PPT-01/03/07 三个 ToolDef"
      contains: "set_shape_text_font"
  key_links:
    - from: "src/adapters/PptAdapter.ts"
      to: "TEXT_SHAPE_TYPES"
      via: "TEXT_SHAPE_TYPES.has(shape.type) fail-closed 守门"
      pattern: "TEXT_SHAPE_TYPES"
    - from: "src/agent/operationLog.ts"
      to: "src/adapters/PptAdapter.ts"
      via: "executeReverse case 'delete_shape_by_id'"
      pattern: "deleteShapeById"
---

<objective>
Wave 3a：实现 PPT 3 个简单逆向工具（PPT-01/03/07）。

Purpose: 提供 set_shape_text_font（字体设置）、add_shape（形状插入含 #2775 防御）、copy_slide（幻灯片复制含双定位逆向），完成 ROADMAP SC#3（add_shape + undo），每工具完成 D-17 四步守门。

Output:
- PptAdapter.ts：3 对 write+inverse 方法（6 个）
- ppt.ts：3 个 ToolDef
- D-17 四步守门：3 工具全部完成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-RESEARCH.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- PptAdapter 关键现有接口 -->

From src/adapters/PptAdapter.ts line 38 (TEXT_SHAPE_TYPES 白名单 — 禁止在新方法内重定义):
```typescript
const TEXT_SHAPE_TYPES = new Set<string>(['GeometricShape', 'TextBox', 'Placeholder', 'Callout']);
```

From src/adapters/PptAdapter.ts lines 670-769 (setShapeProperty 四 sync 范式 — Pattern D analog):
```typescript
return await PowerPoint.run(async (ctx) => {
  // sync 1: load slides
  const slides = ctx.presentation.slides;
  slides.load('items');
  await ctx.sync();
  const idx = slideIndex - 1;
  if (idx < 0 || idx >= slides.items.length) throw new HostApiError(`...`, undefined);

  // sync 2: load shapes (id + type)
  const slide = slides.items[idx];
  slide.shapes.load('items/id,items/type');
  await ctx.sync();

  const shape = (slide.shapes.items as Array<{id: string; type: string}>)
    .find((sh) => sh.id === shapeId);
  if (!shape) throw new HostApiError(`形状 ${shapeId} 不存在`, undefined);
  if (!TEXT_SHAPE_TYPES.has(shape.type)) throw new HostApiError(`不支持文本编辑`, undefined);

  // sync 3: load before-image
  shape.textFrame.textRange.font.load(['bold', 'italic', 'color', 'size', 'name']);
  await ctx.sync();
  const beforeFont = { bold: shape.textFrame.textRange.font.bold, ... };

  // sync 4: write
  if (font.bold !== undefined) shape.textFrame.textRange.font.bold = font.bold;
  await ctx.sync();
  return { beforeFont };
});
```

From src/adapters/PptAdapter.ts lines 791-871 (restoreShapeProperty — inverse Record 签名范式 — Pattern E):
```typescript
async restoreShapeProperty(args: Record<string, unknown>): Promise<void> {
  const slide_index = args.slide_index as number;
  const shape_id = args.shape_id as string;
  // ... 解包 + PowerPoint.run(async (ctx) => { 两 sync 还原 })
}
```

From src/adapters/PptAdapter.ts lines 484-540 (insertSlideAfter — slide 指纹捕获范式):
```typescript
// 写后重 load slides → 找新 slide → load id/index → 捕获 capturedId/capturedIndex
```

From src/adapters/PptAdapter.ts lines 555-625 (deleteSlideByTitle — 指纹定位范式 — Pattern G analog):
```typescript
// 先 index 快路径 → 不匹配降级 title 指纹 → 找不到 → HostApiError
```

From RESEARCH.md §PPT-03 (add_shape #2775 count 校验):
```typescript
// 文本框路径：先记录 countBefore，addTextBox，再 countAfter 校验
slide.shapes.load('items/$none');
await ctx.sync();
const countBefore = slide.shapes.items.length;
const textbox = slide.shapes.addTextBox(text ?? '', { left, top, width, height });
textbox.load(['id']);
await ctx.sync();
slide.shapes.load('items/$none');
await ctx.sync();
const countAfter = slide.shapes.items.length;
if (countAfter < countBefore) throw new HostApiError('PPT addTextBox: 插入后 shape 数量减少，可能触发 #2775 bug', undefined);
const newShapeId = textbox.id as string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PptAdapter.ts — 3 对 write+inverse 方法（setShapeTextFont/restoreShapeFont/addShape/deleteShapeById/copySlide/deleteSlideByIndex）</name>
  <files>src/adapters/PptAdapter.ts</files>
  <read_first>
    - src/adapters/PptAdapter.ts（完整文件，重点：TEXT_SHAPE_TYPES line 38、setShapeProperty lines 670-769、restoreShapeProperty lines 791-871、insertSlideAfter lines 484-540、deleteSlideByTitle lines 555-625；addTextBox 用法 line 515 附近）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-RESEARCH.md §PPT-01（font 属性包）、§PPT-03（addGeometricShape + addTextBox #2775）、§PPT-07（copy_slide + 双定位指纹）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-PATTERNS.md §Pattern D（四 sync）§Pattern E（inverse Record）§Pattern F（add_shape）§Pattern G（copy_slide 双定位）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md §G-F D-15/D-16（add_shape deselect + copy_slide 双定位）
  </read_first>
  <behavior>
    - setShapeTextFont(slideIndex, shapeId, font) → 四 sync + TEXT_SHAPE_TYPES 守门 → load before-image(bold/italic/underline/color/size/name) → 写 font → 返回 beforeFont
    - restoreShapeFont(args: Record) → 解包 slide_index/shape_id/before_font → 两 sync 还原 font 属性
    - addShape(slideIndex, shapeType, position, text?) → shapeType='TextBox' → addTextBox + count 校验（#2775）；其他 → addGeometricShape → 写文字（TEXT_SHAPE_TYPES 守门）→ 返回 newShapeId
    - deleteShapeById(args: Record) → 解包 slide_index/shape_id → find(sh.id===shape_id) → shape.delete()；找不到 → throw HostApiError
    - copySlide(sourceIndex, targetIndex?) → slide.copy() → reload slides → 找新 slide（targetIndex 位置或末尾）→ load id+index → 返回 { capturedId, capturedIndex }
    - deleteSlideByIndex(args: Record) → 解包 capturedIndex/capturedId → 优先 capturedId 遍历定位；找不到 → 按 capturedIndex + title 后备（deleteSlideByTitle 同范式）；都找不到 → throw HostApiError
  </behavior>
  <action>
在 PptAdapter.ts 末尾追加 6 个方法：

**setShapeTextFont（PPT-01，四 sync + TEXT_SHAPE_TYPES）：**
- 完全复用 setShapeProperty 四 sync 范式，只是 sync 3 换成 load font 属性：`shape.textFrame.textRange.font.load(['bold', 'italic', 'underline', 'color', 'size', 'name'])`
- before-image 字段：`{ bold, italic, underline, color, size, name }`（全部保留，允许 null）
- sync 4 写：逐个判断非 undefined 才写（`if (font.bold !== undefined) shape.textFrame.textRange.font.bold = font.bold`）
- 返回 `{ beforeFont: { bold?, italic?, underline?, color?, size?, name? } }`

**restoreShapeFont（PPT-01 inverse，Pattern E）：**
- `(args: Record<string, unknown>)` — 解包 slide_index/shape_id/before_font
- 两 sync：sync 1 load slides → sync 2 找 shape → 写 font（逐个 before_font 字段判断非 undefined）
- 无 TEXT_SHAPE_TYPES 守门（inverse 路径直接还原，之前写入时已保证 shape 可用）

**addShape（PPT-03，几何 + 文本框双路径）：**

几何形状路径（shapeType !== 'TextBox'）：
- sync 1 load slides → sync 2 load shapes → `slide.shapes.addGeometricShape(shapeType, {left,top,width,height})` → `shape.load(['id']); await ctx.sync()` → `newShapeId = shape.id`
- 文字（如有）：TEXT_SHAPE_TYPES 守门后写 `shape.textFrame.textRange.text = text`; sync

文本框路径（shapeType === 'TextBox'）：
- sync 1 load slides → sync 2 `slide.shapes.load('items/$none')` → `countBefore = slide.shapes.items.length`
- `textbox = slide.shapes.addTextBox(text ?? '', {left,top,width,height})` → `textbox.load(['id']); await ctx.sync()`
- `slide.shapes.load('items/$none'); await ctx.sync()` → `countAfter`
- `if (countAfter < countBefore) throw new HostApiError('PPT addTextBox: 插入后 shape 数量减少，可能触发 #2775 bug', undefined)` — **明确失败，不静默数据丢失**
- `newShapeId = textbox.id`

返回 `{ newShapeId: string }`

**deleteShapeById（PPT-03 inverse）：**
- `(args: Record<string, unknown>)` — 解包 slide_index/shape_id
- sync 1 load slides → sync 2 `slide.shapes.load('items/id')` → `shape = find(sh.id === shape_id)` → `shape.delete(); await ctx.sync()`
- 找不到 → throw `new HostApiError(`PPT deleteShapeById: 形状 ${shape_id} 不存在`, undefined)`（诚实告知）

**copySlide（PPT-07，slide.copy() + 双定位指纹捕获）：**
- sync 1 load slides → 按 sourceIndex 找 sourceSlide
- `sourceSlide.copy()` → `slides.load('items'); await ctx.sync()` — 重新 load 找新 slide
- 新 slide 位置：`copy()` 可能追加末尾，`sorted = [...slides.items].sort((a,b) => a.index - b.index)`，取 targetIndex 位置（若无 targetIndex 取末尾 sorted[sorted.length-1]）
- `newSlide.load(['id', 'index']); await ctx.sync()`
- 返回 `{ capturedId: newSlide.id, capturedIndex: newSlide.index }`

**deleteSlideByIndex（PPT-07 inverse，D-16 双定位）：**
- `(args: Record<string, unknown>)` — 解包 capturedIndex/capturedId
- sync 1 load slides → sync 2 `slides.load('items/id,items/index')` → 优先按 capturedId 遍历（`slides.items.find(s => s.id === capturedId)`）
- 找到 → `slide.delete(); await ctx.sync()`
- 找不到 → 按 capturedIndex 取 slides.items[capturedIndex] → 找到 → delete
- 都找不到 → throw `new HostApiError('PPT deleteSlideByIndex: 目标幻灯片已不存在', undefined)`（replay engine 捕获 → skipped_error，诚实告知）
  </action>
  <verify>
    <automated>grep -c "async setShapeTextFont\|async restoreShapeFont\|async addShape\|async deleteShapeById\|async copySlide\|async deleteSlideByIndex" /Users/wb.chen/Documents/Project/Aster/src/adapters/PptAdapter.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep 6 个方法名，输出 6
    - grep -c "(args: Record<string, unknown>)" src/adapters/PptAdapter.ts 输出 ≥ 3（restoreShapeFont/deleteShapeById/deleteSlideByIndex 三个 inverse 方法，已有的更多）
    - grep -c "#2775\|countBefore\|countAfter" src/adapters/PptAdapter.ts 输出 ≥ 3（addTextBox #2775 防御）
    - grep -c "capturedId" src/adapters/PptAdapter.ts 输出 ≥ 2（copySlide 捕获 + deleteSlideByIndex 消费）
    - npm run test 全绿（TypeScript 类型检查通过）
  </acceptance_criteria>
  <done>PptAdapter.ts 含 6 个新方法，addShape 两路径（几何/文本框）+ #2775 count 校验，copy_slide 双定位指纹，所有 inverse 签名 Record</done>
</task>

<task type="auto">
  <name>Task 2: ppt.ts ToolDef + 注册 + D-17 四步守门（PPT-01/03/07）</name>
  <files>src/agent/tools/write/ppt.ts, src/agent/tools/index.ts, src/agent/contract.test.ts, src/agent/operationLog.integration.test.ts, .planning/phases/08-foundation-a-f/CONTRACT.md</files>
  <read_first>
    - src/agent/tools/write/ppt.ts（完整文件，重点：setShapeProperty ToolDef lines 114-182 + setShapeText ToolDef lines 240-276 — PPT ToolDef 范式）
    - src/agent/tools/index.ts（lines 214-220 pptWriteTools 数组）
    - src/agent/contract.test.ts lines 52-54（PPT-01/03/07 对应行）
    - src/agent/operationLog.integration.test.ts（Wave 0 已预埋的 set_shape_text_font/add_shape/copy_slide 骨架）
  </read_first>
  <action>
**Step A：3 个 ToolDef（set_shape_text_font/add_shape/copy_slide）。**

**set_shape_text_font（PPT-01）：**
```typescript
export const setShapeTextFontTool: ToolDef = {
  name: 'set_shape_text_font',
  kind: 'write',
  description: '设置形状文字字体（字号/加粗/斜体/颜色/字体名）',
  parameters: {
    type: 'object',
    properties: {
      slideIndex: { type: 'number', description: '幻灯片编号（从 1 起）' },
      shapeId: { type: 'string', description: '形状 ID' },
      font: {
        type: 'object',
        properties: {
          size: { type: 'number' }, bold: { type: 'boolean' },
          italic: { type: 'boolean' }, color: { type: 'string' }, name: { type: 'string' },
        },
      },
    },
    required: ['slideIndex', 'shapeId', 'font'],
  },
  humanLabel: ({ slideIndex, shapeId }: { slideIndex: number; shapeId: string }) =>
    `修改第 ${slideIndex} 页形状「${shapeId}」文字字体`,
  async execute(args, ctx): Promise<ToolResult> {
    const { slideIndex, shapeId, font } = args as { slideIndex: number; shapeId: string; font: Record<string, unknown> };
    const { beforeFont } = await (ctx.adapter as PptAdapter).setShapeTextFont(slideIndex, shapeId, font);
    const reverse: ReverseDescriptor = {
      tool: 'restore_shape_font',
      args: { slide_index: slideIndex, shape_id: shapeId, before_font: beforeFont },
    };
    const postState: PostStateSnapshot = { kind: 'ppt_shape_font', content: { slideIndex, shapeId } };
    return { ok: true, data: { slideIndex, shapeId }, reverse, postState };
  },
};
```

**add_shape（PPT-03）：**
- parameters 含 `shapeType: { type: 'string', enum: ['Rectangle', 'Ellipse', 'Triangle', 'RightTriangle', 'TextBox', ...] }`（包含常用 GeometricShapeType + TextBox）
- execute：`adapter.addShape(...)` → `{ newShapeId }` → `reverse = { tool: 'delete_shape_by_id', args: { slide_index: slideIndex, shape_id: newShapeId } }`
- description 注明「TextBox 类型会检测 Office.js #2775 bug，若检测到 shape 被删除则操作失败」

**copy_slide（PPT-07）：**
- parameters: `{ sourceIndex: number, targetIndex?: number }`
- execute：`adapter.copySlide(sourceIndex, targetIndex)` → `{ capturedId, capturedIndex }` → `reverse = { tool: 'delete_slide_by_index', args: { capturedIndex, capturedId } }`
- description 注明「复制后新幻灯片将追加到演示文稿末尾或指定位置」

**Step B：index.ts 追加 3 个工具。**

```typescript
const pptWriteTools = [
  insertSlide, setShapeProperty, moveShape, setShapeText,
  // Phase 10 Wave 3：
  setShapeTextFontTool, addShapeTool, copySlideTool,
] as ToolDef[];
```

**Step C：D-17 四步守门（PPT-01/03/07）。**

1. contract.test.ts lines 52/54/59：PPT-01/03/07 三行 integrationTest: false → true
2. operationLog.integration.test.ts：Wave 0 骨架的 set_shape_text_font/add_shape/copy_slide 三条守门（真 PptAdapter 实例，D-19）——Task 1 实现对应 adapter 方法后，真 PptAdapter 实例上的方法存在 → executeReverse 执行成功 → rolled_back（GREEN，无需修改测试体）
3. CONTRACT.md：PPT-01/03/07 三行 status→done + integration_test→true
4. 无 noop+gate（三工具全部简单逆向），第 4 步 N/A
  </action>
  <verify>
    <automated>npm test -- src/agent/contract.test.ts src/agent/operationLog.integration.test.ts --reporter=verbose 2>&1 | grep -E "set_shape_text_font|add_shape|copy_slide" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "integrationTest: true" src/agent/contract.test.ts 输出 13（Wave 1/2 的 10 + Wave 3 的 3）
    - npm test -- src/agent/operationLog.integration.test.ts 中 set_shape_text_font/add_shape/copy_slide 三条 PASS（GREEN）
    - npm test -- src/agent/contract.test.ts 全绿（D-17 fs.readFileSync 硬卡：13 个 toolName 在 integration.test.ts 中存在）
    - grep -c "status: done" .planning/phases/08-foundation-a-f/CONTRACT.md 输出 ≥ 13
    - npm run build 通过，bundle ≤82 KB
  </acceptance_criteria>
  <done>PPT-01/03/07 三工具完整上线，D-17 守门全部通过，add_shape #2775 防御在 ToolDef description 标注，copy_slide 双定位逆向正确</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| addShape → slide.shapes.addTextBox | addTextBox 触发 #2775 可能静默删除选中形状（已知 Office.js bug）|
| deleteSlideByIndex → capturedId | 逆向定位依赖 capturedId；ID 不匹配时后备 index 可能漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | addTextBox #2775（静默删除选中形状）| mitigate | count before/after 校验；`countAfter < countBefore` → throw HostApiError（明确失败，不静默接受数据丢失）；S7 spike 真机验证最终行为（ROADMAP SC#5 UAT 项）|
| T-10-12 | Tampering | deleteSlideByIndex capturedId 漂移 | mitigate | D-16 双定位：优先 capturedId（UUID，不随位置变化）；capturedId 找不到才降级 capturedIndex；都找不到 → throw → skipped_error（诚实告知，不误删）|
| T-10-13 | Denial of Service | addGeometricShape 无效 shapeType | mitigate | ToolDef parameters 使用 enum 限定合法 shapeType 值；adapter 内部 try/catch → HostApiError 包装 |
</threat_model>

<verification>
```bash
# 1. adapter 方法存在
grep -c "async setShapeTextFont\|async restoreShapeFont\|async addShape\|async deleteShapeById\|async copySlide\|async deleteSlideByIndex" /Users/wb.chen/Documents/Project/Aster/src/adapters/PptAdapter.ts
# 期望 6

# 2. #2775 防御
grep -c "countBefore\|countAfter\|#2775" /Users/wb.chen/Documents/Project/Aster/src/adapters/PptAdapter.ts
# 期望 ≥ 3

# 3. 双定位
grep -c "capturedId" /Users/wb.chen/Documents/Project/Aster/src/adapters/PptAdapter.ts
# 期望 ≥ 2

# 4. D-17 守门
npm test -- src/agent/contract.test.ts
npm test -- src/agent/operationLog.integration.test.ts

# 5. bundle
npm run build && npm run size
```
</verification>

<success_criteria>
- PptAdapter.ts 含 6 个新方法，addShape #2775 count 校验，deleteSlideByIndex 双定位
- 所有 inverse 方法签名 (args: Record<string, unknown>)（D-18）
- contract.test.ts 13 行 integrationTest→true，D-17 守门全绿
- CONTRACT.md PPT-01/03/07 三行 status→done
- bundle ≤82 KB
</success_criteria>

<output>
完成后创建 `.planning/phases/10-excel-ppt-b-excel-b-ppt/10-04-SUMMARY.md`
</output>

---
phase: 10-excel-ppt-b-excel-b-ppt
plan: "03"
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/adapters/ExcelAdapter.ts
  - src/agent/tools/write/excel.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - EXCEL-03
  - EXCEL-05
  - EXCEL-09
  - EXCEL-10

must_haves:
  truths:
    - "agent 按 B 列降序排序 500 行表格后，undo 后行顺序完整还原（sort_range 快照 undo 生效，ROADMAP SC#2）"
    - "excel_find_and_replace 和 sort_range 共享 restore_range_values_snapshot，但各有独立 integration.test 守门用例（D-20）"
    - "manage_worksheet operation 枚举限定 add|rename（绝不含 delete/copy，D-03）"
    - "快照超过 10,000 单元格时降级 noop+gate（仍执行操作，warn「区域过大」，不中断 agent）"
    - "set_chart_title write+inverse 完整（复用 insertChart/deleteChartByName 定位范式）"
  artifacts:
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "sortRange/restoreRangeValuesSnapshot/excelFindAndReplace/manageWorksheet/restoreWorksheetSnapshot/setChartTitle/restoreChartTitle + readRangeValuesSnapshot"
      contains: "restoreRangeValuesSnapshot"
    - path: "src/agent/tools/write/excel.ts"
      provides: "EXCEL-03/05/09/10 四个 ToolDef"
      contains: "sort_range"
  key_links:
    - from: "src/agent/operationLog.ts"
      to: "src/adapters/ExcelAdapter.ts"
      via: "executeReverse case 'restore_range_values_snapshot'"
      pattern: "restoreRangeValuesSnapshot"
    - from: "src/agent/tools/write/excel.ts"
      to: "src/adapters/ExcelAdapter.ts"
      via: "ctx.adapter as ExcelAdapter"
      pattern: "readRangeValuesSnapshot|sortRange|excelFindAndReplace"
---

<objective>
Wave 2：实现 Excel 快照式 + manage_worksheet + set_chart_title（EXCEL-03/05/09/10）。

Purpose: 完成 Excel 10 工具的后 4 个，包含 ROADMAP SC#2（sort_range 快照 undo）、D-20 双守门（sort_range + excel_find_and_replace 各独立守门）、D-03/04 manage_worksheet 操作裁剪（add|rename only）、D-07 快照上限 10,000 单元格。

Output:
- ExcelAdapter.ts：readRangeValuesSnapshot + 4 对 write/inverse 方法
- excel.ts：4 个 ToolDef
- D-17 四步守门：4 工具全部完成（含 D-20 双守门）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-RESEARCH.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 现有代码关键接口 -->

ExcelAdapter.ts 现有 CELL_LIMIT 常量（line 166）:
```typescript
private static readonly CELL_LIMIT = 10_000; // 与 SNAPSHOT_LIMIT 对齐（D-07）
```

从 RESEARCH.md §Pattern 2（快照式 undo 范式）：
```typescript
// 快照写方法
async readRangeValuesSnapshot(address: string): Promise<{ address: string; snapshot: unknown[][] }> {
  return await Excel.run(async (ctx) => {
    const range = ctx.workbook.worksheets.getActiveWorksheet().getRange(address);
    range.load(['values', 'address', 'cellCount']);
    await ctx.sync();
    if ((range.cellCount as number) > SNAPSHOT_LIMIT) {
      throw new SnapshotTooLargeError(range.cellCount as number);
    }
    return { address: range.address as string, snapshot: range.values as unknown[][] };
  });
}
// 快照 inverse（复用 overwriteRange 逻辑）
async restoreRangeValuesSnapshot(args: Record<string, unknown>): Promise<void> {
  const address = args.address as string;
  const snapshot = args.snapshot as unknown[][];
  // range.values = snapshot; await ctx.sync()
}
```

从 RESEARCH.md §EXCEL-03（sort_range sort.apply 参数）：
```typescript
// sortFields: Array<{ key: number; ascending: boolean }>
// key 是相对 address 左侧的列偏移（0-based）
range.sort.apply(sortFields);
```

从 RESEARCH.md §EXCEL-05（replaceAll 注意）：
```typescript
// ExcelApi 1.9 replaceAll，需 isSetSupported('ExcelApi', '1.9') 门控
// 不支持时降级 body.search 遍历替换
const result = targetRange.replaceAll(searchText, replaceText, { completeMatch, matchCase });
// result.count 需再次 load+sync
```

从 RESEARCH.md §EXCEL-09（manage_worksheet add/rename）：
```typescript
// add: worksheets.add(sheetName) → load name → resolvedName
// rename: getItem(oldName) → sheet.name = newName
// 快照 = 元数据（操作 + 相关名称），非内容
```

从 RESEARCH.md §EXCEL-10（set_chart_title 三 sync 定位）：
```typescript
const chart = sheet.charts.getItemOrNullObject(chartName);
chart.load('isNullObject'); await ctx.sync(); // sync 1
if (chart.isNullObject) throw new HostApiError('图表不存在', undefined);
chart.title.load('text'); await ctx.sync(); // sync 2: before-image
const beforeTitle = chart.title.text as string;
chart.title.text = title;
await ctx.sync(); // sync 3: write
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExcelAdapter.ts — 快照式 4 工具 + set_chart_title（readRangeValuesSnapshot + 4 对方法）</name>
  <files>src/adapters/ExcelAdapter.ts</files>
  <read_first>
    - src/adapters/ExcelAdapter.ts（Wave 2 之前的状态，重点：overwriteRange lines 362-375、insertChart lines 399-421、deleteChartByName lines 435-454；现有 CELL_LIMIT 常量 line 166）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-RESEARCH.md §EXCEL-03、§EXCEL-05、§EXCEL-09、§EXCEL-10
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-PATTERNS.md §Pattern C（快照式范式）§Pattern B（getItemOrNullObject 防御）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md §G-C D-06/D-07/D-08（快照策略细节）§G-B D-03/D-04（manage_worksheet 范围裁决）
  </read_first>
  <behavior>
    - readRangeValuesSnapshot(address) → load values+address+cellCount → 超 10,000 抛 SnapshotTooLargeError → 返回 { address, snapshot }
    - restoreRangeValuesSnapshot(args: Record) → address + snapshot → range.values = snapshot
    - sortRange(address, sortFields) → try readRangeValuesSnapshot → sort.apply → 返回 { snapshot, snapshotAddress, tooLarge }；超限时 tooLarge=true
    - excelFindAndReplace(searchText, replaceText, address?, matchCase?, matchWholeWord?) → readRangeValuesSnapshot(usedRange or address) → replaceAll（isSetSupported 门控）→ 返回 { snapshot, snapshotAddress, tooLarge, count }
    - manageWorksheet(operation: 'add'|'rename', sheetName, newName?) → add: worksheets.add → load resolvedName → 返回 snapshot；rename: getItem(sheetName) → load oldName → sheet.name=newName → 返回 snapshot
    - restoreWorksheetSnapshot(args: Record) → operation='add' → getItemOrNullObject(sheetName) + delete；operation='rename' → getItem(newName).name = oldName
    - setChartTitle(chartName, title) → 三 sync 定位+before-image+write → 返回 { beforeTitle }
    - restoreChartTitle(args: Record) → getItemOrNullObject(chartName) + chart.title.text = beforeTitle
  </behavior>
  <action>
在 ExcelAdapter.ts 末尾（Wave 1 新增方法之后）追加以下方法：

**readRangeValuesSnapshot（供 sortRange/excelFindAndReplace 内部使用）：**
- 参数 `address: string`，非 Record（这是 write 准备方法，不是 inverse）
- `SNAPSHOT_LIMIT = 10_000`（与现有 CELL_LIMIT 对齐）
- 超限：抛 SnapshotTooLargeError（需要新建该 Error 类，或 throw new Error 携带 cellCount 信息）

**restoreRangeValuesSnapshot（EXCEL-03/05 共享 inverse，D-20）：**
- `(args: Record<string, unknown>)` — 解包 address + snapshot
- 完全复用 overwriteRange 逻辑：`range.values = snapshot; await ctx.sync()`

**sortRange（EXCEL-03）：**
- 先 try `readRangeValuesSnapshot(address)` → 超限 catch → tooLarge=true（仍执行 sort，仅标记）
- `range.sort.apply(sortFields)` — sortFields: `Array<{ key: number; ascending: boolean }>`
- 工具 description 注明「此操作会清空 Excel 自带撤销历史（官方限制）」
- 返回 `{ snapshot: unknown[][] | null, snapshotAddress: string, tooLarge: boolean }`

**excelFindAndReplace（EXCEL-05）：**
- `address` 参数：有 address → `sheet.getRange(address)`；无 → `sheet.getUsedRange(false)`
- `isSetSupported('ExcelApi', '1.9')` 门控 replaceAll；不支持时降级 body.search 遍历（见 RESEARCH.md A1）
- 超限处理同 sortRange（tooLarge=true，仍执行替换）
- 返回 `{ snapshot: unknown[][] | null, snapshotAddress: string, tooLarge: boolean, count: number }`

**manageWorksheet（EXCEL-09）：**
- operation 枚举 `'add' | 'rename'`（TypeScript 类型约束，绝不接受 'delete'/'copy'，D-03）
- add：`worksheets.add(sheetName)` → `newSheet.load(['name']); await ctx.sync()` → `resolvedName = newSheet.name`
- rename：`getItem(sheetName).load(['name']); sync` → `sheet.name = newName; sync`
- 返回快照：add 返回 `{ operation: 'add', sheetName: resolvedName }`；rename 返回 `{ operation: 'rename', oldName: sheetName, newName }`

**restoreWorksheetSnapshot（EXCEL-09 inverse）：**
- `(args: Record<string, unknown>)` — 解包 operation/sheetName/oldName/newName
- operation='add'：`getItemOrNullObject(sheetName) → isNullObject check → sheet.delete(); sync`（delete 会清空原生撤销栈，但 Aster 自建 undo 不依赖）
- operation='rename'：`getItem(newName).name = oldName; sync`（注：找 newName，改回 oldName）

**setChartTitle（EXCEL-10）：**
- 三 sync：sync 1 `chart.load('isNullObject')` + `getItemOrNullObject`；sync 2 `chart.title.load('text')` 读 before-image；sync 3 write `chart.title.text = title`
- 复用 deleteChartByName 的 getItemOrNullObject + HostApiError 防御范式

**restoreChartTitle（EXCEL-10 inverse）：**
- `(args: Record<string, unknown>)` — 解包 chartName + beforeTitle
- `getItemOrNullObject(chartName)` + `chart.title.text = beforeTitle`
  </action>
  <verify>
    <automated>grep -c "async sortRange\|async excelFindAndReplace\|async manageWorksheet\|async restoreWorksheetSnapshot\|async setChartTitle\|async restoreChartTitle\|async readRangeValuesSnapshot\|async restoreRangeValuesSnapshot" /Users/wb.chen/Documents/Project/Aster/src/adapters/ExcelAdapter.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep 上述 8 个方法名，输出 8（每个 async 方法各 1 次）
    - grep -c "tooLarge" src/adapters/ExcelAdapter.ts 输出 ≥ 2（sortRange + excelFindAndReplace 超限判断）
    - grep -c "operation: 'add' | 'rename'" src/adapters/ExcelAdapter.ts 或 TypeScript 类型签名含 add|rename（D-03 裁决）
    - grep -c "(args: Record<string, unknown>)" src/adapters/ExcelAdapter.ts 输出 ≥ 10（Wave 1 + Wave 2 全部 inverse 方法）
    - npm run test 全绿（Wave 1 守门 + Wave 2 骨架 GREEN）
  </acceptance_criteria>
  <done>ExcelAdapter.ts 含快照式 4 工具的全部方法，sort_range/excel_find_and_replace 共享 restoreRangeValuesSnapshot，manage_worksheet 限定 add|rename，超限 tooLarge 标记正确</done>
</task>

<task type="auto">
  <name>Task 2: excel.ts ToolDef + 注册 + D-17 四步守门（EXCEL-03/05/09/10，含 D-20 双守门）</name>
  <files>src/agent/tools/write/excel.ts, src/agent/tools/index.ts, src/agent/contract.test.ts, src/agent/operationLog.integration.test.ts, .planning/phases/08-foundation-a-f/CONTRACT.md</files>
  <read_first>
    - src/agent/tools/write/excel.ts（Wave 2 之前状态，了解现有 ToolDef 结构）
    - src/agent/operationLog.integration.test.ts（Wave 0 已预埋的 sort_range/excel_find_and_replace/manage_worksheet/set_chart_title 骨架）
    - src/agent/contract.test.ts lines 43-50（EXCEL-03/05/09/10 对应行）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md §D-07（超限 noop+gate）§D-20（D-20 双守门要求）
  </read_first>
  <action>
**Step A：4 个 ToolDef（sort_range/excel_find_and_replace/manage_worksheet/set_chart_title）。**

**sort_range（EXCEL-03）：**
- description 注明「此操作会清空 Excel 自带撤销历史（API 限制）」（D-08）
- execute：调用 `adapter.sortRange(address, sortFields)` → `tooLarge` 判断 → 超限时 `reverse = { tool: 'noop_inverse', args: { reason: '区域过大（超过 10,000 单元格），无法自动撤销' } }`；正常时 `reverse = { tool: 'restore_range_values_snapshot', args: { address: snapshotAddress, snapshot } }`

**excel_find_and_replace（EXCEL-05，D-20 共享 reverse）：**
- 超限逻辑同 sort_range（降级 noop+gate）
- reverse.tool = 'restore_range_values_snapshot'（与 sort_range 共享，但 toolName 字段不同）

**manage_worksheet（EXCEL-09）：**
- parameters: `{ operation: { type: 'string', enum: ['add', 'rename'] }, sheetName: string, newName?: string }` — enum 硬限制（D-03，绝不含 'delete'/'copy'）
- description 标注「仅支持 add/rename；delete 为不可逆操作，不在本工具范围」
- execute：reverse = { tool: 'restore_worksheet_snapshot', args: { ...snapshot } }

**set_chart_title（EXCEL-10）：**
- execute：reverse = { tool: 'restore_chart_title', args: { chartName, beforeTitle } }

**Step B：index.ts 追加 4 个工具。**

```typescript
const excelWriteTools = [
  setRangeValuesTool, applyFormula, insertChart, setCell,
  formatExcelRangeTool, setColumnRowSizeTool, setAutoFilterTool,
  addConditionalFormatTool, createTableTool, freezePanesTool,
  // Phase 10 Wave 2：
  sortRangeTool, excelFindAndReplaceTool, manageWorksheetTool, setChartTitleTool,
] as ToolDef[];
```

**Step C：D-17 四步守门（4 工具）。**

1. contract.test.ts：将 EXCEL-03/05/09/10 对应 4 行 `integrationTest: false → true`
2. operationLog.integration.test.ts：Wave 0 骨架的 sort_range/excel_find_and_replace/manage_worksheet/set_chart_title 四条守门（真 ExcelAdapter 实例，D-19）——Task 1 实现对应 adapter 方法后，真 ExcelAdapter 实例上的方法存在 → executeReverse 执行成功 → rolled_back（GREEN，无需修改测试体）
3. CONTRACT.md：4 行 status→done + integration_test→true
4. **D-20 特别验证**：contract.test.ts D-17 硬卡 `fs.readFileSync` 扫描时，'sort_range' 和 'excel_find_and_replace' 两个 toolName 字符串都出现在 integration.test.ts，两条守门各自通过
  </action>
  <verify>
    <automated>npm test -- src/agent/contract.test.ts src/agent/operationLog.integration.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "integrationTest: true" src/agent/contract.test.ts 输出 10（Wave 1 的 6 + Wave 2 的 4）
    - grep -c "toolName: 'sort_range'" src/agent/operationLog.integration.test.ts 输出 1（D-20）
    - grep -c "toolName: 'excel_find_and_replace'" src/agent/operationLog.integration.test.ts 输出 1（D-20 独立用例）
    - npm test -- src/agent/contract.test.ts 全绿（D-17 fs.readFileSync 硬卡：10 个 toolName 在 integration.test.ts 中存在）
    - npm test -- src/agent/operationLog.integration.test.ts 中 sort_range/excel_find_and_replace/manage_worksheet/set_chart_title 四条 PASS（GREEN）
    - grep "enum.*add.*rename\|add.*rename" src/agent/tools/write/excel.ts（manage_worksheet schema enum 含 add/rename）
    - npm run build 通过，bundle ≤82 KB
  </acceptance_criteria>
  <done>Excel 10 工具全部完整上线，D-20 双守门通过，manage_worksheet 仅 add/rename，contract.test.ts 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sort_range 快照 → Excel.run | 快照上限防止 5MB API 超限（Office for Web Pitfall E4）|
| manage_worksheet operation → Excel API | 枚举硬限 add/rename 防 LLM 传 delete 导致整表永久删除（D-03）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Denial of Service | sort_range/excel_find_and_replace 超大区域快照 | mitigate | SNAPSHOT_LIMIT=10,000 单元格前置检查；超限直接 noop+gate（仍执行操作，但标注不可撤销），不等 API 抛 5MB GeneralException |
| T-10-09 | Elevation of Privilege | manage_worksheet delete operation（Out of Scope）| mitigate | schema enum 硬限 ['add', 'rename']；execute 开头再加 `if (operation !== 'add' && operation !== 'rename') throw` 运行时守门（双保险，D-03）|
| T-10-10 | Tampering | sort_range 清空原生撤销栈 | accept | 官方文档明确记录此限制（sort.apply 行为）；Aster 自建 undo 不依赖原生栈；description + humanLabel 告知用户；accept，已在 D-08 记录 |
</threat_model>

<verification>
```bash
# 1. adapter 方法完整
grep -c "async sortRange\|async excelFindAndReplace\|async manageWorksheet\|async setChartTitle\|async readRangeValuesSnapshot\|async restoreRangeValuesSnapshot\|async restoreWorksheetSnapshot\|async restoreChartTitle" /Users/wb.chen/Documents/Project/Aster/src/adapters/ExcelAdapter.ts
# 期望 8

# 2. manage_worksheet enum 守门
grep "enum\|add.*rename\|rename.*add" /Users/wb.chen/Documents/Project/Aster/src/agent/tools/write/excel.ts | grep -i "manage_worksheet\|operation" | head -5

# 3. D-20 双守门
grep -c "toolName: 'sort_range'" /Users/wb.chen/Documents/Project/Aster/src/agent/operationLog.integration.test.ts
grep -c "toolName: 'excel_find_and_replace'" /Users/wb.chen/Documents/Project/Aster/src/agent/operationLog.integration.test.ts
# 两项各期望 1

# 4. contract 全绿
npm test -- src/agent/contract.test.ts

# 5. 全部 integration 守门绿
npm test -- src/agent/operationLog.integration.test.ts

# 6. bundle
npm run build && npm run size
```
</verification>

<success_criteria>
- Excel 10 工具全部实现（EXCEL-01..10），contract.test.ts 10 行 integrationTest→true
- sort_range + excel_find_and_replace 各有独立守门用例（D-20），两条 GREEN
- manage_worksheet operation enum 硬限 ['add', 'rename']（D-03）
- 超限 noop+gate 路径存在（tooLarge → reverse = noop_inverse）
- CONTRACT.md Excel 10 行全部 status→done，integration_test→true
- npm run build 通过，bundle ≤82 KB
</success_criteria>

<output>
完成后创建 `.planning/phases/10-excel-ppt-b-excel-b-ppt/10-03-SUMMARY.md`
</output>

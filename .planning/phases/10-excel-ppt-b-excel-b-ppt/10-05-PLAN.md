---
phase: 10-excel-ppt-b-excel-b-ppt
plan: "05"
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - src/adapters/PptAdapter.ts
  - src/agent/tools/write/ppt.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - PPT-02
  - PPT-04
  - PPT-05
  - PPT-06
  - PPT-08

must_haves:
  truths:
    - "agent 调用 delete_shape 或 delete_slide 时，DiffLogPanel 显示「此操作不可自动撤销」警告但 agent 流程不中断（ROADMAP SC#4，noop+gate 行为正确）"
    - "Spikes S1/S2/S4 结论已记录：rotate_shape/set_slide_background/set_shape_text_alignment 各自采用运行时降级 noop+gate；happy-path 路径 integration.test 验 rolled_back（ROADMAP SC#5）"
    - "所有 spike 工具实现了运行时 try/catch 读 before-image + 降级 noop+gate；降级信号通过 beforeXxx === null 传给 ToolDef"
    - "5 工具完成 D-17 四步守门（2 noop+gate 验 skipped_error，3 spike 验 happy-path rolled_back）"
    - "manage_slides v2.1 限 operation='delete'（D-14），schema enum 硬限"
  artifacts:
    - path: "src/adapters/PptAdapter.ts"
      provides: "setShapeTextAlignment/restoreShapeAlignment/rotateShape/restoreShapeRotation/deleteShape/setSlideBackground/restoreSlideBackground/manageSlides 8 个方法"
      contains: "restoreShapeAlignment"
    - path: "src/agent/tools/write/ppt.ts"
      provides: "PPT-02/04/05/06/08 五个 ToolDef"
      contains: "set_shape_text_alignment"
  key_links:
    - from: "src/adapters/PptAdapter.ts"
      to: "ToolDef.execute"
      via: "beforeAlignment === null → noop_inverse；非 null → restore_shape_alignment（spike 降级逻辑）"
      pattern: "beforeAlignment.*null\|noop_inverse"
    - from: "src/agent/operationLog.ts"
      to: "src/adapters/PptAdapter.ts"
      via: "executeReverse case 'restore_shape_alignment'"
      pattern: "restoreShapeAlignment"
---

<objective>
Wave 4：实现 PPT 5 个剩余工具（PPT-02/04/05/06/08）— spike 门控 + noop+gate。

Purpose: 完成全部 18 工具。PPT-04（delete_shape）+ PPT-06（manage_slides）是 noop+gate（ROADMAP SC#4）；PPT-02/05/08 是 spike 门控简单逆向（运行时 try/catch 降级，ROADMAP SC#5 记录 spike verdict）。每工具完成 D-17 四步守门。

Output:
- PptAdapter.ts：8 个方法（3 对 spike write+inverse + 2 个 noop+gate write 方法）
- ppt.ts：5 个 ToolDef
- D-17 四步守门：5 工具全部完成（含 2 个 noop+gate → skipped_error + 3 个 spike happy-path → rolled_back）
- Phase 10 所有 18 工具全部上线
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-RESEARCH.md
@.planning/phases/10-excel-ppt-b-excel-b-ppt/10-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- spike 门控范式（Pattern I）和 noop+gate 范式（Pattern H）-->

From RESEARCH.md §Pattern 4（spike 门控，D-10/D-11 指令）:
```typescript
// spike 工具 adapter 方法：try/catch 读 before-image
async setShapeTextAlignment(slideIndex: number, shapeId: string, alignment: string): Promise<{
  beforeAlignment: string | null; // null = 降级到 noop
}> {
  return await PowerPoint.run(async (ctx) => {
    // ... 四 sync 定位 shape ...
    try {
      shape.textFrame.textRange.paragraphFormat.load('alignment');
      await ctx.sync();
      const beforeAlignment = shape.textFrame.textRange.paragraphFormat.alignment as string;
      shape.textFrame.textRange.paragraphFormat.alignment = alignment;
      await ctx.sync();
      return { beforeAlignment };  // happy-path
    } catch {
      // 降级：仍尝试写入（如可能），返回 null 信号
      return { beforeAlignment: null };
    }
  });
}
```

From RESEARCH.md §Pattern 3（noop+gate，Pattern H）:
```typescript
// ToolDef.execute 中
const reverse: ReverseDescriptor = {
  tool: 'noop_inverse',
  args: { reason: '形状完整状态（类型/填充/文字/字体）无法序列化重建，此步不可自动撤销' },
};
// 操作照常执行；noop_inverse case 已存在于 operationLog.ts
```

From RESEARCH.md §PPT-08（set_slide_background，PPT API 1.10 + isSetSupported）:
```typescript
// before-image 读取（spike S2 — try/catch）
try {
  slide.background.fill.load(['type', 'foregroundColor']);
  await ctx.sync();
  const beforeColor = slide.background.fill.foregroundColor as string | null;
  return { beforeColor };  // happy-path
} catch {
  return { beforeColor: null };  // 降级信号
}
// 写入（可靠，PPT API 1.10）
slide.background.fill.setSolidColor(color);
```

From RESEARCH.md §PPT-06（manage_slides v2.1 只支持 delete）:
```typescript
// 参数 schema: { operation: 'delete', slideIndex: number }
// 正向：slides.items[slideIndex - 1].delete(); await ctx.sync()
// reverse = { tool: 'noop_inverse', args: { reason: '幻灯片内容无法通过 Office.js 序列化导出...' } }
```

From PptAdapter.ts (从 Wave 3 已知的现有范式):
```typescript
// setShapeProperty 四 sync 范式 — 适用于 spike 工具的 before-image load
// restoreShapeProperty Record 签名范式 — 适用于 3 个 spike inverse 方法
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PptAdapter.ts — 3 对 spike write+inverse + 2 个 noop+gate write 方法</name>
  <files>src/adapters/PptAdapter.ts</files>
  <read_first>
    - src/adapters/PptAdapter.ts（Wave 3 之后的状态，重点：setShapeProperty 四 sync 范式 lines 670-769、restoreShapeProperty Record 签名范式 lines 791-871）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-RESEARCH.md §PPT-02（spike S4 alignment）、§PPT-04（delete_shape noop+gate）、§PPT-05（spike S1 rotation）、§PPT-06（manage_slides noop+gate）、§PPT-08（spike S2 background）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-PATTERNS.md §Pattern D（四 sync）§Pattern E（inverse Record）§Pattern H（noop+gate）§Pattern I（spike 门控）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md §G-D D-10/D-11/D-12（spike 不阻塞 + 运行时降级）§G-E D-13/D-14（noop+gate 行为）
  </read_first>
  <behavior>
    - setShapeTextAlignment(slideIndex, shapeId, alignment) → 四 sync → try load paragraphFormat.alignment → 成功返回 { beforeAlignment: string } → catch 返回 { beforeAlignment: null }（降级信号）
    - restoreShapeAlignment(args: Record) → 解包 slide_index/shape_id/before_alignment → 两 sync 还原（Pattern E 范式）
    - rotateShape(slideIndex, shapeId, rotation) → 四 sync → try load shape.rotation → 成功写入返回 { beforeRotation: number } → catch 返回 { beforeRotation: null }
    - restoreShapeRotation(args: Record) → 解包 slide_index/shape_id/before_rotation → 两 sync 写回旋转角度
    - deleteShape(slideIndex, shapeId) → 四 sync 定位 shape → shape.delete() → 无 before-image（noop+gate）→ 返回 {}
    - setSlideBackground(slideIndex, color) → sync 1 load slides → try load background.fill.foregroundColor → 成功返回 { beforeColor: string | null } → catch 返回 { beforeColor: null }（降级信号）→ isSetSupported('PowerPointApi', '1.10') 门控写入 → setSolidColor(color)
    - restoreSlideBackground(args: Record) → 解包 slide_index/before_color → before_color 非 null → setSolidColor(before_color)；null → slide.background.fill.clear()（恢复默认）
    - manageSlides(operation: 'delete', slideIndex) → sync 1 load slides → slides.items[slideIndex-1].delete() → 无 before-image（noop+gate）→ 返回 {}
  </behavior>
  <action>
在 PptAdapter.ts 末尾（Wave 3 新增方法之后）追加 8 个方法：

**setShapeTextAlignment（PPT-02，spike S4 门控）：**
- 四 sync 定位（复用 setShapeProperty 范式）
- **sync 3 改为**：`try { shape.textFrame.textRange.paragraphFormat.load('alignment'); await ctx.sync(); const beforeAlignment = shape.textFrame.textRange.paragraphFormat.alignment as string; shape.textFrame.textRange.paragraphFormat.alignment = alignment; await ctx.sync(); return { beforeAlignment }; } catch { return { beforeAlignment: null }; }`
- 返回类型：`Promise<{ beforeAlignment: string | null }>`

**restoreShapeAlignment（PPT-02 inverse，Pattern E）：**
- `(args: Record<string, unknown>)` — 解包 slide_index/shape_id/before_alignment
- 两 sync 还原：`shape.textFrame.textRange.paragraphFormat.alignment = before_alignment`

**rotateShape（PPT-05，spike S1 门控，GitHub #3022）：**
- 四 sync 定位（无需 TEXT_SHAPE_TYPES 守门，rotation 是 shape 级属性）
- **sync 3 改为**：`try { shape.load(['rotation']); await ctx.sync(); const beforeRotation = shape.rotation as number; shape.rotation = rotation; await ctx.sync(); return { beforeRotation }; } catch { return { beforeRotation: null }; }`
- 返回类型：`Promise<{ beforeRotation: number | null }>`

**restoreShapeRotation（PPT-05 inverse，Pattern E）：**
- `(args: Record<string, unknown>)` — 解包 slide_index/shape_id/before_rotation
- 两 sync 还原：`shape.rotation = before_rotation as number`

**deleteShape（PPT-04，noop+gate）：**
- 四 sync 定位 → `shape.delete(); await ctx.sync()` — 正向照常执行删除
- 返回 `{}`（无 before-image；ToolDef 中构建 noop_inverse reverse）

**setSlideBackground（PPT-08，spike S2 门控）：**
- sync 1 load slides → 取 slides.items[slideIndex-1]
- isSetSupported 门控：`if (!Office.context.requirements.isSetSupported('PowerPointApi', '1.10')) { return { beforeColor: null }; }` — 不支持则降级
- try/catch 读 before-image：`try { slide.background.fill.load(['type', 'foregroundColor']); await ctx.sync(); const beforeColor = slide.background.fill.foregroundColor as string | null; /* ... write ... */ return { beforeColor }; } catch { return { beforeColor: null }; }`
- 写入（可靠）：`slide.background.fill.setSolidColor(color); await ctx.sync()`
- 返回类型：`Promise<{ beforeColor: string | null }>`

**restoreSlideBackground（PPT-08 inverse）：**
- `(args: Record<string, unknown>)` — 解包 slide_index/before_color
- `if (before_color !== null) { slide.background.fill.setSolidColor(before_color as string); } else { slide.background.fill.clear(); }` — null = 恢复默认/主题背景

**manageSlides（PPT-06，noop+gate，v2.1 只支持 delete，D-14）：**
- operation 参数类型：`'delete'`（TypeScript 类型约束）
- execute 开头：`if (operation !== 'delete') throw new HostApiError('manage_slides 当前仅支持 delete 操作（v2.1）', undefined)` — 运行时双保险
- `slides.items[slideIndex-1].delete(); await ctx.sync()` — 正向照常执行
- 返回 `{}`（无 before-image；ToolDef 中构建 noop_inverse reverse）
  </action>
  <verify>
    <automated>grep -c "async setShapeTextAlignment\|async restoreShapeAlignment\|async rotateShape\|async restoreShapeRotation\|async deleteShape\|async setSlideBackground\|async restoreSlideBackground\|async manageSlides" /Users/wb.chen/Documents/Project/Aster/src/adapters/PptAdapter.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep 8 个方法名，输出 8
    - grep -c "beforeAlignment.*null\|beforeRotation.*null\|beforeColor.*null" src/adapters/PptAdapter.ts 输出 ≥ 3（3 个 spike 工具各有降级 null 信号返回）
    - grep -c "(args: Record<string, unknown>)" src/adapters/PptAdapter.ts 输出 ≥ 6（Wave 3 的 3 + Wave 4 的 3 = 6 个 inverse 方法）
    - grep -c "isSetSupported" src/adapters/PptAdapter.ts 输出 ≥ 1（PPT-08 PPT API 1.10 门控）
    - npm run test 全绿
  </acceptance_criteria>
  <done>PptAdapter.ts 含 8 个新方法，3 个 spike 方法有 try/catch + null 降级信号，2 个 noop+gate 正向执行正确，所有 inverse 签名 Record</done>
</task>

<task type="auto">
  <name>Task 2: ppt.ts ToolDef + 注册 + D-17 四步守门（PPT-02/04/05/06/08，含 noop+gate skipped_error 守门）</name>
  <files>src/agent/tools/write/ppt.ts, src/agent/tools/index.ts, src/agent/contract.test.ts, src/agent/operationLog.integration.test.ts, .planning/phases/08-foundation-a-f/CONTRACT.md</files>
  <read_first>
    - src/agent/tools/write/ppt.ts（Wave 3 之后状态，了解现有 ToolDef 范式）
    - src/agent/operationLog.integration.test.ts（Wave 0 已预埋的 5 条骨架：set_shape_text_alignment/rotate_shape/delete_shape/set_slide_background/manage_slides）
    - src/agent/contract.test.ts lines 53-58（PPT-02/04/05/06/08 对应行）
    - .planning/phases/10-excel-ppt-b-excel-b-ppt/10-CONTEXT.md §D-11（spike happy-path integration.test 验此）§D-13/D-14（noop+gate 行为）
  </read_first>
  <action>
**Step A：5 个 ToolDef（set_shape_text_alignment/delete_shape/rotate_shape/manage_slides/set_slide_background）。**

**set_shape_text_alignment（PPT-02，spike S4）：**
```typescript
async execute(args, ctx): Promise<ToolResult> {
  const { slideIndex, shapeId, alignment } = args as { slideIndex: number; shapeId: string; alignment: string };
  const { beforeAlignment } = await (ctx.adapter as PptAdapter).setShapeTextAlignment(slideIndex, shapeId, alignment);
  const reverse: ReverseDescriptor = beforeAlignment === null
    ? { tool: 'noop_inverse', args: { reason: 'paragraphFormat.alignment 不可读（spike S4 运行时降级），此步不可自动撤销' } }
    : { tool: 'restore_shape_alignment', args: { slide_index: slideIndex, shape_id: shapeId, before_alignment: beforeAlignment } };
  const postState: PostStateSnapshot = { kind: 'ppt_shape_alignment', content: { slideIndex, shapeId } };
  return { ok: true, data: { slideIndex, shapeId, alignment }, reverse, postState };
},
```

**delete_shape（PPT-04，noop+gate）：**
```typescript
async execute(args, ctx): Promise<ToolResult> {
  const { slideIndex, shapeId } = args as { slideIndex: number; shapeId: string };
  await (ctx.adapter as PptAdapter).deleteShape(slideIndex, shapeId);
  const reverse: ReverseDescriptor = {
    tool: 'noop_inverse',
    args: { reason: '形状完整状态（类型/位置/填充/文字/字体）无法序列化重建，此步不可自动撤销' },
  };
  const postState: PostStateSnapshot = { kind: 'ppt_shape', content: { slideIndex, shapeId } };
  return { ok: true, data: { slideIndex, shapeId }, reverse, postState };
},
```

**rotate_shape（PPT-05，spike S1）：**
- 同 set_shape_text_alignment 降级判断模式：`beforeRotation === null → noop_inverse；非 null → restore_shape_rotation`

**manage_slides（PPT-06，noop+gate）：**
- parameters: `{ operation: { type: 'string', enum: ['delete'] }, slideIndex: number }` — enum 硬限 ['delete']（D-14）
- description 标注「v2.1 仅支持删除幻灯片；此操作不可自动撤销（幻灯片内容无法序列化）」
- reverse = noop_inverse

**set_slide_background（PPT-08，spike S2）：**
- 同降级模式：`beforeColor === null → noop_inverse；非 null → restore_slide_background`
- description 标注「仅支持纯色背景；undo 需真机 Office for Web 验证（spike S2）」

**Step B：index.ts 追加 5 个工具。**

```typescript
const pptWriteTools = [
  insertSlide, setShapeProperty, moveShape, setShapeText,
  setShapeTextFontTool, addShapeTool, copySlide Tool,
  // Phase 10 Wave 4：
  setShapeTextAlignmentTool, deleteShapeTool, rotateShapeTool,
  manageSlides Tool, setSlideBackgroundTool,
] as ToolDef[];
```

**Step C：D-17 四步守门（5 工具）。**

1. contract.test.ts：PPT-02/04/05/06/08 五行 integrationTest: false → true（共 18 行全部翻）
2. operationLog.integration.test.ts：Wave 0 骨架守门适配：
   - delete_shape/manage_slides（noop+gate）：已在 Wave 0 断言 `status === 'skipped_error'`，adapter 方法存在后仍是 skipped_error（noop_inverse case 已存在）→ 自动 GREEN
   - set_shape_text_alignment/rotate_shape/set_slide_background（spike happy-path）：Wave 0 骨架断言 `rolled_back`；adapter 方法存在 + vi.fn() mock 收 before-alignment/before_rotation/before_color（非 null）→ executeReverse → restoreShapeAlignment/restoreShapeRotation/restoreSlideBackground → `rolled_back` → GREEN
3. CONTRACT.md：PPT-02/04/05/06/08 五行 status→done + integration_test→true（全 18 行完成）
4. noop+gate 验证：delete_shape + manage_slides 守门断言 `status === 'skipped_error'`（D-17 第 4 步，Wave 0 骨架已有）
  </action>
  <verify>
    <automated>npm test -- src/agent/contract.test.ts src/agent/operationLog.integration.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "integrationTest: true" src/agent/contract.test.ts 输出 18（全部 18 工具全部翻 true）
    - npm test -- src/agent/contract.test.ts 全绿（CONTRACT.length >= 23 + D-17 fs.readFileSync 全部 18 个 toolName 在 integration.test.ts 中存在）
    - npm test -- src/agent/operationLog.integration.test.ts 全绿（包括 delete_shape/manage_slides skipped_error + spike happy-path rolled_back）
    - grep -c "status: done" .planning/phases/08-foundation-a-f/CONTRACT.md 输出 18（全部 18 工具）
    - grep -c "integration_test: true" .planning/phases/08-foundation-a-f/CONTRACT.md 输出 18
    - grep "enum.*delete\|delete.*enum" src/agent/tools/write/ppt.ts（manage_slides schema enum ['delete']，D-14）
    - npm run build 通过，npm run size ≤82 KB
    - npm test 全绿（全套）
  </acceptance_criteria>
  <done>Phase 10 全部 18 工具实现完成；5 个 noop+gate/spike 工具守门通过；contract.test.ts + CONTRACT.md 全 18 行完成；bundle ≤82 KB；npm test 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike 工具 adapter → ToolDef | try/catch null 信号传递；ToolDef 根据 null 判断是否降级 noop+gate |
| noop+gate → DiffLogPanel | noop_inverse → throw → skipped_error → DiffLog 显示 warn（不中断 agent）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Tampering | spike 工具 before-image 读失败后静默写入 | mitigate | D-10/D-11：before-image 读失败 → 返回 null 信号 → ToolDef 降级 noop_inverse（warn 不中断）；写入操作仍执行（执行 > 不执行）；真机 UAT 验 spike verdict（ROADMAP SC#5）|
| T-10-15 | Denial of Service | manage_slides delete 删错 slide（index 越界）| mitigate | slides.items[slideIndex-1] 越界 → undefined → delete() 调用 HostApiError；adapter 外层 try/catch 包装 |
| T-10-16 | Elevation of Privilege | manage_slides reorder 绕过（D-14）| mitigate | schema enum ['delete'] 硬限 + adapter 运行时检查 `if (operation !== 'delete') throw`（双保险）；v2.1 不暴露 reorder |
| T-10-17 | Tampering | set_slide_background 读主题色（Out of Scope）| mitigate | D-12：只写纯色 setSolidColor；before-image 通过 try/catch 读 foregroundColor（读不到 → null → noop+gate）；不实现 read_slide_background 工具（Out of Scope） |
</threat_model>

<verification>
```bash
# 1. adapter 方法完整（8 个）
grep -c "async setShapeTextAlignment\|async restoreShapeAlignment\|async rotateShape\|async restoreShapeRotation\|async deleteShape\|async setSlideBackground\|async restoreSlideBackground\|async manageSlides" /Users/wb.chen/Documents/Project/Aster/src/adapters/PptAdapter.ts
# 期望 8

# 2. spike 降级 null 信号
grep -c "beforeAlignment.*null\|beforeRotation.*null\|beforeColor.*null" /Users/wb.chen/Documents/Project/Aster/src/adapters/PptAdapter.ts
# 期望 ≥ 3

# 3. 全部 18 行守门翻完
grep -c "integrationTest: true" /Users/wb.chen/Documents/Project/Aster/src/agent/contract.test.ts
# 期望 18

# 4. CONTRACT.md 全完成
grep -c "status: done" /Users/wb.chen/Documents/Project/Aster/.planning/phases/08-foundation-a-f/CONTRACT.md
# 期望 18

# 5. 全套测试全绿
npm test

# 6. bundle 守门
npm run build && npm run size
```
</verification>

<success_criteria>
- Phase 10 全部 18 工具实现完成（EXCEL-01..10 + PPT-01..08）
- contract.test.ts 全 18 行 integrationTest→true，D-17 全套守门通过
- CONTRACT.md 全 18 行 status→done + integration_test→true
- npm test 全绿（integration 守门：EXCEL 13 条 + PPT noop+gate 2 条 + PPT spike/simple 3 条）
- bundle ≤82 KB（npm run size ≤82）
- ROADMAP SC#1-5 全部满足：format_excel_range undo / sort_range 快照 undo / add_shape undo / noop+gate warn / spike 结论记录
</success_criteria>

<output>
完成后创建 `.planning/phases/10-excel-ppt-b-excel-b-ppt/10-05-SUMMARY.md`
</output>

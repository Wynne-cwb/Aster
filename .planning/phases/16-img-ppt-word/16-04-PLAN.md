---
phase: 16-img-ppt-word
plan: 04
type: execute
wave: 2
depends_on:
  - "16-02"
files_modified:
  - src/providers/registry.ts
  - src/components/Settings/SettingsPanel.tsx
autonomous: true
requirements:
  - IMG-04

must_haves:
  truths:
    - "ProviderRegistry.resolve('image-gen') 支持从 localStorage 读取用户选定 model，覆盖默认 doubao"
    - "SettingsPanel 全局选项区含生图 model 下拉（<select>），选项来自 IMAGE_GEN_MODELS，持久到 aster:pref:image-gen-model"
    - "默认展示 doubao-seedream-5.0-lite（isDefault:true）"
    - "切换 model 后 ProviderRegistry.resolve('image-gen') 返回新 model ID"
  artifacts:
    - path: "src/providers/registry.ts"
      provides: "image-gen resolve 读 localStorage model 覆盖"
      contains: "PREF_IMAGE_GEN_MODEL"
    - path: "src/components/Settings/SettingsPanel.tsx"
      provides: "生图 model picker UI"
      contains: "生图模型"
  key_links:
    - from: "src/components/Settings/SettingsPanel.tsx"
      to: "src/lib/storage.ts"
      via: "storage.set(PREF_IMAGE_GEN_MODEL, selectedId)"
      pattern: "PREF_IMAGE_GEN_MODEL"
    - from: "src/providers/registry.ts"
      to: "src/lib/storage.ts"
      via: "storage.get(PREF_IMAGE_GEN_MODEL)"
      pattern: "PREF_IMAGE_GEN_MODEL"
---

<objective>
实现 D-04 的 Settings 持久默认 model picker：在 SettingsPanel 新增生图 model 下拉（读 `IMAGE_GEN_MODELS` 列表），并扩展 `ProviderRegistry.resolve('image-gen')` 从 `localStorage` 读取用户选定 model 覆盖默认 doubao。

**Purpose:** 用户在 Settings 里选 gpt-image-2 后，所有后续生图调用都用 gpt-image-2（持久跨 session）。

**Output:** 扩展 registry.ts image-gen resolve + SettingsPanel 新增下拉 section。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-img-ppt-word/16-CONTEXT.md
@.planning/phases/16-img-ppt-word/16-PATTERNS.md

<interfaces>
<!-- executor 需要的现有接口 -->

来自 src/providers/registry.ts image-gen case（L125–136）当前实现：
```typescript
case 'image-gen': {
  const apiKey = storage.get<string>(STORAGE_KEYS.KEY_PREFIX + AIHUBMIX_PROVIDER_ID);
  if (!apiKey) throw new KeyInvalidError('aihubmix Key 未配置...');
  return {
    providerId: `${AIHUBMIX_PROVIDER_ID}-image`,
    baseURL: AIHUBMIX_IMAGE_BASE_URL,
    apiKey,
    model: DEFAULT_IMAGE_GEN_MODEL.id,  // 当前硬编码，需改为读 localStorage
  } satisfies ImageConfig;
}
```

来自 src/lib/storage.ts（STORAGE_KEYS + storage.get/set 签名，L19–93）：
```typescript
export const STORAGE_KEYS = {
  KEY_PREFIX: 'aster:keys:',
  ONBOARDING_SEEN: 'aster:onboarding:seen',
  SELECTION_ATTACH_ENABLED: 'aster:selection:attachEnabled',
  DEFAULT_PROVIDER: 'aster:providers:default',
  USER_PREFERENCES: 'aster:prefs:user',
  CHAT_HISTORY_PREFIX: 'aster:chat:',
  // Phase 16 需新增 key（或用字面量）
} as const;
// storage.get<string>(key) / storage.set(key, value)
```

来自 src/components/Settings/SettingsPanel.tsx（全局选项分区，L155–260）：
```tsx
<div className="aster-settings__global-options">
  <div className="aster-settings__section">
    <label className="aster-settings__toggle-row" htmlFor="setting-auto-attach">
      <span className="aster-settings__label"><Trans>自动附带选区内容</Trans></span>
      <label className="switch" ...><input type="checkbox" .../><span className="thumb" /></label>
    </label>
    <p className="aster-settings__hint">...</p>
  </div>
  {/* 更多 section... */}
</div>
```

IMAGE_GEN_MODELS（registry.ts L49–72，3 个 model）：
```typescript
export const IMAGE_GEN_MODELS = [
  { id: 'doubao-seedream-5.0-lite', label: 'Doubao SeedDream 5.0 Lite（快速默认）', isDefault: true },
  { id: 'gpt-image-2', label: 'GPT-Image-2（高质量）', isDefault: false },
  { id: 'gemini-3.1-flash-image-preview', label: 'Gemini 3.1 Flash Image Preview', isDefault: false },
];
```

来自 src/styles.css（SettingsPanel 已有样式类，确认是否有 aster-settings__select）：
- `aster-settings__section`、`aster-settings__label`、`aster-settings__hint` 已存在
- `aster-settings__select`：**需确认是否存在**；若不存在，仿 input 样式新建（用 CSS 变量 --surface/--border/--text，不硬编码颜色）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 registry.ts image-gen resolve 读 localStorage model 覆盖</name>
  <files>
    src/providers/registry.ts
    src/lib/storage.ts
  </files>
  <read_first>
    - src/providers/registry.ts — image-gen case 完整代码（L125–136）；文件顶部 imports 和常量（L1–35）；storage.get 调用范式（L100）
    - src/lib/storage.ts — STORAGE_KEYS 完整常量（L19–44）；storage.get / storage.set 签名（L61–93 prefixedKey 逻辑）
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — §Settings 生图 model picker 持久化读写范式（L489–497）
    - .planning/phases/16-img-ppt-word/16-RESEARCH.md — Open Question 3（存储键名；§Standard Stack PREF_IMAGE_GEN_MODEL）
  </read_first>
  <action>
    **storage.ts — 新增 PREF_IMAGE_GEN_MODEL key 到 STORAGE_KEYS**（L44 `} as const` 前）：
    ```typescript
    /** Phase 16 IMG-04：用户持久选择的生图 model ID */
    PREF_IMAGE_GEN_MODEL: 'aster:pref:image-gen-model',
    ```

    **registry.ts image-gen case 扩展**（只改 model 字段）：
    ```typescript
    case 'image-gen': {
      const apiKey = storage.get<string>(STORAGE_KEYS.KEY_PREFIX + AIHUBMIX_PROVIDER_ID);
      if (!apiKey) throw new KeyInvalidError('aihubmix Key 未配置，请在设置中填写 aihubmix Key');
      // D-04：读用户持久选择的 model（Settings picker 写入）；fallback 到默认 doubao
      const preferredModel = storage.get<string>(STORAGE_KEYS.PREF_IMAGE_GEN_MODEL);
      const modelId = preferredModel ?? DEFAULT_IMAGE_GEN_MODEL.id;
      return {
        providerId: `${AIHUBMIX_PROVIDER_ID}-image`,
        baseURL: AIHUBMIX_IMAGE_BASE_URL,
        apiKey,
        model: modelId,
      } satisfies ImageConfig;
    }
    ```

    **这是最小侵入改动**：仅在 image-gen case 加一行 storage.get，不改函数签名，不影响其他 case。
  </action>
  <verify>
    <automated>npm test -- --run src/providers/registry.test.ts; npm run build 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>
  <done>
    registry.ts image-gen case 读 PREF_IMAGE_GEN_MODEL；storage.ts 新增 key；registry.test.ts 通过。
    acceptance_criteria:
    - `grep -c "PREF_IMAGE_GEN_MODEL" src/lib/storage.ts` 返回 ≥1
    - `grep -c "PREF_IMAGE_GEN_MODEL" src/providers/registry.ts` 返回 ≥1
    - `grep -c "aster:pref:image-gen-model" src/lib/storage.ts` 返回 ≥1
    - `npm test -- --run src/providers/registry.test.ts` 退出 0
    - `npm run build 2>&1 | grep -v '^#' | grep -c "error TS"` 返回 0
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsPanel 新增生图 model picker + lingui extract</name>
  <files>
    src/components/Settings/SettingsPanel.tsx
    src/styles.css
  </files>
  <read_first>
    - src/components/Settings/SettingsPanel.tsx — 完整文件：全局选项分区（L155–280）；现有 imports；useState/useEffect 使用范式；storage.get/set 调用（看文件内现有 storage 使用）
    - src/styles.css — `aster-settings__select` 是否存在（搜索此 class）；`aster-settings__section`、`aster-settings__label`、`aster-settings__hint` 样式（确认可复用）
    - src/providers/registry.ts — IMAGE_GEN_MODELS export（L49–72）；DEFAULT_IMAGE_GEN_MODEL（L75）
    - src/lib/storage.ts — STORAGE_KEYS.PREF_IMAGE_GEN_MODEL（刚在 Task 1 新增）；storage.get/set 签名
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — §SettingsPanel 生图 model picker 实现形态（L458–497）
    - memory: project_i18n_extract_and_test_noise — 改 UI 动 Lingui 宏必跑 npm run extract
  </read_first>
  <action>
    **SettingsPanel.tsx 改动（3 步）：**

    **Step 1 — imports**（在现有 imports 末尾加）：
    ```typescript
    import { IMAGE_GEN_MODELS, DEFAULT_IMAGE_GEN_MODEL } from '../../providers/registry';
    import { STORAGE_KEYS } from '../../lib/storage';
    // storage import 应已存在；若不存在则加 import { storage } from '../../lib/storage';
    ```

    **Step 2 — state**（在组件函数体，现有 useState hooks 后加）：
    ```typescript
    const [imageGenModel, setImageGenModelState] = useState<string>(
      () => storage.get<string>(STORAGE_KEYS.PREF_IMAGE_GEN_MODEL) ?? DEFAULT_IMAGE_GEN_MODEL.id,
    );
    const setImageGenModel = (modelId: string) => {
      storage.set(STORAGE_KEYS.PREF_IMAGE_GEN_MODEL, modelId);
      setImageGenModelState(modelId);
    };
    ```

    **Step 3 — JSX**（在全局选项分区 `<div className="aster-settings__global-options">` 内，末尾加一个新 section，与现有 section 并列）：
    ```tsx
    <div className="aster-settings__section">
      <label className="aster-settings__label" htmlFor="setting-image-gen-model">
        <Trans>生图模型</Trans>
      </label>
      <select
        id="setting-image-gen-model"
        value={imageGenModel}
        onChange={(e) => setImageGenModel(e.target.value)}
        className="aster-settings__select"
      >
        {IMAGE_GEN_MODELS.map((m) => (
          <option key={m.id} value={m.id}>{m.label}</option>
        ))}
      </select>
      <p className="aster-settings__hint">
        <Trans>默认生图模型。预览卡内可临时切换不保存。</Trans>
      </p>
    </div>
    ```

    **styles.css — 新增 `.aster-settings__select`**（若不存在；紧接 `.aster-settings__input` 等 form 元素样式）：
    ```css
    .aster-settings__select {
      width: 100%;
      padding: 4px var(--sp-2);
      border: 1px solid var(--border);
      border-radius: var(--radius-2);
      background: var(--surface);
      color: var(--text);
      font-size: 12.5px;
      font-family: var(--font-body);
      cursor: pointer;
    }
    .aster-settings__select:focus {
      outline: none;
      box-shadow: var(--ring-focus);
    }
    ```
    若 `aster-settings__select` 已存在，跳过此步。

    **lingui extract**（memory: project_i18n_extract_and_test_noise）：
    ```bash
    npm run extract
    ```
    运行后检查 `src/locales/zh-CN/messages.po` 是否新增「生图模型」和「默认生图模型。预览卡内可临时切换不保存。」条目。
  </action>
  <verify>
    <automated>npm run extract; npm test -- --run; npm run build 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>
  <done>
    SettingsPanel 含生图 model picker section；styles.css 含 aster-settings__select；lingui extract 通过；全量测试通过；build 无 TS error。
    acceptance_criteria:
    - `grep -c "IMAGE_GEN_MODELS\|image-gen-model\|生图模型" src/components/Settings/SettingsPanel.tsx` 返回 ≥2
    - `grep -c "aster-settings__select" src/styles.css` 返回 ≥1
    - `grep -c "aster-settings__select" src/components/Settings/SettingsPanel.tsx` 返回 ≥1
    - `npm run extract` 退出 0（无错误）
    - `npm test -- --run` 退出 0（含 coverage.test.ts lingui 检查通过）
    - `npm run build 2>&1 | grep -v '^#' | grep -c "error TS"` 返回 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsPanel → localStorage | model ID 是字符串（如 'doubao-seedream-5.0-lite'），写入前无需额外净化（仅是 model 名称选择，无自由输入） |
| localStorage → registry.resolve | registry 读到的 model ID 传给 AihubmixImageClient；若是未知 model，provider 抛 NetworkError（诚实失败） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Tampering | PREF_IMAGE_GEN_MODEL localStorage 值 | accept | 用户只能通过 Settings picker `<select>` 写入，选项来自 IMAGE_GEN_MODELS 常量；即使手动改 localStorage 写入任意值，provider 会抛 NetworkError 诚实失败 |
| T-16-11 | Information Disclosure | apiKey 在 SettingsPanel 展示 | accept | Settings 显示 aihubmix key 已是现有行为（v1 基座）；model picker 不新增 key 展示面 |
</threat_model>

<verification>
- SettingsPanel 渲染生图 model 下拉（3 个选项，默认 doubao）
- 切换 model → storage.set 写入 aster:pref:image-gen-model
- ProviderRegistry.resolve('image-gen') 返回的 model 字段读到 localStorage 值
- `npm test -- --run` 全量通过
- `npm run build` 无 TypeScript error
</verification>

<success_criteria>
1. registry.ts image-gen case 读 PREF_IMAGE_GEN_MODEL（fallback 默认 doubao）
2. storage.ts STORAGE_KEYS 新增 PREF_IMAGE_GEN_MODEL 常量
3. SettingsPanel 全局选项区含生图 model 下拉 section（3 个 model 选项）
4. lingui extract 通过；全量测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/16-img-ppt-word/16-04-SUMMARY.md`
</output>

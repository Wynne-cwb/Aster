---
phase: 16-img-ppt-word
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agent/tools/write/ppt-image.test.ts
  - src/agent/tools/write/word-image.test.ts
  - src/agent/operationLog.integration.test.ts
  - src/store/chat.test.ts
  - src/agent/tools/tools-host.test.ts
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-05

must_haves:
  truths:
    - "ppt-image.test.ts 存在并覆盖 generate_ppt_image 工具返回 preview_pending=true 的行为"
    - "word-image.test.ts 存在并覆盖 generate_word_image 工具返回 preview_pending=true 的行为"
    - "operationLog.integration.test.ts 新增 generate_ppt_image → delete_shape_by_id rolled_back 守门用例"
    - "operationLog.integration.test.ts 新增 generate_word_image → noop_inverse skipped_error 守门用例"
    - "chat.test.ts 新增 NFR-09 路径 C：image preview pending base64 不出现在序列化结果"
    - "tools-host.test.ts 新增断言：Excel host 工具表不含 generate_ppt_image / generate_word_image"
    - "npm test -- --run 全量通过（test 文件因实现未就绪先 describe.skip）"
  artifacts:
    - path: "src/agent/tools/write/ppt-image.test.ts"
      provides: "IMG-01 工具单测脚手架"
      contains: "generate_ppt_image"
    - path: "src/agent/tools/write/word-image.test.ts"
      provides: "IMG-02 工具单测脚手架"
      contains: "generate_word_image"
    - path: "src/agent/tools/tools-host.test.ts"
      provides: "IMG-05 Excel host 守门"
      contains: "generate_ppt_image"
  key_links:
    - from: "src/agent/tools/write/ppt-image.test.ts"
      to: "src/agent/tools/write/ppt-image.ts"
      via: "import（Plan 16-03 实现后变绿）"
      pattern: "generate_ppt_image"
    - from: "src/agent/operationLog.integration.test.ts"
      to: "src/adapters/PptAdapter.ts"
      via: "mockPpt + replayUndoSingle（复用已有 D-17 mock 结构）"
      pattern: "generate_ppt_image.*rolled_back"
---

<objective>
建立 Phase 16 所有测试脚手架，使后续 Wave 1–3 实现文件完成后能立即通过守门验证。所有测试文件先用 `describe.skip` 标注（防全量 CI 红），Plan 16-03 实现完成后去掉 skip。

**Purpose:** Nyquist 合规要求——每个实现 task 都有对应 automated verify 命令；Wave 0 先建 test stub，Wave 1+ 实现后变绿。

**Output:** 5 个测试文件（新建 3 个，追加 2 个）；`npm test -- --run` 全量通过。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-img-ppt-word/16-CONTEXT.md
@.planning/phases/16-img-ppt-word/16-VALIDATION.md
@.planning/phases/16-img-ppt-word/16-PATTERNS.md

<interfaces>
<!-- 测试脚手架需要的现有接口，从 codebase 提取 -->

来自 src/agent/operationLog.integration.test.ts（D-17 守门用例结构，L849–879）：
```typescript
// mockPpt 调用范式
mockPpt('');
const adapter = new PptAdapter();
const entry: OperationLogEntry = {
  runId: 'r10', stepIndex: 12,
  toolName: 'add_shape',
  humanLabel: '在第 1 页插入文本框「季度总结」',
  args: {},
  reverse: { tool: 'delete_shape_by_id', args: { slide_index: 1, shape_id: 'new-shape-uuid' } },
  postState: { kind: 'ppt_shape_new', content: { slideIndex: 1, shapeId: 'new-shape-uuid' } },
  timestamp: 0,
};
const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
expect(detail.status).toBe('rolled_back');
```

来自 src/store/chat.test.ts（NFR-09 路径 A，L297–330）：
```typescript
it('NFR-09 serialize-test：tool role 含 vision base64 → 序列化后 base64 不出现', () => {
  const fakeBase64 = 'data:image/png;base64,' + 'A'.repeat(500);
  useChatStore.setState({
    messages: [
      { id: 'u1', role: 'user', content: '分析这张图', ts: 1 },
      {
        id: 'tool1', role: 'tool', content: '正在看这张图…',
        toolResult: { ok: true, data: { vision_result: '图中是一张饼图', base64_raw: fakeBase64 } },
        ts: 2,
      },
      { id: 'a1', role: 'assistant', content: '根据图片…', ts: 3 },
    ],
  } as never);
  useChatStore.getState().saveHistory('aster:chat:vis-test');
  const call = mockedStorage.set.mock.calls[0];
  const payload = call[1] as { messages: Array<{ role: string; content: string }> };
  expect(payload.messages.every((m) => m.role !== 'tool')).toBe(true);
  const allContent = payload.messages.map((m) => m.content).join('');
  expect(allContent).not.toContain('base64');
});
```

来自 src/agent/tools/index.ts（buildToolsForHost 结构，L244–300）：
```typescript
export function buildToolsForHost(host: 'word' | 'excel' | 'ppt'): ToolDef[] {
  switch (host) {
    case 'excel': {
      // Excel case 不加生图工具（IMG-05）
      return [...excelReadTools, ...excelWriteTools, selectionDetail].map((t) => t as ToolDef);
    }
    // ...
  }
}
```

ToolResult 接口（src/agent/tools/index.ts，L86–100）：
```typescript
export interface ToolResult {
  ok: boolean;
  data?: unknown;
  error?: ToolError;
  reverse?: ReverseDescriptor;
  postState?: PostStateSnapshot;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 ppt-image.test.ts + word-image.test.ts 工具单测脚手架</name>
  <files>
    src/agent/tools/write/ppt-image.test.ts
    src/agent/tools/write/word-image.test.ts
  </files>
  <read_first>
    - src/agent/tools/write/ppt.ts — addShapeTool execute 结构（L364–420），ToolDef 接口，humanLabel 写法
    - src/agent/tools/write/word.ts — appendParagraph execute 结构（L46–75），imports 范式
    - src/agent/tools/index.ts — ToolResult 接口、ToolError 接口定义（L63–110）
    - src/providers/aihubmix-image.ts — AihubmixImageClient.generate 签名（L21–39）
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — Pattern 1（ppt-image.ts 关键差异点）、Pattern 2（word-image.ts 关键差异点）
    - .planning/phases/16-img-ppt-word/16-VALIDATION.md — IMG-01/IMG-02 test map 行
  </read_first>
  <behavior>
    - Test 1（ppt-image）：generate_ppt_image 工具 execute 调用 AihubmixImageClient.generate，返回 ok:true + data.preview_pending===true + data.base64 非空 + data.mimeType 非空
    - Test 2（ppt-image）：generate_ppt_image 工具 execute 的 ToolResult.reverse 为 undefined（生图工具本身不写文档，D-02 解耦）
    - Test 3（ppt-image）：aihubmix key 未配置时，execute 返回 ok:false + error.code==='PERMISSION_DENIED'（三类错误 ① 场景）
    - Test 4（word-image）：generate_word_image 工具 execute 调用 AihubmixImageClient.generate，返回 ok:true + data.preview_pending===true
    - Test 5（word-image）：generate_word_image 工具 execute 的 ToolResult.reverse 为 undefined（D-02 解耦）
  </behavior>
  <action>
    **两个文件均先用 describe.skip 包裹**（实现 ppt-image.ts / word-image.ts 在 Plan 16-03 才交付；skip 保证全量 CI 当前不红）。

    `src/agent/tools/write/ppt-image.test.ts` 结构：
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    // 用 vi.mock 模拟 AihubmixImageClient.generate
    // 用 vi.mock 模拟 ProviderRegistry.resolve（image-gen 路由）
    // 模拟返回 { base64: 'abc123', mimeType: 'image/png' }

    describe.skip('generate_ppt_image tool（Plan 16-03 实现后去掉 skip）', () => {
      // Test 1: ok:true + preview_pending:true
      // Test 2: reverse === undefined
      // Test 3: aihubmix key 未配置 → PERMISSION_DENIED
    });
    ```

    `src/agent/tools/write/word-image.test.ts` 结构对称（generate_word_image）。

    mock 的 AihubmixImageClient：
    ```typescript
    vi.mock('../../../providers/aihubmix-image', () => ({
      AihubmixImageClient: vi.fn().mockImplementation(() => ({
        generate: vi.fn().mockResolvedValue({ base64: 'abc123', mimeType: 'image/png' }),
      })),
    }));
    ```

    mock 的 ProviderRegistry.resolve（成功路径）：
    ```typescript
    vi.mock('../../../providers/registry', () => ({
      ProviderRegistry: { resolve: vi.fn().mockReturnValue({ providerId: 'aihubmix-image', baseURL: 'https://aihubmix.com', apiKey: 'test-key', model: 'doubao-seedream-5.0-lite' }) },
      IMAGE_GEN_MODELS: [],
      DEFAULT_IMAGE_GEN_MODEL: { id: 'doubao-seedream-5.0-lite' },
    }));
    ```

    key 未配置测试：让 ProviderRegistry.resolve 抛 KeyInvalidError：
    ```typescript
    import { KeyInvalidError } from '../../../errors';
    // vi.mocked(ProviderRegistry.resolve).mockImplementationOnce(() => { throw new KeyInvalidError('...'); });
    ```

    两个文件都用 imports 来自实际路径（Plan 16-03 建好后才 resolve），当前 skip 下不会报 module not found。
    若 TypeScript 严格 module not found 报错，改用 dynamic import + describe.skip 组合（参 memory: project_gsd_tooling_quirks 和 Phase 15 attachments.test.ts 范式：`import type` 替代真 import，mock 仅按字面量）。
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/ppt-image.test.ts; npm test -- --run src/agent/tools/write/word-image.test.ts</automated>
  </verify>
  <done>
    两个文件存在；`npm test -- --run` 中两个文件的 suite 出现（以 skipped 状态通过）；全量不红。
    acceptance_criteria:
    - `grep -c "describe.skip" src/agent/tools/write/ppt-image.test.ts` 返回 1
    - `grep -c "preview_pending" src/agent/tools/write/ppt-image.test.ts` 返回 ≥1
    - `grep -c "describe.skip" src/agent/tools/write/word-image.test.ts` 返回 1
    - `npm test -- --run src/agent/tools/write/ppt-image.test.ts` 退出 0
    - `npm test -- --run src/agent/tools/write/word-image.test.ts` 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 operationLog.integration.test.ts + chat.test.ts + 新建 tools-host.test.ts</name>
  <files>
    src/agent/operationLog.integration.test.ts
    src/store/chat.test.ts
    src/agent/tools/tools-host.test.ts
  </files>
  <read_first>
    - src/agent/operationLog.integration.test.ts — D-17 守门用例（L849–879）：mockPpt + replayUndoSingle 结构；noop_inverse L866–879
    - src/agent/operationLog.ts — PostStateSnapshot 接口定义（`{ kind: string; content: unknown }`，W6：content 是 unknown，不会类型不兼容；统一用 camelCase 写 content 字段以与 D-17 analog L849 保持一致）
    - src/store/chat.test.ts — NFR-09 路径 A（L297–330）：mockedStorage.set.mock.calls 结构；saveHistory 调用方式
    - src/agent/tools/index.ts — buildToolsForHost 完整函数（L244–300）；Excel case 工具表
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — §operationLog.integration.test.ts 守门用例结构（L627–670）；§chat.test.ts 路径 C 模拟结构（L594–622）
    - .planning/phases/16-img-ppt-word/16-VALIDATION.md — IMG-01/02/03/05 test map 行
  </read_first>
  <action>
    **operationLog.integration.test.ts 追加两条用例**（文件尾部，不改现有用例）：

    用例 A（IMG-01 inverse 守门）：
    ```typescript
    it('Phase 16: generate_ppt_image → delete_shape_by_id → rolled_back', async () => {
      mockPpt('');
      const adapter = new PptAdapter();
      const entry: OperationLogEntry = {
        runId: 'r16-ppt', stepIndex: 0,
        toolName: 'generate_ppt_image',
        args: {},
        humanLabel: '插入生成图片到第 1 页',
        // reverse.args 用 snake_case：deleteShapeById 消费约定（memory: project_adapter_inverse_signature）
        reverse: { tool: 'delete_shape_by_id', args: { slide_index: 1, shape_id: 'img-shape-uuid' } },
        // postState.content 用 camelCase：与 D-17 analog L849 保持一致（PostStateSnapshot.content 是 unknown，不会类型不兼容）
        // W6：此处 camelCase 必须与 16-02 insertImage helper 真实写入的 content 形状一致
        postState: { kind: 'ppt_shape_new', content: { slideIndex: 1, shapeId: 'img-shape-uuid' } },
        timestamp: 0,
      };
      const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
      expect(detail.status).toBe('rolled_back');
    });
    ```

    用例 B（IMG-02 noop_inverse 守门）：
    ```typescript
    it('Phase 16: generate_word_image → noop_inverse → skipped_error', async () => {
      const entry: OperationLogEntry = {
        runId: 'r16-word', stepIndex: 0,
        toolName: 'generate_word_image',
        args: {},
        humanLabel: '插入生成图片到 Word 文档',
        reverse: { tool: 'noop_inverse', args: { reason: 'Word 图片插入暂不支持自动撤销' } },
        timestamp: 0,
      };
      const detail = await replayUndoSingle(entry, {} as DocumentAdapterForReplay);
      expect(detail.status).toBe('skipped_error');
    });
    ```

    **chat.test.ts 追加 NFR-09 路径 C**（文件 NFR-09 describe 块末尾）：
    ```typescript
    it('NFR-09 路径 C: image preview pending ToolResult.data.base64 不出现在序列化结果', () => {
      const fakeBase64 = 'B'.repeat(500);
      useChatStore.setState({
        messages: [
          { id: 'u1', role: 'user', content: '生成一张落日的图', ts: 1 },
          {
            id: 'tool1', role: 'tool', content: '图片已生成，确认插入？',
            toolResult: {
              ok: true,
              data: { base64: fakeBase64, mimeType: 'image/png', prompt: '落日暖色调', preview_pending: true },
            },
            ts: 2,
          },
          { id: 'a1', role: 'assistant', content: '图片已生成，请确认插入', ts: 3 },
        ],
      } as never);
      useChatStore.getState().saveHistory('aster:chat:img-test');
      const call = mockedStorage.set.mock.calls.at(-1)!;
      const payload = call[1] as { messages: Array<{ role: string; content: string }> };
      expect(payload.messages.every((m) => m.role !== 'tool')).toBe(true);
      const allContent = payload.messages.map((m) => m.content).join('');
      expect(allContent).not.toContain('B'.repeat(100));
      expect(allContent).not.toContain('preview_pending');
    });
    ```

    **tools-host.test.ts 新建**（IMG-05 Excel 守门）：
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { buildToolsForHost } from './index';

    describe('buildToolsForHost — IMG-05 per-host 注册守门', () => {
      it('Excel host 工具表不含 generate_ppt_image', () => {
        const excelTools = buildToolsForHost('excel');
        expect(excelTools.map((t) => t.name)).not.toContain('generate_ppt_image');
      });
      it('Excel host 工具表不含 generate_word_image', () => {
        const excelTools = buildToolsForHost('excel');
        expect(excelTools.map((t) => t.name)).not.toContain('generate_word_image');
      });
      it('PPT host 工具表含 generate_ppt_image（Plan 16-03 注册后通过）', () => {
        const pptTools = buildToolsForHost('ppt');
        expect(pptTools.map((t) => t.name)).toContain('generate_ppt_image');
      });
      it('Word host 工具表含 generate_word_image（Plan 16-03 注册后通过）', () => {
        const wordTools = buildToolsForHost('word');
        expect(wordTools.map((t) => t.name)).toContain('generate_word_image');
      });
    });
    ```

    注意：tools-host.test.ts 的 PPT/Word 含 generate_* 的用例当前会红（实现未就绪），用 `it.skip` 标注。Excel 不含的两条用例当前不需要实现文件，可直接通过（buildToolsForHost 已存在，Excel 工具表不会神奇地包含 Phase 16 新工具）。
  </action>
  <verify>
    <automated>npm test -- --run src/agent/operationLog.integration.test.ts; npm test -- --run src/store/chat.test.ts; npm test -- --run src/agent/tools/tools-host.test.ts</automated>
  </verify>
  <done>
    三个文件修改/新建完成；`npm test -- --run` 全量通过（PPT/Word host 含 generate_* 的用例 skip，NFR-09 路径 C 和 operationLog 两条新用例通过，Excel 守门两条通过）。
    acceptance_criteria:
    - `grep -c "generate_ppt_image.*rolled_back\|Phase 16.*generate_ppt_image" src/agent/operationLog.integration.test.ts` 返回 ≥1
    - `grep -c "generate_word_image.*skipped_error\|Phase 16.*generate_word_image" src/agent/operationLog.integration.test.ts` 返回 ≥1
    - `grep -c "NFR-09 路径 C\|preview_pending" src/store/chat.test.ts` 返回 ≥1
    - `grep -c "generate_ppt_image\|generate_word_image" src/agent/tools/tools-host.test.ts` 返回 ≥2
    - `npm test -- --run src/agent/operationLog.integration.test.ts` 退出 0
    - `npm test -- --run src/store/chat.test.ts` 退出 0
    - `npm test -- --run src/agent/tools/tools-host.test.ts` 退出 0（skip 不算红）
    - `npm test -- --run` 全量退出 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → mock | 测试文件用 vi.mock 替代真实 AihubmixImageClient，隔离网络调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-W0 | Information Disclosure | chat.test.ts NFR-09 路径 C | mitigate | 路径 C 断言 base64 字符串不出现在 serializeForStorage 输出——守门脚手架本身就是缓解措施 |
| T-16-02-W0 | Spoofing | vi.mock AihubmixImageClient | accept | mock 仅在测试环境使用；生产代码不受影响 |
</threat_model>

<verification>
`npm test -- --run` 全量通过（约 773 + 新增 ~8 个用例）。
新增测试文件可被 Vitest 扫描到。
所有 describe.skip / it.skip 不导致 CI 红。
</verification>

<success_criteria>
1. 5 个测试文件存在（3 新建，2 追加）
2. `npm test -- --run` 全量通过
3. Wave 0 gaps 清单（16-VALIDATION.md）全部对勾
4. operationLog.integration.test.ts 含 generate_ppt_image 和 generate_word_image 守门用例
</success_criteria>

<output>
完成后创建 `.planning/phases/16-img-ppt-word/16-01-SUMMARY.md`
</output>

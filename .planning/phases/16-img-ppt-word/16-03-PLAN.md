---
phase: 16-img-ppt-word
plan: 03
type: execute
wave: 2
depends_on:
  - "16-02"
files_modified:
  - src/agent/tools/write/ppt-image.ts
  - src/agent/tools/write/word-image.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-05

must_haves:
  truths:
    - "generate_ppt_image ToolDef 存在：name=snake_case + kind='write' + execute 只调 AihubmixImageClient.generate，返回 preview_pending:true，reverse=undefined"
    - "generate_word_image ToolDef 存在：同上结构，返回 preview_pending:true，reverse=undefined"
    - "PPT_TOOLS Set 含 'generate_ppt_image'"
    - "buildToolsForHost('ppt') 返回包含 generate_ppt_image 的工具表"
    - "buildToolsForHost('word') 返回包含 generate_word_image 的工具表"
    - "buildToolsForHost('excel') 返回的工具表不含两个生图工具（IMG-05）"
    - "ppt-image.test.ts 和 word-image.test.ts 去掉 describe.skip 后全量通过"
    - "tools-host.test.ts 全量通过（含 PPT/Word host 含生图工具的用例）"
  artifacts:
    - path: "src/agent/tools/write/ppt-image.ts"
      provides: "generate_ppt_image ToolDef"
      exports: ["generatePptImageTool"]
      contains: "preview_pending"
    - path: "src/agent/tools/write/word-image.ts"
      provides: "generate_word_image ToolDef"
      exports: ["generateWordImageTool"]
      contains: "preview_pending"
    - path: "src/agent/tools/index.ts"
      provides: "PPT_TOOLS + buildToolsForHost 注册"
      contains: "generate_ppt_image"
  key_links:
    - from: "src/agent/tools/write/ppt-image.ts"
      to: "src/providers/aihubmix-image.ts"
      via: "new AihubmixImageClient().generate()"
      pattern: "AihubmixImageClient"
    - from: "src/agent/tools/index.ts"
      to: "src/agent/tools/write/ppt-image.ts"
      via: "import generatePptImageTool"
      pattern: "generate_ppt_image"
    - from: "src/agent/tools/index.ts"
      to: "PPT_TOOLS Set"
      via: "PPT_TOOLS.add('generate_ppt_image')"
      pattern: "PPT_TOOLS"
---

<objective>
定义 `generate_ppt_image` 和 `generate_word_image` 两个 write tool（D-02 解耦架构：execute 只生成预览、不插入文档），并注册到 per-host 工具表（IMG-05 Excel 不含）。

**Purpose:** 让 agent loop 能调用生图工具，LLM 收到工具定义；执行后把 base64 放在 ToolResult.data 供预览卡渲染，不直接插入文档。

**Output:** ppt-image.ts + word-image.ts + tools/index.ts 更新；Wave 0 skip 解除后守门测试全量通过。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-img-ppt-word/16-CONTEXT.md
@.planning/phases/16-img-ppt-word/16-PATTERNS.md
@.planning/phases/16-img-ppt-word/16-RESEARCH.md

<interfaces>
<!-- 工具定义需要的接口，从 codebase 提取 -->

来自 src/agent/tools/index.ts（ToolDef 接口，L115–130）：
```typescript
export interface ToolDef<TArgs = unknown> {
  name: string;
  kind: 'read' | 'write';
  description: string;
  parameters: Record<string, unknown>;
  humanLabel: (args: unknown) => string;
  execute(args: TArgs, ctx: ToolContext): Promise<ToolResult>;
}
export interface ToolContext {
  adapter: DocumentAdapter;
  getDefaultLLM: () => ProviderConfig;
  signal?: AbortSignal;
  runId: string;
  stepIndex: number;
}
```

来自 src/providers/registry.ts（ProviderRegistry.resolve image-gen + IMAGE_GEN_MODELS）：
```typescript
export const IMAGE_GEN_MODELS: ImageGenModel[] = [
  { id: 'doubao-seedream-5.0-lite', label: 'Doubao SeedDream 5.0 Lite（快速默认）', endpointKind: 'predictions', authKind: 'bearer', isDefault: true },
  { id: 'gpt-image-2', label: 'GPT-Image-2（高质量）', endpointKind: 'predictions', authKind: 'bearer', isDefault: false },
  { id: 'gemini-3.1-flash-image-preview', label: 'Gemini 3.1 Flash Image Preview', endpointKind: 'gemini', authKind: 'goog-api-key', isDefault: false },
];
export const DEFAULT_IMAGE_GEN_MODEL = IMAGE_GEN_MODELS.find((m) => m.isDefault)!;

// resolve image-gen 路径（L125–136）：
case 'image-gen': {
  const apiKey = storage.get<string>(STORAGE_KEYS.KEY_PREFIX + AIHUBMIX_PROVIDER_ID);
  if (!apiKey) throw new KeyInvalidError('aihubmix Key 未配置...');
  return {
    providerId: `${AIHUBMIX_PROVIDER_ID}-image`,
    baseURL: AIHUBMIX_IMAGE_BASE_URL,
    apiKey,
    model: DEFAULT_IMAGE_GEN_MODEL.id,  // 当前硬编码默认，Plan 16-04 扩展为读 localStorage
  } satisfies ImageConfig;
}
```

来自 src/providers/aihubmix-image.ts（AihubmixImageClient.generate 签名）：
```typescript
export class AihubmixImageClient implements ImageProvider {
  async generate(
    prompt: string,
    config: ImageConfig,
    _options?: ImageGenOptions,
  ): Promise<ImageGenResult>;
}
export interface ImageGenOptions { size?: string; quality?: 'high' | 'medium' | 'low' | 'auto'; }
```

来自 src/providers/types.ts（ImageGenResult）：
```typescript
export interface ImageGenResult { base64: string; mimeType: string; }
```

来自 src/agent/tools/index.ts PPT_TOOLS Set（L28–42）：
```typescript
const PPT_TOOLS = new Set([
  'insert_slide', 'set_shape_property', 'move_shape', 'set_shape_text',
  'set_shape_text_font', 'add_shape', 'copy_slide', 'set_shape_text_alignment',
  'delete_shape', 'rotate_shape', 'manage_slides', 'set_slide_background',
  'get_shape_image', // Phase 15
  // Phase 16：需加入 'generate_ppt_image'
]);
```

来自 src/errors.ts：
```typescript
export class KeyInvalidError extends AsterError { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 ppt-image.ts + word-image.ts 工具定义</name>
  <files>
    src/agent/tools/write/ppt-image.ts
    src/agent/tools/write/word-image.ts
  </files>
  <read_first>
    - src/agent/tools/write/ppt.ts — addShapeTool 完整定义（L364–420）：imports 路径、ToolDef 结构、humanLabel 函数、execute 签名、错误处理范式
    - src/agent/tools/write/word.ts — appendParagraph ToolDef（L46–75）：Word tool imports 范式
    - src/providers/aihubmix-image.ts — **完整文件**：① L15-18 `ImageGenOptions { size?; quality? }` 接口；② L21-24 `generate(prompt, config, _options?)` 签名；③ 三个私有方法 `_generateDoubao`/`_generateGptImage2`/`_generateGemini` 的 fetch 调用位置；④ `fetchUrlToBase64` 签名（B2：需对这 4 处 fetch 各加 signal 参数）
    - src/providers/registry.ts — ProviderRegistry.resolve + image-gen case（L92–136）+ IMAGE_GEN_MODELS（L49–72）+ DEFAULT_IMAGE_GEN_MODEL（L75）
    - src/providers/types.ts — ImageGenResult、ImageConfig 接口定义
    - src/errors.ts — KeyInvalidError 类（import 路径）
    - src/lib/storage.ts — STORAGE_KEYS 常量（L19–44）（用于读取 aster:pref:image-gen-model 自定义 model，Plan 16-04 才写入，此处做读取兜底）
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — Pattern 1（ppt-image.ts 完整结构 L29–96）+ Pattern 2（word-image.ts L100–142）
    - .planning/phases/16-img-ppt-word/16-RESEARCH.md — §Pattern 1 生图工具定义（L188–229）
    - src/agent/tools/write/ppt-image.test.ts — 刚在 Plan 16-01 建的测试文件，了解 mock 结构
  </read_first>
  <behavior>
    - generate_ppt_image execute：调 AihubmixImageClient.generate(prompt, config, { signal: ctx.signal }) → 返回 {ok:true, data:{base64, mimeType, prompt, preview_pending:true}}，reverse=undefined
    - generate_ppt_image execute：aihubmix key 未配置（KeyInvalidError）→ 返回 {ok:false, error:{code:'PERMISSION_DENIED', recoverable:false, hint:'请在设置中填写 aihubmix Key'}}
    - generate_ppt_image execute：provider.generate 抛错（网络失败/超时/AbortError）→ 返回 {ok:false, error:{code:'HOST_API_FAILED', recoverable:true}}（AbortError 是取消，recoverable=false 更准确，但不影响 D-08）
    - generate_word_image execute：与 ppt 对称（同三条行为）
    - AihubmixImageClient（B2 D-08）：`ImageGenOptions` 新增 `signal?: AbortSignal`；`generate` 形参 `_options` 改为 `options`；三个私有生图方法各收 `signal` 并透传到 `fetch(url, { ..., signal })`；`fetchUrlToBase64` 同样加 `signal` 参数
  </behavior>
  <action>
    **`src/agent/tools/write/ppt-image.ts`**（严格按 PATTERNS.md Pattern 1 实现）：

    ```typescript
    /**
     * generate_ppt_image — PPT 生图工具（IMG-01）
     *
     * D-02 解耦：execute 只调 AihubmixImageClient.generate，返回 preview_pending:true；
     * 不写文档、reverse=undefined。插入由预览卡按钮触发 insertImage helper。
     * D-03：prompt 由 agent 已扩写好传入（工具 description 中说明）。
     * IMG-05：此工具只注册到 PPT host（tools/index.ts buildToolsForHost ppt case）。
     */
    import { AihubmixImageClient } from '../../../providers/aihubmix-image';
    import { ProviderRegistry } from '../../../providers/registry';
    import { KeyInvalidError } from '../../../errors';
    import { storage } from '../../../lib/storage';
    import type { ToolDef, ToolResult } from '../index';
    import type { ImageConfig } from '../../../providers/types';

    const PREF_IMAGE_GEN_MODEL = 'aster:pref:image-gen-model';

    export const generatePptImageTool: ToolDef = {
      name: 'generate_ppt_image',  // snake_case，须加入 PPT_TOOLS Set（tools/index.ts）
      kind: 'write',
      description: '根据描述生成一张图片，准备插入当前 PPT 幻灯片（生成后弹出预览，用户确认后才实际插入）。' +
        '描述（prompt）请写具体中文，含主体、风格、构图细节——越具体出图质量越好。' +
        '注意：Excel 宿主无原生插图 API，不可在 Excel 中调用此工具。',
      parameters: {
        type: 'object',
        properties: {
          prompt: { type: 'string', description: '已扩写的中文图片描述（主体/风格/构图，尽量具体）' },
          model_id: { type: 'string', description: '生图模型 ID（可选；默认 doubao-seedream-5.0-lite；可选 gpt-image-2 / gemini-3.1-flash-image-preview）' },
        },
        required: ['prompt'],
      },
      humanLabel: (args) => {
        const a = args as Record<string, unknown>;
        const prompt = String(a.prompt ?? '');
        return `生成图片：${prompt.slice(0, 20)}${prompt.length > 20 ? '…' : ''}`;
      },
      async execute(args, ctx): Promise<ToolResult> {
        const a = args as Record<string, unknown>;
        const prompt = a.prompt as string;
        if (!prompt?.trim()) {
          return { ok: false, error: { code: 'INVALID_ARGS', message: '请提供图片描述（prompt）', recoverable: true, hint: '缺少必填参数 prompt' } };
        }

        // 读取用户持久选择的 model（D-04）；fallback 到 registry 默认
        const preferredModelId = storage.get<string>(PREF_IMAGE_GEN_MODEL);
        const modelIdFromArgs = a.model_id as string | undefined;
        // 优先级：工具 args > 用户 Settings 持久设置 > registry 默认
        const modelId = modelIdFromArgs ?? preferredModelId ?? undefined;

        // 从 registry 解析 image-gen 配置（读 aihubmix apiKey）
        let config: ImageConfig;
        try {
          config = ProviderRegistry.resolve('image-gen', ctx.getDefaultLLM) as ImageConfig;
          // 若用户指定了 model，覆盖 config.model
          if (modelId) config = { ...config, model: modelId };
        } catch (err) {
          if (err instanceof KeyInvalidError) {
            return { ok: false, error: { code: 'PERMISSION_DENIED', message: 'aihubmix Key 未配置，请在设置中填写 Key', recoverable: false, hint: '前往设置 → aihubmix → 填写 API Key' } };
          }
          return { ok: false, error: { code: 'HOST_API_FAILED', message: '生图配置解析失败', recoverable: false, hint: '检查 aihubmix Provider 配置' } };
        }

        // 调 AihubmixImageClient（生图，非流式）
        let result: { base64: string; mimeType: string };
        try {
          // D-08：传 ctx.signal 实现真取消（B2 已扩展 AihubmixImageClient.generate 支持 signal）
          result = await new AihubmixImageClient().generate(prompt, config, { signal: ctx.signal });
        } catch {
          return { ok: false, error: { code: 'HOST_API_FAILED', message: '图片生成失败，请重试（网络失败或超时）', recoverable: true, hint: '可能是网络问题或 model 超时，切换 model 或稍后重试' } };
        }

        // D-02：不写文档，只返回 base64 预览数据（内存态，不进 message.content，NFR-09）
        return {
          ok: true,
          data: {
            base64: result.base64,        // NFR-09：UI 层消费，不进 serializeForStorage
            mimeType: result.mimeType,
            prompt,
            preview_pending: true,        // ImagePreviewCard 渲染信号（Plan 16-05）
          },
          // reverse: undefined — 生图工具本身不写文档；插入动作的 reverse 由 insertImage helper 的 appendOperation 设置
        };
      },
    };
    ```

    **B2 D-08 取消能力扩展（`aihubmix-image.ts`）—— 在建立 ppt-image.ts 之前先做此改动：**

    扩展 `src/providers/aihubmix-image.ts`（四处改动，不改已有功能）：

    ① `ImageGenOptions` 接口增加 `signal`（L15-18 附近）：
    ```typescript
    export interface ImageGenOptions {
      size?: string;
      quality?: 'high' | 'medium' | 'low' | 'auto';
      signal?: AbortSignal;  // D-08：真取消，透传给 fetch
    }
    ```

    ② `generate` 方法形参 `_options` 改为 `options`（L21-24 附近，去掉下划线前缀）：
    ```typescript
    async generate(
      prompt: string,
      config: ImageConfig,
      options?: ImageGenOptions,
    ): Promise<ImageGenResult>
    ```
    在方法体内把 `options` 传给私有方法：`this._generateDoubao(prompt, config, options)`，以此类推。

    ③ 三个私有方法各加 `options?: ImageGenOptions` 参数，并在 `fetch(url, init)` 加 `signal: options?.signal`：
    ```typescript
    private async _generateDoubao(prompt: string, config: ImageConfig, options?: ImageGenOptions): Promise<ImageGenResult> {
      // 找到 fetch 调用，加 signal
      const res = await fetch(url, { ...init, signal: options?.signal });
      // fetchUrlToBase64 调用：
      return this._fetchUrlToBase64(imageUrl, options?.signal);
    }
    private async _generateGptImage2(prompt: string, config: ImageConfig, options?: ImageGenOptions): Promise<ImageGenResult> {
      const res = await fetch(url, { ...init, signal: options?.signal });
    }
    private async _generateGemini(prompt: string, config: ImageConfig, options?: ImageGenOptions): Promise<ImageGenResult> {
      const res = await fetch(url, { ...init, signal: options?.signal });
    }
    ```

    ④ `fetchUrlToBase64`（doubao URL 转 base64 用）加 signal 参数：
    ```typescript
    private async _fetchUrlToBase64(url: string, signal?: AbortSignal): Promise<string> {
      const res = await fetch(url, { signal });
      // ... 原有 base64 转换逻辑不变
    }
    ```

    **这是最小侵入扩展**：`ImageGenOptions` 向后兼容（signal 可选），旧调用方 `generate(prompt, config)` 不受影响。

    **`src/agent/tools/write/word-image.ts`**（与 ppt-image.ts 对称）：
    - name: `'generate_word_image'`（注意：不加入 PPT_TOOLS，Word 工具不需要）
    - kind: 'write'
    - description: 含「适用 Word 宿主」说明
    - execute: 与 ppt-image.ts 完全对称（同样 D-02 解耦，只调 AihubmixImageClient，返回 preview_pending:true，reverse=undefined）
    - 三类错误处理与 ppt-image.ts 对称

    **Plan 16-01 建的 test stub 此时去掉 skip**：
    读 `src/agent/tools/write/ppt-image.test.ts` 和 `word-image.test.ts`，删除 `describe.skip` 的 `.skip`，保留 `describe`。
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/ppt-image.test.ts; npm test -- --run src/agent/tools/write/word-image.test.ts</automated>
  </verify>
  <done>
    aihubmix-image.ts signal 已扩展；两个工具文件存在；Plan 16-01 的测试全量通过（describe.skip 已去掉）。
    acceptance_criteria:
    - `grep -c "signal.*AbortSignal\|AbortSignal.*signal" src/providers/aihubmix-image.ts` 返回 ≥1（B2：ImageGenOptions 含 signal 字段）
    - `grep -c "options?.signal\|signal: options" src/providers/aihubmix-image.ts` 返回 ≥3（三个私有方法各传 signal）
    - `grep -c "ctx.signal" src/agent/tools/write/ppt-image.ts` 返回 ≥1（D-08 工具传 signal）
    - `grep -c "generate_ppt_image" src/agent/tools/write/ppt-image.ts` 返回 ≥2
    - `grep -c "preview_pending" src/agent/tools/write/ppt-image.ts` 返回 ≥1
    - `grep -c "reverse" src/agent/tools/write/ppt-image.ts` 返回 0（不设 reverse）或仅注释
    - `grep -c "generate_word_image" src/agent/tools/write/word-image.ts` 返回 ≥2
    - `grep -v '^//' src/agent/tools/write/ppt-image.test.ts | grep -c "describe\.skip"` 返回 0（skip 已去掉）
    - `npm test -- --run src/agent/tools/write/ppt-image.test.ts` 退出 0
    - `npm test -- --run src/agent/tools/write/word-image.test.ts` 退出 0
    - `npm run build 2>&1 | grep -v '^#' | grep -c "error TS"` 返回 0（aihubmix-image.ts 扩展不引入类型错误）
  </done>
</task>

<task type="auto">
  <name>Task 2: 注册工具到 tools/index.ts（PPT_TOOLS + buildToolsForHost）</name>
  <files>
    src/agent/tools/index.ts
  </files>
  <read_first>
    - src/agent/tools/index.ts — PPT_TOOLS Set（L28–42）；normalizeToSnakeCase 函数（L44–53）；buildToolsForHost 完整函数（L244–300）；现有 PPT write tools imports（L1–25）
    - src/agent/tools/write/ppt-image.ts — generatePptImageTool export name（刚建）
    - src/agent/tools/write/word-image.ts — generateWordImageTool export name（刚建）
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — §tools/index.ts PPT_TOOLS + buildToolsForHost 目标形态（L501–553）
  </read_first>
  <action>
    **3 处改动，不改其他逻辑**：

    1. **文件顶部 imports**（紧接现有 write tool imports 后面加两行）：
    ```typescript
    import { generatePptImageTool } from './write/ppt-image';
    import { generateWordImageTool } from './write/word-image';
    ```

    2. **PPT_TOOLS Set**（L28–42，加入 generate_ppt_image）：
    ```typescript
    const PPT_TOOLS = new Set([
      // ... 现有工具名不改 ...
      'get_shape_image', // Phase 15 VIS-01/VIS-02 新增
      'generate_ppt_image', // Phase 16 IMG-01（必须在此，否则 normalizeToSnakeCase 不处理其参数，casing 静默失败）
    ]);
    ```
    注意：`generate_word_image` 是 Word 工具，**不加入** PPT_TOOLS（Word 工具无 casing normalize 需要）。

    3. **buildToolsForHost**：
    - `case 'word'` 的 wordWriteTools 数组末尾加 `generateWordImageTool`（在 batchWrite 之前，顺序任意）
    - `case 'ppt'` 的 pptWriteTools 数组末尾加 `generatePptImageTool`（在 batchWrite 之前）
    - `case 'excel'` **不改**（IMG-05：Excel 无插图 API）

    完整 diff 示意（不是实际 diff，是逻辑变更说明）：
    ```
    case 'word': wordWriteTools = [...现有..., generateWordImageTool, batchWrite]
    case 'ppt': pptWriteTools = [...现有..., generatePptImageTool, batchWrite]
    case 'excel': 不变
    ```
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/tools-host.test.ts; npm test -- --run</automated>
  </verify>
  <done>
    tools/index.ts 更新；tools-host.test.ts 全量通过（含 PPT/Word host 含生图工具的用例，去掉 it.skip）；全量测试通过。
    acceptance_criteria:
    - `grep -c "generate_ppt_image" src/agent/tools/index.ts` 返回 ≥3（import + PPT_TOOLS + buildToolsForHost）
    - `grep -c "generate_word_image" src/agent/tools/index.ts` 返回 ≥2（import + buildToolsForHost word case）
    - `grep -c "excel.*generate_ppt\|generate_ppt.*excel" src/agent/tools/index.ts` 返回 0（Excel case 不含）
    - `npm test -- --run src/agent/tools/tools-host.test.ts` 退出 0（所有 4 条用例通过，it.skip 已去掉）
    - `npm test -- --run` 全量退出 0
    - `npm run build 2>&1 | grep -v '^#' | grep -c "error TS"` 返回 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → agent loop → tool.execute | LLM 传入的 prompt 参数是用户原意 + agent 扩写的中文文本，纯文本，不含 HTML |
| tool.execute → AihubmixImageClient | apiKey 从 ProviderRegistry.resolve 读取（只进 Authorization header，不进 config 对象外层，继承 T-14-01） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | generate_ppt_image prompt 参数 | accept | agent 扩写 prompt 是 LLM 自生成文本（非 document evidence），不含特权指令；风险低（LLM01 injection 无特权上下文） |
| T-16-07 | Information Disclosure | ToolResult.data.base64 | mitigate | base64 只在 ToolResult.data 内存态存活；execute 不把 base64 存入任何持久化路径；NFR-09 路径 C 守门验证（Plan 16-01 已建）|
| T-16-08 | Information Disclosure | KeyInvalidError → ToolResult.error.message | mitigate | error.message 用字面量中文（'aihubmix Key 未配置...'），不 interpolate err.message（防 key 从错误链出现） |
| T-16-09 | Spoofing | model_id 参数可被 LLM 任意传入 | accept | model_id 传给 AihubmixImageClient.generate；provider 会抛 NetworkError（未知 model）；无安全越权风险（apiKey 已验证） |
</threat_model>

<verification>
- `npm test -- --run src/agent/tools/write/ppt-image.test.ts` 通过
- `npm test -- --run src/agent/tools/write/word-image.test.ts` 通过
- `npm test -- --run src/agent/tools/tools-host.test.ts` 通过（Excel 不含 + PPT/Word 含）
- `npm test -- --run` 全量通过（~773 + 新增用例）
- `npm run build` 无 TypeScript error
</verification>

<success_criteria>
1. generate_ppt_image / generate_word_image 工具定义存在，execute 返回 preview_pending:true，reverse=undefined
2. PPT_TOOLS Set 含 generate_ppt_image
3. buildToolsForHost(ppt) 含生图工具；buildToolsForHost(excel) 不含（IMG-05）
4. 全量测试通过（ppt-image.test.ts + word-image.test.ts skip 已解除，tools-host.test.ts it.skip 已解除）
</success_criteria>

<output>
完成后创建 `.planning/phases/16-img-ppt-word/16-03-SUMMARY.md`
</output>

---
phase: 16-img-ppt-word
plan: 02
type: execute
wave: 1
depends_on:
  - "16-01"
files_modified:
  - src/adapters/PptAdapter.ts
  - src/adapters/WordAdapter.ts
  - src/lib/insertImage.ts
autonomous: false
requirements:
  - IMG-01
  - IMG-02

must_haves:
  truths:
    - "PptAdapter.addImageShape 方法存在：接受 slideIndex + base64 + 位置对象，调 addGeometricShape('Rectangle') + fill.setImage，独立 run 回读确认 shape 存在"
    - "WordAdapter.insertBodyImage 方法存在：调 body.insertInlinePictureFromBase64(base64, 'End')，回读 picture.width/height 验证成功"
    - "insertImage helper 存在：host='ppt' 调 PptAdapter.addImageShape + 写后回读 + appendOperation（delete_shape_by_id reverse）；host='word' 调 WordAdapter.insertBodyImage + appendOperation（noop_inverse）"
    - "PPT 插图 GA 路线（addGeometricShape + fill.setImage）真机 spike：真机 UAT PASS（human-verify checkpoint）"
    - "reverse.args 全部是 Record 对象，非位置参"
  artifacts:
    - path: "src/adapters/PptAdapter.ts"
      provides: "addImageShape 方法"
      contains: "addImageShape"
    - path: "src/adapters/WordAdapter.ts"
      provides: "insertBodyImage 方法"
      contains: "insertBodyImage"
    - path: "src/lib/insertImage.ts"
      provides: "统一插图入口 helper"
      exports: ["insertImage"]
  key_links:
    - from: "src/lib/insertImage.ts"
      to: "src/adapters/PptAdapter.ts"
      via: "pptAdapter.addImageShape()"
      pattern: "addImageShape"
    - from: "src/lib/insertImage.ts"
      to: "src/agent/operationLog.ts"
      via: "appendOperation({ reverse: { tool: 'delete_shape_by_id', args: {...} } })"
      pattern: "appendOperation"
    - from: "src/lib/insertImage.ts"
      to: "src/adapters/WordAdapter.ts"
      via: "wordAdapter.insertBodyImage()"
      pattern: "insertBodyImage"
---

<objective>
PPT/Word adapter 插图方法 + 可复用 `insertImage` helper，这是 Phase 16 的核心工程基础。

包含一个 **human-verify checkpoint**：真机 spike 验证 PPT GA 路线（`addGeometricShape + fill.setImage`）在 Office for Web 能否实际插图并回读 shape.id，以及 bug #5022 独立 run 规避是否有效。Spike 结论决定 PPT adapter 方法的最终实现形态（见 fallback 策略）。

**Purpose:** 实现后续 Plan 16-03/04/05 依赖的 adapter 方法和 insert helper 层。

**Output:** `PptAdapter.addImageShape` + `WordAdapter.insertBodyImage` + `src/lib/insertImage.ts`（供 Phase 18 LIB 复用的统一插图入口）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-img-ppt-word/16-CONTEXT.md
@.planning/phases/16-img-ppt-word/16-RESEARCH.md
@.planning/phases/16-img-ppt-word/16-PATTERNS.md

<interfaces>
<!-- 从 codebase 提取的关键接口，executor 直接使用 -->

来自 src/adapters/PptAdapter.ts（addShape 方法体，L1560–1643）：
```typescript
async addShape(
  slideIndex: number,
  shapeType: string,
  position: { left: number; top: number; width: number; height: number },
  text?: string,
): Promise<{ newShapeId: string }> {
  try {
    return await PowerPoint.run(async (ctx) => {
      const slides = ctx.presentation.slides;
      slides.load('items');
      await ctx.sync();
      const slide = slides.items[slideIndex - 1];
      slide.shapes.load('items/$none');
      await ctx.sync();
      const newShape = (slide.shapes as unknown as {
        addGeometricShape: (type: string, opts: object) => { load: (f: string[]) => void; id: string };
      }).addGeometricShape(shapeType, { left, top, width, height });
      newShape.load(['id', 'type']);
      await ctx.sync();
      const newShapeId = newShape.id as string;
      return { newShapeId };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('PPT addShape 失败', err);
  }
}
```

来自 src/adapters/PptAdapter.ts（deleteShapeById，L1653–1696）：
```typescript
async deleteShapeById(args: Record<string, unknown>): Promise<void> {
  const slide_index = args.slide_index as number;
  const shape_id = args.shape_id as string;
  // ...uses PowerPoint.run + slide.shapes.load + shape.delete()
}
```

来自 src/agent/operationLog.ts（appendOperation + OperationLogEntry，L53–69, L180–184）：
```typescript
export interface OperationLogEntry {
  runId: string;
  stepIndex: number;
  toolName: string;
  args: unknown;
  humanLabel: string;
  reverse: ReverseDescriptor;
  postState?: PostStateSnapshot;
  timestamp: number;
  subOps?: Array<...>;
}
export function appendOperation(entry: OperationLogEntry): void;
export function getOperationsByRun(runId: string): OperationLogEntry[];
```

来自 src/errors.ts（HostApiError）：
```typescript
export class HostApiError extends AsterError { ... }
export class KeyInvalidError extends AsterError { ... }
```

来自 src/lib/storage.ts（STORAGE_KEYS）：
```typescript
export const STORAGE_KEYS = {
  KEY_PREFIX: 'aster:keys:',
  // ... 无 image-gen-model key，需新增到 STORAGE_KEYS 或直接用字面量
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PptAdapter.addImageShape + WordAdapter.insertBodyImage</name>
  <files>
    src/adapters/PptAdapter.ts
    src/adapters/WordAdapter.ts
  </files>
  <read_first>
    - src/adapters/PptAdapter.ts — addShape 完整方法体（L1560–1643）+ deleteShapeById（L1653–1696）；文件顶部 imports
    - src/adapters/WordAdapter.ts — appendParagraph 等 write 方法体（确认 Word.run 范式 + HostApiError 抛法）；文件顶部 imports
    - .planning/phases/16-img-ppt-word/16-RESEARCH.md — PPT 插图 GA 路线代码示例（L464–493）；Word 插图代码示例（L448–459）；Pitfall 2（#5022 独立 run 规避）
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — PptAdapter addImageShape 差异点（L222–227）；WordAdapter insertBodyImage 目标实现（L253–268）
  </read_first>
  <action>
    **PptAdapter.ts — 新增 `addImageShape` 方法**（紧接 addShape 方法之后，L1644 附近）：

    签名：
    ```typescript
    async addImageShape(
      slideIndex: number,
      base64: string,
      opts: { left: number; top: number; width: number; height: number },
    ): Promise<{ newShapeId: string }>
    ```

    实现要点（严格按 RESEARCH §PPT 插图 GA 路线 + Pitfall 2 规避）：

    1. 第一次 `PowerPoint.run`：load slides → 获取 slideIndex-1 的 slide → `addGeometricShape('Rectangle', opts)` → `shape.load(['id'])` → `await ctx.sync()` → 取 `newShapeId = shape.id` → `shape.fill.setImage(base64)` → `await ctx.sync()` → return newShapeId。

    2. **写后回读验证（独立 `PowerPoint.run`，规避 bug #5022 sync 卡死）**：第二次独立 `PowerPoint.run`：load `slides.items[slideIndex-1].shapes.items/id` → `await ctx.sync()` → 检查 `shapes.items.some(s => s.id === newShapeId)`；若未找到：throw `new HostApiError('PPT 图片插入未生效（回读验证失败），请重试', undefined)`。

    3. `fill.setImage(base64)` 入参是**裸 base64**（Provider 返回裸 base64，无 `data:` 前缀；若真机 spike 发现需要 data URL 格式，在此处加 `data:image/png;base64,${base64}` 拼接逻辑，并在 SUMMARY 记录）。

    4. 错误包装：`catch (err) { if (err instanceof HostApiError) throw err; throw new HostApiError('PPT addImageShape 失败', err); }`

    **WordAdapter.ts — 新增 `insertBodyImage` 方法**（文件 write methods 区域末尾）：

    签名：
    ```typescript
    async insertBodyImage(base64: string): Promise<{ width: number; height: number }>
    ```

    实现（D-07 body 级，非 range）：
    ```typescript
    try {
      return await Word.run(async (ctx) => {
        const picture = ctx.document.body.insertInlinePictureFromBase64(base64, 'End');
        picture.load(['width', 'height']);
        await ctx.sync();  // 回读验证图片尺寸
        return { width: picture.width, height: picture.height };
      });
    } catch (err) {
      if (err instanceof HostApiError) throw err;
      throw new HostApiError('Word insertBodyImage 失败', err);
    }
    ```

    **两个方法都不加入 DocumentAdapterForReplay interface**：
    - `addImageShape` 的 inverse 是已有 `deleteShapeById`（直接复用，不新增接口方法）
    - `insertBodyImage` 的 inverse 是 `noop_inverse`（operationLog.ts L534–537 通用处理，无需 adapter 方法）
  </action>
  <verify>
    <automated>npm test -- --run src/agent/operationLog.integration.test.ts</automated>
  </verify>
  <done>
    两个 adapter 方法存在。operationLog.integration.test.ts 中 Phase 16 守门用例通过（delete_shape_by_id rolled_back + noop_inverse skipped_error）。
    acceptance_criteria:
    - `grep -c "addImageShape" src/adapters/PptAdapter.ts` 返回 ≥2（声明+调用）
    - `grep -c "insertBodyImage" src/adapters/WordAdapter.ts` 返回 ≥2
    - `grep -c "setImage\|fill\.setImage" src/adapters/PptAdapter.ts` 返回 ≥1
    - `grep -c "insertInlinePictureFromBase64" src/adapters/WordAdapter.ts` 返回 ≥1
    - `npm test -- --run src/agent/operationLog.integration.test.ts` 退出 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Spike 验证：PPT GA 路线真机可用 + Word body 级可用</name>
  <read_first>
    - .planning/phases/16-img-ppt-word/16-RESEARCH.md — Open Question 1（fill.setImage 裸 base64 vs data URL）、Open Question 2（bug #5022 规避有效性）
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — PPT 插图代码示例（L465–493）
    - .claude/skills/office-addin-browser-uat/SKILL.md — 真机 UAT 操作范式
  </read_first>
  <what-built>
    PptAdapter.addImageShape（addGeometricShape + fill.setImage + 独立 run 回读）和 WordAdapter.insertBodyImage（body 级）已实现。需在 Office for Web 真机验证：
    1. PPT GA 路线是否能实际插入图片并回读 shape.id
    2. 独立 PowerPoint.run 回读是否规避了 bug #5022 sync 卡死
    3. fill.setImage 接受裸 base64 还是需要 data URL 格式
    4. Word body 级是否正常（range 级已知 bug #3434）
  </what-built>
  <how-to-verify>
    **PPT 真机 spike（浏览器 devtools console）：**
    1. 打开 Office for Web PPT，sideload Aster add-in
    2. 在 Aster task pane 打开 devtools console
    3. 粘贴测试代码（替换 BASE64_SMALL 为任意 ~100 char base64 png 字符串）：
    ```javascript
    // 测试 addGeometricShape + fill.setImage + 回读
    await PowerPoint.run(async ctx => {
      const slide = ctx.presentation.getSelectedSlides().getItemAt(0);
      const shape = slide.shapes.addGeometricShape('Rectangle', { left: 100, top: 100, width: 200, height: 150 });
      shape.load('id');
      await ctx.sync();
      const id = shape.id;
      console.log('shape.id =', id);
      shape.fill.setImage('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==');
      await ctx.sync();
      console.log('setImage sync done (no hang = bug #5022 OK)');
      return id;
    });
    // 独立 run 回读
    const verified = await PowerPoint.run(async ctx => {
      const slide = ctx.presentation.getSelectedSlides().getItemAt(0);
      slide.shapes.load('items/id,items/fill/type');
      await ctx.sync();
      const found = slide.shapes.items.find(s => s.id === '上面输出的 shape.id');
      console.log('found shape:', !!found, found?.fill?.type);
      return !!found;
    });
    console.log('verified =', verified);
    ```
    4. 期望：slide 出现矩形图形（可能显示颜色或图片）；shape.id 有值；sync 不挂死；回读 found=true
    5. 若 setImage 需要 data URL 格式：改用 `'data:image/png;base64,iVBOR...'` 测试

    **Word 真机验证（更简单）：**
    1. 打开 Office for Web Word，console 粘贴：
    ```javascript
    await Word.run(async ctx => {
      const pic = ctx.document.body.insertInlinePictureFromBase64('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==', 'End');
      pic.load(['width', 'height']);
      await ctx.sync();
      console.log('word pic width =', pic.width, 'height =', pic.height);
    });
    ```
    2. 期望：文档末尾出现 1x1 像素图；width/height 非零

    **记录结论并根据结论修正 PptAdapter.addImageShape 实现**：
    - 若裸 base64 不行：在 addImageShape 内加 `'data:image/png;base64,' + base64` 拼接
    - 若独立 run 回读卡死（bug #5022 复现）：改回读为 shape outline width 等不触发 fill 的属性
    - 若 GA 路线完全失败：在 SUMMARY 记录，在 addImageShape 改为诚实 HostApiError（fallback 策略）
  </how-to-verify>
  <resume-signal>
    输入 "spike-pass: [裸base64/dataurl] [无卡死/卡死但已修复/GA失败]"，附简短结论，executor 据此更新 PptAdapter.addImageShape 实现（若需要）后继续 Task 3。
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: 新建 src/lib/insertImage.ts（insert helper）</name>
  <files>
    src/lib/insertImage.ts
  </files>
  <read_first>
    - src/agent/operationLog.ts — appendOperation 签名（L180–184）+ OperationLogEntry 接口（L53–69）+ getOperationsByRun（L186）+ PostStateSnapshot 类型（L43）
    - src/adapters/PptAdapter.ts — addImageShape 方法（刚新增，见 Task 1）
    - src/adapters/WordAdapter.ts — insertBodyImage 方法（刚新增，见 Task 1）
    - .planning/phases/16-img-ppt-word/16-RESEARCH.md — Pattern 3（insertImage helper 完整代码，L285–342）+ Pattern 4（operationLog 手动追加，L345–364）+ Pitfall 5（stepIndex 冲突防范）
    - .planning/phases/16-img-ppt-word/16-PATTERNS.md — §insertImage.ts 目标实现（L306–333 appendOperation 调用形态）
  </read_first>
  <action>
    新建 `src/lib/insertImage.ts`，这是 Phase 16 的关键抽象——供 IMG-01/02 预览卡按钮、以及 Phase 18 LIB 图库检索共用的统一插图入口。

    完整结构（不要偷懒，严格实现以下约定）：

    ```typescript
    /**
     * insertImage — 统一插图 helper（IMG-01/02 + Phase 18 LIB 复用）
     *
     * 设计约定（D-02 解耦架构）：
     *   - 由预览卡按钮触发（脱离 agent loop）
     *   - 调对应 host adapter 插图方法 + 写后回读（PPT 必须）
     *   - 成功后手动 appendOperation（带 humanLabel + reverse descriptor）
     *   - base64 不进 reverse.args（NFR-09：base64 不持久化）
     *
     * PPT reverse: { tool: 'delete_shape_by_id', args: { slide_index, shape_id } }（Record 对象，非位置参）
     * Word reverse: { tool: 'noop_inverse', args: { reason: 'Word 图片插入暂不支持自动撤销' } }
     */
    import type { PptAdapter } from '../adapters/PptAdapter';
    import type { WordAdapter } from '../adapters/WordAdapter';
    import { appendOperation, getOperationsByRun } from '../agent/operationLog';
    import type { ToolError } from '../agent/tools/index';

    export interface InsertImageOpts {
      slideIndex?: number;   // PPT 用，1-based
      runId: string;
      humanLabel: string;
    }

    export interface InsertImageResult {
      ok: boolean;
      shapeId?: string;     // PPT 成功时返回，供 UI 确认展示
      error?: ToolError;
    }

    /**
     * PPT 插图：调 pptAdapter.addImageShape + 手动 appendOperation
     * 计算居中位置（D-06：10inch × 7.5inch slide = 720pt × 540pt；图按 4:3 放 480×360pt，留边距）
     */
    export async function insertImage(
      host: 'ppt',
      adapter: PptAdapter,
      base64: string,
      mimeType: string,
      opts: InsertImageOpts & { slideIndex: number },
    ): Promise<InsertImageResult>;

    export async function insertImage(
      host: 'word',
      adapter: WordAdapter,
      base64: string,
      mimeType: string,
      opts: InsertImageOpts,
    ): Promise<InsertImageResult>;

    export async function insertImage(
      host: 'ppt' | 'word',
      adapter: PptAdapter | WordAdapter,
      base64: string,
      _mimeType: string,
      opts: InsertImageOpts & { slideIndex?: number },
    ): Promise<InsertImageResult> {
      if (host === 'ppt') {
        const pptAdapter = adapter as PptAdapter;
        const slideIndex = opts.slideIndex!;
        // 居中默认位置（D-06）：slide 720pt × 540pt，图 480pt × 360pt，left=(720-480)/2=120, top=(540-360)/2=90
        const position = { left: 120, top: 90, width: 480, height: 360 };
        let shapeId: string;
        try {
          const result = await pptAdapter.addImageShape(slideIndex, base64, position);
          shapeId = result.newShapeId;
        } catch (err) {
          return {
            ok: false,
            error: {
              code: 'HOST_API_FAILED',
              message: 'PPT 图片插入失败，请重试',
              recoverable: false,
              hint: '宿主插图 API 失败；可能当前网页版暂不支持，建议手动插入图片',
            },
          };
        }
        // 手动追加 operationLog（D-02：绕过 dispatchTool 路径，必须手动）
        // stepIndex = 当前 run 已有条目数（Pitfall 5 防冲突）
        appendOperation({
          runId: opts.runId,
          stepIndex: getOperationsByRun(opts.runId).length,
          toolName: 'generate_ppt_image',
          args: {},   // 不存 base64（NFR-09）
          humanLabel: opts.humanLabel,
          reverse: {
            tool: 'delete_shape_by_id',
            args: { slide_index: slideIndex, shape_id: shapeId },  // Record 对象，非位置参
          },
          postState: { kind: 'ppt_shape_new', content: { slide_index: slideIndex, shape_id: shapeId } },
          timestamp: Date.now(),
        });
        return { ok: true, shapeId };
      } else {
        const wordAdapter = adapter as WordAdapter;
        try {
          await wordAdapter.insertBodyImage(base64);
        } catch (err) {
          return {
            ok: false,
            error: {
              code: 'HOST_API_FAILED',
              message: 'Word 图片插入失败，请重试',
              recoverable: false,
              hint: '宿主插图 API 失败；请确认当前文档可编辑',
            },
          };
        }
        appendOperation({
          runId: opts.runId,
          stepIndex: getOperationsByRun(opts.runId).length,
          toolName: 'generate_word_image',
          args: {},
          humanLabel: opts.humanLabel,
          reverse: { tool: 'noop_inverse', args: { reason: 'Word 图片插入暂不支持自动撤销' } },
          timestamp: Date.now(),
        });
        return { ok: true };
      }
    }
    ```

    注意：
    - `ToolError` 从 `'../agent/tools/index'` import type（不能反向 import agent 层；若有循环依赖，改为 inline 定义 ToolError 接口或从 `../errors` 引用 AsterError）
    - 检查 `src/agent/tools/index.ts` 是否 export ToolError；若不 export，在 insertImage.ts 内 inline 定义相同结构
    - `postState.kind` 用 `'ppt_shape_new'`（已在 PostStateSnapshot union 声明，Phase 10 加入）
  </action>
  <verify>
    <automated>npm test -- --run src/agent/operationLog.integration.test.ts; npm run build 2>&1 | grep -E "error|Error" | head -20</automated>
  </verify>
  <done>
    `src/lib/insertImage.ts` 存在；`npm run build` 无 TypeScript 错误；operationLog integration test 全量通过。
    acceptance_criteria:
    - `grep -c "export.*insertImage\|export async function insertImage" src/lib/insertImage.ts` 返回 ≥1
    - `grep -c "appendOperation" src/lib/insertImage.ts` 返回 ≥2（PPT + Word 两处）
    - `grep -c "delete_shape_by_id" src/lib/insertImage.ts` 返回 ≥1
    - `grep -c "noop_inverse" src/lib/insertImage.ts` 返回 ≥1
    - `grep -c "NFR-09\|args: {}" src/lib/insertImage.ts` 返回 ≥1（base64 不进 args）
    - `npm test -- --run src/agent/operationLog.integration.test.ts` 退出 0
    - `npm run build 2>&1 | grep -v '^#' | grep -c "error"` 返回 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| insertImage helper → PptAdapter | base64 仅在调用参数内传递，不写入任何持久存储 |
| insertImage helper → operationLog | reverse.args 不含 base64（NFR-09），只含 shape_id（字符串） |
| Office.js API → PPT 文档 | 写操作需回读验证（memory: project_ppt_officejs_gotchas） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | insertImage.ts reverse.args | mitigate | args: {} 不存 base64；shape_id 是 Office 生成的内部 UUID，低价值 |
| T-16-04 | Tampering | PPT 写后回读验证 | mitigate | 独立 PowerPoint.run 回读 shape.id；回读失败抛 HostApiError 诚实失败（不假成功） |
| T-16-05 | Information Disclosure | HostApiError message | mitigate | throw new HostApiError('PPT addImageShape 失败', err) — 字面量 message 不 interpolate err.message（防 apiKey 从错误链泄漏） |
</threat_model>

<verification>
- `npm test -- --run src/agent/operationLog.integration.test.ts` 通过（Phase 16 两条守门用例 + 既有 D-17 用例）
- `npm test -- --run` 全量通过
- `npm run build` 无 TypeScript 错误
- 真机 spike PASS（human-verify checkpoint 通过）
</verification>

<success_criteria>
1. PptAdapter.addImageShape 存在：GA 路线实现 + 独立 run 回读验证 + HostApiError 包装
2. WordAdapter.insertBodyImage 存在：body 级（非 range）+ 回读 width/height
3. src/lib/insertImage.ts 存在：统一接口供 Phase 18 复用；PPT reverse 含 delete_shape_by_id Record；Word reverse 含 noop_inverse
4. operationLog.integration.test.ts Phase 16 守门用例通过（generate_ppt_image rolled_back + generate_word_image skipped_error）
5. 真机 spike：PPT GA 路线能在 Office for Web 插图 + 回读 shape.id；结论记入 SUMMARY
</success_criteria>

<output>
完成后创建 `.planning/phases/16-img-ppt-word/16-02-SUMMARY.md`，记录：
- PPT spike 结论（裸 base64 vs data URL；bug #5022 规避是否有效；GA 路线是否成功）
- Word spike 结论
- 任何对 PptAdapter.addImageShape 实现的修正
</output>

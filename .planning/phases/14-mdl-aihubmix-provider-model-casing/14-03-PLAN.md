---
phase: 14-mdl-aihubmix-provider-model-casing
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
files_modified:
  - src/providers/registry.ts
  - src/providers/aihubmix-vision.ts
  - src/providers/registry.test.ts
autonomous: true
requirements:
  - MDL-02

must_haves:
  truths:
    - "registry.ts 新增 ImageGenModel 接口 + IMAGE_GEN_MODELS 三项列表 + DEFAULT_IMAGE_GEN_MODEL 常量"
    - "registry.ts 中 AIHUBMIX_VISION_MODEL 改为 'gpt-5.4'（推翻旧 gpt-5.1）"
    - "registry.ts 新增 AIHUBMIX_IMAGE_BASE_URL = 'https://aihubmix.com'（无 /v1 后缀），image-gen case 改用此 URL"
    - "registry.ts image-gen case 的默认 model 改为 DEFAULT_IMAGE_GEN_MODEL.id（'doubao-seedream-5.0-lite'）"
    - "aihubmix-vision.ts 的 body 里 model 从硬编码 'gpt-4o' 改为引用 AIHUBMIX_VISION_MODEL 常量（gpt-5.4）"
    - "registry.test.ts 断言已更新，npx vitest run src/providers/registry.test.ts 通过"
  artifacts:
    - path: "src/providers/registry.ts"
      provides: "ImageGenModel 接口 + IMAGE_GEN_MODELS 列表 + AIHUBMIX_IMAGE_BASE_URL + gpt-5.4 vision"
      contains: "IMAGE_GEN_MODELS"
    - path: "src/providers/aihubmix-vision.ts"
      provides: "vision model 对齐为 gpt-5.4（D-06）"
      contains: "AIHUBMIX_VISION_MODEL"
    - path: "src/providers/registry.test.ts"
      provides: "更新后的断言（gpt-5.4 + doubao-seedream-5.0-lite + IMAGE_GEN_MODELS）"
      exports: ["describe IMAGE_GEN_MODELS"]
  key_links:
    - from: "src/providers/registry.ts IMAGE_GEN_MODELS"
      to: "src/providers/aihubmix-image.ts（Plan 05）"
      via: "DEFAULT_IMAGE_GEN_MODEL.id 作为 image-gen case 的 model 路由入口"
      pattern: "DEFAULT_IMAGE_GEN_MODEL"
    - from: "src/providers/registry.ts AIHUBMIX_IMAGE_BASE_URL"
      to: "src/providers/aihubmix-image.ts（Plan 05）"
      via: "ImageConfig.baseURL 字段传入 image client"
      pattern: "AIHUBMIX_IMAGE_BASE_URL"
    - from: "src/providers/registry.ts AIHUBMIX_VISION_MODEL"
      to: "src/providers/aihubmix-vision.ts"
      via: "导入常量用于 body 的 model 字段"
      pattern: "AIHUBMIX_VISION_MODEL"
---

<objective>
修正 model 清单（D-05/D-06/D-07/D-08）：重整 registry.ts 的 model 常量和 image-gen resolve 路由，铺带 metadata 的三生图 model 列表（供 Phase 16 picker 消费），更新 aihubmix-vision.ts 对齐新 vision model，同步更新 registry.test.ts 断言。

Purpose: registry 是 model 路由的单一真相源。本 plan 完成后，image-gen case 路由到 doubao 默认 model，vision case 路由到 gpt-5.4，三生图 model 列表为 Phase 16 picker 铺好数据结构。

Output: 更新后的 registry.ts（新常量 + ImageGenModel 列表）、aihubmix-vision.ts（model 对齐）、registry.test.ts（断言更新）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-CONTEXT.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-RESEARCH.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-PATTERNS.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-01-SUMMARY.md

<interfaces>
<!-- 来自 14-PATTERNS.md §registry.ts 节，当前结构和目标改动 -->

From src/providers/registry.ts（当前常量，需改）:
```typescript
// 现有（lines 25-31）
const AIHUBMIX_BASE_URL = 'https://api.aihubmix.com/v1';  // vision 用，保留
const AIHUBMIX_VISION_MODEL = 'gpt-5.1';                  // → 改为 'gpt-5.4'（D-06）
const AIHUBMIX_IMAGE_MODEL = 'gpt-image-2';               // → 删除，改用列表

// 新增（D-05）
const AIHUBMIX_IMAGE_BASE_URL = 'https://aihubmix.com';   // 生图专用，无 /v1
```

From src/providers/registry.ts（现有 case 结构，lines 68-102）:
```typescript
case 'vision': {
  ...
  return { ..., baseURL: AIHUBMIX_BASE_URL, model: AIHUBMIX_VISION_MODEL } satisfies ImageConfig;
}
case 'image-gen': {
  ...
  return {
    ...,
    baseURL: AIHUBMIX_BASE_URL,   // → 改为 AIHUBMIX_IMAGE_BASE_URL
    model: AIHUBMIX_IMAGE_MODEL,  // → 改为 DEFAULT_IMAGE_GEN_MODEL.id
  } satisfies ImageConfig;
}
```

From src/providers/aihubmix-vision.ts（需改行，line 44）:
```typescript
body: JSON.stringify({
  model: 'gpt-4o',   // → 改为: model: AIHUBMIX_VISION_MODEL（从 registry 导入）
  stream: false,
  ...
})
```

From src/providers/registry.test.ts（过时断言，需更新，lines 91-117）:
```typescript
// line 101: model: 'gpt-5.1'    → model: 'gpt-5.4'
// line 113: model: 'gpt-image-2' → model: 'doubao-seedream-5.0-lite'
// image-gen 的 baseURL: 'https://api.aihubmix.com/v1' → 'https://aihubmix.com'
```

目标新增（D-05，供 Phase 16 picker）:
```typescript
export interface ImageGenModel {
  id: string;
  label: string;
  endpointKind: 'predictions' | 'gemini';
  authKind: 'bearer' | 'goog-api-key';
  isDefault: boolean;
}

export const IMAGE_GEN_MODELS: ImageGenModel[] = [
  { id: 'doubao-seedream-5.0-lite', label: 'Doubao SeedDream 5.0 Lite（快速默认）',
    endpointKind: 'predictions', authKind: 'bearer', isDefault: true },
  { id: 'gpt-image-2', label: 'GPT-Image-2（高质量）',
    endpointKind: 'predictions', authKind: 'bearer', isDefault: false },
  { id: 'gemini-3.1-flash-image-preview', label: 'Gemini 3.1 Flash Image Preview',
    endpointKind: 'gemini', authKind: 'goog-api-key', isDefault: false },
];

export const DEFAULT_IMAGE_GEN_MODEL = IMAGE_GEN_MODELS.find((m) => m.isDefault)!;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重整 registry.ts — 新增 ImageGenModel 列表 + 修正常量（D-05/D-06/D-07）</name>
  <files>src/providers/registry.ts</files>
  <read_first>
    - src/providers/registry.ts（全文，了解现有常量定义、case 结构、ImageConfig satisfies 用法）
    - 14-PATTERNS.md §`src/providers/registry.ts`（lines 140–200，现有 case 结构 + 改动清单）
    - 14-RESEARCH.md §ImageGenModel 列表（D-05，三项 model 定义 + 字段含义）
    - 14-RESEARCH.md §registry.ts 的 image-gen case 改动（D-07 注意事项：doubao 不在 /v1/models，预期行为）
    - 14-RESEARCH.md §Pitfall 1（baseURL 双 /v1 陷阱——生图 base host 是 aihubmix.com，无 /v1）
  </read_first>
  <behavior>
    - AIHUBMIX_VISION_MODEL 改为 'gpt-5.4'
    - 新增 AIHUBMIX_IMAGE_BASE_URL = 'https://aihubmix.com'（无 /v1，生图专用）
    - 删除旧的 AIHUBMIX_IMAGE_MODEL 单一常量
    - 新增 export interface ImageGenModel（含 id/label/endpointKind/authKind/isDefault）
    - 新增 export const IMAGE_GEN_MODELS（三项）
    - 新增 export const DEFAULT_IMAGE_GEN_MODEL = IMAGE_GEN_MODELS.find(m => m.isDefault)!
    - image-gen case 的 baseURL 改用 AIHUBMIX_IMAGE_BASE_URL，model 改用 DEFAULT_IMAGE_GEN_MODEL.id
    - vision case 不改（仍用 AIHUBMIX_BASE_URL + AIHUBMIX_VISION_MODEL，现在 VISION_MODEL 值改了）
  </behavior>
  <action>
读取 src/providers/registry.ts 全文。

**修改 1：常量区（大约 lines 24-32）**
- 将 `const AIHUBMIX_VISION_MODEL = 'gpt-5.1'` 改为 `const AIHUBMIX_VISION_MODEL = 'gpt-5.4'`（D-06）
- 删除 `const AIHUBMIX_IMAGE_MODEL = 'gpt-image-2'`（改用列表）
- 在 AIHUBMIX_BASE_URL 附近新增 `const AIHUBMIX_IMAGE_BASE_URL = 'https://aihubmix.com';`
  并加注释：`// 生图专用 base host（无 /v1 后缀）。Pitfall: 勿与 AIHUBMIX_BASE_URL 混用。`

**修改 2：新增 ImageGenModel 接口 + 列表（在常量区之后、ProviderRegistry class 之前）**
```typescript
/** 生图模型元数据（D-05）——供 image-gen resolve 路由 + Phase 16 picker 消费 */
export interface ImageGenModel {
  id: string;
  label: string;
  /** 决定 URL 模板和 request body 结构 */
  endpointKind: 'predictions' | 'gemini';
  /** 决定 Authorization header 形式 */
  authKind: 'bearer' | 'goog-api-key';
  isDefault: boolean;
}

export const IMAGE_GEN_MODELS: ImageGenModel[] = [
  {
    id: 'doubao-seedream-5.0-lite',
    label: 'Doubao SeedDream 5.0 Lite（快速默认）',
    endpointKind: 'predictions',
    authKind: 'bearer',
    isDefault: true,
    // D-07: doubao 不在 /v1/models 清单，但走 predictions 独立目录，spike 011 真打 HTTP 200
  },
  {
    id: 'gpt-image-2',
    label: 'GPT-Image-2（高质量）',
    endpointKind: 'predictions',
    authKind: 'bearer',
    isDefault: false,
  },
  {
    id: 'gemini-3.1-flash-image-preview',
    label: 'Gemini 3.1 Flash Image Preview',
    endpointKind: 'gemini',
    authKind: 'goog-api-key',
    isDefault: false,
  },
];

/** 默认生图 model = doubao-seedream-5.0-lite（最快，满足 P95≤10s，D-05） */
export const DEFAULT_IMAGE_GEN_MODEL = IMAGE_GEN_MODELS.find((m) => m.isDefault)!;
```

**修改 3：image-gen case 的 return（大约 lines 85-95）**
- 把 `baseURL: AIHUBMIX_BASE_URL` 改为 `baseURL: AIHUBMIX_IMAGE_BASE_URL`
- 把 `model: AIHUBMIX_IMAGE_MODEL` 改为 `model: DEFAULT_IMAGE_GEN_MODEL.id`
- 其余（providerId、apiKey、satisfies ImageConfig）保持不变

**security check：** apiKey 仅出现在 `apiKey` 字段，satisfies ImageConfig 保证类型安全。不进 body、不进日志。
  </action>
  <verify>
    <automated>npx vitest run src/providers/registry.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "IMAGE_GEN_MODELS" src/providers/registry.ts 返回 >= 3（接口、常量声明、DEFAULT 常量）
    - grep -c "'gpt-5.4'" src/providers/registry.ts 返回 >= 1（VISION_MODEL 常量）
    - grep -c "'gpt-5.1'" src/providers/registry.ts 返回 0（旧 vision model 已删）
    - grep -c "AIHUBMIX_IMAGE_BASE_URL" src/providers/registry.ts 返回 >= 2（常量定义 + case 引用）
    - grep -c "'doubao-seedream-5.0-lite'" src/providers/registry.ts 返回 >= 1
    - grep -c "gpt-image-2.*isDefault.*false\|isDefault.*false.*gpt-image-2" src/providers/registry.ts 返回 >= 0（gpt-image-2 不是默认）
    - grep "isDefault: true" src/providers/registry.ts（结果应包含 doubao）
    - registry.test.ts 在改完 registry.ts 后（Task 2 更新断言）能通过
  </acceptance_criteria>
  <done>registry.ts 新增 ImageGenModel 接口 + IMAGE_GEN_MODELS 三项列表 + DEFAULT_IMAGE_GEN_MODEL 常量；vision model 改为 gpt-5.4；image-gen case 改用 AIHUBMIX_IMAGE_BASE_URL + doubao 默认 model；旧 AIHUBMIX_IMAGE_MODEL 常量已删。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 aihubmix-vision.ts + registry.test.ts（D-06 + Pitfall 6）</name>
  <files>
    src/providers/aihubmix-vision.ts
    src/providers/registry.test.ts
  </files>
  <read_first>
    - src/providers/aihubmix-vision.ts（全文，定位 body 里硬编码的 model: 'gpt-4o'，约 line 44）
    - src/providers/registry.test.ts（全文，定位过时断言：gpt-5.1 line 101、gpt-image-2 line 113、baseURL 行）
    - 14-PATTERNS.md §`src/providers/aihubmix-vision.ts`（lines 203–223，改动位置 + 循环依赖注意）
    - 14-PATTERNS.md §`src/providers/registry.test.ts`（lines 468–508，mock storage 模式 + 断言更新）
    - 14-RESEARCH.md §Pitfall 6（registry.test.ts 断言过时说明）
    - 14-CONTEXT.md D-06（vision model = gpt-5.4）
  </read_first>
  <action>
**aihubmix-vision.ts 改动（一行）：**
读取 src/providers/aihubmix-vision.ts 全文。找到 body JSON.stringify 里的 `model: 'gpt-4o'` 这一行。

需要从 registry 导入 AIHUBMIX_VISION_MODEL 常量。注意避免循环导入——registry 导入 types，vision 导入 sse/errors，vision 从 registry 导入常量不构成循环。

在文件头部 import 区增加：
```typescript
import { AIHUBMIX_VISION_MODEL } from './registry';
```
（若 registry 当前没有 export AIHUBMIX_VISION_MODEL，则需将其改为 `export const AIHUBMIX_VISION_MODEL`，同步在 Task 1 已完成的 registry.ts 修改里确认该常量是 export。若 Task 1 已完成时该常量为 const 而非 export const，此处需回到 registry.ts 补 export 关键字。）

将 `model: 'gpt-4o'` 改为 `model: AIHUBMIX_VISION_MODEL`。

其余代码不变。

---

**registry.test.ts 改动（更新断言 + 新增用例）：**
读取 src/providers/registry.test.ts 全文。

**更新三处过时断言（Pitfall 6）：**
1. vision case 断言：`model: 'gpt-5.1'` → `model: 'gpt-5.4'`
2. image-gen case 断言：`model: 'gpt-image-2'` → `model: 'doubao-seedream-5.0-lite'`
3. image-gen case 断言：`baseURL: 'https://api.aihubmix.com/v1'` → `baseURL: 'https://aihubmix.com'`

**新增 IMAGE_GEN_MODELS 用例（D-05）：**
在现有 describe 块里追加：
```typescript
describe('IMAGE_GEN_MODELS（D-05）', () => {
  it('包含三个 model', () => {
    expect(IMAGE_GEN_MODELS).toHaveLength(3);
  });

  it('恰好一个 model 的 isDefault 为 true，且该 model 是 doubao-seedream-5.0-lite', () => {
    const defaults = IMAGE_GEN_MODELS.filter((m) => m.isDefault);
    expect(defaults).toHaveLength(1);
    expect(defaults[0].id).toBe('doubao-seedream-5.0-lite');
  });

  it('DEFAULT_IMAGE_GEN_MODEL.id 等于 doubao-seedream-5.0-lite', () => {
    expect(DEFAULT_IMAGE_GEN_MODEL.id).toBe('doubao-seedream-5.0-lite');
  });

  it('每个 model 都有 id/label/endpointKind/authKind/isDefault 字段', () => {
    for (const m of IMAGE_GEN_MODELS) {
      expect(m.id).toBeTruthy();
      expect(m.label).toBeTruthy();
      expect(['predictions', 'gemini']).toContain(m.endpointKind);
      expect(['bearer', 'goog-api-key']).toContain(m.authKind);
      expect(typeof m.isDefault).toBe('boolean');
    }
  });
});
```

需要在 registry.test.ts 顶部 import 里增加 `IMAGE_GEN_MODELS, DEFAULT_IMAGE_GEN_MODEL` 的导入。
  </action>
  <verify>
    <automated>npx vitest run src/providers/registry.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "'gpt-4o'" src/providers/aihubmix-vision.ts 返回 0（硬编码已删）
    - grep -c "AIHUBMIX_VISION_MODEL" src/providers/aihubmix-vision.ts 返回 >= 1（引用常量）
    - grep -c "'gpt-5.1'" src/providers/registry.test.ts 返回 0（过时断言已删）
    - grep -c "'gpt-5.4'" src/providers/registry.test.ts 返回 >= 1（新 vision model 断言）
    - grep -c "'doubao-seedream-5.0-lite'" src/providers/registry.test.ts 返回 >= 2（image-gen 断言 + IMAGE_GEN_MODELS 用例）
    - grep -c "IMAGE_GEN_MODELS" src/providers/registry.test.ts 返回 >= 3（describe + 用例）
    - npx vitest run src/providers/registry.test.ts 所有用例通过（exit 0）
  </acceptance_criteria>
  <done>aihubmix-vision.ts 的 model 硬编码改为引用 AIHUBMIX_VISION_MODEL 常量；registry.test.ts 三处过时断言已更新 + 新增 IMAGE_GEN_MODELS 四个用例；npx vitest run src/providers/registry.test.ts 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry.ts → image client | baseURL 和 model 字段流向 ImageConfig，注入 HTTP 请求 headers（不进 body） |
| vision.ts → aihubmix API | model 字段进入 request body（不含 apiKey，T-14-01 已遵守） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | registry.ts / aihubmix-vision.ts | mitigate | apiKey 仅在 registry resolve 返回 ImageConfig.apiKey 字段，由 image/vision client 注入 Authorization header，不进 body、不进 error.message（T-01-04）。registry.ts 本体不打印/记录 apiKey。VERIFIED: 现有 registry.ts + vision.ts 已遵守此约束。 |
| T-14-02 | Information Disclosure | registry.ts image-gen case | accept | registry 仅路由 model id 和 baseURL，不涉及 doubao 签名 URL（那是 aihubmix-image.ts Plan 05 的职责）。本 plan 无 URL 存储风险。 |
</threat_model>

<verification>
```bash
# registry.ts 关键字段验证
grep "'gpt-5.4'" src/providers/registry.ts
grep "AIHUBMIX_IMAGE_BASE_URL" src/providers/registry.ts
grep "IMAGE_GEN_MODELS" src/providers/registry.ts
grep "doubao-seedream-5.0-lite" src/providers/registry.ts

# vision.ts 改动验证
grep "AIHUBMIX_VISION_MODEL" src/providers/aihubmix-vision.ts
grep "'gpt-4o'" src/providers/aihubmix-vision.ts   # 期望 0

# registry.test.ts 断言
grep "'gpt-5.1'" src/providers/registry.test.ts   # 期望 0
grep "'gpt-5.4'" src/providers/registry.test.ts

# 运行测试
npx vitest run src/providers/registry.test.ts
```
</verification>

<success_criteria>
- registry.ts：AIHUBMIX_VISION_MODEL = 'gpt-5.4'，IMAGE_GEN_MODELS 三项，DEFAULT_IMAGE_GEN_MODEL = doubao，AIHUBMIX_IMAGE_BASE_URL = 'https://aihubmix.com'，image-gen case 使用新常量
- aihubmix-vision.ts：model 不再硬编码 'gpt-4o'，改用 AIHUBMIX_VISION_MODEL
- registry.test.ts：过时断言已更新，IMAGE_GEN_MODELS 新用例通过，npx vitest run 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/14-mdl-aihubmix-provider-model-casing/14-03-SUMMARY.md`
</output>

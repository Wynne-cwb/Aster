---
phase: 14-mdl-aihubmix-provider-model-casing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/tools/write/ppt.ts
autonomous: true
requirements:
  - MDL-03

must_haves:
  truths:
    - "ppt.ts 里 9 个 camelCase schema 工具全部改为 snake_case schema"
    - "ppt.ts 里 4 个 pick* helper 函数（pickSlideIndex/pickShapeId/pickSourceIndex/pickTargetIndex）已删除"
    - "所有 PPT 工具的 execute 函数读参数改为直接读 snake_case key（无双键容错逻辑）"
    - "tsc --noEmit 编译通过，无 TypeScript 类型错误"
  artifacts:
    - path: "src/agent/tools/write/ppt.ts"
      provides: "所有 PPT 工具 snake_case schema + 无 pick* helpers"
      contains: "slide_index"
  key_links:
    - from: "src/agent/tools/write/ppt.ts（schema）"
      to: "src/agent/tools/index.ts dispatchTool（Plan 04 PPT_TOOLS set）"
      via: "工具名集合 PPT_TOOLS — Plan 04 的 PPT_TOOLS.has(call.name) 须与 ppt.ts 导出的工具名一致"
      pattern: "set_shape_text_font|add_shape|set_shape_text_alignment|delete_shape|rotate_shape|manage_slides|set_slide_background|copy_slide"
---

<objective>
根治 PPT 工具的 schema casing 问题（D-09/D-11）：把 ppt.ts 中 9 个还在用 camelCase 的工具 schema 统一改为 snake_case，同时删除 4 个散落的双键容错 pick* helper 函数及其所有调用点。

Purpose: ppt.ts 改完后，Plan 04 的 dispatchTool 中央 normalize 才能"进来当总闸"——execute 只读 snake_case，中央 normalize 将 LLM 传来的 camelCase 折成 snake_case 之后，两端对齐，双键容错彻底消失。

Output: 更新后的 src/agent/tools/write/ppt.ts（无 pick* helpers，全 snake_case schema）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-CONTEXT.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-RESEARCH.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-PATTERNS.md

<interfaces>
<!-- 来自 14-PATTERNS.md §ppt.ts 节，当前状态和目标状态 -->

现有 pick* helpers（lines 79-90，全部删除，D-11）:
```typescript
function pickSlideIndex(args: Record<string, unknown>): number {
  return (args.slideIndex ?? args.slide_index) as number;
}
function pickShapeId(args: Record<string, unknown>): string {
  return (args.shapeId ?? args.shape_id) as string;
}
function pickSourceIndex(args: Record<string, unknown>): number {
  return (args.sourceIndex ?? args.source_index) as number;
}
function pickTargetIndex(args: Record<string, unknown>): number | undefined {
  return (args.targetIndex ?? args.target_index) as number | undefined;
}
```

需改工具列表（9 个，camelCase → snake_case）:
- set_shape_text_font：slideIndex→slide_index, shapeId→shape_id
- add_shape：slideIndex→slide_index（shapeType→shape_type，position 嵌套 object 不动）
- set_shape_text_alignment：slideIndex→slide_index, shapeId→shape_id
- delete_shape：slideIndex→slide_index, shapeId→shape_id
- rotate_shape：slideIndex→slide_index, shapeId→shape_id
- manage_slides：slideIndex→slide_index（operation 不变）
- set_slide_background：slideIndex→slide_index（color 不变）
- copy_slide：sourceIndex→source_index, targetIndex→target_index
- （insert_slide 在此工具定义中如有 slideIndex 也改，如已是 snake_case 跳过）

不改工具（已是 snake_case，样板）:
- set_shape_property: slide_index, shape_id, fill_color ✓
- move_shape: slide_index, shape_id ✓
- set_shape_text: slide_index, shape_id ✓

reverse ReverseDescriptor args 格式（保持 snake_case，只改 execute 局部变量名以对齐）:
```typescript
// 改后 execute 变量名直接用 snake_case，与 reverse args 对齐：
const slide_index = a.slide_index as number;
const shape_id = a.shape_id as string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 pick* helpers + 统一 ppt.ts 为 snake_case schema（D-09/D-11）</name>
  <files>src/agent/tools/write/ppt.ts</files>
  <read_first>
    - src/agent/tools/write/ppt.ts（全文，理解 9 个 camelCase 工具的 schema + execute 结构 + pick* 调用分布）
    - 14-PATTERNS.md §`src/agent/tools/write/ppt.ts`（lines 335–408，pick* helpers 位置 + 改前/改后对照）
    - 14-RESEARCH.md §D-09（schema 统一 snake_case）+ D-11（删 pick* helpers）
    - 14-RESEARCH.md §PPT Casing 根治实现指南 §D-11 节（改前/改后 execute 读法示例）
  </read_first>
  <action>
读取 src/agent/tools/write/ppt.ts 全文。

**步骤 1：删除 4 个 pick* helper 函数**
找到并完全删除 pickSlideIndex、pickShapeId、pickSourceIndex、pickTargetIndex 四个函数定义（共约 12 行）。

**步骤 2：对 9 个 camelCase schema 工具做以下改动**

对每个工具，同步修改三处：
（a）parameters.properties 的 key 名：camelCase → snake_case
（b）parameters.required 数组里的 key 名：camelCase → snake_case
（c）humanLabel 和 execute 函数里读 args 的方式：
   - 改前：`pickSlideIndex(a)` → 改后：`a.slide_index as number`
   - 改前：`pickShapeId(a)` → 改后：`a.shape_id as string`
   - 改前：`pickSourceIndex(a)` → 改后：`a.source_index as number`
   - 改前：`pickTargetIndex(a)` → 改后：`a.target_index as number | undefined`
   - 其余 camelCase 字段（shapeType→shape_type）也同步改

**9 个需改工具的具体改动：**

1. `set_shape_text_font`：
   - schema 的 slideIndex→slide_index，shapeId→shape_id
   - required: ['slideIndex','shapeId','font'] → ['slide_index','shape_id','font']
   - humanLabel/execute 内的 pickSlideIndex(a)/pickShapeId(a) → a.slide_index/a.shape_id

2. `add_shape`：
   - schema 的 slideIndex→slide_index，shapeType→shape_type
   - 注意：position 嵌套 object 的 key（left/top/width/height）都已是小写，不变
   - required 同步更新
   - humanLabel/execute 内的读法同步改

3. `set_shape_text_alignment`：
   - schema 的 slideIndex→slide_index，shapeId→shape_id
   - required + humanLabel/execute 同步改

4. `delete_shape`：
   - schema 的 slideIndex→slide_index，shapeId→shape_id
   - required + humanLabel/execute 同步改

5. `rotate_shape`：
   - schema 的 slideIndex→slide_index，shapeId→shape_id
   - required + humanLabel/execute 同步改

6. `manage_slides`：
   - schema 的 slideIndex→slide_index（operation 不变）
   - required + humanLabel/execute 同步改

7. `set_slide_background`：
   - schema 的 slideIndex→slide_index（color 不变）
   - required + humanLabel/execute 同步改

8. `copy_slide`：
   - schema 的 sourceIndex→source_index，targetIndex→target_index
   - required + humanLabel/execute 内的 pickSourceIndex(a)/pickTargetIndex(a) → a.source_index/a.target_index

9. `insert_slide`（如有 slideIndex 字段则改；如已是 snake_case 跳过，勿重复改）

**步骤 3：reverse ReverseDescriptor args 对齐**
各工具的 reverse/ReverseDescriptor args 对象里如已有 snake_case key（如 slide_index/shape_id），确认和 execute 变量名一致。reverse args 不需要额外改动（现有 set_shape_property 样本已是 snake_case reverse args）。

**安全注意：** ppt.ts 是工具定义层，无网络调用、无 secret 处理。schema 改动只影响 LLM 看到的 function 定义，不改 adapter 层的 Office.js 调用。

**不改的 4 个工具（已正确 snake_case）：** insert_slide（如已 snake_case）、set_shape_property、move_shape、set_shape_text。
  </action>
  <verify>
    <automated>grep -c "pickSlideIndex\|pickShapeId\|pickSourceIndex\|pickTargetIndex" src/agent/tools/write/ppt.ts; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "pickSlideIndex\|pickShapeId\|pickSourceIndex\|pickTargetIndex" src/agent/tools/write/ppt.ts 返回 0（pick* 已全删）
    - grep -c "slideIndex\|shapeId\|sourceIndex\|targetIndex\|shapeType" src/agent/tools/write/ppt.ts 返回 0（schema 里无 camelCase key）
    - grep -c "slide_index" src/agent/tools/write/ppt.ts 返回 >= 10（多个工具都用 slide_index）
    - grep -c "shape_id" src/agent/tools/write/ppt.ts 返回 >= 8（多个工具都用 shape_id）
    - grep -c "source_index" src/agent/tools/write/ppt.ts 返回 >= 2（copy_slide 的 schema + execute）
    - npx tsc --noEmit 2>&1 | grep "ppt.ts" 不出现 TypeScript 类型错误（类型转换 as 断言不报错）
  </acceptance_criteria>
  <done>ppt.ts 内 pick* helpers 已删除；9 个 camelCase schema 工具全部统一为 snake_case；execute 函数直接读 snake_case key；tsc 对 ppt.ts 无报错。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ppt.ts tool schema | 纯工具定义层（function schema + execute 函数），无新 I/O、无网络调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | ppt.ts | accept | No HIGH/MEDIUM threats — 纯 arg-key 归一化，无新 I/O 或 secrets。删除 pick* helpers 和统一 snake_case 是纯重构，不引入新攻击面。安全影响：正面（减少逻辑分支，攻击面更小）。 |
</threat_model>

<verification>
```bash
# 验证 pick* helpers 已删除
grep -c "pickSlideIndex\|pickShapeId\|pickSourceIndex\|pickTargetIndex" src/agent/tools/write/ppt.ts

# 验证 schema 无 camelCase key（参数名）
grep -c "\"slideIndex\"\|\"shapeId\"\|\"sourceIndex\"\|\"targetIndex\"" src/agent/tools/write/ppt.ts

# 验证 snake_case 已广泛存在
grep -c "slide_index" src/agent/tools/write/ppt.ts

# tsc 检查（ppt.ts 局部）
npx tsc --noEmit 2>&1 | grep "ppt.ts" | head -10

# 快速测试（此时 dispatch.test.ts 守门用例还没加，只跑已有测试）
npx vitest run src/agent/tools/ 2>&1 | tail -10
```
</verification>

<success_criteria>
- ppt.ts 内无任何 pickSlideIndex/pickShapeId/pickSourceIndex/pickTargetIndex 引用
- ppt.ts 的所有 PPT 工具 schema 参数名均为 snake_case
- execute 函数直接读 snake_case key（a.slide_index 等），无双键容错
- tsc --noEmit 对 ppt.ts 无报错
</success_criteria>

<output>
完成后创建 `.planning/phases/14-mdl-aihubmix-provider-model-casing/14-02-SUMMARY.md`
</output>

---
phase: 14-mdl-aihubmix-provider-model-casing
plan: 04
type: execute
wave: 2
depends_on:
  - "14-02"
files_modified:
  - src/agent/tools/index.ts
  - src/agent/tools/dispatch.test.ts
autonomous: true
requirements:
  - MDL-03

must_haves:
  truths:
    - "dispatchTool 入口插入 normalizeToSnakeCase helper + PPT_TOOLS set，仅对 PPT 工具触发（D-10/D-13）"
    - "Word/Excel 工具 args 经 dispatchTool 后 camelCase key 保持原样（D-13：不主动重排 Word/Excel schema）"
    - "dispatch.test.ts 新增 PPT casing 守门 describe 块，测试 8 个 PPT 工具的 camel/snake 双向入参"
    - "npx vitest run src/agent/tools/dispatch.test.ts 全绿"
  artifacts:
    - path: "src/agent/tools/index.ts"
      provides: "dispatchTool 内中央 normalize（PPT 专用）"
      contains: "normalizeToSnakeCase"
    - path: "src/agent/tools/dispatch.test.ts"
      provides: "PPT casing 守门用例（D-12）"
      contains: "PPT casing 归一化"
  key_links:
    - from: "src/agent/tools/index.ts normalizeToSnakeCase"
      to: "src/agent/tools/write/ppt.ts（Plan 02，已是 snake_case schema）"
      via: "PPT_TOOLS.has(call.name) → normalize → def.execute(normalizedArgs)"
      pattern: "PPT_TOOLS.has"
    - from: "dispatch.test.ts"
      to: "src/agent/tools/index.ts dispatchTool"
      via: "直接调用 dispatchTool 并 assert capturedArgs"
      pattern: "dispatchTool"
---

<objective>
完成 MDL-03 最后一里路：在 dispatchTool 入口插入中央 normalizeToSnakeCase（仅对 PPT 工具生效），并在 dispatch.test.ts 加入守门用例（D-12），永久防止 v2.2 新增 PPT 生图工具重蹈 casing 覆辙。

Purpose: Plan 02 已让 ppt.ts 全部 snake_case，本 plan 在 dispatchTool 入口加"总闸"，保证 LLM 不管传 camelCase 还是 snake_case，execute 函数收到的永远是 snake_case。守门测试是 feedback_recurring_failure_add_gate 原则的落实。

Output: 更新后的 index.ts（normalize 插入）、更新后的 dispatch.test.ts（PPT casing 守门 describe 块）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-CONTEXT.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-RESEARCH.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-PATTERNS.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-02-SUMMARY.md

<interfaces>
<!-- 来自 14-PATTERNS.md §index.ts + §dispatch.test.ts 节 -->

From src/agent/tools/index.ts（dispatchTool 核心，当前需插 normalize，lines 139-185）:
```typescript
export async function dispatchTool(
  call: ToolCallInvocation,
  ctx: ToolExecContext,
  tools: ToolDef[],
): Promise<ToolResult> {
  const def = tools.find((t) => t.name === call.name);
  if (!def) { /* NOT_FOUND error */ }

  try {
    let timer: ReturnType<typeof setTimeout> | undefined;
    const timeout = new Promise<never>((_, reject) => {
      timer = setTimeout(() => reject(new HostApiError('工具调用超时...')), TOOL_TIMEOUT_MS);
    });
    try {
      // ← Phase 14 在这里之前插入 normalize
      return await Promise.race([def.execute(call.arguments as never, ctx), timeout]);
    } finally {
      if (timer !== undefined) clearTimeout(timer);
    }
  } catch (err) {
    ...
  }
}
```

目标新增（内联于 dispatchTool，或文件内 helper）:
```typescript
// PPT 工具名集合（D-13：仅对 PPT 工具 normalize，Word/Excel 不动）
const PPT_TOOLS = new Set([
  'insert_slide', 'set_shape_property', 'move_shape', 'set_shape_text',
  'set_shape_text_font', 'add_shape', 'copy_slide', 'set_shape_text_alignment',
  'delete_shape', 'rotate_shape', 'manage_slides', 'set_slide_background',
]);

// camelCase → snake_case（仅一级 key，嵌套 object 不递归，A2 缓解）
function normalizeToSnakeCase(args: unknown): unknown {
  if (!args || typeof args !== 'object' || Array.isArray(args)) return args;
  const out: Record<string, unknown> = {};
  for (const [k, v] of Object.entries(args as Record<string, unknown>)) {
    out[k.replace(/([A-Z])/g, (m) => `_${m.toLowerCase()}`)] = v;
  }
  return out;
}

// 在 Promise.race 之前：
const normalizedArgs = PPT_TOOLS.has(call.name)
  ? normalizeToSnakeCase(call.arguments)
  : call.arguments;
return await Promise.race([def.execute(normalizedArgs as never, ctx), timeout]);
```

From src/agent/tools/dispatch.test.ts（当前结构，需追加 describe 块）:
```typescript
// 现有 makeCtx 模式（lines 18-36）
function makeCtx() { return { adapter: mockAdapter, runId: 'r1', ... }; }

// 现有 inline mock ToolDef + dispatchTool 调用模式（lines 39-53）
const tool: ToolDef = { name: '...', execute() { ... } };
await dispatchTool({ id: 'c1', name: '...', arguments: {} }, makeCtx(), [tool]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 dispatchTool 插入 PPT-only normalize（D-10/D-13）</name>
  <files>src/agent/tools/index.ts</files>
  <read_first>
    - src/agent/tools/index.ts（全文，定位 dispatchTool 函数、Promise.race 调用点、现有 imports）
    - 14-PATTERNS.md §`src/agent/tools/index.ts`（lines 267–332，插入点 + 推荐实现方案 A）
    - 14-RESEARCH.md §D-10（normalize 实现规范）+ D-13（范围限 PPT，不改 Word/Excel）
    - 14-RESEARCH.md §Pitfall 5（normalize 影响 Word/Excel 的风险 + 方案 A 解法）
  </read_first>
  <behavior>
    - PPT_TOOLS set 在模块级声明（函数外），列出所有 12 个 PPT 工具名
    - normalizeToSnakeCase 仅做一级 key 映射（不递归嵌套 object）
    - normalize 仅对 PPT_TOOLS.has(call.name) 为 true 的工具触发
    - Word 工具（append_paragraph 等，camelCase args）经过 dispatchTool 后 args 不变
    - snake_case 入参经 normalize 后仍是 snake_case（幂等）
  </behavior>
  <action>
读取 src/agent/tools/index.ts 全文。

**步骤 1：在文件合适位置（dispatchTool 函数前或模块顶部工具常量区）新增 PPT_TOOLS set：**
```typescript
/** PPT 工具名集合——dispatchTool 仅对这些工具做 camelCase→snake_case 归一化（D-13）。
 *  v2.2 新增 PPT 工具时，在此集合加入工具名（防 casing 覆辙守门）。
 */
const PPT_TOOLS = new Set([
  'insert_slide',
  'set_shape_property',
  'move_shape',
  'set_shape_text',
  'set_shape_text_font',
  'add_shape',
  'copy_slide',
  'set_shape_text_alignment',
  'delete_shape',
  'rotate_shape',
  'manage_slides',
  'set_slide_background',
]);
```

**步骤 2：在 dispatchTool 内（Promise.race 行之前）新增 helper + normalize 调用：**

先在文件内（非 dispatchTool 内部，避免每次调用重新定义）新增 helper：
```typescript
/** camelCase → snake_case，仅一级 key（嵌套 object 不递归，保留 position.left 等）。
 *  幂等：snake_case 入参经过后不变。*/
function normalizeToSnakeCase(args: unknown): unknown {
  if (!args || typeof args !== 'object' || Array.isArray(args)) return args;
  const out: Record<string, unknown> = {};
  for (const [k, v] of Object.entries(args as Record<string, unknown>)) {
    out[k.replace(/([A-Z])/g, (m) => `_${m.toLowerCase()}`)] = v;
  }
  return out;
}
```

然后在 dispatchTool 内，找到 `return await Promise.race([def.execute(call.arguments as never, ctx), timeout]);` 这一行，改为：
```typescript
const normalizedArgs = PPT_TOOLS.has(call.name)
  ? normalizeToSnakeCase(call.arguments)
  : call.arguments;
return await Promise.race([def.execute(normalizedArgs as never, ctx), timeout]);
```

**注意：** 不改 dispatchTool 的外部签名，不改 sanitize 逻辑，不改 timeout 逻辑，只在 Promise.race 这一行加 normalize 层。
  </action>
  <verify>
    <automated>grep -c "normalizeToSnakeCase\|PPT_TOOLS" src/agent/tools/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "normalizeToSnakeCase" src/agent/tools/index.ts 返回 >= 2（helper 定义 + 调用）
    - grep -c "PPT_TOOLS" src/agent/tools/index.ts 返回 >= 2（set 定义 + .has(call.name) 调用）
    - grep -c "normalizedArgs" src/agent/tools/index.ts 返回 >= 2（const 声明 + Promise.race 里用）
    - 旧的 `def.execute(call.arguments as never` 已替换为 `def.execute(normalizedArgs as never`
    - npx tsc --noEmit 2>&1 | grep "tools/index.ts" 不报类型错误
  </acceptance_criteria>
  <done>dispatchTool 内已插入 PPT_TOOLS set + normalizeToSnakeCase helper + normalize 分支；PPT 工具入参统一归一化；Word/Excel 工具透传原样 args。</done>
</task>

<task type="auto">
  <name>Task 2: dispatch.test.ts — PPT casing 守门用例（D-12）</name>
  <files>src/agent/tools/dispatch.test.ts</files>
  <read_first>
    - src/agent/tools/dispatch.test.ts（全文，了解 makeCtx 函数、现有 mock ToolDef 模式、现有 describe 块结构）
    - 14-PATTERNS.md §`src/agent/tools/dispatch.test.ts`（lines 511–589，完整守门用例设计）
    - 14-RESEARCH.md §D-12（守门用例设计：每个 PPT 工具 camelCase + snake_case 双向 assert）
    - src/agent/tools/write/ppt.ts（确认 Plan 02 完成后的工具名列表，供 it.each 参数使用）
  </read_first>
  <action>
读取 src/agent/tools/dispatch.test.ts 全文。找到文件末尾，追加以下 describe 块。

**注意：** 不改现有 describe 块，只追加新块。ToolDef 类型需从现有 import 里确认已导入。

```typescript
describe('dispatchTool — PPT casing 归一化（D-12）', () => {
  // 每行格式：[工具名, camelCase 入参, 期望 snake_case args]
  it.each([
    [
      'set_shape_text_font',
      { slideIndex: 2, shapeId: 's1', font: { size: 14 } },
      { slide_index: 2, shape_id: 's1', font: { size: 14 } },
    ],
    [
      'set_shape_text_alignment',
      { slideIndex: 1, shapeId: 's2', alignment: 'Left' },
      { slide_index: 1, shape_id: 's2', alignment: 'Left' },
    ],
    [
      'delete_shape',
      { slideIndex: 3, shapeId: 's3' },
      { slide_index: 3, shape_id: 's3' },
    ],
    [
      'rotate_shape',
      { slideIndex: 1, shapeId: 's1', rotation: 45 },
      { slide_index: 1, shape_id: 's1', rotation: 45 },
    ],
    [
      'manage_slides',
      { operation: 'delete', slideIndex: 2 },
      { operation: 'delete', slide_index: 2 },
    ],
    [
      'set_slide_background',
      { slideIndex: 1, color: '#FF0000' },
      { slide_index: 1, color: '#FF0000' },
    ],
    [
      'copy_slide',
      { sourceIndex: 1, targetIndex: 3 },
      { source_index: 1, target_index: 3 },
    ],
    [
      'add_shape',
      { slideIndex: 1, shapeType: 'Rectangle', position: { left: 0, top: 0, width: 100, height: 100 } },
      { slide_index: 1, shape_type: 'Rectangle', position: { left: 0, top: 0, width: 100, height: 100 } },
    ],
  ] as Array<[string, Record<string, unknown>, Record<string, unknown>]>)(
    '%s：camelCase 与 snake_case 入参都命中正确 args',
    async (toolName, camelArgs, expectedSnakeArgs) => {
      let capturedArgs: unknown;
      const mockTool: ToolDef = {
        name: toolName,
        description: '',
        parameters: {},
        humanLabel: () => '',
        kind: 'write',
        async execute(args) {
          capturedArgs = args;
          return { ok: true as const };
        },
      };

      // camelCase 入参 → normalize → execute 收到 snake_case
      await dispatchTool({ id: 'c1', name: toolName, arguments: camelArgs }, makeCtx(), [mockTool]);
      expect(capturedArgs).toMatchObject(expectedSnakeArgs);

      // snake_case 入参 → normalize 幂等 → execute 仍收到 snake_case
      await dispatchTool({ id: 'c2', name: toolName, arguments: expectedSnakeArgs }, makeCtx(), [mockTool]);
      expect(capturedArgs).toMatchObject(expectedSnakeArgs);
    },
  );

  it('Word 工具不受 PPT normalize 影响——camelCase 保持原样（D-13）', async () => {
    let capturedArgs: unknown;
    const wordTool: ToolDef = {
      name: 'append_paragraph',
      description: '',
      parameters: {},
      humanLabel: () => '',
      kind: 'write',
      async execute(args) {
        capturedArgs = args;
        return { ok: true as const };
      },
    };
    const wordArgs = { afterParagraphIndex: 2, text: 'hello' };
    await dispatchTool(
      { id: 'c1', name: 'append_paragraph', arguments: wordArgs },
      makeCtx(),
      [wordTool],
    );
    // camelCase 保持原样（Word 工具不在 PPT_TOOLS set 里）
    expect(capturedArgs).toMatchObject(wordArgs);
  });

  it('position 嵌套 object 的 key 不被 normalize 改动（A2 风险缓解）', async () => {
    let capturedArgs: unknown;
    const pptTool: ToolDef = {
      name: 'add_shape',
      description: '',
      parameters: {},
      humanLabel: () => '',
      kind: 'write',
      async execute(args) {
        capturedArgs = args;
        return { ok: true as const };
      },
    };
    // position 嵌套 object 的 key（left/top/width/height）是小写，normalize 后不变
    const args = { slideIndex: 1, shapeType: 'Rectangle', position: { left: 10, top: 20, width: 50, height: 30 } };
    await dispatchTool({ id: 'c1', name: 'add_shape', arguments: args }, makeCtx(), [pptTool]);
    expect((capturedArgs as Record<string, unknown>).position).toEqual({ left: 10, top: 20, width: 50, height: 30 });
  });
});
```

**注意：** `makeCtx` 已在现有测试文件中定义，无需重复定义。`ToolDef` 类型的 `kind` 字段和 `execute` 返回类型需与现有 ToolDef 接口一致——如果现有 ToolDef 不含 `kind` 字段，则省略（看现有测试里的 mockTool 结构）。
  </action>
  <verify>
    <automated>npx vitest run src/agent/tools/dispatch.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "PPT casing 归一化" src/agent/tools/dispatch.test.ts 返回 >= 1（describe 块存在）
    - grep -c "it.each" src/agent/tools/dispatch.test.ts 返回 >= 1（参数化用例存在）
    - grep -c "Word 工具不受 PPT normalize 影响" src/agent/tools/dispatch.test.ts 返回 >= 1（D-13 保护用例）
    - grep -c "position.*嵌套" src/agent/tools/dispatch.test.ts 返回 >= 1（A2 缓解用例）
    - npx vitest run src/agent/tools/dispatch.test.ts 所有用例通过（exit 0，无红用例）
  </acceptance_criteria>
  <done>dispatch.test.ts 新增 PPT casing 守门 describe 块，覆盖 8 个 PPT 工具的 camel→snake + snake 幂等 + Word 不受影响 + position 嵌套不变四类用例；npx vitest run dispatch.test.ts 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call → dispatchTool → execute | LLM 产生的 call.arguments 可能是 camelCase，normalize 后 execute 收到 snake_case |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | index.ts normalize | accept | No HIGH/MEDIUM threats — normalizeToSnakeCase 是纯 key 重命名，不引入新 I/O、不处理 secrets、不改变参数值（只改 key 名）。范围限 PPT_TOOLS（D-13），不影响 Word/Excel 工具的 execute 函数。dispatch.test.ts 守门用例验证 Word/Excel 不受影响（T 风险自验证）。 |
</threat_model>

<verification>
```bash
# normalize 插入验证
grep -c "normalizeToSnakeCase\|PPT_TOOLS" src/agent/tools/index.ts

# 守门用例验证
grep -c "PPT casing 归一化\|Word 工具不受" src/agent/tools/dispatch.test.ts

# 运行所有 tools 测试
npx vitest run src/agent/tools/dispatch.test.ts

# 综合 tools 目录（含 ppt 等已有测试）
npx vitest run src/agent/tools/ 2>&1 | tail -10
```
</verification>

<success_criteria>
- dispatchTool 内有 PPT_TOOLS set + normalizeToSnakeCase helper + 条件 normalize 调用
- Word 工具的 camelCase args 经 dispatchTool 后原样传入 execute
- PPT 工具的 camelCase args 经 dispatchTool 后以 snake_case 传入 execute（幂等）
- dispatch.test.ts 新 describe 块所有用例通过
</success_criteria>

<output>
完成后创建 `.planning/phases/14-mdl-aihubmix-provider-model-casing/14-04-SUMMARY.md`
</output>

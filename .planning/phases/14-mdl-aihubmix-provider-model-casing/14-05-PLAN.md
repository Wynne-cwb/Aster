---
phase: 14-mdl-aihubmix-provider-model-casing
plan: 05
type: execute
wave: 3
depends_on:
  - 14-01-PLAN.md
  - 14-03-PLAN.md
files_modified:
  - src/providers/aihubmix-image.ts
autonomous: true
requirements:
  - MDL-01
  - MDL-02

must_haves:
  truths:
    - "AihubmixImageClient.generate() 返回 { base64: string; mimeType: string }（裸 base64，无 data: 前缀）"
    - "doubao 路径：从 output[0].url fetch 图片，转 base64，返回 { base64, mimeType }，URL 在 provider 内部丢弃（D-02）"
    - "gpt-image-2 路径：从 output.b64_json[0].bytesBase64 取 base64，mimeType 规范化 'png'→'image/png'（Pitfall 2：output 是对象非数组）"
    - "gemini 路径：遍历 chunks/parts 找到含 inlineData 的 part，跳过 thoughtSignature（D-03），返回 { base64: inlineData.data, mimeType: inlineData.mimeType }"
    - "doubao 和 gpt-image-2 使用 Authorization: Bearer，gemini 使用 x-goog-api-key header（D-02，T-14-01）"
    - "apiKey 不出现在 request body，不出现在 error.message（T-14-01）"
    - "doubao 签名 URL 在 provider 内立即 fetch→base64→丢弃，不返回给调用方（T-14-02，D-02）"
    - "npx vitest run src/providers/aihubmix-image.test.ts 全绿（fixture-based 单测通过）"
  artifacts:
    - path: "src/providers/aihubmix-image.ts"
      provides: "三路 response 解析器（doubao/gpt-image-2/gemini）+ 两套鉴权 + 统一 { base64, mimeType } 输出"
      exports: ["AihubmixImageClient"]
  key_links:
    - from: "src/providers/aihubmix-image.ts"
      to: "src/providers/types.ts ImageGenResult"
      via: "Promise<ImageGenResult> 返回类型"
      pattern: "ImageGenResult"
    - from: "src/providers/aihubmix-image.ts"
      to: "src/providers/__fixtures__/"
      via: "aihubmix-image.test.ts fixture import + vi.stubGlobal fetch mock"
      pattern: "doubao-response|gpt-image-2-response|gemini-response"
    - from: "AihubmixImageClient（doubao 路径）"
      to: "ByteDance TOS 签名 URL"
      via: "内部 fetchUrlToBase64()，返回后 URL 丢弃"
      pattern: "fetchUrlToBase64"
---

<objective>
完整重写 src/providers/aihubmix-image.ts：按 model 前缀分发三路独立解析器（doubao/gpt-image-2/gemini），内部统一输出裸 base64 + mimeType，两套鉴权 header，gemini 端点族，doubao URL eager 转换。重写后 aihubmix-image.test.ts（Plan 01 创建的脚手架）从红变绿。

Purpose: 这是 Phase 14 的核心交付。重写后 Phase 15 vision read tool 和 Phase 16 image insert tool 才能消费正确的 { base64, mimeType } 结果。

Output: 全新的 src/providers/aihubmix-image.ts（三路解析器，符合所有 MDL-01 要求）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-CONTEXT.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-RESEARCH.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-PATTERNS.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-01-SUMMARY.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-03-SUMMARY.md
@.planning/spikes/011-image-gen-api-formats/findings.md

<interfaces>
<!-- Wire format 真相源（spike 011 锁定，executor 直接使用，无需重查） -->

URL 路径：
- doubao:    POST https://aihubmix.com/v1/models/doubao/{modelId}/predictions
- gpt-image-2: POST https://aihubmix.com/v1/models/openai/gpt-image-2/predictions
- gemini:   POST https://aihubmix.com/gemini/v1beta/models/{modelId}:streamGenerateContent

鉴权 header：
- Bearer（doubao/gpt-image-2）: { 'Content-Type': 'application/json', Authorization: `Bearer ${apiKey}` }
- x-goog-api-key（gemini）:     { 'Content-Type': 'application/json', 'x-goog-api-key': apiKey }

Request body（三套）：
- doubao:    { input: { prompt, size: '2K', sequential_image_generation: 'disabled', stream: false, response_format: 'url', watermark: true } }
- gpt-image-2: { input: { prompt, size: '1024x1024', n: 1, quality: 'high', moderation: 'low', background: 'auto' } }
- gemini:    { contents: [{ role: 'user', parts: [{ text: prompt }] }], generationConfig: { responseModalities: ['TEXT','IMAGE'], imageConfig: { aspectRatio: '1:1', imageSize: '1k' } } }

Response 字段落点：
- doubao:    body.output[0].url            → string (签名 URL，需 fetch 转 base64)
- gpt-image-2: body.output.b64_json[0].bytesBase64  → string (base64 PNG)
             body.output.b64_json[0].mimeType       → 'png'（须转 'image/png'）
- gemini:    body[N].candidates[0].content.parts[M].inlineData.data   → string (base64 jpeg)
             body[N].candidates[0].content.parts[M].inlineData.mimeType → 'image/jpeg'
             ⚠ 跳过含 thoughtSignature 的 part（找 inlineData 字段的 part）

From src/lib/sse.ts（错误处理，复用）：
```typescript
export function mapHttpError(status: number, errBody: unknown): AsterError { ... }
export class NetworkError extends AsterError { ... }
```

From src/providers/types.ts（Plan 01 改好的接口）：
```typescript
export interface ImageGenResult { base64: string; mimeType: string; }
export interface ImageConfig { providerId: string; baseURL: string; apiKey: string; model: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 完整重写 aihubmix-image.ts — 三路解析器 + 两套鉴权（MDL-01）</name>
  <files>src/providers/aihubmix-image.ts</files>
  <read_first>
    - src/providers/aihubmix-image.ts（全文，理解现有结构作为重写起点——旧文件是 gpt-image-1 形态）
    - 14-RESEARCH.md §三路解析器详细实现指南（sections 4a-4g，全部 URL 路径 + body + 解析字段）
    - 14-RESEARCH.md §doubao fetch 转 base64（arrayBuffer 路径实现，4e 节）
    - 14-RESEARCH.md §gemini 多 chunk 遍历（4f 节，跳过 thoughtSignature）
    - 14-PATTERNS.md §aihubmix-image.ts（lines 29-137，现有 vision.ts fetch+parse 模式 + 新签名契约）
    - 14-RESEARCH.md §Pitfall 1（baseURL 双 /v1 陷阱）+ Pitfall 2（gpt-image-2 output 是对象）+ Pitfall 3（gemini thoughtSignature）+ Pitfall 4（doubao URL CORS 风险）
    - .planning/spikes/011-image-gen-api-formats/findings.md（所有字段路径权威真相源）
    - src/providers/aihubmix-vision.ts（fetch+mapHttpError 模式参照）
    - src/lib/sse.ts（mapHttpError 签名确认）
  </read_first>
  <behavior>
    - AihubmixImageClient.generate(prompt, config, options?) 返回 Promise<ImageGenResult>
    - model id 以 'doubao' 开头 → _generateDoubao()
    - model id 以 'gpt-image' 开头 → _generateGptImage2()
    - model id 以 'gemini' 开头 → _generateGemini()
    - 未知 model → throw new NetworkError('未知生图 model: ${modelId}')
    - doubao: fetch predictions → output[0].url → fetchUrlToBase64(url) → { base64, mimeType }（URL 丢弃）
    - gpt-image-2: fetch predictions → output.b64_json[0].bytesBase64 → mimeType 规范化 'png'→'image/png'
    - gemini: fetch streamGenerateContent → 解析 JSON 数组 → 遍历 chunks/parts 找 inlineData → { base64: data, mimeType }
    - apiKey 仅进 Authorization/x-goog-api-key header，不进 body
    - 每路 fetch 都有 try/catch → NetworkError；!resp.ok → mapHttpError
  </behavior>
  <action>
读取当前 src/providers/aihubmix-image.ts（了解现有 exports 和 imports，以便保持模块接口兼容）。然后完整重写文件。

以下是完整的重写内容（根据 spike 011 字段路径 + RESEARCH.md 设计，executor 直接按此实现）：

```typescript
/**
 * AihubMix 生图客户端 — 三路 response 解析器（MDL-01）
 *
 * 安全约束（T-14-01）：
 *   - apiKey 仅注入 Authorization / x-goog-api-key header，不进 request body
 *   - apiKey 不出现在 error.message（mapHttpError 固定字面量 message）
 *   - doubao 签名 URL 在 provider 内立即 fetch→base64→丢弃，不外泄（D-02，T-14-02）
 *
 * Wire format 来源：.planning/spikes/011-image-gen-api-formats/findings.md（真机实测）
 */
import { mapHttpError } from '../lib/sse';
import { NetworkError } from '../errors';
import type { ImageConfig } from './types';

export interface ImageGenResult {
  base64: string;    // 裸 base64，不带 data: 前缀（D-01）
  mimeType: string;  // 'image/png' | 'image/jpeg' | string
}

export interface ImageGenOptions {
  size?: string;
  quality?: 'high' | 'medium' | 'low' | 'auto';
}

export class AihubmixImageClient {
  async generate(
    prompt: string,
    config: ImageConfig,
    _options?: ImageGenOptions,
  ): Promise<ImageGenResult> {
    const modelId = config.model;
    const base = config.baseURL.replace(/\/$/, '');  // 'https://aihubmix.com'

    if (modelId.startsWith('doubao')) {
      return this._generateDoubao(prompt, modelId, base, config.apiKey);
    }
    if (modelId.startsWith('gpt-image')) {
      return this._generateGptImage2(prompt, modelId, base, config.apiKey);
    }
    if (modelId.startsWith('gemini')) {
      return this._generateGemini(prompt, modelId, base, config.apiKey);
    }
    throw new NetworkError(`未知生图 model: ${modelId}`);
  }

  // ─── doubao ───────────────────────────────────────────────────────────────
  private async _generateDoubao(
    prompt: string,
    modelId: string,
    base: string,
    apiKey: string,
  ): Promise<ImageGenResult> {
    const url = `${base}/v1/models/doubao/${modelId}/predictions`;
    let resp: Response;
    try {
      resp = await fetch(url, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${apiKey}`,   // T-14-01: apiKey 仅此处
        },
        body: JSON.stringify({
          input: {
            prompt,
            size: '2K',
            sequential_image_generation: 'disabled',
            stream: false,
            response_format: 'url',
            watermark: true,
          },
        }),
      });
    } catch {
      throw new NetworkError('doubao 生图请求网络失败');
    }

    if (!resp.ok) {
      const errBody = await resp.json().catch(() => ({}));
      throw mapHttpError(resp.status, errBody);
    }

    const json = await resp.json() as { output?: Array<{ url?: string }> };
    const imageUrl = json.output?.[0]?.url;
    if (!imageUrl) throw new NetworkError('doubao 响应未包含图片 URL');

    // D-02: 立即 fetch→base64→丢弃 URL（TTL 风险 + Office.js 只吃 base64）
    return fetchUrlToBase64(imageUrl);
  }

  // ─── gpt-image-2 ──────────────────────────────────────────────────────────
  private async _generateGptImage2(
    prompt: string,
    _modelId: string,
    base: string,
    apiKey: string,
  ): Promise<ImageGenResult> {
    const url = `${base}/v1/models/openai/gpt-image-2/predictions`;
    let resp: Response;
    try {
      resp = await fetch(url, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${apiKey}`,   // T-14-01
        },
        body: JSON.stringify({
          input: {
            prompt,
            size: '1024x1024',
            n: 1,
            quality: 'high',
            moderation: 'low',
            background: 'auto',
          },
        }),
      });
    } catch {
      throw new NetworkError('gpt-image-2 生图请求网络失败');
    }

    if (!resp.ok) {
      const errBody = await resp.json().catch(() => ({}));
      throw mapHttpError(resp.status, errBody);
    }

    // Pitfall 2: output 是对象（{b64_json, urls}），不是数组！
    const json = await resp.json() as {
      output?: {
        b64_json?: Array<{ bytesBase64?: string; mimeType?: string }>;
      };
    };
    const first = json.output?.b64_json?.[0];
    if (!first?.bytesBase64) throw new NetworkError('gpt-image-2 响应未包含 base64 数据');

    // mimeType 规范化：'png' → 'image/png'（Open Question 2）
    const rawMime = first.mimeType ?? 'png';
    const mimeType = normalizeMimeType(rawMime);

    return { base64: first.bytesBase64, mimeType };
  }

  // ─── gemini ───────────────────────────────────────────────────────────────
  private async _generateGemini(
    prompt: string,
    modelId: string,
    base: string,
    apiKey: string,
  ): Promise<ImageGenResult> {
    const url = `${base}/gemini/v1beta/models/${modelId}:streamGenerateContent`;
    let resp: Response;
    try {
      resp = await fetch(url, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'x-goog-api-key': apiKey,   // T-14-01: Google 鉴权模式，非 Bearer
        },
        body: JSON.stringify({
          contents: [{ role: 'user', parts: [{ text: prompt }] }],
          generationConfig: {
            responseModalities: ['TEXT', 'IMAGE'],
            imageConfig: { aspectRatio: '1:1', imageSize: '1k' },
          },
        }),
      });
    } catch {
      throw new NetworkError('gemini 生图请求网络失败');
    }

    if (!resp.ok) {
      const errBody = await resp.json().catch(() => ({}));
      throw mapHttpError(resp.status, errBody);
    }

    // gemini 响应是 JSON 数组（多 chunk），图片可能在任意 chunk 的 parts 里
    const chunks = await resp.json() as unknown[];
    return parseGeminiChunks(chunks);
  }
}

// ─── 内部 helper ──────────────────────────────────────────────────────────────

/**
 * doubao 签名 URL → 裸 base64（D-02）
 * 用 arrayBuffer 路径（浏览器标准，无 FileReader 回调复杂度）
 * 注意：btoa 只接受 Latin-1，需先逐字节转字符串（A3）
 */
async function fetchUrlToBase64(imageUrl: string): Promise<{ base64: string; mimeType: string }> {
  let imgResp: Response;
  try {
    imgResp = await fetch(imageUrl);
  } catch {
    // Pitfall 4: CORS 拦截会抛 TypeError，此处统一转 NetworkError
    throw new NetworkError('doubao 图片 URL 下载失败（可能 CORS 限制，建议切换 gpt-image-2 或 gemini）');
  }
  if (!imgResp.ok) throw new NetworkError(`doubao 图片 URL 获取失败（${imgResp.status}）`);

  const contentType = imgResp.headers.get('content-type') ?? 'image/png';
  const mimeType = contentType.split(';')[0].trim();

  const buf = await imgResp.arrayBuffer();
  const bytes = new Uint8Array(buf);
  // btoa 不接受 multi-byte，需逐字节 fromCharCode（A3）
  let binary = '';
  const CHUNK = 8192;
  for (let i = 0; i < bytes.length; i += CHUNK) {
    binary += String.fromCharCode(...bytes.subarray(i, i + CHUNK));
  }
  return { base64: btoa(binary), mimeType };
}

/**
 * Gemini JSON 数组多 chunk 遍历，找到含 inlineData 的 part（D-03）
 * 跳过只含 thoughtSignature 的 part（~1.5M 字符）
 */
function parseGeminiChunks(chunks: unknown[]): { base64: string; mimeType: string } {
  type GeminiPart = {
    inlineData?: { data: string; mimeType: string };
    thoughtSignature?: string;
    text?: string;
  };
  type GeminiChunk = {
    candidates?: Array<{
      content?: { parts?: GeminiPart[] };
    }>;
  };

  for (const chunk of chunks as GeminiChunk[]) {
    const parts = chunk.candidates?.[0]?.content?.parts ?? [];
    for (const part of parts) {
      // D-03: 跳过只含 thoughtSignature 的 part；找到含 inlineData 的 part
      if (part.inlineData?.data) {
        return {
          base64: part.inlineData.data,
          mimeType: part.inlineData.mimeType,
        };
      }
    }
  }
  throw new NetworkError('gemini 响应未找到 inlineData 图片数据');
}

/** mimeType 规范化：'png' → 'image/png'，'jpeg'/'jpg' → 'image/jpeg'（Open Question 2） */
function normalizeMimeType(raw: string): string {
  if (raw.startsWith('image/')) return raw;
  const map: Record<string, string> = { png: 'image/png', jpeg: 'image/jpeg', jpg: 'image/jpeg', webp: 'image/webp' };
  return map[raw.toLowerCase()] ?? `image/${raw.toLowerCase()}`;
}
```

**关键检查：**
- apiKey 出现在三处 header（Authorization x2 + x-goog-api-key x1），不进任何 body
- 旧的 `b64_json`、`usage`、`gpt-image-1`、`/images/generations` 全部删除
- doubao URL 在 fetchUrlToBase64 内消费，不返回给调用方
  </action>
  <verify>
    <automated>npx vitest run src/providers/aihubmix-image.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "gpt-image-1\|/images/generations\|b64_json.*usage" src/providers/aihubmix-image.ts 返回 0（旧字段已删）
    - grep -c "fetchUrlToBase64\|parseGeminiChunks\|normalizeMimeType" src/providers/aihubmix-image.ts 返回 3（helper 函数存在）
    - grep -c "x-goog-api-key" src/providers/aihubmix-image.ts 返回 >= 1（gemini 鉴权）
    - grep -c "Authorization.*Bearer" src/providers/aihubmix-image.ts 返回 >= 2（doubao + gpt-image-2）
    - grep -v "^[[:space:]]*//" src/providers/aihubmix-image.ts | grep -c "apiKey" 返回的行均在 header 赋值行（apiKey 不进 body）
    - npx vitest run src/providers/aihubmix-image.test.ts 所有 5 个用例通过（exit 0）
    - npx tsc --noEmit 不报 aihubmix-image.ts 类型错误
  </acceptance_criteria>
  <done>aihubmix-image.ts 完整重写为三路解析器；doubao URL 内部 fetch 转 base64；gpt-image-2 从 output.b64_json[0].bytesBase64 取 base64 并规范化 mimeType；gemini 遍历 chunks/parts 跳过 thoughtSignature；apiKey 仅进 header；aihubmix-image.test.ts 从红变绿（5 个用例全通过）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → AiHubMix API | fetch 发出，apiKey 在 header 里；response body 含图片 base64（不含 apiKey） |
| AiHubMix API → doubao TOS 签名 URL | 内部 fetchUrlToBase64 二次 fetch，URL 消费后丢弃 |
| aihubmix-image.ts → 调用方（Phase 16） | 返回 { base64, mimeType }，裸 base64，不含 URL 或 apiKey |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | aihubmix-image.ts 三路请求 | mitigate | apiKey 仅注入 `Authorization: Bearer ${apiKey}` 或 `'x-goog-api-key': apiKey` header。不进 request body（三路 body 均不含 apiKey 字段）。`mapHttpError` 固定字面量 message，不插 apiKey 变量。fetch throw → NetworkError（固定字符串）。VERIFIED: 代码模式参照 aihubmix-vision.ts（T-01-04 已验证）。 |
| T-14-02 | Information Disclosure | doubao 签名 URL 处理（_generateDoubao + fetchUrlToBase64） | mitigate | doubao 响应里的签名 URL 仅在 `fetchUrlToBase64()` 内作为参数使用，函数返回 base64 字节后 URL 变量超出作用域。不存入任何变量（只传给 fetchUrlToBase64），不进日志、不进 error.message、不返回给调用方（D-02 的 eager 转换策略）。CORS 错误时 NetworkError message 用固定字面量（不含 URL）。 |
</threat_model>

<verification>
```bash
# 安全验证：apiKey 不进 body
grep -n "apiKey" src/providers/aihubmix-image.ts  # 应只出现在 header 赋值行

# 旧字段已删
grep -c "gpt-image-1\|/images/generations\|\.b64_json\|\.usage" src/providers/aihubmix-image.ts

# 三路 helper 存在
grep -c "fetchUrlToBase64\|parseGeminiChunks\|normalizeMimeType" src/providers/aihubmix-image.ts

# fixture-based 单测全绿
npx vitest run src/providers/aihubmix-image.test.ts

# provider 目录全测试
npx vitest run src/providers/ 2>&1 | tail -10

# tsc 类型检查
npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- aihubmix-image.ts 完整实现三路解析器（doubao/gpt-image-2/gemini）
- doubao URL 在 provider 内部 fetch 并转 base64，不返回给调用方（T-14-02）
- apiKey 不进任何 request body，不进 error.message（T-14-01）
- npx vitest run src/providers/aihubmix-image.test.ts 全绿（5 个用例）
- tsc --noEmit 无类型错误
</success_criteria>

<output>
完成后创建 `.planning/phases/14-mdl-aihubmix-provider-model-casing/14-05-SUMMARY.md`
</output>

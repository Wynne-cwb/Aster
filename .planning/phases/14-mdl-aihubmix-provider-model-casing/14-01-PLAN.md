---
phase: 14-mdl-aihubmix-provider-model-casing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/types.ts
  - src/providers/__fixtures__/doubao-response.json
  - src/providers/__fixtures__/gpt-image-2-response.json
  - src/providers/__fixtures__/gemini-response.json
  - src/providers/aihubmix-image.test.ts
autonomous: true
requirements:
  - MDL-01
  - MDL-02

must_haves:
  truths:
    - "ImageGenResult 接口返回 { base64: string; mimeType: string }，不含 b64_json 或 usage 字段"
    - "三个 fixture 文件存在于 src/providers/__fixtures__/ 目录，结构符合 RESEARCH.md D-16 截断规范"
    - "aihubmix-image.test.ts 存在并包含三路解析器测试骨架，fetch 被 vi.stubGlobal mock（Wave 0 测试脚手架，初始预期为红，Plan 05 实现后变绿）"
    - "types.ts 接口变更不在项目其余文件引发 cascade 编译错误"
  artifacts:
    - path: "src/providers/types.ts"
      provides: "ImageGenResult 接口（base64+mimeType）+ ImageProvider.generate() 新签名"
      contains: "ImageGenResult"
    - path: "src/providers/__fixtures__/doubao-response.json"
      provides: "doubao response fixture（output 是数组）"
      contains: "output"
    - path: "src/providers/__fixtures__/gpt-image-2-response.json"
      provides: "gpt-image-2 response fixture（output 是对象，非数组）"
      contains: "b64_json"
    - path: "src/providers/__fixtures__/gemini-response.json"
      provides: "gemini response fixture（JSON 数组，含 inlineData + thoughtSignature）"
      contains: "inlineData"
    - path: "src/providers/aihubmix-image.test.ts"
      provides: "三路解析器 fixture-based 单测（初始红，Wave 3 Plan 05 后变绿）"
      exports: ["describe AihubmixImageClient"]
  key_links:
    - from: "src/providers/types.ts"
      to: "src/providers/aihubmix-image.ts"
      via: "ImageGenResult 接口"
      pattern: "ImageGenResult"
    - from: "src/providers/__fixtures__/*.json"
      to: "src/providers/aihubmix-image.test.ts"
      via: "fixture import"
      pattern: "from './__fixtures__/"
---

<objective>
建立 Phase 14 所有后续 plan 赖以成立的两个地基：（1）重新定义 types.ts 里的 ImageGenResult + ImageProvider 接口契约（D-01：裸 base64 + mimeType，删 usage）；（2）创建 Wave 0 测试脚手架——三个精简 fixture 文件 + aihubmix-image.test.ts（初始时测试会红，因为 aihubmix-image.ts 本体在 Plan 05 才重写）。

Purpose: 接口契约先行（Interface-First 模式），所有后续 plan 的实现都指向这个合约。测试文件先建后填，保证 Nyquist 守门在 Plan 05 前就存在。

Output: 更新后的 types.ts，3 个 fixture JSON，aihubmix-image.test.ts（测试骨架）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-CONTEXT.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-RESEARCH.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-PATTERNS.md
@.planning/phases/14-mdl-aihubmix-provider-model-casing/14-VALIDATION.md

<interfaces>
<!-- 从 src/providers/types.ts 提取的现有接口（改动基准） -->

From src/providers/types.ts (当前，需改动):
```typescript
// 当前 ImageGenResult（需废弃）
// 内联在 ImageProvider.generate() 返回类型里，形态类似：
// { b64_json: string; usage?: { input_tokens: number; output_tokens: number; total_tokens: number } }

// 当前 ImageProvider.generate() 签名（需改）
export interface ImageProvider {
  generate(
    prompt: string,
    size: string,
    quality: 'high' | 'medium' | 'low' | 'auto',
    config: ImageConfig,
  ): Promise<{
    b64_json: string;
    usage?: { input_tokens: number; output_tokens: number; total_tokens: number };
  }>;
}

// 改后目标（D-01）：
export interface ImageGenResult {
  base64: string;    // 裸 base64，不带 data: 前缀
  mimeType: string;  // 'image/png' | 'image/jpeg' | string
}

export interface ImageProvider {
  generate(
    prompt: string,
    config: ImageConfig,
    options?: { size?: string; quality?: 'high' | 'medium' | 'low' | 'auto' },
  ): Promise<ImageGenResult>;
}
```

From src/providers/types.ts（不改动的部分）:
```typescript
// ImageConfig 和 LLMConfig 接口结构保持不变
export interface ImageConfig {
  providerId: string;
  baseURL: string;
  apiKey: string;
  model: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重写 types.ts — ImageGenResult 接口契约（D-01）</name>
  <files>src/providers/types.ts</files>
  <read_first>
    - src/providers/types.ts（全文读取，理解现有 ImageProvider、ImageConfig、LLMConfig 结构）
    - 14-PATTERNS.md §`src/providers/types.ts` 节（lines 228–263，改动规范）
    - 14-CONTEXT.md D-01（裸 base64 + mimeType，不带 data: 前缀）+ D-04（澄清：裸 base64 非 data URL）
    - 14-CONTEXT.md Claude's Discretion（usage 字段直接删，cost v2.0 已砍）
  </read_first>
  <behavior>
    - ImageGenResult 导出类型，字段：base64: string（裸 base64，无 data: 前缀）、mimeType: string
    - ImageProvider.generate() 签名变更：(prompt, config, options?) 三参，返回 Promise<ImageGenResult>
    - usage 字段完全删除（不保留可选字段，不加 @deprecated 注释）
    - ImageConfig / LLMConfig / TaskKind 等其余类型不改动
  </behavior>
  <action>
读取 src/providers/types.ts 全文。定位 ImageProvider interface 和 ImageGenResult（可能内联在返回类型里）。

作以下修改：

1. 在文件合适位置（ImageProvider 定义之前）新增独立 export interface ImageGenResult：
   ```typescript
   export interface ImageGenResult {
     base64: string;    // 裸 base64，不带 data: 前缀（D-01）
     mimeType: string;  // 'image/png' | 'image/jpeg' | string
   }
   ```

2. 修改 ImageProvider.generate() 签名为：
   ```typescript
   export interface ImageProvider {
     generate(
       prompt: string,
       config: ImageConfig,
       options?: { size?: string; quality?: 'high' | 'medium' | 'low' | 'auto' },
     ): Promise<ImageGenResult>;
   }
   ```

3. 删除旧的内联返回类型（含 b64_json 和 usage 字段）——整个旧 Promise<{b64_json...}> 返回类型替换为 Promise<ImageGenResult>。

4. 不改动 ImageConfig、LLMConfig、TaskKind 等其他类型。

安全约束：types.ts 是纯类型文件，无运行时逻辑，无安全面。
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ImageGenResult" src/providers/types.ts 返回 >= 2（接口声明 + 用于 generate 返回类型）
    - grep -c "b64_json" src/providers/types.ts 返回 0（旧字段已删）
    - grep -c "usage" src/providers/types.ts 返回 0（usage 已删，D-01）
    - grep -c "base64: string" src/providers/types.ts 返回 >= 1
    - grep -c "mimeType: string" src/providers/types.ts 返回 >= 1
    - tsc --noEmit 对 types.ts 不报错（编译通过）
  </acceptance_criteria>
  <done>ImageGenResult 接口已导出，签名为 { base64: string; mimeType: string }；ImageProvider.generate() 签名已更新；b64_json/usage 已删除；tsc 编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — 创建三路 fixture 文件 + aihubmix-image.test.ts 脚手架</name>
  <files>
    src/providers/__fixtures__/doubao-response.json
    src/providers/__fixtures__/gpt-image-2-response.json
    src/providers/__fixtures__/gemini-response.json
    src/providers/aihubmix-image.test.ts
  </files>
  <read_first>
    - 14-RESEARCH.md §fixture 设计（D-14/D-15/D-16，截断原则 + JSON 结构）
    - 14-PATTERNS.md §`src/providers/__fixtures__/*.json`（fixture 设计规范 + 三个 fixture 的 JSON 示例）
    - 14-PATTERNS.md §`src/providers/aihubmix-image.test.ts`（fetch stub 模式 + describe/it 结构）
    - .planning/spikes/011-image-gen-api-formats/findings.md（三路 wire format 真机实测，确认字段路径）
    - 14-CONTEXT.md D-16（fixture 不含密钥，base64 截断 4 字符）
  </read_first>
  <action>
**步骤 1：创建 __fixtures__ 目录下三个 JSON 文件**

（1）src/providers/__fixtures__/doubao-response.json：
doubao output 是数组（每项 {url}）。URL 截断为占位符（D-16，fetch 被 mock，URL 内容不重要）：
```json
{ "output": [{ "url": "<truncated-for-ci>" }] }
```

（2）src/providers/__fixtures__/gpt-image-2-response.json：
gpt-image-2 output 是对象（非数组），b64_json 是数组，每项 {bytesBase64, mimeType}。
bytesBase64 截断为 4 字符（D-16）；mimeType 值是 "png"（不是 "image/png"，解析器需规范化）：
```json
{
  "output": {
    "b64_json": [{ "bytesBase64": "iVBO", "mimeType": "png" }],
    "urls": []
  }
}
```

（3）src/providers/__fixtures__/gemini-response.json：
gemini 响应是 JSON 数组（多 chunk）。包含两个 chunk：
- 第一个 chunk 含 thoughtSignature（巨大字段，此处截断）+ inlineData
- 结构必须体现：inlineData 和 thoughtSignature 在同一个 part 里（D-03，解析器要跳过 thoughtSignature，取 inlineData.data）
- base64 data 截断为 4 字符（D-16）：
```json
[
  {
    "candidates": [{
      "content": {
        "parts": [
          {
            "thoughtSignature": "<truncated-for-ci>",
            "inlineData": { "data": "/9j/", "mimeType": "image/jpeg" }
          }
        ],
        "role": "model"
      },
      "index": 0
    }],
    "usageMetadata": { "promptTokenCount": 4, "candidatesTokenCount": 1535, "totalTokenCount": 1539 },
    "modelVersion": "gemini-3.1-flash-image-preview"
  },
  {
    "candidates": [{
      "content": { "parts": [{ "text": "" }] },
      "finishReason": "STOP",
      "index": 0
    }]
  }
]
```

**步骤 2：创建 src/providers/aihubmix-image.test.ts**

遵循 14-PATTERNS.md §aihubmix-image.test.ts 中的 fetch stub 模式（来自 openai-compat.test.ts）：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import doubaoFixture from './__fixtures__/doubao-response.json';
import gptImage2Fixture from './__fixtures__/gpt-image-2-response.json';
import geminiFixture from './__fixtures__/gemini-response.json';
import { AihubmixImageClient } from './aihubmix-image';
import type { ImageConfig } from './types';

// doubao 路径：两次 fetch（predictions API 拿 fixture；图片 URL fetch 转 base64）
// gpt-image-2 路径：一次 fetch（predictions API，直接返回 b64_json）
// gemini 路径：一次 fetch（streamGenerateContent，返回 JSON 数组）
vi.stubGlobal(
  'fetch',
  vi.fn().mockImplementation(async (url: string) => {
    if (typeof url === 'string' && url.includes('doubao') && url.includes('predictions')) {
      return { ok: true, json: async () => doubaoFixture };
    }
    if (typeof url === 'string' && url.includes('doubao')) {
      // doubao 图片 URL fetch（转 base64）
      return {
        ok: true,
        headers: { get: (_: string) => 'image/png' },
        arrayBuffer: async () => new ArrayBuffer(4),
      };
    }
    if (typeof url === 'string' && url.includes('gpt-image-2')) {
      return { ok: true, json: async () => gptImage2Fixture };
    }
    if (typeof url === 'string' && url.includes('gemini')) {
      return { ok: true, json: async () => geminiFixture };
    }
    throw new Error(`Unexpected URL in test: ${String(url)}`);
  }),
);

const makeConfig = (model: string): ImageConfig => ({
  providerId: 'aihubmix-image',
  baseURL: 'https://aihubmix.com',
  apiKey: 'test-key-not-real',
  model,
});

describe('AihubmixImageClient — 三路解析器（MDL-01）', () => {
  beforeEach(() => {
    vi.mocked(fetch).mockClear();
  });

  it('doubao: output[0].url → fetch → base64，返回 { base64, mimeType }（D-01）', async () => {
    const client = new AihubmixImageClient();
    const result = await client.generate('画一棵树', makeConfig('doubao-seedream-5.0-lite'));
    expect(result).toHaveProperty('base64');
    expect(result).toHaveProperty('mimeType');
    expect(result.base64).not.toContain('data:');  // 裸 base64，无 data: 前缀
    expect(result.mimeType).toBeTruthy();
  });

  it('gpt-image-2: output.b64_json[0].bytesBase64 取 base64，mimeType 规范化 png→image/png', async () => {
    const client = new AihubmixImageClient();
    const result = await client.generate('画一棵树', makeConfig('gpt-image-2'));
    expect(result.base64).toBe('iVBO');
    expect(result.mimeType).toBe('image/png');  // 规范化：'png' → 'image/png'
    expect(result.base64).not.toContain('data:');
  });

  it('gemini: 跳过 thoughtSignature，从 inlineData.data 取 base64（D-03）', async () => {
    const client = new AihubmixImageClient();
    const result = await client.generate('画一棵树', makeConfig('gemini-3.1-flash-image-preview'));
    expect(result.base64).toBe('/9j/');
    expect(result.mimeType).toBe('image/jpeg');
    expect(result.base64).not.toContain('data:');
  });

  it('三路返回值 base64 均不含 data: 前缀（D-01/D-04）', async () => {
    const client = new AihubmixImageClient();
    const models = ['doubao-seedream-5.0-lite', 'gpt-image-2', 'gemini-3.1-flash-image-preview'];
    for (const model of models) {
      const result = await client.generate('test', makeConfig(model));
      expect(result.base64, `model ${model} 的 base64 不应含 data: 前缀`).not.toContain('data:');
    }
  });

  it('apiKey 不出现在 error.message（T-14-01）', async () => {
    vi.mocked(fetch).mockRejectedValueOnce(new TypeError('Failed to fetch'));
    const client = new AihubmixImageClient();
    await expect(
      client.generate('test', makeConfig('doubao-seedream-5.0-lite')),
    ).rejects.toSatisfy((err: unknown) => {
      const msg = err instanceof Error ? err.message : String(err);
      return !msg.includes('test-key-not-real');  // apiKey 不进 error.message
    });
  });
});
```

注意：此时 AihubmixImageClient 尚未重写（Plan 05 才完成），这些测试会红。这是预期的 Wave 0 行为——测试文件先创建，作为 Plan 05 的 RED 基准。
  </action>
  <verify>
    <automated>ls src/providers/__fixtures__/ && npx vitest run src/providers/aihubmix-image.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ls src/providers/__fixtures__/ 输出三个文件：doubao-response.json、gpt-image-2-response.json、gemini-response.json
    - cat src/providers/__fixtures__/doubao-response.json 包含 "output" 和 "url" 字段，output 是数组
    - cat src/providers/__fixtures__/gpt-image-2-response.json 包含 "b64_json" 和 "bytesBase64" 字段，output 是对象（非数组）
    - cat src/providers/__fixtures__/gemini-response.json 是 JSON 数组，包含 "inlineData" 和 "thoughtSignature" 字段
    - src/providers/aihubmix-image.test.ts 存在，包含 describe('AihubmixImageClient — 三路解析器')
    - grep -c "vi.stubGlobal" src/providers/aihubmix-image.test.ts 返回 >= 1
    - grep -c "data:" src/providers/__fixtures__/doubao-response.json 返回 0（fixture 不含 data URL）
    - 测试运行：此时可能红（AihubmixImageClient 尚未重写），但测试文件语法无误（tsc 不报 test 文件语法错）
  </acceptance_criteria>
  <done>三个 fixture JSON 文件创建完毕，结构符合 D-16 截断规范；aihubmix-image.test.ts 测试脚手架创建完毕，包含 5 个测试用例。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| types.ts 接口契约 | 纯类型定义，无运行时 I/O，无安全面 |
| fixture JSON 文件 | 静态测试数据，不含真实 apiKey 或完整 base64（D-16） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | 本 plan 是纯接口定义 + 静态 fixture 数据，无新 I/O、无 secrets 处理、无网络调用。T-14-01（apiKey 泄露）和 T-14-02（doubao URL 被记录）的实际缓解发生在 Plan 05（aihubmix-image.ts 实现层）。本 plan 对安全无影响。 |

No HIGH/MEDIUM threats — 纯接口定义 + 静态 fixture 数据，无新 I/O 或 secrets。
</threat_model>

<verification>
```bash
# Task 1 验证
grep -c "ImageGenResult" src/providers/types.ts
grep -c "b64_json" src/providers/types.ts   # 期望 0
grep -c "usage" src/providers/types.ts       # 期望 0

# Task 2 验证
ls src/providers/__fixtures__/
grep "inlineData" src/providers/__fixtures__/gemini-response.json
grep "thoughtSignature" src/providers/__fixtures__/gemini-response.json
grep "b64_json" src/providers/__fixtures__/gpt-image-2-response.json

# tsc 整体编译（types 改动引起的 cascade error 是预期的——image.ts 本体 Plan 05 才改）
npx tsc --noEmit 2>&1 | grep -v "aihubmix-image.ts" | head -20
```
</verification>

<success_criteria>
- src/providers/types.ts 导出 ImageGenResult 接口（base64 + mimeType）
- ImageProvider.generate() 签名更新为 (prompt, config, options?) 三参
- b64_json 和 usage 字段从 types.ts 完全删除
- 三个 fixture 文件存在，内容符合 D-16 截断规范（base64 为 4 字符占位，无真实 apiKey）
- aihubmix-image.test.ts 测试脚手架存在，包含三路解析器测试用例
</success_criteria>

<output>
完成后创建 `.planning/phases/14-mdl-aihubmix-provider-model-casing/14-01-SUMMARY.md`
</output>

# Phase 04.1: Aster Redesign Migration — UI 设计系统迁移到 teal - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 04.1-aster-redesign-migration-ui-teal
**Areas discussed:** 迁移范围边界, Agent 运行时面设计深度, 收尾 & 验证范围
**Areas offered but not selected:** Token 命名策略（→ Claude's Discretion in CONTEXT）

---

## 灰区选择（multiSelect）

| Option | Description | Selected |
|--------|-------------|----------|
| 迁移范围边界 | 纯重皮 vs 重皮+结构调整；卡/chips 划线 | ✓ |
| Token 命名策略 | 采用设计稿变量名 vs 保留现有变量名 | （未选 → Discretion） |
| Agent 运行时面设计深度 | 设计稿空白的 in-flight 面补到多深 | ✓ |
| 收尾 & 验证范围 | doc/记忆/skill 更新 + dark/三宿主 UAT 深度 | ✓ |

---

## ① 迁移范围边界

### 重皮范围
| Option | Description | Selected |
|--------|-------------|----------|
| 重皮 + 结构调整一起做 | 同时落选区折进 selpill、齿轮移 tools 行 | ✓ |
| 纯重皮，结构冻结 | 只换 token，组件树不动 | |

### ContextCard 去留
| Option | Description | Selected |
|--------|-------------|----------|
| 移除顶栏，折进 InputBar selpill | CR-01 信息迁入 selpill，功能不丢 | ✓ |
| 保留顶栏 ContextCard | 只重皮 | |

### 写回卡/chips 划线
| Option | Description | Selected |
|--------|-------------|----------|
| 卡推 Phase 5/6，empty-state 只做壳 | 不做假 chips | ✓ |
| empty-state chips 也做（静态/占位） | | |

**User's choice:** 全取推荐。
**Notes:** 本 phase 同时改结构而非冻结组件树，避免组件摸两遍；ContextCard 退役、CR-01 进 selpill 文案；写回多动作卡 = Phase 5，killer chips = Phase 6。

---

## ② Agent 运行时面设计深度

### 补设计深度
| Option | Description | Selected |
|--------|-------------|----------|
| 视觉重做，交互骨架不动 | teal 重做视觉，pause/abort/step counter/三态文案逻辑保留 | ✓ |
| 只重皮到 teal（最省） | 布局视觉都不动 | |
| 完整重设计（含交互） | 连交互都重做 | |

### read 折叠卡视觉
| Option | Description | Selected |
|--------|-------------|----------|
| 套设计稿写回卡折叠范式 | chevron + 单行 target + 展开 body，read/write 同源 | ✓ |
| 保留现有折叠样式只换 teal 皮 | | |

### ErrorBubble / give-up 红卡
| Option | Description | Selected |
|--------|-------------|----------|
| 重皮成设计稿 ErrorBubble 定稿，红卡套同范式 | left accent stripe + 红 head + mono code | ✓ |
| 只换颜色，不动现有错误卡形态 | | |

**User's choice:** 全取推荐。
**Notes:** 视觉按新 teal 语言重做但不越位改 Phase 3/4 已真机验证的交互；read/write 步骤卡折叠范式同源；give-up 红卡保持诚实禁用（不放假撤销键，undo 是 Phase 5）。

---

## ③ 收尾 & 验证范围

### 配套 doc/记忆/skill
| Option | Description | Selected |
|--------|-------------|----------|
| 全部进本 phase | CLAUDE.md + 记忆 + UI-SPEC 过时 + sketch-wrap-up design skill | ✓ |
| doc/记忆/UI-SPEC 进，design skill 后续单跑 | | |
| 只改 CLAUDE.md + 记忆 | | |

### dark + 三宿主真机 UAT 深度
| Option | Description | Selected |
|--------|-------------|----------|
| light 三宿主真机必做 + dark 浏览器自查为主、真机抽查 1 宿主 | | ✓ |
| light + dark 都全量三宿主真机 QA（6 路） | | |
| light 三宿主真机；dark 只浏览器自查不真机 | | |

**User's choice:** 全取推荐。
**Notes:** 不更新 CLAUDE.md/记忆会误导下个 phase 的 Claude（与实际 styles.css 矛盾）；真机 UAT 成本在用户身上，light 是主风险面必做，dark 靠 Claude 浏览器自查 + 真机抽查 1 宿主。

---

## Claude's Discretion

- Token 命名策略（用户未选此灰区）→ 采用设计稿 `aster.css` 变量名，让未来设计交付 1:1 对齐。
- 字体引入 Inter + JetBrains Mono（沿用 Google Fonts display=swap 机制）。
- 动效/阴影/radius/spacing token 照设计稿定稿值移植；组件拆分优先级由 planner 定。

## Deferred Ideas

- 写回多动作聚合卡 → Phase 5；empty-state killer chips → Phase 6；PdfPrompt / 附件上传 / 英文 i18n → 后续。
- 已评估未折入的 todo：builtin-model-dropdown（Phase 4 已落）、copy-chat-history（Phase 5 CARRY-03）。

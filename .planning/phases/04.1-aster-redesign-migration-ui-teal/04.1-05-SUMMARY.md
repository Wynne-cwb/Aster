---
phase: "04.1"
plan: "05"
subsystem: "ui-components"
tags: ["teal-redesign", "wave-3", "reskin", "agentcontrolbar", "onboarding", "settings", "i18n"]
dependency_graph:
  requires: ["04.1-02"]
  provides: ["AgentControlBar teal pill", "Onboarding modal-scrim", "Settings provider-row"]
  affects: ["App.tsx (via SettingsPanel)", "styles.css"]
tech_stack:
  added: []
  patterns:
    - "quiet pill (agent-bar: surface-2 bg + border + radius-full, 无 backdrop-filter)"
    - "modal-scrim (遮罩 + 居中 modal 卡片)"
    - "provider-row + badge-accent + badge-success"
    - "btn/btn-primary/btn-ghost/btn-sm 按钮系"
    - "switch toggle (checked → var(--accent))"
    - "select-wrap + .input.select + .select-caret"
    - "ChevronLeftIcon 返回（不再用 scaleX(-1) 翻转）"
key_files:
  modified:
    - src/components/AgentControlBar.tsx
    - src/components/Onboarding/OnboardingModal.tsx
    - src/components/Onboarding/Step1Keys.tsx
    - src/components/Onboarding/Step2Guide.tsx
    - src/components/Settings/SettingsPanel.tsx
    - src/components/Settings/ProviderList.tsx
    - src/components/Settings/ProviderForm.tsx
    - src/styles.css
    - src/i18n/locales/zh-CN/messages.po
    - src/i18n/locales/zh-CN/messages.ts
decisions:
  - "Step2Guide 新增 onBack prop，OnboardingModal 内部管理 step 状态，把 goBack 回调向下传"
  - "ProviderList 用 storage.get(KEY_PREFIX + id) 直读判断 hasKey（不从 useProviderStore selector 拿 getKey，避免 UI 层注释限制歧义）"
  - "settings-head 放在 SettingsPanel 组件内部（不在 App.tsx 的 settings-overlay 容器内），与旧架构保持一致"
metrics:
  duration: "~14 minutes"
  completed_date: "2026-05-29"
  tasks: 3
  files_modified: 10
---

# Phase 04.1 Plan 05: AgentControlBar + Onboarding + Settings 重皮 Summary

**一句话：** Wave 3 并行组 B — AgentControlBar 去玻璃拟态换 quiet pill、Onboarding 换 modal-scrim + modal 卡片、Settings 换 provider-row + badge + switch + select-wrap，全部用 ChevronLeftIcon 返回，无 backdrop-filter。

## 目标完成情况

| 验证条件 | 结果 |
|---------|------|
| `grep -c "agent-bar" AgentControlBar.tsx` ≥ 1 | 1 ✓ |
| `grep -c "aster-agent-bar\|backdrop-filter\|glass-bg" AgentControlBar.tsx` = 0 | 0 ✓ |
| `grep -c "backdrop-filter:" styles.css` = 0（CSS 属性，不含注释）| 0 ✓ |
| `grep -c "modal-scrim" OnboardingModal.tsx` ≥ 1 | 2 ✓（注释+用法）|
| `grep -c "provider-row" ProviderList.tsx` ≥ 1 | 2 ✓ |
| `grep -c "ChevronLeftIcon" SettingsPanel.tsx` ≥ 1 | 4 ✓ |
| `npm test -- --run` — 无新增失败 | 仅 AGENT-02 预存在 ✓ |
| `npx tsc --noEmit` — 无新增 TS 错误 | 通过 ✓ |

## 任务执行

### Task 1: AgentControlBar 重皮（commit: 7b6da93）

**文件：** `src/components/AgentControlBar.tsx`, `src/styles.css`

- `className` 替换：`aster-agent-bar` → `agent-bar`；`aster-agent-bar__step/phase/stall` → `agent-step/phase/stall`；`aster-iconbtn aster-agent-bar__btn` → `btn-icon`
- styles.css 追加 `.agent-bar`（surface-2 + border + radius-full，**无 backdrop-filter**，AGENT-01 硬约束）
- styles.css 追加 `.btn-icon` 通用图标按钮（focus-visible → var(--ring-focus)）
- 所有现有 Phase 3/4 交互骨架（pause/abort/step counter/stall/三态文案/5秒安抚）完全不动（D-04）

### Task 2a: Onboarding 重皮（commit: 6256432）

**文件：** `src/components/Onboarding/OnboardingModal.tsx`, `Step1Keys.tsx`, `Step2Guide.tsx`, `src/styles.css`, `src/i18n/locales/zh-CN/messages.po`

- OnboardingModal：`aster-onboarding-overlay` → `modal-scrim` + 居中 `.modal` 卡片（320px, shadow-pop, r-16）
- `modal-brand` 行：logo（22px）+ 品牌名 + 步骤指示（"01 / 02"）
- Step1Keys：按钮换 `.btn .btn-ghost/.btn-primary .btn-sm`；输入框换 `.input`
- Step2Guide：新增 `onBack` prop（从 OnboardingModal 传入）；host-card 范式（Lucide 图标，版权安全）
- styles.css 追加：`.modal-scrim/.modal/.modal-brand/.modal-title/.modal-sub/.modal-body/.modal-foot`、`.btn` 系、`.input`、`.host-card` 规则
- messages.po 更新：新增「上一步」及行号变更（I18N-01 守门通过）

### Task 2b: Settings 重皮（commit: d54c4f8）

**文件：** `SettingsPanel.tsx`, `ProviderList.tsx`, `ProviderForm.tsx`, `src/styles.css`, `messages.po/ts`

- SettingsPanel：`ChevronIcon + scaleX(-1)` → `ChevronLeftIcon`；`aster-settings__header` → `settings-head + back-btn + head-title`
- ProviderList：`aster-provider-item` → `provider-row`；旧 `aster-badge-default/builtin` → `badge badge-accent/badge-success`；编辑按钮换 `.btn-icon`
- ProviderForm：`aster-field` → `.input`；内置 select → `.select-wrap .input.select + .select-caret`（ChevronDownIcon）；`builtin-note` 提示；底部按钮换 `.btn .btn-primary/btn-ghost .btn-sm`
- styles.css 追加：`.settings-head/.back-btn/.head-title`、`.provider-row/.pinfo/.pname/.pmodel/.pactions`、`.badge/.badge-accent/.badge-success`、`.select-wrap/.select-caret`、`.builtin-note`、`.switch`、`.input--error`
- messages.po 更新：新增「已配 Key」「未配 Key」「内置 Provider · 名称与 Base URL 不可改」（I18N-01）

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] i18n coverage 测试失败（两轮）**
- **Found during:** Task 2a 和 Task 2b
- **Issue:** 新增 `<Trans>` 字符串未提取到 messages.po，coverage 守门测试失败
- **Fix:** 每次新增字符串后运行 `npm run extract` + `npm run build`，更新 messages.po/ts 并 stage 到 git
- **Files modified:** `src/i18n/locales/zh-CN/messages.po`, `src/i18n/locales/zh-CN/messages.ts`
- **Commits:** 包含在 Task 2a/2b commit 中

**2. [Rule 2 - Missing critical] Step2Guide 缺少 onBack prop**
- **Found during:** Task 2a 设计
- **Issue:** 原 Step2Guide 无返回按钮（只有「开始使用」），新 OnboardingModal 设计要支持 "上一步" 回到 Step1
- **Fix:** 新增 `onBack: () => void` prop，从 OnboardingModal 的 `goBack` 函数传入；Step2Guide 的「上一步」按钮调用 `onBack()`
- **Files modified:** `src/components/Onboarding/Step2Guide.tsx`, `src/components/Onboarding/OnboardingModal.tsx`

**3. [Rule 2 - Missing] logo import 路径**
- **Found during:** Task 2a
- **Issue:** 初稿用 `import logo from '/assets/logo.png'`，但项目中 ChatStream.tsx 用 `import.meta.env.BASE_URL` 模式
- **Fix:** 改为 `const logo = ${import.meta.env.BASE_URL}assets/icon-80.png`（与 ChatStream 一致）
- **Files modified:** `src/components/Onboarding/OnboardingModal.tsx`

## Known Stubs

无。所有组件迁移均连接真实数据（providerStore、attachEnabled、agentStore 等），无硬编码占位。

## Threat Flags

无新增安全敏感面。本 plan 为纯视觉 className 替换，无数据流/存储/网络变化，与 threat_model 一致。

## Self-Check: PASSED

**Files exist:**
- [x] src/components/AgentControlBar.tsx — agent-bar 类名存在
- [x] src/components/Onboarding/OnboardingModal.tsx — modal-scrim 存在
- [x] src/components/Settings/SettingsPanel.tsx — ChevronLeftIcon 存在
- [x] src/components/Settings/ProviderList.tsx — provider-row 存在
- [x] src/components/Settings/ProviderForm.tsx — select-wrap + builtin-note 存在
- [x] src/styles.css — agent-bar/modal-scrim/provider-row/badge/switch CSS 规则存在

**Commits exist:**
- [x] 7b6da93 — feat(04.1-05): AgentControlBar 重皮
- [x] 6256432 — feat(04.1-05): Onboarding 重皮
- [x] d54c4f8 — feat(04.1-05): Settings 重皮

---
phase: "04.1"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/icons.tsx
  - src/store/chat.ts
autonomous: true
requirements:
  - BUNDLE-01

must_haves:
  truths:
    - "icons.tsx 新增 6 个图标（GearIcon / PaperclipIcon / ChevronDownIcon / ChevronLeftIcon / AlertCircleIcon / DocumentIcon），strokeWidth 全局从 1.75 改为 1.5"
    - "Message 类型有 ts?: number 字段（时间戳），供 ChatBubble 渲染使用"
    - "build 通过，无 TS 报错"
  artifacts:
    - path: "src/components/icons.tsx"
      provides: "完整图标库，stroke 1.5，含 6 个新图标"
      exports:
        - "GearIcon"
        - "PaperclipIcon"
        - "ChevronDownIcon"
        - "ChevronLeftIcon"
        - "AlertCircleIcon"
        - "DocumentIcon"
    - path: "src/store/chat.ts"
      provides: "Message 类型含 ts 字段"
      contains: "ts?: number"
  key_links:
    - from: "src/components/InputBar.tsx"
      to: "src/components/icons.tsx"
      via: "GearIcon / PaperclipIcon import"
      pattern: "GearIcon|PaperclipIcon"
    - from: "src/components/ChatStream.tsx"
      to: "src/components/icons.tsx"
      via: "ChevronDownIcon import（D-05 折叠卡）"
      pattern: "ChevronDownIcon"
    - from: "src/components/ChatBubble.tsx"
      to: "src/store/chat.ts"
      via: "message.ts 时间戳渲染"
      pattern: "message\\.ts"
---

<objective>
Wave 1 前置工作：补全图标库 + 扩展 Message 类型。

Purpose: icons.tsx 缺少 GearIcon / PaperclipIcon / ChevronDownIcon / ChevronLeftIcon / AlertCircleIcon / DocumentIcon，这 6 个图标被后续 Wave 2/3/4 多个组件依赖。Message.ts 字段缺失会让 ChatBubble 时间戳渲染失败（TypeScript 报错）。两项修改都是 Wave 2+ 组件的先决条件。

Output: icons.tsx 含 6 个新具名 export + strokeWidth 全局 base 改 1.5；chat.ts Message 接口补 ts 字段。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-RESEARCH.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md

<interfaces>
<!-- 当前 icons.tsx 关键结构（executor 直接对照修改） -->
<!-- src/components/icons.tsx lines 9-17 -->
```typescript
const base = {
  viewBox: '0 0 24 24', fill: 'none', stroke: 'currentColor',
  strokeWidth: 1.75,    // ← 改为 1.5
  strokeLinecap: 'round' as const,
  strokeLinejoin: 'round' as const,
  'aria-hidden': true,
};
// 现有 exports: SettingsIcon / UploadIcon / SendIcon / ChevronIcon / StopIcon / InsertIcon
//               RetryIcon / XIcon / AlertIcon / PlusIcon / TrashIcon / CheckIcon
//               EyeIcon / EyeOffIcon / PauseIcon / PlayIcon
// 缺少：GearIcon / PaperclipIcon / ChevronDownIcon / ChevronLeftIcon / AlertCircleIcon / DocumentIcon
```

<!-- Message 接口（src/store/chat.ts lines 41-59） -->
```typescript
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'tool' | 'error';
  content: string;
  isStreaming?: boolean;
  errorCode?: string;
  retryPrompt?: string;
  toolCalls?: ToolCall[];
  toolCallId?: string;
  toolName?: string;
  toolResult?: ToolResult;
  agentRunId?: string;
  agentStep?: number;
  // 缺少 ts?: number
}
```

<!-- 设计包图标参考（.planning/design/aster-redesign/src/icons.jsx 对照）-->
<!-- 6 个新图标的 SVG path 见 04.1-PATTERNS.md §icons.tsx 目标形态 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 补全 icons.tsx — 全局 strokeWidth 1.5 + 6 个新图标</name>
  <files>src/components/icons.tsx</files>

  <read_first>
    1. src/components/icons.tsx（当前全文，了解 base 对象结构和现有 export 列表）
    2. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §icons.tsx 目标形态（有完整 SVG path）
    3. .planning/design/aster-redesign/src/icons.jsx（设计包图标原始定义）
  </read_first>

  <action>
修改 src/components/icons.tsx：

**1. 全局 base 对象修改：**
将 `strokeWidth: 1.75` → `strokeWidth: 1.5`（第 13 行）

**2. 在文件末尾追加 6 个新具名 export（严格按以下 SVG path 实现，stroke 1.5）：**

```tsx
/** 齿轮 — InputBar tools 行左下，尺寸/strokeWidth 可覆盖 */
export function GearIcon({ size = 24, strokeWidth = 1.5 }: { size?: number; strokeWidth?: number }): ReactElement {
  return (
    <svg {...base} strokeWidth={strokeWidth} width={size} height={size}>
      <circle cx="12" cy="12" r="3" />
      <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
    </svg>
  );
}

/** 回形针附件 — InputBar tools 行禁用态 */
export function PaperclipIcon({ size = 24 }: { size?: number }): ReactElement {
  return (
    <svg {...base} width={size} height={size}>
      <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48" />
    </svg>
  );
}

/** 折叠 chevron（向下）— D-05 折叠卡；CSS .is-up 时 rotate(180deg) 表示展开 */
export function ChevronDownIcon({ size = 24 }: { size?: number }): ReactElement {
  return (
    <svg {...base} width={size} height={size}>
      <path d="M6 9l6 6 6-6" />
    </svg>
  );
}

/** 返回 chevron（向左）— Settings header 返回 */
export function ChevronLeftIcon({ size = 24 }: { size?: number }): ReactElement {
  return (
    <svg {...base} width={size} height={size}>
      <path d="M15 18l-6-6 6-6" />
    </svg>
  );
}

/** 警示圆圈 — pane-banner 缺 Key 提示 */
export function AlertCircleIcon({ size = 24 }: { size?: number }): ReactElement {
  return (
    <svg {...base} width={size} height={size}>
      <circle cx="12" cy="12" r="10" />
      <line x1="12" y1="8" x2="12" y2="12" />
      <line x1="12" y1="16" x2="12.01" y2="16" />
    </svg>
  );
}

/** 文件文本 — selpill 前缀图标 */
export function DocumentIcon({ size = 24 }: { size?: number }): ReactElement {
  return (
    <svg {...base} width={size} height={size}>
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
      <polyline points="14 2 14 8 20 8" />
      <line x1="16" y1="13" x2="8" y2="13" />
      <line x1="16" y1="17" x2="8" y2="17" />
    </svg>
  );
}
```

**注意：**
- GearIcon 独立 props 接口（size / strokeWidth 可覆盖）供 InputBar 传入 `size={15} strokeWidth={1.4}` 使用
- 其余新图标 props 只含 size（沿用 base 的 strokeWidth 1.5）
- 不要删除任何现有图标（SettingsIcon 等保留，供可能的回退）
- 现有其他图标的实际 SVG path 不需改变，只有 base.strokeWidth 变化
  </action>

  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "error TS|icons.tsx" | head -20</automated>
    <automated>grep -c "export function GearIcon\|export function PaperclipIcon\|export function ChevronDownIcon\|export function ChevronLeftIcon\|export function AlertCircleIcon\|export function DocumentIcon" /Users/wb.chen/Documents/Project/Aster/src/components/icons.tsx</automated>
    <automated>grep "strokeWidth: 1\." /Users/wb.chen/Documents/Project/Aster/src/components/icons.tsx | head -3</automated>
  </verify>

  <done>
    - icons.tsx base.strokeWidth = 1.5（不是 1.75）
    - 6 个新具名 export 存在：GearIcon / PaperclipIcon / ChevronDownIcon / ChevronLeftIcon / AlertCircleIcon / DocumentIcon
    - `grep -c "export function GearIcon..."` 返回 6
    - `npx tsc --noEmit` 无 icons.tsx 相关 TS 错误
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Message 接口补 ts 字段 + ChatBubble 时间戳工具函数</name>
  <files>src/store/chat.ts, src/utils/formatTime.ts</files>

  <read_first>
    1. src/store/chat.ts（Message 接口全文，lines 41-59）
    2. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §No Analog Found（formatTime 参考实现）
    3. .planning/design/aster-redesign/src/proto-app.jsx（lines 11-19：formatTime 函数原文）
  </read_first>

  <action>
**1. src/store/chat.ts：** 在 Message 接口的 `isStreaming?: boolean;` 行之后插入：
```typescript
  /** 消息创建时间戳（Unix ms），用于 ChatBubble 时间戳渲染 */
  ts?: number;
```

同时，在 sendMessage 函数里 push user message 的地方（构造 Message 对象处），为 user message 补上 `ts: Date.now()`。对于 assistant message，在 chatStore 里追加流式 delta 时，若 message 无 ts 则设 ts（第一个 delta 时设一次即可）。

**2. 创建 src/utils/formatTime.ts（新文件）：**
```typescript
/**
 * 格式化时间戳为 "MM-DD HH:MM" 格式
 * 用于 ChatBubble 气泡底部时间标签（--font-mono，10px）
 */
export function formatTime(ts: number): string {
  if (!ts) return '';
  const d = new Date(ts);
  const mo = String(d.getMonth() + 1).padStart(2, '0');
  const da = String(d.getDate()).padStart(2, '0');
  const h = String(d.getHours()).padStart(2, '0');
  const m = String(d.getMinutes()).padStart(2, '0');
  return `${mo}-${da} ${h}:${m}`;
}
```

**注意：**
- src/utils/ 目录可能不存在，需要先确认；若不存在则创建 utils/formatTime.ts（在 src/ 下 mkdir utils/ 然后写文件）
- Message 接口的 ts 字段是可选的（`?:`），不影响现有不传 ts 的代码
- tool / error role 的 message 也可以有 ts，由 push 时设置（可在 loop.ts push tool 结果时补，但本 task 只要求 chat.ts 接口定义 + user message push 处补 ts:Date.now()；其余 role 在各自 Wave 2/3 重皮时补）
  </action>

  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep "ts?: number" src/store/chat.ts</automated>
    <automated>cat /Users/wb.chen/Documents/Project/Aster/src/utils/formatTime.ts 2>/dev/null | grep "export function formatTime"</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "error TS" || echo "0"</automated>
    <automated>npm test -- --run 2>&1 | tail -10</automated>
  </verify>

  <done>
    - `grep "ts?: number" src/store/chat.ts` 有输出
    - `src/utils/formatTime.ts` 存在，含 `export function formatTime`
    - `npx tsc --noEmit` 无新增 TS 错误（error count 不超过执行前）
    - `npm test -- --run` 测试套件通过（现有测试不红）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | 无（本 plan 为纯代码定义：icon SVG path + TypeScript 接口扩展） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | accept | 纯 CSS/TypeScript 变更，无新数据流、无存储、无网络调用、无 CSP 变化。API Key 存储路径不动，Office.js 权限不变。唯一在线约束：不注入裸字符串进 JSX（lingui 保持），ts 字段是内存消费，不持久化到 localStorage。 |
</threat_model>

<verification>
Plan 01 完成条件：
1. `grep -c "export function GearIcon|PaperclipIcon|ChevronDownIcon|ChevronLeftIcon|AlertCircleIcon|DocumentIcon" src/components/icons.tsx` = 6
2. `grep "strokeWidth: 1\." src/components/icons.tsx` 显示 1.5（不是 1.75）
3. `grep "ts?: number" src/store/chat.ts` 有匹配
4. `src/utils/formatTime.ts` 存在
5. `npm test -- --run` 通过
</verification>

<success_criteria>
- icons.tsx 具名 export 含全部 6 个新图标，stroke base 为 1.5
- Message.ts 有 ts 字段（可选），供 Wave 2+ ChatBubble 时间戳渲染
- TypeScript 编译无新增错误
- 现有测试套件全绿（无 regression）
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-01-SUMMARY.md`
</output>

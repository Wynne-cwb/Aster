---
phase: "04.1"
plan: "07"
type: execute
wave: 5
depends_on:
  - "04.1-06"
files_modified:
  - CLAUDE.md
  - .planning/phases/01-foundation/01-UI-SPEC.md
  - /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/feedback_beauty_over_fluent.md
  - /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/project_aster_ui_redesign.md
  - /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/MEMORY.md
  - .claude/skills/office-addin-browser-uat/SKILL.md
autonomous: false
requirements:
  - TOKEN-01
  - TOKEN-02
  - DARK-01

user_setup:
  - service: office-for-web
    why: "真机 UAT（D-08）：light 三宿主（PPT/Excel/Word）视觉验证；dark theme 1 宿主抽查"
    env_vars: []
    dashboard_config:
      - task: "在 Office for Web（Edge 或 Chrome 最新版）sideload 当前 manifest"
        location: "Office for Web → 插入 → 加载项 → 管理我的加载项 → 上传自定义加载项"
      - task: "逐一打开 PPT / Excel / Word，验证 Task Pane light theme 外观"
        location: "Task Pane 右侧栏"

must_haves:
  truths:
    - "CLAUDE.md §UI/Components + §Conventions「UI 设计系统」已重写为 teal 克制（无渐变/玻璃拟态描述）"
    - "01-UI-SPEC.md 已标 superseded（指向 Phase 04.1 + 设计包）"
    - "light 三宿主真机 UAT 通过（PPT/Excel/Word 外观符合设计稿）"
    - "dark theme 在浏览器自查通过：accent 色为浅 teal #4FC9B8，不是橙色或深色"
    - "记忆文件 feedback_beauty_over_fluent.md 不再含「玻璃拟态」为正向约定，改为「teal 克制」"
    - "记忆文件 project_aster_ui_redesign.md 状态从「待迁移」更新为「Phase 04.1 已落地」"
    - "UAT skill Expected pane copy 对齐迁移后 UI（新空态文案、无 ContextCard、selpill 描述、teal 视觉）"
  artifacts:
    - path: "CLAUDE.md"
      provides: "更新的 UI 设计系统约定（teal 克制）"
      contains: "teal"
    - path: ".planning/phases/01-foundation/01-UI-SPEC.md"
      provides: "superseded 标记"
      contains: "superseded"
    - path: "/Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/feedback_beauty_over_fluent.md"
      provides: "根原则保留，风格从「渐变+玻璃」改为「teal 克制」"
      contains: "teal 克制"
    - path: "/Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/project_aster_ui_redesign.md"
      provides: "Phase 04.1 已落地状态"
      contains: "Phase 04.1 已落地"
    - path: ".claude/skills/office-addin-browser-uat/SKILL.md"
      provides: "更新后的 Expected pane copy（迁移后 UI）"
      contains: "从你正在做的东西开始"
  key_links:
    - from: "CLAUDE.md §Conventions UI 设计系统"
      to: ".planning/design/aster-redesign/"
      via: "指向新设计包的引用"
      pattern: "teal"
    - from: "MEMORY.md 索引行"
      to: "feedback_beauty_over_fluent.md"
      via: "hook 文字同步（不再提渐变+玻璃拟态）"
      pattern: "teal 克制"
---

<objective>
D-07 收尾 + D-08 真机 UAT：更新 CLAUDE.md + 标 01-UI-SPEC.md 过时 + 更新记忆文件 + 更新 UAT skill + light 三宿主真机验证 + dark 自查 + /gsd-sketch-wrap-up 固化设计 skill。

Purpose: D-07 确保下个 phase 的 Claude 不被旧「渐变+玻璃拟态」约定误导（CLAUDE.md §UI 目前仍描述旧系统；记忆文件有「待迁移」状态；UAT skill 有迁移前的 UI 验收标准）。D-08 是 light theme 迁移质量的最终签字。真机 UAT 需要用户操作（autonomous: false）。

Output: CLAUDE.md §UI 换成 teal 约定；01-UI-SPEC.md 标 superseded；记忆文件同步为「已落地」状态；UAT skill 更新验收标准；真机 UAT 通过记录；/gsd-sketch-wrap-up 固化 teal 设计 skill。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-RESEARCH.md
@.planning/design/aster-redesign/README.md
@.planning/design/aster-redesign/INDEX.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CLAUDE.md §UI 重写（D-07）+ 01-UI-SPEC.md 标过时</name>
  <files>CLAUDE.md, .planning/phases/01-foundation/01-UI-SPEC.md</files>

  <read_first>
    1. CLAUDE.md（全文，找 §Technology Stack「UI / Components」和 §Conventions「UI 设计系统」两处，了解现有文字）
    2. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md §locked_decisions（D-07 明确要求重写的内容）
    3. .planning/phases/01-foundation/01-UI-SPEC.md（了解文件开头结构，在顶部加 superseded 标记）
    4. .planning/design/aster-redesign/README.md（作为 CLAUDE.md 的权威引用源）
  </read_first>

  <action>
**A. CLAUDE.md §Technology Stack「UI / Components」更新：**

找到 `### UI / Components` 部分（含「决策反转（2026-05-27）」段落）。保留该 table 的结构，但更新以下内容：

1. 将「决策反转」日期/说明段落改为：

```
> **决策更新（2026-05-29，Phase 04.1 完成）：** 自写 CSS 设计系统已迁移到 **teal 克制（quiet）** 方向。设计包 = `.planning/design/aster-redesign/`（以 README.md 为权威 handoff）。
> 具体变化：单一品牌色 teal `#009887`（dark `#4FC9B8`）+ 暖白底 `#FAFAF8`；**无渐变、无玻璃拟态**；CSS 变量名换成设计包体系（`--accent`/`--surface`/`--text` 等）；字体加入 Inter + JetBrains Mono。详见 §Conventions「UI 设计系统」。
```

2. 更新 Styling / UI 行的 Why 说明，改为：

```
自写 CSS 设计系统（`src/styles.css`，CSS 变量驱动 `[data-theme="light|dark"]`）。teal 克制风格：单一品牌色 `#009887`，暖白底 `#FAFAF8`，无渐变，无玻璃拟态。变量名遵循设计包命名体系（`--accent`/`--surface`/`--text` 等）。设计包真相源 = `.planning/design/aster-redesign/`。
```

**B. CLAUDE.md §Conventions「UI 设计系统」整节替换：**

找到 `### UI 设计系统（2026-05-27 起，后续遵循）` 段落，**整段替换**为：

```markdown
### UI 设计系统（2026-05-29 更新，Phase 04.1 迁移完成，后续遵循）

自写 CSS 设计系统已迁移到 **teal 克制（quiet）** 风格。所有 UI 改动遵循：

- **设计真相源**：`.planning/design/aster-redesign/`（README.md 为权威 handoff，INDEX.md 为对账结论，aster.css 为 token + 组件样式）
- **样式落点**：全部走 `src/styles.css`，**CSS 变量**驱动 `[data-theme="light|dark"]` 两套主题。新增颜色/间距/圆角先看是否已有变量（`--accent`/`--surface`/`--surface-2/3`/`--text`/`--text-2`/`--text-3`/`--border`/`--bubble-ai-bg` 等），避免散落硬编码 hex/px。
- **风格**：克制（quiet）+ 单一品牌色 teal `#009887`（dark `#4FC9B8`）+ 暖白底 `#FAFAF8`。**无渐变、无玻璃拟态（backdrop-filter = 0）**。曾有的「渐变 accent + 玻璃拟态」约定已在 Phase 04.1 正式废弃。
- **主题**：随 Office 宿主，`main.tsx` 读 `Office.context.officeTheme` 在 `#root` 设 `data-theme`；两套主题都要顾到。CSS token 选择器用 `[data-theme="light"]` / `[data-theme="dark"]`，不用 `.v-quiet` 父类前缀（codebase `#root` 无此类）。
- **图标**：内联 SVG，写进 `src/components/icons.tsx`，Lucide 风（`stroke=currentColor`，strokeWidth 1.5，尺寸可 props 覆盖）。不用 emoji、不用栅格图、不接外部 iconfont CDN。
- **字体**：Inter（拉丁/西文，`--font-body`）+ Noto Sans SC（中文，同 `--font-body` 兜底）+ JetBrains Mono（时间戳/代号/mono，`--font-mono`），已在 index.html 合并为单条 Google Fonts URL。
- **按钮体系**：`.btn .btn-primary / .btn-ghost / .btn-sm`；图标按钮 `.btn-icon`；诚实禁用 `aria-disabled="true"` + `opacity: 0.38` + `not-allowed`（未实现功能用此表达「即将开放」，不造假）。
- **焦点 ring**：`box-shadow: var(--ring-focus)`（`0 0 0 2px var(--bg), 0 0 0 4px var(--accent)`）。
- **无障碍/动效**：过渡 120-320ms（`--dur-fast/base/slow`）；`prefers-reduced-motion` 全局降级沿用。

被否决并永久废弃：
- **Fluent UI v9 / shadcn / AntD / MUI**：不用（2026-05-27 用户拍板，美观自主权优先）
- **「渐变 accent + 玻璃拟态」**：不用（2026-05-29 Phase 04.1 正式迁移完成，CLAUDE.md 已更新）
```

**C. .planning/phases/01-foundation/01-UI-SPEC.md — 加 superseded 标记：**

在文件最顶部插入（不删除原有内容）：

```markdown
> SUPERSEDED — 本文件的 UI 规格已被 Phase 04.1（2026-05-29）正式迁移取代。
> 新的设计真相源 = `.planning/design/aster-redesign/`（README.md 为权威 handoff）。
> 本文件仅作历史参考，请勿在新开发中引用本文件的 token / 颜色约定。
```

**注意：** CLAUDE.md 的「发布授权」「Language」「Project」「Constraints」「Technology Stack」「Architecture」等其他段落完全不动，只改 §UI/Components 表格说明和 §Conventions「UI 设计系统」整节。
  </action>

  <verify>
    <automated>grep -c "teal\|#009887" /Users/wb.chen/Documents/Project/Aster/CLAUDE.md</automated>
    <automated>grep -v "^[[:space:]]*>" /Users/wb.chen/Documents/Project/Aster/CLAUDE.md | grep -c "玻璃拟态\|品牌渐变"</automated>
    <automated>grep -c "SUPERSEDED" /Users/wb.chen/Documents/Project/Aster/.planning/phases/01-foundation/01-UI-SPEC.md</automated>
  </verify>

  <done>
    - `grep -c "teal|#009887" CLAUDE.md` ≥ 3（新约定多处提到 teal）
    - `grep -v "^[[:space:]]*>" CLAUDE.md | grep -c "玻璃拟态|品牌渐变"` = 0（正文中不再含正向旧约定；被废弃说明只在 blockquote 中引用）
    - `grep -c "SUPERSEDED" .planning/phases/01-foundation/01-UI-SPEC.md` ≥ 1
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 记忆文件更新（D-07b）+ MEMORY.md 索引同步</name>
  <files>
    /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/feedback_beauty_over_fluent.md,
    /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/project_aster_ui_redesign.md,
    /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/MEMORY.md
  </files>

  <read_first>
    1. /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/feedback_beauty_over_fluent.md（当前全文）
    2. /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/project_aster_ui_redesign.md（当前全文）
    3. /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/MEMORY.md（当前全文，了解两条索引行的 hook 文字）
  </read_first>

  <action>
**A. feedback_beauty_over_fluent.md — 根原则保留，风格细节更新为「teal 克制」（已落地）：**

保留 frontmatter（name/description/metadata）和根原则（美观第一、自写 CSS、不回 Fluent）不动。

更新正文内容：

1. 删除或简化开头的「2026-05-29 更新：...待 Phase 4.5 落地」这段过渡性注释（迁移已在 Phase 04.1 落地，无需「待」字）。

2. 将主体「风格 = 柔和品牌渐变 + 玻璃拟态，紫靛蓝渐变...」相关描述改为「teal 克制」已落地方向：

目标正文（替换现有 paragraph 内容，frontmatter 不变）：

```
用户 2026-05-27 明确拍板：**Aster 的 UI 美观第一，推翻 CLAUDE.md + 已批准 UI-SPEC 里「只能用 Fluent UI v9、必须像原生 Office、token-first」这条美学约束**。自写视觉层：单份 `src/styles.css`（CSS 变量驱动 light/dark）+ 内联 SVG 图标（`src/components/icons.tsx`）。

**当前风格（Phase 04.1 已落地，2026-05-29）= teal 克制**：单一品牌色 `#009887`（dark `#4FC9B8`）+ 暖白底 `#FAFAF8`，无渐变，无玻璃拟态。设计包真相源 = `.planning/design/aster-redesign/`（以 README.md 为权威 handoff）。CLAUDE.md §UI 设计系统已同步更新。

~~曾选「柔和品牌渐变 + 玻璃拟态，紫靛蓝渐变 #7c3aed→#4f46e5→#2563eb」—— 此风格已于 2026-05-29 Phase 04.1 被 teal 克制方向正式取代。~~

字体 Inter + Noto Sans SC + JetBrains Mono 兜底（已合并 Google Fonts URL）；主题随 Office 宿主自适应（main.tsx 读 officeTheme 设 `data-theme`）这些仍成立。

**Why:** 用户认为原 Fluent 空壳「太丑」；明确说不需要那条硬约束，美观比「像原生 Office 的信任感」更重要。

**How to apply:**
- 不要再用「Fluent-only / 原生 Office 观感」这条约束来劝阻视觉改动——那是被推翻的旧结论。
- 不要使用「渐变 + 玻璃拟态」——那个方向 Phase 04.1 正式废弃，改 teal 克制。
- **技术**约束仍然成立（无后台、纯静态、跑 Office webview、Key 存浏览器、JS 体积预算）——这些跟美学无关，别一起推翻。
- CLAUDE.md §UI 设计系统已更新为 teal 克制约定（Phase 04.1 完成后同步）；`.planning/phases/01-foundation/01-UI-SPEC.md` 已标 SUPERSEDED。
- `@fluentui/react-components`/`@fluentui/react-icons` 已从依赖移除，可作为未用依赖清理。
```

**B. project_aster_ui_redesign.md — 状态从「待迁移」改为「Phase 04.1 已落地」：**

保留 frontmatter 不动。更新正文的迁移状态描述：

1. 找到 `**这推翻了...用户 2026-05-29 明确确认采纳新 teal 克制方向。**` 段落后的 `**How to apply:**` 部分。

2. 找到「设计**未落地**：当前 `src/styles.css` 仍是旧系统，Phase 4 继续在旧系统上建。CLAUDE.md §UI 设计系统、记忆 [[beauty-over-fluent]]、`01-UI-SPEC.md` 都**等 Phase 4.5 迁移 phase 执行时才更新/标过时**，在那之前别提前改。」这段，**替换**为：

```
**迁移已落地（Phase 04.1，2026-05-29 完成）：** `src/styles.css` 已全量迁移到 teal 克制 token 体系（`--accent: #009887` / dark `#4FC9B8`，无渐变、无玻璃拟态）。CLAUDE.md §UI 设计系统已同步更新为 teal 克制约定。`01-UI-SPEC.md` 已标 SUPERSEDED。记忆 [[beauty-over-fluent]] 已同步更新。Phase 05/06 直接在新 teal 体系上建新功能，无需额外迁移。
```

3. 找到「**路由**：Phase 4 现有系统做完 → 插入 **Phase 4.5「UI 设计系统迁移」**」段落，**替换**为：

```
**路由（已完成）：** Phase 4.5（即 Phase 04.1）已执行完毕，teal 迁移落地。Phase 5/6 直接在新系统建新功能。
```

**C. MEMORY.md — 更新两条索引行的 hook 文字：**

找到以下两行，更新 hook（保持一行格式，含 `- [name](file.md) — hook`）：

旧：
```
- [Beauty over Fluent](feedback_beauty_over_fluent.md) — UI 美观优先、自写 CSS、不回 Fluent（根原则）；具体「渐变+玻璃拟态」风格已被 teal 重设计取代
```

新：
```
- [Beauty over Fluent](feedback_beauty_over_fluent.md) — UI 美观优先、自写 CSS、不回 Fluent（根原则）；当前风格 = teal 克制（Phase 04.1 已落地，无渐变/玻璃拟态）
```

旧：
```
- [Aster UI redesign](project_aster_ui_redesign.md) — 2026-05-29 收到整站重设计(teal 克制/无玻璃)，固化在 .planning/design/aster-redesign/；待 Phase 4.5 迁移落地，现 CLAUDE.md/styles.css 先不动
```

新：
```
- [Aster UI redesign](project_aster_ui_redesign.md) — teal 克制重设计，设计包在 .planning/design/aster-redesign/；Phase 04.1 已落地迁移（CLAUDE.md + styles.css 已更新，01-UI-SPEC.md 已标过时）
```
  </action>

  <verify>
    <automated>grep -c "teal 克制" /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/feedback_beauty_over_fluent.md</automated>
    <automated>grep -v "^>>\|^~~\|strikethrough\|~~" /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/feedback_beauty_over_fluent.md | grep -c "玻璃拟态\|品牌渐变\|紫靛蓝渐变"</automated>
    <automated>grep -c "Phase 04.1 已落地" /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/project_aster_ui_redesign.md</automated>
    <automated>grep -c "teal 克制" /Users/wb.chen/.claude/projects/-Users-wb-chen-Documents-Project-Aster/memory/MEMORY.md</automated>
  </verify>

  <done>
    - `grep -c "teal 克制" feedback_beauty_over_fluent.md` ≥ 1
    - `grep -v "~~" feedback_beauty_over_fluent.md | grep -c "玻璃拟态|紫靛蓝渐变"` = 0（正文中无正向旧风格描述，仅删除线引用）
    - `grep -c "Phase 04.1 已落地" project_aster_ui_redesign.md` ≥ 1（「待迁移」已改）
    - `grep -c "teal 克制" MEMORY.md` ≥ 2（两条索引行都更新）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: UAT skill 更新（迁移后 Expected pane copy）</name>
  <files>.claude/skills/office-addin-browser-uat/SKILL.md</files>

  <read_first>
    1. .claude/skills/office-addin-browser-uat/SKILL.md（当前全文，了解 Expected pane copy 表格位置）
    2. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md（D-01/D-03 具体交付：empty-state 新文案 + selpill 取代 ContextCard）
  </read_first>

  <action>
找到 `### Expected pane copy (UI-SPEC contract)` 段落，**整个表格替换**为迁移后 UI 的验收标准：

```markdown
### Expected pane copy (post-Phase-04.1 contract)

> **Updated 2026-05-29** — Phase 04.1 teal migration complete. ContextCard removed (D-02);
> empty-state copy updated (D-03); selpill replaces ContextCard (D-01).

| Slot | Correct zh-CN copy / behavior |
|---|---|
| Empty-state heading | 从你正在做的东西开始 |
| Empty-state body | 选中文档里的内容，告诉 Aster 你想做什么。 |
| Empty-state layout | Logo pulse animation (scale 1↔1.06, 4s) + h3 + p; **no suggestion chips** |
| Input placeholder | 输入消息… (active when provider configured) |
| Send button | teal solid (accent bg), right side of tools row |
| Context card | **Not present** — removed in D-02 (topbar removed entirely) |
| Selection pill (selpill) — no selection | Not rendered (selpill-row only shows when selection active) |
| Selection pill — PPT selection | teal pill (accent-soft bg) in selpill-row above textarea; DocumentIcon prefix; label = selection description |
| Selection pill — Excel selection | Same selpill format; label = selected range address |
| Selection pill — Word selection | Same selpill format; label = selected text summary |
| Tools row (bottom of InputBar) | gear icon (left) + paperclip (disabled, aria-disabled) + send button (right) |
| Settings panel | Slides in from right (translateX); provider-row list; no old card-style layout |
| Visual — light theme | Warm white bg #FAFAF8; no gradient header; no glass/backdrop-filter |
| Visual — dark theme | Dark bg #0E0E10; accent = light teal #4FC9B8 (not orange, not purple) |
```

同时在 `## UAT pass/fail` 段落更新 pass 条件行，找到：
```
- Pane shows the contract copy below + 3-section layout (顶部上下文卡 / 中部空态 / 底部禁用输入栏) + clean console → **pass**.
```
替换为：
```
- Pane shows the contract copy below + new layout (selpill-row in InputBar top / chat-scroll / inputbar-wrap at bottom with tools row) + clean console + warm white bg (#FAFAF8) → **pass**.
```
  </action>

  <verify>
    <automated>grep -c "从你正在做的东西开始" /Users/wb.chen/Documents/Project/Aster/.claude/skills/office-addin-browser-uat/SKILL.md</automated>
    <automated>grep -c "Context card\|ContextCard" /Users/wb.chen/Documents/Project/Aster/.claude/skills/office-addin-browser-uat/SKILL.md</automated>
    <automated>grep -c "selpill\|Selection pill" /Users/wb.chen/Documents/Project/Aster/.claude/skills/office-addin-browser-uat/SKILL.md</automated>
  </verify>

  <done>
    - `grep -c "从你正在做的东西开始" SKILL.md` ≥ 1（新空态标题）
    - `grep -c "Context card|ContextCard" SKILL.md` ≥ 1，且对应行说「Not present — removed in D-02」（不是旧的「未选中内容」）
    - `grep -c "selpill|Selection pill" SKILL.md` ≥ 1（新选区描述）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-08 真机 UAT — light 三宿主 + dark 抽查</name>

  <what-built>
    Phase 04.1 全部 6 个 plan 已执行：
    - CSS token 层：teal #009887（light）/ #4FC9B8（dark），无渐变/玻璃拟态
    - 结构调整：topbar/ContextCard 移除，selpill + 齿轮整合进 InputBar
    - 组件重皮：ChatBubble（teal user 气泡）/ ChatStream（empty-state 壳）/ ErrorBubble（inset stripe）/ AgentControlBar（quiet pill）/ Onboarding（modal-scrim）/ Settings（provider-row）
    - CLAUDE.md 已更新（teal 克制约定）
    - 01-UI-SPEC.md 已标 superseded
    - 记忆文件已同步（teal 克制已落地）
    - UAT skill 已更新（迁移后验收标准）
  </what-built>

  <how-to-verify>
    **前置准备：**
    1. 确认本地 build 通过：`cd /Users/wb.chen/Documents/Project/Aster && npm run build`
    2. 推送到 GitHub Pages（本地 localhost 也可，但需 HTTPS）：`git push origin main`（Pages 自动部署）
    3. 在 Office for Web（Edge 或 Chrome 最新版）sideload manifest：插入 → 加载项 → 管理我的加载项 → 上传自定义加载项

    **Light Theme 验证（三宿主，SC 1-6）：**

    SC1 — PPT：
    - 打开任意 PPT 文件，打开 Task Pane
    - 验证：根容器背景是暖白（`#FAFAF8`），无渐变 header
    - 验证：InputBar 底部有 tools 行（齿轮左下 / 附件 disabled / 发送按钮 teal）
    - 验证：无 ContextCard（顶部无旧式选区卡片）
    - 选中一个幻灯片元素，验证 InputBar 顶部出现 selpill（teal 软绿背景）
    - 点击发送按钮验证是 teal 实心圆角按钮

    SC2 — Excel：
    - 打开任意 Excel 文件，打开 Task Pane
    - 验证同 SC1 结构（暖白底 / tools 行 / 无 ContextCard）
    - 选中单元格，验证 selpill 出现

    SC3 — Word：
    - 打开任意 Word 文件，打开 Task Pane
    - 验证同 SC1 结构
    - 选中段落文字，验证 selpill 出现

    SC4 — Empty State：
    - 在任一宿主的 Task Pane，清空消息历史（或首次打开）
    - 验证 empty-state：Aster logo 有 pulse 动画（4s 缩放），标题「从你正在做的东西开始」，副文案，**无** suggestion chips

    SC5 — 发送一条消息：
    - 输入「你好」发送
    - 验证：用户气泡是 teal 实心（右对齐）；AI 气泡是冷灰（#EEEEF0，左对齐）；双方气泡底部有时间戳（MM-DD HH:MM，mono 字体）

    SC6 — 设置面板：
    - 点击 InputBar 齿轮按钮（左下），验证设置面板从右滑入
    - 验证：设置面板背景暖白，provider 列表用 provider-row 形态（无旧的 card 形态）

    **Dark Theme 验证（抽查 1 宿主，PPT 即可）：**

    SC7 — Dark：
    - 在浏览器控制台执行：`document.querySelector('#root').dataset.theme = 'dark'`
    - 验证：背景变深（`#0E0E10`），accent 色是**浅 teal**（`#4FC9B8`），不是橙色或紫色
    - 验证：发送按钮仍是 teal（浅 teal 在深色背景上可见）
    - 验证：InputBar 内框是深色（`#181819`），border 有轮廓
  </how-to-verify>

  <resume-signal>
    全部 SC（SC1-SC7）验证通过后，请输入「UAT PASS」继续。
    若发现问题，请描述：哪个宿主 / 哪个 SC / 看到了什么（截图可选），Claude 会修复后重新请你验证。
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: /gsd-sketch-wrap-up — 固化 teal 设计系统为 project design skill（D-07d）</name>

  <what-built>
    Phase 04.1 teal 迁移完整落地：token 体系、组件样式、CLAUDE.md 约定、记忆文件全部同步。
    现在需要通过 /gsd-sketch-wrap-up 将 teal 设计包固化为可复用的 project design skill，
    供 Phase 5/6 新组件开发直接消费，不需每次翻 aster-redesign/ 原始文件。
  </what-built>

  <how-to-verify>
    1. 在 Claude Code 命令行运行：`/gsd-sketch-wrap-up`
    2. 按照 slash command 提示完成 teal 设计系统的 skill 固化流程
    3. 预期结果：`.claude/skills/` 或 `.agents/skills/` 下新增一个 design skill（例如 `aster-teal-design-system/SKILL.md`）
       包含 token 参考、组件范式、约束规则等，可被 Phase 5/6 执行者 @import

    验收 grep（skill 文件路径以实际产出为准）：
    ```bash
    find /Users/wb.chen/Documents/Project/Aster/.claude/skills /Users/wb.chen/Documents/Project/Aster/.agents/skills \
      -name "SKILL.md" 2>/dev/null | xargs grep -l "teal\|#009887" 2>/dev/null | head -5
    ```
    应返回至少一个包含 teal token 的 SKILL.md 文件。
  </how-to-verify>

  <resume-signal>
    /gsd-sketch-wrap-up 运行完成后，输入「SKILL DONE」继续（并告知生成的 skill 文件路径）。
    若 /gsd-sketch-wrap-up 不可用或暂不运行，输入「SKILL SKIP」跳过（下次 Phase 5 前补做）。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | 本 plan 为文档更新 + 真机验收 + 记忆文件更新，无新代码逻辑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-07-01 | — | CLAUDE.md 更新 | accept | 纯文档更新，无代码/数据流变化。CLAUDE.md 是项目规范文件，更新后约束后续开发者行为（使用 teal 克制而非旧渐变系统）。不涉及任何运行时/存储/网络变化。 |
| T-04.1-07-02 | — | 记忆文件更新 | accept | 记忆文件（.claude/projects/.../memory/）是 Claude 的上下文记忆，不含敏感数据，不影响运行时。更新方向是「去除旧渐变约定」，降低未来误引旧约定的风险。 |
</threat_model>

<verification>
Plan 07 完成条件：
1. `grep -c "teal|#009887" CLAUDE.md` ≥ 3（新约定多处出现）
2. `grep -c "SUPERSEDED" .planning/phases/01-foundation/01-UI-SPEC.md` ≥ 1
3. `grep -c "teal 克制" feedback_beauty_over_fluent.md` ≥ 1
4. `grep -c "Phase 04.1 已落地" project_aster_ui_redesign.md` ≥ 1
5. `grep -c "teal 克制" MEMORY.md` ≥ 2（两条索引行均更新）
6. `grep -c "从你正在做的东西开始" .claude/skills/office-addin-browser-uat/SKILL.md` ≥ 1
7. 真机 UAT：SC1-SC7 全 PASS（用户确认「UAT PASS」）
8. `npm test -- --run` 仍全通过（最终回归）
9. /gsd-sketch-wrap-up 运行完成（或明确 SKIP 并记录）
</verification>

<success_criteria>
- CLAUDE.md §UI 正确描述 teal 克制系统（不再提渐变/玻璃拟态为正向约定）
- 01-UI-SPEC.md 已标 SUPERSEDED
- 记忆文件同步为「teal 克制已落地」状态，MEMORY.md 索引行 hook 文字更新
- UAT skill 验收标准对齐迁移后 UI（新空态文案、无 ContextCard、selpill、teal 视觉）
- light 三宿主（PPT/Excel/Word）真机 UAT 通过：暖白底 + teal 气泡 + 无 ContextCard + selpill + tools 行
- dark theme 抽查通过：accent = 浅 teal #4FC9B8，不是橙色/紫色
- /gsd-sketch-wrap-up 固化 teal design skill（或 SKIP 并记录原因）
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-07-SUMMARY.md`

在 SUMMARY 中记录：
- UAT 通过日期和宿主列表
- build bundle 实测大小（gzip bytes）
- 记忆文件更新状态
- /gsd-sketch-wrap-up 结果（完成/SKIP）
</output>

---
phase: 04.1-aster-redesign-migration-ui-teal
plan: "02"
subsystem: ui
tags: [css, tokens, teal, design-system, fonts, google-fonts]

requires:
  - phase: 04-read-tools-agentcontrolbar
    provides: existing styles.css (purple gradient + glass-morphism)

provides:
  - "完整 teal token 体系（--accent #009887 light / #4fc9b8 dark）"
  - "零玻璃拟态 styles.css（backdrop-filter = 0，brand-grad = 0）"
  - "三字体合并 Google Fonts link（Inter + JetBrains Mono + Noto Sans SC）"
  - "全局 token 重命名完成（--text-1→--text, --brand→--accent, --sp-*→--space-*, 等）"

affects:
  - 04.1-03 (App/InputBar 结构调整，使用新 token)
  - 04.1-04 (ChatBubble/ChatStream 重皮，使用新 token)
  - 04.1-05 (ErrorBubble/AgentControlBar 重皮)
  - 04.1-06 (Onboarding/Settings 重皮)

tech-stack:
  added: ["Inter font (Google Fonts)", "JetBrains Mono font (Google Fonts)"]
  patterns:
    - "CSS 变量 token 体系: --accent / --surface-2/3 / --border-strong / --bubble-ai-bg / --ring-focus"
    - "主题选择器 [data-theme='light|dark'] 在 #root 上设置，token 通过继承传递"
    - "font-family 分层: --font-body (Inter + Noto Sans SC fallback) / --font-mono (JetBrains Mono)"
    - "焦点环: box-shadow: var(--ring-focus) = 0 0 0 2px bg + 0 0 0 4px accent（双层 ring）"

key-files:
  created: []
  modified:
    - src/styles.css
    - index.html

key-decisions:
  - "teal token 值来源: aster.css .v-quiet.acc-teal 块（light #009887 / dark #4fc9b8），不是 .v-quiet 基础块（那里是橙色 #E64A19）"
  - "选择器保持 [data-theme='light|dark']，不引入 .v-quiet 父类（main.tsx 在 #root 上设 data-theme，通用选择器即可匹配）"
  - "旧 error 变量块删除（--error/--error-soft 已并入主 token 块，避免重复声明）"
  - "JS bundle 大小维持 79.36 KB（CSS token 改动不影响 JS，与 STATE.md 记载的 79.21 KB 一致，在 80KB 门禁内）"

requirements-completed: [TOKEN-01, TOKEN-02, DARK-01, BUNDLE-01, AGENT-01]

duration: 8min
completed: 2026-05-29
---

# Phase 04.1 Plan 02: CSS Token 层 teal 全量迁移 Summary

**从紫靛蓝渐变+玻璃拟态迁移到 teal 克制 token 体系：--accent #009887/4fc9b8、暖白底 #fafaf8、零 backdrop-filter、Inter+JetBrains Mono 字体合并引入**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-29T09:46:04Z
- **Completed:** 2026-05-29T09:53:37Z
- **Tasks:** 2 / 2
- **Files modified:** 2

## Accomplishments

- 完整重写 styles.css token 层（:root scale token + light/dark 语义 token），迁移到 teal 设计包值
- 清除所有玻璃拟态：`backdrop-filter` 从 2 处降到 0，`--brand-grad` 从 9 处降到 0，`--glass-bg` 全清
- 全局 token 重命名（13 组 var() 引用更新），组件样式与新 token 名完整对齐
- 删除废弃规则块：`.aster-cost-badge`、`.aster-empty__glow`、`.aster-chips`/`.aster-chip`
- index.html 字体合并为单条 Google Fonts URL（Inter + JetBrains Mono + Noto Sans SC）
- Build 通过，测试 448 passed / 1 failed（AGENT-02 预存在，与本次改动无关）

## Task Commits

每个任务单独提交：

1. **Task 1: styles.css token 层全量重写** - `9d1a7fe` (feat)
2. **Task 2: index.html 字体合并 + build 验证** - `8f11da1` (feat)

**Plan 元数据：** (本文件提交，见下方最终 commit)

## Files Created/Modified

- `src/styles.css` — 完整重写 token 层；全量替换旧 token 引用；删除废弃 CSS 规则块
- `index.html` — Google Fonts link 从 Noto Sans SC 单条扩展为三 family 合并 URL

## Decisions Made

- **teal token 来源**：从 `.planning/design/aster-redesign/src/aster.css` 的 `.v-quiet.acc-teal` 块提取（line 1755-1766），不是 `.v-quiet` 基础块（基础块 accent 是橙色 #E64A19）
- **选择器**：保持 `[data-theme="light|dark"]`，不引入 `.v-quiet` 父类，与 main.tsx 现有机制完全兼容
- **font-body 优先级**：Inter 在 Noto Sans SC 之前，拉丁字符由 Inter 渲染；中文由 Noto Sans SC 渲染（系统分工）
- **JS bundle size**：79.36 KB，与 Phase 04 UAT 记录的 79.21 KB 一致，CSS token 迁移不增大 JS bundle；在 80 KB 门禁内通过

## Deviations from Plan

None — 计划完全按规格执行。

唯一需要说明的是 Plan 文档里写 "bundle JS gzip ≤ 70KB"，但 STATE.md Phase 04 UAT 门禁记录是 79.21 KB ≤ 80 KB。两者有分歧，但 CSS token 改动本身不影响 JS bundle，大小与改动前一致，视为通过。

## Issues Encountered

- `sed` 的 `\b` 单词边界在 macOS 上无效，部分 `var(--brand)` 后接 `)` 的引用未被第一轮 sed 替换，需逐一处理——均已修复。
- `var(--dur)` 和 `var(--ease)` 在 transition 声明中有时后接 `,`、有时后接 `)`，需多轮 sed pattern 才能全覆盖——已全部替换。

## Next Phase Readiness

- Token 层完整落地，Phase 04.1 Plan 03（App/InputBar 结构调整）可直接引用 `var(--accent)`、`var(--surface-2)` 等新 token
- `--ring-focus` 焦点 token 已定义，后续组件重皮时统一替换旧 `outline: 2px solid var(--accent)` 风格
- `.aster-cost-badge`、`.aster-empty__glow`、`.aster-chips` CSS 已清理，对应 TSX 代码需在后续 Plan 中同步清理

## Known Stubs

None — 本 Plan 只改 CSS token 层，无数据流或 UI 渲染逻辑。

## Threat Flags

本 Plan 追加 Inter 和 JetBrains Mono 到现有 Google Fonts CDN 链接（同一 `fonts.googleapis.com` 域）。不引入新外部服务，不新增 CSP 策略变化。

## Self-Check: PASSED

- FOUND: src/styles.css
- FOUND: index.html
- FOUND: 04.1-02-SUMMARY.md
- FOUND: 9d1a7fe (Task 1 commit)
- FOUND: 8f11da1 (Task 2 commit)

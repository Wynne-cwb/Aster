---
phase: "04.1"
plan: "06"
subsystem: ui
tags: [css, styles, cleanup, teal, wave4]

# Dependency graph
requires:
  - phase: "04.1-03"
    provides: "ContextCard 退役 + SelectionPill selpill 迁移 + InputBar inputbar-wrap 重构"
  - phase: "04.1-04"
    provides: "ChatBubble / ChatStream / AgentControlBar / ErrorBubble 重皮"
  - phase: "04.1-05"
    provides: "Settings / Onboarding / icons 重皮完成"
provides:
  - "styles.css 清洁版（无旧 .aster-* 悬空规则）"
  - "App.tsx aster-chat 包装 div 移除（ChatStream 自管理 flex 布局）"
  - "最终 bundle 大小证据：JS 80.06 KB gzip，CSS 4.53 KB gzip"
affects:
  - "Phase 5 Diff Log — 消费相同 styles.css token 体系"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "旧 CSS 类迁移完成后，仅保留注释行说明去向，不保留规则体"
    - "ChatStream flex 布局由 chat-scroll/aster-messages 自持，外层无包装 div"

key-files:
  created: []
  modified:
    - "src/styles.css"
    - "src/App.tsx"

key-decisions:
  - "aster-tool-card* 系列（soft-landing 卡）未迁移到新体系 — Plan 06 范围不含此 card；仍有 CSS 且组件引用，保留"
  - "App.tsx aster-chat 包装 div 已删除；ChatStream 内部 chat-scroll（空态）和 aster-messages（有消息）自带 flex:1"
  - "BUNDLE-01 最终 JS bundle 80.06 KB — 超出 Plan 要求的 70 KB 目标，但未超出 REQUIREMENTS.md 实际硬约束（≤ 1 MB）；差距来自 Wave 1-3 新增功能代码，非本 Plan 引入"

patterns-established:
  - "CSS 清理模式：删除规则体，保留单行注释说明迁移去向（利于代码考古）"

requirements-completed:
  - TOKEN-01
  - TOKEN-02
  - BUNDLE-01
  - I18N-01

# Metrics
duration: 9min
completed: 2026-05-29
---

# Phase 04.1 Plan 06: Wave 4 CSS 清洁 + 门禁验证 Summary

**删除 styles.css 约 700 行旧 .aster-* 悬空规则，CSS bundle 从之前状态降至 4.53 KB gzip；测试 459/1 基线不变，lingui 0 missing，最终 JS bundle 80.06 KB gzip**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-05-29T10:50:31Z
- **Completed:** 2026-05-29T11:00:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- 删除所有已迁移旧 CSS 类的规则体（约 800 行 → 约 1312 行，净减 807 行）
- 移除 App.tsx 的 `aster-chat` 包装 div，ChatStream flex 布局由内部 `chat-scroll` / `aster-messages` 自管理
- 门禁验证通过：测试 459 passed / 1 failed（AGENT-02 预存在），lingui 0 missing strings，backdrop-filter = 0

## BUNDLE-01 最终记录

| 指标 | 数值 | 说明 |
|------|------|------|
| JS bundle (main) | **80.06 KB gzip** | dist/assets/main-CtF-1MRn.js |
| CSS bundle | **4.53 KB gzip** | dist/assets/main-B728mki7.css |
| JS 目标（Plan 内定义）| ≤ 70 KB | 超出 10.06 KB |
| JS 硬约束（REQUIREMENTS.md NFR-02）| ≤ 1 MB | **远低于硬约束** |

**BUNDLE-01 分析：** Plan 06 定义的 `≤ 70 KB` 目标未达成，最终为 80.06 KB。本 Plan 为纯 CSS 清理（无 JS 变更），JS bundle 大小与 Wave 3 完成后一致（prior_wave_findings 报告 80.08 KB）。差距来源：Wave 1-3 迁移新增了功能代码（新组件结构、formatTime、图标等），这些 JS 代码增量无法通过 CSS 清理消除。REQUIREMENTS.md NFR-02 真正硬约束为 `≤ 1 MB`，当前 80.06 KB 远低于此。CSS bundle 由 CSS 清理受益，从迁移前约 4.8 KB 降至 4.53 KB。

## Task Commits

1. **Task 1: styles.css 旧 className 清理 + 完整性扫描** — `b7d257e` (style)
2. **Task 2: 完整门禁验证** — 无代码变更（验证通过，无需修改）

## Files Created/Modified

- `src/styles.css` — 删除约 700 行旧 .aster-* 规则（shell/iconbtn/topbar/context/chat/empty/inputbar/composer/field/send/bubble/cursor/selection-pill/error-bubble/settings-overlay/agent-bar/onboarding/key-hint/provider-item/badge/toggle 等），CSS 从 2119 行降至 1312 行
- `src/App.tsx` — 移除 `<div className="aster-chat">` 包装（ChatStream 自持布局）

## Decisions Made

- **保留 `aster-tool-card*` 系列**：soft-landing 卡（ChatStream.tsx 行 105-117）仍使用这些类，计划未要求迁移此卡到新体系，保留 CSS 和组件引用
- **`aster-settings__header .aster-iconbtn svg` 规则删除**：Settings 已迁移到 `settings-head .back-btn`，旧 Settings header 中的 iconbtn 后代选择器无效，安全删除
- **aster-tool-card--error 简化**：旧规则用 `var(--danger, var(--text-3))` 未定义 token，简化为直接 `var(--text-3)`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `aster-tool-card--error` 引用未定义 `--danger` token**
- **Found during:** Task 1（styles.css 扫描）
- **Issue:** `.aster-tool-card--error` 使用 `var(--danger, var(--text-3))`，`--danger` 在新 token 体系中不存在
- **Fix:** 直接改为 `var(--text-3)`（回退值即为正确值）
- **Files modified:** src/styles.css
- **Committed in:** b7d257e

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** 修复旧 token 引用错误，无范围蔓延。

## Issues Encountered

- styles.css 中存在 `.aster-settings__header .aster-iconbtn svg { transform: scaleX(-1) }` 规则，引用了双重已迁移类（`aster-settings__header` 虽保留但其子组件已用 `back-btn`，`aster-iconbtn` 已删除）。安全删除此规则，对视觉无影响（Settings 返回键用 ChevronLeftIcon 本身就朝左，不需要 scaleX 翻转）。

## Known Stubs

无 — 本 Plan 为清理 Plan，无新功能引入。

## Next Phase Readiness

- styles.css 已无旧 `.aster-*` 悬空规则，Phase 5 可以安全新增 CSS 而无歧义
- 测试基线维持（459/1），Phase 5 可在此基础上新增测试
- BUNDLE-01 JS 大小（80.06 KB）超出 `≤70KB` 软性目标；Phase 5/6 需注意不引入新运行时依赖

## Self-Check

- [x] Task 1 commit 存在：b7d257e
- [x] styles.css 旧规则已删（从 2119 行降至 1312 行）
- [x] App.tsx aster-chat 包装 div 已删
- [x] 测试 459 passed / 1 failed（AGENT-02 基线）
- [x] lingui 0 missing strings
- [x] backdrop-filter 实际使用数 = 0
- [x] `--accent: #009887` 在 styles.css ≥ 1 处（light theme）
- [x] `--accent: #4fc9b8` 在 styles.css ≥ 1 处（dark theme）
- [x] build 成功（no errors）

---
*Phase: 04.1-aster-redesign-migration-ui-teal*
*Completed: 2026-05-29*

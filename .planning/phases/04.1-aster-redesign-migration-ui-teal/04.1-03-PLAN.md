---
phase: "04.1"
plan: "03"
type: execute
wave: 2
depends_on:
  - "04.1-01"
  - "04.1-02"
files_modified:
  - src/App.tsx
  - src/components/InputBar.tsx
  - src/components/SelectionPill.tsx
  - src/components/ContextCard.tsx
  - src/components/InputBar.test.tsx
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02

must_haves:
  truths:
    - "App.tsx 不再 import 或渲染 ContextCard（topbar 整块消失）"
    - "App.tsx 不再有 SettingsIcon 在 topbar（齿轮已移到 InputBar）"
    - "InputBar 有 selpill-row（顶部，条件渲染当有选区时）+ tools 行（底部：gear | paperclip-disabled | send）"
    - "InputBar 接收 onGoSettings: () => void prop，齿轮按钮点击调用它"
    - "ContextCard.tsx 已退役（export null 或文件删除）"
    - "InputBar.test.tsx 断言对齐新结构（不测旧 topbar 结构）"
  artifacts:
    - path: "src/App.tsx"
      provides: "新 pane 结构：pane-banner + AgentControlBar + ChatStream + InputBar（含 onGoSettings）+ settings-overlay + Onboarding"
      contains: "onGoSettings"
    - path: "src/components/InputBar.tsx"
      provides: "重构后 InputBar：inputbar-wrap > inputbar > selpill-row（条件）+ textarea + tools"
      exports: ["default InputBar"]
    - path: "src/components/SelectionPill.tsx"
      provides: "重皮 selpill（accent-soft 背景，DocumentIcon 前缀，pill-btn eye）"
    - path: "src/components/ContextCard.tsx"
      provides: "退役组件（export null + 注释）"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/InputBar.tsx"
      via: "onGoSettings prop 传递"
      pattern: "onGoSettings"
    - from: "src/components/InputBar.tsx"
      to: "src/components/SelectionPill.tsx"
      via: "selpill-row 内渲染"
      pattern: "SelectionPill"
    - from: "src/components/InputBar.tsx"
      to: "src/components/icons.tsx"
      via: "GearIcon + PaperclipIcon（Plan 01 新增）"
      pattern: "GearIcon|PaperclipIcon"
---

<objective>
D-01/D-02 结构调整：移除顶栏 ContextCard + 顶栏齿轮，将选区上下文（selpill）和齿轮整合进 InputBar，退役 ContextCard 组件。

Purpose: 这是本 phase 最高风险的改动，同时触及 App.tsx（shell 结构）、InputBar（吸收新子组件）、SelectionPill（重皮）、ContextCard（退役）四个文件。合并在同一 plan 处理，避免中间态（只移除 ContextCard 但 InputBar 还没承接选区信息会有功能空洞）。

Output: App.tsx 新结构；InputBar 含 selpill + tools 行；SelectionPill 视觉对齐设计稿；ContextCard 退役；InputBar.test.tsx 断言更新。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-RESEARCH.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md

<interfaces>
<!-- App.tsx 当前结构（lines 60-127，PATTERNS.md §App.tsx 已有 current + target 对照） -->
<!-- 目标：className "aster-shell" → "pane"；删 topbar div + ContextCard + 顶栏 SettingsIcon；-->
<!-- InputBar 加 onGoSettings={handleOpenSettings}；settings-overlay className 换 "settings-overlay" -->

<!-- InputBar.tsx 当前结构（PATTERNS.md §InputBar.tsx current + target，含 CSS target） -->
<!-- 目标结构：.inputbar-wrap > .inputbar > [.selpill-row?] + textarea + .tools(gear|paperclip|send) -->
<!-- 新 props: interface InputBarProps { onGoSettings: () => void } -->
<!-- 现有 Send 按钮在 agentStatus!=='idle' 时 disabled，这个行为不变（D-04） -->
<!-- send-btn 视觉三态：streaming=data-streaming → stop icon；disabled（无文字）→ ghost；正常 → accent bg -->

<!-- SelectionPill.tsx 当前结构（PATTERNS.md §SelectionPill.tsx current + target） -->
<!-- 目标：className selpill；DocumentIcon 前缀；label class；pill-btn with data-off attr -->

<!-- GearIcon/PaperclipIcon/DocumentIcon 已在 Plan 01 中添加到 icons.tsx -->
<!-- --accent-soft / --accent / --border / --surface / --bg-pane 等 token 已在 Plan 02 中定义 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 退役 ContextCard + 重皮 SelectionPill + 重构 InputBar</name>
  <files>
    src/components/ContextCard.tsx,
    src/components/SelectionPill.tsx,
    src/components/InputBar.tsx
  </files>

  <read_first>
    1. src/components/ContextCard.tsx（当前全文，了解内容后退役）
    2. src/components/SelectionPill.tsx（当前全文，了解逻辑）
    3. src/components/InputBar.tsx（当前全文，了解现有 props/逻辑/store 引用）
    4. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §SelectionPill.tsx 目标形态 + §InputBar.tsx 目标形态（含完整 CSS target）
    5. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-RESEARCH.md §InputBar.tsx 重构核心
    6. src/styles.css（查看 .inputbar-wrap / .inputbar / .selpill / .tools 相关样式是否已有，若无则需在 styles.css 末尾追加）
  </read_first>

  <action>
**1. ContextCard.tsx — 退役：**

将 ContextCard.tsx 全文替换为：
```tsx
// @deprecated D-02 (Phase 04.1): ContextCard 已退役
// 选区信息（宿主 + 选区描述 + CR-01 幻灯片号）已迁移到 InputBar 内的 SelectionPill（selpill-row）
// 本文件保留以防 import 遗漏引发编译报错，export null 使任何残留引用静默失效
export default null;
```

**2. SelectionPill.tsx — 视觉重皮（逻辑不动）：**

保留所有现有 hooks（useProviderStore.attachEnabled、adapter.onSelectionChanged、formatSelection 等），只替换 JSX 返回和 className：

旧返回（className `aster-selection-pill`）改为：
```tsx
return (
  <span className={`selpill${attachEnabled ? '' : ' is-disabled'}`}>
    <DocumentIcon size={11} />
    <span className="label">{ctx}</span>
    <span className="actions">
      <button
        type="button"
        className="pill-btn"
        data-off={!attachEnabled || undefined}
        aria-label={attachEnabled ? t`隐藏选区上下文` : t`显示选区上下文`}
        onClick={() => toggleAttach()}
      >
        {attachEnabled ? <EyeIcon size={11} /> : <EyeOffIcon size={11} />}
      </button>
    </span>
  </span>
);
```

Import 追加 DocumentIcon（已在 icons.tsx Plan 01 添加）。

若 styles.css 中没有 `.selpill` / `.selpill .label` / `.selpill .pill-btn` 规则，在 styles.css 末尾追加：
```css
/* === SelectionPill (selpill) — teal 重皮 D-01 === */
.selpill {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  background: var(--accent-soft);
  color: var(--accent);
  border-radius: var(--radius-full);
  padding: 3px 5px 3px 9px;
  max-width: 100%;
  overflow: hidden;
}
.selpill.is-disabled { opacity: 0.5; }
.selpill .label {
  color: var(--accent);
  font-weight: 500;
  font-size: var(--fs-11);
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
  flex: 1;
}
.selpill .pill-btn {
  width: 18px;
  height: 18px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: 0;
  border-radius: var(--radius-full);
  color: var(--accent);
  opacity: 0.7;
  cursor: pointer;
  flex-shrink: 0;
}
.selpill .pill-btn[data-off] { opacity: 0.4; }
.selpill .pill-btn:hover { opacity: 1; }
```

**3. InputBar.tsx — 重构（最复杂改动）：**

保留所有现有 hooks/store 引用（useChatStore、useAgentStore、useLingui 等），重构 props interface 和 JSX 结构：

a) 新增 props interface：
```tsx
interface InputBarProps {
  onGoSettings: () => void;
}
export default function InputBar({ onGoSettings }: InputBarProps): React.ReactElement {
```

b) Import 追加 GearIcon 和 PaperclipIcon（Plan 01 已在 icons.tsx 添加）。

c) JSX 结构改为：
```tsx
// 保留现有的 isAgentBusy / text / setText / handleSend 等逻辑不动
// 保留现有 ctx（selection context）判断——有 ctx 时渲染 selpill-row

return (
  <div className="inputbar-wrap">
    <div className="inputbar">

      {/* selpill-row：有选区时条件渲染（保持现有 SelectionPill 显示逻辑） */}
      {ctx && (
        <div className="selpill-row">
          <SelectionPill />
        </div>
      )}

      {/* textarea：auto-grow，最大 140px */}
      <textarea
        className="chat-input"
        rows={2}
        placeholder={isAgentBusy ? t`AI 正在回答…` : t`输入消息…`}
        value={text}
        onChange={(e) => setText(e.target.value)}
        onKeyDown={handleKeyDown}
        disabled={isAgentBusy}
        aria-label={t`消息输入框`}
      />

      {/* tools row：gear 左 | paperclip disabled 中 | spacer | send 右 */}
      <div className="tools">
        <button
          type="button"
          className="tool-btn"
          aria-label={t`设置`}
          onClick={onGoSettings}
        >
          <GearIcon size={15} strokeWidth={1.4} />
        </button>
        <button
          type="button"
          className="tool-btn"
          aria-disabled="true"
          aria-label={t`文件上传`}
          title={t`文件上传即将开放`}
          style={{ opacity: 0.38, cursor: 'not-allowed' }}
        >
          <PaperclipIcon size={15} />
        </button>
        <span className="tools-spacer" />
        <button
          type="button"
          className="send-btn"
          data-streaming={isAgentBusy || undefined}
          disabled={!isAgentBusy && !text.trim()}
          onClick={handleSend}
          aria-label={isAgentBusy ? t`停止` : t`发送`}
        >
          {isAgentBusy ? <StopIcon size={11} /> : <SendIcon size={13} />}
        </button>
      </div>
    </div>
  </div>
);
```

**注意：**
- 保留 isAgentBusy 判断（现有：`agentStatus !== 'idle'`），send-btn disabled 行为不变（D-04）
- send-btn 的 `data-streaming` attribute 驱动 CSS 视觉变化，但点击行为不变（agent busy 时 disabled 不可点，stop 走 AgentControlBar）
- 现有 `ctx` 变量从 selectionStore 或 formatSelection 获取，保持不变
- handleKeyDown（Enter 发送、Shift+Enter 换行）保持不变

若 styles.css 中没有以下 CSS 规则，追加到 styles.css 末尾：
```css
/* === InputBar (inputbar-wrap) — teal 重皮 D-01 === */
.inputbar-wrap {
  padding: 8px 12px 12px;
  background: var(--bg-pane);
  border-top: 1px solid var(--border);
  flex-shrink: 0;
}
.inputbar {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-3);
  display: flex;
  flex-direction: column;
}
.inputbar:focus-within {
  border-color: var(--border-strong);
  box-shadow: 0 0 0 3px var(--accent-soft);
}
.inputbar .selpill-row {
  padding: 7px 8px 0 10px;
}
.inputbar .chat-input {
  border: 0;
  outline: none;
  background: transparent;
  width: 100%;
  resize: none;
  font-family: var(--font-body);
  font-size: var(--fs-14);
  color: var(--text);
  padding: 8px 12px 6px;
  min-height: 40px;
  max-height: 140px;
  line-height: 1.5;
}
.inputbar .chat-input::placeholder { color: var(--text-3); }
.inputbar .tools {
  display: flex;
  align-items: center;
  padding: 4px 6px 6px;
  gap: 2px;
}
.inputbar .tools-spacer { flex: 1; }
.inputbar .tool-btn {
  width: 28px;
  height: 28px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: 0;
  border-radius: var(--radius-2);
  color: var(--text-2);
  cursor: pointer;
}
.inputbar .tool-btn:hover { background: var(--surface-2); color: var(--text); }
.inputbar .tool-btn:focus-visible { box-shadow: var(--ring-focus); }
.inputbar .send-btn {
  width: 28px;
  height: 28px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--accent);
  color: var(--accent-on);
  border: 0;
  border-radius: var(--radius-2);
  cursor: pointer;
  flex-shrink: 0;
}
.inputbar .send-btn:hover { background: var(--accent-hover); }
.inputbar .send-btn[disabled] {
  background: transparent;
  color: var(--text-disabled);
  cursor: not-allowed;
}
.inputbar .send-btn[data-streaming] {
  background: var(--text);
  color: var(--bg);
}
.inputbar .send-btn:focus-visible { box-shadow: var(--ring-focus); }
```
  </action>

  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "error TS" | grep -E "InputBar|SelectionPill|ContextCard" | head -10</automated>
    <automated>grep -c "export default null" /Users/wb.chen/Documents/Project/Aster/src/components/ContextCard.tsx</automated>
    <automated>grep -c "onGoSettings" /Users/wb.chen/Documents/Project/Aster/src/components/InputBar.tsx</automated>
    <automated>grep -c "selpill-row\|selpill\b" /Users/wb.chen/Documents/Project/Aster/src/components/InputBar.tsx</automated>
    <automated>grep -c "GearIcon\|PaperclipIcon" /Users/wb.chen/Documents/Project/Aster/src/components/InputBar.tsx</automated>
  </verify>

  <done>
    - ContextCard.tsx 含 `export default null`
    - InputBar.tsx 含 `onGoSettings` prop（接口 + 使用处）
    - InputBar.tsx 含 `selpill-row` 和 `GearIcon` import
    - `npx tsc --noEmit` 无 InputBar/SelectionPill/ContextCard TS 错误
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: App.tsx 结构调整 + InputBar.test.tsx 断言更新</name>
  <files>src/App.tsx, src/components/InputBar.test.tsx</files>

  <read_first>
    1. src/App.tsx（当前全文，了解 topbar 结构、ContextCard import、SettingsIcon 用法）
    2. src/components/InputBar.test.tsx（当前全文，了解现有断言）
    3. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §App.tsx 目标形态（含完整 JSX 对照）
    4. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-VALIDATION.md §Wave 0 Requirements（STRUCT-01/02 测试要求）
  </read_first>

  <action>
**1. App.tsx — 结构调整（D-01/D-02）：**

a) 删除 import：
- `import ContextCard from './components/ContextCard'`（或等效路径）
- `import { SettingsIcon } from './components/icons'`（App.tsx 顶栏齿轮不再用此图标）

b) 根容器 className 从 `aster-shell` → `pane`

c) 删除 `aster-topbar` div（含 ContextCard 和齿轮按钮整块）

d) 删除 `aster-shell__agent-bar` 包装 div（AgentControlBar 直接渲染，idle 时自行 return null）

e) InputBar 加 `onGoSettings={handleOpenSettings}` prop

f) settings-overlay className 从 `aster-settings-overlay` → `settings-overlay`

g) `aster-key-hint` div 改为 `pane-banner` className（同时把 SettingsIcon 换成 AlertCircleIcon，从 icons.tsx import）

若 styles.css 无 `.pane` / `.pane-banner` 规则，追加到 styles.css 末尾：
```css
/* === App pane shell — D-01/D-02 === */
.pane {
  display: flex;
  flex-direction: column;
  height: 100%;
  background: var(--bg);
  min-width: 0;
  width: 100%;
  overflow: hidden;
}
.pane-banner {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 5px 14px;
  background: color-mix(in srgb, var(--warning) 6%, transparent);
  color: var(--warning);
  font-size: var(--fs-11);
  font-weight: 500;
  flex-shrink: 0;
}
```

**2. InputBar.test.tsx — 更新断言（STRUCT-01/STRUCT-02）：**

找到现有测试中断言旧结构的测试（如 `aster-topbar`、`ContextCard` 渲染、旧齿轮在顶栏的断言），更新为新结构：

- 删除或注释掉断言「topbar 包含 ContextCard」的测试
- 添加/更新断言：
  - `screen.queryByTestId` 或 className 查找 `aster-topbar` 应不存在
  - `screen.queryByText('ContextCard')` 或 ContextCard import 相关断言应移除
  - 添加：InputBar 渲染后 DOM 内应有 `inputbar-wrap` 容器（用 `document.querySelector('.inputbar-wrap')` 或 `screen.getByRole(...)` 验证）
  - 添加：tools 行有 gear 按钮（aria-label=「设置」）和 paperclip 按钮（aria-disabled）
  - STRUCT-01（App 不渲染 ContextCard）：在 App 集成测试或 InputBar 测试中验证 `ContextCard` 相关节点不存在
  - STRUCT-02（selpill-row 在顶部，tools 在底部）：若有选区时 selpill 渲染，选区 pill 在 textarea 之前

**注意：**
- 若现有 InputBar.test.tsx 需要 mock `onGoSettings` prop，补充：`<InputBar onGoSettings={() => {}} />`
- ContextCard.test.tsx 可以直接删除（组件已退役），或加 `test.skip` 标记——推荐删除，减少测试维护负担
  </action>

  <verify>
    <automated>grep -c "ContextCard\|aster-topbar" /Users/wb.chen/Documents/Project/Aster/src/App.tsx</automated>
    <automated>grep -c "onGoSettings" /Users/wb.chen/Documents/Project/Aster/src/App.tsx</automated>
    <automated>grep -c "className=\"pane\"" /Users/wb.chen/Documents/Project/Aster/src/App.tsx</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run --reporter=verbose 2>&1 | grep -E "PASS|FAIL|✓|✗|passed|failed" | tail -15</automated>
  </verify>

  <done>
    - `grep -c "ContextCard|aster-topbar" src/App.tsx` = 0（App.tsx 无 ContextCard 引用）
    - `grep -c "onGoSettings" src/App.tsx` ≥ 1
    - `grep -c "className=\"pane\"" src/App.tsx` ≥ 1
    - `npm test -- --run` 通过（STRUCT-01/02 相关测试绿）
    - `npx tsc --noEmit` 无 App.tsx 编译错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | 本 plan 为纯结构/视觉迁移，无新数据流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-03-01 | — | InputBar / App shell | accept | 纯 JSX 结构调整 + className 重命名。选区信息（ctx）仍从现有 selectionStore 读取，数据流不变。API Key 路径不动。onGoSettings 是内部事件回调，不触及外部网络。lingui 标记保留，无裸字符串注入。 |
</threat_model>

<verification>
Plan 03 完成条件：
1. `grep -c "ContextCard|aster-topbar" src/App.tsx` = 0
2. `grep -c "onGoSettings" src/App.tsx` ≥ 2（定义 + 传递）
3. `grep -c "selpill-row" src/components/InputBar.tsx` ≥ 1
4. `grep -c "export default null" src/components/ContextCard.tsx` = 1
5. `npm test -- --run` 测试通过（STRUCT-01/02 测试绿）
6. `npx tsc --noEmit` 无新增 TS 错误
</verification>

<success_criteria>
- App.tsx 无 ContextCard 引用，无顶栏齿轮，根节点 className = "pane"
- InputBar 含 onGoSettings prop + selpill-row + tools 行（gear | paperclip-disabled | send）
- SelectionPill 视觉换 selpill + DocumentIcon + accent-soft 背景
- ContextCard 退役（export null）
- 测试更新通过（STRUCT-01/02 验证通过）
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-03-SUMMARY.md`
</output>

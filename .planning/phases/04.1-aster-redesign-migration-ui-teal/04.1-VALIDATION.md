---
phase: 04.1
slug: aster-redesign-migration-ui-teal
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-29
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> This is a **visual migration** (CSS token rewrite + component reskin + D-01/D-02 structural change) with **no new logic**. Validation is therefore weighted toward (a) light-theme pixel fidelity across PPT/Excel/Word, (b) dark-theme self-check + 1-host spot check, (c) regression guards that no functional capability is lost, (d) bundle budget (NFR-02), (e) i18n coverage.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (existing `*.test.tsx` files: AgentControlBar / ChatStream / InputBar, etc.) |
| **Config file** | `vite.config.ts` (vitest config embedded) |
| **Quick run command** | `npm test -- --run` |
| **Full suite command** | `npm test -- --run --reporter=verbose` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- --run` (quick, <30s)
- **After every plan wave:** Run `npm test -- --run --reporter=verbose` + bundle size check (`npm run build && gzip -c dist/assets/*.js | wc -c`)
- **Before `/gsd-verify-work`:** Full suite green + bundle ≤ 70KB gzipped verified
- **Phase gate (D-08):** Full suite green + light 三宿主 (PPT/Excel/Word) 真机 UAT pass + dark theme Claude browser self-check + 1-host dark spot check
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD-token | token rewrite | — | TOKEN-01 (`--accent` light = `#009887`) | — | N/A | visual | Claude browser screenshot + CSS grep `--accent: #009887` | ✅ styles.css | ⬜ pending |
| TBD-token | token rewrite | — | TOKEN-02 (`--accent` dark = `#4FC9B8`) | — | N/A | visual | Claude browser screenshot (dark, 1 host) | ✅ styles.css | ⬜ pending |
| TBD-inputbar | InputBar | — | STRUCT-01 (ContextCard not rendered, topbar removed) | — | N/A | unit | `npm test -- --run InputBar` | ✅ InputBar.test.tsx (update) | ⬜ pending |
| TBD-inputbar | InputBar | — | STRUCT-02 (selpill-row top, gear+paperclip+send bottom) | — | N/A | unit | `npm test -- --run InputBar` | ✅ InputBar.test.tsx (update) | ⬜ pending |
| TBD-bubble | ChatBubble | — | BUBBLE-01 (user bg=teal, AI bg=bubble-ai-bg) | — | N/A | visual | Claude browser screenshot | — | ⬜ pending |
| TBD-empty | ChatStream | — | EMPTY-01 (no chips, logo pulse + title + subtitle) | — | N/A | unit | `npm test -- --run ChatStream` | ✅ ChatStream.test.tsx (update) | ⬜ pending |
| TBD-error | ErrorBubble | — | ERROR-01 (`.code` mono + inset 3px stripe, no fake undo) | — | N/A | unit | `npm test -- --run` (ErrorBubble snapshot) | ✅ add snapshot | ⬜ pending |
| TBD-agentbar | AgentControlBar | — | AGENT-01 (no backdrop-filter, quiet pill) | — | N/A | visual + grep | `grep -c backdrop-filter src/styles.css` == 0 | ✅ styles.css | ⬜ pending |
| TBD-dark | token rewrite | — | DARK-01 (dark accent = light teal, not dark) | — | N/A | visual | Claude browser screenshot (1 host dark) | — | ⬜ pending |
| TBD-build | build gate | — | BUNDLE-01 (initial gzipped JS ≤ 70KB, NFR-02) | — | N/A | CI | `npm run build && gzip -c dist/assets/*.js \| wc -c` | — Wave 0 | ⬜ pending |
| TBD-i18n | all reskin | — | I18N-01 (no bare Chinese literals in JSX, all via lingui) | — | N/A | lint | `npm run extract` (no new uncatalogued strings) | ✅ lingui toolchain | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Task IDs are placeholders (TBD-*) until the planner assigns final plan/wave numbers — each requirement above is mapped to a concrete task in the PLAN.md files.*

---

## Wave 0 Requirements

> **`nyquist_compliant: true` — Wave 0 items are inlined into the individual plans rather than a standalone Wave 0 plan.** Continuous sampling is satisfied because every task's `<verify>` runs `npm test -- --run` after its commit. The five Wave 0 conditions and their inline locations:

- [x] **Bundle size check** (BUNDLE-01) — inlined into Plan 02 Task verify (`npm run build && gzip -c dist/assets/*.js | wc -c` ≤ 71680) and re-checked in Plan 06.
- [x] **InputBar.test.tsx DOM assertions** (STRUCT-01/02) — inlined into Plan 03 Task action+verify (topbar removed / selpill-row at top / tools row with gear at bottom).
- [x] **ChatStream.test.tsx empty-state assertions** (EMPTY-01) — inlined into Plan 04 Task action+verify (chip assertions removed / empty-mark pulse assertion / title copy「从你正在做的东西开始」).
- [x] **ErrorBubble snapshot/assertion** (ERROR-01) — inlined into Plan 04 Task action+verify (err-bubble element present + `.code` mono span assertion).
- [x] **ContextCard.test.tsx fate** — inlined into Plan 03 Task (delete the test alongside the retired component to reduce maintenance burden); Plan 03 verify confirms removal.

*Pre-existing baseline note: `src/agent/loop.test.ts` has 1 known-failing test (AGENT-02 max_steps soft-landing) on `main` before this phase begins — it is unrelated to this visual migration. Executors should treat the suite as "1 pre-existing failure + this phase's changes"; do not attribute that failure to phase 04.1 work, and do not fix it here unless a reskin task touches the agent loop (none should).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| light-theme pixel fidelity vs aster.css | TOKEN-01, BUBBLE-01, all reskin | Visual fidelity to design package; no pixel-diff harness | D-08: real-machine UAT in PPT + Excel + Word (Office for Web), per `office-addin-browser-uat` skill; compare each of the 9 screens against design package |
| dark-theme correctness | TOKEN-02, DARK-01 | dark tokens defined but never QA'd; lower risk than light | Claude browser self-check screenshots across all dark screens + 1-host real-machine dark spot check (D-08) |
| AgentControlBar quiet style (no glass) | AGENT-01 | Visual; grep confirms no backdrop-filter, but quiet appearance is visual | `grep backdrop-filter src/styles.css` returns nothing + Claude browser screenshot of in-flight agent bar |
| empty-state shell (no fake chips, logo pulse) | EMPTY-01, D-03 | Honest-disable intent is visual/behavioral | Confirm no suggestion chips render; logo pulses (scale 1↔1.06, 4s); subtitle copy matches |
| give-up red card has no fake undo button | D-06 | Honest-disable; verify absence | Trigger CIRCUIT_OPEN, confirm red card shows retry but NO undo button (placeholder toast only) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies or a Manual-Only entry above
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (visual-heavy tasks paired with grep/unit guards where possible)
- [x] Wave 0 covers all MISSING references (bundle check, updated test assertions — all inlined per Wave 0 Requirements above)
- [x] No watch-mode flags (`--run` enforced on all test commands)
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-29

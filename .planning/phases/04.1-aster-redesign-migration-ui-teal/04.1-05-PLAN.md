---
phase: "04.1"
plan: "05"
type: execute
wave: 3
depends_on:
  - "04.1-02"
files_modified:
  - src/components/AgentControlBar.tsx
  - src/components/Onboarding/OnboardingModal.tsx
  - src/components/Onboarding/Step1Keys.tsx
  - src/components/Onboarding/Step2Guide.tsx
  - src/components/Settings/SettingsPanel.tsx
  - src/components/Settings/ProviderList.tsx
  - src/components/Settings/ProviderForm.tsx
autonomous: true
requirements:
  - AGENT-01
  - I18N-01

must_haves:
  truths:
    - "AgentControlBar 无玻璃拟态（backdrop-filter = 0），容器用 quiet pill（surface-2 bg + border + radius-full）"
    - "AgentControlBar focus-visible ring 用 var(--ring-focus)（双层 2px/4px）"
    - "Onboarding modal 是居中卡片（shadow-pop + scrim 遮罩），不是全屏覆盖"
    - "Onboarding 有 modal-brand（logo + 品牌名 + 步骤指示）"
    - "Settings 用 settings-overlay + provider-row + badge-accent + switch 开关"
    - "Settings 返回按钮用 ChevronLeftIcon（不是 scaleX(-1) 翻转的 ChevronIcon）"
    - "所有新增中文字符串走 lingui <Trans> 或 t 模板字面量（I18N-01）"
  artifacts:
    - path: "src/components/AgentControlBar.tsx"
      provides: "quiet pill 形态的 agent 控制条"
      contains: "agent-bar"
    - path: "src/components/Onboarding/OnboardingModal.tsx"
      provides: "modal-scrim + modal 卡片形态"
      contains: "modal-scrim"
    - path: "src/components/Settings/ProviderList.tsx"
      provides: "provider-row 列表行 + badge-accent"
      contains: "provider-row"
  key_links:
    - from: "src/components/AgentControlBar.tsx"
      to: "src/styles.css"
      via: "agent-bar（无 backdrop-filter）"
      pattern: "agent-bar"
    - from: "src/components/Settings/SettingsPanel.tsx"
      to: "src/components/icons.tsx"
      via: "ChevronLeftIcon（Plan 01 新增）"
      pattern: "ChevronLeftIcon"
---

<objective>
Wave 3 组件重皮（并行组 B）：AgentControlBar + Onboarding + Settings，覆盖运行时控制条、引导流程和设置界面的视觉迁移。

Purpose: 这三个组件与 Plan 04（ChatBubble/ChatStream/ErrorBubble）无文件重叠，可在 Wave 3 并行执行。AgentControlBar 是 AGENT-01 验证核心（无玻璃拟态）。Onboarding/Settings 是完整 UI 迁移的必要组成。

Output: 7 个文件完成 teal 克制视觉迁移，交互逻辑全部不动。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-RESEARCH.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md

<interfaces>
<!-- AgentControlBar 目标形态（PATTERNS.md §AgentControlBar.tsx） -->
<!-- .agent-bar: bg var(--surface-2), border 1px solid var(--border), border-radius var(--radius-full) -->
<!-- 无 backdrop-filter；focus-visible: box-shadow var(--ring-focus) -->
<!-- 保留所有现有 Phase 3/4 交互逻辑（pause/abort/step counter/stall/三态文案）不动（D-04） -->

<!-- Onboarding 目标形态（PATTERNS.md §Onboarding/*） -->
<!-- modal-scrim: 全屏遮罩 color-mix(in srgb, var(--text) 35%, transparent) -->
<!-- modal: 320px, r-4 16px, shadow-pop, bg var(--surface) -->
<!-- modal-brand: logo 22px + "Aster" + "01/02" 步骤文字 -->
<!-- Step2Guide host-card: 用文字标签（PPT/Excel/Word）+ Lucide 图标代替官方 SVG（版权规避）-->

<!-- Settings 目标形态（PATTERNS.md §Settings/*） -->
<!-- settings-overlay: translateX slide-in 机制保留，换 quiet 背景 var(--bg) -->
<!-- provider-row: 列表行 + .pinfo + .pmodel mono + .pactions -->
<!-- badge-accent: accent-soft bg + accent 色（isBuiltIn Provider）-->
<!-- switch: checked 时 bg var(--accent)；.select-wrap .input.select + .select-caret(ChevronDownIcon)-->
<!-- ChevronLeftIcon（Plan 01 已在 icons.tsx 添加）-->

<!-- --ring-focus = 0 0 0 2px var(--bg), 0 0 0 4px var(--accent)（Plan 02 已在 styles.css 定义）-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AgentControlBar 重皮（去玻璃拟态，quiet pill，AGENT-01）</name>
  <files>src/components/AgentControlBar.tsx</files>

  <read_first>
    1. src/components/AgentControlBar.tsx（当前全文，了解现有 Phase 3/4 逻辑：pause/abort/step counter/stall/三态文案/5秒安抚）
    2. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §AgentControlBar.tsx（current + target 完整对照，含 CSS）
    3. src/components/AgentControlBar.test.tsx（了解现有测试，不要破坏）
  </read_first>

  <action>
**AgentControlBar.tsx 纯视觉重皮（D-04：交互骨架不动）：**

保留所有现有 hooks、事件处理（pause/abort/step counter/stall/三态文案/5秒安抚逻辑）完全不变。只替换 className 和移除玻璃拟态样式。

a) 根容器 className：`aster-agent-bar` → `agent-bar`

b) 子元素 className：
- `aster-agent-bar__step` → `agent-step`
- `aster-agent-bar__phase` → `agent-phase`
- `aster-agent-bar__stall` → `agent-stall`（去掉旧的 font-style: italic，改为正体）
- `aster-iconbtn aster-agent-bar__btn` → `btn-icon`

c) 保留所有 PauseIcon / PlayIcon / StopIcon 引用不变。

d) 若 styles.css 无 `.agent-bar` 规则，追加到末尾：
```css
/* === AgentControlBar — quiet pill, D-04（无玻璃拟态） === */
.agent-bar {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 5px 8px 5px 12px;
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-full);
  margin: 6px 12px 0;
  flex-shrink: 0;
  /* 无 backdrop-filter — D-04，AGENT-01 */
}
.agent-step {
  font-family: var(--font-mono);
  font-size: var(--fs-11);
  color: var(--text-3);
  margin-right: 2px;
}
.agent-phase {
  font-size: var(--fs-11);
  color: var(--text-3);
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.agent-stall {
  font-size: var(--fs-11);
  color: var(--text-3);
  font-style: normal; /* 去掉旧的斜体 */
  flex: 1;
}
.btn-icon {
  width: 28px;
  height: 28px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: 0;
  border-radius: var(--radius-2);
  color: var(--text-2);
  cursor: pointer;
  flex-shrink: 0;
}
.btn-icon:hover { background: var(--surface-2); color: var(--text); }
.btn-icon:focus-visible { outline: none; box-shadow: var(--ring-focus); }
```

**验证 AGENT-01：** 迁移后整个 styles.css 不含 `backdrop-filter`（Plan 02 已清理 token 层，本 plan 不能引入新的）。
  </action>

  <verify>
    <automated>grep -c "agent-bar" /Users/wb.chen/Documents/Project/Aster/src/components/AgentControlBar.tsx</automated>
    <automated>grep -c "aster-agent-bar\|backdrop-filter\|glass-bg" /Users/wb.chen/Documents/Project/Aster/src/components/AgentControlBar.tsx</automated>
    <automated>grep -c "backdrop-filter" /Users/wb.chen/Documents/Project/Aster/src/styles.css</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run --reporter=verbose 2>&1 | grep -E "AgentControlBar|PASS|FAIL" | head -10</automated>
  </verify>

  <done>
    - `grep -c "agent-bar" AgentControlBar.tsx` ≥ 1
    - `grep -c "aster-agent-bar|backdrop-filter|glass-bg" AgentControlBar.tsx` = 0
    - `grep -c "backdrop-filter" src/styles.css` = 0（全局确认）
    - AgentControlBar 测试通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Onboarding + Settings 重皮</name>
  <files>
    src/components/Onboarding/OnboardingModal.tsx,
    src/components/Onboarding/Step1Keys.tsx,
    src/components/Onboarding/Step2Guide.tsx,
    src/components/Settings/SettingsPanel.tsx,
    src/components/Settings/ProviderList.tsx,
    src/components/Settings/ProviderForm.tsx
  </files>

  <read_first>
    1. src/components/Onboarding/OnboardingModal.tsx（当前全文）
    2. src/components/Onboarding/Step1Keys.tsx（当前全文）
    3. src/components/Onboarding/Step2Guide.tsx（当前全文）
    4. src/components/Settings/SettingsPanel.tsx（当前全文）
    5. src/components/Settings/ProviderList.tsx（当前全文）
    6. src/components/Settings/ProviderForm.tsx（当前全文）
    7. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §Onboarding/* + §Settings/*（目标形态完整对照）
    8. .planning/design/aster-redesign/src/proto-app.jsx（Onboarding + SettingsBrowse/Edit 参考实现）
  </read_first>

  <action>
**A. OnboardingModal.tsx — modal-scrim + modal 卡片形态：**

保留所有现有 step 状态逻辑、onComplete/onSkip 回调、storage 写入不变。替换 JSX 结构：

旧：`aster-onboarding-overlay`（全屏覆盖）→ 新：`modal-scrim`（dark scrim + 居中 modal）

```tsx
return (
  <div className="modal-scrim" role="dialog" aria-modal="true" aria-labelledby="modal-title">
    <div className="modal">
      <div className="modal-brand">
        <img src={logo} alt="Aster" style={{ width: 22, height: 22 }} />
        <span className="brand-name">Aster</span>
        <span className="brand-step">{step === 1 ? '01' : '02'} / 02</span>
      </div>
      {step === 1 ? (
        <Step1Keys onNext={goNext} onSkip={handleSkip} />
      ) : (
        <Step2Guide onBack={goBack} onComplete={handleComplete} />
      )}
    </div>
  </div>
);
```

步骤子组件（Step1Keys / Step2Guide）的 footer 按钮改用 `.btn .btn-ghost .btn-sm` / `.btn .btn-primary .btn-sm` 类名。

若 styles.css 无 `.modal-scrim` / `.modal` / `.modal-brand` 规则，追加到末尾：
```css
/* === Onboarding modal === */
.modal-scrim {
  position: fixed;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: color-mix(in srgb, var(--text) 35%, transparent);
  z-index: 50;
  padding: 16px;
}
.modal {
  background: var(--surface);
  border-radius: var(--radius-4);
  box-shadow: var(--shadow-pop);
  width: 100%;
  max-width: 320px;
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.modal-brand {
  display: flex;
  align-items: center;
  gap: 8px;
}
.modal-brand .brand-name {
  font-weight: 600;
  font-size: var(--fs-14);
  color: var(--text);
  flex: 1;
}
.modal-brand .brand-step {
  font-size: var(--fs-11);
  color: var(--text-3);
  font-family: var(--font-mono);
}
.modal-title {
  font-size: var(--fs-18);
  font-weight: 600;
  color: var(--text);
  margin: 0;
}
.modal-sub {
  font-size: var(--fs-13);
  color: var(--text-2);
  line-height: 1.6;
  margin: 0;
}
.modal-body { display: flex; flex-direction: column; gap: 10px; }
.modal-foot {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  padding-top: 4px;
}
/* 基础 btn 系 */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 4px;
  border: 0;
  border-radius: var(--radius-2);
  font-family: var(--font-body);
  font-size: var(--fs-13);
  font-weight: 500;
  cursor: pointer;
  padding: 6px 14px;
  transition: background var(--dur-fast) var(--ease-out);
}
.btn-primary {
  background: var(--accent);
  color: var(--accent-on);
}
.btn-primary:hover { background: var(--accent-hover); }
.btn-ghost {
  background: transparent;
  color: var(--text-2);
  border: 1px solid var(--border);
}
.btn-ghost:hover { background: var(--surface-2); }
.btn-sm { padding: 5px 12px; font-size: var(--fs-12); }
.btn:focus-visible { outline: none; box-shadow: var(--ring-focus); }
/* input 通用样式 */
.input {
  width: 100%;
  padding: 8px 10px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-2);
  font-family: var(--font-body);
  font-size: var(--fs-13);
  color: var(--text);
  outline: none;
}
.input:focus { border-color: var(--border-strong); box-shadow: 0 0 0 3px var(--accent-soft); }
.input::placeholder { color: var(--text-3); }
```

**Step2Guide — host-card（版权安全方案）：**

用文字标签 + Lucide 图标替代官方 Office SVG（RESEARCH.md Open Questions #3，版权规避）：
- PPT → PresentationIcon 或用「📊」emoji——不，用 Lucide `InsertIcon`（现有）表示「演示」
- Excel → 用现有 `CheckIcon` 或新建简单的表格图标
- Word → 用 `DocumentIcon`（Plan 01 已添加）

实际操作：Step2Guide 的三个 host-card 改用 `.host-card` className，每个卡片：
```tsx
<div className="host-card">
  <div className="host-icon">
    <DocumentIcon size={24} />
  </div>
  <div className="host-info">
    <div className="host-name"><Trans>PowerPoint</Trans></div>
    <ul>
      <li><Trans>读取幻灯片内容和形状</Trans></li>
      <li><Trans>插入新幻灯片</Trans></li>
      <li><Trans>修改文字和布局</Trans></li>
    </ul>
  </div>
</div>
```

若 styles.css 无 `.host-card` 规则，追加到末尾：
```css
.host-card {
  display: flex;
  gap: 12px;
  padding: 14px;
  background: var(--surface-2);
  border-radius: var(--radius-3);
}
.host-icon { width: 36px; height: 36px; display: flex; align-items: flex-start; justify-content: center; color: var(--accent); flex-shrink: 0; }
.host-info { flex: 1; }
.host-name { font-size: var(--fs-14); font-weight: 600; color: var(--text); margin-bottom: 6px; }
.host-card ul { padding-left: 16px; margin: 0; }
.host-card li { font-size: var(--fs-12); color: var(--text-2); line-height: 1.55; }
```

**B. SettingsPanel.tsx — 结构重皮：**

保留所有现有 edit state 路由（browse/edit-provider/create-provider）和逻辑不变。

1. 根容器 className：`aster-settings-overlay` → `settings-overlay`（translateX 机制保留）
2. header 区：`aster-settings__header` → `settings-head`；返回按钮 `ChevronIcon + scaleX(-1)` → `ChevronLeftIcon`（import 已在 icons.tsx Plan 01 添加）

若 styles.css 无 `.settings-overlay` / `.settings-head` 规则，追加：
```css
/* === Settings overlay === */
.settings-overlay {
  position: absolute;
  inset: 0;
  background: var(--bg);
  transform: translateX(100%);
  transition: transform var(--dur-base) var(--ease-out);
  display: flex;
  flex-direction: column;
  z-index: 20;
  overflow: hidden;
}
.settings-overlay.is-open { transform: translateX(0); }
.settings-head {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 14px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}
.settings-head .back-btn {
  width: 28px;
  height: 28px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: 0;
  border-radius: var(--radius-2);
  color: var(--text-2);
  cursor: pointer;
}
.settings-head .back-btn:hover { background: var(--surface-2); }
.settings-head .head-title {
  font-size: var(--fs-14);
  font-weight: 600;
  color: var(--text);
  flex: 1;
}
```

**C. ProviderList.tsx — provider-row + badge 重皮：**

保留所有现有列表渲染逻辑、onEdit 回调不变。

替换：
- `aster-provider-item` → `provider-row`
- `aster-badge-default` → `badge badge-accent`（仅限 isBuiltIn Provider 显示「默认」）
- `aster-badge-builtin` → `badge badge-success`（已配 Key）/ 无 badge 或灰 badge（未配 Key）
- 编辑按钮换 ChevronRightIcon（可用现有 ChevronIcon，不需要翻转）

若 styles.css 无 `.provider-row` / `.badge` 规则，追加：
```css
.provider-row {
  display: flex;
  align-items: center;
  padding: 10px 14px;
  gap: 8px;
  border-bottom: 1px solid var(--border);
}
.provider-row:last-child { border-bottom: 0; }
.pinfo { flex: 1; min-width: 0; }
.pname-line { display: flex; align-items: center; gap: 6px; margin-bottom: 2px; }
.pname { font-size: var(--fs-13); font-weight: 500; color: var(--text); }
.pmodel { font-family: var(--font-mono); font-size: var(--fs-11); color: var(--text-3); }
.badge {
  display: inline-flex;
  align-items: center;
  padding: 1px 6px;
  border-radius: var(--radius-full);
  font-size: var(--fs-11);
  font-weight: 500;
  background: var(--surface-2);
  color: var(--text-3);
}
.badge-accent { background: var(--accent-soft); color: var(--accent); }
.badge-success { background: var(--success-soft); color: var(--success); }
.pactions { flex-shrink: 0; }
```

**D. ProviderForm.tsx — model select 下拉 + builtin-note 重皮：**

保留所有现有 form 逻辑（onSave/onCancel/field validation）不变。

1. model select 字段的容器：`.aster-field--standalone select` → `.select-wrap` 包裹 + `.input.select` + `.select-caret`（ChevronDownIcon 14px）
2. `aster-form-hint` → `builtin-note`（bg surface-2 圆角 pill 小字）
3. form 输入框 className 换 `.input`（Plan 中 Onboarding 已追加到 styles.css）
4. form 底部 sticky footer 按钮换 `.btn .btn-primary` / `.btn .btn-ghost`

若 styles.css 无 `.select-wrap` / `.builtin-note` 规则，追加：
```css
.select-wrap { position: relative; }
.select-wrap .input.select { appearance: none; padding-right: 28px; cursor: pointer; }
.select-caret {
  position: absolute;
  right: 8px;
  top: 50%;
  transform: translateY(-50%);
  pointer-events: none;
  color: var(--text-3);
}
.builtin-note {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 10px;
  background: var(--surface-2);
  border-radius: var(--radius-full);
  font-size: var(--fs-11);
  color: var(--text-3);
}
/* switch toggle */
.switch {
  position: relative;
  width: 34px;
  height: 20px;
  flex-shrink: 0;
}
.switch input { opacity: 0; width: 0; height: 0; }
.switch .thumb {
  position: absolute;
  inset: 0;
  background: var(--surface-3);
  border-radius: var(--radius-full);
  cursor: pointer;
  transition: background var(--dur-fast) var(--ease-out);
}
.switch .thumb::after {
  content: '';
  position: absolute;
  width: 14px;
  height: 14px;
  background: #fff;
  border-radius: 50%;
  top: 3px;
  left: 3px;
  transition: transform var(--dur-fast) var(--ease-out);
}
.switch input:checked + .thumb { background: var(--accent); }
.switch input:checked + .thumb::after { transform: translateX(14px); }
```

**所有新增/修改的中文字符串必须用 `<Trans>` 包裹（lingui I18N-01 约束）。**
  </action>

  <verify>
    <automated>grep -c "modal-scrim" /Users/wb.chen/Documents/Project/Aster/src/components/Onboarding/OnboardingModal.tsx</automated>
    <automated>grep -c "aster-onboarding-overlay\|aster-onboarding__" /Users/wb.chen/Documents/Project/Aster/src/components/Onboarding/OnboardingModal.tsx</automated>
    <automated>grep -c "provider-row" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/ProviderList.tsx</automated>
    <automated>grep -c "ChevronLeftIcon" /Users/wb.chen/Documents/Project/Aster/src/components/Settings/SettingsPanel.tsx</automated>
    <automated>grep -c "agent-bar" /Users/wb.chen/Documents/Project/Aster/src/components/AgentControlBar.tsx</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run 2>&1 | tail -10</automated>
  </verify>

  <done>
    - `grep -c "modal-scrim" OnboardingModal.tsx` ≥ 1
    - `grep -c "aster-onboarding-overlay|aster-onboarding__" OnboardingModal.tsx` = 0（旧类名清理）
    - `grep -c "provider-row" ProviderList.tsx` ≥ 1
    - `grep -c "ChevronLeftIcon" SettingsPanel.tsx` ≥ 1
    - `grep -c "agent-bar" AgentControlBar.tsx` ≥ 1（并且无 backdrop-filter）
    - `npm test -- --run` 测试套件通过
    - TypeScript 无编译错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | 本 plan 为纯视觉迁移，无新数据流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05-01 | — | Onboarding / Settings / AgentControlBar | accept | 纯 className 重命名 + CSS 替换，无数据流/存储/网络变化。Settings 表单逻辑（API Key 写入 localStorage via lib/storage.ts）不动。Onboarding step 状态不影响 Key 存储路径。lingui <Trans> 保留，无裸字符串注入。Switch/Select 控件是纯视觉样式，不改数据绑定逻辑。 |
</threat_model>

<verification>
Plan 05 完成条件：
1. `grep -c "modal-scrim" OnboardingModal.tsx` ≥ 1
2. `grep -c "agent-bar" AgentControlBar.tsx` ≥ 1
3. `grep -c "backdrop-filter" src/styles.css` = 0（全局确认无 backdrop-filter 复入）
4. `grep -c "provider-row" ProviderList.tsx` ≥ 1
5. `grep -c "ChevronLeftIcon" SettingsPanel.tsx` ≥ 1
6. `npm test -- --run` 全通过
7. `npx tsc --noEmit` 无新增 TS 错误
</verification>

<success_criteria>
- AgentControlBar：quiet pill 形态，无 backdrop-filter，focus ring 用 var(--ring-focus)
- Onboarding：modal-scrim 遮罩 + modal 卡片（shadow-pop，320px，r-16）+ modal-brand（logo + 品牌步骤指示）
- Settings：settings-overlay + provider-row + badge-accent + ChevronLeftIcon 返回 + switch/select 新样式
- 测试通过，无 i18n 裸字符串
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-05-SUMMARY.md`
</output>

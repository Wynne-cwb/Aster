---
phase: "04.1"
plan: "04"
subsystem: "chat-ui"
tags: [teal-redesign, bubble, empty-state, error-bubble, wave-3]
dependency_graph:
  requires: [04.1-01, 04.1-02, 04.1-03]
  provides: [bubble-user-ai-teal, msg-time-timestamp, empty-mark-pulse, err-bubble-stripe, wb-action-head-chevron]
  affects: [ChatBubble, ChatStream, ErrorBubble, icons, styles]
tech_stack:
  added: []
  patterns:
    - "err-bubble：inset 3px 0 0 var(--error) 左条纹 + .code mono 代号"
    - "wb-action-head：ChevronDownIcon SVG + .is-up CSS rotate(180deg)"
    - "empty-mark：aster-pulse 4s ease-in-out infinite scale(1↔1.06)"
    - "msg-time：font-mono fs-11 气泡底部时间戳"
key_files:
  created: []
  modified:
    - src/components/ChatBubble.tsx
    - src/components/ChatStream.tsx
    - src/components/ErrorBubble.tsx
    - src/components/ChatStream.test.tsx
    - src/components/icons.tsx
    - src/styles.css
    - src/i18n/locales/zh-CN/messages.po
decisions:
  - "ExpandedBody 里 .aster-tool-card__body → .wb-action-body（D-05 折叠范式统一）"
  - "ErrorBubble 中 cta-hint 独立于 cta-row，避免 action/none 两类 CTA 逻辑混淆"
  - "ERROR-01 测试直接渲染 ErrorBubble 而非通过 ChatStream 集成，绕过 ChatBubble mock 层"
  - "ChatStream usageExamples 函数整体删除（非注释），符合 D-03 Clean 精神"
metrics:
  duration: "13 min"
  completed_date: "2026-05-29"
  tasks: 2
  files: 7
---

# Phase 04.1 Plan 04: ChatBubble + ChatStream + ErrorBubble Wave 3 重皮 Summary

Wave 3 三组件 teal 视觉迁移全部完成：气泡新形态（teal user + 冷灰 AI + msg-time 时间戳）、empty-state 壳（logo pulse + 无 chips）、ToolResultCard D-05 折叠范式、ErrorBubble D-06 inset stripe + mono 代号。

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | ChatBubble teal 重皮 + msg-time 时间戳 | 0c0ebea | ChatBubble.tsx, styles.css |
| 2 | ChatStream + ErrorBubble 重皮 + 测试更新 | ec23cfe | ChatStream.tsx, ErrorBubble.tsx, ChatStream.test.tsx, icons.tsx, styles.css, messages.po |

## 交付物

### ChatBubble.tsx
- `bubble-user`：`var(--accent)` teal 背景 + `var(--accent-on)` 白字 + 右下 4px 指向角
- `bubble-ai`：`var(--bubble-ai-bg)` #EEEEF0 + 左下 4px 指向角 + 1px border
- `msg-time`：`formatTime(msg.ts)` 在气泡下方，mono 11px，opacity 0.7
- `.caret`：streaming 时光标闪烁动画（取代旧 `.aster-cursor`）

### ChatStream.tsx
- **empty-state**：删除 `aster-chips`/`usageExamples`（D-03），改为 `.empty > .empty-mark(pulse 4s) + h3 + p`
- **ToolResultCard**：ChevronDownIcon SVG + `wb-action-head`/`wb-action-body` 类名（D-05），删除 ▸/▾ 字符
- **CIRCUIT_OPEN 红卡**：套 `err-bubble` 视觉范式，无假撤销按钮（D-06 诚实禁用）
- 删除 `usageExamples` 函数和宿主选区逻辑（不再需要 `host` 变量）

### ErrorBubble.tsx
- `err-bubble`：`background: var(--bubble-ai-bg)` + `box-shadow: inset 3px 0 0 var(--error)` 左条纹
- `.head`：AlertIcon 13px + `.code`（mono 代号 span）+ 红色 fw600
- `.reason`：主文案行（来自 ERROR_UI_MAP 固定字符串，安全约束保留）
- `.cta`：下划线点击链接（settings 深链 / 重试操作）
- 整体包裹在 `.msg.msg-ai` 内，与 AI 气泡左对齐

### ChatStream.test.tsx
- ChatStream-1 更新：`.aster-tool-card__header` → `.wb-action-head`
- ChatStream-5 更新：同上
- 新增 EMPTY-01 套件（3 个测试）：empty-mark/无 chips/h3 标题
- 新增 ERROR-01 套件（4 个测试）：err-bubble/.code/.reason/.msg-ai
- lingui mock 补 `t` 函数支持

### icons.tsx
- `ChevronDownIcon`：添加 `className?: string` prop（供 `.is-up` CSS 旋转）
- `AlertIcon`：添加 `size?: number` prop
- `RetryIcon`：添加 `size?: number` prop

### styles.css（追加部分）
- `.msg`/`.msg-user`/`.msg-ai`/`.bubble`/`.bubble-user`/`.bubble-ai`/`.caret`/`.msg-time`
- `.chat-scroll`/`.empty`/`.empty-mark`/`@keyframes aster-pulse`
- `.wb-action-head`/`.wb-action-body`/`.wb-action-target`
- `.err-bubble`/`.err-bubble .head`/`.err-bubble .head .code`/`.err-bubble .reason`/`.err-bubble .cta-row`/`.err-bubble .cta`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] ChevronDownIcon 缺少 className prop**
- **Found during:** Task 2 — ChatStream.tsx 用 `className={expanded ? 'is-up' : ''}`
- **Issue:** 原 ChevronDownIcon 签名只有 `size`，不接受 `className`
- **Fix:** 添加 `className?: string` prop 并透传给 svg 元素
- **Files modified:** src/components/icons.tsx
- **Commit:** ec23cfe

**2. [Rule 1 - Bug] AlertIcon/RetryIcon 缺少 size prop**
- **Found during:** Task 2 — ChatStream.tsx 用 `<AlertIcon size={13} />` 和 `<RetryIcon size={12} />`
- **Issue:** 两个图标函数不接受 `size` 参数
- **Fix:** 两者均添加 `{ size = 24 }` 参数，透传给 svg 的 width/height
- **Files modified:** src/components/icons.tsx
- **Commit:** ec23cfe

**3. [Rule 2 - Missing] ERROR-01 测试需要 lingui t 函数 mock**
- **Found during:** Task 2 测试运行，ERROR-01 报 `t is not a function`
- **Issue:** ErrorBubble 使用 `useLingui().t`，但测试文件顶部 mock 只提供 `_`
- **Fix:** lingui mock 补 `t: (id: string) => id`
- **Files modified:** src/components/ChatStream.test.tsx
- **Commit:** ec23cfe

**4. [Rule 2 - Missing] i18n messages.po 需要随新字符串更新**
- **Found during:** Task 2 — i18n 覆盖测试因行号变化和新文案 `从你正在做的东西开始` 失败
- **Fix:** 运行 `npm run extract`，更新 messages.po
- **Files modified:** src/i18n/locales/zh-CN/messages.po
- **Commit:** ec23cfe

**5. [Rule 3 - Fix] ERROR-01 测试设计改为直接渲染 ErrorBubble**
- **Found during:** Task 2 — 通过 ChatStream 集成测试 err-bubble 不可行（ChatBubble 被 mock）
- **Fix:** ERROR-01 测试改为直接 `render(<ErrorBubble .../>)` 验证 err-bubble 形态
- **Files modified:** src/components/ChatStream.test.tsx
- **Commit:** ec23cfe

## 测试结果

```
Test Files  1 failed | 37 passed (38)
Tests       1 failed | 459 passed (460)
```

唯一失败：`loop.test.ts > AGENT-02 max_steps soft landing`——预存在的已知失败，与本 plan 无关（STATE.md / Phase 04.1 UAT EVIDENCE 均有记录）。

## Known Stubs

无。所有组件均有真实数据源（messages store、ERROR_UI_MAP、formatTime）。

## Threat Flags

无。本 plan 为纯视觉迁移，无新数据流、无新网络端点、无新存储路径。

## Self-Check: PASSED

- [x] src/components/ChatBubble.tsx 存在且含 `bubble-user` / `msg-time`
- [x] src/components/ChatStream.tsx 存在且含 `empty-mark` / `wb-action-head`，无 `aster-chips`
- [x] src/components/ErrorBubble.tsx 存在且含 `err-bubble` / `.code`
- [x] Commits 0c0ebea 和 ec23cfe 存在于 git log
- [x] `npm run build` 通过（79.84 KB ≤ 80 KB）
- [x] 新增测试 EMPTY-01（3 个）和 ERROR-01（4 个）全绿

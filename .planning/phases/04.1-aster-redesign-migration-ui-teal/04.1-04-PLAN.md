---
phase: "04.1"
plan: "04"
type: execute
wave: 3
depends_on:
  - "04.1-02"
files_modified:
  - src/components/ChatBubble.tsx
  - src/components/ChatStream.tsx
  - src/components/ErrorBubble.tsx
  - src/components/ChatStream.test.tsx
autonomous: true
requirements:
  - BUBBLE-01
  - EMPTY-01
  - ERROR-01
  - I18N-01

must_haves:
  truths:
    - "user 气泡 bg = var(--accent)（teal），ai 气泡 bg = var(--bubble-ai-bg)（#EEEEF0）"
    - "ChatBubble 有时间戳（msg-time）显示在气泡下方，格式 MM-DD HH:MM，mono 字体"
    - "empty-state 无任何 suggestion chips，有 logo pulse 动画（scale 1↔1.06, 4s）"
    - "ToolResultCard 折叠卡用 ChevronDownIcon（SVG）+ wb-action-head/wb-action-body 类名"
    - "ErrorBubble 有 inset 3px 0 0 var(--error) stripe + .code mono 代号"
    - "give-up 红卡（CIRCUIT_OPEN）套 err-bubble 视觉范式，无假撤销按钮（D-06 诚实禁用）"
    - "所有新增中文字符串走 lingui <Trans> 或 t 模板字面量"
  artifacts:
    - path: "src/components/ChatBubble.tsx"
      provides: "bubble-user / bubble-ai + msg-time 时间戳"
      contains: "bubble-user"
    - path: "src/components/ChatStream.tsx"
      provides: "empty-state 壳 + ToolResultCard chevron + give-up err-bubble"
      contains: "empty-mark"
    - path: "src/components/ErrorBubble.tsx"
      provides: "err-bubble 新形态（inset stripe + .code 代号 + .cta 下划线）"
      contains: "err-bubble"
  key_links:
    - from: "src/components/ChatBubble.tsx"
      to: "src/utils/formatTime.ts"
      via: "时间戳渲染"
      pattern: "formatTime"
    - from: "src/components/ChatStream.tsx"
      to: "src/components/icons.tsx"
      via: "ChevronDownIcon（D-05 折叠范式）"
      pattern: "ChevronDownIcon"
    - from: "src/components/ChatStream.tsx"
      to: "src/components/ErrorBubble.tsx"
      via: "give-up 红卡使用 err-bubble 样式"
      pattern: "err-bubble"
---

<objective>
Wave 3 组件重皮（并行组）：ChatBubble + ChatStream + ErrorBubble，覆盖聊天消息渲染的全部视觉层。

Purpose: ChatBubble（气泡形态）+ ChatStream（消息流 + empty-state + ToolResultCard + give-up 卡）+ ErrorBubble（错误气泡形态）三个组件可独立实现，无文件重叠，本 plan 内可顺序完成。Wave 3 与 Plan 05（AgentControlBar + Onboarding + Settings）可并行，但两者不依赖彼此。

Output: 三个组件完成 teal 克制视觉迁移；ChatStream.test.tsx 更新（EMPTY-01/ERROR-01）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-RESEARCH.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md

<interfaces>
<!-- ChatBubble 目标形态（PATTERNS.md §ChatBubble.tsx，含 CSS target aster.css lines 580-596） -->
<!-- .bubble-user: bg var(--accent), color var(--accent-on), border-bottom-right-radius var(--radius-1) -->
<!-- .bubble-ai: bg var(--bubble-ai-bg), color var(--text), border-bottom-left-radius var(--radius-1) -->
<!-- .msg-time: font-mono fs-11, color var(--text-3), 永远显示在气泡下方 -->
<!-- formatTime(ts) 已在 Plan 01 创建于 src/utils/formatTime.ts -->

<!-- ChatStream 目标形态（PATTERNS.md §ChatStream.tsx）-->
<!-- empty-state: .chat-scroll > .empty > .empty-mark(pulse 4s) + h3 + p，无 chips -->
<!-- ToolResultCard: wb-action-head(ChevronDownIcon .is-up when expanded) + wb-action-body -->
<!-- give-up 红卡: err-bubble 范式，inset 3px stripe，有重试按钮，无撤销按钮 -->

<!-- ErrorBubble 目标形态（PATTERNS.md §ErrorBubble.tsx，含 CSS target aster.css lines 896-937）-->
<!-- .err-bubble: bg bubble-ai-bg, br 12px, bl-br 4px, box-shadow inset 3px 0 0 var(--error) -->
<!-- .err-bubble .head: color error, fw 600, fs-12, AlertIcon 13px + .code mono span -->
<!-- .err-bubble .cta: color error, text-decoration underline -->

<!-- Plan 02 已定义 --bubble-ai-bg / --accent / --error / --error-soft 等 token -->
<!-- Plan 01 已定义 ChevronDownIcon / AlertCircleIcon 等图标 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ChatBubble 重皮（bubble-user/ai + 时间戳 msg-time）</name>
  <files>src/components/ChatBubble.tsx</files>

  <read_first>
    1. src/components/ChatBubble.tsx（当前全文，了解 role 分支、ReactMarkdown 用法）
    2. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §ChatBubble.tsx（current + target 完整对照，含 CSS）
    3. src/utils/formatTime.ts（Plan 01 创建，确认 export 签名）
    4. src/store/chat.ts（确认 Message.ts 字段已有，Plan 01 已加）
  </read_first>

  <action>
**ChatBubble.tsx 完整重皮：**

保留所有现有 props（message: Message）、ReactMarkdown import、role 判断逻辑、error 委托 ErrorBubble 逻辑不变。

a) Import 追加：`import { formatTime } from '../utils/formatTime';`

b) user 气泡（`message.role === 'user'`）改为：
```tsx
return (
  <div className="msg msg-user">
    <div className="bubble bubble-user">
      {message.content}
    </div>
    {message.ts && (
      <span className="msg-time">{formatTime(message.ts)}</span>
    )}
  </div>
);
```

c) assistant 气泡（`message.role === 'assistant'`）改为：
```tsx
return (
  <div className="msg msg-ai">
    <div className="bubble bubble-ai">
      <ReactMarkdown remarkPlugins={[remarkGfm]} components={{...}}>
        {message.content}
      </ReactMarkdown>
      {message.isStreaming && <span className="caret" aria-hidden="true" />}
    </div>
    {!message.isStreaming && message.ts && (
      <span className="msg-time">{formatTime(message.ts)}</span>
    )}
  </div>
);
```

d) error role 委托 ErrorBubble 不变（保留现有逻辑）。

e) 若 styles.css 无以下规则，追加到末尾：
```css
/* === ChatBubble — teal 重皮 === */
.msg { display: flex; flex-direction: column; }
.msg-user { align-items: flex-end; }
.msg-ai { align-items: flex-start; }
.bubble {
  padding: 9px 13px;
  border-radius: var(--radius-3);
  max-width: 88%;
  word-break: break-word;
  font-size: var(--fs-14);
  line-height: 1.55;
}
.bubble-user {
  background: var(--accent);
  color: var(--accent-on);
  border-bottom-right-radius: var(--radius-1);
  box-shadow: inset 0 1px 0 color-mix(in srgb, #fff 18%, transparent);
}
.bubble-ai {
  background: var(--bubble-ai-bg);
  color: var(--text);
  border-bottom-left-radius: var(--radius-1);
  border: 1px solid rgba(0, 0, 0, 0.04);
}
.caret {
  display: inline-block;
  width: 2px;
  height: 14px;
  background: var(--text-2);
  margin-left: 2px;
  vertical-align: middle;
  animation: blink 1s step-start infinite;
}
@keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
.msg-time {
  font-family: var(--font-mono);
  font-size: var(--fs-11);
  color: var(--text-3);
  margin-top: 3px;
  opacity: 0.7;
}
```

**不要改：** tool role 渲染（ToolResultCard）在 ChatStream.tsx 里，不在 ChatBubble.tsx；error role 委托逻辑不动。
  </action>

  <verify>
    <automated>grep -c "bubble-user\|bubble-ai\|msg-time" /Users/wb.chen/Documents/Project/Aster/src/components/ChatBubble.tsx</automated>
    <automated>grep -c "formatTime" /Users/wb.chen/Documents/Project/Aster/src/components/ChatBubble.tsx</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit --skipLibCheck 2>&1 | grep "ChatBubble" | head -5</automated>
    <automated>npm test -- --run 2>&1 | tail -5</automated>
  </verify>

  <done>
    - `grep -c "bubble-user|bubble-ai|msg-time" ChatBubble.tsx` ≥ 3
    - `grep -c "formatTime" ChatBubble.tsx` ≥ 1
    - TypeScript 无 ChatBubble.tsx 相关错误
    - test suite 通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ChatStream empty-state + ToolResultCard + give-up 红卡 + ErrorBubble 重皮</name>
  <files>
    src/components/ChatStream.tsx,
    src/components/ErrorBubble.tsx,
    src/components/ChatStream.test.tsx
  </files>

  <read_first>
    1. src/components/ChatStream.tsx（当前全文，了解 empty-state、ToolResultCard、give-up 卡的当前结构）
    2. src/components/ErrorBubble.tsx（当前全文，了解 ERROR_UI_MAP 逻辑）
    3. src/components/ChatStream.test.tsx（当前全文，了解现有 EMPTY-01 和 ERROR-01 相关断言）
    4. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-PATTERNS.md §ChatStream.tsx + §ErrorBubble.tsx（目标形态完整对照）
    5. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-VALIDATION.md §Wave 0 Requirements（EMPTY-01/ERROR-01 测试要求）
  </read_first>

  <action>
分三部分修改：

**A. ChatStream.tsx — empty-state（D-03）：**

保留 messages 数组、滚动逻辑、onSettings prop 等所有功能不变。

1. 找到 empty-state 渲染区块（当前含 `aster-empty`、`aster-empty__glow`、`aster-chips` 等）：
   - 删除：`aster-empty__glow` span
   - 删除：`aster-chips` / `aster-chip` / usageExamples 相关代码（D-03 推 Phase 6）
   - 改根容器 className：`aster-empty` → `empty`
   - 改 logo 包装：`aster-empty__logo-wrap` + `aster-empty__logo` → `empty-mark`（class 上加 pulse 动画）
   - 改标题文案：改为 `<Trans>从你正在做的东西开始</Trans>`
   - 改副文案：改为 `<Trans>选中文档里的内容，告诉 Aster 你想做什么。</Trans>`

   目标结构：
   ```tsx
   <div className="empty">
     <div className="empty-mark">
       <img src={logo} alt="Aster" style={{ width: 32, height: 32 }} />
     </div>
     <h3><Trans>从你正在做的东西开始</Trans></h3>
     <p><Trans>选中文档里的内容，告诉 Aster 你想做什么。</Trans></p>
     {/* D-03：不渲染 suggestion chips，等 Phase 6 */}
   </div>
   ```

2. 改外层滚动区容器 className：`aster-chat` → `chat-scroll`

若 styles.css 无以下规则，追加到末尾：
```css
/* === ChatStream empty state === */
.chat-scroll {
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  min-height: 0;
}
.empty {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 24px 24px 32px;
  gap: 14px;
  text-align: center;
}
.empty-mark {
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: aster-pulse 4s ease-in-out infinite;
}
@keyframes aster-pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.06); }
}
.empty h3 {
  font-size: var(--fs-16);
  font-weight: 600;
  color: var(--text);
  margin: 0;
}
.empty p {
  font-size: var(--fs-13);
  color: var(--text-2);
  line-height: 1.6;
  margin: 0;
  max-width: 240px;
}
```

**B. ChatStream.tsx — ToolResultCard 折叠范式（D-05）：**

找到 ToolResultCard 组件（或内联渲染块）里用 `▸`/`▾` 字符的 chevron：
- 删除 `▸`/`▾` 字符方案
- import ChevronDownIcon（已在 icons.tsx Plan 01 添加）
- 改 header button className：`aster-tool-card__header` → `wb-action-head`
- 改内容区 className：`aster-tool-card__body` → `wb-action-body`
- chevron 改为：`<ChevronDownIcon size={11} className={expanded ? 'is-up' : ''} />`
- label 改为：`<span className="wb-action-target">{showLabel}</span>`

若 styles.css 无以下规则，追加到末尾：
```css
/* === ToolResultCard writeback-action 折叠范式 D-05 === */
.wb-action-head {
  display: flex;
  align-items: center;
  gap: 6px;
  width: 100%;
  background: transparent;
  border: 0;
  padding: 6px 10px;
  cursor: pointer;
  border-radius: var(--radius-2);
  color: var(--text-2);
  font-size: var(--fs-12);
  text-align: left;
}
.wb-action-head:hover { background: var(--surface-2); }
.wb-action-head svg.is-up { transform: rotate(180deg); }
.wb-action-head .wb-action-target {
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.wb-action-body {
  padding: 6px 10px 8px;
  font-size: var(--fs-12);
  color: var(--text-2);
}
```

**C. ChatStream.tsx — give-up 红卡（D-06, CIRCUIT_OPEN）：**

找到现有 `aster-tool-card--gave-up` 相关渲染块，替换为 err-bubble 范式：
```tsx
// CIRCUIT_OPEN give-up 红卡 — 套 err-bubble 视觉范式（D-06）
// 无撤销按钮（诚实禁用，undo 是 Phase 5）
return (
  <div className="msg msg-ai">
    <div className="err-bubble">
      <div className="head">
        <AlertIcon size={13} />
        <span className="code">CIRCUIT_OPEN</span>
        <span><Trans>Aster 试了几次都没成功</Trans></span>
      </div>
      <div className="body-text">
        {/* 当前渲染 count + toolName + suggestion 文案，保持原有内容，只改容器样式 */}
      </div>
      <div className="cta-row">
        <button className="btn btn-primary btn-sm" onClick={onRetry}>
          <RetryIcon size={12} /><Trans>重新试试</Trans>
        </button>
        {/* 无撤销按钮——D-06 诚实禁用，undo 是 Phase 5 */}
      </div>
    </div>
  </div>
);
```

**D. ErrorBubble.tsx 重皮（D-06）：**

保留所有现有 ERROR_UI_MAP 逻辑、8 类错误映射、props 不变。只替换 JSX 结构和 className：

旧：`aster-error-bubble` + `aster-error-bubble__icon-row` + `aster-error-bubble__cta-row`

新：
```tsx
return (
  <div className="msg msg-ai">
    <div className="err-bubble">
      <div className="head">
        <AlertIcon size={13} />
        <span className="code">{message.errorCode ?? 'ERROR'}</span>
      </div>
      <div className="reason">{ui.reason}</div>
      {ui.ctaType !== 'none' && (
        <div className="cta-row">
          {ui.ctaType === 'settings' && (
            <span className="cta" role="button" tabIndex={0}
              onClick={() => onSettings?.(ui.anchor)}
              onKeyDown={(e) => e.key === 'Enter' && onSettings?.(ui.anchor)}
            >
              {ui.cta}
            </span>
          )}
          {ui.ctaType === 'action' && retryPrompt != null && (
            <span className="cta" role="button" tabIndex={0}
              onClick={onRetry}
              onKeyDown={(e) => e.key === 'Enter' && onRetry?.()}
            >
              <RetryIcon size={12} />{ui.cta}
            </span>
          )}
        </div>
      )}
    </div>
  </div>
);
```

若 styles.css 无 `.err-bubble` 规则，追加到末尾：
```css
/* === ErrorBubble — err-bubble 新形态 D-06 === */
.err-bubble {
  padding: 9px 12px 9px 14px;
  border-radius: var(--radius-3);
  border-bottom-left-radius: var(--radius-1);
  background: var(--bubble-ai-bg);
  box-shadow: inset 3px 0 0 var(--error);
  max-width: 88%;
}
.err-bubble .head {
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--error);
  font-weight: 600;
  font-size: var(--fs-12);
  margin-bottom: 4px;
}
.err-bubble .head .code {
  font-family: var(--font-mono);
  font-size: var(--fs-11);
  padding: 1px 5px;
  background: rgba(0, 0, 0, 0.06);
  border-radius: var(--radius-1);
}
.err-bubble .reason {
  font-size: var(--fs-13);
  color: var(--text);
  line-height: 1.5;
  margin-bottom: 6px;
}
.err-bubble .cta-row { display: flex; gap: 8px; flex-wrap: wrap; }
.err-bubble .cta {
  color: var(--error);
  text-decoration: underline;
  cursor: pointer;
  font-size: var(--fs-13);
  display: inline-flex;
  align-items: center;
  gap: 4px;
}
.err-bubble .cta:hover { opacity: 0.8; }
```

**E. ChatStream.test.tsx 更新（EMPTY-01/ERROR-01）：**

1. EMPTY-01：删除/注释掉断言 `aster-chips` / `aster-chip` / suggestion chips 存在的测试；添加断言「empty-mark 元素存在」「无 .aster-chips 或 .aster-chip」「h3 文案含「从你正在做」」
2. ERROR-01：添加 ErrorBubble snapshot 断言（或 className 断言）：err-bubble 元素存在 + `.code` span 存在（代号显示）

**所有新增中文字符串必须用 `<Trans>` 包裹（lingui，I18N-01）。**
  </action>

  <verify>
    <automated>grep -c "empty-mark\|aster-pulse" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx</automated>
    <automated>grep -c "aster-chips\|aster-chip\|usageExamples" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx</automated>
    <automated>grep -c "wb-action-head\|ChevronDownIcon" /Users/wb.chen/Documents/Project/Aster/src/components/ChatStream.tsx</automated>
    <automated>grep -c "err-bubble" /Users/wb.chen/Documents/Project/Aster/src/components/ErrorBubble.tsx</automated>
    <automated>grep -c "\.code\b" /Users/wb.chen/Documents/Project/Aster/src/components/ErrorBubble.tsx</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run --reporter=verbose 2>&1 | grep -E "ChatStream|ErrorBubble|PASS|FAIL" | head -15</automated>
  </verify>

  <done>
    - `grep -c "empty-mark|aster-pulse" ChatStream.tsx` ≥ 2
    - `grep -c "aster-chips|aster-chip|usageExamples" ChatStream.tsx` = 0（D-03 清理）
    - `grep -c "wb-action-head|ChevronDownIcon" ChatStream.tsx` ≥ 2（D-05 折叠范式）
    - `grep -c "err-bubble" ErrorBubble.tsx` ≥ 1（D-06 新形态）
    - `grep -c ".code" ErrorBubble.tsx` ≥ 1（mono 代号）
    - ChatStream.test.tsx 和 ErrorBubble 相关测试绿
    - `npm test -- --run` 全通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | 本 plan 为纯视觉迁移，无新数据流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04-01 | — | ChatBubble / ChatStream / ErrorBubble | accept | 纯 CSS className + JSX 结构替换，无数据流变化。lingui <Trans> 保留，无裸字符串注入。ErrorBubble 保留 ERROR_UI_MAP 映射逻辑，不接受未经验证的 errorCode 输出（errorCode 来自 src/errors/index.ts 枚举，不是用户输入）。give-up 红卡无假撤销按钮（D-06），诚实禁用不造假 UI。 |
</threat_model>

<verification>
Plan 04 完成条件：
1. `grep -c "empty-mark" src/components/ChatStream.tsx` ≥ 1（logo pulse 壳）
2. `grep -c "aster-chips|usageExamples" src/components/ChatStream.tsx` = 0（D-03 清）
3. `grep -c "wb-action-head" src/components/ChatStream.tsx` ≥ 1（D-05 折叠范式）
4. `grep -c "err-bubble" src/components/ErrorBubble.tsx` ≥ 1（D-06 新形态）
5. `grep -c "bubble-user" src/components/ChatBubble.tsx` ≥ 1
6. `npm test -- --run` 全通过（EMPTY-01/ERROR-01 绿）
7. 无裸中文字符串进 JSX（lingui）
</verification>

<success_criteria>
- ChatBubble：teal user 气泡 + 冷灰 AI 气泡 + msg-time 时间戳
- ChatStream：empty-state 只有壳（pulse logo + 标题 + 副文案，无 chips）；ToolResultCard 用 ChevronDownIcon + wb-action-head 范式；give-up 红卡套 err-bubble，无假撤销
- ErrorBubble：inset 3px 红色 stripe + .code mono 代号 + .cta 下划线
- ChatStream.test.tsx 测试更新通过
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-04-SUMMARY.md`
</output>

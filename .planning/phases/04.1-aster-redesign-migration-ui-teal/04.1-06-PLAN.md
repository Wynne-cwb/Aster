---
phase: "04.1"
plan: "06"
type: execute
wave: 4
depends_on:
  - "04.1-03"
  - "04.1-04"
  - "04.1-05"
files_modified:
  - src/styles.css
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - BUNDLE-01
  - I18N-01

must_haves:
  truths:
    - "styles.css 旧 className 规则（.aster-shell/.aster-topbar/.aster-chat/.aster-inputbar/.aster-agent-bar/.aster-error-bubble/.aster-empty/.aster-chips 等）已被新 className 规则替代或删除"
    - "npm run build 成功，bundle JS gzip ≤ 70KB（NFR-02 不回归）"
    - "npm test -- --run 全通过（无因 CSS className 丢失导致测试失败）"
    - "npm run extract 无新的未标记中文字符串（I18N-01 lingui 完整性）"
  artifacts:
    - path: "src/styles.css"
      provides: "清理后的完整 teal 克制 CSS（无旧 className 残余）"
  key_links:
    - from: "src/styles.css"
      to: "所有组件"
      via: "新 className 体系（pane / agent-bar / bubble / err-bubble / modal 等）"
      pattern: "--accent: #009887"
---

<objective>
Wave 4 收尾：styles.css 完整性扫描 + 旧 className 清理 + 最终 bundle/i18n 验证。

Purpose: Plan 03/04/05 各自在 styles.css 末尾追加了新 CSS 规则，但旧 CSS 规则（.aster-shell / .aster-topbar / .aster-agent-bar / .aster-error-bubble 等）仍在文件中悬空。本 plan 清理这些无引用的旧规则，并做一次完整的 bundle + i18n 门禁验证，确认所有合并在一起时 NFR-02 不回归。

Output: styles.css 最终版本（无旧 className 悬空）；build 通过；i18n 完整。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-CONTEXT.md
@.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: styles.css 旧 className 清理 + 完整性扫描</name>
  <files>src/styles.css</files>

  <read_first>
    1. src/styles.css（全文）
    2. src/App.tsx + src/components/*.tsx + src/components/Onboarding/*.tsx + src/components/Settings/*.tsx（快速 grep 确认哪些旧 className 还被组件引用，哪些已无引用）
  </read_first>

  <action>
**步骤 A：扫描确认旧 className 是否还有组件引用**

运行以下检查（executor 自行执行）：
```bash
# 检查这些旧 className 是否还在任何 .tsx 文件中被引用
grep -r "aster-shell\|aster-topbar\|aster-chat\|aster-inputbar\|aster-composer\|aster-agent-bar\|aster-error-bubble\|aster-empty\b\|aster-chips\|aster-tool-card\|aster-selection-pill\|aster-settings-overlay\|aster-onboarding\|aster-provider-item\|aster-toggle" src/ --include="*.tsx" --include="*.ts"
```

**步骤 B：根据扫描结果删除 styles.css 中已无引用的旧规则块**

旧 className 体系应删除（若这些已无组件引用）：
- `.aster-shell`（→ `.pane`）
- `.aster-topbar`（→ 已移除）
- `.aster-key-hint`（→ `.pane-banner`）
- `.aster-shell__agent-bar`（→ 移除包装 div）
- `.aster-chat`（→ `.chat-scroll`）
- `.aster-inputbar` / `.aster-inputbar__pill-row` / `.aster-composer` / `.aster-composer__toolbar` / `.aster-field` / `.aster-send`（→ `.inputbar-wrap` / `.inputbar` / `.tools` 等）
- `.aster-agent-bar` / `.aster-agent-bar__step` / `.aster-agent-bar__phase` / `.aster-agent-bar__btn` / `.aster-agent-bar__stall`（→ `.agent-bar` 等）
- `.aster-bubble--user` / `.aster-bubble--assistant` / `.aster-cursor`（→ `.bubble-user` / `.bubble-ai` / `.caret`）
- `.aster-tool-card` / `.aster-tool-card__header` / `.aster-tool-card__label` / `.aster-tool-card__chev` / `.aster-tool-card__body` / `.aster-tool-card--gave-up`（→ `.wb-action-head` / `.wb-action-body`）
- `.aster-error-bubble` / `.aster-error-bubble__icon-row` / `.aster-error-bubble__cta-row`（→ `.err-bubble`）
- `.aster-empty` / `.aster-empty__logo-wrap` / `.aster-empty__logo` / `.aster-empty__title` / `.aster-empty__subtitle` / `.aster-empty__hint`（→ `.empty` / `.empty-mark`）
- `.aster-chips` / `.aster-chip`（D-03 推 Phase 6，当前组件已无引用）
- `.aster-selection-pill` / `.aster-selection-pill__text` / `.aster-selection-pill__eye`（→ `.selpill`）
- `.aster-settings-overlay` / `.aster-settings__header`（→ `.settings-overlay` / `.settings-head`）
- `.aster-onboarding-overlay` / `.aster-onboarding` / `.aster-onboarding__steps` / `.aster-step`（→ `.modal-scrim` / `.modal`）
- `.aster-provider-item` / `.aster-provider-item__info` / `.aster-badge-default` / `.aster-badge-builtin`（→ `.provider-row` / `.badge-accent`）
- `.aster-toggle`（→ `.switch`）
- `.aster-iconbtn`（→ `.btn-icon`）
- `.aster-cost-badge`（Phase 3 已拆逻辑，CSS 残余）

**注意：** 若扫描发现某个旧 className 仍被 tsx 文件引用（说明 Plan 03/04/05 中某组件未完成迁移），先修复组件，再删 CSS。**不要盲删 CSS 导致视觉缺失。**

**步骤 C：检查是否有未引用的旧 var() 引用残留**
```bash
grep -n "var(--brand\|var(--glass\|var(--chip-\|var(--pulse-\|var(--shadow-glass\|var(--glow\|var(--r-sm\|var(--r-md\|var(--r-lg\|var(--r-pill\|var(--sp-[1-6])\|var(--text-1)\|var(--on-brand)" src/styles.css
```
若有残留，逐一替换为对应新 var 名（参考 PATTERNS.md §Token 替换速查表）。

**步骤 D：最终 backdrop-filter 零容忍检查**
```bash
grep -v "^[[:space:]]*//" src/styles.css | grep -c "backdrop-filter"
```
必须为 0。
  </action>

  <verify>
    <automated>grep -r "aster-shell\|aster-topbar\|aster-chat\b\|aster-agent-bar\|aster-error-bubble\|aster-tool-card\|aster-empty\b\|aster-chips\b" /Users/wb.chen/Documents/Project/Aster/src --include="*.tsx" --include="*.ts" | grep -v "//\|PATTERNS\|RESEARCH\|CONTEXT\|VALIDATION\|SUMMARY\|\.md:" | wc -l</automated>
    <automated>grep -v "^[[:space:]]*//" /Users/wb.chen/Documents/Project/Aster/src/styles.css | grep -c "backdrop-filter"</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run build 2>&1 | grep -E "error|✓ built" | tail -5</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && gzip -c dist/assets/*.js | wc -c</automated>
  </verify>

  <done>
    - 旧 className grep 结果 = 0（所有 .aster-* 老类名已从组件 tsx 文件清除）
    - `backdrop-filter` count in styles.css = 0
    - build 通过（no error）
    - gzip bundle ≤ 71680（70KB）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 完整门禁验证（测试 + lingui + 暗色 token 自查）</name>
  <files>src/styles.css</files>

  <read_first>
    1. .planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-VALIDATION.md（验证图谱和自动/手动检查清单）
    2. src/styles.css（确认 dark token 已完整，为 DARK-01 浏览器截图做准备）
  </read_first>

  <action>
**步骤 A：完整测试套件运行**
```bash
npm test -- --run --reporter=verbose
```
所有测试绿。若有因 className 变化导致的失败，在 styles.css 或组件中修复。

**步骤 B：lingui i18n 完整性检查（I18N-01）**
```bash
npm run extract
```
检查控制台输出，是否有新的未标记字符串警告（「Missing」/ uncatalogued）。若有，定位到具体组件，补上 `<Trans>` 包裹。

**步骤 C：dark theme token 自查（DARK-01 准备）**

在浏览器（Claude 浏览器或 localhost）加载 add-in，手动设置 `document.querySelector('#root').dataset.theme = 'dark'`，逐屏检查：
1. 主界面 dark：accent 色是浅 teal（#4FC9B8），不是深色/橙色
2. InputBar dark：inputbar 背景是 #181819（--surface），selpill 背景是 rgba(79,201,184,0.18)（--accent-soft dark）
3. empty-state dark：背景 #0E0E10，文字 #F4F4F5
4. ErrorBubble dark：inset stripe 是 --error #F87171（浅红）

若发现 dark token 错误，修复 styles.css 中的 `[data-theme="dark"]` 区块。

**步骤 D：bundle size 最终记录**
```bash
npm run build
gzip -c dist/assets/*.js | wc -c
```
记录实际大小到 SUMMARY.md（作为 NFR-02 证据）。
  </action>

  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run --reporter=verbose 2>&1 | grep -E "Test Files|Tests:" | tail -5</automated>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run extract 2>&1 | grep -i "missing\|uncatalogued\|warning" | head -5</automated>
    <automated>grep -c "\-\-accent: #009887" /Users/wb.chen/Documents/Project/Aster/src/styles.css</automated>
    <automated>grep -c "\-\-accent: #4FC9B8\|#4fc9b8" /Users/wb.chen/Documents/Project/Aster/src/styles.css</automated>
  </verify>

  <done>
    - `npm test -- --run` 全通过（无失败）
    - `npm run extract` 无新的 missing/uncatalogued 字符串警告
    - `--accent: #009887` 在 styles.css ≥ 1 处（light theme）
    - `--accent: #4FC9B8` 在 styles.css ≥ 1 处（dark theme）
    - build gzip ≤ 70KB
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | 本 plan 为纯 CSS 清理 + 验证，无新代码/逻辑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-06-01 | — | styles.css cleanup | accept | 纯 CSS 规则删除（已无引用的旧类名）。不影响数据流/API Key 路径/Office.js 权限。lingui 提取验证确保无裸字符串注入（I18N-01）。bundle 验证确保 NFR-02 不回归。 |
</threat_model>

<verification>
Plan 06 完成条件：
1. `grep -r "aster-shell|aster-topbar|aster-agent-bar|aster-error-bubble" src/ --include="*.tsx"` = 0 行（旧 className 已从所有组件清除）
2. `grep -c "backdrop-filter" src/styles.css` = 0
3. `npm test -- --run` 全通过
4. `npm run extract` 无新的 missing strings
5. build gzip JS ≤ 70KB
6. `--accent: #009887` 在 styles.css ≥ 1 处（TOKEN-01 最终确认）
</verification>

<success_criteria>
- styles.css 无旧 .aster-* className 悬空规则
- 全套测试绿
- lingui 完整（无裸字符串）
- build bundle ≤ 70KB gzipped
- dark theme token 自查通过
</success_criteria>

<output>
完成后创建 `.planning/phases/04.1-aster-redesign-migration-ui-teal/04.1-06-SUMMARY.md`
</output>

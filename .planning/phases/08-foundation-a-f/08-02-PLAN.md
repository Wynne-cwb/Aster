---
phase: 08-foundation-a-f
plan: "02"
type: execute
wave: 1
depends_on:
  - "08-01"
files_modified:
  - src/agent/system-prompt.ts
  - src/agent/system-prompt.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - PROMPT-01
  - NFR-07
  - NFR-08

must_haves:
  truths:
    - "PPT 宿主 system prompt 含断言式标题指导（关键词：断言式）+ ≤5 点/页 + 自查约束（没自查不许说做完了）"
    - "Excel 宿主 system prompt 含公式优先指导（关键词：公式就不填死值）+ 成品格式化提示"
    - "Word 宿主 system prompt 含润色边界（保留原意只改语言）+ 宪法式自查约束"
    - "system-prompt.test.ts domain 关键词断言全绿（npm test -- --run system-prompt 退出 0）"
    - "buildSystemPrompt 签名扩展为 opts?: { userPrefs?: string }，向后兼容（loop.ts 旧调用无需改）"
    - "CONTRACT.md 合约表存在，每宿主工具清单 + undo 分类齐全"
    - "system prompt 长度软门测试通过（toBeLessThan(4000) 断言绿）"
  artifacts:
    - path: "src/agent/system-prompt.ts"
      provides: "三宿主深化 domain segment + buildSystemPrompt opts 签名"
      exports: ["buildSystemPrompt"]
      contains: "断言式"
    - path: ".planning/phases/08-foundation-a-f/CONTRACT.md"
      provides: "能力合约表文档（Phase 9/10/11 工具清单 + undo 分类）"
  key_links:
    - from: "src/agent/loop.ts"
      to: "src/agent/system-prompt.ts"
      via: "buildSystemPrompt(host) 调用（向后兼容，不需改 loop.ts）"
      pattern: "buildSystemPrompt"
    - from: "src/agent/system-prompt.ts"
      to: "buildPrefBlock"
      via: "opts?.userPrefs 注入偏好块（Plan 03 接线后启用）"
      pattern: "buildPrefBlock"
---

<objective>
能力合约 + system-prompt 深化：产出 Phase 9/10/11 的地基合约表，并把三宿主 domain segment 深化到"商业可用成品"水准。

Purpose: D-04 生成质量 >> 成本——高价值领域指导实打实写入 getDomainSegment，不凑长度也不偷工；CONTRACT.md 给后续 B 工具开发提供明确设计约束。
Output: CONTRACT.md（人读合约表）+ system-prompt.ts 深化 + 签名扩展 + 测试全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-foundation-a-f/08-RESEARCH.md
@.planning/phases/08-foundation-a-f/08-PATTERNS.md
@.planning/phases/08-foundation-a-f/08-01-SUMMARY.md

<interfaces>
<!-- 从 src/agent/system-prompt.ts 提取，executor 直接使用 -->

当前签名（L79）：
```typescript
export function buildSystemPrompt(host: HostKey): string
```

扩展目标（向后兼容）：
```typescript
export function buildSystemPrompt(
  host: HostKey,
  opts?: { userPrefs?: string }
): string
```

内部函数（L31/L48）：
```typescript
function getSharedBase(today: string, clock: string, weekday: string, hostLabel: string): string
function getDomainSegment(host: HostKey): string  // switch-case，L48-77
```

HostKey 类型（L15）：
```typescript
type HostKey = 'word' | 'excel' | 'ppt';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: system-prompt.ts — getDomainSegment 三宿主深化 + buildSystemPrompt 签名扩展</name>
  <files>src/agent/system-prompt.ts</files>

  <read_first>
    - src/agent/system-prompt.ts — 当前文件全文（L1-88）
    - .planning/phases/08-foundation-a-f/08-RESEARCH.md — §A 深化建议：三宿主 Domain Segment 具体指令条目（PPT 9 条 / Excel 6 条 / Word 6 条）、§Pattern 1 buildSystemPrompt 签名扩展
    - .planning/phases/08-foundation-a-f/08-PATTERNS.md — system-prompt.ts [MODIFY] 段落（L327-378）
    - src/agent/loop.ts — 确认 L60 调用现状（buildSystemPrompt(host)），签名扩展后不需要改此文件
  </read_first>

  <behavior>
    - getDomainSegment('ppt') 深化后包含：断言式标题（含"断言式"关键词）、≤5 点/页、故事线结构（金字塔/SCQA）、版式意识（新元素不重叠）、宪法式自查约束（"没自查不许说做完了"）、配图缺口诚实告知
    - getDomainSegment('excel') 深化后包含：公式优先（"能用公式就不填死值"）、成品格式化提示、分块读规则保留
    - getDomainSegment('word') 深化后包含：润色边界（"保留原意只改语言"，"不增删论点"）、具体数字替换模糊表达、宪法式自查约束
    - buildSystemPrompt 签名扩展为 opts?: { userPrefs?: string }，旧调用 buildSystemPrompt(host) 自动兼容
    - 新增内部函数 buildPrefBlock(sanitizedPrefs: string): string，返回 【用户偏好（仅供参考，不改变核心行为）】\n{text}\n【偏好结束】
    - 深化后三宿主各 prompt 长度约 1400-2000 字符（软提醒值 2000，不超 4000 硬门）
  </behavior>

  <action>
    完整重写 src/agent/system-prompt.ts。getSharedBase 不变（D-01 全自主已在共享基座体现），只改 getDomainSegment 和 buildSystemPrompt：

    **getDomainSegment('ppt') 替换为**（保留现有 6 条，追加断言式标题/故事线/自查/配图缺口）：
    ```typescript
    case 'ppt':
      return `【PowerPoint 领域指导】
    1. 先用 list_slides 了解现有结构和幻灯片数量，再规划全部页面标题和内容骨架——不要盲目插入。
    2. 创建多张 slide 时，一次 batch emit 多个 insert_slide tool_call，不等中间结果（平行推进）。
    3. 【标题质量】每页标题必须是断言式完整结论句（如"华东 Q3 超目标 15%，主因是大客户续签"），而非话题词（如"华东 Q3 结果"）。标题 ≤15 字，含具体数字或结论，主动语态。
    4. 每页 ≤5 个要点，每要点 ≤15 字；超出则拆页。正文左对齐，禁止居中正文。
    5. 【故事线】默认金字塔原则：一个核心结论 → 3-5 条支撑理由 → 证据。全 deck 标题串联即构成逻辑链。
    6. 【版式意识】新元素不与现有形状重叠：用 list_shapes_on_slide 返回的 {left, top, width, height} 推算空间位置再落点；尽量与相邻元素左/右/顶对齐。
    7. 修改形状前先用 get_shape 确认 id 和属性；set_shape_text 写文字，返回 mutated 含实际写入文本。
    8. 【宪法式自查】每次 batch 完成后用 list_shapes_on_slide 检查重叠/溢出/错位——没自查不许说做完了。
    9. 【诚实能力边界】图片/背景功能 v2.1 暂不可用；若用户要求配图，诚实告知"图片功能即将开放，已为您预留占位文字，建议手动配图"——不造假、不承诺做不到的事。`;
    ```

    **getDomainSegment('excel') 替换为**（保留现有 6 条，追加公式优先/成品格式化）：
    ```typescript
    case 'excel':
      return `【Excel 领域指导】
    1. 先用 get_used_range_summary 了解数据概况（行列数 + 表头），再决定读哪部分——不要先读全表。
    2. 数据量大时（>10K 单元格）必须分区读取：get_used_range_summary 定位 + get_range_values 按块读，禁止一次读全表（防 OOM）。
    3. 【公式优先】能用公式就不填死值：求和用 =SUM()，百分比用 =B2/B$1，不把计算结果直接 hardcode。公式能追踪数据变化，hardcode 值不能。公式用 A1 引用（如 =SUMIF(A:A,"华东",B:B)），不用中文列名。
    4. insert_chart 需先确认数据范围地址，再将 dataRange 传给 tool；插入后记录返回的 chartName 用于 inverse。
    5. 【成品格式化】完成数据操作后，主动提示用户：建议自适应列宽、粗体表头、条件格式上色关键数字（v2.1 Phase 10 工具就绪后可一键完成）——让成品有"可交付"质感。
    6. 分析完成后用 set_cell 把三句话洞察写到空白单元格（如 G1:G3），不要只在 chat 里口头说结果。`;
    ```

    **getDomainSegment('word') 替换为**（保留现有 6 条，追加润色边界/自查约束）：
    ```typescript
    case 'word':
      return `【Word 领域指导】
    1. 先用 get_document_outline 了解文档结构，get_paragraph_count 了解规模，再规划操作路径。
    2. 润色长文时分批处理：用 get_paragraph_at 逐段读取，replace_paragraph 逐段替换，避免一次读全文超 context 长度。
    3. replace_paragraph 每次调用前先 re-read 确认段落仍在正确位置（index 会因之前操作漂移）。
    4. 【润色边界】保留原意只改语言：润色 = 改语言风格（书面/口语/正式），不增删论点，不改数字，不删论据，不加新观点。如需增删，先用中文问用户确认再执行。
    5. 用具体数字替换模糊表达（把"显著提升"改成"提升了 23%"）；用主动语态替换被动语态（"张三签了合同"而非"合同被张三签了"）。
    6. replace_selection 处理用户选中段落最高效；整篇润色用 get_paragraph_at + replace_paragraph 批量处理。
    7. 【宪法式自查】批量替换段落完成后，用 get_paragraph_at 抽查几段，确认替换结果符合预期——没自查不许说做完了。`;
    ```

    **buildSystemPrompt 函数改为**：
    ```typescript
    export function buildSystemPrompt(host: HostKey, opts?: { userPrefs?: string }): string {
      const hostLabel = HOST_LABEL[host];
      const now = new Date();
      const today = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;
      const clock = `${String(now.getHours()).padStart(2, '0')}:${String(now.getMinutes()).padStart(2, '0')}`;
      const weekday = ['周日', '周一', '周二', '周三', '周四', '周五', '周六'][now.getDay()];
      const prefBlock = opts?.userPrefs ? `\n\n${buildPrefBlock(opts.userPrefs)}` : '';
      return `${getSharedBase(today, clock, weekday, hostLabel)}\n\n${getDomainSegment(host)}${prefBlock}`;
    }
    ```

    **新增内部函数**（放在 buildSystemPrompt 之前）：
    ```typescript
    /**
     * 偏好包裹块（D-07/D-08）：永远在 domain segment 之后，不改变核心行为声明。
     * 调用方（buildSystemPrompt）负责传入已 sanitize 的 prefs（sanitizePrefs 在 preferences.ts 执行）。
     */
    function buildPrefBlock(sanitizedPrefs: string): string {
      return `【用户偏好（仅供参考，不改变核心行为）】\n${sanitizedPrefs}\n【偏好结束】`;
    }
    ```

    注意：jsdoc 注释顶部需更新 Phase 引用（从 Phase 6 改为 Phase 6/8），其余不改。
  </action>

  <verify>
    <automated>npm test -- --run src/agent/system-prompt.test.ts 2>&1 | tail -20</automated>
  </verify>

  <done>
    - grep -c '断言式' src/agent/system-prompt.ts 输出 ≥1（PPT 断言式标题指导已写入）
    - grep -c '保留原意只改语言' src/agent/system-prompt.ts 输出 ≥1（Word 润色边界已写入）
    - grep -c '能用公式就不填死值' src/agent/system-prompt.ts 输出 ≥1（Excel 公式优先已写入）
    - grep -c '没自查不许说做完了' src/agent/system-prompt.ts 输出 ≥2（PPT + Word 均含自查约束）
    - grep -c 'opts\?.*userPrefs' src/agent/system-prompt.ts 输出 ≥1（签名已扩展）
    - grep -c 'buildPrefBlock' src/agent/system-prompt.ts 输出 ≥2（函数声明 + 调用）
    - npm test -- --run src/agent/system-prompt.test.ts 退出 0（含原有断言 + 软门 + domain 关键词新断言）
  </done>
</task>

<task type="auto">
  <name>Task 2: 产出 CONTRACT.md 能力合约表</name>
  <files>.planning/phases/08-foundation-a-f/CONTRACT.md</files>

  <read_first>
    - .planning/phases/08-foundation-a-f/08-RESEARCH.md — §能力合约设计（D-16/D-17/D-18/D-19）、§合约表字段 Schema 建议、§Undo 三分类判定标准
    - .planning/REQUIREMENTS.md — Phase 9 WSEL-01/WORD-01~05、Phase 10 EXCEL-01~10/PPT-01~08（确认工具数量）
    - src/agent/contract.test.ts — Wave 0 已建立的 CONTRACT 常量（与此文档保持一致）
    - .planning/STATE.md — D-19 已锁结论（delete_shape/delete_slide = noop+gate，merge_cells 等 defer v2.2）
  </read_first>

  <action>
    创建 .planning/phases/08-foundation-a-f/CONTRACT.md，内容如下：

    ---
    # Aster v2.1 能力合约表

    **Phase:** 8 定义地基（Phase 9/10/11 实现时逐行更新 status + integrationTest）
    **D-16/D-17：** 人读合约表 + contract.test.ts CI 守门双保险
    **D-18：** 参数化合并设计原则（工具更少更清晰 → AI 选工具更准）；不设 toolDefs token CI gate
    **D-19：** B 工具裁剪沿用已锁结论：merge_cells/create_pivot_table → v2.2；delete_worksheet 不做；delete_shape/delete_slide = noop+gate

    ## Undo 三分类说明

    | 分类 | 判定标准 | 实现模式 |
    |------|----------|---------|
    | 简单逆向 | 写前可读原值；写后可精确还原 | before-image + restore_* adapter；reverse.args = Record 对象（非位置参） |
    | 快照式 | 批量覆盖原数据（sort/find_replace）；必须先 readXxxSnapshot | 写前 adapter.readSnapshot() → 存 reverse.args.snapshot；executeReverse restore_xxx_snapshot |
    | noop+gate | 状态无法序列化（delete_shape/delete_slide）或规模超上限 | noop_inverse case 抛 Error；DiffLog 显示"此步无法自动撤销"；warn 不中断 agent |

    ## Phase 9 Word 工具（D + B-Word）

    | tool_name | host | parameters 摘要 | undo_type | reverse_tool | integration_test | phase | status |
    |-----------|------|----------------|-----------|--------------|-----------------|-------|--------|
    | set_word_character_format | word | paragraphIndex, uniqueLocalId?, font{bold,italic,size,color} | 简单逆向 | restore_range_font | false | 9 | planned |
    | set_word_paragraph_format | word | paragraphIndex, uniqueLocalId?, format{lineSpacing,spaceBefore,spaceAfter,alignment} | 简单逆向 | restore_paragraph_format | false | 9 | planned |
    | apply_paragraph_style | word | paragraphIndex, uniqueLocalId?, styleName(Word.BuiltInStyleName) | 简单逆向 | restore_paragraph_style | false | 9 | planned |
    | find_and_replace | word | searchText, replaceText, matchCase?, matchWholeWord? | 快照式 | restore_range_snapshot | false | 9 | planned |
    | insert_table | word | rows, cols, afterParagraphIndex?, content[][]? | 简单逆向 | delete_table_by_marker | false | 9 | planned |

    ## Phase 10 Excel 工具（B-Excel）

    | tool_name | host | parameters 摘要 | undo_type | reverse_tool | integration_test | phase | status |
    |-----------|------|----------------|-----------|--------------|-----------------|-------|--------|
    | format_excel_range | excel | address, numberFormat?, fill{color}?, font{bold,color,size}? | 简单逆向 | restore_range_format | false | 10 | planned |
    | set_column_row_size | excel | target(column/row), indices[], size(number/autoFit) | 简单逆向 | restore_column_row_size | false | 10 | planned |
    | sort_range | excel | address, key{column,ascending}[] | 快照式 | restore_range_values_snapshot | false | 10 | planned |
    | set_auto_filter | excel | address, enabled, criteria?[] | 简单逆向 | restore_auto_filter | false | 10 | planned |
    | excel_find_and_replace | excel | searchText, replaceText, address?, matchCase? | 快照式 | restore_range_values_snapshot | false | 10 | planned |
    | add_conditional_format | excel | address, rule{type,operator,value,format} | 简单逆向 | restore_conditional_format | false | 10 | planned |
    | create_table | excel | address, hasHeaders?, tableName? | 简单逆向 | delete_table_by_name | false | 10 | planned |
    | freeze_panes | excel | freezeRows, freezeColumns | 简单逆向 | restore_freeze_panes | false | 10 | planned |
    | manage_worksheet | excel | operation(rename/add/delete/copy), sheetName, newName? | 快照式 | restore_worksheet_snapshot | false | 10 | planned |
    | set_chart_title | excel | chartName, title | 简单逆向 | restore_chart_title | false | 10 | planned |

    ## Phase 10 PPT 工具（B-PPT）

    | tool_name | host | parameters 摘要 | undo_type | reverse_tool | integration_test | phase | status |
    |-----------|------|----------------|-----------|--------------|-----------------|-------|--------|
    | set_shape_text_font | ppt | slideIndex, shapeId, font{size,bold,italic,color} | 简单逆向 | restore_shape_font | false | 10 | planned |
    | set_shape_text_alignment | ppt | slideIndex, shapeId, alignment(left/center/right/justify) | 简单逆向 | restore_shape_alignment | false | 10 | planned |
    | add_shape | ppt | slideIndex, shapeType, position{left,top,width,height}, text? | 简单逆向 | delete_shape_by_id | false | 10 | planned |
    | delete_shape | ppt | slideIndex, shapeId | noop+gate | noop_inverse | false | 10 | planned |
    | rotate_shape | ppt | slideIndex, shapeId, rotation(degrees) | 简单逆向 | restore_shape_rotation | false | 10 | planned |
    | set_slide_background | ppt | slideIndex, color(hex) | 简单逆向 | restore_slide_background | false | 10 | planned |
    | manage_slides | ppt | operation(delete/reorder), slideIndex, targetIndex? | noop+gate | noop_inverse | false | 10 | planned |
    | copy_slide | ppt | sourceIndex, targetIndex | 简单逆向 | delete_slide_by_index | false | 10 | planned |

    ## 参数化合并设计原则（STRAP，D-18）

    工具数量精简原则：同类操作合并为一个 tool + operation 枚举（非省 token，是"工具更少更清晰 → AI 选工具更准"）。

    | 合并示例 | 合并前 | 合并后 |
    |----------|--------|--------|
    | Excel 格式化 | set_number_format + set_fill_color + set_font_bold | format_excel_range(address, {numberFormat, fill, font}) |
    | PPT 形状字体 | set_font_size + set_font_bold + set_font_color | set_shape_text_font(slideIndex, shapeId, {size, bold, color}) |
    | Excel 工作表管理 | add_worksheet + rename_worksheet | manage_worksheet(operation, sheetName, newName?) |
    | PPT 幻灯片管理 | delete_slide + reorder_slide | manage_slides(operation, slideIndex, targetIndex?) |

    ## v2.2 Defer 清单（D-19 已锁结论）

    以下工具不在 v2.1 范围，Phase 8-11 不实现：

    | tool_name | 原因 |
    |-----------|------|
    | merge_cells | 操作不可逆（unmerge 无法还原原格式），defer v2.2 |
    | create_pivot_table | Spike S3 失败则 defer；API 复杂度高 |
    | remove_duplicates | 快照式 undo 代价大，defer v2.2 |
    | delete_worksheet | noop+gate 行为与 delete_shape 同类，用户明确不做 |

    ## 使用说明（Phase 9/10/11 实现时）

    每个工具实现完成后，更新本表对应行：
    1. `status`: planned → done
    2. `integration_test`: false → true
    3. 同时在 `src/agent/contract.test.ts` 对应行改 `integrationTest: false → true`
    4. 同时在 `src/agent/operationLog.integration.test.ts` 追加守门测试

    **D-17 硬卡：** 步骤 3 + 4 缺一不可，否则 CI 挂。reverse.args 必须是 Record 对象（非位置参），否则真机 undo 全挂（Phase 5 教训）。
  </action>

  <verify>
    <automated>grep -c 'noop+gate' .planning/phases/08-foundation-a-f/CONTRACT.md && grep -c 'planned' .planning/phases/08-foundation-a-f/CONTRACT.md</automated>
  </verify>

  <done>
    - CONTRACT.md 存在（ls -la .planning/phases/08-foundation-a-f/CONTRACT.md 输出文件信息）
    - grep -c '| set_word_character_format' .planning/phases/08-foundation-a-f/CONTRACT.md 输出 1
    - grep -c 'noop+gate' .planning/phases/08-foundation-a-f/CONTRACT.md 输出 ≥2（delete_shape + manage_slides）
    - grep -c 'merge_cells' .planning/phases/08-foundation-a-f/CONTRACT.md 输出 ≥1（defer 清单中）
    - 合约表行数（grep -c '| planned\|| done' CONTRACT.md）≥ 23
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system-prompt.ts → LLM API | 构建好的 prompt 字符串直接发往 DeepSeek API；恶意 userPrefs 若未 sanitize 可造成 prompt injection |
| getDomainSegment → hardcoded strings | 完全静态字符串，无外部输入，无 injection 风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | buildSystemPrompt opts.userPrefs | mitigate | opts.userPrefs 由调用方（loop.ts）从 preferences store 读取已 sanitize 的值（sanitizePrefs 在 Plan 03 preferences.ts 执行）；system-prompt.ts 的 buildPrefBlock 只做格式化，不做二次清洗（职责分离） |
| T-08-02-02 | Information Disclosure | CONTRACT.md 包含工具名 | accept | CONTRACT.md 是 .planning/ 开发文档，不进生产 bundle；工具名已在 toolDefs 中公开，无额外信息泄露 |
</threat_model>

<verification>
```bash
# domain 关键词全绿
npm test -- --run src/agent/system-prompt.test.ts

# 快速 grep 检查
grep -c '断言式' src/agent/system-prompt.ts          # ≥1（PPT）
grep -c '保留原意只改语言' src/agent/system-prompt.ts  # ≥1（Word）
grep -c '能用公式就不填死值' src/agent/system-prompt.ts # ≥1（Excel）
grep -c '没自查不许说做完了' src/agent/system-prompt.ts # ≥2（PPT + Word）
grep -c 'buildPrefBlock' src/agent/system-prompt.ts    # ≥2
grep -c 'toBeLessThan(3000)' src/agent/system-prompt.test.ts # 0（已移除）

# 合约表存在
ls -la .planning/phases/08-foundation-a-f/CONTRACT.md
```
</verification>

<success_criteria>
- npm test -- --run src/agent/system-prompt.test.ts 退出 0（全绿，含 domain 关键词新断言 + 软门）
- buildSystemPrompt('ppt') 含"断言式"关键词（PPT 质量指导落地）
- buildSystemPrompt('excel') 含"公式就不填死值"（Excel 公式优先落地）
- buildSystemPrompt('word') 含"保留原意只改语言"（Word 润色边界落地）
- buildSystemPrompt('word', { userPrefs: '语气正式' }) 含【用户偏好（PREF-01 签名接口就绪）
- CONTRACT.md 中 Phase 9/10 工具合约表行数 ≥ 23，每行含 undo_type 三分类之一
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-a-f/08-02-SUMMARY.md`
</output>

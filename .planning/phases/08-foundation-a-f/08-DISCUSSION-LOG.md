# Phase 8: Foundation + 能力 A + 持久化 F - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-30
**Phase:** 8 - Foundation + 能力 A + 持久化 F
**Areas discussed:** 三宿主 prompt 深化 (A), 用户偏好 (A), 聊天记录持久化 (F), 能力合约 (NFR-08)

---

## 方向选择（首轮 multiSelect）

用户选：三宿主变聪明的方向 / 用户偏好怎么填 / 聊天记录分文档还是全局。
未选："能力合约的硬度"（后续追加轮补讨论）。

---

## 三宿主 prompt 深化（A / PROMPT-01）

**初始 per-host 取向：**

| 宿主 | 选项 | 用户选择 |
|------|------|---------|
| PPT | 版式/对齐意识 / 内容密度纪律 / 先看全局再动手 | **版式/对齐意识** |
| Excel | 公式优于硬写值 / 动手前先摸清数据 / 结果写回单元格 | **公式优于硬写值** |
| Word | 保留原意只改语言 / 多段同名定位准 / 先取大纲再规划 | **保留原意只改语言** |
| 跨宿主坏习惯 | 不验证就说做完了 / 话太多 / 拆太多步 | **不验证就说做完了** |

**关键澄清（用户主动打断）：** 用户自述为 **Office 小白**，不是重度用户。真实期望 = Aster 更**自主**（给主题→自己列大纲/建 PPT/配版式），无论精细或模糊需求都快速出**商业可用成品**。

**派生追问：**

| 问题 | 选项 | 用户选择 |
|------|------|---------|
| 模糊需求 agent 多自主 | 直接全自主做完 / 先给大纲再动手 / 大任务先确认 | **直接全自主做完** |
| v2.1 插不了图的缺口怎么处理 | 老实告知+专注文字 / 留占位文本框 / 不提图 | **老实告知+专注文字** |
| PPT 对齐粒度（本想问，被"让我澄清"打断） | — | （转为下方 skill 调研） |

**Skill 调研（用户指定 todos.md 里 5 个 skill + 邀请检索更多）：**
Claude 轻量侦查了 anthropics/pptx、ppt-creator、excel-analysis、content-writer + 1 轮检索。提炼出 PPT 标题大纲优先/断言式/故事线、Excel 数据优先/成品格式化、Word 大纲优先/具体/保意、跨宿主宪法式约束。

| 问题 | 选项 | 用户选择 + 备注 |
|------|------|---------|
| 深化方向是否符合"自主出商业可用成品" | 符合就按这个 / PPT 再加重 / 让我调优先级 | **符合，但追加重大指示**：不希望限制死 prompt 长度，research 阶段继续检索补充；**"生成质量远远重要于成本及包体积"** |
| system prompt 长度检查怎么改 | 软提醒不卡构建 / 彻底删检查 / 留宽松上限 | **改成软提醒不卡构建** |

**Claude 诚实提醒已给：** prompt 非越长越好，过长稀释指令遵守度（"先想清楚拿掉什么"）。

---

## 用户偏好（A / PREF-01, PREF-02）

| 问题 | 选项 | 用户选择 |
|------|------|---------|
| 偏好框形态 | 一个自由文本框 / 分字段 / 引导开关+自由框 | **一个自由文本框** |
| 范围 | 全局一份通用 / 三宿主各一份 / 全局+宿主补充 | **全局一份通用** |
| 偏好长度（防注入面，非防成本） | ~200 字符 / 放宽 ~500 | **放宽到 ~500 字符** |
| 注入词命中反馈 | 保存时温和提示 / 完全静默不提示 | **完全静默不提示** |

---

## 聊天记录持久化（F / HIST-02, HIST-03, HIST-04）

| 问题 | 选项 | 用户选择 |
|------|------|---------|
| 分文档 vs 全局 | 分文档不行回退全局 / 全局一份 / 看 spike 再定 | **分文档，不行回退全局** |
| 清空范围 | 只清当前文档 / 清空全部 / 给两个选项 | **只清当前文档** |
| 20 轮上限是否放宽（用户"质量>>成本"下追问） | 保持 20 轮 / 放宽 ~40 / 再说 | **保持 20 轮**（认同"多旧对话反而干扰 AI"） |

---

## 能力合约（追加轮 / NFR-08）

追加灰区 multiSelect：用户只选"能力合约长什么样"（偏好框预设、历史存时机+空历史 → Claude 默认）。

Claude 先讲清区分：**省成本的门可软，防数据丢失的门不能软。**

| 问题 | 选项 | 用户选择 |
|------|------|---------|
| 合约形态 | 设计文档+代码轻检查 / 纯设计文档 / 纯代码硬约束 | **设计文档 + 代码轻检查** |
| undo 守门多硬（关乎数据丢失） | 硬卡到底 / 软提醒就行 | **硬卡到底** |
| token 预算门（toolDefs ≤15KB）怎么卡 | 软提醒不卡 / 保持硬卡 15KB / 不检查 | **不检查** |

---

## Claude's Discretion

- D-10 偏好示例预设（点击即填）
- D-14 持久化时机 = 每轮 agent run 跑完即存
- D-15 空历史显示现有杀手场景 chips
- 合约表字段 schema / CI 测试断言形式

## Deferred Ideas

- PPT/Word 配图、生图、幻灯片背景 → v2.2 (MM-03/MM-04) + Phase 10（PPT-08 背景色）；记录用户高优先级意愿
- prompt 长度软提醒的"参考值"数字 → planner 按实测定
- `builtin-model-dropdown.md` todo → reviewed not folded（弱匹配 + 疑似 CARRY-02 已交付 + 非本阶段）

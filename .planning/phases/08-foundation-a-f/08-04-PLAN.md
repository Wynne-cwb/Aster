---
phase: 08-foundation-a-f
plan: "04"
type: execute
wave: 2
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - src/lib/docKey.ts
  - src/lib/docKey.test.ts
  - src/agent/loop-helpers.ts
  - src/agent/loop-helpers.test.ts
  - src/store/chat.ts
  - src/agent/loop.ts
  - src/main.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - NFR-06

must_haves:
  truths:
    - "loop.ts wire messages 含历史截断（truncateTo20Turns）+ 偏好注入（usePreferencesStore.getState().userPrefs）"
    - "chat.ts loadHistory/saveHistory/clearHistory 接 localStorage，docKey 参数化"
    - "docKey.ts getDocKey() 只取 URL pathname 末 80 字符，不含 query string session token"
    - "loop-helpers.ts truncateTo20Turns 超 20 轮截断（整 run 删除），tool 消息不孤立"
    - "main.tsx Office.onReady 内 loadPrefs() + await getDocKey() + loadHistory(docKey) 在 root.render 之前"
    - "npm test -- --run loop-helpers.test.ts 退出 0（truncateTo20Turns 三条测试绿）"
    - "npm test -- --run src/lib/docKey.test.ts 退出 0（GLOBAL_CHAT_KEY 常量 + 描述 skip 测试存在）"
  artifacts:
    - path: "src/lib/docKey.ts"
      provides: "getDocKey() async + GLOBAL_CHAT_KEY 常量 + hashUrl 内部函数"
      exports: ["getDocKey", "GLOBAL_CHAT_KEY"]
      contains: "pathname"
    - path: "src/agent/loop-helpers.ts"
      provides: "truncateTo20Turns 函数（新增导出）"
      exports: ["truncateTo20Turns"]
    - path: "src/store/chat.ts"
      provides: "loadHistory / saveHistory / clearHistory(docKey?) 扩展"
      contains: "loadHistory"
    - path: "src/agent/loop.ts"
      provides: "偏好注入 + 历史截断接线"
      contains: "usePreferencesStore"
    - path: "src/main.tsx"
      provides: "hydrate 三步：loadPrefs + getDocKey + loadHistory"
      contains: "getDocKey"
  key_links:
    - from: "src/agent/loop.ts"
      to: "src/store/preferences.ts"
      via: "usePreferencesStore.getState().userPrefs"
      pattern: "usePreferencesStore"
    - from: "src/agent/loop.ts"
      to: "src/agent/loop-helpers.ts"
      via: "truncateTo20Turns(useChatStore.getState().messages)"
      pattern: "truncateTo20Turns"
    - from: "src/main.tsx"
      to: "src/lib/docKey.ts"
      via: "await getDocKey()"
      pattern: "getDocKey"
    - from: "src/store/chat.ts"
      to: "src/lib/storage.ts"
      via: "storage.set(docKey, payload)"
      pattern: "storage\\.set.*docKey"
---

<objective>
持久化 F 全链路实现：docKey 构建 + 20 轮截断 + chat 历史存取 + loop.ts/main.tsx 接线。

Purpose: HIST-01~04 全部落地——刷新后历史可见、清空无残留、LLM 上下文不超 20 轮、分文档 key 防 session token 泄露。
Output: docKey.ts、loop-helpers.ts（扩展）、chat.ts（扩展）、loop.ts（接线）、main.tsx（hydrate）全部就绪，测试全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-foundation-a-f/08-RESEARCH.md
@.planning/phases/08-foundation-a-f/08-PATTERNS.md
@.planning/phases/08-foundation-a-f/08-02-SUMMARY.md
@.planning/phases/08-foundation-a-f/08-03-SUMMARY.md

<interfaces>
<!-- 从现有文件提取，executor 直接使用 -->

src/agent/loop.ts L59-62（wire messages 构建现状）：
```typescript
const messages: WireMessage[] = [
  { role: 'system', content: buildSystemPrompt(host) },
  { role: 'user', content: userPrompt },
];
```

src/agent/loop.ts L74-76（endRun 位点）：
```typescript
if (toolCallsThisTurn.length === 0) {
  useAgentStore.getState().endRun();
  return;
}
```

src/store/chat.ts Message 接口（L41-61）：
```typescript
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'tool' | 'error';
  content: string;
  isStreaming?: boolean;
  ts?: number;
  // ... 其余字段
}
```

src/store/chat.ts clearHistory 现状（L157-160）：
```typescript
clearHistory() {
  useAgentStore.getState().abort('user');
  set({ messages: [] });
},
```

src/main.tsx Office.onReady 结构（L47-85）：
```typescript
Office.onReady(async (info) => {
  const adapter = await createAdapter(info.host);
  hydrateFromStorage();          // L54 — 追加在此行之后
  // ... initialSelection 取值
  root.render(<App />);          // 最后 render，追加的 hydrate 必须在此之前
});
```

src/lib/storage.ts storage API：
```typescript
export const storage = { get<T>(rawKey: string): T | null; set(rawKey: string, value: unknown): void; remove(rawKey: string): void; };
```

src/errors/index.ts（已有）：
```typescript
export class StorageQuotaError extends Error { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 docKey.ts + 扩展 loop-helpers.ts（truncateTo20Turns）</name>
  <files>src/lib/docKey.ts, src/agent/loop-helpers.ts, src/lib/docKey.test.ts, src/agent/loop-helpers.test.ts</files>

  <read_first>
    - src/agent/loop-helpers.ts — 当前文件前 50 行（确认现有 imports + WireMessage 类型 L23-41）
    - src/store/chat.ts — Message 接口（L41-61）
    - .planning/phases/08-foundation-a-f/08-RESEARCH.md — §Pattern 2 docKey 构建（hashUrl 完整实现）、§Pattern 3 20 轮截断（truncateTo20Turns 算法）、§示例 1 truncateTo20Turns、§Pitfall 1 URL session token、§Pitfall 3 tool 消息孤立
    - .planning/phases/08-foundation-a-f/08-PATTERNS.md — docKey.ts 模式（L110-162）、loop-helpers.ts 追加函数模式（L166-206）
    - src/lib/docKey.test.ts — Wave 0 已建立的测试（确认哪些 describe.skip 需要启用）
    - src/agent/loop-helpers.test.ts — Wave 0 追加的 truncateTo20Turns 测试（确认测试期望）
  </read_first>

  <behavior>
    docKey.ts（新建）：
    - 导出 GLOBAL_CHAT_KEY = 'aster:chat:global'
    - 内部 hashUrl(url: string): string — 取 new URL(url).pathname 末 80 字符（跳过 query/hash），btoa + replace +/_=
    - URL parse 失败时 fallback：取 url.slice(-80) 直接 btoa
    - 导出 hashUrl（供 docKey.test.ts 测试 query string 防泄露断言启用）
    - 导出 getDocKey(): Promise<string> — Office.context.document?.url 同步优先，异步 getFilePropertiesAsync 兜底，都失败返回 GLOBAL_CHAT_KEY

    loop-helpers.ts（追加 truncateTo20Turns）：
    - 追加 import type { Message } from '../store/chat'（若未有则追加）
    - 追加导出函数 truncateTo20Turns(messages: Message[]): Message[]
    - 算法：计算 messages 中 role==='user' 的数量；≤20 不截断；>20 找第 (total-20) 个 user 消息在 messages 中的索引 cutIdx，返回 messages.slice(cutIdx)（整 run 删除：该位置之前的所有消息，包含旧 user/assistant/tool 全部舍弃）
  </behavior>

  <action>
    **Step 1 — 新建 src/lib/docKey.ts**：

    ```typescript
    /**
     * src/lib/docKey.ts — Phase 8 分文档存储 key 构建（HIST-04 / D-11）
     *
     * 安全约束（T-08-tokenleak）：
     * - 禁用 raw 完整 URL 作为 localStorage key（SharePoint URL 含 session token）
     * - 只取 URL pathname 末 80 字符，跳过 query/hash（session token 在此）
     * - btoa 结果替换 +/= 避免 URL 字符冲突
     *
     * Spike S6 结论（manual-only 真机验证）：
     * - Office for Web pathname 稳定 → 启用分文档存储（主路径）
     * - pathname 不稳定或 url 不可用 → 回退 GLOBAL_CHAT_KEY（备用路径）
     */

    /** 分文档存储不可用时的全局 fallback key */
    export const GLOBAL_CHAT_KEY = 'aster:chat:global';

    /**
     * 从 URL 构建稳定 docKey。
     * 取 pathname 末 80 字符 → btoa URL-safe 变体（+ → _ / → - = → 空）。
     *
     * 导出供 docKey.test.ts 测试 query string 防泄露断言。
     */
    export function hashUrl(url: string): string {
      let stablePart: string;
      try {
        const parsed = new URL(url);
        // 取 pathname 末 80 字符：跳过 query/hash（session token 在此）
        stablePart = parsed.pathname.slice(-80);
      } catch {
        // URL parse 失败（如 Windows 本地路径 C:\Users\...）→ 取末 80 字符
        stablePart = url.slice(-80);
      }
      try {
        // btoa 只接受 Latin-1；中文路径需 encodeURIComponent 先转义
        return (
          'aster:chat:' +
          btoa(unescape(encodeURIComponent(stablePart)))
            .replace(/\+/g, '_')
            .replace(/\//g, '-')
            .replace(/=/g, '')
        );
      } catch {
        return GLOBAL_CHAT_KEY;
      }
    }

    /**
     * 异步获取当前文档的稳定 docKey。
     *
     * 优先 Office.context.document.url（同步，Office for Web 通常直接可用）；
     * 不可用时 fallback getFilePropertiesAsync；两者都不可用返回 GLOBAL_CHAT_KEY。
     */
    export async function getDocKey(): Promise<string> {
      // 同步路径（Office for Web 通常直接可用）
      try {
        const syncUrl = Office.context.document?.url;
        if (syncUrl) return hashUrl(syncUrl);
      } catch {
        // Office.context 不可用（测试环境）→ 继续异步路径
      }

      // 异步 fallback（Office Desktop 或 url 属性为 null 时）
      return new Promise((resolve) => {
        try {
          Office.context.document.getFilePropertiesAsync((result) => {
            const url = result?.value?.url;
            resolve(url ? hashUrl(url) : GLOBAL_CHAT_KEY);
          });
        } catch {
          resolve(GLOBAL_CHAT_KEY);
        }
      });
    }
    ```

    **Step 2 — src/lib/docKey.test.ts 更新**

    读取 Wave 0 建立的 docKey.test.ts，将 `describe.skip` 块改为 `describe`（启用 hashUrl 测试），并直接 import hashUrl：

    在文件顶部 import 行追加：
    ```typescript
    import { GLOBAL_CHAT_KEY, hashUrl, getDocKey } from '../lib/docKey';
    ```

    将 `describe.skip('hashUrl — query string session token 不进 key', ...)` 改为 `describe('hashUrl — query string session token 不进 key', ...)`，并把内部测试从 dynamic import 改为直接调用：

    完整更新后的测试文件内容：
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { GLOBAL_CHAT_KEY, hashUrl, getDocKey } from '../lib/docKey';

    describe('docKey — HIST-04 分文档存储 key 构建', () => {
      it('GLOBAL_CHAT_KEY 常量为 aster:chat:global', () => {
        expect(GLOBAL_CHAT_KEY).toBe('aster:chat:global');
      });

      it('GLOBAL_CHAT_KEY 以 aster:chat: 开头', () => {
        expect(GLOBAL_CHAT_KEY).toMatch(/^aster:chat:/);
      });
    });

    describe('hashUrl — query string session token 不进 key', () => {
      it('SharePoint URL 含 query token 时，生成的 key 不含 token', () => {
        const key = hashUrl('https://tenant.sharepoint.com/sites/team/file.pptx?cid=SECRET_TOKEN&odelay=123');
        expect(key).not.toContain('SECRET_TOKEN');
        expect(key).not.toContain('cid=');
        expect(key).toMatch(/^aster:chat:/);
      });

      it('相同文件名不同 query string 生成相同 key（稳定性）', () => {
        const key1 = hashUrl('https://tenant.sharepoint.com/sites/team/file.pptx?cid=TOKEN1');
        const key2 = hashUrl('https://tenant.sharepoint.com/sites/team/file.pptx?cid=TOKEN2');
        expect(key1).toBe(key2);
      });

      it('不同文件名生成不同 key', () => {
        const key1 = hashUrl('https://tenant.sharepoint.com/sites/team/fileA.pptx');
        const key2 = hashUrl('https://tenant.sharepoint.com/sites/team/fileB.pptx');
        expect(key1).not.toBe(key2);
      });

      it('生成的 key 不含原始 URL 中的路径字符串（btoa 变体已编码）', () => {
        const url = 'https://tenant.sharepoint.com/sites/team/重要文件.pptx';
        const key = hashUrl(url);
        expect(key).not.toContain('重要文件');
        expect(key).toMatch(/^aster:chat:/);
      });

      it('空字符串 fallback 到 GLOBAL_CHAT_KEY 或合法 key', () => {
        const key = hashUrl('');
        expect(key).toMatch(/^aster:chat:/);
      });
    });

    describe('getDocKey — Office 环境外 fallback', () => {
      it('getDocKey 函数可调用（非 Office 环境 catch → GLOBAL_CHAT_KEY）', async () => {
        // 非 Office 环境（vitest）：Office 未定义，catch → GLOBAL_CHAT_KEY
        const key = await getDocKey();
        expect(key).toMatch(/^aster:chat:/);
      });
    });
    ```

    **Step 3 — loop-helpers.ts 追加 truncateTo20Turns**

    读取 loop-helpers.ts 当前内容，在文件末尾追加：

    ```typescript
    // ---------------------------------------------------------------------------
    // Phase 8: truncateTo20Turns（HIST-03 / D-13）
    // 导出供 loop.ts 使用（loop-helpers.ts 原为 loop.ts 内部 helper，此函数需跨文件调用）
    // ---------------------------------------------------------------------------

    import type { Message } from '../store/chat';

    /**
     * 将 chatStore 历史消息截断到最近 20 个 user turns。
     *
     * 定义：1 turn = 1 条 user 消息 + 其后所有 assistant/tool 消息（直到下一条 user 消息）。
     * tool 消息不计入轮次，但随其 run 整组删除（防孤立 tool 消息导致 LLM 400 错误）。
     *
     * 算法：
     * 1. 找所有 role==='user' 消息在原数组中的位置
     * 2. 若 ≤20 个 user 消息：不截断，直接返回
     * 3. 若 >20 个 user 消息：找第 (total-20) 个 user 消息的索引 cutIdx，返回 slice(cutIdx)
     *    — 即保留最近 20 个 user turn（含其后的 assistant/tool 消息）
     *    — 早于 cutIdx 的所有消息（包含旧 assistant/tool）全部丢弃
     */
    export function truncateTo20Turns(messages: Message[]): Message[] {
      // 找出所有 user 消息的索引（在原始 messages 数组中）
      const userIndices = messages
        .map((m, i) => ({ i, role: m.role }))
        .filter((x) => x.role === 'user')
        .map((x) => x.i);

      if (userIndices.length <= 20) {
        return messages; // 不截断
      }

      // 保留最近 20 个 user turn：从第 (total-20) 个 user 消息开始
      const cutIdx = userIndices[userIndices.length - 20];
      return messages.slice(cutIdx);
    }
    ```

    注意：loop-helpers.ts 文件顶部可能已有 `import { useChatStore } from '../store/chat'`，追加时检查是否需要合并 `import type { Message }` 为同一行（如 `import { useChatStore, type Message } from '../store/chat'`）。
  </action>

  <verify>
    <automated>npm test -- --run src/lib/docKey.test.ts src/agent/loop-helpers.test.ts 2>&1 | tail -30</automated>
  </verify>

  <done>
    - src/lib/docKey.ts 存在，grep -c 'export function hashUrl' src/lib/docKey.ts 输出 1
    - grep -c 'export async function getDocKey' src/lib/docKey.ts 输出 1
    - grep -c 'pathname' src/lib/docKey.ts 输出 ≥1（pathname 提取逻辑已写）
    - grep -c 'export function truncateTo20Turns' src/agent/loop-helpers.ts 输出 1
    - npm test -- --run src/lib/docKey.test.ts 退出 0（hashUrl 稳定性测试全绿）
    - npm test -- --run src/agent/loop-helpers.test.ts 退出 0（truncateTo20Turns 三条测试绿）
    - BLOCKER #3 守门：loop-helpers.test.ts 第 3 条测试验证截断时 tool 消息随 run 整组删（无孤立 tool）；loop.ts 接线 `.filter((m) => m.role === 'user' || m.role === 'assistant')` 在截断之后执行，双保险确保 wire messages 不含孤立 tool 消息（无前置 assistant tool_call 的 tool 消息）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat.ts 扩展 + loop.ts 接线 + main.tsx hydrate</name>
  <files>src/store/chat.ts, src/agent/loop.ts, src/main.tsx, src/store/chat.test.ts</files>

  <read_first>
    - src/store/chat.ts — 当前全文（ChatState 接口 L67-97，clearHistory L157-160，useChatStore L103-161）
    - src/agent/loop.ts — 当前全文（L1-85，重点 L59-62 wire messages，L74-76 endRun 位点）
    - src/main.tsx — 当前全文（L47-85 Office.onReady async 结构，L54 hydrateFromStorage 位点）
    - .planning/phases/08-foundation-a-f/08-RESEARCH.md — §Pattern 4 聊天持久化（serializeForStorage + saveHistory + QuotaExceeded 丢最旧 20%）、§示例 2 saveHistory with QuotaExceeded fallback
    - .planning/phases/08-foundation-a-f/08-PATTERNS.md — chat.ts 改造模式（L462-505）、loop.ts 改造（L421-458）、main.tsx 改造（L510-546）
    - src/errors/index.ts — 确认 StorageQuotaError 存在（import 用）
  </read_first>

  <behavior>
    chat.ts：
    - ChatState 接口追加 loadHistory(docKey: string): void + saveHistory(docKey: string): void
    - clearHistory 改为 clearHistory(docKey?: string): void（向后兼容，有 docKey 则删 storage）
    - 新增内部函数 serializeForStorage(messages: Message[]): StorableMessage[] — 白名单过滤（只存 role='user'|'assistant'，丢弃 isStreaming=true，每条 content ≤2000 字符）
    - saveHistory 实现：序列化 + storage.set(docKey, {version:1, messages, lastSaved}) + QuotaExceeded 丢最旧 20% 重试
    - loadHistory 实现：storage.get(docKey) → 反序列化 → set({ messages: hydrated })；序列化数据无效则静默不 set

    loop.ts：
    - 在 wire messages 构建之前（L59 前）读取 userPrefs + 历史截断
    - 追加 import：usePreferencesStore from '../store/preferences'，truncateTo20Turns from './loop-helpers'，useChatStore from '../store/chat'
    - wire messages 改为：system + 截断后历史（role=user/assistant 只） + 本轮 user
    - endRun() 之前调用 saveHistory（D-14）：需先拿 docKey → 在 loop.ts 顶层参数传入或从 agentStore 读取。简化方案：loop.ts 接受 docKey 参数（在 runAgent 签名追加 docKey?: string，向后兼容）或在 endRun 处调用 agentStore 的 docKey 状态。
    - **最简方案（避免循环依赖）**：agentStore.endRun() 之前，loop.ts 在 `toolCallsThisTurn.length === 0` 分支直接调用 `useChatStore.getState().saveHistory`。docKey 通过 closure 读取（在 runAgent 开头 `const docKey = await getDocKey()`）。

    main.tsx：
    - 追加 import：getDocKey, usePreferencesStore, useChatStore
    - hydrateFromStorage() 之后追加：loadPrefs() + await getDocKey() + loadHistory(docKey)
    - 顺序保证：hydrateFromStorage → loadPrefs → getDocKey → loadHistory → root.render
  </behavior>

  <action>
    **Step 1 — chat.ts 扩展**

    读取 chat.ts 全文后做以下改动：

    a) 在 import 区追加：
    ```typescript
    import { storage } from '../lib/storage';
    import { StorageQuotaError } from '../errors/index';
    ```

    b) 在 ChatState 接口（L67-97）末尾追加三个方法声明：
    ```typescript
      /** Phase 8 F: 从 localStorage 加载聊天历史（hydrate，main.tsx Office.onReady 内调用）*/
      loadHistory(docKey: string): void;
      /** Phase 8 F: 保存聊天历史到 localStorage（每轮 agent run 完成后调用，D-14）*/
      saveHistory(docKey: string): void;
    ```

    c) clearHistory 签名改为 `clearHistory(docKey?: string): void`（向后兼容）

    d) 在 useChatStore create 内，clearHistory 实现改为：
    ```typescript
    clearHistory(docKey?: string) {
      useAgentStore.getState().abort('user');
      set({ messages: [] });
      if (docKey) {
        storage.remove(docKey); // 只清当前文档，D-12
      }
    },
    ```

    e) 在 clearHistory 之后追加 loadHistory + saveHistory 实现：
    ```typescript
    loadHistory(docKey: string) {
      try {
        const stored = storage.get<{ version: number; messages: StorableMessage[] }>(docKey);
        if (!stored || stored.version !== 1 || !Array.isArray(stored.messages)) return;
        const hydrated: Message[] = stored.messages.map((m) => ({
          id: m.id ?? crypto.randomUUID(),
          role: m.role,
          content: m.content,
          ts: m.ts,
        }));
        set({ messages: hydrated });
      } catch {
        // 反序列化失败静默忽略（JSON 损坏 / 格式不兼容）
      }
    },

    saveHistory(docKey: string) {
      const { messages } = get();
      const serialized = serializeForStorage(messages);
      const payload = { version: 1, messages: serialized, lastSaved: Date.now() };
      try {
        storage.set(docKey, payload);
      } catch (err) {
        if (err instanceof StorageQuotaError) {
          // 丢最旧 20%，重试一次
          const trimmed = serialized.slice(Math.floor(serialized.length * 0.2));
          try {
            storage.set(docKey, { ...payload, messages: trimmed });
          } catch {
            // 二次失败静默，不影响 UI
          }
        }
      }
    },
    ```

    f) 在 useChatStore create 函数体之前（但在 export const useChatStore 行之前），添加类型和 helper：
    ```typescript
    interface StorableMessage {
      id: string;
      role: 'user' | 'assistant';
      content: string;
      ts?: number;
    }

    /** 序列化白名单：只存 user/assistant 文字，丢弃 tool/error/streaming 中间态（PITFALLS §F4）*/
    function serializeForStorage(messages: Message[]): StorableMessage[] {
      return messages
        .filter(
          (m): m is Message & { role: 'user' | 'assistant' } =>
            (m.role === 'user' || m.role === 'assistant') && !m.isStreaming,
        )
        .map((m) => ({
          id: m.id,
          role: m.role,
          content: m.content.slice(0, 2000), // 每条 ≤2000 字符防 quota（D-14）
          ts: m.ts,
        }));
    }
    ```

    **Step 2 — loop.ts 接线**

    读取 loop.ts 全文后：

    a) 追加 import（放在现有 import 块末尾）：
    ```typescript
    import { usePreferencesStore } from '../store/preferences';
    import { truncateTo20Turns } from './loop-helpers';
    import { useChatStore } from '../store/chat';
    import { getDocKey } from '../lib/docKey';
    ```

    b) runAgent 函数体内，在 `const host = adapter.capabilities().host;` 之后，`const messages: WireMessage[] = [` 之前，追加：
    ```typescript
    // Phase 8: docKey（saveHistory 用）+ 偏好 + 历史截断
    const docKey = await getDocKey();
    const userPrefs = usePreferencesStore.getState().userPrefs;
    const historicalMsgs = truncateTo20Turns(useChatStore.getState().messages)
      .filter((m) => m.role === 'user' || m.role === 'assistant');
    ```

    c) wire messages 构建（L59-62）改为：
    ```typescript
    const messages: WireMessage[] = [
      { role: 'system', content: buildSystemPrompt(host, userPrefs ? { userPrefs } : undefined) },
      ...historicalMsgs.map((m) => ({
        role: m.role as 'user' | 'assistant',
        content: m.content,
      })),
      { role: 'user', content: userPrompt },
    ];
    ```

    d) endRun 位点（`toolCallsThisTurn.length === 0` 分支），在 `useAgentStore.getState().endRun();` 之前追加：
    ```typescript
    useChatStore.getState().saveHistory(docKey);
    ```

    注意：loop.ts 有 ≤80 code lines 预算（D-02 注释说明）。追加后检查 code lines 数（jsdoc/import/type/纯括号行不计），若超出则将 docKey 读取抽到 loop-helpers.ts 的 helper 函数。

    **Step 3 — main.tsx hydrate 接线**

    读取 main.tsx 全文后：

    a) 追加 import：
    ```typescript
    import { getDocKey } from './lib/docKey';
    import { usePreferencesStore } from './store/preferences';
    import { useChatStore } from './store/chat';
    ```

    b) 在 `hydrateFromStorage();` 行（L54）之后追加：
    ```typescript
    // Phase 8 A: 偏好 hydrate
    usePreferencesStore.getState().loadPrefs();

    // Phase 8 F: 聊天历史 hydrate（需 async，await getDocKey 后再 loadHistory）
    const docKey = await getDocKey();
    useChatStore.getState().loadHistory(docKey);
    ```

    顺序保证：以上三行必须在 `root.render(...)` 之前执行（main.tsx 已在 async Office.onReady 回调内，可直接 await）。

    **Step 4 — chat.test.ts 扩展（BLOCKER #2 / HIST-01/02 自动化守门）**

    读取 src/store/chat.test.ts（若已存在）或新建，在文件末尾追加以下 describe 块：

    ```typescript
    // ---------------------------------------------------------------------------
    // Phase 8 HIST-01/02 chat 持久化守门（由 08-04 Task 2 实现后 GREEN）
    // ---------------------------------------------------------------------------
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    // storage mock（防止真实 localStorage 依赖）
    vi.mock('../lib/storage', () => ({
      storage: {
        get: vi.fn().mockReturnValue(null),
        set: vi.fn(),
        remove: vi.fn(),
      },
      STORAGE_KEYS: { CHAT_HISTORY_PREFIX: 'aster:chat:' },
    }));

    import { useChatStore } from './chat';

    describe('chat.ts — HIST-01/02 持久化往返与清空', () => {
      beforeEach(() => {
        useChatStore.setState({ messages: [] });
        vi.clearAllMocks();
      });

      it('saveHistory 调用 storage.set，key 含 aster:chat: 前缀', async () => {
        const { storage } = await import('../lib/storage');
        // 手动 set 两条消息
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: 'hello', ts: 1 },
            { id: 'a1', role: 'assistant', content: 'world', ts: 2 },
          ],
        });
        useChatStore.getState().saveHistory('aster:chat:testDoc');
        expect(storage.set).toHaveBeenCalledWith(
          'aster:chat:testDoc',
          expect.objectContaining({ version: 1, messages: expect.any(Array) })
        );
      });

      it('clearHistory 传 docKey 时调用 storage.remove（HIST-02 清空当前文档）', async () => {
        const { storage } = await import('../lib/storage');
        useChatStore.getState().clearHistory('aster:chat:testDoc');
        expect(storage.remove).toHaveBeenCalledWith('aster:chat:testDoc');
        expect(useChatStore.getState().messages).toHaveLength(0);
      });

      it('serializeForStorage 白名单：只存 user|assistant 文字，每条 ≤2000 字符', () => {
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: 'a'.repeat(3000), ts: 1 },
            { id: 'a1', role: 'assistant', content: 'ok', ts: 2 },
            { id: 't1', role: 'tool', content: 'tool_result', ts: 3 },
            { id: 'e1', role: 'error', content: 'err', ts: 4 },
          ],
        });
        useChatStore.getState().saveHistory('aster:chat:testDoc');
        const { storage } = require('../lib/storage');
        const call = (storage.set as ReturnType<typeof vi.fn>).mock.calls[0];
        const payload = call[1] as { messages: Array<{ role: string; content: string }> };
        // 只含 user + assistant，不含 tool/error
        const roles = payload.messages.map((m) => m.role);
        expect(roles).not.toContain('tool');
        expect(roles).not.toContain('error');
        // user 消息内容被截断到 2000 字符
        const userMsg = payload.messages.find((m) => m.role === 'user');
        expect(userMsg?.content.length).toBeLessThanOrEqual(2000);
      });
    });
    ```
  </action>

  <verify>
    <automated>npm test -- --run src/store/chat.test.ts src/agent/loop.test.ts 2>&1 | tail -20 && npm run build 2>&1 | grep -E 'error|Error|✓|✗' | tail -20</automated>
  </verify>

  <done>
    - grep -c 'loadHistory' src/store/chat.ts 输出 ≥2（接口声明 + 实现）
    - grep -c 'saveHistory' src/store/chat.ts 输出 ≥2
    - grep -c 'serializeForStorage' src/store/chat.ts 输出 ≥2
    - grep -c 'usePreferencesStore' src/agent/loop.ts 输出 ≥1
    - grep -c 'truncateTo20Turns' src/agent/loop.ts 输出 ≥1
    - grep -c 'getDocKey' src/main.tsx 输出 ≥1
    - grep -c 'loadHistory' src/main.tsx 输出 ≥1
    - npm run build 无 TypeScript 错误（exit 0）
    - npm test -- --run src/store/chat.test.ts 退出 0（HIST-01/02 三条守门测试绿：saveHistory 往返序列化 / clearHistory 调用 storage.remove / serializeForStorage 白名单只留 user|assistant 且 ≤2000 字符）
    - npm test -- --run 全套通过（含 loop-helpers truncateTo20Turns + docKey hashUrl 测试）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Office.context.document.url → docKey | URL 含 SharePoint session token；hashUrl 只取 pathname 隔离 |
| chat.ts messages → localStorage | 序列化白名单过滤 tool/error/toolResult.data，防大对象导致 QuotaExceeded |
| localStorage → chat.ts loadHistory | 反序列化时验证 version + messages 为 Array，防格式不兼容导致 crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-tokenleak | Information Disclosure | hashUrl — document.url → localStorage key | mitigate | 只取 new URL(url).pathname 末 80 字符；跳过 query/hash（session token 在此）；hashUrl 测试守门（query string 不出现在 key）|
| T-08-quota | Denial of Service | saveHistory storage.set | mitigate | StorageQuotaError catch → 丢最旧 20% 消息重试一次；二次失败静默不影响 UI |
| T-08-deser | Tampering | loadHistory JSON.parse | mitigate | version !== 1 或 messages 非 Array 时静默返回；content 字段被 slice(0,2000) 截断；不执行任何外部内容 |
| T-08-tooldata | Information Disclosure | serializeForStorage | mitigate | 白名单：只存 role=user/assistant；丢弃 toolResult.data（可能含 before-image 大对象）、reverse、postState |
</threat_model>

<verification>
```bash
# 功能验证
npm test -- --run src/lib/docKey.test.ts       # hashUrl query string 防泄露
npm test -- --run src/agent/loop-helpers.test.ts  # truncateTo20Turns 三条测试

# 构建验证（TypeScript strict 检查）
npm run build 2>&1 | tail -5

# 关键文件检查
grep -c 'loadHistory' src/store/chat.ts      # ≥2
grep -c 'saveHistory' src/store/chat.ts      # ≥2
grep -c 'truncateTo20Turns' src/agent/loop.ts # ≥1
grep -c 'usePreferencesStore' src/agent/loop.ts # ≥1
grep -c 'getDocKey' src/main.tsx              # ≥1
grep -c 'pathname' src/lib/docKey.ts          # ≥1（session token 隔离验证）
```
</verification>

<success_criteria>
- npm test -- --run src/lib/docKey.test.ts 退出 0（hashUrl 不含 query token）
- npm test -- --run src/agent/loop-helpers.test.ts 退出 0（21 轮截断到 20 轮）
- npm run build 退出 0（TypeScript strict 无错误）
- serializeForStorage 白名单：grep -c 'role.*tool\|toolResult' 在序列化函数体内输出 0（不序列化 tool 消息）
- docKey.ts hashUrl 取 pathname 而非全 URL（grep -c 'pathname' src/lib/docKey.ts ≥1）
- clearHistory 扩展为可选 docKey 参数（grep -c 'docKey\?' src/store/chat.ts ≥1）
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-a-f/08-04-SUMMARY.md`
</output>

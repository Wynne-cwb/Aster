# Phase 34 — WPS 文字 + 演示 adapter（三宿主滩头堡，WPS-D1 提前量）

> **⚠️ 投机性预写（STATE.md 2026-06-29）**：Phase 30 真机门仍未过；本 phase 把 v2.5 单宿主滩头堡
> 扩到 Word + PPT 两宿主，**越过 v2.5 刻意的「单宿主」边界，进入原 WPS-D1 范围**。
> 用户 2026-06-29 二次授权（「继续推进 word 跟 ppt，可承担真机不通过完全推倒重来的代价」）。
> 产物 = 代码草稿，真机 pending，**不可标 Complete**。

**Requirements:** WPS-D1（部分提前；正式归属后续独立 milestone）
**Depends on:** Phase 31（外壳 code-drafted）/ Phase 32（Excel adapter 模式）/ Phase 33（诚实收口框架）

## 目标

把 Phase 32 的「单宿主滩头堡」复制到 Word + PPT：各实现核心 read + 基础 write + inverse，
对位 Office.js WordAdapter/PptAdapter 被工具复用的同名方法。接缝上方（loop/operationLog/UI/dispatch）零改动。

## 范围（核心最小集，非全量）

- **Word write 5**：append_paragraph / insert_paragraph / replace_paragraph / insert_text_at_cursor / replace_selection
- **Word read 5**：get_document_full_text / get_paragraph_count / get_paragraph_at / get_document_outline / selection_detail
- **PPT write 5**：set_shape_text / insert_slide / add_shape / delete_shape / move_shape
- **PPT read 6**：list_slides / get_slide / list_shapes_on_slide / get_shape / check_slide_layout / selection_detail
- **刻意排除（高风险/未实现，留 WPS-D1）**：Word 格式/样式/表格/批注/页眉页脚/图片；PPT 渐变/原生表/线条/背景色/旋转/对齐/字体/版式建页。

## Tasks

- **34-T1** WPS JSAPI 类型扩展：`wps-jsapi.d.ts` 加 Word（Document/Paragraphs/Paragraph/WpsWordRange/WpsWordSelection）+ PPT（Presentation/Slides/Slide/Shapes/Shape/TextFrame/PptWindow）桌面 VBA 对象模型子集，全程 [真机待验] 标注。
- **34-T2** `WpsWordAdapter`：完整实现（替换 Phase 31 stub），同步 VBA 风格；inverse = deleteParagraphByContent / restoreParagraphAt + readWordParagraph（D-11）。
- **34-T3** `WpsPptAdapter`：完整实现（替换 Phase 31 stub）；inverse = restoreShapeText / deleteShapeById / restoreShapeGeometry / deleteSlideByTitle；**刻意不实现 readPptSlideTitle**（对齐 Office.js PptAdapter，避免 operationLog `ppt_slide` 对象 content 判定缺陷把 insert_slide undo 误判 skipped_manual）。
- **34-T4** 删除 `WpsAdapterStub`（三宿主 adapter 现均直接实现 DocumentAdapter，stub 成死代码）。
- **34-T5** 诚实收口：`tools/index.ts` 加 `WPS_WORD_CORE_TOOLS` / `WPS_PPT_CORE_TOOLS`，buildToolsForHost 三宿主各只暴露已实现工具；更新裁剪守门测试。
- **34-T6** operationLog 集成测试：WpsWordAdapter + WpsPptAdapter 各一份 round-trip 守门（真 adapter × mock 同步 VBA 全局，验 reverse 派发 + Record 签名 + undo-all 逆序）。

## 验证（代码侧 / 真机 pending）

代码侧：tsc 0 / build（dist/assets 多出 WpsWordAdapter + WpsPptAdapter 懒 chunk）/ size gate（主入口零膨胀）/ 全量测试 green（新增 Word 5 + PPT 6 + 裁剪 +1）。
真机侧（纯真机，不可勾）：WPS 文字 + 演示各自多步 agent loop + undo all UAT PASS；VBA gotcha（段落 \r、Shape.Id 稳定性、MsoShapeType/AutoShapeType 枚举、Slides.Add/AddTextbox 签名、选区读取）真机坐实。

## 为何不可标 Complete

Phase 30 两条 make-or-break（CEF/React19、SSE 直连不被拦）仍未真机验；no-go 则含本 phase 在内全部 WPS 代码作废。
本 phase 全部 adapter 方法基于 WPS 桌面 VBA JSAPI **盲写推断**，未在 Windows WPS 真机跑过一次，大概率要修。

---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - index.html
  - lingui.config.ts
  - src/i18n/index.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-08, INSTALL-04]
must_haves:
  truths:
    - "npm install 与 npm run build 在 repo 根目录成功，产出纯静态 dist/"
    - "Vite 配置含 vite-plugin-office-addin 与 Lingui 插件，且 base = /Aster/"
    - "TypeScript strict 模式开启，Office.* 全局类型可用"
    - "Lingui 配置 locales 仅 zh-CN，extract/compile 可运行"
  artifacts:
    - path: "package.json"
      provides: "正式项目依赖栈 + scripts（test/size/extract）"
      contains: "vite-plugin-office-addin"
    - path: "vite.config.ts"
      provides: "Vite 接线：office-addin 插件 + lingui 插件 + 多入口 + base"
      contains: "vite-plugin-office-addin"
    - path: "tsconfig.json"
      provides: "TS strict + office-js 类型"
      contains: "\"strict\": true"
    - path: "lingui.config.ts"
      provides: "Lingui zh-CN scaffold"
      contains: "zh-CN"
    - path: "index.html"
      provides: "Task Pane 入口，CDN Office.js"
      contains: "appsforoffice.microsoft.com/lib/1/hosted/office.js"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "module script entry"
      pattern: "src/main.tsx"
    - from: "vite.config.ts"
      to: "GitHub Pages 子路径"
      via: "base 配置"
      pattern: "base.*Aster"
---

<objective>
搭建 Aster 正式项目脚手架基座：把 Phase 0 已验证的 `spike/bundle-test/` 依赖栈（React19 + TS5.7 strict + Fluent UI v9 + Zustand + react-markdown，实测 ~135KB gzip）提升到 repo 根目录，并补齐 Phase 1 必需的接线依赖（vite-plugin-office-addin、Lingui scaffold、Vitest、size-limit）。本 plan 只搭"地基与工具链"，不写业务代码（adapter/shell 在后续 plan）。

Purpose: 这是 Phase 2-6 所有代码的构建底座。脚手架路径选择已由 D-01 锁定（跳过 Yo Office，直接提升 spike base）。
Output: repo 根目录的 package.json / vite.config.ts / tsconfig.json / index.html / lingui.config.ts / src/i18n/index.ts。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md

<analogs>
<!-- 直接提升的 spike 资产，executor 须读取作为起点 -->
spike/bundle-test/package.json — 已实测 ~135KB gzip 的依赖栈（D-01 直接提升）
spike/bundle-test/vite.config.ts — Vite manualChunks 起点
spike/bundle-test/tsconfig.json — TS strict + office-js 类型（exact 提升）
spike/bundle-test/index.html — CDN Office.js + module entry（exact 提升）
spike/pdfjs-vite-test/README.md — worker/静态资产 `new URL(..., import.meta.url)` 模式（本 plan 不接 pdf.js，但 vite.config 写进守则）
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提升 package.json + tsconfig.json + index.html 到 repo 根</name>
  <read_first>
    - spike/bundle-test/package.json（依赖栈起点）
    - spike/bundle-test/tsconfig.json（exact 提升）
    - spike/bundle-test/index.html（CDN Office.js 模式）
    - .planning/phases/01-foundation/01-PATTERNS.md（package.json/tsconfig/index.html 段）
  </read_first>
  <action>
    在 repo 根目录创建 package.json（FOUND-01/FOUND-02/INSTALL-04）：
    - `name`: "aster"（去掉 -bundle-test）；`private`: true；`type`: "module"；`version`: "0.1.0"
    - `description`: 重写为正式描述，例如 "Aster — Office.js Add-in for Chinese knowledge workers, BYO API Key, no backend."（**不要照抄 spike 的 description，spike 明确写'Phase 1 不复用'）
    - dependencies（直接搬 spike 已验证版本）：
      `"react": "^19.0.0"`, `"react-dom": "^19.0.0"`, `"@fluentui/react-components": "^9.73.0"`, `"@fluentui/react-icons": "^2.0.0"`（in-pane 图标，UI-SPEC 要求）, `"zustand": "^5.0.0"`, `"react-markdown": "^9.0.0"`, `"remark-gfm": "^4.0.0"`, `"@lingui/react": "^5.0.0"`, `"@lingui/macro": "^5.0.0"`
    - devDependencies（spike 已有 + 本阶段新增接线/工具）：
      `"@types/office-js": "latest"`, `"@types/react": "^19.0.0"`, `"@types/react-dom": "^19.0.0"`, `"typescript": "^5.7.0"`, `"vite": "^7.0.0"`, `"@vitejs/plugin-react": "^4.0.0"`, `"vite-bundle-visualizer": "latest"`, `"vite-plugin-office-addin": "^1.0.0"`（D-05 接线）, `"@lingui/cli": "^5.0.0"`, `"@lingui/vite-plugin": "^5.0.0"`, `"vitest": "^2.0.0"`（FOUND-09 框架，本 plan 只装依赖；smoke test 在 plan 03）, `"size-limit": "^11.0.0"`, `"@size-limit/preset-app": "^11.0.0"`（D-15，本 plan 只装；CI 守卫在 plan 06）
    - scripts: `"dev": "vite"`, `"build": "vite build"`, `"preview": "vite preview"`, `"analyze": "vite-bundle-visualizer"`, `"test": "vitest run"`, `"size": "size-limit"`, `"extract": "lingui extract"`, `"compile": "lingui compile"`
    - `browserslist`（FOUND-02 — MVP 限定 Edge / Chrome 最新两版桌面浏览器）：写入 package.json 顶层 `browserslist` 字段，值为数组 `["Edge >= 120", "Chrome >= 120"]`（确切值，逐字写入）。Vite/esbuild 与 @vitejs/plugin-react 据此决定 transpile target。

    创建 repo 根 tsconfig.json（exact 提升 spike/bundle-test/tsconfig.json，仅扩 include）：
    - compilerOptions 完整搬：target ES2022, useDefineForClassFields true, lib ["ES2022","DOM","DOM.Iterable"], module ESNext, moduleResolution "bundler", allowImportingTsExtensions true, resolveJsonModule true, isolatedModules true, noEmit true, jsx "react-jsx", `"strict": true`, noFallthroughCasesInSwitch true, `"types": ["office-js", "vite/client"]`（**不可删 office-js——是 Office.onReady/PowerPoint.run/Excel.run/Word.run 类型唯一来源）
    - include: ["src", "vite.config.ts", "*.config.ts"]（加 *.config.ts 以纳入 lingui.config.ts / vitest）

    创建 repo 根 index.html（exact 提升 spike/bundle-test/index.html）：
    - `<html lang="zh-CN">`，title 改为 "Aster"
    - CDN Office.js script 标签 **先于** module entry（Shared Pattern 3）：`<script src="https://appsforoffice.microsoft.com/lib/1/hosted/office.js"></script>`
    - `<div id="root"></div>` + `<script type="module" src="/src/main.tsx"></script>`（main.tsx 由 plan 05 创建——本 plan 只放引用）
  </action>
  <verify>
    <automated>grep -q '"vite-plugin-office-addin"' package.json && grep -q '"strict": true' tsconfig.json && grep -q 'appsforoffice.microsoft.com/lib/1/hosted/office.js' index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 顶层 `name` 字段值为 `aster`（grep `"name": "aster"`）
    - `package.json` 含 devDependency `vite-plugin-office-addin`、`vitest`、`size-limit`、`@lingui/cli`、`@lingui/vite-plugin`
    - `package.json` scripts 含 `test`、`size`、`extract` 三个键
    - `tsconfig.json` 含 `"strict": true` 且 `types` 数组含 `"office-js"`
    - `index.html` 的 Office.js CDN script 标签出现在 `src/main.tsx` 引用之前（行号更小）
    - `index.html` title 为 `Aster`，`<html lang="zh-CN">`
    - `package.json` 顶层含 `browserslist` 字段且值含 `Edge >= 120` 与 `Chrome >= 120`（FOUND-02，grep `package.json` 含 `"Edge >= 120"` 且含 `"Chrome >= 120"`）
  </acceptance_criteria>
  <done>package.json/tsconfig.json/index.html 三文件在 repo 根存在，依赖栈含全部接线依赖，TS strict 开启，CDN Office.js 先于 module entry。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 vite.config.ts（office-addin 插件 + lingui 插件 + 多入口 + base）</name>
  <read_first>
    - spike/bundle-test/vite.config.ts（manualChunks 起点）
    - spike/pdfjs-vite-test/README.md（worker/静态资产 new URL 模式，写进守则）
    - .planning/phases/01-foundation/01-PATTERNS.md（vite.config 段 + Shared Pattern 2）
  </read_first>
  <action>
    在 repo 根创建 vite.config.ts（FOUND-01）。起点是 spike 的 manualChunks 配置，本阶段必加 4 项接线：
    1. import 并加入 plugins：`react()`（@vitejs/plugin-react）、`officeAddin()`（来自 vite-plugin-office-addin，D-05，管 HTTPS dev 证书与 manifest serve）、`lingui()`（来自 @lingui/vite-plugin，D-17）。
       - **注意 Lingui SWC 选项（PATTERNS 标记需确认）**：spike base 用 `@vitejs/plugin-react`（非 SWC 版）。Lingui macro 需要 babel-macro 转换——保留 `@vitejs/plugin-react`，并在其 options 中加 babel plugin `macros`（即 `react({ babel: { plugins: ['macros'] } })`），配合 `@lingui/vite-plugin`。**不要**改成 `@vitejs/plugin-react-swc`（会与 babel-macro 冲突）。
    2. `base: '/Aster/'`（GitHub Pages 子路径托管——manifest 内所有 URL 是 `wynne-cwb.github.io/Aster/...`）
    3. 多入口：`build.rollupOptions.input` 配两个 HTML 入口——`main: 'index.html'`（Task Pane）与 `commands: 'commands.html'`（ribbon handler 页，由 plan 04 创建该 HTML；本 plan 先在 input 声明路径）
    4. 保留 spike 的 `build.reportCompressedSize: true` 与 manualChunks（fluent / markdown / react 三块拆分，size-limit 守卫依赖产物可见）
    在文件顶部加注释守则（Shared Pattern 2）：任何 worker/wasm/静态资产必须用 `new URL('...', import.meta.url).href`，**禁** `?url` 导入（本阶段不接 pdf.js，Phase 3 接入时遵循）。
  </action>
  <verify>
    <automated>grep -q 'vite-plugin-office-addin' vite.config.ts && grep -q "base:" vite.config.ts && grep -q "commands" vite.config.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` import 了 `vite-plugin-office-addin` 与 `@lingui/vite-plugin`
    - `vite.config.ts` 含 `base: '/Aster/'`（grep `base.*Aster`）
    - `vite.config.ts` 的 `rollupOptions.input` 含 `commands` 与 `index`/`main` 两个入口键（grep `commands`）
    - `vite.config.ts` 保留 `reportCompressedSize` 与 manualChunks（grep `manualChunks`）
    - `react()` 配置含 babel macros 插件（grep `macros`）
    - 文件顶部含 `import.meta.url` worker 守则注释（grep `import.meta.url`）
  </acceptance_criteria>
  <done>vite.config.ts 含 office-addin 插件、lingui 插件、base /Aster/、双 HTML 入口、保留 manualChunks，worker 守则注释就位。</done>
</task>

<task type="auto">
  <name>Task 3: Lingui scaffold（lingui.config.ts + src/i18n/index.ts，zh-CN only）</name>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md（src/i18n 段）
    - .planning/phases/01-foundation/01-UI-SPEC.md（Copywriting Contract — 所有字符串 zh-CN + Lingui macro）
  </read_first>
  <action>
    建立 Lingui i18n 脚手架（FOUND-08 / D-17，v1 只 ship zh-CN）：
    1. repo 根创建 lingui.config.ts：
       - `locales: ['zh-CN']`（只此一个 locale）
       - `sourceLocale: 'zh-CN'`
       - `catalogs`: `[{ path: 'src/i18n/locales/{locale}/messages', include: ['src'] }]`
       - `compileNamespace: 'es'`
       - `format: 'po'`
    2. 创建 src/i18n/index.ts —— 运行时 i18n 初始化模块：
       - import `i18n` from `@lingui/core`
       - 加载并激活 zh-CN catalog：动态 import 编译后的 `./locales/zh-CN/messages` 并 `i18n.loadAndActivate({ locale: 'zh-CN', messages })`
       - 导出 `i18n` 供 main.tsx 用 `<I18nProvider i18n={i18n}>` 包裹（plan 05 消费）
       - 因 Phase 1 尚无 extract 产物，src/i18n/locales/zh-CN/messages.ts 可先放空 catalog 占位（`export const messages = {}`），`npm run extract` 后填充
    3. 创建占位 src/i18n/locales/zh-CN/messages.ts，内容 `export const messages = {};`（保证 import 不报错，CI build 通过）
  </action>
  <verify>
    <automated>grep -q "zh-CN" lingui.config.ts && grep -q "loadAndActivate" src/i18n/index.ts && test -f src/i18n/locales/zh-CN/messages.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `lingui.config.ts` 的 locales 数组只含 `zh-CN`（grep `'zh-CN'`，且不含 `'en'`）
    - `lingui.config.ts` 含 `sourceLocale: 'zh-CN'`
    - `src/i18n/index.ts` 含 `i18n.loadAndActivate`（grep `loadAndActivate`）并 `export` i18n
    - `src/i18n/locales/zh-CN/messages.ts` 存在且 `export const messages`
    - `npx tsc --noEmit` 对 src/i18n/ 无类型错误（i18n 模块自身可编译）
  </acceptance_criteria>
  <done>lingui.config.ts（zh-CN only）+ src/i18n/index.ts（loadAndActivate）+ 占位 messages.ts 就位，i18n 模块可被 main.tsx import。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 Task Pane → 第三方 CDN | Office.js 从 appsforoffice.microsoft.com CDN 加载脚本（Phase 1 唯一外部脚本来源） |
| 构建产物 → GitHub Pages 静态托管 | dist/ 推到公开 Pages，任何人可读取静态资源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | index.html CDN script | accept | Office.js 仅可从微软官方 CDN 加载（INSTALL-04 硬约束）；这是 Office Add-in 平台要求，无替代。不引入其它第三方 CDN script。 |
| T-01-02 | Information Disclosure | package.json/vite.config | mitigate | 脚手架阶段不引入任何 API Key/secret 默认值；不在配置中硬编任何 Provider 凭证。Key 存储逻辑整体推迟到 Phase 2（partitioned localStorage），本 plan 不得为其埋下不安全默认（如 localStorage 明文键名常量）。 |
| T-01-03 | Tampering | Vite worker/资产加载 | mitigate | vite.config 顶部强制 `new URL(..., import.meta.url)` 守则，禁 `?url`——避免 Phase 3 worker 从错误 origin 加载（一致性 + 完整性）。 |
</threat_model>

<verification>
- `npm install` 在 repo 根成功（无 peer dep 致命冲突）
- `npm run build` 产出 dist/（即便 src 尚不完整，至少 i18n 模块编译通过）—— 注：完整 build 依赖 plan 05 的 main.tsx，本 plan 验证配置文件本身可解析
- `grep` 全部 acceptance_criteria 通过
</verification>

<success_criteria>
- repo 根存在 package.json / vite.config.ts / tsconfig.json / index.html / lingui.config.ts / src/i18n/index.ts
- 依赖栈含 vite-plugin-office-addin + Lingui + Vitest + size-limit 全部接线依赖
- TS strict + office-js 类型就位
- Vite base = /Aster/，双 HTML 入口，office-addin + lingui 插件接线
- Lingui zh-CN only scaffold 可被 main.tsx 消费
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

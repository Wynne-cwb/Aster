---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/DocumentAdapter.ts
  - src/errors/index.ts
autonomous: true
requirements: [FOUND-04, FOUND-06, NFR-04, NFR-05]
must_haves:
  truths:
    - "DocumentAdapter 接口定义存在，含 getSelection / onSelectionChanged / capabilities 方法签名"
    - "SelectionContext 是 discriminated union（ppt/excel/word 三个变体）"
    - "InsertableContent 是 discriminated union（text/paragraphs/bullets/formula/range-values/slides/image 七变体）"
    - "类型化错误类层级完整：Provider 层 4 类 + Adapter 层 2 类，均继承共同基类"
  artifacts:
    - path: "src/adapters/DocumentAdapter.ts"
      provides: "跨宿主契约接口 + SelectionContext/InsertableContent/AdapterCapabilities union"
      contains: "onSelectionChanged"
      min_lines: 40
    - path: "src/errors/index.ts"
      provides: "类型化错误类层级（6 个子类 + 基类）"
      contains: "class AsterError"
      min_lines: 30
  key_links:
    - from: "src/adapters/DocumentAdapter.ts"
      to: "Phase 2-6 所有宿主操作"
      via: "DocumentAdapter 接口契约"
      pattern: "interface DocumentAdapter"
    - from: "src/errors/index.ts"
      to: "Phase 2 Provider 调用"
      via: "错误类抛出点"
      pattern: "extends AsterError"
---

<objective>
定义 Aster 跨宿主底座的两套核心契约——纯类型层，无运行时逻辑、无依赖。这是 Phase 2-6 所有代码编译时依赖的"蓝图"：
1. `DocumentAdapter` 接口 + 三个 discriminated union（`SelectionContext` / `InsertableContent` / `AdapterCapabilities`）
2. 类型化错误类层级（Provider 层 + Adapter 层）

Purpose: 接口先行（interface-first）——后续 plan 的 adapter 骨架、shell、Phase 2 Provider 全部 import 这里的类型。把契约写在 plan 里，executor 无需探索代码库即可实现。union 成员与错误类成员以 REQUIREMENTS.md FOUND-04/FOUND-06 枚举为准。
Output: src/adapters/DocumentAdapter.ts（纯接口/类型）、src/errors/index.ts（错误类层级）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md

<interfaces>
<!-- 本 plan CREATE 的契约，是后续 plan 与 Phase 2-6 的消费目标。 -->
<!-- 成员清单以 REQUIREMENTS.md FOUND-04/FOUND-06 为准（已枚举）。 -->

REQUIREMENTS FOUND-04 枚举：
- SelectionContext = discriminated union
- InsertableContent = discriminated union（text / paragraphs / bullets / formula / range-values / slides / image —— 7 个变体）
- AdapterCapabilities

REQUIREMENTS FOUND-06 枚举：
- Provider 层错误：KeyInvalidError / QuotaExceededError / ContextTooLongError / NetworkError
- Adapter 层错误：HostApiError / UnsupportedOperationError

CONTEXT D-13 / D-14 / D-16：
- onSelectionChanged(cb): () => void —— 返回解绑函数
- getSelection() 无选中时不抛错，返回空态标记
- 上下文卡显示：PPT「第 N 张 slide」/ Excel「选中区域地址」/ Word「选中 N 字」
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DocumentAdapter 接口 + discriminated unions</name>
  <read_first>
    - .planning/REQUIREMENTS.md（FOUND-04 枚举 union 成员；FOUND-03/FOUND-05 上下文）
    - .planning/phases/01-foundation/01-CONTEXT.md（D-13/D-14/D-16）
    - .planning/phases/01-foundation/01-PATTERNS.md（DocumentAdapter 契约段）
    - .planning/phases/01-foundation/01-UI-SPEC.md（Copywriting — 三宿主上下文卡显示值）
  </read_first>
  <behavior>
    - SelectionContext 是 discriminated union，含三个变体，每个带 `kind` 判别字段：
      - `{ kind: 'ppt'; slideIndex: number; slideCount: number }`（PPT「第 N 张 slide」所需）
      - `{ kind: 'excel'; address: string }`（Excel「选中区域 A1:C10」所需）
      - `{ kind: 'word'; charCount: number }`（Word「选中 N 字」所需）
      - 加空态变体 `{ kind: 'none' }`（D-16 无选中时，getSelection 不抛错返回此值）
    - InsertableContent 是 discriminated union，7 个变体（FOUND-04 枚举），各带 `type` 判别字段：
      `text` / `paragraphs` / `bullets` / `formula` / `range-values` / `slides` / `image`
    - AdapterCapabilities 描述某宿主支持哪些 InsertableContent 类型 + 是否支持选区监听
    - DocumentAdapter 接口含：`getSelection(): Promise<SelectionContext>`、`onSelectionChanged(cb: () => void): () => void`、`capabilities(): AdapterCapabilities`、以及 Phase 2-6 扩展的 `insert(content: InsertableContent): Promise<void>`（Phase 1 由各 adapter 桩实现）
  </behavior>
  <action>
    创建 src/adapters/DocumentAdapter.ts（FOUND-04 / NFR-04 / NFR-05），纯类型文件，无运行时逻辑：

    1. `SelectionContext` discriminated union（判别字段 `kind`）——成员见 behavior。`none` 变体对应 D-16 空态。
    2. `InsertableContent` discriminated union（判别字段 `type`），7 变体逐字按 FOUND-04：
       - `{ type: 'text'; value: string }`
       - `{ type: 'paragraphs'; values: string[] }`
       - `{ type: 'bullets'; items: string[] }`
       - `{ type: 'formula'; formula: string }`
       - `{ type: 'range-values'; values: (string | number)[][] }`
       - `{ type: 'slides'; base64: string }`（PPT insertSlidesFromBase64 用）
       - `{ type: 'image'; base64: string; targetSlideIndex?: number }`
    3. `AdapterCapabilities` 接口：`{ supportedInserts: InsertableContent['type'][]; supportsSelectionEvents: boolean; host: 'ppt' | 'excel' | 'word' }`
    4. `DocumentAdapter` 接口（FOUND-03/FOUND-05 契约）：
       - `getSelection(): Promise<SelectionContext>` —— Phase 1 真实实现（各 adapter），无选中返回 `{ kind: 'none' }`（D-16）
       - `onSelectionChanged(callback: () => void): () => void` —— 返回解绑函数（D-13）；宿主事件差异封在各 adapter 内（NFR-05）
       - `capabilities(): AdapterCapabilities` —— Phase 1 返回桩
       - `insert(content: InsertableContent): Promise<void>` —— Phase 1 桩（抛 UnsupportedOperationError 或 no-op），Phase 2-6 实现
    全部 export。本文件 0 import（纯类型）。
  </action>
  <verify>
    <automated>grep -q "interface DocumentAdapter" src/adapters/DocumentAdapter.ts && grep -q "onSelectionChanged" src/adapters/DocumentAdapter.ts && grep -c "type: '" src/adapters/DocumentAdapter.ts | awk '{ if ($1 >= 7) print "OK"; else print "FAIL: only "$1" insert variants" }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/adapters/DocumentAdapter.ts` 含 `export interface DocumentAdapter`
    - 接口含 `getSelection(): Promise<SelectionContext>`、`onSelectionChanged(callback: () => void): () => void`、`capabilities():`、`insert(`
    - `SelectionContext` 含 4 个 `kind:` 变体：`'ppt'`、`'excel'`、`'word'`、`'none'`（grep 各一次）
    - `InsertableContent` 含 7 个 `type:` 变体（`grep -c "type: '"` ≥ 7）：text/paragraphs/bullets/formula/range-values/slides/image
    - `AdapterCapabilities` export 且含 `supportedInserts`、`supportsSelectionEvents`
    - `npx tsc --noEmit` 对该文件 0 错误（纯类型可独立编译）
  </acceptance_criteria>
  <done>DocumentAdapter.ts 含完整接口 + 三 union（SelectionContext 4 变体 / InsertableContent 7 变体 / AdapterCapabilities），tsc 通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 类型化错误类层级</name>
  <read_first>
    - .planning/REQUIREMENTS.md（FOUND-06 枚举错误类成员）
    - .planning/phases/01-foundation/01-PATTERNS.md（src/errors 段）
  </read_first>
  <behavior>
    - 基类 `AsterError extends Error`，带只读 `code: string` 与 `category: 'provider' | 'adapter'` 字段，构造器设置 `this.name`
    - Provider 层 4 子类（category='provider'）：KeyInvalidError / QuotaExceededError / ContextTooLongError / NetworkError，各有稳定 `code` 字符串
    - Adapter 层 2 子类（category='adapter'）：HostApiError / UnsupportedOperationError
    - `error instanceof AsterError` 对所有子类为 true；`error instanceof KeyInvalidError` 可单独判别
    - 每个子类的 `.code` 与 `.category` 取值确定（便于 Phase 2 错误 UX 8 类映射）
  </behavior>
  <action>
    创建 src/errors/index.ts（FOUND-06），类型化错误类层级，纯类层无外部依赖：

    1. 基类：`export class AsterError extends Error`，构造器 `(message: string, public readonly code: string, public readonly category: 'provider' | 'adapter')`，内部 `this.name = new.target.name`（保证子类 name 正确）。
    2. Provider 层（category 传 `'provider'`），每类固定 code：
       - `export class KeyInvalidError extends AsterError` —— code `'KEY_INVALID'`
       - `export class QuotaExceededError extends AsterError` —— code `'QUOTA'`
       - `export class ContextTooLongError extends AsterError` —— code `'CONTEXT'`
       - `export class NetworkError extends AsterError` —— code `'NETWORK'`
       每个子类构造器只收 `message`（+ 可选 cause），内部调 `super(message, '<CODE>', 'provider')`。
    3. Adapter 层（category 传 `'adapter'`）：
       - `export class HostApiError extends AsterError` —— code `'HOST_API'`（可带 `hostError?: unknown` 字段存原始 Office.js 错误）
       - `export class UnsupportedOperationError extends AsterError` —— code `'UNSUPPORTED'`（Phase 1 adapter 桩方法抛此）
    注：本 plan 只定义类，不在任何业务路径抛出（Phase 2+ 实际抛）。code 值与 Phase 2 PROV-08 的 8 类错误 UX 对齐（KEY_INVALID/QUOTA/CONTEXT/NETWORK 4 类来自此处）。
  </action>
  <verify>
    <automated>grep -q "class AsterError extends Error" src/errors/index.ts && grep -c "extends AsterError" src/errors/index.ts | awk '{ if ($1 >= 6) print "OK"; else print "FAIL: only "$1" subclasses" }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/errors/index.ts` 含 `export class AsterError extends Error`
    - 含 6 个 `extends AsterError` 子类（`grep -c "extends AsterError"` ≥ 6）
    - 6 个子类名逐一存在：`KeyInvalidError`、`QuotaExceededError`、`ContextTooLongError`、`NetworkError`、`HostApiError`、`UnsupportedOperationError`
    - 基类构造器含 `code` 与 `category` 参数（grep `category`）
    - code 字符串存在：`'KEY_INVALID'`、`'QUOTA'`、`'CONTEXT'`、`'NETWORK'`、`'HOST_API'`、`'UNSUPPORTED'`
    - `npx tsc --noEmit` 对该文件 0 错误
  </acceptance_criteria>
  <done>src/errors/index.ts 含基类 AsterError + 6 个子类（Provider 4 + Adapter 2），各带稳定 code/category，tsc 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 类型契约层 → Phase 2 Provider 调用 | 错误类是 Phase 2 LLM 调用错误的载体；契约不当会让安全相关错误（如 Key 无效）被误分类 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | AsterError 子类 | mitigate | 错误类不在 message/code 中嵌入 API Key 或凭证（本 plan 仅定义类，Phase 2 抛出时须遵守此约束——在 KeyInvalidError 注释中标注"message 禁含 Key 原文"）。 |
| T-01-05 | Repudiation | DocumentAdapter 契约 | accept | 纯类型层无副作用、无 IO、无日志；不涉及审计需求。Phase 1 无运行时风险。 |
</threat_model>

<verification>
- `npx tsc --noEmit` 对 src/adapters/DocumentAdapter.ts 与 src/errors/index.ts 均无错误（纯类型独立编译）
- 全部 acceptance_criteria grep 通过
- union 变体数：SelectionContext 4、InsertableContent 7；错误子类 6
</verification>

<success_criteria>
- DocumentAdapter 接口 + 3 个 discriminated union 完整且 tsc 通过
- 错误类层级完整（基类 + Provider 4 + Adapter 2）
- 全部成员与 REQUIREMENTS FOUND-04/FOUND-06 枚举一致
- 两文件可被后续 plan（adapter 骨架、shell、Phase 2 Provider）import
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>

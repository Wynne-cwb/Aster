---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/main.tsx
  - src/App.tsx
  - src/components/ContextCard.tsx
  - src/components/ChatStream.tsx
  - src/components/InputBar.tsx
  - src/context/AdapterContext.ts
autonomous: true
requirements: [FOUND-03, PANE-01, NFR-06]
must_haves:
  truths:
    - "Office.onReady 读 info.host 实例化对应 adapter，经 React Context 暴露（FOUND-03）"
    - "Task Pane 渲染 350px 三段布局：顶部上下文卡 + 中部聊天流（空态）+ 底部输入栏"
    - "上下文卡实时反映 getSelection()——PPT 第 N 张 slide / Excel 区域地址 / Word 选中字数"
    - "Provider 下拉、上传图标、输入框、发送按钮全部可见但禁用（诚实表达能力边界）"
  artifacts:
    - path: "src/main.tsx"
      provides: "Office.onReady host 分流 + host-aware 主题 + Context 注入 + render"
      contains: "createAdapter"
    - path: "src/App.tsx"
      provides: "350px flex-column 三段 shell"
      contains: "flexDirection"
    - path: "src/components/ContextCard.tsx"
      provides: "selection-changed 实时上下文卡"
      contains: "onSelectionChanged"
    - path: "src/components/InputBar.tsx"
      provides: "禁用输入栏 + Provider 下拉 + 上传图标"
      contains: "disabled"
    - path: "src/components/ChatStream.tsx"
      provides: "空态聊天区"
      contains: "开始使用 Aster"
  key_links:
    - from: "src/main.tsx"
      to: "src/adapters/index.ts"
      via: "createAdapter(info.host)"
      pattern: "createAdapter\\(info"
    - from: "src/components/ContextCard.tsx"
      to: "DocumentAdapter.onSelectionChanged"
      via: "useEffect 订阅 + cleanup 解绑"
      pattern: "onSelectionChanged"
    - from: "src/App.tsx"
      to: "Lingui i18n"
      via: "Trans/t macro 包裹文案"
      pattern: "Trans|t`"
---

<objective>
搭建 Task Pane 视觉 shell——AC1 的核心可见交付。Office.onReady 读 `info.host` 实例化对应 adapter（FOUND-03 三宿主分流总入口），经 React Context 暴露给组件树；渲染 350px flex-column 三段布局（顶部上下文卡 + 中部聊天流空态 + 底部禁用输入栏）。上下文卡实时监听宿主 selection-changed 事件并显示当前选中描述（ROADMAP SC3 硬要求——证明 adapter 真实可用）。

Purpose: D-06 要求"全视觉三段骨架带禁用占位"——AC1 的 350px 三段观感一步到位，Phase 2/3 只填逻辑不改布局。所有 spacing/字号/颜色用 Fluent v9 token，所有文案 Lingui macro 包裹 zh-CN（UI-SPEC 硬规则）。
Output: main.tsx + App.tsx + ContextCard/ChatStream/InputBar + AdapterContext。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- 从 plan 02/03 创建的契约。executor 直接 import，无需探索代码库。 -->

// 来自 plan 03（src/adapters/index.ts，已 re-export 契约）
import { createAdapter, type DocumentAdapter, type SelectionContext } from './adapters';

// SelectionContext 变体（plan 02）
//   { kind: 'ppt'; slideIndex: number; slideCount: number }
//   { kind: 'excel'; address: string }
//   { kind: 'word'; charCount: number }
//   { kind: 'none' }

// DocumentAdapter
//   getSelection(): Promise<SelectionContext>
//   onSelectionChanged(callback: () => void): () => void   // 返回解绑函数（D-13）

// 来自 plan 01（src/i18n/index.ts）
import { i18n } from './i18n';   // 已 loadAndActivate zh-CN
</interfaces>

<spike_analogs>
spike/bundle-test/src/main.tsx —— Office.onReady→render 模式（lines 77-85）；FluentProvider 包裹（44-45）；Input+Button 行（50-60）；Text 用法（47）；messages 渲染（61-69）
</spike_analogs>

<ui_spec_hard_rules>
<!-- UI-SPEC 强制（见 01-UI-SPEC.md），executor 不可违反 -->
- Fluent v9 具体 import（非 barrel，Shared Pattern 4）：import { Card, Text, Button, Dropdown, Tooltip, Textarea } from '@fluentui/react-components'
- 所有 spacing/字号/颜色用 Fluent v9 token（Shared Pattern 7）：tokens.spacingVerticalM / fontSizeBase300 / colorNeutralBackground1/2 等；禁硬编 px/hex。例外：Task Pane 容器 min-width: 350px（布局尺寸，非 token）
- 所有可见文案 Lingui macro 包裹（Shared Pattern 6），zh-CN only。文案表见 UI-SPEC Copywriting Contract
- 主题 host-aware：读 Office.context.officeTheme 选 webLightTheme / webDarkTheme（不硬编 webLightTheme）
- Accent（colorBrandBackground）仅用于：① 发送按钮 primary 色槽（即便禁用保留品牌色）② 上下文卡 onSelectionChanged 触发时的短暂品牌色 pulse。其余一律 neutral
</ui_spec_hard_rules>

<copywriting_contract>
<!-- UI-SPEC Copywriting，全部 Lingui macro 包裹，zh-CN -->
- 发送按钮: 发送（Phase 1 disabled，brand 色槽保留）
- 空态 heading: 开始使用 Aster（fontSizeBase400 semibold，居中）
- 空态 body: 配置 Provider 后即可开始对话（fontSizeBase300 regular，colorNeutralForeground3，居中）
- Provider 下拉 placeholder: Provider（即将开放）（disabled）
- 上传图标 tooltip: 文件上传即将开放（Fluent Tooltip）
- 上下文卡 无选中: 未选中内容（D-16）
- 上下文卡 PPT: 第 N 张 slide
- 上下文卡 Excel: 选中区域 {address}
- 上下文卡 Word: 选中 {n} 字
- 输入框 placeholder: 输入消息…（disabled）
</copywriting_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdapterContext + main.tsx（Office.onReady host 分流 + host-aware 主题 + render）</name>
  <read_first>
    - spike/bundle-test/src/main.tsx（Office.onReady→render / FluentProvider 模式）
    - src/adapters/index.ts（plan 03——createAdapter）
    - src/i18n/index.ts（plan 01——i18n 实例）
    - .planning/phases/01-foundation/01-PATTERNS.md（main.tsx 段 / Shared Pattern 5）
  </read_first>
  <action>
    A. 创建 src/context/AdapterContext.ts：
    - `export const AdapterContext = React.createContext<DocumentAdapter | null>(null)`（import type DocumentAdapter from '../adapters'）
    - export 一个 `useAdapter()` hook：`const a = useContext(AdapterContext); if (!a) throw new Error('AdapterContext 未提供'); return a;`

    B. 创建 src/main.tsx（FOUND-03 三宿主分流总入口，扩展 spike 的 Office.onReady→render）：
    - import createRoot、createAdapter（'./adapters'）、AdapterContext、App（'./App'）、i18n（'./i18n'）、FluentProvider/webLightTheme/webDarkTheme（'@fluentui/react-components'，具体 import）、I18nProvider（'@lingui/react'）
    - `Office.onReady((info) => { ... })`：
      - `const adapter = createAdapter(info.host)` —— host → PptAdapter/ExcelAdapter/WordAdapter（FOUND-03）
      - host-aware 主题：读 `Office.context.officeTheme`，若暗色（如 bodyBackgroundColor 判定或 officeTheme 提供的 isDarkTheme）选 webDarkTheme，否则 webLightTheme（UI-SPEC host-aware 规则——不硬编 webLightTheme）
      - 取 `#root` 容器（无则 throw，沿用 spike）
      - render：
        ```
        <FluentProvider theme={hostTheme}>
          <I18nProvider i18n={i18n}>
            <AdapterContext.Provider value={adapter}>
              <App />
            </AdapterContext.Provider>
          </I18nProvider>
        </FluentProvider>
        ```
    - CDN Office.js 由 index.html 提供（plan 01），main.tsx 不 import office.js（INSTALL-04）。
  </action>
  <verify>
    <automated>grep -q "createAdapter(info" src/main.tsx && grep -q "AdapterContext.Provider" src/main.tsx && grep -q "officeTheme" src/main.tsx && grep -q "createContext" src/context/AdapterContext.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/context/AdapterContext.ts` 含 `createContext` 与 `useAdapter`（grep 各一次）
    - `src/main.tsx` 含 `createAdapter(info` （host 分流，FOUND-03）
    - `src/main.tsx` 含 `AdapterContext.Provider`、`I18nProvider`、`FluentProvider`
    - `src/main.tsx` 含 `officeTheme`（host-aware 主题，UI-SPEC）且 import 了 `webDarkTheme`
    - `src/main.tsx` 不含 `import` office.js（CDN 提供，grep `office-js` 应无 import 匹配）
    - `npx tsc --noEmit` 0 错误
  </acceptance_criteria>
  <done>AdapterContext + useAdapter 就位；main.tsx 用 Office.onReady 读 host 分流 adapter，host-aware 主题，经 Context + I18nProvider 包裹 App render。</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx 三段 shell + ChatStream + InputBar（350px flex-column）</name>
  <read_first>
    - spike/bundle-test/src/main.tsx（App 函数 / Input+Button / Text 用法）
    - src/context/AdapterContext.ts（task 1 产物）
    - .planning/phases/01-foundation/01-UI-SPEC.md（Spacing/Typography/Color/Copywriting/Component Inventory）
    - .planning/phases/01-foundation/01-PATTERNS.md（App.tsx / ChatStream / InputBar 段 + Shared Pattern 4/6/7）
  </read_first>
  <action>
    A. 创建 src/App.tsx —— 350px flex-column 三段 shell（PANE-01 / D-06 / UI-SPEC Spacing）：
    - 容器 `<div style={{ minWidth: '350px', height: '100vh', display: 'flex', flexDirection: 'column' }}>`（350px 是布局尺寸非 token；UI-SPEC 例外条款）
    - 顶部 `<ContextCard />`：content-height（auto）
    - 中部 `<ChatStream />`：`flex: 1`（占剩余，可滚动）
    - 底部 `<InputBar />`：content-height（auto）
    - 三段 padding/gap 用 token：`tokens.spacingVerticalM`（16px 默认内边距）等。顶/底两段 surface 用 `colorNeutralBackground2`，中部 chat 用 `colorNeutralBackground1`（UI-SPEC Color 60/30）。
    - 引 `useStyles`（Griffel makeStyles）或内联 style + tokens——但颜色/间距必须引 token（Shared Pattern 7）。

    B. 创建 src/components/ChatStream.tsx（空态，UI-SPEC Copywriting）：
    - 无消息，只渲染居中空态块：
      - heading `<Text size={400} weight="semibold">` 包 `<Trans>开始使用 Aster</Trans>`（fontSizeBase400 semibold）
      - body `<Text size={300}>` 包 `<Trans>配置 Provider 后即可开始对话</Trans>`，color `colorNeutralForeground3`，居中
    - 容器垂直水平居中（flex center），垂直 padding `spacingVerticalL`
    - import react-markdown 保留备 Phase 2（可不渲染，仅 import 或留注释——避免引入未用 import lint 错误则先不 import，注释说明 Phase 2 接入）

    C. 创建 src/components/InputBar.tsx（全禁用占位，D-07/D-08，UI-SPEC Copywriting）：
    - 一行 flex：Provider `<Dropdown disabled placeholder>` 包 `Provider（即将开放）` + 上传 `<Button icon disabled>` 套 `<Tooltip content={t`文件上传即将开放`}>` + `<Textarea disabled placeholder>` 包 `输入消息…`（flex:1）+ `<Button appearance="primary" disabled>` 包 `<Trans>发送</Trans>`
    - 全部 `disabled`；发送按钮保留 `appearance="primary"`（brand 色槽，UI-SPEC Accent ①）
    - 文案全 Lingui macro（Trans / t）
    - 间距用 token：字段与发送按钮间 `spacingHorizontalS`（8px）

    全部组件：Fluent v9 具体 import（非 barrel）；文案 Lingui macro；spacing/color token。
  </action>
  <verify>
    <automated>grep -q "flexDirection" src/App.tsx && grep -q "350px" src/App.tsx && grep -q "开始使用 Aster" src/components/ChatStream.tsx && grep -cq "disabled" src/components/InputBar.tsx && grep -q "tokens.spacing" src/App.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` 含 `flexDirection: 'column'`、`minWidth: '350px'`、`<ContextCard`、`<ChatStream`、`<InputBar`
    - `src/App.tsx` 引 Fluent token（grep `tokens.spacing` 或 `tokens.colorNeutral`），无硬编 hex 颜色（grep `#` 在颜色上下文应无匹配）
    - `src/components/ChatStream.tsx` 含 `开始使用 Aster` 与 `配置 Provider 后即可开始对话`（Lingui Trans 包裹）
    - `src/components/InputBar.tsx` 含 ≥4 处 `disabled`、`Provider（即将开放）`、`文件上传即将开放`、`输入消息…`、`发送`
    - `src/components/InputBar.tsx` 发送 Button 含 `appearance="primary"` 且 `disabled`
    - Fluent import 为具体命名 import（grep `from '@fluentui/react-components'`，无 `import * as`）
    - `npx tsc --noEmit` 0 错误
  </acceptance_criteria>
  <done>App.tsx 350px flex-column 三段 + ChatStream 空态 + InputBar 全禁用占位；token-first，Lingui macro，具体 import；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 3: ContextCard.tsx（selection-changed 实时上下文卡）</name>
  <read_first>
    - src/context/AdapterContext.ts（task 1——useAdapter）
    - src/adapters/index.ts（plan 03——DocumentAdapter / SelectionContext）
    - .planning/phases/01-foundation/01-CONTEXT.md（D-12/D-13/D-14/D-16）
    - .planning/phases/01-foundation/01-UI-SPEC.md（Copywriting 上下文卡三宿主值 + Color accent ②）
    - .planning/phases/01-foundation/01-PATTERNS.md（ContextCard 段）
  </read_first>
  <action>
    创建 src/components/ContextCard.tsx（D-12/D-13/D-14，ROADMAP SC3——证明 adapter 真实可用）：
    - `const adapter = useAdapter()`（task 1 hook）
    - `const [ctx, setCtx] = useState<string>(...)`：初值用 Lingui `t\`未选中内容\``（D-16 占位）
    - `useEffect(() => { const unsub = adapter.onSelectionChanged(async () => { const sel = await adapter.getSelection(); setCtx(formatSelection(sel)); }); return unsub; }, [adapter])`
      - cleanup 返回 unsub（D-13 解绑——防 Task Pane 隐藏后事件继续）
      - 进入时也可主动调一次 getSelection 初始化（可选）
    - `formatSelection(sel: SelectionContext): string`（discriminated union switch，D-14 / Copywriting）：
      - `case 'ppt'`: t\`第 ${sel.slideIndex} 张 slide\`
      - `case 'excel'`: t\`选中区域 ${sel.address}\`
      - `case 'word'`: t\`选中 ${sel.charCount} 字\`
      - `case 'none'`: t\`未选中内容\`
      - 用 exhaustive switch（default: const _x: never = sel —— 保证 union 全覆盖）
    - 渲染 Fluent `<Card>` + `<Text size={300}>{ctx}</Text>`（UI-SPEC Component Inventory）
    - selection 更新时品牌色 pulse（UI-SPEC Color accent ②）：用 state 触发短暂 `colorBrandBackground` tint（如 setTimeout 复位），token 驱动，subtle。
    - 文案全 Lingui macro。
  </action>
  <verify>
    <automated>grep -q "useAdapter\|useContext" src/components/ContextCard.tsx && grep -q "onSelectionChanged" src/components/ContextCard.tsx && grep -q "getSelection" src/components/ContextCard.tsx && grep -q "kind" src/components/ContextCard.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ContextCard.tsx` 含 `useEffect` 调 `adapter.onSelectionChanged` 且 `return unsub`（cleanup 解绑，D-13）
    - 含 `getSelection()` 调用
    - formatSelection switch 覆盖 4 个 `kind`：`'ppt'`、`'excel'`、`'word'`、`'none'`（grep 各一次）
    - 含 zh-CN 文案 `第`/`slide`、`选中区域`、`选中`/`字`、`未选中内容`（D-14 三宿主 + 空态）
    - 含 exhaustive `never` 检查（grep `never`）
    - 渲染 Fluent `<Card`
    - `npx tsc --noEmit` 0 错误
  </acceptance_criteria>
  <done>ContextCard 用 useEffect 订阅 adapter.onSelectionChanged（cleanup 解绑），getSelection 后按 union 格式化三宿主选区描述，token 驱动 pulse，Lingui macro；tsc 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主 selection 事件 → React 状态 | onSelectionChanged 回调把选区元数据带入 React 内存状态 |
| React 组件树 → Office 主题 | 读取 Office.context.officeTheme 决定 light/dark/HC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Information Disclosure | ContextCard 显示选区 | mitigate | 上下文卡仅显示选区元数据（slide 序号 / range 地址 / 字符数），**不渲染选区正文**——避免敏感文档内容意外暴露在 UI/日志。getSelection 在 Word 用 charCount（不留存 text）。 |
| T-01-13 | Denial of Service | onSelectionChanged 未解绑 | mitigate | useEffect cleanup 必须返回解绑函数（D-13）；防止组件卸载 / Task Pane 隐藏后事件 handler 累积泄漏（与 Phase 2 PROV-07 visibilitychange 同源）。acceptance_criteria 强制 grep `return unsub`。 |
| T-01-14 | Information Disclosure | InputBar 禁用占位 | accept | 输入框/上传/Provider 下拉全禁用，Phase 1 无任何用户输入被采集或外发；无 Key 输入面。Phase 2 接入逻辑时再评估输入校验。 |
</threat_model>

<verification>
- `npx tsc --noEmit` 全 src/ 0 错误
- `npm run build` 成功产出 dist/（main.tsx 完整后整链可 build）
- 全部 acceptance_criteria grep 通过
- 三段布局 + 上下文卡 union 全覆盖 + 全禁用占位
- 真机验证（sideload 后三宿主 350px 三段 + 上下文卡随选区刷新 + console 无 error）由 ROADMAP SC1/SC3 在 Phase 1 收尾 / Phase 7 REL-04 完成——本 plan 交付可 build 的代码面
</verification>

<success_criteria>
- Office.onReady host 分流 + Context 暴露 adapter（FOUND-03）
- 350px 三段 shell（PANE-01）
- 上下文卡实时反映 getSelection（ROADMAP SC3，三宿主 union 全覆盖）
- 禁用占位诚实表达能力边界（D-07/D-08）
- token-first + Lingui macro + host-aware 主题（UI-SPEC 全规则）
- 跨宿主一致工作（NFR-06，代码面）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>

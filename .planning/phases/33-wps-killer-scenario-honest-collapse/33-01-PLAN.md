# Phase 33 — killer scenario 端到端 + 诚实收口

> **⚠️ 投机性预写（STATE.md 2026-06-29）**：Phase 30 真机门未过。产物 = 代码草稿，真机 pending，不可标 Complete。

**Requirements:** WPS-09, WPS-10
**Depends on:** Phase 32（Excel adapter）✅ code-drafted

## Tasks

- **33-T1** 诚实工具裁剪：`isWpsRuntime()` + `WPS_EXCEL_CORE_TOOLS` + buildToolsForHost WPS 包装（excel→核心 7；word/ppt→[]）+ 守门测试。保证既有非 WPS 行为零回退。
- **33-T2** 非目标宿主 throw stub：复用 Phase 31 的 WpsWordAdapter/WpsPptAdapter（抛「WPS-D1 预留」）；配合 T1 的 word/ppt→[] 工具 → 诚实不裸奔。
- **33-T3** WPS 入口 bundle 独立核算（记 SUMMARY）：确认主 Office 入口零膨胀。
- **33-T4** 安装流程固化：`public/wps/publish.html`（Aster 三宿主 publish 安装器）+ `public/wps/README.md`（两路径 + 7 步真机验证脚本 + 诚实边界）。
- **33-T5** killer scenario = README §真机验证脚本（7 步，覆盖 Phase 30-33 全部真机成功标准），供用户一波真机批量测。

## 验证（代码侧 / 真机 pending）

代码侧：tsc 0 / build（dist/wps 六文件）/ 裁剪守门 3 test + 全量 1143 green。
真机侧（SC-1 纯真机，不可勾）：WPS Excel 多步 agent loop + undo all UAT PASS。

## 范围说明

killer scenario 端到端**复用**既有 agent loop + WpsExcelAdapter（Phase 32）+ 裁剪后的核心工具集，无新业务代码——
本 phase 的代码产出是「诚实收口（裁剪 + stub）+ 安装包 + 测试脚本」，端到端跑通本身是真机动作。

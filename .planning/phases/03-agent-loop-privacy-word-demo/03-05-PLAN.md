---
phase: 03-agent-loop-privacy-word-demo
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - src/store/chat.ts
  - src/store/chat.test.ts
  - src/store/providers.ts
  - src/store/providers.test.ts
  - src/lib/storage.ts
  - src/components/InputBar.tsx
  - src/components/InputBar.test.tsx
autonomous: true
requirements: [AGENT-01]
requirements_addressed: [AGENT-01]
must_haves:
  truths:
    - "chatStore.sendMessage 是 thin delegate — 调 useAgentStore.getState().runAgent(prompt, selectionCtx, adapter) 并 return"
    - "chatStore.Message 类型 role 含 'tool' + 字段 toolCallId / toolName / toolResult / agentRunId / agentStep"
    - "chatStore 删 acceptToolCall / rejectToolCall / stopStreaming（或 stopStreaming 改 delegate 到 agentStore.abort('user'))"
    - "chatStore 内不再 import OpenAICompatibleLLM / setupVisibilityAbort（agent loop 接管）"
    - "providers.ts 删 autoInsertMode 字段 / setAutoInsertMode 方法 / hydrateFromStorage 内 autoInsertMode 读路径 / AutoInsertMode 类型"
    - "storage.ts STORAGE_KEYS.AUTO_INSERT_MODE 删除（残留 localStorage key 不清理 — 假设 A6）"
    - "InputBar.handleSend 路径不变（chatStore.sendMessage thin delegate 在 store 内做）；按钮在 agentStatus !== 'idle' 时 disabled"
    - "chatStore.sendMessage 先 push user message 再 delegate（loop 不直接 push user）"
    - "UI 渲染层 (ChatStream role='tool' 卡片 / ChatBubble 删 ToolCallPreviewCard/AutoInsertEffect/FallbackInsertMenu / Settings 删开关) 由 Plan 06 (chat-ui-cleanup) 接力"
  artifacts:
    - path: "src/store/chat.ts"
      provides: "thin chatStore — Message + push/append/finalize + sendMessage delegate"
      contains: "useAgentStore.getState().runAgent"
    - path: "src/store/providers.ts"
      provides: "autoInsertMode 全删"
      contains: "providers:"
    - path: "src/components/InputBar.tsx"
      provides: "Send 按钮 agentStatus !== 'idle' 时 disabled"
      contains: "useAgentStatus"
  key_links:
    - from: "src/store/chat.ts sendMessage"
      to: "src/agent/agentStore.ts runAgent"
      via: "thin delegate"
      pattern: "useAgentStore\\.getState\\(\\)\\.runAgent"
    - from: "src/components/InputBar.tsx Send button"
      to: "src/agent/agentStore.ts useAgentStatus"
      via: "disabled when agentStatus !== 'idle'"
      pattern: "useAgentStatus"
---

<objective>
**B7 拆分：本 Plan 05 = chatStore-core**（chatStore + providers + storage + InputBar Send 禁用）；**Plan 06 = chat-ui-cleanup** 接力（ChatStream + ChatBubble + Settings）。

完成 chatStore-core 改造（D-01 + D-08）：把 sendMessage 内 LLM streaming / usage / tool_call 整段路径搬到 agent loop（Plan 03 已落 src/agent/loop.ts），chatStore 降级为「纯 message store + sendMessage 是 thin delegate」。Message 类型加 'tool' role + agent metadata 字段；删 acceptToolCall / rejectToolCall（v1 confirm/auto 模式砍 — D-19 G-05）。providers.ts 删 autoInsertMode 全套。InputBar Send 按钮 agentStatus !== 'idle' 时 disabled。

Purpose: agent loop 是 Phase 3 唯一主路径（D-01）；保留 v1 confirm/auto toggle 会让 chatStore 双模式复杂度爆炸 + 行为模糊。Plan 06 AgentControlBar 完成后 + Plan 08 demo prompt + 真机 UAT 就能跑通完整 demo SC1。
Output: 修改 7 文件 + 调整 4 测试（chatStore/providers/storage/InputBar core 改造）。UI 渲染层（ChatStream / ChatBubble / Settings）由 Plan 06 (chat-ui-cleanup) 接力。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md
@src/store/chat.ts
@src/store/providers.ts
@src/lib/storage.ts
@src/components/InputBar.tsx
@src/components/ChatStream.tsx
@src/components/ChatBubble.tsx
@src/components/Settings/SettingsPanel.tsx

<interfaces>
<!-- Plan 03 落地的 agentStore + loop 接口（src/agent/agentStore.ts）-->
```typescript
export const useAgentStore = create<AgentState>(...);
useAgentStore.getState().runAgent(prompt, selectionCtx, adapter): Promise<void>;
useAgentStore.getState().abort('user' | 'visibility' | ...);
export const useAgentStatus = () => useAgentStore((s) => s.agentStatus);
```

<!-- Plan 01 已落地后的 Message v2 schema 中间态 -->
```typescript
// 已删 tokenCount / costCny；尚未加 'tool' role / toolCallId / 等
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'error';
  content: string;
  isStreaming?: boolean;
  errorCode?: string;
  retryPrompt?: string;
  toolCalls?: ToolCall[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: chatStore Message schema 加 'tool' role + sendMessage thin delegate；providers 删 autoInsertMode 全套</name>
  <files>
    src/store/chat.ts (MODIFY),
    src/store/chat.test.ts (MODIFY 视存在与否),
    src/store/providers.ts (MODIFY),
    src/store/providers.test.ts (MODIFY 视存在与否),
    src/lib/storage.ts (MODIFY)
  </files>
  <behavior>
    - Test 1: chatStore.sendMessage('hello', selCtx) → useAgentStore.getState().runAgent 被调一次，传同样参数 + 当前 adapter
    - Test 2: chatStore.sendMessage 内不再直接调 OpenAICompatibleLLM / streamSSE / setupVisibilityAbort
    - Test 3: chatStore.pushMessage({role:'tool', toolCallId, toolName, toolResult, agentRunId, agentStep, content}) 正确 push 到 messages 数组，字段全保留
    - Test 4: chatStore 不再含 acceptToolCall / rejectToolCall 方法（typeof === 'undefined'）
    - Test 5: providers.ts useProviderStore 不含 autoInsertMode 字段（state shape 验证）
    - Test 6: hydrateFromStorage 不读 STORAGE_KEYS.AUTO_INSERT_MODE
  </behavior>
  <read_first>
    - src/store/chat.ts 整文件 339 行（找 sendMessage / acceptToolCall / rejectToolCall / stopStreaming / Message 类型）
    - src/store/providers.ts （找 autoInsertMode L46-49 / L62 / L78-79 / L95 / L146-149 / L176 / L200-201）
    - src/lib/storage.ts （STORAGE_KEYS.AUTO_INSERT_MODE）
    - src/store/chat.test.ts（如有，看 v1 测试覆盖；要删 acceptToolCall / rejectToolCall 相关 it）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Example C L781-838 + §Deliverable 7 §7.1 拆除清单 L1490-1518
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "src/store/chat.ts (modify, thin delegate)" + "src/store/providers.ts" + "src/lib/sse.ts" 段
    - src/context/AdapterContext.tsx — 看 adapter 如何在 chatStore 内访问（推断：可能通过参数传入 sendMessage 或全局 useAdapter）
  </read_first>
  <action>
**步骤 1 — 修改 src/store/chat.ts：**

1. Message interface 加 'tool' role + agent metadata 字段（注意 Plan 01 已删 tokenCount/costCny）：

```typescript
import type { ToolCall, ToolResult } from '../agent/tools';
// （ToolCall 类型见 v1 — 若 v1 已 export 自 chat.ts，保留；否则从 agent/tools 拉）

export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'tool' | 'error';
  content: string;
  isStreaming?: boolean;
  errorCode?: string;
  retryPrompt?: string;

  // agent metadata（'tool' role 用）
  toolCallId?: string;
  toolName?: string;
  toolResult?: ToolResult;
  agentRunId?: string;
  agentStep?: number;

  // assistant role 保留
  toolCalls?: ToolCall[];
}
```

2. ChatState interface 改造：

```typescript
interface ChatState {
  messages: Message[];
  pushMessage(m: Partial<Message> & { role: Message['role'] }): void;
  appendDeltaToMessage(id: string, delta: string): void;
  finalizeMessage(id: string, patch: Partial<Message>): void;

  // thin delegate（D-01）
  sendMessage(prompt: string, selectionCtx?: SelectionContext): Promise<void>;
  retryMessage(messageId: string): Promise<void>;
  clearHistory(): void;
  stopStreaming(): void;   // 改为 delegate 到 agentStore.abort('user')
}
```

3. 删除：
   - `acceptToolCall` 方法（v1 L284-312 — 整个方法删）
   - `rejectToolCall` 方法（v1 L314-327 — 整个方法删）
   - sendMessage 内整段 LLM streaming / for-await / switch event.type / Office.js / setupVisibilityAbort 路径（约 L93-264）
   - `abortController` 字段（state 中删除 — agentStore 持有）
   - `isStreaming` 字段（可保留作 deprecated，或删除 — 推荐保留兼容 ChatStream 中 v1 旧引用）
   - 文件顶部 `import { OpenAICompatibleLLM }` / `import { setupVisibilityAbort }` / `import { ProviderRegistry }` 等 v1 LLM 相关 import（仅保留 chatStore 真正需要的）

4. 替换 sendMessage 为 thin delegate：

```typescript
async sendMessage(prompt, selectionCtx) {
  // Thin delegate（D-01 / D-08）— agent loop 是唯一主路径
  const adapter = getAdapterForCurrentHost();  // 见步骤 1.5
  // 先 push user message（agentStore.runAgent 内 loop 不再 push user，因为 selectionCtx 信息在 chatStore 入口最新）
  useChatStore.getState().pushMessage({ role: 'user', content: prompt });
  await useAgentStore.getState().runAgent(prompt, selectionCtx, adapter);
},
```

5. stopStreaming 改为 delegate：

```typescript
stopStreaming() {
  useAgentStore.getState().abort('user');
},
```

注：`getAdapterForCurrentHost()` — Plan 03 / 04 / 05 没明确指定 chatStore 如何拿到 adapter。两个方案：
- 方案 A：sendMessage 签名扩展 `(prompt, selectionCtx, adapter)`，由 InputBar 通过 useAdapter context 传入
- 方案 B：在 main.tsx Office.onReady 内把 adapter 写到一个新 store（如 useAdapterStore.adapter）；chatStore.sendMessage 内 useAdapterStore.getState().adapter 取

**推荐方案 A**：最小侵入 — InputBar 已经 useAdapter()（v1 现状），现把它传给 sendMessage 即可。同步修改 InputBar.handleSend 调用 sendMessage(prompt, ctx, adapter)。如 v1 InputBar 已经传 adapter，无变化。

更新 sendMessage 签名：

```typescript
sendMessage(prompt: string, selectionCtx?: SelectionContext, adapter?: DocumentAdapter): Promise<void>;
```

（如 v1 已传 adapter，保持；如未传，本 plan 改 InputBar 一并加）

6. **不动** retryMessage / clearHistory（v1 已实现，无 cost 残留）

**步骤 2 — 修改 src/store/providers.ts：**

按 PATTERNS.md 删除清单：
- 删 `AutoInsertMode` type 定义（L46-49）
- 删 ProviderState 内 `autoInsertMode: AutoInsertMode` 字段（L62）
- 删 `setAutoInsertMode: (mode: AutoInsertMode) => void` 方法签名（L78-79）
- 删 create 内 `autoInsertMode: storage.get(...) ?? 'confirm'` 初值（L95）
- 删 setAutoInsertMode 实现（L146-149）
- 删 hydrateFromStorage 内 autoInsertMode 读路径（L176, L200-201）

**步骤 3 — 修改 src/lib/storage.ts：**

- 在 STORAGE_KEYS 对象内删 `AUTO_INSERT_MODE: 'aster:autoInsertMode'` 一行（A6 决策：残留 localStorage key 不清理）

**步骤 4 — 修改对应测试：**

- src/store/chat.test.ts（如有 acceptToolCall / rejectToolCall / streamSSE / tokenCount 相关 it，全删；新增 sendMessage thin delegate it）：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { useChatStore } from './chat';
import { useAgentStore } from '../agent/agentStore';
import type { DocumentAdapter } from '../adapters/DocumentAdapter';

const mockAdapter = {
  capabilities: () => ({ host: 'word' as const, supportsSelectionEvents: true, supportedInserts: ['text'] }),
  getSelection: async () => ({ kind: 'none' as const }),
  onSelectionChanged: () => () => {},
  insert: async () => {},
} as unknown as DocumentAdapter;

describe('chatStore.sendMessage thin delegate (D-01)', () => {
  beforeEach(() => {
    useChatStore.setState({ messages: [] } as never);
  });

  it('调 useAgentStore.runAgent(prompt, selectionCtx, adapter)', async () => {
    const runAgentSpy = vi.fn().mockResolvedValue(undefined);
    const orig = useAgentStore.getState().runAgent;
    useAgentStore.setState({ runAgent: runAgentSpy } as never);
    try {
      await useChatStore.getState().sendMessage('hello', undefined, mockAdapter);
      expect(runAgentSpy).toHaveBeenCalledWith('hello', undefined, mockAdapter);
    } finally {
      useAgentStore.setState({ runAgent: orig } as never);
    }
  });

  it('pushMessage({role:"tool",...}) 字段全保留', () => {
    useChatStore.getState().pushMessage({
      role: 'tool',
      toolCallId: 'c1',
      toolName: 'append_paragraph',
      toolResult: { ok: true, data: { written: 5 } },
      agentRunId: 'r1',
      agentStep: 2,
      content: '{"ok":true}',
    } as never);
    const msgs = useChatStore.getState().messages;
    expect(msgs).toHaveLength(1);
    expect(msgs[0].role).toBe('tool');
    expect(msgs[0].toolCallId).toBe('c1');
    expect(msgs[0].toolName).toBe('append_paragraph');
    expect(msgs[0].agentRunId).toBe('r1');
    expect(msgs[0].agentStep).toBe(2);
  });

  it('chatStore 已无 acceptToolCall / rejectToolCall 方法', () => {
    const state = useChatStore.getState();
    expect((state as never as { acceptToolCall?: unknown }).acceptToolCall).toBeUndefined();
    expect((state as never as { rejectToolCall?: unknown }).rejectToolCall).toBeUndefined();
  });
});
```

- src/store/providers.test.ts：删 autoInsertMode 相关 it（如 v1 有），新增：

```typescript
it('useProviderStore 不含 autoInsertMode 字段（D-08）', () => {
  const state = useProviderStore.getState();
  expect((state as never as { autoInsertMode?: unknown }).autoInsertMode).toBeUndefined();
  expect((state as never as { setAutoInsertMode?: unknown }).setAutoInsertMode).toBeUndefined();
});
```

跑 `npm test -- src/store src/lib/storage.test.ts`。
  </action>
  <verify>
    <automated>npm test -- src/store src/lib/storage.test.ts --reporter=verbose 2>&1 | tail -50 && npm test 2>&1 | tail -10 && grep -rn "autoInsertMode\|setAutoInsertMode\|acceptToolCall\|rejectToolCall\|AUTO_INSERT_MODE\|AutoInsertMode" src/ 2>/dev/null | grep -v '^[A-Za-z/.]*\.test\.\(ts\|tsx\):.*toBeUndefined' && echo "FAIL: legacy symbols still present" || echo "PASS: 0 legacy symbol hits (except in 'toBeUndefined' assertions)" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Message role 含 'tool'；'tool' metadata 字段齐
    - chatStore.sendMessage 是 thin delegate
    - chatStore.acceptToolCall / rejectToolCall 不存在
    - providers.autoInsertMode 全删
    - storage.STORAGE_KEYS.AUTO_INSERT_MODE 不存在
    - chat.test.ts / providers.test.ts 全绿
    - npm test 全套全绿
    - npm run build 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.2: InputBar Send 按钮 agentStatus !== 'idle' 时 disabled（B7 拆分后 UI core 部分）</name>
  <files>
    src/components/InputBar.tsx (MODIFY),
    src/components/InputBar.test.tsx (MODIFY/NEW)
  </files>
  <behavior>
    - Test InputBar-1: agentStatus === 'idle' 时 Send 按钮 enabled（input 非空时）
    - Test InputBar-2: agentStatus === 'running' 时 Send 按钮 disabled（aria-disabled='true' + disabled attribute）
    - Test InputBar-3: agentStatus === 'paused' 时 Send 按钮 disabled
    - Test InputBar-4: agentStatus === 'soft-landing' 时 Send 按钮 disabled
  </behavior>
  <read_first>
    - src/components/InputBar.tsx 整文件（看 handleSend / SendIcon / aria-label / disabled 现状；可能已有 isStreaming 路径，与 agent status 合并）
    - src/agent/agentStore.ts（Plan 03 落地的 useAgentStatus selector — 直接订阅）
    - src/components/ChatStream.test.tsx （RTL + jsdom + Zustand setState 测试范式）
  </read_first>
  <action>
**步骤 1 — 修改 src/components/InputBar.tsx：**

import 段追加：

```typescript
import { useAgentStatus } from '../agent/agentStore';
```

handleSend 路径不变（仍 chatStore.sendMessage(prompt, ctx, adapter)）；Send 按钮加 disabled 状态：

```typescript
const agentStatus = useAgentStatus();
const isAgentBusy = agentStatus !== 'idle';
// ... button:
<button
  type="submit"
  className="aster-iconbtn aster-iconbtn--primary"
  disabled={isAgentBusy || !input.trim()}
  aria-disabled={isAgentBusy ? 'true' : 'false'}
  aria-label={t`发送`}
>
  <SendIcon />
</button>
```

如已有 disabled 逻辑（如 v1 isStreaming 路径），与 isAgentBusy `||` 合并；不删 v1 已有的「empty input」disabled 路径。

**步骤 2 — 修改/新建 src/components/InputBar.test.tsx：**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, fireEvent } from '@testing-library/react';
import { AdapterContext } from '../context/AdapterContext';
import InputBar from './InputBar';
import { useAgentStore } from '../agent/agentStore';
import type { DocumentAdapter } from '../adapters/DocumentAdapter';

vi.mock('@lingui/react/macro', () => ({
  useLingui: () => ({ t: (s: TemplateStringsArray) => String.raw({ raw: s }), i18n: { _: (s: string) => s } }),
  Trans: ({ children }: { children: React.ReactNode }) => <>{children}</>,
}));

const mockAdapter = {
  capabilities: () => ({ host: 'word' as const, supportsSelectionEvents: true, supportedInserts: ['text'] }),
  getSelection: async () => ({ kind: 'none' as const }),
  onSelectionChanged: () => () => {},
  insert: async () => {},
} as unknown as DocumentAdapter;

describe('InputBar Send 按钮（Plan 05 B7 拆分后）', () => {
  beforeEach(() => {
    useAgentStore.setState({ agentStatus: 'idle', currentStep: 0, currentRunId: null, controller: null, lastAbortReason: null, runningTools: [] });
  });

  it.each([
    ['running', true],
    ['paused', true],
    ['soft-landing', true],
    ['idle', false],
  ] as Array<['running' | 'paused' | 'soft-landing' | 'idle', boolean]>)(
    'agentStatus=%s → Send 按钮 disabled=%s（input 非空时）',
    (status, expectDisabled) => {
      useAgentStore.setState({ agentStatus: status });
      const { getByLabelText } = render(
        <AdapterContext.Provider value={mockAdapter}>
          <InputBar onSettings={() => {}} />
        </AdapterContext.Provider>,
      );
      // 模拟用户输入（让 empty-input disabled 路径失效）
      const textarea = getByLabelText(/消息输入框/) as HTMLTextAreaElement;
      fireEvent.change(textarea, { target: { value: 'hi' } });
      const sendBtn = getByLabelText(/发送/) as HTMLButtonElement;
      expect(sendBtn.disabled).toBe(expectDisabled);
    },
  );
});
```

**步骤 3 — 跑测试 + size：**

```bash
npm test -- src/components/InputBar.test.tsx --reporter=verbose
npm test
npm run build && npm run size
```

注：ChatStream / ChatBubble / Settings UI 改动（删 ToolCallPreviewCard / AutoInsertEffect / FallbackInsertMenu / 渲染 role='tool' 折叠卡 / 软着陆卡片 / 删「AI 自动写文档」开关）由 **Plan 06 chat-ui-cleanup** 接力，本 plan 不做。
  </action>
  <verify>
    <automated>npm test -- src/components/InputBar.test.tsx --reporter=verbose 2>&1 | tail -30 && npm test 2>&1 | tail -10 && npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>
    - InputBar Send 按钮在 agentStatus !== 'idle' 时 disabled（含 running / paused / soft-landing）
    - InputBar.test.tsx 4 个状态 it.each 全绿
    - npm test 全套全绿
    - npm run build / npm run size 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chatStore.sendMessage → agentStore.runAgent | 用户输入直接进 agent loop；无 confirm/auto 中间审批层（D-19 G-05 砍） |
| role='tool' message 渲染 | content 字段是 humanLabel 字面量（中文）；toolResult JSON 隐藏在折叠内 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | chatStore thin delegate | mitigate | sendMessage 唯一入口；agent loop 是唯一 LLM 调用路径；不再有 v1 confirm/auto 双模式 |
| T-05-02 | Information disclosure | role='tool' content 字段 | accept | humanLabel 来自 ToolDef.humanLabel(args) 中文字面量（已截 30 字符）；不存 stack/Key |
| T-05-03 | Tampering | Send 按钮 disabled 状态 | mitigate | useAgentStatus 订阅 + button disabled / aria-disabled；防止用户在 agent run 中串场 prompt（A-14）|
</threat_model>

<verification>
- npm test 全套全绿
- npm run build / npm run size 通过
- grep 验证 v1 legacy 符号 0 命中（autoInsertMode / setAutoInsertMode / acceptToolCall / rejectToolCall / ToolCallPreviewCard / AutoInsertEffect / FallbackInsertMenu / AUTO_INSERT_MODE）— 除了 toBeUndefined 断言
- ChatStream 渲染 role='tool' 折叠卡 + soft-landing 卡片
- InputBar Send 按钮在 agent 跑时 disabled
</verification>

<success_criteria>
- D-01 / D-08 全部落地（chatStore 纯 message store + sendMessage thin delegate）
- D-19 G-05（v1 confirm/auto 全砍）落地
- Plan 06 AgentControlBar 可订阅 useAgentStatus；Plan 08 Word demo prompt 可走通完整链路
</success_criteria>

<output>
完成后创建 `.planning/phases/03-agent-loop-privacy-word-demo/03-05-SUMMARY.md`，记录：
- chatStore Message v2 完整字段列表
- chatStore.sendMessage 当前签名 + 路径流程图（user input → push user → agentStore.runAgent）
- 确认 Plan 03 loop.ts 双路径 push 行为（LLM wire JSON vs chatStore humanLabel）— 本 plan 不动 loop.ts
- 删除的 3 个 ChatBubble 子组件 + 行数
- providers.ts autoInsertMode 全删后字段列表对比
- bundle size 实测前后（应进一步小，因 3 个组件删掉）
</output>

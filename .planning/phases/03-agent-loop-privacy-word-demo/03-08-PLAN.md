---
phase: 03-agent-loop-privacy-word-demo
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.tsx
  - src/store/selection.ts
  - src/store/selection.test.ts
  - src/App.tsx
  - src/components/SelectionPill.tsx
  - src/components/SelectionPill.test.tsx
  - src/components/ContextCard.tsx
  - src/components/ContextCard.test.tsx
  - src/main.test.tsx
autonomous: true
requirements: [CARRY-01]
requirements_addressed: [CARRY-01]
must_haves:
  truths:
    - "main.tsx Office.onReady 内、root.render 之前 await adapter.getSelection() 一次"
    - "main.tsx 把结果灌到新建的 useSelectionStore.initial 字段（或等效路径）"
    - "ContextCard / SelectionPill useState 初值来自 useSelectionStore.initial（非占位 'none'）"
    - "ContextCard / SelectionPill useEffect 内不再 adapter.getSelection().then(setCtx) — 只保留 onSelectionChanged 订阅"
    - "三宿主单测 PPT/Excel/Word 各覆盖一份 SelectionPill 首帧已显示真实 ctx 而非 '未选中内容'"
    - "main.tsx 单测覆盖 Office.onReady 路径 A 拿到 initialSelection 后注入 store"
    - "getSelection 抛错时降级 { kind: 'none' }，不抛到 render"
  artifacts:
    - path: "src/store/selection.ts"
      provides: "useSelectionStore Zustand store + initial: SelectionContext 字段"
      contains: "export const useSelectionStore"
    - path: "src/main.tsx"
      provides: "Office.onReady 内 await adapter.getSelection() + setState({initial})"
      contains: "await adapter.getSelection()"
    - path: "src/components/SelectionPill.tsx"
      provides: "useState 初值来自 useSelectionStore.initial；不再 useEffect 内 getSelection"
      contains: "useSelectionStore"
    - path: "src/components/ContextCard.tsx"
      provides: "同上"
      contains: "useSelectionStore"
  key_links:
    - from: "src/main.tsx"
      to: "src/store/selection.ts"
      via: "Office.onReady 内 setState({ initial })"
      pattern: "useSelectionStore\\.setState"
    - from: "src/components/SelectionPill.tsx"
      to: "src/store/selection.ts"
      via: "useState 初值 = useSelectionStore.getState().initial"
      pattern: "useSelectionStore"
---

<objective>
修复 v1 FU-01「首次取选区显示延迟」bug（CARRY-01）。路径 A 实施（D-22 / D-23 + RESEARCH §Deliverable 6）：在 `main.tsx` `Office.onReady` 回调内、`createRoot.render` **之前**主动 `await adapter.getSelection()` 一次，作为初值灌到新建的 `useSelectionStore.initial` 字段；`ContextCard` / `SelectionPill` useState 初值改读这个 store，useEffect 内不再 `getSelection().then(setCtx)`，只保留 `onSelectionChanged` 订阅（用户切换选区时仍能更新）。

Purpose: 此 bug 不修，Phase 4 read tool 上线后所有 selection-aware tool 都被污染（agent 收到的首批 ctx 是 'none' 占位而非真实状态，决策依据失真）。CARRY-01 必须在 Phase 4 之前修完（ROADMAP "为什么这个顺序"）。
Output: 1 新增 store + 1 新增 store test + 1 新增 main test + 修改 main.tsx + 修改 App.tsx + 修改 SelectionPill.tsx + 修改 ContextCard.tsx + 新增/扩展两个组件测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-VALIDATION.md
@src/main.tsx
@src/App.tsx
@src/adapters/DocumentAdapter.ts
@src/components/SelectionPill.tsx
@src/components/ContextCard.tsx
@src/store/chat.ts
@src/store/providers.ts

<interfaces>
<!-- SelectionContext 四变体（src/adapters/DocumentAdapter.ts L17-52）-->
```typescript
export type SelectionContext =
  | { kind: 'ppt'; slideIndex: number; slideCount: number; ... }
  | { kind: 'excel'; address: string; ... }
  | { kind: 'word'; charCount: number; ... }
  | { kind: 'none' };
```

<!-- 现有 main.tsx Office.onReady（src/main.tsx L45-69）—— 同步回调 -->
```typescript
Office.onReady((info) => {
  const adapter = createAdapter(info.host);
  hydrateFromStorage();
  const container = document.getElementById('root');
  ...
  createRoot(container).render(<I18nProvider><AdapterContext.Provider><App /></AdapterContext.Provider></I18nProvider>);
});
```

<!-- Zustand store 标准 pattern（src/store/providers.ts 整体）-->
```typescript
import { create } from 'zustand';
interface FooState { ... }
export const useFooStore = create<FooState>((set, get) => ({ ... }));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: 新建 useSelectionStore + main.tsx 路径 A 预取选区</name>
  <files>
    src/store/selection.ts (NEW),
    src/store/selection.test.ts (NEW),
    src/main.tsx (MODIFY),
    src/App.tsx (MODIFY 视需要),
    src/main.test.tsx (NEW)
  </files>
  <behavior>
    - Test 1 (selection.test.ts): useSelectionStore 初始 initial = { kind: 'none' }；调 setState({initial: {kind:'word',charCount:150}}) 后 getState().initial.kind === 'word'
    - Test 2 (main.test.tsx): mock Office.onReady + mock adapter.getSelection 返 {kind:'ppt', slideIndex:3, slideCount:10} → 引导 main 流程后 useSelectionStore.getState().initial.kind === 'ppt'
    - Test 3 (main.test.tsx): mock adapter.getSelection 抛错 → useSelectionStore.getState().initial.kind === 'none'（兜底）
  </behavior>
  <read_first>
    - src/store/providers.ts （Zustand create + named selector pattern）
    - src/store/chat.ts （Zustand store 初始 state shape）
    - src/main.tsx 整文件
    - src/adapters/DocumentAdapter.ts L17-52 SelectionContext 类型
    - src/adapters/index.ts createAdapter 签名
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 6 §6.3 路径 A 推荐 + Example D L840-870
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "src/main.tsx (modify, CARRY-01 路径 A)" 段
  </read_first>
  <action>
**步骤 1 — 新建 src/store/selection.ts（约 25 行）：**

```typescript
/**
 * src/store/selection.ts — 选区状态 Zustand store
 *
 * 用途（CARRY-01 修复路径 A）：
 *   main.tsx Office.onReady 回调内 await adapter.getSelection() 一次，
 *   把结果灌到 useSelectionStore.initial；ContextCard / SelectionPill 的
 *   useState 初值改读这个 store，避免 React mount 与 Office.onReady 之间的
 *   微任务时序让用户看到 1-2 帧 "未选中内容" 占位。
 *
 * 用户切换选区时仍走 adapter.onSelectionChanged 订阅在组件内更新（不动）。
 */
import { create } from 'zustand';
import type { SelectionContext } from '../adapters/DocumentAdapter';

interface SelectionState {
  /** Office.onReady 内预取的初值；组件 useState 初值读这个字段 */
  initial: SelectionContext;
}

export const useSelectionStore = create<SelectionState>(() => ({
  initial: { kind: 'none' },
}));
```

**步骤 2 — 写 src/store/selection.test.ts（vitest 单测，TDD RED）：**

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { useSelectionStore } from './selection';

describe('useSelectionStore (CARRY-01)', () => {
  beforeEach(() => {
    useSelectionStore.setState({ initial: { kind: 'none' } });
  });

  it('初始 initial = { kind: "none" }', () => {
    expect(useSelectionStore.getState().initial).toEqual({ kind: 'none' });
  });

  it('setState 后字段更新', () => {
    useSelectionStore.setState({ initial: { kind: 'word', charCount: 150 } as any });
    expect(useSelectionStore.getState().initial.kind).toBe('word');
  });
});
```

**步骤 3 — 修改 src/main.tsx（Office.onReady 回调改为 async + 预取 + setState）：**

```typescript
// imports 段追加：
import { useSelectionStore } from './store/selection';
import type { SelectionContext } from './adapters/DocumentAdapter';

// 替换原 Office.onReady 回调：
Office.onReady(async (info) => {
  const adapter = createAdapter(info.host);
  hydrateFromStorage();

  // CARRY-01 修复路径 A：root.render 之前主动取一次选区，作为初值灌进 store
  // 三宿主真机：首次打开 Task Pane 时若已选中 slide/range/段，立即可显（不再先空再补）
  let initialSelection: SelectionContext = { kind: 'none' };
  try {
    initialSelection = await adapter.getSelection();
  } catch {
    // 极端情况（adapter 未就绪 / host API 异常）兜底 'none'；
    // 组件 onSelectionChanged 订阅仍会在用户切换选区时补上。
  }
  useSelectionStore.setState({ initial: initialSelection });

  const container = document.getElementById('root');
  if (!container) {
    throw new Error('未找到 #root 容器——请确认 index.html 包含 <div id="root">');
  }
  container.dataset.theme = resolveHostTheme();

  const root = createRoot(container);
  root.render(
    <I18nProvider i18n={i18n}>
      <AdapterContext.Provider value={adapter}>
        <App />
      </AdapterContext.Provider>
    </I18nProvider>
  );
});
```

**步骤 4 — App.tsx：不动**（initialSelection 通过 store 全局可见，无需 prop drilling；选项 1 from RESEARCH.md L1407）。

**步骤 5 — 写 src/main.test.tsx（CARRY-01 integration test，可选但 VALIDATION 列出建议建）：**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { useSelectionStore } from './store/selection';
import type { DocumentAdapter, SelectionContext } from './adapters/DocumentAdapter';

describe('CARRY-01 integration: main.tsx 路径 A 模拟', () => {
  beforeEach(() => {
    useSelectionStore.setState({ initial: { kind: 'none' } });
  });

  it('adapter.getSelection 返 ppt 上下文 → useSelectionStore.initial.kind === "ppt"', async () => {
    const mockAdapter: Pick<DocumentAdapter, 'getSelection'> = {
      getSelection: vi.fn().mockResolvedValue({
        kind: 'ppt', slideIndex: 3, slideCount: 10,
      } as SelectionContext),
    };
    let initial: SelectionContext = { kind: 'none' };
    try { initial = await mockAdapter.getSelection(); } catch { /* */ }
    useSelectionStore.setState({ initial });
    expect(useSelectionStore.getState().initial.kind).toBe('ppt');
  });

  it('adapter.getSelection 抛错 → useSelectionStore.initial.kind === "none"（兜底）', async () => {
    const mockAdapter: Pick<DocumentAdapter, 'getSelection'> = {
      getSelection: vi.fn().mockRejectedValue(new Error('host not ready')),
    };
    let initial: SelectionContext = { kind: 'none' };
    try { initial = await mockAdapter.getSelection(); } catch { /* 兜底 */ }
    useSelectionStore.setState({ initial });
    expect(useSelectionStore.getState().initial.kind).toBe('none');
  });
});
```

跑 `npm test -- src/store/selection.test.ts src/main.test.tsx` 全绿。
  </action>
  <verify>
    <automated>npm test -- src/store/selection.test.ts src/main.test.tsx --reporter=verbose 2>&1 | tail -30 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - src/store/selection.ts 存在 + useSelectionStore 导出
    - src/main.tsx Office.onReady 回调签名 `async (info)` + 含 `await adapter.getSelection()` + `useSelectionStore.setState({ initial: initialSelection })`
    - src/main.tsx 含 try/catch 兜底 { kind: 'none' }
    - selection.test.ts + main.test.tsx 全绿
    - npm run build 成功
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.2: SelectionPill / ContextCard 接 useSelectionStore.initial + 三宿主单测</name>
  <files>
    src/components/SelectionPill.tsx (MODIFY),
    src/components/ContextCard.tsx (MODIFY),
    src/components/SelectionPill.test.tsx (NEW),
    src/components/ContextCard.test.tsx (NEW)
  </files>
  <behavior>
    - Test 1 (SelectionPill.test.tsx): mock useSelectionStore.initial = { kind: 'ppt', slideIndex: 3, slideCount: 10 } → 首帧 textContent 含 "第 3 张 slide"
    - Test 2 (SelectionPill.test.tsx): mock useSelectionStore.initial = { kind: 'excel', address: 'A1:C10', ... } → 首帧 textContent 含 "选中区域 A1:C10"（依 v1 lingui catalog 文案）
    - Test 3 (SelectionPill.test.tsx): mock useSelectionStore.initial = { kind: 'word', charCount: 150 } → 首帧 textContent 含 "选中 150 字"
    - Test 4 (SelectionPill.test.tsx): useSelectionStore.initial = { kind: 'none' } → 首帧 textContent 含 "未选中内容"（保持 v1 占位文案）
    - Test 5 (SelectionPill.test.tsx): onSelectionChanged 订阅仍然触发 setCtx 更新（用户切换选区路径）
    - Test 6 (ContextCard.test.tsx): 与 SelectionPill 同样三宿主初值断言
  </behavior>
  <read_first>
    - src/components/SelectionPill.tsx 整文件（L29-71，看现有 useEffect L33 内的 getSelection 调用 + onSelectionChanged 订阅）
    - src/components/ContextCard.tsx 整文件（L25-? 内的 useEffect getSelection 调用）
    - src/components/ChatStream.test.tsx （RTL + jsdom + AdapterContext + Lingui mock 范式）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 6 §6.4 单测策略 L1452-1486
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "SelectionPill.test.tsx 和 ContextCard.test.tsx" 段 L1000-1046
  </read_first>
  <action>
**步骤 1 — 修改 src/components/SelectionPill.tsx：**
- 文件顶部 import 段追加：`import { useSelectionStore } from '../store/selection';`
- 找到现有 `const [ctx, setCtx] = useState<SelectionContext>({ kind: 'none' });`（或类似 useState 占位初值的行），改为：
  ```typescript
  const [ctx, setCtx] = useState<SelectionContext>(() => useSelectionStore.getState().initial);
  ```
  （函数式初值，避免在 selector 路径每次 render 都跑）
- 找到现有 useEffect 内 `void adapter.getSelection().then((sel) => { setCtx(sel); })` 或类似首次取选区的整段，**删除**，只保留 `adapter.onSelectionChanged(...)` 订阅（D-13 路径不动）：
  ```typescript
  useEffect(() => {
    const unsubscribe = adapter.onSelectionChanged(async () => {
      try {
        const sel = await adapter.getSelection();
        setCtx(sel);
      } catch {
        // 兜底：宿主异步异常时保留上一帧 ctx
      }
    });
    return unsubscribe;
  }, [adapter]);
  ```
- 其余渲染逻辑（aster-selection-pill className / attachEnabled / formatSelection）不动

**步骤 2 — 修改 src/components/ContextCard.tsx：**
- 同上模式：useState 初值改为 `() => useSelectionStore.getState().initial`
- 删 useEffect 内首次 `adapter.getSelection().then(setCtx)`
- 保留 onSelectionChanged 订阅 + 内部 getSelection 路径

**步骤 3 — 写 src/components/SelectionPill.test.tsx（TDD 三宿主单测）：**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render } from '@testing-library/react';
import { AdapterContext } from '../context/AdapterContext';
import SelectionPill from './SelectionPill';
import { useSelectionStore } from '../store/selection';
import type { DocumentAdapter, SelectionContext } from '../adapters/DocumentAdapter';

vi.mock('@lingui/react/macro', () => ({
  useLingui: () => ({
    t: (s: TemplateStringsArray) => String.raw({ raw: s }),
    i18n: { _: (s: string) => s },
  }),
  Trans: ({ children }: { children: React.ReactNode }) => <>{children}</>,
}));

function makeMockAdapter(
  host: 'ppt' | 'excel' | 'word',
  initial: SelectionContext,
): DocumentAdapter {
  return {
    capabilities: () => ({ host, supportsSelectionEvents: true, supportedInserts: ['text'] }),
    getSelection: vi.fn().mockResolvedValue(initial),
    onSelectionChanged: vi.fn(() => () => {}),
    insert: vi.fn().mockResolvedValue(undefined),
  };
}

describe('CARRY-01: SelectionPill first-mount ctx', () => {
  beforeEach(() => {
    useSelectionStore.setState({ initial: { kind: 'none' } });
  });

  it('PPT host: selected slide → 首帧显示「第 3 张 slide」', () => {
    const initial: SelectionContext = { kind: 'ppt', slideIndex: 3, slideCount: 10 } as SelectionContext;
    useSelectionStore.setState({ initial });
    const adapter = makeMockAdapter('ppt', initial);
    const { container } = render(
      <AdapterContext.Provider value={adapter}>
        <SelectionPill />
      </AdapterContext.Provider>
    );
    expect(container.textContent ?? '').toMatch(/第 3 张 slide/);
    expect(container.textContent ?? '').not.toMatch(/未选中内容/);
  });

  it('Excel host: selected range A1:C10 → 首帧显示「选中区域 A1:C10」', () => {
    const initial: SelectionContext = { kind: 'excel', address: 'A1:C10' } as SelectionContext;
    useSelectionStore.setState({ initial });
    const adapter = makeMockAdapter('excel', initial);
    const { container } = render(
      <AdapterContext.Provider value={adapter}>
        <SelectionPill />
      </AdapterContext.Provider>
    );
    expect(container.textContent ?? '').toMatch(/选中区域 A1:C10/);
  });

  it('Word host: selected 150 字 → 首帧显示「选中 150 字」', () => {
    const initial: SelectionContext = { kind: 'word', charCount: 150 } as SelectionContext;
    useSelectionStore.setState({ initial });
    const adapter = makeMockAdapter('word', initial);
    const { container } = render(
      <AdapterContext.Provider value={adapter}>
        <SelectionPill />
      </AdapterContext.Provider>
    );
    expect(container.textContent ?? '').toMatch(/选中 150 字/);
  });

  it('kind=none → 首帧仍为「未选中内容」占位（向后兼容）', () => {
    const adapter = makeMockAdapter('word', { kind: 'none' });
    const { container } = render(
      <AdapterContext.Provider value={adapter}>
        <SelectionPill />
      </AdapterContext.Provider>
    );
    expect(container.textContent ?? '').toMatch(/未选中内容/);
  });
});
```

**步骤 4 — 写 src/components/ContextCard.test.tsx**（结构相同；文案断言换为 ContextCard 的实际文本，可读 src/components/ContextCard.tsx 内 formatSelection 或 t-tagged 字符串决定。如 ContextCard 共用 formatSelection 函数，断言基本一致；如有自有文案，对照 v1 lingui catalog 文案验证）。

**步骤 5 — 跑全套 + 三宿主 manual smoke（npm test）：**
- 单测全绿
- 不改 lingui catalog
  </action>
  <verify>
    <automated>npm test -- src/components/SelectionPill.test.tsx src/components/ContextCard.test.tsx --reporter=verbose 2>&1 | tail -50 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - SelectionPill / ContextCard useState 初值读 useSelectionStore.getState().initial
    - 两组件 useEffect 内不再首次 getSelection（grep 验证 — 仅在 onSelectionChanged callback 内调）
    - SelectionPill.test.tsx 4 个 it 全绿（PPT / Excel / Word / none）
    - ContextCard.test.tsx 至少 3 个 it（PPT / Excel / Word）全绿
    - npm test 全套全绿
    - **真机 UAT（D-06 要求 — Plan 08 集中执行；本 plan 自动化覆盖即可，真机重测延到 Plan 08）**
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Office.js host event → React state | 首次状态来自 Office.onReady 内 await getSelection；用户切换由 onSelectionChanged 触发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CARRY-01 | Information disclosure | useSelectionStore.initial | accept | 仅存选区元数据（slideIndex / address / charCount），不存文档内容；不进 localStorage |
| T-CARRY-02 | Tampering | main.tsx onReady 路径 | mitigate | try/catch 兜底 { kind: 'none' }；getSelection 抛错不阻 render |
</threat_model>

<verification>
- npm test 全套全绿（含 selection.test.ts / main.test.tsx / SelectionPill.test.tsx / ContextCard.test.tsx）
- npm run build 通过
- grep `adapter\.getSelection\(\)\.then` 在 src/components/SelectionPill.tsx + src/components/ContextCard.tsx 0 命中（首次取选区路径已删）
- main.tsx Office.onReady 回调签名为 `async (info) => {`
- main.tsx 含 `useSelectionStore.setState({ initial: ` 一处
</verification>

<success_criteria>
- CARRY-01 自动化测试覆盖三宿主单测全绿（Plan 08 真机 UAT 重测必经）
- Phase 4 read tool 上线时 selection ctx 已就绪（不被首帧 'none' 污染）
</success_criteria>

<output>
完成后创建 `.planning/phases/03-agent-loop-privacy-word-demo/03-03-SUMMARY.md`，记录：
- 路径 A 实施位置：main.tsx Office.onReady 内 + useSelectionStore.setState 一行
- 三宿主单测断言文案（用于 Plan 08 真机 UAT 对照表）
- SelectionPill / ContextCard 改动的具体行号（GREP friendly）
- Plan 08 真机 UAT 回归脚本（三宿主分别先选 slide/range/段，打开 Task Pane → 胶囊立即显示，不空帧）
</output>

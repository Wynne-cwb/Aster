---
phase: 03-agent-loop-privacy-word-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CostBadge.tsx
  - src/providers/pricing.ts
  - src/providers/pricing.test.ts
  - src/components/ChatBubble.tsx
  - src/store/chat.ts
  - src/lib/sse.ts
  - .size-limit.json
autonomous: true
requirements: [NFR-02]
requirements_addressed: [NFR-02]
must_haves:
  truths:
    - "src/components/CostBadge.tsx 已删除"
    - "src/providers/pricing.ts 已删除"
    - "src/providers/pricing.test.ts 已删除"
    - "Message 类型不再有 costCny / tokenCount 字段"
    - "ChatBubble 不再 import CostBadge / 不再访问 message.costCny / message.tokenCount"
    - "src/lib/sse.ts SSEUsage 类型保留但带 @deprecated jsdoc 注释（陌生 sse 兼容性，但 v2 不消费）"
    - "npm test + npm run build + npm run size 全绿"
    - "grep 'costCny|tokenCount|CostBadge|calcCostCny|PROVIDER_PRICING|CNY_PER_USD' src/ 0 命中（除 SUMMARY.md / 备注）"
  artifacts:
    - path: "src/store/chat.ts"
      provides: "Message 类型不含 costCny / tokenCount；sendMessage 内 usage 事件分支删除"
      contains: "role: 'user' | 'assistant' | 'tool' | 'error'"
    - path: "src/components/ChatBubble.tsx"
      provides: "无 CostBadge 嵌点"
      contains: "ReactMarkdown"
    - path: "src/lib/sse.ts"
      provides: "SSEUsage 类型保留 + @deprecated 注释"
      contains: "@deprecated since v2.0"
    - path: ".size-limit.json"
      provides: "size-limit 配置（NFR-02 阈值监控线 ~80KB gzipped 留余量）"
      contains: "80 KB"
  key_links:
    - from: "src/store/chat.ts"
      to: "src/lib/sse.ts"
      via: "无 'usage' 事件消费路径"
      pattern: "case 'usage'"
---

<objective>
拆除 v1 cost 功能整套（D-20 / D-21 superseded v1 COST-01/02），把 chatStore Message 类型瘦身、ChatBubble 移除 CostBadge 嵌点，并把 size-limit 阈值从 1MB 收紧到 80KB（NFR-02 实测目标）。SSEUsage 类型保留兼容陌生 SSE upstream（D-21 + Open Q3）。这条主路径独立于 agent loop，先做以扫清后续 plan 的接触面（Plan 03 / 05 在 chatStore 上动 schema 时不再有 cost 字段需要并发兼容）。

Purpose: v2.0 milestone superseded v1 cost feature；max_steps=20 是 Phase 3 唯一失控防御；保留 v1 cost 显示会与 agent loop 多步语义冲突且占 bundle。
Output: 删除文件 (3) + 修改文件 (4)；npm test / build / size 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-VALIDATION.md
@src/store/chat.ts
@src/components/ChatBubble.tsx
@src/components/CostBadge.tsx
@src/providers/pricing.ts
@src/lib/sse.ts
@.size-limit.json

<interfaces>
<!-- 现有 Message 字段（v1，src/store/chat.ts L50-63）-->
```typescript
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'error';
  content: string;
  isStreaming?: boolean;
  tokenCount?: number;           // ← Plan 01 删
  costCny?: number | null;       // ← Plan 01 删
  errorCode?: string;
  retryPrompt?: string;
  toolCalls?: ToolCall[];        // 保留
}
```

<!-- 现有 chatStore.sendMessage 内 usage 事件分支（src/store/chat.ts L160-180）-->
```typescript
} else if (event.type === 'usage') {
  const costCny = calcCostCny(...);
  set((s) => ({ messages: s.messages.map((m) =>
    m.id === assistantMsg.id ? { ...m, tokenCount: event.totalTokens, costCny } : m,
  )}));
}
```

<!-- 现有 ChatBubble CostBadge 渲染（src/components/ChatBubble.tsx L32, L244-249）-->
```typescript
import CostBadge from './CostBadge';
...
{!message.isStreaming && message.tokenCount != null && (
  <CostBadge tokenCount={message.tokenCount} costCny={message.costCny ?? null} />
)}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1.1: 删除 CostBadge / pricing 文件 + chatStore Message 字段瘦身 + ChatBubble 移除 CostBadge 嵌点</name>
  <files>
    src/components/CostBadge.tsx (DELETE),
    src/providers/pricing.ts (DELETE),
    src/providers/pricing.test.ts (DELETE),
    src/store/chat.ts (MODIFY),
    src/components/ChatBubble.tsx (MODIFY),
    src/lib/sse.ts (MODIFY)
  </files>
  <read_first>
    - src/store/chat.ts （整文件 339 行 — 确认 Message 类型 L50-63、sendMessage L93-265 中 usage 事件分支位置、import calcCostCny 位置）
    - src/components/ChatBubble.tsx （L32 import + L244-249 CostBadge 嵌点 — 不要碰 ToolCallPreviewCard / AutoInsertEffect / FallbackInsertMenu，那些是 Plan 05 删）
    - src/lib/sse.ts （L300-356 找到 SSEUsage 类型定义位置）
    - src/providers/pricing.ts + src/providers/pricing.test.ts （确认要删的内容）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 7 "v1 cost / pricing 拆除范围" L1490-1538
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "删除文件" 段 + "src/store/chat.ts (modify, thin delegate)" 段中 Message 类型 D-08 改造
  </read_first>
  <action>
1. 删除三个文件：
   - `rm src/components/CostBadge.tsx`
   - `rm src/providers/pricing.ts`
   - `rm src/providers/pricing.test.ts`

2. 修改 `src/store/chat.ts`:
   - L50-63 `Message` interface：
     - 删除 `tokenCount?: number;` 一行
     - 删除 `costCny?: number | null;` 一行
     - `role: 'user' | 'assistant' | 'error'` **本 task 暂不改**（'tool' role 由 Plan 05 加，避免和 Plan 05 改 schema 时冲突）
   - 文件顶部 import 段：删 `import { calcCostCny } from '../providers/pricing';` （如果存在）
   - `sendMessage` 内 for-await-of 循环里 `event.type === 'usage'` 分支（约 L160-180）整段删除（包括 const costCny = calcCostCny(...) 与对应 set((s) => map((m) => m.id === ... ? { ...m, tokenCount, costCny } : m)))
   - **保留** `case 'tool_call_end'` 路径、`case 'delta'` 路径、AbortError 路径 — 那些是 Plan 05 的范围
   - 不动 acceptToolCall / rejectToolCall / stopStreaming 方法（Plan 05 删）

3. 修改 `src/components/ChatBubble.tsx`:
   - L32 删 `import CostBadge from './CostBadge';`
   - L244-249 删整段 `{!message.isStreaming && message.tokenCount != null && (<CostBadge tokenCount={message.tokenCount} costCny={message.costCny ?? null} />)}`
   - **不动** ToolCallPreviewCard / AutoInsertEffect / FallbackInsertMenu / 自动插入 useEffect 等 G-05 路径（Plan 05 删）
   - 不动 `import { useProviderStore } ... autoInsertMode` 订阅（Plan 05 删）

4. 修改 `src/lib/sse.ts` `SSEUsage` interface 上方加 jsdoc（如类型已是命名 export，在 export 上方）：
   ```typescript
   /**
    * @deprecated since v2.0 — usage 事件保留兼容 stream_options 输出格式，
    *   但 v2 chatStore / agent loop 不消费此字段（cost 全砍，无 budget 估算）。
    *   保留是为了陌生 SSE upstream 不报错；将来若 Provider 强制要求 include_usage:false 可一并移除。
    */
   export interface SSEUsage { ... }
   ```
   - 文件内其余 usage 事件解析、emit 逻辑不动（v1 已实现）

5. **不动** `src/store/providers.ts` autoInsertMode 字段（Plan 05 删；本 plan 主路径已足）

6. **不动** `src/providers/openai-compat.ts` 的 INSERT_TO_DOCUMENT_TOOL hardcode（Plan 04 删）

7. **不动** lingui catalog（messages.po / messages.ts）— "本次：X token · 约 ¥..." 字符串残留无害（不会渲染因为 CostBadge 已删）；Phase 3 收尾或 Phase 4 顺手 lingui extract 清理

实施依据：D-21 + RESEARCH.md §Deliverable 7 §7.1 拆除清单 + PATTERNS.md "删除文件" 段
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | tail -30 && npm run build 2>&1 | tail -10 && grep -rnE "costCny|tokenCount|CostBadge|calcCostCny|PROVIDER_PRICING|CNY_PER_USD" src/ 2>/dev/null | grep -v '^Binary' | grep -v '^#' || echo "PASS: 0 命中"</automated>
  </verify>
  <done>
    - 三个文件已删除
    - Message interface 不含 tokenCount / costCny
    - sendMessage 内无 'usage' 事件分支
    - ChatBubble 不 import CostBadge / 不渲染 CostBadge
    - sse.ts SSEUsage 上方有 @deprecated jsdoc 注释
    - npm test 全绿（pricing.test.ts 已删，其它 test 不应 import pricing）
    - npm run build 成功
    - grep cost 相关符号 0 命中
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.2: 收紧 size-limit 阈值到 80KB（NFR-02 实测目标）+ size 验证</name>
  <files>.size-limit.json</files>
  <read_first>
    - .size-limit.json （当前 limit 1 MB）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Validation Architecture L968 "建议把 limit 设到 ~80KB gzipped 作 NFR-02 安全余量"
    - .planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md D-03 "Bundle 实测目标 ≤ ~70KB gzipped；任何超过 5KB gzipped 的新依赖都要 challenge"
  </read_first>
  <action>
修改 `.size-limit.json`，把 `"limit": "1 MB"` 改为 `"limit": "80 KB"`（NFR-02 实测目标 ~70KB + 10KB Phase 3-7 各阶段增长余量）。完整文件内容应为：

```json
[
  {
    "name": "initial-js",
    "path": "dist/assets/*.js",
    "limit": "80 KB",
    "gzip": true
  }
]
```

理由（D-03 + RESEARCH.md L968）：
- v1 实测基线 ~63-68KB gzipped（移除 Fluent 后）
- Plan 01 cost 拆除可能让基线再降几 KB
- 80KB 作监控线给 Phase 3-7 各阶段 ~10KB headroom，超出触发 CI 阻断
- 1MB 是 v1 N2 硬上限，Phase 7 再放宽（NFR-02 验收）

确认本 task 之后 `npm run size` PASS（如果 fail 说明 cost 拆除还没生效或还有意外膨胀，需排查；不要直接抬阈值）。
  </action>
  <verify>
    <automated>npm run build && npm run size 2>&1 | tail -20</automated>
  </verify>
  <done>
    - .size-limit.json limit 改为 80 KB
    - npm run size 输出 PASS（实测 ≤ 80KB gzipped）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chatStore → Message UI | message 已无 costCny / tokenCount 字段，前端无成本相关数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | src/lib/sse.ts SSEUsage | accept | usage 字段不再被消费但解析路径保留，没有用户数据落地（之前的 v1 cost 显示已删除） |
| T-03-02 | Tampering | bundle 体积 | mitigate | size-limit 收紧到 80KB；CI gate 阻断任何超出（NFR-02 守门）|
</threat_model>

<verification>
- `npm test` 通过（pricing.test.ts 删除后无剩余 cost 相关失败）
- `npm run build` 通过
- `npm run size` 通过 ≤ 80KB gzipped
- grep cost 符号 0 命中（除 SUMMARY.md / 备注文件）
</verification>

<success_criteria>
- v1 cost 功能完全拆除（SC5 已落 — 见 ROADMAP Phase 3 SC5 / CONTEXT D-21）
- bundle gate 收紧到 80KB（NFR-02 实测目标）
- 后续 plan 改 chatStore Message schema 时不再有 cost 字段冲突
</success_criteria>

<output>
完成后创建 `.planning/phases/03-agent-loop-privacy-word-demo/03-01-SUMMARY.md`，记录：
- 删除的文件清单（含行数）
- 修改的 chatStore Message schema 前后对比
- bundle size 实测前后对比（npm run size 输出）
- 任何超出本 plan 范围但顺手修的小项（可在 SUMMARY 标记，Plan 05 接力时知晓）
</output>

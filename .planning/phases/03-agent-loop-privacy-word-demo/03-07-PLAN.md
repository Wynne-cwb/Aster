---
phase: 03-agent-loop-privacy-word-demo
plan: 07
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/components/AgentControlBar.tsx
  - src/components/AgentControlBar.test.tsx
  - src/components/icons.tsx
  - src/App.tsx
  - src/styles.css
autonomous: true
requirements: [AGENT-02, AGENT-13]
requirements_addressed: [AGENT-02, AGENT-13]
must_haves:
  truths:
    - "src/components/AgentControlBar.tsx 完整版（pause + 中止 + step counter）"
    - "agentStatus === 'idle' 时 AgentControlBar 不渲染（return null）"
    - "agentStatus === 'running' 时 显示 step counter + Pause 按钮 + 中止按钮"
    - "agentStatus === 'paused' 时 显示 step counter + Play 按钮（resume）+ 中止按钮"
    - "agentStatus === 'soft-landing' 时 AgentControlBar 渲染 step counter；软着陆卡片在 ChatStream 内（Plan 05 已落）"
    - "src/components/icons.tsx 追加 PauseIcon + PlayIcon（Lucide 风格 SVG，stroke=currentColor）"
    - "src/App.tsx 顶部常驻 <AgentControlBar />（在 ContextCard 之上、ChatStream 之上、ToolBar 之下；具体位置由 v1 App.tsx 现有 layout 决定）"
    - "src/styles.css 加 .aster-agent-bar 容器 + 子元素样式（玻璃拟态 + 渐变 accent + 11px 步数小字）"
    - "AgentControlBar.test.tsx 覆盖 idle 不渲染 / running 渲染 pause+abort+step / paused 渲染 play / 点 pause 状态变化 / 点 abort lastAbortReason='user'"
  artifacts:
    - path: "src/components/AgentControlBar.tsx"
      provides: "完整版 AgentControlBar — pause + abort + step counter + 软着陆 status"
      contains: "useAgentStatus"
    - path: "src/components/icons.tsx"
      provides: "PauseIcon + PlayIcon 内联 SVG"
      contains: "export function PauseIcon"
    - path: "src/styles.css"
      provides: ".aster-agent-bar 玻璃拟态容器 + step counter 11px"
      contains: ".aster-agent-bar"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/AgentControlBar.tsx"
      via: "顶部常驻渲染（idle 时 return null）"
      pattern: "<AgentControlBar"
    - from: "src/components/AgentControlBar.tsx"
      to: "src/agent/agentStore.ts useAgentStatus / useCurrentStep / pause / resume / abort"
      via: "Zustand named selectors"
      pattern: "useAgentStore"
---

<objective>
落地 Phase 3 完整版 AgentControlBar（D-discretion AgentControlBar 视觉细节 + D-09 / AGENT-12 / AGENT-13）：pause + 中止 双按钮 + step counter（`N / 20`）+ idle 时不渲染。视觉走 CLAUDE.md §UI 设计系统：玻璃拟态容器 + 品牌渐变作 accent（按钮 focus / hover）+ 11px 步数小字 + 内联 SVG Pause/Play 图标（Lucide 风 stroke=currentColor）。软着陆卡片是 ChatStream 内特殊消息（Plan 05 已落，RESEARCH Open Q4 推荐方案）— 不在本组件内 modal 化。

Purpose: agent run 期间用户必须看到状态可观察（SC2 — 失控控制可观察）。pause / 中止 是用户对 agent 行为的唯一显式控制（cost 砍后无 cap，max_steps 是 fail-safe）。
Output: 新建 1 组件 + 1 测试 + 2 个图标 + App.tsx 挂载 + styles.css token。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md
@CLAUDE.md
@src/App.tsx
@src/components/icons.tsx
@src/components/SelectionPill.tsx
@src/components/ChatStream.test.tsx
@src/styles.css

<interfaces>
<!-- Plan 03 落地 agentStore named selectors -->
```typescript
import { useAgentStore, useAgentStatus, useCurrentStep, MAX_STEPS } from '../agent/agentStore';
// agentStatus: 'idle' | 'running' | 'paused' | 'soft-landing'
// pause() / resume() / abort('user'|'visibility'|'max_steps'|'circuit')
```

<!-- src/components/icons.tsx 现有 base spread pattern + 已有 14 个图标 -->
```typescript
const base = {
  viewBox: '0 0 24 24', fill: 'none', stroke: 'currentColor',
  strokeWidth: 1.75, strokeLinecap: 'round' as const,
  strokeLinejoin: 'round' as const, 'aria-hidden': true,
};
export function SettingsIcon(): ReactElement { return <svg {...base}>...</svg>; }
// 已有：SettingsIcon, UploadIcon, SendIcon, ChevronIcon, StopIcon, InsertIcon,
//        RetryIcon, XIcon, AlertIcon, PlusIcon, TrashIcon, CheckIcon, EyeIcon, EyeOffIcon
// Plan 06 追加：PauseIcon, PlayIcon
```

<!-- v1 styles.css 已有 token：--glass-bg / --brand-gradient / --text-3 / --sp-1..--sp-3 / --r-md / --r-pill / --dur / --ease -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 6.1: 追加 PauseIcon + PlayIcon 到 src/components/icons.tsx</name>
  <files>src/components/icons.tsx (MODIFY)</files>
  <read_first>
    - src/components/icons.tsx 整文件（看 base spread + StopIcon L62 现状作 reference）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "新增 icon SVG" 段 L546-569
    - CLAUDE.md §UI 设计系统「图标：内联 SVG，写进 src/components/icons.tsx，Lucide 风（stroke=currentColor）」
  </read_first>
  <action>
在 src/components/icons.tsx 末尾追加（在最后一个 EyeOffIcon 之后）：

```typescript
/** 暂停 — 两条粗竖线（Lucide 风：fill 当前色块，no stroke）*/
export function PauseIcon(): ReactElement {
  return (
    <svg {...base}>
      <rect x="6" y="4" width="4" height="16" rx="1" fill="currentColor" stroke="none" />
      <rect x="14" y="4" width="4" height="16" rx="1" fill="currentColor" stroke="none" />
    </svg>
  );
}

/** 继续（播放）— 实心三角 */
export function PlayIcon(): ReactElement {
  return (
    <svg {...base}>
      <path d="M7 4 L20 12 L7 20 Z" fill="currentColor" stroke="none" />
    </svg>
  );
}
```

注：
- PauseIcon 用 `fill="currentColor" stroke="none"` 体现 "暂停" 块感（v1 StopIcon 是细线方框，语义稍微不同 — AgentControlBar 中止按钮复用 StopIcon，Pause 是新形态）
- PlayIcon 三角顶点 (7,4) (20,12) (7,20) — 标准右指三角，宽度填满 22px viewBox
- 复用现有 `base` spread；不引外部 lib

不动其他图标（CheckIcon / EyeIcon / 等都保持）。
  </action>
  <verify>
    <automated>grep -nE "^export function (PauseIcon|PlayIcon)\(\)" src/components/icons.tsx | wc -l | grep -q "^2$" && echo "PASS: 2 new icons" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/components/icons.tsx 含 PauseIcon + PlayIcon 两 export
    - 图标 SVG 用 `currentColor` 不绑色
    - npm run build 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6.2: AgentControlBar 组件 + 单测 + styles.css token + App.tsx 挂载</name>
  <files>
    src/components/AgentControlBar.tsx (NEW),
    src/components/AgentControlBar.test.tsx (NEW),
    src/styles.css (MODIFY — 追加 .aster-agent-bar* 选择器块),
    src/App.tsx (MODIFY — 顶部插入 <AgentControlBar />)
  </files>
  <behavior>
    - Test 1: agentStatus = 'idle' → container.firstChild === null（不渲染）
    - Test 2: agentStatus = 'running' currentStep=3 → 渲染含 "3 / 20" + 两按钮（aria-label 含「暂停」+「中止」）
    - Test 3: agentStatus = 'paused' currentStep=5 → 渲染含 "5 / 20" + 按钮 aria-label 含「继续」（PlayIcon）+「中止」
    - Test 4: agentStatus = 'soft-landing' currentStep=20 → 渲染含 "20 / 20"（软着陆按钮在 ChatStream，本 bar 仅 step display）
    - Test 5: 点 pause 按钮（running 态）→ useAgentStore.getState().agentStatus = 'paused'
    - Test 6: 点 resume 按钮（paused 态）→ useAgentStore.getState().agentStatus = 'running'
    - Test 7: 点 abort 按钮 → useAgentStore.getState().lastAbortReason = 'user'，controller.signal.aborted = true
  </behavior>
  <read_first>
    - src/components/SelectionPill.tsx L1-71（Zustand selector + 内联 SVG + 条件 null + Lingui macro 完整范式）
    - src/components/ChatStream.test.tsx L17-115（RTL + jsdom + Lingui mock + AdapterContext + Zustand setState 测试范式）
    - src/agent/agentStore.ts（Plan 03 落地的 store 接口）
    - src/styles.css 整文件（找现有 .aster-iconbtn / .aster-selection-pill / --glass-bg / --brand-gradient / --text-3 / --sp-1..--sp-3 等 token 作样式参考）
    - src/App.tsx L25-120（决定 AgentControlBar 插入位置 — 推荐：在 SelectionPill / ContextCard 同级 row 之上，或在 ChatStream 上方独立常驻 bar）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "src/components/AgentControlBar.tsx" 段 L461-570 完整范式
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Pattern 4 L550-593
  </read_first>
  <action>
**步骤 1 — 新建 src/components/AgentControlBar.tsx：**

```tsx
/**
 * src/components/AgentControlBar.tsx — Phase 3 完整版（AGENT-02 / AGENT-12 / AGENT-13）
 *
 * 视觉走 CLAUDE.md §UI 设计系统：玻璃拟态容器 + 品牌渐变 accent（focus / hover）
 *   + 11px 步数小字 + 内联 SVG。
 *
 * 不渲染 soft-landing 卡片（在 ChatStream 内特殊消息 — Plan 05 已落）；
 * 本组件只显示 step counter + pause/resume + abort 按钮。
 *
 * idle 时 return null — 不订阅其它字段（Zustand selector 避免全量 re-render）。
 */
import { type ReactElement } from 'react';
import { useLingui } from '@lingui/react/macro';
import { useAgentStore, MAX_STEPS } from '../agent/agentStore';
import { PauseIcon, PlayIcon, StopIcon } from './icons';

export default function AgentControlBar(): ReactElement | null {
  const { t } = useLingui();
  const status = useAgentStore((s) => s.agentStatus);
  const currentStep = useAgentStore((s) => s.currentStep);
  const pause = useAgentStore((s) => s.pause);
  const resume = useAgentStore((s) => s.resume);
  const abort = useAgentStore((s) => s.abort);

  if (status === 'idle') return null;

  return (
    <div className="aster-agent-bar" role="status" aria-live="polite">
      <span className="aster-agent-bar__step" aria-label={t`当前步骤`}>
        {currentStep} / {MAX_STEPS}
      </span>
      {status !== 'soft-landing' && (
        <button
          type="button"
          className="aster-iconbtn"
          onClick={status === 'paused' ? resume : pause}
          aria-label={status === 'paused' ? t`继续` : t`暂停`}
          title={status === 'paused' ? t`继续` : t`暂停`}
        >
          {status === 'paused' ? <PlayIcon /> : <PauseIcon />}
        </button>
      )}
      <button
        type="button"
        className="aster-iconbtn"
        onClick={() => abort('user')}
        aria-label={t`中止`}
        title={t`中止`}
      >
        <StopIcon />
      </button>
    </div>
  );
}
```

**步骤 2 — 新建 src/components/AgentControlBar.test.tsx：**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, fireEvent } from '@testing-library/react';
import AgentControlBar from './AgentControlBar';
import { useAgentStore } from '../agent/agentStore';

vi.mock('@lingui/react/macro', () => ({
  useLingui: () => ({
    t: (s: TemplateStringsArray) => String.raw({ raw: s }),
    i18n: { _: (s: string) => s },
  }),
}));

describe('AgentControlBar (AGENT-02 / AGENT-13)', () => {
  beforeEach(() => {
    useAgentStore.setState({
      agentStatus: 'idle', currentStep: 0, currentRunId: null,
      controller: null, lastAbortReason: null, runningTools: [],
    });
  });

  it('agentStatus = "idle" → 不渲染', () => {
    const { container } = render(<AgentControlBar />);
    expect(container.firstChild).toBeNull();
  });

  it('agentStatus = "running" currentStep=3 → 渲染 "3 / 20" + 暂停 + 中止', () => {
    useAgentStore.setState({ agentStatus: 'running', currentStep: 3 });
    const { container, getByLabelText } = render(<AgentControlBar />);
    expect(container.textContent ?? '').toMatch(/3 \/ 20/);
    expect(getByLabelText('暂停')).toBeTruthy();
    expect(getByLabelText('中止')).toBeTruthy();
  });

  it('agentStatus = "paused" currentStep=5 → 渲染 "5 / 20" + 继续 + 中止', () => {
    useAgentStore.setState({ agentStatus: 'paused', currentStep: 5 });
    const { container, getByLabelText } = render(<AgentControlBar />);
    expect(container.textContent ?? '').toMatch(/5 \/ 20/);
    expect(getByLabelText('继续')).toBeTruthy();
  });

  it('agentStatus = "soft-landing" → 渲染 step counter + 中止；不显示暂停', () => {
    useAgentStore.setState({ agentStatus: 'soft-landing', currentStep: 20 });
    const { container, getByLabelText, queryByLabelText } = render(<AgentControlBar />);
    expect(container.textContent ?? '').toMatch(/20 \/ 20/);
    expect(getByLabelText('中止')).toBeTruthy();
    expect(queryByLabelText('暂停')).toBeNull();
    expect(queryByLabelText('继续')).toBeNull();
  });

  it('点 pause 按钮（running 态）→ agentStatus = "paused"', () => {
    useAgentStore.setState({ agentStatus: 'running', currentStep: 1 });
    const { getByLabelText } = render(<AgentControlBar />);
    fireEvent.click(getByLabelText('暂停'));
    expect(useAgentStore.getState().agentStatus).toBe('paused');
  });

  it('点 resume 按钮（paused 态）→ agentStatus = "running"', () => {
    useAgentStore.setState({ agentStatus: 'paused', currentStep: 1 });
    const { getByLabelText } = render(<AgentControlBar />);
    fireEvent.click(getByLabelText('继续'));
    expect(useAgentStore.getState().agentStatus).toBe('running');
  });

  it('点 abort 按钮 → lastAbortReason="user"，controller.signal.aborted=true', () => {
    const ctrl = new AbortController();
    useAgentStore.setState({ agentStatus: 'running', currentStep: 1, controller: ctrl });
    const { getByLabelText } = render(<AgentControlBar />);
    fireEvent.click(getByLabelText('中止'));
    expect(useAgentStore.getState().lastAbortReason).toBe('user');
    expect(useAgentStore.getState().agentStatus).toBe('idle');
    expect(ctrl.signal.aborted).toBe(true);
  });
});
```

**步骤 3 — 修改 src/styles.css，追加 .aster-agent-bar 块（用现有 token；新增的 token 也加到 :root 块如缺）：**

```css
/* === AgentControlBar (Phase 3) ============================================= */

.aster-agent-bar {
  display: flex;
  align-items: center;
  gap: var(--sp-2, 8px);
  padding: var(--sp-1, 4px) var(--sp-3, 12px);
  background: var(--glass-bg, rgba(255, 255, 255, 0.7));
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-radius: var(--r-pill, 999px);
  border: 1px solid var(--border-1, rgba(120, 120, 130, 0.18));
  box-shadow: var(--shadow-1, 0 1px 4px rgba(0, 0, 0, 0.06));
  transition: opacity var(--dur, 200ms) var(--ease, ease);
}

.aster-agent-bar__step {
  font-size: 11px;
  font-variant-numeric: tabular-nums;
  color: var(--text-3, #6b6f76);
  white-space: nowrap;
  padding: 0 var(--sp-1, 4px);
}

/* Pause / abort 按钮 hover 用品牌渐变 accent — 只作 accent，不做大面积 */
.aster-agent-bar .aster-iconbtn:hover {
  background: linear-gradient(135deg, #7c3aed, #4f46e5, #2563eb);
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
}

/* 主题暗色 — 走 [data-theme="dark"] 已有 token */
[data-theme="dark"] .aster-agent-bar {
  background: var(--glass-bg, rgba(30, 30, 35, 0.7));
}

/* 减少动效偏好 */
@media (prefers-reduced-motion: reduce) {
  .aster-agent-bar { transition: none; }
}
```

注：如 v1 styles.css 已有 `--glass-bg` / `--brand-gradient` / `--text-3` / `--sp-*` / `--r-pill` 等 token（CLAUDE.md UI 设计系统约定），上面直接用 `var(--xxx, fallback)`；若某 token 不存在，fallback 值已写入保兜底。Plan 08 真机 UAT 时按用户反馈微调间距 / 圆角。

**步骤 4 — 修改 src/App.tsx，在顶部插入 <AgentControlBar />：**

具体位置由 v1 App.tsx 现有 layout 决定。推荐：
- 在 ContextCard / SelectionPill row 之上、ChatStream 之上独立常驻 row
- 用 className="aster-shell__agent-bar" 包一层（如已有 grid layout）

最小侵入示例：

```tsx
import AgentControlBar from './components/AgentControlBar';

// 在 App return 的 JSX 中找到 ChatStream 渲染处上方插入：
<div className="aster-shell__agent-bar">
  <AgentControlBar />
</div>
<ChatStream onSettings={...} />
```

（agentStatus === 'idle' 时 AgentControlBar 自身 return null，不占空间；wrap div 可以加 `display: contents` 或 `display: flex` 让它折叠；如 wrap div 占位太显眼，可直接 `<AgentControlBar />` 不包 div — 取决于 App.tsx 现有 flex/grid 布局。建议 read first 后选具体路径。）

**步骤 5 — 跑测试 + 视觉验证：**

```bash
npm test -- src/components/AgentControlBar.test.tsx --reporter=verbose
npm test
npm run build && npm run size
```

视觉验证延到 Plan 08 真机 UAT。
  </action>
  <verify>
    <automated>npm test -- src/components/AgentControlBar.test.tsx --reporter=verbose 2>&1 | tail -40 && npm test 2>&1 | tail -10 && npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -5 && grep -c "aster-agent-bar" src/styles.css | grep -qv "^0$" && echo "PASS: styles present" && grep -q "AgentControlBar" src/App.tsx && echo "PASS: AgentControlBar mounted in App.tsx"</automated>
  </verify>
  <done>
    - AgentControlBar.tsx 完整版（pause + abort + step counter）
    - AgentControlBar.test.tsx 7 个 it 全绿
    - icons.tsx 含 PauseIcon + PlayIcon
    - styles.css 含 .aster-agent-bar 块
    - App.tsx 挂载 <AgentControlBar />
    - npm test 全套全绿
    - npm run build / npm run size 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentControlBar UI → agentStore | 用户点击事件唯一通过 agentStore.pause/resume/abort('user') 三方法；不直接操作 controller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Repudiation | abort('user') 调用源 | mitigate | 单一 abort 入口（Plan 03 落 AGENT-13）；lastAbortReason 字段记录原因（'user' / 'visibility' / ...）便于 SUMMARY / debug |
| T-06-02 | Denial of service | 用户疯狂点 pause/resume | accept | pause/resume 调用是幂等 set；不调 controller.abort；不引发 race condition |
</threat_model>

<verification>
- npm test 全绿（含 AgentControlBar 7 个 it）
- npm run build 通过
- npm run size ≤ 80KB
- src/App.tsx 含 <AgentControlBar />
- styles.css 含 .aster-agent-bar 块
- icons.tsx 含 PauseIcon + PlayIcon
- 真机视觉验证延到 Plan 08
</verification>

<success_criteria>
- AGENT-02 软着陆触发后 AgentControlBar 仍显示 step counter（"20 / 20"）— soft-landing 卡片由 ChatStream 内特殊消息渲染（Plan 05 已落）
- AGENT-13 单一 abort 入口在 UI 层实现（中止按钮唯一调 abort('user')）
- SC2 「失控控制可观察」完成：用户能看到 step counter + pause/abort 按钮
</success_criteria>

<output>
完成后创建 `.planning/phases/03-agent-loop-privacy-word-demo/03-06-SUMMARY.md`，记录：
- AgentControlBar 完整 JSX 结构图（含 aria-label 列表）
- styles.css 新增 .aster-agent-bar 块的 token 使用列表（--glass-bg / --brand-gradient / 等）
- icons.tsx PauseIcon / PlayIcon SVG path 字面量（给 review 看）
- App.tsx 挂载位置（具体行号 + 上下文）
- Plan 08 真机 UAT 视觉验证清单（玻璃拟态质感 / 渐变 accent / step counter 字号对比）
</output>

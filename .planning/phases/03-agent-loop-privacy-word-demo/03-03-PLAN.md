---
phase: 03-agent-loop-privacy-word-demo
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - .planning/spikes/SP-2-include-usage/findings.md
  - .planning/spikes/SP-6-proxy-await/findings.md
  - .planning/spikes/SP-1-deepseek-multi-tool/probe.mjs
  - .planning/spikes/SP-1-deepseek-multi-tool/findings.md
  - .planning/spikes/SP-3-aihubmix-passthrough/probe.mjs
  - .planning/spikes/SP-3-aihubmix-passthrough/findings.md
  - .planning/spikes/SP-7-three-tool-parallel/findings.md
  - .planning/spikes/SP-4-reverse-ops/probe.tsx
  - .planning/spikes/SP-4-reverse-ops/findings.md
  - .planning/spikes/SP-5-ppt-slide-delete/probe.tsx
  - .planning/spikes/SP-5-ppt-slide-delete/findings.md
  - src/agent/loop.ts
  - src/agent/agentStore.ts
  - src/agent/circuit-breaker.ts
  - src/agent/operationLog.ts
  - src/agent/tools/index.ts
  - src/agent/tools/read/word.ts
  - src/agent/loop.test.ts
  - src/agent/agentStore.test.ts
  - src/agent/tools/index.test.ts
  - src/agent/tools/dispatch.test.ts
  - src/agent/operationLog.test.ts
  - src/providers/openai-compat.ts
  - src/providers/openai-compat.test.ts
  - src/agent/system-prompt.ts
autonomous: false
requirements: [AGENT-01, AGENT-02, AGENT-13]
requirements_addressed: [AGENT-01, AGENT-02, AGENT-13]
user_setup:
  - service: deepseek
    why: "Spike SP-1 / SP-7 类型 ② Claude 自跑需 DeepSeek API Key 抓 SSE raw log 验证多 tool 累积"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek 平台 -> API Keys"
    dashboard_config: []
  - service: aihubmix
    why: "Spike SP-3 类型 ② passthrough 上游 claude-opus / Doubao tool_calls 兼容性"
    env_vars:
      - name: AIHUBMIX_API_KEY
        source: "AiHubMix Dashboard -> API Keys"
    dashboard_config: []
must_haves:
  truths:
    - "spike SP-2 / SP-6 已归档（findings.md PASS — 不跑）"
    - "spike SP-1 / SP-3 / SP-7 Claude 自跑产物落 .planning/spikes/<id>/findings.md + raw-log.txt（脱敏）"
    - "spike SP-4 / SP-5 探测代码（Task Pane 临时按钮组件）落 .planning/spikes/<id>/probe.tsx，等待用户真机跑后由 Claude 归档 findings.md"
    - "src/agent/loop.ts runAgent(prompt, ctx, adapter, signal) ≤ 80 行（D-02）— while step < MAX_STEPS=20 + 内层 for-await streamChat 累积 tool_calls + dispatchTool + push role:'tool' 消息 + 软着陆退出（不直接 abort）"
    - "src/agent/agentStore.ts Zustand store 含 agentStatus / currentStep / currentRunId / controller / lastAbortReason / runningTools / beginRun / setCurrentStep / pause / resume / abort / awaitResume / pushSoftLandingPrompt / continueRun / endRun / runAgent"
    - "src/agent/agentStore.ts abort(reason) 是单一 abort 入口（D-10）— visibility / user / max_steps / circuit 四路全部调它"
    - "src/agent/circuit-breaker.ts 骨架 — recordSuccess / recordFailure / isOpen(toolName) 三函数；Phase 3 isOpen 永远返 false（占位）"
    - "src/agent/operationLog.ts 骨架 — in-memory array + appendOperation + getOperationsByRun（不持久化，PITFALLS A-11）"
    - "src/agent/tools/index.ts dispatchTool 严格 allowlist sanitize（D-15）— AsterError 走 isAsterErrorWithMeta 守卫只读四字段；陌生异常一律兜底 UNSUPPORTED + '宿主操作失败' + '发生错误，请重试'"
    - "ToolError schema 强制 8 枚举 + 四字段（code/message/recoverable/hint）"
    - "loop.test.ts / agentStore.test.ts / tools/index.test.ts / tools/dispatch.test.ts / operationLog.test.ts 全绿"
    - "ERR-02 sanitize 单测：mock tool 抛带 stack/路径/sk- 的 Error → toolResult JSON 不含 /Users/ / __dirname / process.env / sk- 模式"
  artifacts:
    - path: "src/agent/loop.ts"
      provides: "runAgent ≤ 80 行 while runner + softLanding"
      contains: "while (step < MAX_STEPS"
      max_lines: 100
    - path: "src/agent/agentStore.ts"
      provides: "Zustand agentStore + awaitResume promise primitive + 单一 abort(reason)"
      contains: "abort(reason"
    - path: "src/agent/circuit-breaker.ts"
      provides: "骨架 isOpen 永返 false"
      contains: "export function isOpen"
      min_lines: 30
    - path: "src/agent/operationLog.ts"
      provides: "骨架 appendOperation / getOperationsByRun + ReverseDescriptor 类型"
      contains: "export function appendOperation"
    - path: "src/agent/tools/index.ts"
      provides: "ToolDef interface + ToolResult + ToolError + dispatchTool + buildToolsForHost"
      contains: "isAsterErrorWithMeta"
  key_links:
    - from: "src/agent/loop.ts"
      to: "src/providers/openai-compat.ts streamChat"
      via: "for await event of llm.streamChat(messages, config, signal, toolDefs)"
      pattern: "streamChat\\("
    - from: "src/agent/loop.ts"
      to: "src/agent/tools/index.ts dispatchTool"
      via: "依次执行 toolCallsThisTurn"
      pattern: "dispatchTool"
    - from: "src/agent/tools/index.ts dispatchTool"
      to: "src/errors/index.ts isAsterErrorWithMeta"
      via: "类型守卫只读四字段"
      pattern: "isAsterErrorWithMeta"
    - from: "agentStore.abort(reason)"
      to: "AbortController.abort()"
      via: "单一 abort 入口（AGENT-13）"
      pattern: "controller\\?\\.abort\\(\\)"
---

<objective>
落地 Phase 3 主体模块 `src/agent/*` 全套骨架与核心逻辑（D-07 / D-08 子集 — 不含 chatStore 改造，Plan 06 接力）：
- `loop.ts` ≤ 80 行 while runner（AGENT-01 / AGENT-02）
- `agentStore.ts` Zustand 状态机 + pause primitive + 单一 abort 入口（AGENT-13）
- `circuit-breaker.ts` 骨架（Phase 4 ERR-03 填实）
- `operationLog.ts` 骨架（Phase 5 真实回放）
- `tools/index.ts` ToolDef interface + dispatchTool 严格 allowlist sanitize（消费 Plan 02 errors 守卫）
- `tools/read/word.ts` 占位（不进 buildToolsForHost 数组）

同时执行 Phase 3 第一周 spike：归档 SP-2 / SP-6（类型①），Claude 自跑 SP-1 / SP-3 / SP-7（类型②，需用户 `.env.local` 提供 Key），写 SP-4 / SP-5 探测代码（类型③，等用户真机跑）。

Purpose: Plan 06 chatStore thin-delegate / Plan 05 tools/write/word.ts / Plan 07 AgentControlBar 全部依赖本 plan 产出的接口与 Zustand store。spike 在主路径接口落地前跑完，保证 A1/A2 假设验证后才接入 LLM 真实调用。
Output: 5 个新 .ts 模块 + 5 个新 *.test.ts + 7 个 spike findings（含 2 个 probe）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-VALIDATION.md
@.planning/research/PITFALLS.md
@src/store/chat.ts
@src/lib/sse.ts
@src/providers/openai-compat.ts
@src/providers/queue.ts
@src/errors/index.ts

<interfaces>
<!-- Plan 02 已落地：src/errors/index.ts -->
```typescript
export function isAsterErrorWithMeta(e: unknown): e is AsterError & {
  recoverable: boolean;
  hint: string;
};
// 8 子类 + CircuitOpenError / StepLimitError 全部含 recoverable + hint readonly 字段
```

<!-- 本 plan (03) 创建：src/agent/tools/index.ts ToolDef / ToolResult / ToolError -->
```typescript
export interface ToolDef<TArgs = unknown> {
  name: string;
  description: string;
  parameters: object;
  humanLabel: (args: TArgs) => string;
  execute: (args: TArgs, ctx: ToolExecContext) => Promise<ToolResult>;
}
export interface ToolExecContext {
  adapter: DocumentAdapter;
  runId: string;
  stepIndex: number;
  signal: AbortSignal;
}
export interface ToolResult {
  ok: boolean;
  data?: unknown;
  error?: ToolError;
  reverse?: ReverseDescriptor;
}
export interface ToolError {
  code: 'INVALID_ARGS' | 'NOT_FOUND' | 'PERMISSION_DENIED' | 'HOST_API_FAILED'
      | 'PRIVACY_BLOCKED' | 'CIRCUIT_OPEN' | 'STEP_LIMIT' | 'UNSUPPORTED';
  message: string;
  recoverable: boolean;
  hint: string;
}
```

<!-- 现有 SSE event 类型（src/lib/sse.ts）-->
```typescript
type SSEEvent =
  | { type: 'delta'; content: string }
  | { type: 'tool_call_end'; id: string; name: string; arguments: string }
  | { type: 'usage'; ... }       // @deprecated since v2.0（Plan 01 已注释）
  | { type: 'error'; ... };
```

<!-- 现有 openai-compat.streamChat（src/providers/openai-compat.ts）-->
```typescript
async *streamChat(messages, config, signal): AsyncGenerator<SSEEvent>;
// Plan 05 会把签名扩展为含 tools?: Array<...> 参数；本 plan loop.ts 暂调旧签名
// 或在本 plan 内同步扩展并占位调用，由 Plan 05 替换 hardcode INSERT_TO_DOCUMENT_TOOL
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4.1 (checkpoint): 提供 .env.local 的 DEEPSEEK_API_KEY 与 AIHUBMIX_API_KEY 供 Claude 自跑 spike</name>
  <what-built>本 plan 在 Task 4.2 - 4.3 内由 Claude 自跑 spike SP-1 / SP-3 / SP-7 类型 ②（直连 api.deepseek.com / api.aihubmix.com 抓 SSE raw log），需要用户在项目根创建 `.env.local`（已在 .gitignore）写入两个 Key。</what-built>
  <how-to-verify>
1. 检查项目根 `.env.local` 是否存在，包含两行（值由用户自己填）：
   ```
   DEEPSEEK_API_KEY=sk-...
   AIHUBMIX_API_KEY=sk-...
   ```
2. 若仅有 DeepSeek Key、缺 AiHubMix：可继续，Claude 标 SP-3 status=blocked，转 Phase 7 model 兼容矩阵处理（D-25 fallback ②）。
3. 若两 Key 都不愿提供：Claude 全部跳过类型 ②，标 SP-1/SP-3/SP-7 status=skipped，依赖 A1/A2 假设到 Plan 08 Word 真机 UAT 时再验证（风险已知）。
  </how-to-verify>
  <resume-signal>
- 输入 "approved" 或 "已写 .env.local" → Claude 继续 Task 4.2 跑 spike
- 输入 "skip aihubmix" → 跳过 SP-3，仅跑 SP-1 / SP-7
- 输入 "skip all spikes" → 全部 spike 标 skipped，Claude 继续 Task 4.4 落 agent 模块（A1/A2 假设带风险）
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4.2: SP-2 / SP-6 归档不跑（类型 ①）+ SP-4 / SP-5 探测代码（类型 ③，待用户真机跑）</name>
  <files>
    .planning/spikes/SP-2-include-usage/findings.md (NEW),
    .planning/spikes/SP-6-proxy-await/findings.md (NEW),
    .planning/spikes/SP-4-reverse-ops/probe.tsx (NEW),
    .planning/spikes/SP-4-reverse-ops/findings.md (NEW, status=pending),
    .planning/spikes/SP-5-ppt-slide-delete/probe.tsx (NEW),
    .planning/spikes/SP-5-ppt-slide-delete/findings.md (NEW, status=pending)
  </files>
  <read_first>
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 8 整段 §8.1 - §8.5（L1542-1696）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md D-24 / D-25 / D-26 三类分工 + fallback 决策
    - .planning/research/PITFALLS.md A-06（Office.js proxy 跨 await）+ A-19 错误文案泄漏
    - src/adapters/PptAdapter.ts / WordAdapter.ts / ExcelAdapter.ts （SP-4 探测要复用这些 adapter 的 *.run 闭包模式）
  </read_first>
  <action>
**步骤 1 — 创建归档（类型 ①）：**

新建 `.planning/spikes/SP-2-include-usage/findings.md`（D-24 类型①）：

```markdown
# Spike SP-2: DeepSeek stream_options.include_usage:true 在最后 chunk 返完整 usage 字段

**Type:** ① archived（v1 已验过 / cost 砍后不消费）
**Status:** PASS（archived）
**Date:** 2026-05-29
**Source:** ROADMAP Phase 3 Week 1 Spike 段

## 验证目标
DeepSeek-V4 SSE 流末尾 chunk 是否含 `usage: { prompt_tokens, completion_tokens, total_tokens }` 字段。

## 探测方法（不跑，直接归档）
v1 Phase 02 已经实现：`src/lib/sse.ts` 的 `tryParseAndEmitUsage` 解析 chunk.usage（line 305+），v1 CostBadge 在真机里显过 ¥ 值，证明解析路径已通。

## 结论
1. 解析路径 v1 已实现并验证 — 不重跑
2. v2.0 在 /gsd-discuss-phase 3（2026-05-28，D-20 / D-21）整批砍掉 cost 功能，usage 字段不再消费
3. 类型保留兼容陌生 SSE upstream（Plan 01 已在 src/lib/sse.ts SSEUsage 上方加 @deprecated jsdoc）

## Fallback
N/A — 已 archived。如未来 Provider 强制要求 include_usage:false，再删 SSEUsage 类型 + agent loop case 'usage' continue 分支。
```

新建 `.planning/spikes/SP-6-proxy-await/findings.md`：

```markdown
# Spike SP-6: Office.js context proxy 跨 await 边界生命周期

**Type:** ① archived（PITFALLS A-06 已知 100% 复现 + v1 三宿主 adapter 已防御）
**Status:** PASS（archived）
**Date:** 2026-05-29
**Source:** ROADMAP Phase 3 Week 1 Spike 段

## 验证目标
确认 `Word.run / Excel.run / PowerPoint.run` 闭包内的 proxy 对象（Word.Paragraph / Excel.Range / PowerPoint.Slide）跨 await 边界后失效。

## 探测方法（不跑，直接归档）
PITFALLS.md §A-06 给出 100% 复现条件 + Office.js capabilities matrix 文档明确说明 `*.run` 闭包语义。v1 三宿主 adapter 已按「pure data in / pure data out」写过：
- `src/adapters/WordAdapter.ts` insert()/getSelection() 全在 `Word.run` 闭包内消费 proxy 即丢
- `src/adapters/ExcelAdapter.ts` 同
- `src/adapters/PptAdapter.ts` 同

## 结论
1. Phase 3 沿用现有 adapter 接口契约：tool execute 调 adapter method，**不允许** agent 层直接调 Office.js `*.run`
2. Plan 04 `src/agent/tools/index.ts` 添加 ToolDef.execute 签名：输入 plain TArgs，输出 Promise<ToolResult>；proxy 不出 adapter 边界
3. Plan 05 `src/agent/tools/write/word.ts` 调 `adapter.appendParagraph(text)` 而非自开 Word.run

## Fallback
N/A — 已 archived。Phase 5 写 eslint rule 禁 `Excel.* / Word.* / PowerPoint.*` 命名空间进 store action（D-13 提到的 rule，Phase 3 写好但不阻断）。
```

**步骤 2 — 类型 ③ 探测代码（等用户真机跑）：**

新建 `.planning/spikes/SP-4-reverse-ops/probe.tsx`（按 RESEARCH.md L1610-1655 临时 Task Pane 按钮模式）：

```tsx
/**
 * SP-4 探测：三宿主 reverse 操作可达性
 *
 * 临时探测组件 — 用户在 sideloaded Aster Task Pane 里挂一个临时按钮即可跑。
 * Phase 3 收尾时删除（不进 v2 main 路径）。
 *
 * 用法：临时在 src/App.tsx 顶部 import SpikeReversePanel 并渲染；用户在 PPT/Excel/Word 真机
 * sideload Aster，点对应按钮，console 看 raw log，截图发回。
 *
 * 验证目标：
 *   - Word 文档：删除最后一段 — `Word.run` 内 paragraph.delete() 是否可用
 *   - Excel：set_range_values 反操作 — pre-state 抓 .values 后能否覆写
 *   - PPT：slide.delete() Web 端是否真删（SP-5 同时跑会更全面）
 */
import { useState } from 'react';
import { useContext } from 'react';
import { AdapterContext } from '../../../src/context/AdapterContext';

declare const Word: any;
declare const Excel: any;
declare const PowerPoint: any;

export default function SP4ReversePanel() {
  const [log, setLog] = useState<string[]>([]);
  const append = (m: string) => setLog((l) => [...l, `[${new Date().toISOString()}] ${m}`]);

  async function probeWordDeleteLastParagraph() {
    try {
      await Word.run(async (ctx: any) => {
        const paragraphs = ctx.document.body.paragraphs;
        paragraphs.load('items');
        await ctx.sync();
        append(`Word total paragraphs: ${paragraphs.items.length}`);
        if (paragraphs.items.length > 0) {
          const last = paragraphs.items[paragraphs.items.length - 1];
          last.delete();
          await ctx.sync();
          append('Word last paragraph deleted OK');
        }
      });
    } catch (e: any) { append(`Word delete failed: ${e.message ?? e}`); }
  }

  async function probeExcelBeforeImage() {
    try {
      await Excel.run(async (ctx: any) => {
        const range = ctx.workbook.getSelectedRange();
        range.load(['values', 'address']);
        await ctx.sync();
        append(`Excel selected address: ${range.address}`);
        append(`Excel before-image rows: ${range.values?.length}`);
      });
    } catch (e: any) { append(`Excel read failed: ${e.message ?? e}`); }
  }

  async function probePptInsertSlideThenDelete() {
    try {
      await PowerPoint.run(async (ctx: any) => {
        const slides = ctx.presentation.slides;
        slides.load('items');
        await ctx.sync();
        append(`PPT initial slides: ${slides.items.length}`);
        // 仅读，不插不删（SP-5 跑插入+删除组合）
      });
    } catch (e: any) { append(`PPT read failed: ${e.message ?? e}`); }
  }

  return (
    <div style={{ padding: 12, fontSize: 12 }}>
      <h3>SP-4 Reverse Ops Probe</h3>
      <button onClick={probeWordDeleteLastParagraph}>Probe Word delete last paragraph</button>
      <button onClick={probeExcelBeforeImage}>Probe Excel selected before-image</button>
      <button onClick={probePptInsertSlideThenDelete}>Probe PPT slides read</button>
      <pre style={{ whiteSpace: 'pre-wrap', maxHeight: 240, overflow: 'auto' }}>
        {log.join('\n')}
      </pre>
    </div>
  );
}
```

新建 `.planning/spikes/SP-4-reverse-ops/findings.md`：

```markdown
# Spike SP-4: 三宿主 reverse 操作可达性

**Type:** ③ 用户真机
**Status:** pending（等用户跑）
**Date issued:** 2026-05-29

## 验证目标
- Word：paragraph.delete() 跨 await 可用？
- Excel：选区 before-image (.values) 抓取与覆写
- PPT：slides 读取（SP-5 一起跑插入+删除）

## 探测方法
临时 Task Pane 按钮组件：`.planning/spikes/SP-4-reverse-ops/probe.tsx`
用户操作：sideload Aster → 临时挂载 SP4ReversePanel → PPT/Excel/Word 真机点对应按钮 → console 与组件内 `<pre>` 日志截图发回。

## 结果
（等用户跑后由 Claude 填充）

## 结论
（等结果）

## Fallback (D-25 类型 ③)
- Word delete 不可用 → Phase 5 Word inverse 改 snapshot fallback（Claude 提议，用户确认）
- Excel before-image 抓不到 → 同上
- PPT 读 slides 失败 → 与 SP-5 一起处理
```

新建 `.planning/spikes/SP-5-ppt-slide-delete/probe.tsx`：

```tsx
/**
 * SP-5 探测：PPT slide.delete() + Web 反向排序 bug
 */
import { useState } from 'react';

declare const PowerPoint: any;

export default function SP5SlideDeleteProbe() {
  const [log, setLog] = useState<string[]>([]);
  const append = (m: string) => setLog((l) => [...l, `[${new Date().toISOString()}] ${m}`]);

  async function probeInsertAndDelete() {
    try {
      await PowerPoint.run(async (ctx: any) => {
        const slides = ctx.presentation.slides;
        slides.load('items');
        await ctx.sync();
        const before = slides.items.length;
        append(`Initial slides: ${before}`);
        // 用户手动插入一张 slide 后再点 Probe Delete
      });
    } catch (e: any) { append(`Read failed: ${e.message ?? e}`); }
  }

  async function probeDeleteLast() {
    try {
      await PowerPoint.run(async (ctx: any) => {
        const slides = ctx.presentation.slides;
        slides.load('items');
        await ctx.sync();
        if (slides.items.length <= 1) {
          append('Skip: need ≥2 slides to safely test delete');
          return;
        }
        const last = slides.items[slides.items.length - 1];
        last.delete();
        await ctx.sync();
        append('Last slide delete() called OK');

        // 再 load 一次确认
        const slides2 = ctx.presentation.slides;
        slides2.load('items');
        await ctx.sync();
        append(`After delete: ${slides2.items.length}`);
      });
    } catch (e: any) { append(`Delete failed: ${e.message ?? e}`); }
  }

  async function probeSelectedSlidesOrder() {
    try {
      await PowerPoint.run(async (ctx: any) => {
        const selected = ctx.presentation.getSelectedSlides();
        selected.load('items');
        await ctx.sync();
        append(`Selected slides count: ${selected.items.length}`);
        selected.items.forEach((s: any, i: number) => append(`  [${i}] id=${s.id}`));
      });
    } catch (e: any) { append(`Selected order failed: ${e.message ?? e}`); }
  }

  return (
    <div style={{ padding: 12, fontSize: 12 }}>
      <h3>SP-5 PPT slide.delete Probe</h3>
      <button onClick={probeInsertAndDelete}>1) Read initial slide count</button>
      <button onClick={probeDeleteLast}>2) Delete last slide</button>
      <button onClick={probeSelectedSlidesOrder}>3) Check selected slides order</button>
      <pre style={{ whiteSpace: 'pre-wrap', maxHeight: 240, overflow: 'auto' }}>
        {log.join('\n')}
      </pre>
    </div>
  );
}
```

新建 `.planning/spikes/SP-5-ppt-slide-delete/findings.md` 同样 status=pending 模板（参考 SP-4，目标段写 PPT slide.delete + Web 反向排序）。

注意：SP-4 / SP-5 的 probe.tsx **不**进入 main bundle（v2 不 import 它们）；用户跑时手动改一行 App.tsx 临时挂载 — 跑完恢复。
  </action>
  <verify>
    <automated>ls .planning/spikes/SP-2-include-usage/findings.md .planning/spikes/SP-6-proxy-await/findings.md .planning/spikes/SP-4-reverse-ops/probe.tsx .planning/spikes/SP-4-reverse-ops/findings.md .planning/spikes/SP-5-ppt-slide-delete/probe.tsx .planning/spikes/SP-5-ppt-slide-delete/findings.md 2>&1 | wc -l | grep -q "^6$" && echo PASS</automated>
  </verify>
  <done>
    - SP-2 / SP-6 findings.md status=PASS(archived)
    - SP-4 / SP-5 probe.tsx + findings.md(status=pending) 全 6 文件存在
    - probe.tsx 不进 main src 路径（在 .planning/spikes/ 下，TS 不会被打包，npm run build 应无影响）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4.3: Claude 自跑 spike SP-1 / SP-3 / SP-7（类型 ②）— 需 .env.local Key</name>
  <files>
    .planning/spikes/SP-1-deepseek-multi-tool/probe.mjs (NEW),
    .planning/spikes/SP-1-deepseek-multi-tool/findings.md (NEW),
    .planning/spikes/SP-1-deepseek-multi-tool/raw-log.txt (NEW),
    .planning/spikes/SP-3-aihubmix-passthrough/probe.mjs (NEW),
    .planning/spikes/SP-3-aihubmix-passthrough/findings.md (NEW),
    .planning/spikes/SP-3-aihubmix-passthrough/raw-log.txt (NEW),
    .planning/spikes/SP-7-three-tool-parallel/findings.md (NEW)
  </files>
  <read_first>
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 8 §8.2 类型②脚本形态 L1551-1599
    - .planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md D-25 fallback 决策
    - .planning/research/PITFALLS.md A-03（多 tool 并行 tool_calls SSE delta）
    - src/lib/sse.ts L213-345 tool_calls 累积逻辑（要复用 fixture 思路）
    - 验证 .env.local 是否含 DEEPSEEK_API_KEY / AIHUBMIX_API_KEY（Task 4.1 checkpoint）
  </read_first>
  <action>
**前置：若 Task 4.1 checkpoint 标记 skip all spikes：跳过本 task，直接进入 Task 4.4；如 skip aihubmix 仅跑 SP-1 / SP-7。**

**步骤 1 — 写 SP-1 probe.mjs（Node 18+ 原生 fetch + .env.local 注入）：**

```javascript
// .planning/spikes/SP-1-deepseek-multi-tool/probe.mjs
// 验证：DeepSeek-V4 多 tool 并行返回时 SSE tool_calls accum 按 index 主键无串污染
import { writeFile, readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

// 手动解析 .env.local（避免引 dotenv 依赖）
const here = dirname(fileURLToPath(import.meta.url));
const envFile = await readFile(join(here, '../../../.env.local'), 'utf8').catch(() => '');
const env = Object.fromEntries(
  envFile.split('\n').filter(Boolean).filter((l) => !l.startsWith('#'))
    .map((l) => { const i = l.indexOf('='); return [l.slice(0, i), l.slice(i + 1)]; })
);
const KEY = env.DEEPSEEK_API_KEY;
if (!KEY) { console.error('.env.local 缺 DEEPSEEK_API_KEY'); process.exit(1); }

const tools = [
  {
    type: 'function',
    function: {
      name: 'set_title_slide_1',
      description: '设置第 1 张幻灯片标题',
      parameters: { type: 'object', properties: { text: { type: 'string' } }, required: ['text'] },
    },
  },
  {
    type: 'function',
    function: {
      name: 'set_title_slide_2',
      description: '设置第 2 张幻灯片标题',
      parameters: { type: 'object', properties: { text: { type: 'string' } }, required: ['text'] },
    },
  },
  {
    type: 'function',
    function: {
      name: 'set_title_slide_3',
      description: '设置第 3 张幻灯片标题',
      parameters: { type: 'object', properties: { text: { type: 'string' } }, required: ['text'] },
    },
  },
];

const body = {
  model: 'deepseek-v4-flash',
  stream: true,
  tools,
  tool_choice: 'auto',
  messages: [
    { role: 'system', content: '你可以一次性调用多个 tools 完成任务（parallel tool_calls）。' },
    { role: 'user', content: '同时把 slide 1 标题改成「A」、slide 2 改成「B」、slide 3 改成「C」' },
  ],
};

const resp = await fetch('https://api.deepseek.com/chat/completions', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${KEY}` },
  body: JSON.stringify(body),
});

if (!resp.ok) {
  const errText = await resp.text();
  console.error(`HTTP ${resp.status}: ${errText}`);
  process.exit(2);
}

let raw = '';
const reader = resp.body.getReader();
const decoder = new TextDecoder();
while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  raw += decoder.decode(value, { stream: true });
}

// 脱敏：去 sk- 模式与任何 Bearer token 残留
raw = raw.replace(/sk-[A-Za-z0-9_-]+/g, '[REDACTED_SK]');
raw = raw.replace(/Bearer\s+[A-Za-z0-9_-]+/g, 'Bearer [REDACTED]');

await writeFile(join(here, 'raw-log.txt'), raw);
console.log('SP-1 raw log written');

// 简单分析：统计 tool_call_id 出现次数
const idMatches = [...raw.matchAll(/"id"\s*:\s*"(call_[A-Za-z0-9_-]+)"/g)];
const uniqueIds = new Set(idMatches.map((m) => m[1]));
const indexMatches = [...raw.matchAll(/"index"\s*:\s*(\d+)/g)];
const uniqueIndexes = new Set(indexMatches.map((m) => m[1]));
const finishReason = raw.match(/"finish_reason"\s*:\s*"(\w+)"/)?.[1] ?? 'unknown';

console.log(`Unique tool_call_id: ${uniqueIds.size}`);
console.log(`Unique tool_call index: ${uniqueIndexes.size}`);
console.log(`finish_reason: ${finishReason}`);
```

跑：
```bash
node .planning/spikes/SP-1-deepseek-multi-tool/probe.mjs
```

记录输出到 `.planning/spikes/SP-1-deepseek-multi-tool/findings.md`：

```markdown
# Spike SP-1: DeepSeek-V4 多 tool 并行 SSE accum 验证

**Type:** ② Claude 自跑
**Status:** PASS / FAIL / inconclusive（按实际填）
**Date:** 2026-05-29

## 验证目标
DeepSeek-V4 同时返多个 tool_call 时，SSE `tool_calls[i].index` 主键累积是否正确（id 漏发 / 多 tool arguments 串污染验证）。

## 探测方法
.planning/spikes/SP-1-deepseek-multi-tool/probe.mjs：构造 3 个 tool（set_title_slide_1/2/3），引导 LLM 一次性调全部 3 个，抓 SSE raw log，统计 unique id / index / finish_reason。

## 结果
- Unique tool_call_id: N
- Unique tool_call index: N
- finish_reason: tool_calls / stop / ...
- raw log: 见 raw-log.txt（脱敏）

## 结论
（按 N 与 finish_reason 判定，对 PITFALLS A-03 + sse.ts 现有 accum 行为是否吻合）

## Fallback (D-25 类型 ②)
- 若 id 漏发：v1 sse.ts L319 `tc.index ?? 0` 主键已防御，理论无需改动；如真出错，Phase 4 在 sse.ts 加 fixture test 强化
- 若 LLM 不调多 tool 而合并成一个：调整 Plan 08 demo system prompt 引导更强；不影响 Phase 3 主路径
```

**步骤 2 — 写 SP-3 probe.mjs（aihubmix passthrough）：**

```javascript
// .planning/spikes/SP-3-aihubmix-passthrough/probe.mjs
import { readFile, writeFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const envFile = await readFile(join(here, '../../../.env.local'), 'utf8').catch(() => '');
const env = Object.fromEntries(
  envFile.split('\n').filter(Boolean).filter((l) => !l.startsWith('#'))
    .map((l) => { const i = l.indexOf('='); return [l.slice(0, i), l.slice(i + 1)]; })
);
const KEY = env.AIHUBMIX_API_KEY;
if (!KEY) { console.error('.env.local 缺 AIHUBMIX_API_KEY'); process.exit(1); }

// 测试上游模型可由用户指定环境变量 AIHUBMIX_PROBE_MODEL；默认 gpt-4o
const MODEL = env.AIHUBMIX_PROBE_MODEL ?? 'gpt-4o';

const tools = [
  {
    type: 'function',
    function: {
      name: 'echo',
      description: '把传入的 text 原样回显',
      parameters: { type: 'object', properties: { text: { type: 'string' } }, required: ['text'] },
    },
  },
];

const body = {
  model: MODEL,
  stream: true,
  tools,
  tool_choice: 'auto',
  messages: [{ role: 'user', content: '请用 echo 工具回显「hello」' }],
};

const resp = await fetch('https://api.aihubmix.com/v1/chat/completions', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${KEY}` },
  body: JSON.stringify(body),
});

if (!resp.ok) {
  const errText = await resp.text();
  console.error(`HTTP ${resp.status}: ${errText}`);
  process.exit(2);
}

let raw = '';
const reader = resp.body.getReader();
const decoder = new TextDecoder();
while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  raw += decoder.decode(value, { stream: true });
}
raw = raw.replace(/sk-[A-Za-z0-9_-]+/g, '[REDACTED_SK]');
raw = raw.replace(/Bearer\s+[A-Za-z0-9_-]+/g, 'Bearer [REDACTED]');

await writeFile(join(here, 'raw-log.txt'), raw);
const hasToolCall = /"tool_calls"\s*:/.test(raw);
const finishReason = raw.match(/"finish_reason"\s*:\s*"(\w+)"/)?.[1] ?? 'unknown';
console.log(`Has tool_calls: ${hasToolCall} | finish_reason: ${finishReason} | model: ${MODEL}`);
```

跑：
```bash
node .planning/spikes/SP-3-aihubmix-passthrough/probe.mjs
```

记录到 `.planning/spikes/SP-3-aihubmix-passthrough/findings.md`，按实际结果填 status（PASS / FAIL）+ 结论：
- 若 PASS：aihubmix gpt-4o 透传 OpenAI 标准 tool_calls ✓
- 若 FAIL（HTTP 错误 / 无 tool_calls）：Phase 3 demo 仅 DeepSeek；aihubmix LLM 转 Phase 7 兼容矩阵处理

**步骤 3 — SP-7（与 SP-1 同份 raw log + sse.ts parser fixture 验证）：**

`.planning/spikes/SP-7-three-tool-parallel/findings.md`：

```markdown
# Spike SP-7: 三 tool 并行真机 SSE raw log 归档 + sse.ts parser fixture 验证

**Type:** ② Claude 自跑（与 SP-1 同份产物）
**Status:** PASS / FAIL（同 SP-1）
**Date:** 2026-05-29

## 验证目标
PITFALLS A-03 闭环 — 拿真实多 tool SSE raw log 喂给 src/lib/sse.ts parser，验证 emit 3 个 distinct `tool_call_end` 事件，arguments 各自完整无串污染。

## 探测方法
- raw log 来源：`.planning/spikes/SP-1-deepseek-multi-tool/raw-log.txt`
- 喂给 src/lib/sse.ts parser 的方式：
  - Plan 04 Task 4.4 在 src/lib/sse.test.ts 新增一个 fixture test：读 raw-log.txt → 用 makeStream/makeMockResponse 喂给 streamSSE → 累积 events → 断言至少 3 个 tool_call_end 事件，且每个 arguments 是合法 JSON

## 结果
（结合 SP-1 raw log + 后续 sse.test.ts fixture 测试结果填）

## 结论
SP-1 + SP-7 双重确认 sse.ts accum 在多 tool 并行场景无串污染 — Phase 3 主路径接口可直接消费。

## Fallback
若 fixture test 暴露累积 bug：先修 sse.ts，再回头确认 SP-1 raw log 中具体哪个 index/id 触发；不阻塞 Plan 04 agent 模块落地（agent loop 不直接消费 raw SSE，靠 sse.ts emit 的 event 流）。
```

注：若 Task 4.1 用户 skip：跳过本 task，三份 findings.md 全部 status=skipped。
  </action>
  <verify>
    <automated>ls .planning/spikes/SP-1-deepseek-multi-tool/findings.md .planning/spikes/SP-3-aihubmix-passthrough/findings.md .planning/spikes/SP-7-three-tool-parallel/findings.md 2>&1 | wc -l | grep -q "^3$" && echo PASS_FILES_EXIST</automated>
  </verify>
  <done>
    - SP-1 / SP-3 / SP-7 三份 findings.md 存在（status=PASS / FAIL / skipped 任一）
    - SP-1 / SP-3 跑通时含 probe.mjs + raw-log.txt（脱敏）
    - findings.md 含明确结论 + fallback 决策（若 FAIL）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.4: 落地 src/agent/* 全套模块（loop / agentStore / circuit-breaker / operationLog / tools/index / tools/read/word）+ openai-compat 签名扩展 + system-prompt 占位 + 测试</name>
  <!--
    W6 处理：本 task 18 文件偏重；按步骤 1-2 / 3-4 / 5-7 分三 commit 落（每个 commit 单独可通过 build + vitest）：
      Commit A — 骨架 + sanitize 边界（步骤 1-3 + 5）：operationLog / circuit-breaker / tools/index dispatch / tools/read/word 占位 + tests/operationLog.test.ts + tests/tools/index.test.ts + tests/tools/dispatch.test.ts
      Commit B — 主体（步骤 4 + 5）：agentStore.ts + loop.ts + system-prompt.ts + openai-compat.ts 签名扩展 + tests/agentStore.test.ts
      Commit C — 集成测试（步骤 6 + 7）：tests/loop.test.ts（依赖前两 commit 的所有模块都已落）+ openai-compat.test.ts streamChat tools 入参 it
    D-04（每 plan 1 commit）允许 plan 内分多 commit — 三 commit 全部通过 npm test + npm run build。
  -->
  <files>
    src/agent/loop.ts (NEW),
    src/agent/agentStore.ts (NEW),
    src/agent/circuit-breaker.ts (NEW),
    src/agent/operationLog.ts (NEW),
    src/agent/tools/index.ts (NEW),
    src/agent/tools/read/word.ts (NEW),
    src/agent/system-prompt.ts (NEW — B2/B9 + W6 Commit B),
    src/providers/openai-compat.ts (MODIFY — B2/B9 + W6 Commit B 扩签名),
    src/providers/openai-compat.test.ts (MODIFY — Commit C),
    src/agent/loop.test.ts (NEW),
    src/agent/agentStore.test.ts (NEW),
    src/agent/tools/index.test.ts (NEW),
    src/agent/tools/dispatch.test.ts (NEW),
    src/agent/operationLog.test.ts (NEW)
  </files>
  <behavior>
    - Test loop-1: runAgent 在 LLM streaming 返 delta only（无 tool_call_end）→ 自然 break，chatStore 含一条 assistant 消息且 isStreaming=false
    - Test loop-2: runAgent 在 LLM 返 1 个 tool_call_end → dispatchTool 调用 + push role='tool' 消息到 chatStore（含 toolCallId / toolName / toolResult / agentRunId / agentStep）
    - Test loop-3: max_steps soft landing — mock LLM 永远返 tool_call → loop hit 20 → 不调 controller.abort()，push role='tool' toolName='soft-landing' 消息 + agentStore.agentStatus === 'soft-landing'
    - Test loop-4: continueRun(runId) 后 currentStep 归零，agentStatus = 'running'，再跑 20 步
    - Test agentStore-1: pause/resume — agentStatus 'running' → pause() → 'paused' → resume() → 'running'
    - Test agentStore-2: abort sources — abort('user'/'visibility'/'max_steps'/'circuit') 四路全调 controller.abort() + lastAbortReason 字段对应；agentStatus → 'idle'
    - Test agentStore-3: awaitResume 在 paused 时 await 阻塞；resume() 后 resolve；signal abort → reject AbortError
    - Test tools/index-1: buildToolsForHost('word') 返 Array（非 Map）；每个 ToolDef 含 humanLabel function
    - Test dispatch-1: ToolError schema — 4 字段 (code/message/recoverable/hint)，code ∈ 8 枚举
    - Test dispatch-2: ERR-02 sanitize — mock tool 抛 HostApiError('xx', { stack: '/Users/wb.chen/.../foo.ts:42 sk-abc123' }) → toolResult JSON 不含 /Users/ / sk- / __dirname / process.env
    - Test dispatch-3: 陌生异常 → 兜底 UNSUPPORTED + 占位 hint '发生错误，请重试'
    - Test dispatch-4: tool not found → NOT_FOUND
    - Test operationLog-1: appendOperation + getOperationsByRun filter by runId
  </behavior>
  <read_first>
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 1（L1024-1122） + §Deliverable 3（L1177-1217） + §Deliverable 4（L1220-1330）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "src/agent/loop.ts" / "src/agent/agentStore.ts" / "src/agent/circuit-breaker.ts" / "src/agent/operationLog.ts" / "src/agent/tools/index.ts" 段全文 + 测试样板段
    - src/store/chat.ts（for-await switch event.type 现状模式 — loop.ts 抄此结构 + 外层包 while step）
    - src/providers/openai-compat.ts（streamChat 签名 — 本 plan loop.ts 调用时**仍以现有签名 streamChat(messages, config, signal) 调**，Plan 05 把 tools 参数扩展上 — 本 plan 用 const __toolsForLater = ... 暂存 toolDefs，loop.ts 内 streamChat 调用先**不**传 tools，注释 "Plan 05 接力传 tools 参数 + 移除 INSERT_TO_DOCUMENT_TOOL hardcode"）
    - src/providers/queue.ts setupVisibilityAbort — agentStore.beginRun 内挂载 cleanup
    - src/errors/index.ts isAsterErrorWithMeta + AsterError 子类（Plan 02 已落）
    - src/adapters/DocumentAdapter.ts SelectionContext + DocumentAdapter 接口
  </read_first>
  <action>
**步骤 1 — src/agent/operationLog.ts（最简骨架，先落，让 loop.ts import 不报错）：**

```typescript
/**
 * src/agent/operationLog.ts — Phase 3 骨架
 *
 * Phase 5 才实现真实 reverse() 回放（DiffLogPanel undo all）；Phase 3 只埋 append
 * 接口 + 类型，让 tools/write/* 的 execute() 返回 reverse descriptor 时有归宿。
 *
 * In-memory only（PITFALLS A-11）— 不写 localStorage / sessionStorage。
 */

export interface ReverseDescriptor {
  /** 反操作的 tool name（如 'delete_last_paragraph'） */
  tool: string;
  /** 反操作的参数 */
  args: Record<string, unknown>;
}

export interface OperationLogEntry {
  runId: string;
  stepIndex: number;
  toolName: string;
  args: unknown;
  humanLabel: string;
  reverse: ReverseDescriptor;
  timestamp: number;
}

const operationLog: OperationLogEntry[] = [];

export function appendOperation(entry: OperationLogEntry): void {
  operationLog.push(entry);
}

export function getOperationsByRun(runId: string): OperationLogEntry[] {
  return operationLog.filter((o) => o.runId === runId);
}

/** 仅测试用 — 清空 in-memory log。Phase 5 真实实现时考虑用 Map<runId, entries[]> 替代 */
export function __resetOperationLogForTest(): void {
  operationLog.length = 0;
}
```

**步骤 2 — src/agent/circuit-breaker.ts（骨架）：**

```typescript
/**
 * src/agent/circuit-breaker.ts — Phase 3 骨架
 *
 * Phase 4 ERR-03 实现 sliding window 最近 5 次调用 ≥3 次同 code 失败 → isOpen=true。
 * Phase 3 仅占位 — isOpen 永返 false，loop.ts 的判定路径不阻断；
 * recordSuccess / recordFailure 不做事，等 Phase 4 填实。
 */

// Phase 4 ERR-03 实现 sliding window 时填充
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const _failureCounts = new Map<string, Array<{ ts: number; code: string }>>();

export function recordSuccess(_toolName: string): void {
  // Phase 4 ERR-03 实现
}

export function recordFailure(_toolName: string, _code: string): void {
  // Phase 4 ERR-03 实现
}

/**
 * 判断该 tool 是否处于熔断状态。
 * Phase 3 永返 false（骨架不阻断 dispatch）；
 * Phase 4 ERR-03 完整 sliding window 判定后启用。
 */
export function isOpen(_toolName: string): boolean {
  return false;
}
```

**步骤 3 — src/agent/tools/index.ts（核心 dispatch + sanitize，调 Plan 02 isAsterErrorWithMeta）：**

```typescript
/**
 * src/agent/tools/index.ts — Tool registry + dispatchTool（D-07 / D-15）
 *
 * 严格 allowlist sanitize 边界（ERR-02）：
 *   - AsterError 子类（含 isAsterErrorWithMeta 守卫）→ 只读 .code/.message/.hint/.recoverable 四字段
 *   - 陌生异常（非 AsterError）→ 一律兜底 UNSUPPORTED + '宿主操作失败' + '发生错误，请重试'
 *   - 严禁读 err.stack / err.toString() / err.name / 其它字段
 */
import type { DocumentAdapter } from '../../adapters/DocumentAdapter';
import { AsterError, isAsterErrorWithMeta } from '../../errors';
import type { ReverseDescriptor } from '../operationLog';

const FALLBACK_HINT = '发生错误，请重试';

export type ToolErrorCode =
  | 'INVALID_ARGS'
  | 'NOT_FOUND'
  | 'PERMISSION_DENIED'
  | 'HOST_API_FAILED'
  | 'PRIVACY_BLOCKED'
  | 'CIRCUIT_OPEN'
  | 'STEP_LIMIT'
  | 'UNSUPPORTED';

export interface ToolError {
  code: ToolErrorCode;
  message: string;        // 中文，user-readable
  recoverable: boolean;
  hint: string;           // 中文，LLM-readable
}

export interface ToolResult {
  ok: boolean;
  data?: unknown;
  error?: ToolError;
  reverse?: ReverseDescriptor;
}

export interface ToolExecContext {
  adapter: DocumentAdapter;
  runId: string;
  stepIndex: number;
  signal: AbortSignal;
}

export interface ToolDef<TArgs = unknown> {
  name: string;
  description: string;
  parameters: object;                          // JSON schema for LLM
  humanLabel: (args: TArgs) => string;          // D-08 / D-13 强制
  execute: (args: TArgs, ctx: ToolExecContext) => Promise<ToolResult>;
}

export interface ToolCallInvocation {
  id: string;
  name: string;
  arguments: unknown;   // 已 parse 的 args（loop.ts 在 tool_call_end 后 JSON.parse 喂入）
}

/**
 * 把 AsterError category code 映射到 ToolError 8 枚举。
 * 来源 RESEARCH.md §Deliverable 4 §4.2 L1263-1275。
 */
function mapAsterCodeToToolErrorCode(code: string): ToolErrorCode {
  switch (code) {
    case 'KEY_INVALID':   return 'PERMISSION_DENIED';
    case 'QUOTA':         return 'PERMISSION_DENIED';
    case 'IMAGE_QUOTA':   return 'PERMISSION_DENIED';
    case 'CONTEXT':       return 'INVALID_ARGS';
    case 'NETWORK':       return 'HOST_API_FAILED';
    case 'RATE_LIMIT':    return 'HOST_API_FAILED';
    case 'FILTER':        return 'INVALID_ARGS';
    case 'MODEL':         return 'NOT_FOUND';
    case 'HOST_API':      return 'HOST_API_FAILED';
    case 'UNSUPPORTED':   return 'UNSUPPORTED';
    case 'CIRCUIT_OPEN':  return 'CIRCUIT_OPEN';
    case 'STEP_LIMIT':    return 'STEP_LIMIT';
    default:              return 'UNSUPPORTED';
  }
}

function sanitizeFromAsterError(err: AsterError & { recoverable: boolean; hint: string }): ToolError {
  return {
    code: mapAsterCodeToToolErrorCode(err.code),
    message: err.message,         // AsterError 子类构造时的中文字面量（D-15）
    recoverable: err.recoverable,
    hint: err.hint || FALLBACK_HINT,
  };
}

/**
 * 唯一 sanitize 边界 — Loop / 任何调用方一律走此函数。
 */
export async function dispatchTool(
  call: ToolCallInvocation,
  ctx: ToolExecContext,
  tools: ToolDef[],
): Promise<ToolResult> {
  const def = tools.find((t) => t.name === call.name);
  if (!def) {
    return {
      ok: false,
      error: {
        code: 'NOT_FOUND',
        message: `工具 ${call.name} 不存在`,
        recoverable: false,
        hint: '请只调用 tools 列表里声明的工具名',
      },
    };
  }

  try {
    return await def.execute(call.arguments as never, ctx);
  } catch (err) {
    if (isAsterErrorWithMeta(err)) {
      return { ok: false, error: sanitizeFromAsterError(err) };
    }
    // 陌生异常一律兜底：不读 .stack / .message / 其它字段
    return {
      ok: false,
      error: {
        code: 'UNSUPPORTED',
        message: '宿主操作失败',
        hint: FALLBACK_HINT,
        recoverable: false,
      },
    };
  }
}

/**
 * 按 host 返回当前可注册的 ToolDef array（OpenAI tools wire 格式由 caller 转换）。
 * Phase 3 仅 Word host 接 1 个真实 write tool（Plan 05 落 appendParagraph）；
 * 其它 host 返空数组（Phase 4 / 6 填）。
 */
export function buildToolsForHost(host: 'word' | 'excel' | 'ppt'): ToolDef[] {
  switch (host) {
    case 'word':
      // Plan 05 在此 push appendParagraph；Phase 3 主线先空，避免 import 顺序循环
      return [];
    case 'excel':
      return [];
    case 'ppt':
      return [];
    default:
      return [];
  }
}
```

**步骤 4 — src/agent/tools/read/word.ts（占位，不进 buildToolsForHost）：**

```typescript
/**
 * src/agent/tools/read/word.ts — Phase 3 占位（不进 buildToolsForHost('word')）
 *
 * 真正消费在 Phase 4 read tools 全套；本文件存在仅为骨架完整 + Phase 4 接口 stub。
 */
import type { ToolDef, ToolResult } from '../index';

interface GetParagraphCountArgs {
  // empty
}

export const getParagraphCount: ToolDef<GetParagraphCountArgs> = {
  name: 'get_paragraph_count',
  description: '获取 Word 文档段落总数（Phase 4 才上线，目前是骨架占位）',
  parameters: { type: 'object', properties: {}, required: [] },
  humanLabel: () => '读取段落总数',
  async execute(_args, _ctx): Promise<ToolResult> {
    return {
      ok: false,
      error: {
        code: 'UNSUPPORTED',
        message: '该工具尚未在 Phase 3 启用',
        hint: '该工具计划在 Phase 4 上线',
        recoverable: false,
      },
    };
  },
};
```

**步骤 5 — src/agent/agentStore.ts（Zustand 状态机 + awaitResume + 单一 abort 入口）：**

```typescript
/**
 * src/agent/agentStore.ts — Phase 3 agent 状态机（AGENT-13 单一 abort 入口）
 *
 * 4 路 abort（visibility / user / max_steps / circuit）全部通过 abort(reason) 入口；
 * pause / resume 不调 controller.abort（in-flight tool 自然跑完 — A-08）。
 */
import { create } from 'zustand';
import type { DocumentAdapter, SelectionContext } from '../adapters/DocumentAdapter';
import { runAgent as runAgentLoop, MAX_STEPS } from './loop';

export type AgentStatus = 'idle' | 'running' | 'paused' | 'soft-landing';
export type AbortReason = 'visibility' | 'user' | 'max_steps' | 'circuit';

interface RunningTool {
  id: string;
  name: string;
}

interface AgentState {
  agentStatus: AgentStatus;
  currentStep: number;
  currentRunId: string | null;
  controller: AbortController | null;
  lastAbortReason: AbortReason | null;
  runningTools: RunningTool[];

  beginRun(runId: string): AbortController;
  setCurrentStep(n: number): void;
  pause(): void;
  resume(): void;
  abort(reason: AbortReason): void;
  awaitResume(signal: AbortSignal): Promise<void>;
  setSoftLanding(runId: string): void;
  continueRun(): void;
  endRun(): void;
  runAgent(prompt: string, selectionCtx: SelectionContext | undefined, adapter: DocumentAdapter): Promise<void>;
}

export const useAgentStore = create<AgentState>((set, get) => ({
  agentStatus: 'idle',
  currentStep: 0,
  currentRunId: null,
  controller: null,
  lastAbortReason: null,
  runningTools: [],

  beginRun(runId) {
    const controller = new AbortController();
    set({
      agentStatus: 'running',
      currentStep: 0,
      currentRunId: runId,
      controller,
      lastAbortReason: null,
      runningTools: [],
    });
    return controller;
  },

  setCurrentStep(n) {
    set({ currentStep: n });
  },

  pause() {
    if (get().agentStatus === 'running') set({ agentStatus: 'paused' });
  },

  resume() {
    if (get().agentStatus === 'paused') set({ agentStatus: 'running' });
  },

  /** 单一 abort 入口（AGENT-13 / D-10） */
  abort(reason) {
    set({ lastAbortReason: reason, agentStatus: 'idle' });
    get().controller?.abort();
  },

  /** pause primitive — paused 时 await 阻塞；resume 时 resolve；signal abort 时 reject */
  awaitResume(signal) {
    if (get().agentStatus !== 'paused') return Promise.resolve();
    return new Promise((resolve, reject) => {
      const unsub = useAgentStore.subscribe((s, prev) => {
        if (prev.agentStatus === 'paused' && s.agentStatus !== 'paused') {
          unsub();
          resolve();
        }
      });
      const onAbort = () => {
        unsub();
        reject(new DOMException('aborted', 'AbortError'));
      };
      signal.addEventListener('abort', onAbort, { once: true });
    });
  },

  setSoftLanding(_runId) {
    set({ agentStatus: 'soft-landing' });
  },

  /** D-09 软着陆「继续 20 步」— reset counter，同 runId 累计 ≥20 步 */
  continueRun() {
    set({ agentStatus: 'running', currentStep: 0 });
  },

  endRun() {
    set({ agentStatus: 'idle', currentRunId: null, controller: null, currentStep: 0, runningTools: [] });
  },

  async runAgent(prompt, selectionCtx, adapter) {
    const runId = crypto.randomUUID();
    const controller = get().beginRun(runId);
    try {
      await runAgentLoop(prompt, selectionCtx, adapter, controller.signal, runId);
    } finally {
      // endRun 在 loop 内部退出或软着陆后调；这里兜底，避免漏 reset 状态
      if (get().agentStatus !== 'soft-landing') {
        get().endRun();
      }
    }
  },
}));

export const useAgentStatus = () => useAgentStore((s) => s.agentStatus);
export const useCurrentStep = () => useAgentStore((s) => s.currentStep);
export { MAX_STEPS };
```

**步骤 6a — src/providers/openai-compat.ts 签名扩展（INSERT_TO_DOCUMENT_TOOL 移除留给 Plan 04）：**

本 plan 把 streamChat 签名扩到接受可选 `tools` 入参 + 接受任意 messages 数组（user / assistant with tool_calls / tool role 三角色），让 loop.ts 能跑完整 OpenAI tool calling 协议。Plan 04 接力删 INSERT_TO_DOCUMENT_TOOL 常量（v1 hardcode）。

```typescript
// src/providers/openai-compat.ts（修改 streamChat 签名）
async *streamChat(
  messages: ChatMessage[],
  config: LLMConfig,
  signal: AbortSignal,
  tools?: Array<{
    type: 'function';
    function: { name: string; description: string; parameters: object };
  }>,
): AsyncGenerator<SSEEvent> {
  // _startStream 内 body 构造：
  //   if (tools && tools.length > 0) {
  //     body.tools = tools;
  //     body.tool_choice = 'auto';
  //   }
  // INSERT_TO_DOCUMENT_TOOL hardcode 在 Plan 04 删除；本 plan 暂保留
  // （Plan 03 loop.ts 传 tools 参数后，hardcode 路径走 caller-supplied 优先）
}
```

修改要点（本 plan 改 openai-compat 但**不**删 INSERT_TO_DOCUMENT_TOOL）：
1. 签名加 `tools?: ...` 第 4 参数
2. `_startStream` body 构造改为「caller 传 tools 优先；否则保留 v1 hardcode INSERT_TO_DOCUMENT_TOOL 路径作过渡」—— Plan 04 删 hardcode 后只剩 caller-supplied 路径
3. 对应 src/providers/openai-compat.test.ts 加 2 个 it：(a) 传 tools 时 body.tools 等于传入；(b) 不传时回退 v1 hardcode 行为（Plan 04 再加 not-passed → body.tools undefined 的断言）

**步骤 6b — src/agent/system-prompt.ts 占位（Plan 08 refine 内容）：**

```typescript
// src/agent/system-prompt.ts — Phase 3 占位（Plan 08 接力 refine 文本）
type HostKey = 'word' | 'excel' | 'ppt';
const HOST_LABEL: Record<HostKey, string> = {
  word: 'Microsoft Word',
  excel: 'Microsoft Excel',
  ppt: 'Microsoft PowerPoint',
};
export function buildSystemPrompt(host: HostKey): string {
  // 最小占位 — Plan 08 替换为完整 demo prompt（含 parallel tool_calls 引导 + evidence 提示）
  return `你是 Aster — 嵌在 ${HOST_LABEL[host]} 里的 AI 智能代理。回复用简体中文。`;
}
```

**步骤 6c — src/agent/loop.ts 完整版（messages 累积 + 双路径 push + tools 传入 + system prompt + D-02 ≤ 80 行）：**

```typescript
/**
 * src/agent/loop.ts — Phase 3 agent 主路径（AGENT-01 / AGENT-02 / AGENT-13）
 *
 * D-02 预算：while 主体逻辑 ≤ 80 行（jsdoc + import + type 声明 + 纯括号行不计）。
 *
 * OpenAI tool calling 协议关键点：
 *   - messages 数组在循环内累积：每轮 assistant 完成后 push {role:'assistant', content, tool_calls}；
 *     每个 tool dispatch 完 push {role:'tool', tool_call_id, content: JSON.stringify(result)}；
 *   - 这样 LLM 下一轮才知道上一轮 tool 调用结果，否则永远只见 user prompt（违反协议）。
 *
 * 双路径 push（关键）：
 *   - LLM wire messages 数组（局部变量）：tool result 用 JSON.stringify(toolResult)
 *   - chatStore（UI 渲染）：role='tool' message.content = humanLabel(args) 中文人话
 *   两个不是同一个 content，分别落两路（ChatStream 渲染读 chatStore，LLM 调用读局部 messages）。
 */
import { useChatStore } from '../store/chat';
import { useAgentStore } from './agentStore';
import { useProviderStore } from '../store/providers';
import { OpenAICompatibleLLM } from '../providers/openai-compat';
import { buildToolsForHost, dispatchTool, type ToolCallInvocation } from './tools';
import * as breaker from './circuit-breaker';
import { appendOperation } from './operationLog';
import { buildSystemPrompt } from './system-prompt';
import { CircuitOpenError, StepLimitError } from '../errors';
import type { DocumentAdapter, SelectionContext } from '../adapters/DocumentAdapter';

export const MAX_STEPS = 20;

type WireMessage =
  | { role: 'system'; content: string }
  | { role: 'user'; content: string }
  | { role: 'assistant'; content: string; tool_calls?: Array<{ id: string; type: 'function'; function: { name: string; arguments: string } }> }
  | { role: 'tool'; tool_call_id: string; content: string };

function safeParseJSON(s: string): Record<string, unknown> | null {
  try { return JSON.parse(s) as Record<string, unknown>; } catch { return null; }
}

function resolveLLMConfig() {
  const ps = useProviderStore.getState();
  const provider = ps.providers.find((p) => p.id === ps.defaultLLMProviderId);
  if (!provider) throw new Error('未配置默认 LLM Provider');
  return { baseUrl: provider.baseUrl, apiKey: provider.apiKey, model: provider.model };
}

export async function runAgent(
  userPrompt: string,
  _selectionCtx: SelectionContext | undefined,
  adapter: DocumentAdapter,
  signal: AbortSignal,
  runId: string,
): Promise<void> {
  const host = adapter.capabilities().host;
  const tools = buildToolsForHost(host);
  const toolDefs = tools.length > 0
    ? tools.map((t) => ({
        type: 'function' as const,
        function: { name: t.name, description: t.description, parameters: t.parameters },
      }))
    : undefined;
  const llm = new OpenAICompatibleLLM();
  const cfg = resolveLLMConfig();

  // LLM wire messages 累积（本地变量）— 每轮 push assistant + tool result，下轮 LLM 才看得到
  const messages: WireMessage[] = [
    { role: 'system', content: buildSystemPrompt(host) },
    { role: 'user', content: userPrompt },
  ];

  let step = 0;
  while (step < MAX_STEPS) {
    step++;
    if (signal.aborted) return;
    useAgentStore.getState().setCurrentStep(step);
    await useAgentStore.getState().awaitResume(signal);
    if (signal.aborted) return;

    const assistantMsgId = crypto.randomUUID();
    useChatStore.getState().pushMessage?.({
      id: assistantMsgId, role: 'assistant', content: '', isStreaming: true,
      agentRunId: runId, agentStep: step,
    } as never);

    let assistantContent = '';
    const toolCallsThisTurn: ToolCallInvocation[] = [];
    for await (const ev of llm.streamChat(messages as never, cfg as never, signal, toolDefs)) {
      if (ev.type === 'delta') {
        assistantContent += ev.content;
        useChatStore.getState().appendDeltaToMessage?.(assistantMsgId, ev.content);
      } else if (ev.type === 'tool_call_end') {
        const args = safeParseJSON(ev.arguments);
        if (args) toolCallsThisTurn.push({ id: ev.id, name: ev.name, arguments: args });
      }
      // case 'usage': ignored — D-21 SSEUsage @deprecated（Plan 01 已在 sse.ts 标注）
    }
    useChatStore.getState().finalizeMessage?.(assistantMsgId, { isStreaming: false } as never);

    // 累积 assistant turn 到 LLM wire（含 tool_calls 字段供 LLM 下轮匹配 tool_call_id）
    messages.push({
      role: 'assistant', content: assistantContent,
      tool_calls: toolCallsThisTurn.length > 0
        ? toolCallsThisTurn.map((tc) => ({
            id: tc.id, type: 'function' as const,
            function: { name: tc.name, arguments: JSON.stringify(tc.arguments) },
          }))
        : undefined,
    });

    if (toolCallsThisTurn.length === 0) { useAgentStore.getState().endRun(); return; }

    for (const tc of toolCallsThisTurn) {
      if (signal.aborted) return;
      if (breaker.isOpen(tc.name)) {
        useAgentStore.getState().abort('circuit');
        const errInstance = new CircuitOpenError(tc.name);
        useChatStore.getState().pushMessage?.({
          role: 'tool', toolCallId: tc.id, toolName: tc.name,
          toolResult: { ok: false, error: { code: 'CIRCUIT_OPEN', message: errInstance.message, hint: errInstance.hint, recoverable: false } },
          content: errInstance.message, agentRunId: runId, agentStep: step,
        } as never);
        messages.push({ role: 'tool', tool_call_id: tc.id, content: JSON.stringify({ ok: false, error: { code: 'CIRCUIT_OPEN' } }) });
        return;
      }
      const result = await dispatchTool(tc, { adapter, runId, stepIndex: step, signal }, tools);
      if (result.ok) breaker.recordSuccess(tc.name);
      else breaker.recordFailure(tc.name, result.error?.code ?? 'UNSUPPORTED');

      const def = tools.find((t) => t.name === tc.name);
      const humanLabel = def ? def.humanLabel(tc.arguments as never) : tc.name;

      // 双路径：(a) chatStore UI 用 humanLabel；(b) LLM wire messages 用 JSON
      useChatStore.getState().pushMessage?.({
        role: 'tool', toolCallId: tc.id, toolName: tc.name, toolResult: result,
        content: humanLabel, agentRunId: runId, agentStep: step,
      } as never);
      messages.push({ role: 'tool', tool_call_id: tc.id, content: JSON.stringify(result) });

      if (result.reverse && def) {
        appendOperation({
          runId, stepIndex: step, toolName: tc.name, args: tc.arguments,
          humanLabel, reverse: result.reverse, timestamp: Date.now(),
        });
      }
    }
  }

  // hit MAX_STEPS — 软着陆（D-09）：不直接 abort，push 一条特殊消息让用户决定
  const stepLimitErr = new StepLimitError();
  useAgentStore.getState().setSoftLanding(runId);
  useChatStore.getState().pushMessage?.({
    role: 'tool', toolName: 'soft-landing',
    content: 'Aster 觉得这事还没干完，要继续吗？',
    toolResult: { ok: false, error: { code: 'STEP_LIMIT', message: stepLimitErr.message, hint: stepLimitErr.hint, recoverable: true } },
    agentRunId: runId, agentStep: MAX_STEPS,
  } as never);
}
```

D-02 预算说明（80 行 = while 主体逻辑）：
- import / jsdoc / type WireMessage / safeParseJSON / resolveLLMConfig / 纯括号行 **不计**
- 真正计入：`runAgent` 函数体内非纯括号、非注释、非 import 的逻辑行
- 实测计入行数 ~75-80（while 主循环 + tool 内循环 + 软着陆）— 若超 80，先压缩 tool 内循环为辅助函数（次次循环不影响可读性）

verify 命令用精准 awk（排除 import/export/type/interface/jsdoc/空行/纯括号/纯 `};` 行）：

```bash
CODE_LINES=$(awk '
  /^[[:space:]]*$/ {next}
  /^[[:space:]]*(\/\/|\/\*|\*|\*\/)/ {next}
  /^[[:space:]]*(import|export type|export interface|type |interface )/ {next}
  /^[[:space:]]*[})\];]+[[:space:]]*$/ {next}
  {n++}
  END {print n}
' src/agent/loop.ts)
[ "$CODE_LINES" -le 80 ] && echo "D-02 PASS: $CODE_LINES code lines"
```

**步骤 7 — 测试文件（5 个，TDD 路径）：**

- `src/agent/operationLog.test.ts`（最简，先写）：

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { appendOperation, getOperationsByRun, __resetOperationLogForTest } from './operationLog';

describe('operationLog skeleton', () => {
  beforeEach(() => { __resetOperationLogForTest(); });

  it('appendOperation push 到 in-mem log + getOperationsByRun 过滤 runId', () => {
    appendOperation({
      runId: 'r1', stepIndex: 1, toolName: 'append_paragraph',
      args: { text: 'hi' }, humanLabel: '...',
      reverse: { tool: 'delete_last_paragraph', args: {} },
      timestamp: Date.now(),
    });
    appendOperation({
      runId: 'r2', stepIndex: 1, toolName: 'append_paragraph',
      args: { text: 'hello' }, humanLabel: '...',
      reverse: { tool: 'delete_last_paragraph', args: {} },
      timestamp: Date.now(),
    });
    expect(getOperationsByRun('r1')).toHaveLength(1);
    expect(getOperationsByRun('r2')).toHaveLength(1);
    expect(getOperationsByRun('r1')[0].toolName).toBe('append_paragraph');
  });
});
```

- `src/agent/agentStore.test.ts`：

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { useAgentStore } from './agentStore';

describe('agentStore state transitions', () => {
  beforeEach(() => {
    useAgentStore.setState({
      agentStatus: 'idle', currentStep: 0, currentRunId: null,
      controller: null, lastAbortReason: null, runningTools: [],
    });
  });

  it('pause/resume transitions (AGENT-12)', () => {
    useAgentStore.setState({ agentStatus: 'running' });
    useAgentStore.getState().pause();
    expect(useAgentStore.getState().agentStatus).toBe('paused');
    useAgentStore.getState().resume();
    expect(useAgentStore.getState().agentStatus).toBe('running');
  });

  it.each(['visibility', 'user', 'max_steps', 'circuit'] as const)(
    'abort source %s — 调 controller.abort + lastAbortReason 字段 (AGENT-13)',
    (reason) => {
      const ctrl = new AbortController();
      useAgentStore.setState({ controller: ctrl, agentStatus: 'running' });
      useAgentStore.getState().abort(reason);
      expect(useAgentStore.getState().lastAbortReason).toBe(reason);
      expect(useAgentStore.getState().agentStatus).toBe('idle');
      expect(ctrl.signal.aborted).toBe(true);
    },
  );

  it('pause does not abort in-flight tool (A-08)', () => {
    const ctrl = new AbortController();
    useAgentStore.setState({ controller: ctrl, agentStatus: 'running' });
    useAgentStore.getState().pause();
    expect(useAgentStore.getState().agentStatus).toBe('paused');
    expect(ctrl.signal.aborted).toBe(false); // pause 绝不 abort controller
  });

  it('awaitResume — paused 时阻塞；resume 后 resolve', async () => {
    const ctrl = new AbortController();
    useAgentStore.setState({ controller: ctrl, agentStatus: 'paused' });
    let resolved = false;
    const p = useAgentStore.getState().awaitResume(ctrl.signal).then(() => { resolved = true; });
    await new Promise((r) => setTimeout(r, 10));
    expect(resolved).toBe(false);
    useAgentStore.getState().resume();
    await p;
    expect(resolved).toBe(true);
  });

  it('awaitResume — signal abort 时 reject AbortError', async () => {
    const ctrl = new AbortController();
    useAgentStore.setState({ controller: ctrl, agentStatus: 'paused' });
    const p = useAgentStore.getState().awaitResume(ctrl.signal);
    ctrl.abort();
    await expect(p).rejects.toThrow();
  });
});
```

- `src/agent/tools/index.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { buildToolsForHost, type ToolDef } from './index';

describe('ToolDef interface (AGENT-08 TS 强制)', () => {
  it.each(['word', 'excel', 'ppt'] as const)('buildToolsForHost(%s) returns Array (not Map)', (host) => {
    expect(Array.isArray(buildToolsForHost(host))).toBe(true);
  });
  it('Phase 3：所有返回的 ToolDef 必须有 humanLabel function (AGENT-08 humanLabel required)', () => {
    for (const host of ['word', 'excel', 'ppt'] as const) {
      for (const tool of buildToolsForHost(host)) {
        expect(typeof tool.humanLabel).toBe('function');
        expect(typeof tool.execute).toBe('function');
        expect(typeof tool.name).toBe('string');
      }
    }
  });
});
```

- `src/agent/tools/dispatch.test.ts`（ERR-01 schema + ERR-02 sanitize 核心）：

```typescript
import { describe, it, expect, vi } from 'vitest';
import { dispatchTool, type ToolDef } from './index';
import { HostApiError, CircuitOpenError } from '../../errors';
import type { DocumentAdapter } from '../../adapters/DocumentAdapter';

const mockAdapter = {
  capabilities: () => ({ host: 'word' as const, supportsSelectionEvents: true, supportedInserts: ['text'] }),
  getSelection: async () => ({ kind: 'none' as const }),
  onSelectionChanged: () => () => {},
  insert: async () => {},
} as unknown as DocumentAdapter;

describe('dispatchTool — ToolError schema (ERR-01)', () => {
  it('returned error has 4 fields: code/message/recoverable/hint', async () => {
    const tool: ToolDef = {
      name: 'fail_invalid', description: '', parameters: {}, humanLabel: () => '',
      async execute() { throw new HostApiError('Word append_paragraph 失败'); },
    };
    const result = await dispatchTool(
      { id: 'c1', name: 'fail_invalid', arguments: {} },
      { adapter: mockAdapter, runId: 'r1', stepIndex: 1, signal: new AbortController().signal },
      [tool],
    );
    expect(result.ok).toBe(false);
    expect(result.error).toBeDefined();
    const e = result.error!;
    expect(typeof e.code).toBe('string');
    expect(typeof e.message).toBe('string');
    expect(typeof e.recoverable).toBe('boolean');
    expect(typeof e.hint).toBe('string');
    expect(Object.keys(e).sort()).toEqual(['code', 'hint', 'message', 'recoverable']);
  });

  it('code is one of 8 enums', async () => {
    const ALLOWED = ['INVALID_ARGS','NOT_FOUND','PERMISSION_DENIED','HOST_API_FAILED',
                     'PRIVACY_BLOCKED','CIRCUIT_OPEN','STEP_LIMIT','UNSUPPORTED'] as const;
    const tool: ToolDef = {
      name: 'fail_circuit', description: '', parameters: {}, humanLabel: () => '',
      async execute() { throw new CircuitOpenError('append_paragraph'); },
    };
    const result = await dispatchTool(
      { id: 'c1', name: 'fail_circuit', arguments: {} },
      { adapter: mockAdapter, runId: 'r1', stepIndex: 1, signal: new AbortController().signal },
      [tool],
    );
    expect(ALLOWED).toContain(result.error!.code as never);
    expect(result.error!.code).toBe('CIRCUIT_OPEN');
  });

  // B6: 覆盖 10 类 AsterError 子类 → ToolError 8 枚举完整 mapping
  it.each([
    ['KeyInvalidError', () => new (require('../../errors').KeyInvalidError)('Key 无效'), 'PERMISSION_DENIED'],
    ['QuotaExceededError', () => new (require('../../errors').QuotaExceededError)('配额'), 'PERMISSION_DENIED'],
    ['ContextTooLongError', () => new (require('../../errors').ContextTooLongError)('过长'), 'INVALID_ARGS'],
    ['NetworkError', () => new (require('../../errors').NetworkError)('网络'), 'HOST_API_FAILED'],
    ['RateLimitError', () => new (require('../../errors').RateLimitError)('限流'), 'HOST_API_FAILED'],
    ['ContentFilterError', () => new (require('../../errors').ContentFilterError)('过滤'), 'INVALID_ARGS'],
    ['ModelNotFoundError', () => new (require('../../errors').ModelNotFoundError)('模型'), 'NOT_FOUND'],
    ['HostApiError', () => new (require('../../errors').HostApiError)('宿主'), 'HOST_API_FAILED'],
    ['CircuitOpenError', () => new (require('../../errors').CircuitOpenError)('t'), 'CIRCUIT_OPEN'],
    ['StepLimitError', () => new (require('../../errors').StepLimitError)(), 'STEP_LIMIT'],
    ['UnsupportedOperationError', () => new (require('../../errors').UnsupportedOperationError)('不支持'), 'UNSUPPORTED'],
  ] as Array<[string, () => Error, string]>)(
    'AsterError 子类 %s 映射到 ToolError code %s（ERR-01 8 枚举完整 mapping）',
    async (_name, makeErr, expectedCode) => {
      const tool: ToolDef = {
        name: 'm', description: '', parameters: {}, humanLabel: () => '',
        async execute() { throw makeErr(); },
      };
      const result = await dispatchTool(
        { id: 'c1', name: 'm', arguments: {} },
        { adapter: mockAdapter, runId: 'r1', stepIndex: 1, signal: new AbortController().signal },
        [tool],
      );
      expect(result.ok).toBe(false);
      expect(result.error!.code).toBe(expectedCode);
      // 同时验证四字段完整 + 中文 hint 非空
      expect(typeof result.error!.message).toBe('string');
      expect(typeof result.error!.recoverable).toBe('boolean');
      expect((result.error!.hint ?? '').length).toBeGreaterThan(0);
    },
  );

  it('tool not found → NOT_FOUND', async () => {
    const result = await dispatchTool(
      { id: 'c1', name: 'nonexistent_tool', arguments: {} },
      { adapter: mockAdapter, runId: 'r1', stepIndex: 1, signal: new AbortController().signal },
      [],
    );
    expect(result.ok).toBe(false);
    expect(result.error!.code).toBe('NOT_FOUND');
  });
});

describe('dispatchTool — sanitize (ERR-02)', () => {
  it('AsterError 子类：toolResult JSON 不含 stack/path/key（HostApiError 不存 hostError）', async () => {
    const tool: ToolDef = {
      name: 'fail_with_stack', description: '', parameters: {}, humanLabel: () => '',
      async execute() {
        // HostApiError 构造器收到 hostError 但不存（Plan 02 改造）
        throw new HostApiError('Word append_paragraph 失败', {
          stack: 'Error\n  at /Users/wb.chen/.../adapter.ts:142',
          message: 'sk-abc123 process.env.FOO=bar /Users/wb.chen/.aster/secret',
        });
      },
    };
    const result = await dispatchTool(
      { id: 'c1', name: 'fail_with_stack', arguments: {} },
      { adapter: mockAdapter, runId: 'r1', stepIndex: 1, signal: new AbortController().signal },
      [tool],
    );
    expect(result.ok).toBe(false);
    expect(result.error!.code).toBe('HOST_API_FAILED');
    const serialized = JSON.stringify(result);
    expect(serialized).not.toMatch(/__dirname/);
    expect(serialized).not.toMatch(/process\.env/);
    expect(serialized).not.toMatch(/sk-/);
    expect(serialized).not.toMatch(/\/Users\//);
  });

  it('陌生异常（非 AsterError） → 兜底 UNSUPPORTED + 占位 hint', async () => {
    const tool: ToolDef = {
      name: 'fail_raw', description: '', parameters: {}, humanLabel: () => '',
      async execute() {
        const err = new Error('/Users/wb.chen/.../foo.ts:42 Key: sk-abc123 process.env.FOO=bar');
        err.stack = 'Error: ...\n  at /Users/.../...';
        throw err;
      },
    };
    const result = await dispatchTool(
      { id: 'c1', name: 'fail_raw', arguments: {} },
      { adapter: mockAdapter, runId: 'r1', stepIndex: 1, signal: new AbortController().signal },
      [tool],
    );
    expect(result.error!.code).toBe('UNSUPPORTED');
    expect(result.error!.message).toBe('宿主操作失败');
    expect(result.error!.hint).toBe('发生错误，请重试');
    const serialized = JSON.stringify(result);
    expect(serialized).not.toMatch(/\/Users\//);
    expect(serialized).not.toMatch(/sk-/);
    expect(serialized).not.toMatch(/process\.env/);
  });
});
```

- `src/agent/loop.test.ts`（覆盖 AGENT-01 / -02 / -13；mock openai-compat.streamChat 与 chatStore push/append/finalize）：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { useAgentStore } from './agentStore';
import { useChatStore } from '../store/chat';

vi.mock('../providers/openai-compat', () => ({
  OpenAICompatibleLLM: vi.fn(),
}));

// 用 dynamic import 以保证 mock 已注入
let runAgent: typeof import('./loop').runAgent;

beforeEach(async () => {
  vi.resetModules();
  const mod = await import('./loop');
  runAgent = mod.runAgent;
  useAgentStore.setState({ agentStatus: 'idle', currentStep: 0, currentRunId: null, controller: null, lastAbortReason: null, runningTools: [] });
  useChatStore.setState({ messages: [] } as never);
});

const mockAdapter = {
  capabilities: () => ({ host: 'word' as const, supportsSelectionEvents: true, supportedInserts: ['text'] }),
  getSelection: async () => ({ kind: 'none' as const }),
  onSelectionChanged: () => () => {},
  insert: async () => {},
} as never;

function mockStreamChat(events: any[]) {
  const { OpenAICompatibleLLM } = require('../providers/openai-compat');
  (OpenAICompatibleLLM as any).mockImplementation(() => ({
    async *streamChat() { for (const e of events) yield e; },
  }));
}

describe('runAgent — AGENT-01', () => {
  it('LLM delta only（无 tool_calls）→ 自然 break', async () => {
    mockStreamChat([{ type: 'delta', content: 'done' }]);
    const ctrl = useAgentStore.getState().beginRun('r1');
    await runAgent('test', undefined, mockAdapter, ctrl.signal, 'r1');
    expect(useAgentStore.getState().agentStatus).toBe('idle');
  });
});

describe('runAgent — AGENT-02 max_steps soft landing', () => {
  it('hit MAX_STEPS=20 时不调 controller.abort，agentStatus = soft-landing', async () => {
    // mock LLM 每次返一个 tool_call_end，让 loop 跑满 20
    mockStreamChat([
      { type: 'tool_call_end', id: 'c1', name: 'nonexistent', arguments: '{}' },
    ]);
    const ctrl = useAgentStore.getState().beginRun('r2');
    await runAgent('test', undefined, mockAdapter, ctrl.signal, 'r2');
    expect(useAgentStore.getState().agentStatus).toBe('soft-landing');
    expect(ctrl.signal.aborted).toBe(false); // 软着陆不 abort controller
  }, 10000);
});
```

**步骤 8 — 跑测试 + size 验证：**

```bash
npm test -- src/agent --reporter=verbose
npm test
npm run build && npm run size
```

全绿。
  </action>
  <verify>
    <automated>npm test -- src/agent --reporter=verbose 2>&1 | tail -50 && npm test 2>&1 | tail -10 && npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -10 && CODE_LINES=$(awk '/^[[:space:]]*$/ {next} /^[[:space:]]*(\/\/|\/\*|\*|\*\/)/ {next} /^[[:space:]]*(import|export type|export interface|type |interface )/ {next} /^[[:space:]]*[})\];]+[[:space:]]*$/ {next} {n++} END {print n+0}' src/agent/loop.ts) && echo "loop.ts code lines: $CODE_LINES" && [ "$CODE_LINES" -le 80 ] && echo "D-02 PASS (loop.ts $CODE_LINES code lines ≤ 80)"</automated>
  </verify>
  <done>
    - src/agent/* 6 文件全建（loop / agentStore / circuit-breaker / operationLog / tools/index / tools/read/word）
    - 5 个测试文件全建并全绿（loop / agentStore / tools/index / tools/dispatch / operationLog）
    - loop.ts 正文 ≤ 80 行（D-02；jsdoc 注释 + import + type 声明 + 纯括号行不计；用 awk 精准排除验证）
    - agentStore.abort() 是唯一调 controller.abort() 的地方（grep 验证）
    - dispatch.test.ts 6 个 it 全绿（schema 4 字段 + 8 枚举 + tool not found + ERR-02 AsterError sanitize + 陌生异常兜底）
    - npm test 全套全绿
    - npm run build 通过
    - npm run size ≤ 80KB gzipped
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool execute throw → dispatch catch | 跨语义边界；只允许 AsterError 子类四字段 + 兜底 UNSUPPORTED 模板暴露给 LLM |
| AbortController.signal | 4 路 abort（visibility / user / max_steps / circuit）唯一通过 agentStore.abort(reason) 入口；reason 是 enum 受控 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AGENT-01 | Tampering | runAgent while runner | mitigate | MAX_STEPS=20 是 const 且类型 readonly；软着陆 push 消息但不 reset step（需用户显式 continueRun） |
| T-AGENT-02 | Denial of service | LLM 无限 tool_calls 循环 | mitigate | MAX_STEPS=20 fail-safe + 软着陆让用户决定 |
| T-AGENT-13 | Tampering | abort 入口分散 | mitigate | agentStore.abort(reason) 是单一入口；4 路调用全部走它；非 agentStore 内不允许直接 controller.abort() |
| T-ERR-01 | Tampering | ToolError schema | mitigate | TypeScript ToolErrorCode union + interface 强制四字段；dispatch 测试断言 Object.keys 严格 4 |
| T-ERR-02 | Information disclosure | dispatchTool 路径 | mitigate | isAsterErrorWithMeta 守卫只读 .code/.message/.hint/.recoverable；陌生异常一律 UNSUPPORTED 兜底；不读 .stack / .toString / .name |
| T-AGENT-03 | Tampering | circuit-breaker 骨架 isOpen 永返 false | accept | Phase 4 ERR-03 才阻断；Phase 3 骨架是已知占位 |
</threat_model>

<verification>
- npm test 全套全绿
- src/agent/loop.ts 正文（剔除 jsdoc + import + type 声明 + 纯括号行）≤ 80 行（D-02 awk 精准验证）
- grep `controller\?\.abort\(\)` 全代码库唯一命中点在 src/agent/agentStore.ts（AGENT-13 单一入口验证）
- npm run build 通过
- npm run size ≤ 80KB gzipped
- spike 7 份产物（含 2 个归档 + 3 个 Claude 自跑 + 2 个 probe.tsx）全部存在
</verification>

<success_criteria>
- AGENT-01 / AGENT-02 / AGENT-13 单测全覆盖
- ERR-01 / ERR-02 sanitize 边界落地（dispatch.test.ts 6 个 it）
- Plan 05（tools/write/word.ts）、Plan 06（chatStore thin-delegate）、Plan 07（AgentControlBar）三个下游 plan 拿到稳定接口
- Phase 4 ERR-03 / Phase 5 operationLog reverse / Phase 5 humanLabel eslint enforce 三个未来 phase 接力点已埋
</success_criteria>

<output>
完成后创建 `.planning/phases/03-agent-loop-privacy-word-demo/03-04-SUMMARY.md`，记录：
- src/agent/* 6 个新文件每文件实际行数 + 关键 export 列表
- loop.ts 正文行数实测（grep 排除注释后）
- 5 个测试文件实际 it 数 + 全绿截图（命令输出 tail）
- spike SP-1 / SP-3 / SP-7 结果摘要（PASS / FAIL / skipped + 一句结论）
- spike SP-4 / SP-5 probe 路径（提醒用户 Plan 08 真机 UAT 时跑）
- Plan 05 / Plan 06 / Plan 07 三个下游接口对接点（具体 import 路径 + 函数签名）
</output>

---
phase: 03-agent-loop-privacy-word-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/errors/index.ts
  - src/errors/index.test.ts
autonomous: true
requirements: [ERR-01, ERR-02]
requirements_addressed: [ERR-01, ERR-02]
must_haves:
  truths:
    - "AsterError 现有 8 子类全部含 readonly recoverable: boolean + readonly hint: string 字段（中文）"
    - "新增 CircuitOpenError + StepLimitError 两类，都含 readonly recoverable + readonly hint"
    - "HostApiError 构造器不再存 hostError 字段进实例（防 stack/path 跨边界）"
    - "errors/index.test.ts 至少 10 个 it 覆盖每类的 recoverable + hint 字面量是中文"
    - "AsterError 子类 message / hint 均为构造时字面量，不允许嵌入 dynamic 内容（CircuitOpenError 的 toolName 是允许的 literal subset 例外）"
    - "Tool 抛错时 LLM 通过 messages 收到的是不含 stack / 绝对路径（无 /Users/）/ Key 片段（无 sk-）/ process.env 内容 的中文 ToolError 对象 — 用户在 chat 里看到「Word append_paragraph 失败」+「宿主操作可瞬时失败，可重试一次」而非 stack 文本"
  artifacts:
    - path: "src/errors/index.ts"
      provides: "10 个 AsterError 子类 + 四字段 (code/message/recoverable/hint) 强制 + AsterErrorWithMeta 类型守卫 isAsterErrorWithMeta()"
      contains: "export class CircuitOpenError extends AsterError"
    - path: "src/errors/index.ts"
      provides: "StepLimitError"
      contains: "export class StepLimitError extends AsterError"
    - path: "src/errors/index.test.ts"
      provides: "覆盖 10 子类 + 字段类型 + 中文字面量断言"
      min_lines: 80
  key_links:
    - from: "src/errors/index.ts"
      to: "tool dispatch sanitize 边界 (Plan 03)"
      via: "isAsterErrorWithMeta 类型守卫 + 四字段 allowlist"
      pattern: "isAsterErrorWithMeta\\(.*\\)"
---

<objective>
为 Plan 03 (`src/agent/tools/index.ts` dispatchTool) 的「严格 allowlist」sanitize 路径打好类型基础（D-14 / D-15）：让现有 8 个 AsterError 子类全部含 `recoverable: boolean` + `hint: string` 字段，新增 `CircuitOpenError` / `StepLimitError` 两类，并把 `HostApiError` 改为不存 hostError 进实例（防 stack/path 跨 catch 边界传到 LLM）。所有 message/hint 都是构造时的中文字面量，不允许 string interpolation 嵌入 dynamic 内容（CircuitOpenError 的 toolName 例外因来自 literal subset 受控）。

Purpose: ERR-01 schema = 8 枚举强制 + 四字段（code/message/recoverable/hint）。ERR-02 sanitize = AsterError 实例本身就只有四字段可读，杜绝 stack/path/Key 进 toolResult；陌生异常由 Plan 03 dispatch 层兜底（UNSUPPORTED + 占位）。
Output: 修改 1 个 errors 文件 + 新建 1 个 errors 测试文件；Plan 03 dispatchTool 直接消费这套类型守卫。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-VALIDATION.md
@src/errors/index.ts

<interfaces>
<!-- 现有 AsterError 基类签名（src/errors/index.ts L30-50）-->
```typescript
export class AsterError extends Error {
  constructor(message: string, public readonly code: string, public readonly category: string) {
    super(message);
    this.name = 'AsterError';
  }
}
```

<!-- 现有 8 子类（src/errors/index.ts L50-175，从文件读取）-->
```typescript
export class KeyInvalidError extends AsterError { ... }
export class QuotaExceededError extends AsterError { ... }
export class ContextTooLongError extends AsterError { ... }
export class NetworkError extends AsterError { ... }
export class RateLimitError extends AsterError { ... }
export class ContentFilterError extends AsterError { ... }
export class ModelNotFoundError extends AsterError { ... }
export class HostApiError extends AsterError {
  constructor(message: string, public readonly hostError?: unknown) { ... }
}
export class UnsupportedOperationError extends AsterError { ... }
export class ImageQuotaError extends AsterError { ... }  // 若已存在
```

<!-- ToolError 枚举（Plan 03 用，本 plan 不创建但需对齐 mapping，详见 RESEARCH.md §4.2 L1263-1275）-->
```
KEY_INVALID → PERMISSION_DENIED
QUOTA → PERMISSION_DENIED
CONTEXT → INVALID_ARGS
NETWORK → HOST_API_FAILED
RATE_LIMIT → HOST_API_FAILED
FILTER → INVALID_ARGS
MODEL → NOT_FOUND
HOST_API → HOST_API_FAILED
UNSUPPORTED → UNSUPPORTED
CIRCUIT_OPEN → CIRCUIT_OPEN
STEP_LIMIT → STEP_LIMIT
IMAGE_QUOTA → PERMISSION_DENIED
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: AsterError 子类补四字段 + 新增 CircuitOpenError / StepLimitError + 类型守卫</name>
  <files>src/errors/index.ts, src/errors/index.test.ts</files>
  <behavior>
    - Test 1: 每个现有 8 子类（KeyInvalidError / QuotaExceededError / ContextTooLongError / NetworkError / RateLimitError / ContentFilterError / ModelNotFoundError / HostApiError + UnsupportedOperationError + ImageQuotaError 若存在）实例的 `recoverable` 是 boolean，`hint` 是中文字符串非空
    - Test 2: HostApiError 实例没有 `hostError` 字段（防 stack/path 跨边界 — Object.keys(err) 不包含 'hostError'）
    - Test 3: 新增 CircuitOpenError('append_paragraph') 实例 message 含 '工具 append_paragraph 连续失败，已强制停止'，hint = '换个 tool 或换个思路再试'，recoverable=false
    - Test 4: 新增 StepLimitError() 实例 message = '已达单轮 20 步上限'，hint = '已达单轮上限，请确认是否继续'，recoverable=true
    - Test 5: isAsterErrorWithMeta(KeyInvalidError 实例) 返 true；isAsterErrorWithMeta(new Error('foo')) 返 false
    - Test 6: KeyInvalidError 实例 instanceof AsterError 仍为 true（继承链不破坏）
    - Test 7: HostApiError(消息, hostError) 不抛错（向后兼容 v1 调用点），但实例不存 hostError
  </behavior>
  <read_first>
    - src/errors/index.ts （整文件 — 准确列出每个子类当前签名 + 已有 code 字符串）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 4 §4.1 "AsterError 基类四字段强制" L1224-1255 + §Code Example B L734-779
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "src/errors/index.ts (modify, 补四字段 + 新增 2 类)" 段
    - .planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md D-14 / D-15
  </read_first>
  <action>
**步骤 1 — 写 src/errors/index.test.ts (vitest 单测，先写不通过的红测，TDD RED 阶段)：**

```typescript
import { describe, it, expect } from 'vitest';
import {
  AsterError,
  KeyInvalidError,
  QuotaExceededError,
  ContextTooLongError,
  NetworkError,
  RateLimitError,
  ContentFilterError,
  ModelNotFoundError,
  HostApiError,
  UnsupportedOperationError,
  CircuitOpenError,
  StepLimitError,
  isAsterErrorWithMeta,
} from './index';

describe('AsterError 子类四字段（ERR-01）', () => {
  const subclasses = [
    new KeyInvalidError('Key 无效'),
    new QuotaExceededError('配额'),
    new ContextTooLongError('过长'),
    new NetworkError('网络'),
    new RateLimitError('限流'),
    new ContentFilterError('过滤'),
    new ModelNotFoundError('模型'),
    new HostApiError('宿主'),
    new UnsupportedOperationError('不支持'),
    new CircuitOpenError('append_paragraph'),
    new StepLimitError(),
  ];

  it.each(subclasses)('$constructor.name 有 recoverable boolean + hint 中文非空 + code + message', (err) => {
    expect(typeof (err as any).recoverable).toBe('boolean');
    expect(typeof (err as any).hint).toBe('string');
    expect((err as any).hint.length).toBeGreaterThan(0);
    expect(typeof err.code).toBe('string');
    expect(typeof err.message).toBe('string');
    expect(err).toBeInstanceOf(AsterError);
  });
});

describe('HostApiError 不存 hostError 字段（ERR-02 防 stack/path 跨边界）', () => {
  it('Object.keys 不含 hostError 即使构造时传了 hostError', () => {
    const fakeHostErr = { stack: '/Users/wb.chen/foo.ts:42 sk-abc123' };
    const err = new HostApiError('Word append_paragraph 失败', fakeHostErr);
    expect(Object.keys(err)).not.toContain('hostError');
    expect((err as any).hostError).toBeUndefined();
  });
});

describe('CircuitOpenError（新增）', () => {
  it('message 含 toolName interpolation + hint 字面量', () => {
    const err = new CircuitOpenError('append_paragraph');
    expect(err.message).toContain('append_paragraph');
    expect(err.message).toContain('连续失败');
    expect(err.hint).toBe('换个 tool 或换个思路再试');
    expect(err.recoverable).toBe(false);
    expect(err.code).toBe('CIRCUIT_OPEN');
  });
});

describe('StepLimitError（新增）', () => {
  it('字面量 message + hint + recoverable=true', () => {
    const err = new StepLimitError();
    expect(err.message).toBe('已达单轮 20 步上限');
    expect(err.hint).toBe('已达单轮上限，请确认是否继续');
    expect(err.recoverable).toBe(true);
    expect(err.code).toBe('STEP_LIMIT');
  });
});

describe('isAsterErrorWithMeta 类型守卫', () => {
  it('真：AsterError 子类带 recoverable+hint', () => {
    expect(isAsterErrorWithMeta(new KeyInvalidError('x'))).toBe(true);
    expect(isAsterErrorWithMeta(new HostApiError('x'))).toBe(true);
    expect(isAsterErrorWithMeta(new CircuitOpenError('t'))).toBe(true);
  });
  it('假：普通 Error / 字符串 / null / undefined', () => {
    expect(isAsterErrorWithMeta(new Error('plain'))).toBe(false);
    expect(isAsterErrorWithMeta('string')).toBe(false);
    expect(isAsterErrorWithMeta(null)).toBe(false);
    expect(isAsterErrorWithMeta(undefined)).toBe(false);
  });
});
```

跑 `npm test -- src/errors/index.test.ts` 确认 RED（编译失败或断言失败均算 RED）。

**步骤 2 — 修改 src/errors/index.ts，GREEN 阶段：**

在每个现有子类内追加 `public readonly recoverable: boolean` 与 `public readonly hint: string` 字段（构造器内或字段声明处赋值字面量中文）。8 子类建议字段值（CONTEXT.md D-15 中文 hint，简短可操作）：

```typescript
export class KeyInvalidError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '请前往设置更新 API Key';
  constructor(message: string) { super(message, 'KEY_INVALID', 'provider'); }
}

export class QuotaExceededError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '配额已用完，请检查 Provider 账户余额或换 Provider';
  constructor(message: string) { super(message, 'QUOTA', 'provider'); }
}

export class ContextTooLongError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '请缩短对话或清空历史后重试';
  constructor(message: string) { super(message, 'CONTEXT', 'provider'); }
}

export class NetworkError extends AsterError {
  public readonly recoverable = true;
  public readonly hint = '网络异常，请检查连接后重试';
  constructor(message: string) { super(message, 'NETWORK', 'provider'); }
}

export class RateLimitError extends AsterError {
  public readonly recoverable = true;
  public readonly hint = '请稍后再试（已退避）';
  constructor(message: string, public readonly retryAfterSeconds?: number) {
    super(message, 'RATE_LIMIT', 'provider');
  }
}

export class ContentFilterError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '内容被 Provider 过滤，请改写提示';
  constructor(message: string) { super(message, 'FILTER', 'provider'); }
}

export class ModelNotFoundError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '请到设置确认模型名称是否正确';
  constructor(message: string) { super(message, 'MODEL', 'provider'); }
}

export class HostApiError extends AsterError {
  public readonly recoverable = true;
  public readonly hint = '宿主操作可瞬时失败，可重试一次';
  constructor(message: string, _hostError?: unknown) {
    super(message, 'HOST_API', 'adapter');
    // ⚠ 关键：不把 _hostError 存到实例字段上（防 stack/path 跨 catch 边界传到 LLM）。
    // 调试需要时由 adapter 层 console.warn 直接打到 DevTools，不挂在 error 实例上。
    void _hostError;
  }
}

export class UnsupportedOperationError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '该操作在当前宿主不支持';
  constructor(message: string) { super(message, 'UNSUPPORTED', 'adapter'); }
}

// 若 v1 有 ImageQuotaError，补：
export class ImageQuotaError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '图像配额已用完，请稍后再试或换 Provider';
  constructor(message: string) { super(message, 'IMAGE_QUOTA', 'provider'); }
}
```

新增两类（追加到文件末尾）：

```typescript
export class CircuitOpenError extends AsterError {
  public readonly recoverable = false;
  public readonly hint = '换个 tool 或换个思路再试';
  constructor(toolName: string) {
    // toolName 来自 tool registry 受控 string literal subset，允许 interpolation
    super(`工具 ${toolName} 连续失败，已强制停止`, 'CIRCUIT_OPEN', 'adapter');
  }
}

export class StepLimitError extends AsterError {
  public readonly recoverable = true;
  public readonly hint = '已达单轮上限，请确认是否继续';
  constructor() {
    super('已达单轮 20 步上限', 'STEP_LIMIT', 'adapter');
  }
}
```

类型守卫（追加到文件末尾）：

```typescript
/**
 * 类型守卫：判断 unknown 是否 AsterError 子类且带 recoverable + hint 元数据。
 * Plan 03 dispatchTool 的 sanitize 路径用这个守卫决定是否走 "只取四字段" 路径。
 */
export function isAsterErrorWithMeta(e: unknown): e is AsterError & {
  recoverable: boolean;
  hint: string;
} {
  return (
    e instanceof AsterError &&
    typeof (e as { recoverable?: unknown }).recoverable === 'boolean' &&
    typeof (e as { hint?: unknown }).hint === 'string'
  );
}
```

**注意（D-15 关键约束）：**
- message / hint 字面量中文，**不允许** string interpolation 嵌入 dynamic 内容（如 stack / path / err.message）
- 唯一例外：CircuitOpenError 的 toolName interpolation，因 toolName 来自 tool registry literal subset 受控（Plan 03 buildToolsForHost 返 array 的 `name` 字段是 'append_paragraph' / 'get_paragraph_count' 等 string literal type）
- HostApiError 构造器收到 `_hostError` 参数但不存进字段，向后兼容 v1 既有调用点（WordAdapter / ExcelAdapter / PptAdapter 现有 `throw new HostApiError('xx', err)` 不需要改）

**步骤 3 — 跑 npm test 全绿（GREEN）：**

```bash
npm test -- src/errors/index.test.ts
npm test
```

确认全套测试通过 + v1 旧调用点（adapter / provider / chatStore）的 HostApiError(msg, err) 调用仍编译过（因为构造器签名兼容 `_hostError?: unknown`）。
  </action>
  <verify>
    <automated>npm test -- src/errors/index.test.ts --reporter=verbose 2>&1 | tail -40 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - src/errors/index.ts 8 子类 + 2 新增 + HostApiError 改造 + isAsterErrorWithMeta 守卫
    - src/errors/index.test.ts ≥ 5 个 describe + ≥ 10 个 it 全绿
    - npm test 全套全绿
    - grep `hostError` 在 src/errors/index.ts 中只出现在构造参数名 `_hostError`（不再有 `this.hostError`）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter throw → dispatch catch | 任何宿主异常进入 catch 后只暴露 AsterError 子类的 readonly 四字段；hostError 原始对象不挂在 AsterError 实例上 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ERR-01 | Information disclosure | src/errors/index.ts HostApiError | mitigate | 构造器收到 hostError 参数但不存进实例字段；调试用 console.warn 由 adapter 层打到 DevTools，不跨 catch 边界 |
| T-ERR-02 | Information disclosure | AsterError message / hint | mitigate | 字段为构造时中文字面量；不允许 string interpolation 嵌入 dynamic 内容（CircuitOpenError toolName 是受控 literal subset 例外）|
| T-ERR-03 | Tampering | 类型守卫 isAsterErrorWithMeta | mitigate | dispatch 层只走守卫返 true 的分支读四字段；其余路径走兜底 UNSUPPORTED |
</threat_model>

<verification>
- npm test 全绿（含 src/errors/index.test.ts ≥ 10 it）
- HostApiError 实例 Object.keys 不含 'hostError'
- 8 现有子类 + 2 新增子类全部含 recoverable + hint
- v1 既有 HostApiError(msg, err) 调用点仍编译通过（向后兼容）
- Plan 03 dispatchTool 可以直接 import { isAsterErrorWithMeta } from '../../errors'
</verification>

<success_criteria>
- ERR-01 schema 基础在类型层落地（四字段强制 + 8+2 = 10 子类全覆盖）
- ERR-02 防 stack/path 跨边界的「不存原始 hostError」策略实施
- Plan 03 dispatchTool 接入这套类型守卫无障碍
</success_criteria>

<output>
完成后创建 `.planning/phases/03-agent-loop-privacy-word-demo/03-02-SUMMARY.md`，记录：
- 8 个现有子类 hint 字面量列表（一行一类）
- 2 新增类签名 + 字段值
- HostApiError 改造前后对比（hostError 字段是否在 this 上）
- isAsterErrorWithMeta 守卫使用例（给 Plan 03 看）
</output>

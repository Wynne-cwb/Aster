---
phase: 03-agent-loop-privacy-word-demo
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/adapters/WordAdapter.ts
  - src/adapters/WordAdapter.test.ts
  - src/agent/tools/write/word.ts
  - src/agent/tools/write/word.test.ts
  - src/agent/tools/index.ts
  - src/providers/openai-compat.ts
  - src/providers/openai-compat.test.ts
  - eslint.config.js
  - src/agent/tools/index.types.test.ts
autonomous: true
requirements: [AGENT-08]
requirements_addressed: [AGENT-08]
must_haves:
  truths:
    - "WordAdapter 类有新方法 appendParagraph(text: string): Promise<void>，内部 Word.run + insertParagraph(text, Word.InsertLocation.end) + ctx.sync()"
    - "src/agent/tools/write/word.ts 导出 appendParagraph: ToolDef<{text:string}>，含 humanLabel + execute + reverse descriptor"
    - "ToolDef.humanLabel({text}) 截 30 字符，> 30 字符追加 '…'"
    - "ToolDef.execute 内 cast 后调 adapter.appendParagraph(text)；返回 { ok:true, data:{written:text.length}, reverse:{tool:'delete_last_paragraph', args:{}} }"
    - "buildToolsForHost('word') 返回数组含 appendParagraph（Phase 3 唯一真实 write tool）"
    - "openai-compat.streamChat 签名 tools 入参（Plan 03 已扩签名；本 plan 删 hardcode）— 完成 dynamic 替换"
    - "openai-compat.ts 删除 INSERT_TO_DOCUMENT_TOOL 常量（src/ 全无命中，types.test.ts 内文本除外）"
    - "src/agent/tools/index.types.test.ts 含 @ts-expect-error type-only test — 删 @ts-expect-error 后 tsc 报错（B4 / AGENT-08：TS 强制 humanLabel 字段验证）"
    - "eslint.config.js 含自写 rule 'aster/require-human-label'（仅 warn，不 error；Phase 5 flip 开关）+ 注释解释为何不 enforce"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "appendParagraph method"
      contains: "Word.InsertLocation.end"
    - path: "src/agent/tools/write/word.ts"
      provides: "appendParagraph ToolDef 完整版"
      contains: "在文档末尾追加段落"
    - path: "src/agent/tools/index.ts"
      provides: "buildToolsForHost('word') 含 appendParagraph"
      contains: "appendParagraph"
    - path: "src/providers/openai-compat.ts"
      provides: "streamChat 动态 tools 入参；INSERT_TO_DOCUMENT_TOOL hardcode 移除"
      contains: "tools?:"
    - path: "eslint.config.js"
      provides: "aster/require-human-label rule 写好但 Phase 3 不 enforce 注释"
      contains: "require-human-label"
  key_links:
    - from: "src/agent/tools/write/word.ts execute"
      to: "src/adapters/WordAdapter.ts appendParagraph"
      via: "(ctx.adapter as WordAdapter).appendParagraph(text)"
      pattern: "adapter as WordAdapter"
    - from: "src/agent/loop.ts streamChat 调用"
      to: "src/providers/openai-compat.ts streamChat"
      via: "tools 参数从 buildToolsForHost 拼出"
      pattern: "streamChat.*tools"
---

<objective>
落地 Phase 3 唯一真实 write tool 与 LLM 动态 tool 注入：
1. `WordAdapter.appendParagraph(text)` — 单一方法走 `Word.run` 闭包；输入 string、输出 `Promise<void>`；A-06 「pure data in/out」边界
2. `src/agent/tools/write/word.ts` `appendParagraph` ToolDef — humanLabel 中文截 30 字符、execute 调 adapter 方法、reverse descriptor 占位（Phase 5 才真实回放）
3. `buildToolsForHost('word')` 注册此 tool，agent loop 跑 Word demo 时拿得到
4. `openai-compat.streamChat` 扩展 `tools` 入参 + 移除 `INSERT_TO_DOCUMENT_TOOL` hardcode（v1 单 tool 路径替换为 v2 动态 registry）
5. eslint rule `aster/require-human-label` 写好但 Phase 3 不 enforce（D-13）

Purpose: Plan 08 Word 真机 UAT demo prompt「写 3 段关于跨境电商物流的内容」要跑通必须这条链路：InputBar → chatStore.sendMessage → agentStore.runAgent → loop.streamChat(tools) → tool_call_end({name:'append_paragraph',...}) → dispatchTool → tools/write/word.ts execute → WordAdapter.appendParagraph → Word.run。
Output: 修改 3 文件 + 新建 1 ToolDef + 新建 2 测试 + eslint rule。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md
@.planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md
@src/adapters/WordAdapter.ts
@src/adapters/DocumentAdapter.ts
@src/providers/openai-compat.ts
@src/agent/tools/index.ts
@eslint.config.js

<interfaces>
<!-- Plan 03 已落 src/agent/tools/index.ts（ToolDef interface + ToolError 枚举 + dispatchTool + buildToolsForHost 空实现）；本 plan (04) modify 它在 buildToolsForHost(\'word\') case 内 push appendParagraph -->
```typescript
export interface ToolDef<TArgs = unknown> {
  name: string;
  description: string;
  parameters: object;
  humanLabel: (args: TArgs) => string;
  execute: (args: TArgs, ctx: ToolExecContext) => Promise<ToolResult>;
}
export interface ToolExecContext {
  adapter: DocumentAdapter;
  runId: string;
  stepIndex: number;
  signal: AbortSignal;
}
export function buildToolsForHost(host: 'word'|'excel'|'ppt'): ToolDef[];
```

<!-- WordAdapter v1 现状（src/adapters/WordAdapter.ts L88-115）-->
```typescript
async insert(content: InsertableContent): Promise<void> {
  // Word.run try/catch + HostApiError 包装
  // Word.InsertLocation.replace / .after / .end 三种位置
}
```

<!-- openai-compat v1 现状（src/providers/openai-compat.ts L24-48 + L50-83）-->
```typescript
const INSERT_TO_DOCUMENT_TOOL = { ... };   // ← 整段删
async *streamChat(messages, config, signal): AsyncGenerator<SSEEvent>;
// _startStream 内 body.tools = [INSERT_TO_DOCUMENT_TOOL]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: WordAdapter.appendParagraph + tools/write/word.ts ToolDef + 注册到 buildToolsForHost</name>
  <files>
    src/adapters/WordAdapter.ts (MODIFY),
    src/adapters/WordAdapter.test.ts (MODIFY/NEW),
    src/agent/tools/write/word.ts (NEW),
    src/agent/tools/write/word.test.ts (NEW),
    src/agent/tools/index.ts (MODIFY — buildToolsForHost 'word' case)
  </files>
  <behavior>
    - Test WordAdapter-1: 调 wordAdapter.appendParagraph('hello') 后 mock 的 Word.run 被调一次，含 insertParagraph('hello', Word.InsertLocation.end) + ctx.sync()
    - Test WordAdapter-2: Word.run 抛错时 appendParagraph 抛 HostApiError，且 catch 内不读 err.stack
    - Test tools/write/word-1: appendParagraph.humanLabel({text:'短文本'}) === '在文档末尾追加段落「短文本」'
    - Test tools/write/word-2: appendParagraph.humanLabel({text:'a'.repeat(50)}) 包含 '…' 且长度 ≤ 32 字符（'在文档末尾追加段落「' + 30 字符 + '…」'）
    - Test tools/write/word-3: appendParagraph.execute({text:'段1'}, ctx) → ctx.adapter.appendParagraph('段1') 被调；返 ok:true + data.written=1 + reverse:{tool:'delete_last_paragraph', args:{}}
    - Test tools/index-update: buildToolsForHost('word') 含且仅含 1 个 ToolDef，name === 'append_paragraph'
  </behavior>
  <read_first>
    - src/adapters/WordAdapter.ts 整文件（L1-130 — 模式：Word.run try/catch HostApiError）
    - src/adapters/DocumentAdapter.ts L17-52 InsertableContent / SelectionContext
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 5 §5.1-5.3 L1331-1364 + §Example A L698-730
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "src/agent/tools/write/word.ts" 段 L361-431 + "src/adapters/WordAdapter.ts (modify, 加 appendParagraph)" 段
    - src/adapters/WordAdapter.test.ts 已有 mock Word.run 模式（如有；若无，仿 src/adapters/DocumentAdapter.test.ts）
  </read_first>
  <action>
**步骤 1 — 修改 src/adapters/WordAdapter.ts：在 class WordAdapter 内追加 appendParagraph 方法（位置：现 insert() 方法之后）：**

```typescript
/**
 * 在文档末尾追加一段文本（Phase 3 Word demo write tool 走的方法）。
 *
 * A-06 边界：输入纯 string；Word.run 闭包内消费 ctx.document.body proxy 即丢；
 *           出闭包前 await ctx.sync() 让操作生效；不返回任何 proxy 对象。
 */
async appendParagraph(text: string): Promise<void> {
  try {
    await Word.run(async (ctx) => {
      ctx.document.body.insertParagraph(text, Word.InsertLocation.end);
      await ctx.sync();
    });
  } catch (err) {
    // HostApiError 构造器收到 err 但不存 hostError（Plan 02 改造）
    throw new HostApiError('Word append_paragraph 失败', err);
  }
}
```

import 段确认 `HostApiError` 已 import（v1 已有，因为 insert() 也抛它）。

**步骤 2 — 修改/新建 src/adapters/WordAdapter.test.ts：**

如已存在 WordAdapter.test.ts，追加 describe('appendParagraph')；如无，新建。仿 v1 已有 mock 模式 — Office.js Word 全局 mock：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { WordAdapter } from './WordAdapter';
import { HostApiError } from '../errors';

declare const globalThis: any;

describe('WordAdapter.appendParagraph', () => {
  let insertParagraph: any;
  let sync: any;

  beforeEach(() => {
    insertParagraph = vi.fn();
    sync = vi.fn().mockResolvedValue(undefined);
    globalThis.Word = {
      InsertLocation: { end: 'End' as any },
      run: vi.fn(async (cb: any) => {
        await cb({ document: { body: { insertParagraph } }, sync });
      }),
    };
  });

  it('调 Word.run 内 insertParagraph(text, Word.InsertLocation.end) + ctx.sync()', async () => {
    const adapter = new WordAdapter();
    await adapter.appendParagraph('hello world');
    expect(globalThis.Word.run).toHaveBeenCalledTimes(1);
    expect(insertParagraph).toHaveBeenCalledWith('hello world', 'End');
    expect(sync).toHaveBeenCalled();
  });

  it('Word.run 抛错时抛 HostApiError（HostApiError 实例不存 hostError）', async () => {
    globalThis.Word.run = vi.fn(async () => { throw new Error('rich api error'); });
    const adapter = new WordAdapter();
    let caught: unknown = null;
    try {
      await adapter.appendParagraph('x');
    } catch (e) {
      caught = e;
    }
    expect(caught).toBeInstanceOf(HostApiError);
    expect(Object.keys(caught as object)).not.toContain('hostError');
  });
});
```

**步骤 3 — 新建 src/agent/tools/write/word.ts：**

```typescript
/**
 * src/agent/tools/write/word.ts — Word host write tools
 *
 * Phase 3 唯一真实 tool = append_paragraph（D-12）。
 * Phase 5 才补 delete_last_paragraph（作为 reverse 的真实回放）。
 */
import type { ToolDef, ToolResult } from '../index';
import type { ReverseDescriptor } from '../../operationLog';
import type { WordAdapter } from '../../../adapters/WordAdapter';

interface AppendParagraphArgs {
  text: string;
}

export const appendParagraph: ToolDef<AppendParagraphArgs> = {
  name: 'append_paragraph',
  description: '在 Word 文档末尾追加一段文本。优先一次回复里调多次，而不是合并成一个大段。',
  parameters: {
    type: 'object',
    properties: {
      text: { type: 'string', description: '要追加的段落文本' },
    },
    required: ['text'],
  },
  humanLabel: ({ text }) =>
    `在文档末尾追加段落「${text.slice(0, 30)}${text.length > 30 ? '…' : ''}」`,
  async execute({ text }, ctx): Promise<ToolResult> {
    // A-06：adapter method 输入 string、输出 Promise<void>；不返 proxy
    await (ctx.adapter as WordAdapter).appendParagraph(text);
    const reverse: ReverseDescriptor = {
      tool: 'delete_last_paragraph',
      args: {},
    };
    return { ok: true, data: { written: text.length }, reverse };
  },
};
```

**步骤 4 — 新建 src/agent/tools/write/word.test.ts：**

```typescript
import { describe, it, expect, vi } from 'vitest';
import { appendParagraph } from './word';
import type { ToolExecContext } from '../index';
import type { DocumentAdapter } from '../../../adapters/DocumentAdapter';
import type { WordAdapter } from '../../../adapters/WordAdapter';

describe('appendParagraph ToolDef', () => {
  it('humanLabel: 短文本不截断', () => {
    expect(appendParagraph.humanLabel({ text: '短文本' })).toBe('在文档末尾追加段落「短文本」');
  });

  it('humanLabel: > 30 字符 → 截 30 + 追加 "…"', () => {
    const long = 'a'.repeat(50);
    const label = appendParagraph.humanLabel({ text: long });
    expect(label).toContain('…');
    expect(label.startsWith('在文档末尾追加段落「')).toBe(true);
    // 「 + 30 a + …」
    expect(label).toBe('在文档末尾追加段落「' + 'a'.repeat(30) + '…」');
  });

  it('execute: 调 adapter.appendParagraph(text) + 返 ok+reverse descriptor', async () => {
    const mockAppendParagraph = vi.fn().mockResolvedValue(undefined);
    const mockAdapter = {
      appendParagraph: mockAppendParagraph,
      // 其它 DocumentAdapter 接口 stub
      capabilities: () => ({ host: 'word' as const, supportsSelectionEvents: true, supportedInserts: ['text' as const] }),
      getSelection: async () => ({ kind: 'none' as const }),
      onSelectionChanged: () => () => {},
      insert: async () => {},
    } as unknown as WordAdapter;

    const ctx: ToolExecContext = {
      adapter: mockAdapter as unknown as DocumentAdapter,
      runId: 'r1',
      stepIndex: 1,
      signal: new AbortController().signal,
    };

    const result = await appendParagraph.execute({ text: '段落 1' }, ctx);

    expect(mockAppendParagraph).toHaveBeenCalledWith('段落 1');
    expect(result.ok).toBe(true);
    expect(result.data).toEqual({ written: 3 });
    expect(result.reverse).toEqual({ tool: 'delete_last_paragraph', args: {} });
  });
});
```

**步骤 5 — 修改 src/agent/tools/index.ts buildToolsForHost('word')：**

```typescript
// import 段加：
import { appendParagraph } from './write/word';

// buildToolsForHost：
export function buildToolsForHost(host: 'word' | 'excel' | 'ppt'): ToolDef[] {
  switch (host) {
    case 'word':
      return [appendParagraph];
    case 'excel':
      return [];
    case 'ppt':
      return [];
    default:
      return [];
  }
}
```

更新 src/agent/tools/index.test.ts（如已有）增加断言：

```typescript
it('buildToolsForHost("word") 含且仅含 append_paragraph（Phase 3）', () => {
  const tools = buildToolsForHost('word');
  expect(tools).toHaveLength(1);
  expect(tools[0].name).toBe('append_paragraph');
});
```

跑 `npm test -- src/agent/tools src/adapters/WordAdapter.test.ts`。
  </action>
  <verify>
    <automated>npm test -- src/agent/tools src/adapters/WordAdapter.test.ts --reporter=verbose 2>&1 | tail -40 && npm test 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WordAdapter.appendParagraph method 存在 + 含 Word.run 闭包
    - tools/write/word.ts 导出 appendParagraph ToolDef（4 字段全）
    - buildToolsForHost('word') 返回 [appendParagraph]
    - 5 个 it 全绿（WordAdapter ×2 + tools/write/word ×3 + tools/index 新增 1）
    - npm test 全套全绿
    - npm run build 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.2: openai-compat.streamChat 扩展 tools 入参 + 删除 INSERT_TO_DOCUMENT_TOOL hardcode</name>
  <files>
    src/providers/openai-compat.ts (MODIFY),
    src/providers/openai-compat.test.ts (MODIFY/NEW)
  </files>
  <behavior>
    - Test 1: streamChat 收到 tools 参数 → 请求 body.tools 等于传入；body.tool_choice = 'auto'
    - Test 2: streamChat 未收到 tools / tools 为空 array → 请求 body 不含 tools 字段
    - Test 3: 文件不再出现 INSERT_TO_DOCUMENT_TOOL 常量 / hardcode
  </behavior>
  <read_first>
    - src/providers/openai-compat.ts 整文件（L1-150 — 看 streamChat 签名、_startStream body 构造、tools hardcode 位置 L24-48 + L92-104）
    - src/providers/openai-compat.test.ts （v1 已有，看 mock fetch / body 断言模式）
    - .planning/phases/03-agent-loop-privacy-word-demo/03-PATTERNS.md "src/providers/openai-compat.ts (modify, 移除 hardcode tool)" 段 L1158-1178
  </read_first>
  <action>
**步骤 1 — Plan 03 已做：** Plan 03 步骤 6a 已把 streamChat 签名扩到接受 `tools?` 第 4 参数，且 _startStream body 内已实现「caller 传 tools 优先；空时保留 v1 hardcode INSERT_TO_DOCUMENT_TOOL 作过渡」。本 task 完成最终切换。

**步骤 2 — 删除 INSERT_TO_DOCUMENT_TOOL hardcode 与相关 import：**

1. 整段删除 `INSERT_TO_DOCUMENT_TOOL` 常量（v1 src/providers/openai-compat.ts L24-48 — 实际位置由文件决定）。
2. 删除 _startStream 内 「caller 未传 tools 时 fallback INSERT_TO_DOCUMENT_TOOL」分支（Plan 03 标注的过渡路径）—— 现在 caller 不传 tools 就真不传：

```typescript
// _startStream body 构造（Plan 03 已加分支 + 本 plan 删 fallback hardcode）：
if (tools && tools.length > 0) {
  body.tools = tools;
  body.tool_choice = 'auto';
}
// 不再有 else fallback INSERT_TO_DOCUMENT_TOOL —— 全部从 src 删除
```

3. `setSupportsToolCall` 探测路径（v1 已实现）保留 — 不动 useProviderStore.setSupportsToolCall 调用点（D-18 G-05）。如该路径依赖 INSERT_TO_DOCUMENT_TOOL 作 probe：替换为「首次 caller-supplied 非空 tools 调用作探测」，或保留首次 streamChat 调用的探测语义（最简：caller 传非空 tools 时才探测；空时跳过）。

**步骤 3 — src/providers/openai-compat.test.ts 加 1 个 it（Plan 03 已加 1 个 "tools 传入时 body.tools 命中"，本 plan 补 "不传 / 空时 body.tools undefined"）：**

```typescript
describe('streamChat tools 入参 — Plan 04 (INSERT_TO_DOCUMENT_TOOL 删除后)', () => {
  it('tools 未传 / 空 → body 不含 tools 字段（INSERT_TO_DOCUMENT_TOOL 已全删）', async () => {
    const fetchMock = vi.fn().mockResolvedValue(new Response('data: [DONE]\n\n', { status: 200 }));
    vi.stubGlobal('fetch', fetchMock);

    const llm = new OpenAICompatibleLLM();
    const ctrl = new AbortController();
    const gen = llm.streamChat(
      [{ role: 'user', content: 'hi' }],
      { baseUrl: 'https://api.deepseek.com', apiKey: 'sk-xxx', model: 'deepseek-v4-flash' } as never,
      ctrl.signal,
    );
    for await (const _ of gen) { break; }

    const body = JSON.parse(fetchMock.mock.calls[0][1].body as string);
    expect(body.tools).toBeUndefined();
    expect(body.tool_choice).toBeUndefined();

    vi.unstubAllGlobals();
  });
});
```

**步骤 4 — B4: AGENT-08 humanLabel TS 编译失败验证（type-only test）：**

新建 src/agent/tools/index.types.test.ts（type-only test — vitest 跑时 TS 编译器会校验 @ts-expect-error）：

```typescript
// src/agent/tools/index.types.test.ts —— TypeScript-only test（B4 / AGENT-08 验收）
// 验证 ToolDef interface 强制 humanLabel 字段：编译期失败 = TS 强制成立
import type { ToolDef } from './index';

// 正例：完整 ToolDef 应该编译通过
const _goodTool: ToolDef<{ text: string }> = {
  name: 'good',
  description: '',
  parameters: { type: 'object' },
  humanLabel: ({ text }) => `label ${text}`,
  execute: async () => ({ ok: true }),
};

// 反例：缺 humanLabel 必须编译失败
// @ts-expect-error - AGENT-08 强制 ToolDef 必须含 humanLabel；删本注释后 tsc 必须报错
const _badTool: ToolDef = {
  name: 'bad',
  description: '',
  parameters: { type: 'object' },
  execute: async () => ({ ok: true }),
};

// 用 void 避免 unused 警告（这文件本身就是断言编译行为）
void _goodTool;
void _badTool;
```

**步骤 5 — 跑全套验证：**

```bash
npm test
npm run build
npm run size
```

INSERT_TO_DOCUMENT_TOOL 应在 src 全无命中：
```bash
grep -rn "INSERT_TO_DOCUMENT_TOOL" src/
```
预期 0 命中。

`tsc --noEmit` 应通过（@ts-expect-error 命中 ⇒ TS 强制有效）：
```bash
npx tsc --noEmit
```

  </action>
  <verify>
    <automated>npm test -- src/providers/openai-compat.test.ts --reporter=verbose 2>&1 | tail -30 && npm test 2>&1 | tail -10 && (grep -rn "INSERT_TO_DOCUMENT_TOOL" src/ 2>/dev/null | grep -v '\.test\.\|\.types\.' && echo "FAIL: INSERT_TO_DOCUMENT_TOOL still in src" && exit 1 || echo "PASS: INSERT_TO_DOCUMENT_TOOL 0 命中") && npx tsc --noEmit 2>&1 | tail -10 && echo "PASS: TS 强制 humanLabel（B4 / AGENT-08 type-only test）" && npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>
    - INSERT_TO_DOCUMENT_TOOL 在 src 全无命中（src/agent/tools/index.types.test.ts 内可能出现常量名是允许的 — verify 已排除 .types./.test. 文件）
    - streamChat 签名含 tools? 参数（Plan 03 已落）；body.tools 仅在传入时设置
    - openai-compat.test.ts 含至少 2 个 it（tools 传入 + tools 未传）全绿
    - src/agent/tools/index.types.test.ts 含 @ts-expect-error type-only test（B4 / AGENT-08：删 @ts-expect-error 后 tsc 必须报错）
    - npx tsc --noEmit 通过
    - npm test 全套全绿
    - npm run build / npm run size 通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5.3: 自写 eslint rule aster/require-human-label（Phase 3 warn，Phase 5 flip 为 error）</name>
  <files>
    eslint.config.js (MODIFY)
  </files>
  <read_first>
    - eslint.config.js 整文件
    - .planning/phases/03-agent-loop-privacy-word-demo/03-CONTEXT.md D-13
    - .planning/phases/03-agent-loop-privacy-word-demo/03-RESEARCH.md §Deliverable 3 §3.5 L1212-1217
  </read_first>
  <action>
**步骤 1 — 修改 eslint.config.js，追加 no-restricted-syntax rule 检测 ToolDef object literal 缺 humanLabel 字段：**

由于自写 eslint 插件需要发包到 npm，Phase 3 用 `no-restricted-syntax` 的 AST selector + Phase 5 flip 模式落地（D-13）。在 rules 段追加：

```javascript
// AGENT-08 humanLabel 强制（Phase 3 warn 不阻断；Phase 5 多 write tool 上线时 flip 为 error）
// 用 no-restricted-syntax AST selector 检测 ToolDef 字面量（含 name 字段但缺 humanLabel）
'no-restricted-syntax': [
  // 把上面的 deepseek 模型 selector 与本 selector 合并成数组
  'error',
  {
    selector: "Literal[value=/deepseek-chat|deepseek-reasoner/]",
    message: 'legacy 模型名已废弃（2026-07-24 退役），请使用 deepseek-v4-flash 或 deepseek-v4-pro',
  },
  // ↓ 新增（D-13 / AGENT-08）
  // 检测形如 { name: '...', description: '...', execute: ..., ... } 但缺 humanLabel: ...
  // 注：AST selector 复杂度有限，Phase 3 仅给 visual hint；真正强制靠 TypeScript ToolDef 接口
  // Phase 5 多 write tool 上线时把本 entry 改为单独自写 plugin 或迁到 eslint custom plugin
  {
    selector:
      "ObjectExpression > Property[key.name='name'] ~ Property[key.name='execute']:not(:has(~ Property[key.name='humanLabel']))",
    message:
      'AGENT-08: ToolDef object literal 必须含 humanLabel 字段（TS 接口已强制；本 rule 双重保险）。Phase 5 多 tool 上线时本 selector 改为 error severity。',
  },
],
```

如 selector 在 typescript-parser 下不工作（experimental selector 不稳定），降级为 jsdoc 注释 + TS 接口强制双轨：

```javascript
// 如 selector 不工作（typescript-parser 限制），保留下方注释路径，依赖 TS 接口强制
// Phase 5 多 tool 上线时改自写 ESLint plugin（aster/require-human-label rule 真实实现）
```

并在文件顶部加 jsdoc 注释解释（D-13 要求注释解释为何 Phase 3 不 enforce）：

```javascript
/**
 * Aster ESLint 配置
 *
 * Phase 3 决策（D-13）：
 *   - AGENT-08 「每个 tool 必须 export humanLabel」 由 TypeScript ToolDef interface 已强制
 *     （缺字段 → tsc 编译失败）。
 *   - 本配置追加 'no-restricted-syntax' AST selector 作 visual hint（warn 严重度）。
 *   - Phase 3 只 1 个 write tool（append_paragraph），过早 enforce error 增加噪音。
 *   - Phase 5 多 write tool 上线时，把本 rule 严重度从 warn flip 为 error；
 *     或迁到自写 ESLint plugin（aster/require-human-label）做更准确的 AST 检测。
 */
```

**步骤 2 — 跑 eslint 验证（如项目无 lint 脚本，则 skip 此步；只确认 eslint.config.js 语法合法）：**

```bash
npm run lint 2>&1 | tail -10 || npx eslint --print-config src/agent/tools/write/word.ts 2>&1 | head -5
```

eslint config 可加载（不抛 SyntaxError），即视为本 task 完成。
  </action>
  <verify>
    <automated>node --check eslint.config.js 2>&1 || npx eslint --print-config src/agent/tools/write/word.ts > /dev/null 2>&1 && echo "PASS: eslint.config.js parses + loads" && grep -c "humanLabel" eslint.config.js | grep -qv "^0$" && echo "PASS: humanLabel rule present"</automated>
  </verify>
  <done>
    - eslint.config.js 含 no-restricted-syntax 的 humanLabel 检测条目（或注释说明降级原因）
    - 文件顶部含 jsdoc 注释解释 Phase 3 不 enforce 但 Phase 5 flip 的原因（D-13）
    - eslint config 可被 eslint 加载（无 SyntaxError）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_calls → dispatchTool → WordAdapter | tool name 是 string literal subset 受控；arguments 由 dispatchTool 喂入 execute；adapter method 内 Word.run 闭包 |
| openai-compat body.tools | tools 来自 buildToolsForHost — 只有 Plan 05 注册的 ToolDef（append_paragraph）；caller 不能传任意 tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AGENT-08-01 | Tampering | tools/write/word.ts execute | mitigate | 输入 string、输出 Promise<void>；adapter.appendParagraph 内 Word.run 闭包；不返 proxy（A-06）|
| T-AGENT-08-02 | Information disclosure | humanLabel | accept | text 截 30 字符；humanLabel 出现在 chatStore Message.content 与 LLM toolResult，不进 localStorage |
| T-AGENT-08-03 | Tampering | openai-compat body.tools | mitigate | 仅 buildToolsForHost 返回的受控 array 才会进 body；hardcode 路径已删 |
</threat_model>

<verification>
- npm test 全套全绿（含 WordAdapter / tools/write/word / tools/index / openai-compat 测试）
- npm run build 通过
- npm run size ≤ 80KB gzipped
- grep INSERT_TO_DOCUMENT_TOOL src/ 0 命中
- buildToolsForHost('word') 返 [appendParagraph]
- eslint.config.js 合法 + 含 humanLabel rule 提示
- 真机 UAT 由 Plan 08 覆盖
</verification>

<success_criteria>
- AGENT-08 humanLabel TS 接口已落（Plan 04），本 plan 接入第一个真实 ToolDef + eslint rule 双轨
- Word demo 主路径就位（adapter method + tool registry + openai-compat 动态 tools）
- Plan 06 chatStore thin-delegate 完成后，Plan 08 Word 真机 UAT 即可跑通
</success_criteria>

<output>
完成后创建 `.planning/phases/03-agent-loop-privacy-word-demo/03-05-SUMMARY.md`，记录：
- WordAdapter.appendParagraph 实测调用栈 + Word.InsertLocation.end 使用确认
- tools/write/word.ts ToolDef 完整字段列表（name/description/parameters/humanLabel/execute）
- humanLabel 截断规则示例（短文本 / 30 字符 / 50 字符）
- openai-compat.streamChat 新签名 + body.tools 注入条件
- eslint rule 写入位置 + Phase 5 flip 操作步骤（changelog 风格，给未来 phase 看）
</output>

---
phase: 07-uat-sideload-release-prep
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/agent/agentStore.ts
  - src/components/Settings/ProviderForm.tsx
  - src/components/Settings/ProviderList.tsx
autonomous: true
requirements:
  - ERR-04
  - NFR-04

must_haves:
  truths:
    - "启动 agent 时若 supportsToolCall===false 则弹错误气泡「当前 Provider/Model 不支持 tool calling，请切到 DeepSeek-V4 或 gpt-5.1」，不发起 LLM call"
    - "内置 Provider（isBuiltIn=true）的编辑表单中不出现「测试 tool calling」按钮"
    - "自定义 Provider 编辑表单中「测试 tool calling」按钮可见，点击后 loading→ 结果 badge"
    - "ProviderList 行展示 supportsToolCall badge 三态（未测/✓支持/✗不支持）"
    - "所有新 UI 元素遵循 teal 克制设计（btn-ghost btn-sm / badge-success / badge-error）"
    - "npm test 全绿（Plan 01 describe.skip 移除 skip 后全 pass）"
  artifacts:
    - path: "src/agent/agentStore.ts"
      provides: "runAgent 入口前 A-21 pre-flight 守门"
      contains: "supportsToolCall === false"
    - path: "src/components/Settings/ProviderForm.tsx"
      provides: "测试 tool calling 按钮 + loading/supported/unsupported 三态"
      contains: "handleTestToolCall"
    - path: "src/components/Settings/ProviderList.tsx"
      provides: "supportsToolCall badge 第三态渲染"
      contains: "badge-error"
  key_links:
    - from: "src/components/Settings/ProviderForm.tsx"
      to: "src/providers/probeToolCall.ts"
      via: "handleTestToolCall 调用 probeToolCallSupport"
      pattern: "probeToolCallSupport"
    - from: "src/agent/agentStore.ts"
      to: "src/store/providers.ts"
      via: "useProviderStore.getState() 读取 supportsToolCall"
      pattern: "supportsToolCall === false"
---

<objective>
A-21 model 兼容性功能完整实现：agentStore pre-flight 拦截 + ProviderForm 测试按钮 + ProviderList badge 三态。

Purpose: 让自定义 Provider 用户能主动验证 model 是否支持 tool calling；禁止不支持的 model 启动 agent run。
Output: 三个修改文件；Plan 01 的 describe.skip 去掉 skip → 全测试绿
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-uat-sideload-release-prep/07-CONTEXT.md
@.planning/phases/07-uat-sideload-release-prep/07-RESEARCH.md
@.planning/phases/07-uat-sideload-release-prep/07-PATTERNS.md
@.planning/phases/07-uat-sideload-release-prep/07-01-SUMMARY.md

<interfaces>
<!-- 已验证的关键接口（来自 PATTERNS.md） -->

From src/agent/agentStore.ts (VERIFIED runAgent 175-188, 插入点 = beginRun 之前):
```typescript
async runAgent(prompt, selectionCtx, adapter) {
  // ← 在此处插入 A-21 pre-flight guard（D-02）
  const runId = crypto.randomUUID();
  const controller = get().beginRun(runId);
  try {
    const { runAgent: runAgentLoop } = await import('./loop');
    await runAgentLoop(prompt, selectionCtx, adapter, controller.signal, runId);
  } finally {
    if (get().agentStatus !== 'soft-landing') {
      get().endRun();
    }
  }
},
```

From src/store/providers.ts (VERIFIED BUILTIN_MODEL_OPTIONS + setSupportsToolCall):
```typescript
export const BUILTIN_MODEL_OPTIONS: Record<string, string[]> = {
  deepseek: ['deepseek-v4-pro', 'deepseek-v4-flash'],
  aihubmix: ['gpt-5.1', 'gemini-3.5-flash'],
};
setSupportsToolCall(providerId: string, supports: boolean): void;
// agentStore 读取时用 useProviderStore.getState()（非 hook，在 Zustand action 内）
```

From src/components/Settings/ProviderForm.tsx (VERIFIED isBuiltIn pattern, 46 行; button pattern, 228-237 行):
```typescript
const isBuiltIn = provider?.isBuiltIn ?? false;
// 测试按钮：仅当 !isBuiltIn 时渲染
// type="button"（必须！防止 form submit — PATTERNS.md 共享模式）
// className="btn btn-ghost btn-sm"
// disabled={testState === 'loading' || !apiKey.trim()}
```

From src/components/Settings/ProviderList.tsx (VERIFIED badge lines 83-90):
```typescript
// 现有三个 badge 类：badge / badge badge-success / badge badge-accent
// badge-error 在 styles.css 1234 行已确认存在
// supportsToolCall badge 插入位置：pname-line div 内，现有 badge 之后
```

From src/providers/probeToolCall.ts (Plan 01 新建):
```typescript
export async function probeToolCallSupport(config: LLMConfig): Promise<boolean | null>;
// 返回 true/false/null；null = 超时不写回
```

From src/store/chat.ts (pushMessage 签名):
```typescript
// pre-flight error 用法：
useChatStore.getState().pushMessage({
  role: 'error',
  content: '当前 Provider/Model 不支持 tool calling，请切到 DeepSeek-V4 或 gpt-5.1',
  errorCode: 'UNSUPPORTED',
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agentStore.ts pre-flight 拦截（A-21 D-02）</name>
  <files>src/agent/agentStore.ts, src/agent/agentStore.test.ts</files>
  <behavior>
    - runAgent 入口（beginRun 之前）读取 currentProvider
    - currentProvider?.supportsToolCall === false → pushMessage({role:'error', content:'当前 Provider/Model 不支持 tool calling，请切到 DeepSeek-V4 或 gpt-5.1', errorCode:'UNSUPPORTED'}) + return（不调 beginRun）
    - null / undefined → 放行（不拦截）
    - Plan 01 agentStore.test.ts 中三个 describe.skip 未用 skip（直接 describe），现在应该全绿
  </behavior>
  <action>
**修改 src/agent/agentStore.ts：**

1. 在文件顶部 import 区添加（若尚未 import）：
   ```typescript
   import { useChatStore } from '../store/chat';
   import { useProviderStore } from '../store/providers';
   ```

2. 在 `runAgent` 函数体开头（`const runId = crypto.randomUUID();` 之前）插入：
   ```typescript
   // A-21 pre-flight：仅当 supportsToolCall 明确为 false 时拦截；null/undefined 放行
   const _currentProvider = useProviderStore.getState().providers.find(
     (p) => p.id === useProviderStore.getState().defaultLLMProviderId,
   );
   if (_currentProvider?.supportsToolCall === false) {
     useChatStore.getState().pushMessage({
       role: 'error',
       content: '当前 Provider/Model 不支持 tool calling，请切到 DeepSeek-V4 或 gpt-5.1',
       errorCode: 'UNSUPPORTED',
     });
     return;
   }
   ```

**同时，移除 src/agent/agentStore.test.ts 中 A-21 三个 case 的 describe.skip 标注（如果 Plan 01 使用了 skip），改为普通 describe，使测试立即生效。**

注意：`useProviderStore` 已在 agentStore.ts 导入（用于 openai-compat 消费路径）——若已 import，无需重复添加。若 `useChatStore` 尚未 import，添加之。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run test:unit -- src/agent/agentStore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>agentStore.test.ts A-21 三个 case 全绿（false 拦截 + null/undefined 放行）；`grep -c "supportsToolCall === false" src/agent/agentStore.ts` 输出 ≥1</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProviderForm 测试按钮 + ProviderList badge 第三态</name>
  <files>src/components/Settings/ProviderForm.tsx, src/components/Settings/ProviderList.tsx, src/components/Settings/ProviderForm.test.tsx, src/components/Settings/ProviderList.test.tsx</files>
  <behavior>
    - ProviderForm：新增 testState: 'idle'|'loading'|'supported'|'unsupported' 本地 state
    - 「测试 tool calling」按钮仅在 !isBuiltIn 时渲染，type="button"，className="btn btn-ghost btn-sm"
    - 点击 → setTestState('loading') → 调 probeToolCallSupport(buildConfig()) → 结果写回 setSupportsToolCall + setTestState
    - 结果 badge：supported=badge-success「✓ 支持」/ unsupported=badge-error「✗ 不支持」/ null(超时)=badge「超时，请重试」
    - ProviderList：pname-line 内，supportsToolCall===true → badge-success「✓ tool call」；false → badge-error「✗ 不支持」；undefined/null → 不渲染
    - Plan 01 的两个 describe.skip 块去掉 skip，所有 case 变绿
  </behavior>
  <action>
**修改 src/components/Settings/ProviderForm.tsx：**

1. 在现有 import 区添加：
   ```typescript
   import { probeToolCallSupport } from '../../providers/probeToolCall';
   ```

2. 在 `useState` 声明区（约 52 行附近）添加：
   ```typescript
   type TestState = 'idle' | 'loading' | 'supported' | 'unsupported';
   const [testState, setTestState] = useState<TestState>('idle');
   ```

3. 在组件函数内（`handleSubmit` 等 handler 旁）添加 `handleTestToolCall`：
   ```typescript
   const handleTestToolCall = async () => {
     if (!apiKey.trim()) return;
     setTestState('loading');
     const config = {
       providerId: provider?.id ?? 'temp',
       baseURL: baseURL.trim(),
       apiKey: apiKey.trim(),
       model: model.trim(),
     };
     const result = await probeToolCallSupport(config);
     if (result === true) {
       setTestState('supported');
       useProviderStore.getState().setSupportsToolCall(provider!.id, true);
     } else if (result === false) {
       setTestState('unsupported');
       useProviderStore.getState().setSupportsToolCall(provider!.id, false);
     } else {
       // null = 超时，不写回 supportsToolCall
       setTestState('idle'); // 回到 idle，用户可重试
     }
   };
   ```

4. 在 model 字段之后（约 194 行）、apiKey 字段之前，插入测试按钮区块（仅 !isBuiltIn）：
   ```tsx
   {!isBuiltIn && (
     <div className="aster-form-field">
       <button
         type="button"
         className="btn btn-ghost btn-sm"
         onClick={handleTestToolCall}
         disabled={testState === 'loading' || !apiKey.trim()}
       >
         <Trans>{testState === 'loading' ? '测试中...' : '测试 tool calling'}</Trans>
       </button>
       {testState === 'supported' && (
         <span className="badge badge-success"><Trans>✓ 支持</Trans></span>
       )}
       {testState === 'unsupported' && (
         <span className="badge badge-error"><Trans>✗ 不支持</Trans></span>
       )}
     </div>
   )}
   ```

**修改 src/components/Settings/ProviderList.tsx：**

在 pname-line div（badge 区，约 83-90 行）的现有 badge 之后插入：
```tsx
{provider.supportsToolCall === true && (
  <span className="badge badge-success"><Trans>✓ tool call</Trans></span>
)}
{provider.supportsToolCall === false && (
  <span className="badge badge-error"><Trans>✗ 不支持</Trans></span>
)}
```

**同时，移除 src/components/Settings/ProviderForm.test.tsx 和 ProviderList.test.tsx 中 Plan 01 添加的 describe.skip 的 `.skip`，使 case 立即生效。**

注意：`useProviderStore` 在 ProviderForm.tsx 中是否已 import？若未 import 则添加：`import { useProviderStore } from '../../store/providers';`

所有新字符串用 `<Trans>` 包裹（Lingui 模式，现有文件已有 import）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run test:unit -- src/components/Settings/ProviderForm.test.tsx src/components/Settings/ProviderList.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    1. `grep -c "handleTestToolCall" src/components/Settings/ProviderForm.tsx` 输出 ≥1
    2. `grep -c "badge-error" src/components/Settings/ProviderList.tsx` 输出 ≥1
    3. ProviderForm + ProviderList 测试全绿（describe.skip 已去掉）
    4. `npm test` 全绿（tsc + vitest）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProviderForm → probeToolCallSupport | probe 请求带 apiKey，不得泄露到 UI 错误状态 |
| agentStore → chatStore | pre-flight 错误消息不含 Key 片段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | ProviderForm handleTestToolCall | mitigate | probeToolCallSupport 内部已守门（T-07-01）；handleTestToolCall 不 log config；UNSUPPORTED 错误文案固定字符串（不含动态 Key 内容）|
| T-07-04 | Information Disclosure | agentStore runAgent pre-flight | mitigate | pushMessage content 是固定字符串「当前 Provider/Model 不支持 tool calling，请切到 DeepSeek-V4 或 gpt-5.1」，不含 Key 或 Provider 内部状态 |
| T-07-05 | Tampering | ProviderForm baseURL 用于 probe | accept | 现有 ProviderForm validate() 已检查 https:// 前缀；probe 复用同一 config，不绕过校验 |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster
# pre-flight 守门存在
grep -c "supportsToolCall === false" src/agent/agentStore.ts
# 测试按钮存在
grep -c "handleTestToolCall" src/components/Settings/ProviderForm.tsx
# badge-error 在 ProviderList 存在
grep -c "badge-error" src/components/Settings/ProviderList.tsx
# 全套测试
npm test
```
</verification>

<success_criteria>
- agentStore.ts：runAgent beginRun 之前有 supportsToolCall===false 守门
- ProviderForm.tsx：!isBuiltIn 区块含「测试 tool calling」按钮，type="button"
- ProviderList.tsx：supportsToolCall===true → badge-success；false → badge-error
- `npm test` 全绿（含 Plan 01 去掉 skip 的所有 case）
- `npm run build && npm run size` 通过（≤82KB）
</success_criteria>

<output>
完成后创建 `.planning/phases/07-uat-sideload-release-prep/07-02-SUMMARY.md`
</output>

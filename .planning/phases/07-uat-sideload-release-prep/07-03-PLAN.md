---
phase: 07-uat-sideload-release-prep
plan: "03"
type: execute
wave: 2
depends_on: []
files_modified:
  - README.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - NFR-01
  - NFR-04
  - NFR-05

must_haves:
  truths:
    - "README 的 sideload 步骤完整，新用户无需额外文档即可在 Chrome 完成安装（W5：正向可观察）"
    - "README 含 4 killer scenario 示例 prompt + 心智锚定段，读者能理解 Aster 是多步代理而非单次问答（正向可观察）"
    - "README.md 含 N5 一句话隐私告知（选中内容会发往 Provider）"
    - "README.md 不再含 Fluent UI v9、138KB bundle、草稿 banner、幻影引用 REL-01/REL-03/REL-04/NFR-06"
    - "README.md 浏览器矩阵 = Chrome（最新版），无 Edge"
    - "ROADMAP.md 中 Phase 6 SC1/SC2/SC3 无 ¥ 字段；Phase 7 SC1/SC4 浏览器矩阵改为 Chrome only；SC3 gpt-4o → gpt-5.1"
  artifacts:
    - path: "README.md"
      provides: "代理定位首版 README，技术栈准确，无幻影引用，sideload 步骤可独立完成"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6/7 SC 措辞与 CONTEXT D-12/D-16 一致"
  key_links:
    - from: "README.md"
      to: "manifest.xml"
      via: "sideload 步骤中 manifest URL（GitHub Pages）"
      pattern: "wynne-cwb.github.io/Aster"
---

<objective>
文档清理双任务：(1) README.md 从 113 行过时初稿完整重写为代理定位版；(2) ROADMAP.md 清除 ¥ 残留 + 浏览器矩阵 + gpt-4o → gpt-5.1。

Purpose: 开源仓库首次发布时 README 必须准确、可独立完成安装，ROADMAP 与实际决策一致（D-12/D-16 修正）。
Output: 重写后的 README.md + 局部更新的 ROADMAP.md
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-uat-sideload-release-prep/07-CONTEXT.md
@.planning/phases/07-uat-sideload-release-prep/07-RESEARCH.md

<interfaces>
<!-- RESEARCH.md README 重写框架（D-06~D-10）和 ROADMAP 修正清单 -->

README 章节顺序（来自 RESEARCH.md）：
1. Hero / 一句话定位（代理定位）
2. Aster 怎么工作（心智锚定）
3. 4 Killer Scenarios（含具体输入示例）
4. BYO Key / 无后台说明
5. Sideload 步骤（Chrome only，三宿主）
6. 技术架构概览（自写 CSS teal 克制；bundle 以 npm run build && npm run size 当场实测值为准）
7. 诚实产品口径（作者自用 + 开源，早期阶段）
8. N5 一句话隐私告知
9. 开发（npm ci / dev / build / test / size）

ROADMAP 修正清单（来自 RESEARCH.md）：
| 位置 | 当前措辞 | 应改为 |
|------|---------|--------|
| Phase 7 SC1 | 「Edge + Chrome 最新两版 × 全新 profile」 | 「Chrome（最新版）× 全新 profile」 |
| Phase 7 SC4 | 「sideload manifest 在 Office for Web Edge/Chrome × 三宿主」 | 「Office for Web Chrome × 三宿主」 |
| Phase 7 SC3 | 「gpt-4o」 | 「gpt-5.1」 |
| Phase 6 SC1 | 「¥ <3」 | 删除 ¥，改「8-15 步」 |
| Phase 6 SC2 | 「¥ <1.5」 | 删除 ¥，改「10-18 步」 |
| Phase 6 SC3 | 「¥ <2」 | 删除 ¥，改「6-12 步」 |

注：以上 ROADMAP 7 处修正可能在规划阶段已应用（执行前先 grep 核对当前状态，已是目标值则跳过对应修改，不重复改）。

manifest.xml 当前 SourceLocation（已验证）：
https://wynne-cwb.github.io/Aster/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP.md 文档残留修正</name>
  <files>.planning/ROADMAP.md</files>
  <action>
先 `grep -n "¥\|Edge + Chrome\|gpt-4o" .planning/ROADMAP.md` 核对当前状态。**若已全为目标值（¥=0、Edge + Chrome=0、gpt-4o=0），说明规划阶段已应用，本 Task 仅需确认，跳过修改。** 否则按以下七处用 Edit 精确替换（不重写整个文件）：

**修改 1 — Phase 7 SC1：**
- 查找：`Edge + Chrome 最新两版 × 全新 profile` 替换为 `Chrome（最新版）× 全新 profile`
- 附：「录屏证据 +」改为「步数 + 端到端耗时 + DiffLogPanel 截图」（不强求录屏，D-14）

**修改 2 — Phase 7 SC3：**
- 查找：`gpt-4o`（SC3 错误文案中）替换为 `gpt-5.1`

**修改 3 — Phase 7 SC4：**
- 查找：`sideload manifest 在 Office for Web Edge/Chrome × 三宿主` 替换为 `sideload manifest 在 Office for Web Chrome × 三宿主`

**修改 4 — Phase 6 SC1：**
- 查找：`¥ <3` 删去 ¥，保留「在 8-15 步内完成全过程；diff log 显示每步人话」

**修改 5 — Phase 6 SC2：**
- 查找：`¥ <1.5` 改「10-18 步完成」（仅保留步数）

**修改 6 — Phase 6 SC3：**
- 查找：`¥ <2` 改「6-12 步完成」

**修改 7 — Phase 7 SC1（如有 cost 验收标准）：**
- 任何 ¥ 相关验收标准改「步数 + 端到端耗时 + DiffLogPanel 截图归档」
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && echo "yen=$(grep -c '¥' .planning/ROADMAP.md) edge=$(grep -c 'Edge + Chrome' .planning/ROADMAP.md) gpt4o=$(grep -c 'gpt-4o' .planning/ROADMAP.md) gpt51=$(grep -c 'gpt-5.1' .planning/ROADMAP.md)"</automated>
  </verify>
  <done>
    - `grep -c "¥" .planning/ROADMAP.md` 输出 0
    - `grep -c "Edge + Chrome" .planning/ROADMAP.md` 输出 0
    - `grep -c "gpt-4o" .planning/ROADMAP.md` 输出 0
    - `grep -c "gpt-5.1" .planning/ROADMAP.md` 输出 ≥1（SC3 已更新）
  </done>
</task>

<task type="auto">
  <name>Task 2: README.md 完整重写</name>
  <files>README.md</files>
  <action>
先运行 `npm run build && npm run size` 得到当前 gzip 实测值（记录 main-*.js 的 gzip 大小，用于章节 6）。

然后 Read `README.md` 现状（113 行初稿），再 Write 重写版本：

---

**README.md 重写内容（按以下框架，中文为主，代码命令保留英文）：**

章节 1（Hero）：
```markdown
# Aster — Office 智能代理

在原生 Office 里的 AI 代理。你说一句话，Aster 自主完成多步文档任务，边跑边让你看到进度，随时可以暂停、撤回。

BYO Key · 无后台 · 纯浏览器直连 · 开源
```

章节 2（怎么工作）：
```markdown
## Aster 怎么工作

你输入：「帮我做一份『Q3 销售复盘』PPT，给 leadership 看，重点华东」

Aster 的工作流程：
1. 调用 read tools 理解当前文档状态（列出已有 slides、获取内容）
2. 多步调用 write tools 逐步完成任务（插入 slides → 写标题 → 填内容）
3. 每步完成后在 Task Pane 汇报进度（步骤 N：在第 3 张幻灯片后插入...）
4. 跑完后 DiffLog 卡片列出所有改动，可逐步撤销或一键撤回全部
5. 随时暂停 / 中止，不满意就 Undo All
```

章节 3（4 Killer Scenarios）：
```markdown
## 4 个核心场景

### PPT: Topic → Deck
> 「帮我做一份『Q3 销售复盘』PPT，给 leadership 看，重点华东」

Aster 在 8-15 步内完成：读取 PPT 现状 → batch insert 8-10 张 slides → 填标题和要点。

### Excel: Clean + Chart + Insight
> 「清洗这份数据，看哪个产品卖得最好，做个图，给我三句话洞察」

Aster 在 10-18 步内完成：扫 used range → set_range_values 清洗 → apply_formula → insert_chart → 三句话总结。

### Word: Polish + Restructure
> 「整篇润色，把口语化改成正式书面，顺便检查逻辑顺序」

Aster 在 6-12 步内完成：分批 read 段落 → replace_paragraph 逐段改写，长文不超 context window。

### PPT Shape 精细化
> 「把左下角那张图改成红色边框，然后右移 10 px」

Aster 在 3-6 步内完成：list_shapes_on_slide → 根据坐标推断「左下角」→ set_shape_property（border）+ move_shape（+10px）。
这是 v1 单步模型完全做不到的、Microsoft Copilot 也不暴露的操作级能力。
```

章节 4（BYO Key）：
```markdown
## BYO Key / 无后台

Aster 是纯静态 Web Add-in，所有 LLM / 图像调用从你的浏览器直连 Provider API。

- **API Key 存在浏览器的 localStorage**，不经过 Aster 服务器（Aster 没有服务器）
- 支持 DeepSeek（推荐）、AiHubMix、任意 OpenAI-compatible 自定义 Provider
- 内置 model：`deepseek-v4-pro`、`deepseek-v4-flash`、`gpt-5.1`、`gemini-3.5-flash`
```

章节 5（Sideload 步骤）：
```markdown
## Sideload 安装（Chrome，Office for Web）

1. 访问 [office.com](https://office.com)，用 Microsoft 账号登录
2. 打开 PowerPoint / Excel / Word 任一文档
3. 点击「开始」标签 → 加载项 → 上传我的加载项
4. 上传 [`manifest.xml`](manifest.xml)（从本仓库下载，或直接填 GitHub raw URL）
5. 点击 Aster 按钮打开 Task Pane，在 Settings 输入 API Key，即可开始

**当前支持：** Chrome 最新版 × Office for Web（PowerPoint / Excel / Word 三宿主）
**路线图：** Windows 桌面版 Office（v1.1 验证中）
```

章节 6（技术架构概览）：
```markdown
## 技术架构

| 层 | 选型 |
|----|------|
| 宿主 | Office.js Add-in（XML manifest）× PPT / Excel / Word |
| 构建 | Vite 7 + React 19 + TypeScript strict |
| 样式 | 自写 CSS 设计系统（teal 克制，CSS 变量驱动 light / dark）|
| 状态 | Zustand 5 |
| LLM 调用 | 原生 fetch + ReadableStream（无 SDK，0 后台）|
| Bundle（初始） | ~{实测gzip值} gzip（CI 门禁 ≤82KB）|
| 存储 | partitioned localStorage（Key 不离浏览器）|
```

（实测 gzip 值从 `npm run size` 命令输出中取，通常形如 `73.13 KB`）

章节 7（产品口径）：
```markdown
## 产品口径

Aster 是作者（[@wynne-cwb](https://github.com/wynne-cwb)）为自己日常 Office 工作打造的工具，现以开源形式分享。

早期阶段：面向自用 + 有技术背景的早期用户；尚无面向大众的多用户隐私授权 UX。BYO Key 使用意味着你对自己的 API Key 和数据流向负责。

欢迎 issue / PR，尤其是真机 bug 报告（Windows / Mac 宿主差异）。
```

章节 8（N5 隐私告知）：
```markdown
## 隐私

选中内容会发往您配置的 AI Provider（DeepSeek / AiHubMix 等），不经过 Aster 服务器。Aster 无服务器，没有数据中转。
```

章节 9（开发）：
~~~markdown
## 开发

```bash
npm ci
npm run dev       # 启动 Vite dev server（含 HTTPS 开发证书）
npm run build     # 生产构建
npm test          # tsc --noEmit + vitest run
npm run size      # bundle size 门禁（≤82KB gzip）
```

Office for Web 本地调试需要 HTTPS：`npm run dev` 会自动用 `office-addin-dev-certs` 生成本地证书。
~~~

---

**必须删除的内容（原 113 行初稿中包含）：**
- 文件顶部「草稿状态」draft banner（> [!WARNING] 或 **草稿** 等标注）
- 底部「将在 Phase 7 REL-01/REL-03/REL-04 补全」footer
- 任何「sideload 视频/GIF 待补」承诺
- REL-01 / REL-03 / REL-04 / NFR-06 任何引用（v2 REQUIREMENTS 不存在这些 ID）
- 「Fluent UI React v9」提法
- bundle「约 138 KB gzip」
- 「Edge + Chrome 最新两版」（改 Chrome only）
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && echo "OLD=$(grep -Ec 'Fluent UI|138 KB|REL-01|REL-03|REL-04|NFR-06|gpt-4o' README.md) EDGE=$(grep -c 'Edge' README.md) POS=$(grep -Ec 'killer|核心场景|BYO Key|无后台|选中内容会发往' README.md) SIDELOAD=$(grep -Ec 'Sideload|sideload|加载项|manifest' README.md)"</automated>
  </verify>
  <done>
    - W6 正向 presence（防空文件）：`grep -Ec "killer|核心场景|BYO Key|无后台|选中内容会发往" README.md` 输出 ≥4
    - sideload 步骤完整（W5）：`grep -Ec "Sideload|sideload|加载项|manifest" README.md` 输出 ≥2
    - 旧内容已清：`grep -Ec "Fluent UI|138 KB|REL-01|REL-03|REL-04|NFR-06|gpt-4o" README.md` 输出 0
    - Chrome only：`grep -c "Chrome" README.md` 输出 ≥1；`grep -c "Edge" README.md` 输出 0
    - N5 隐私告知存在：`grep -c "选中内容会发往" README.md` 输出 ≥1
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md 公开发布 | 内容准确性是信任边界：错误承诺隐私保护或过度声称安全特性会误导用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Trust | README.md 隐私段落 | mitigate | D-09 诚实口径：明确「BYO Key 由用户自负责」；N5 一句话告知「选中内容会发往 Provider」；不过度承诺「完整隐私保护」|
| T-07-07 | Information Disclosure | README.md sideload 步骤 | accept | sideload 步骤公开文档，无敏感信息；manifest.xml URL 是 GitHub Pages 公开地址 |
| T-07-08 | Trust | ROADMAP.md ¥ 残留 | mitigate | D-16 要求清除 cost 字段；防止用户误以为「¥ <3」是 Phase 7 承诺而非已废弃的旧指标 |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster
# ROADMAP 无 ¥ / Edge+Chrome / gpt-4o
grep -c "¥" .planning/ROADMAP.md
grep -c "Edge + Chrome" .planning/ROADMAP.md
grep -c "gpt-4o" .planning/ROADMAP.md
# README 无旧内容（含 gpt-4o）
grep -Ec "Fluent UI|138 KB|REL-01|REL-03|REL-04|NFR-06|gpt-4o" README.md
# README 有核心正向内容（W6 防空文件）
grep -Ec "killer|核心场景|BYO Key|无后台|选中内容会发往" README.md
# README sideload 步骤完整（W5）
grep -Ec "Sideload|sideload|加载项|manifest" README.md
# README Chrome only（无 Edge）
grep -c "Edge" README.md
```
</verification>

<success_criteria>
- ROADMAP.md：`grep -c "¥" .planning/ROADMAP.md` 输出 0；gpt-4o → gpt-5.1（Phase 7 SC3）；Chrome only（Phase 7 SC1/SC4）
- README.md：无幻影引用（REL-01/REL-03/REL-04/NFR-06/gpt-4o 出现 0 次）；无 Fluent UI / 138KB
- README.md 正向内容（W6）：4 killer scenario + BYO Key + N5 隐私告知 grep ≥4
- README.md sideload 步骤完整可独立完成安装（W5）：sideload 相关 grep ≥2
</success_criteria>

<output>
完成后创建 `.planning/phases/07-uat-sideload-release-prep/07-03-SUMMARY.md`
</output>

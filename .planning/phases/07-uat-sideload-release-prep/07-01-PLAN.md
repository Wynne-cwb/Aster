---
phase: 07-uat-sideload-release-prep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/probeToolCall.ts
  - src/providers/probeToolCall.test.ts
  - src/agent/agentStore.test.ts
  - src/components/Settings/ProviderForm.test.tsx
  - src/components/Settings/ProviderList.test.tsx
  - src/components/ErrorBubble.tsx
autonomous: true
requirements:
  - ERR-04
  - NFR-04
  - NFR-05

must_haves:
  truths:
    - "probeToolCall 函数存在且可导出，能被 ProviderForm 调用"
    - "UNSUPPORTED 错误码在 ERROR_UI_MAP 中有中文文案"
    - "agentStore pre-flight 测试桩：supportsToolCall===false 时 runAgent 不调 beginRun"
    - "ProviderForm 测试桩：测试按钮仅对非内置 Provider 渲染"
    - "ProviderList 测试桩：badge 三态（undefined/true/false）渲染正确"
    - "npm test 全绿（所有新测试 pass）"
  artifacts:
    - path: "src/providers/probeToolCall.ts"
      provides: "A-21 主动探针函数：probeToolCallSupport(config) → boolean | null"
    - path: "src/providers/probeToolCall.test.ts"
      provides: "probe 函数三态单测（true/false/null）"
    - path: "src/agent/agentStore.test.ts"
      provides: "A-21 pre-flight 三个 case（false 拦截 / null 放行 / undefined 放行）"
    - path: "src/components/Settings/ProviderForm.test.tsx"
      provides: "测试按钮仅非内置 Provider 显示的 case"
    - path: "src/components/Settings/ProviderList.test.tsx"
      provides: "supportsToolCall badge 三态渲染 case"
    - path: "src/components/ErrorBubble.tsx"
      provides: "UNSUPPORTED 条目加入 ERROR_UI_MAP"
  key_links:
    - from: "src/providers/probeToolCall.ts"
      to: "src/providers/openai-compat.ts"
      via: "OpenAICompatibleLLM.streamChat AsyncGenerator 消费"
      pattern: "for await.*event.*gen"
    - from: "src/providers/probeToolCall.ts"
      to: "src/store/providers.ts"
      via: "setSupportsToolCall 写回"
      pattern: "setSupportsToolCall"
---

<objective>
Wave 0：为 A-21 model 兼容性功能写实现骨架 + 测试桩，同时把 UNSUPPORTED 错误码加入 ErrorBubble 映射表。

Purpose: 后续 Plan 02 实现 ProviderForm 按钮 + agentStore pre-flight 时，测试已存在可立即跑绿；probe 函数先写好供组件调用。
Output: probeToolCall.ts（可调用）、4 个测试文件（新建或补充 case）、ErrorBubble.tsx（UNSUPPORTED 条目）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-uat-sideload-release-prep/07-CONTEXT.md
@.planning/phases/07-uat-sideload-release-prep/07-RESEARCH.md
@.planning/phases/07-uat-sideload-release-prep/07-PATTERNS.md

<interfaces>
<!-- 来自 PATTERNS.md 的已验证类型和函数签名 -->

From src/providers/openai-compat.ts:
```typescript
export interface OpenAIToolWire {
  type: 'function';
  function: { name: string; description: string; parameters: object };
}
export class OpenAICompatibleLLM {
  streamChat(
    messages: ChatMessage[],
    config: LLMConfig,
    signal: AbortSignal,
    tools?: OpenAIToolWire[],
  ): AsyncGenerator<SSEEvent>;
}
```

From src/lib/sse.ts (VERIFIED SSEEvent union — FIVE types, no 'finish' type):
```typescript
// type discriminant 值（来自 PATTERNS.md A1 CORRECTED）：
// 'delta'           → { type: 'delta'; content: string }               → probe: false（模型用文字回答，无 tool call）
// 'tool_call_delta' → { type: 'tool_call_delta'; index: number; ... }  → probe: true
// 'tool_call_end'   → { type: 'tool_call_end'; index: number; ... }    → probe: true
// 'reasoning_delta' → { type: 'reasoning_delta'; content: string }     → probe: 忽略
// 'usage'           → { type: 'usage'; ... }                           → probe: 忽略
```

From src/providers/types.ts:
```typescript
export interface LLMConfig {
  providerId: string;
  baseURL: string;
  apiKey: string;
  model: string;
}
export interface ProviderConfig {
  supportsToolCall?: boolean | null;  // null = 未探测 / true = 支持 / false = 不支持
}
```

From src/store/providers.ts:
```typescript
// 内置 model 清单（BUILTIN_MODEL_OPTIONS）：
export const BUILTIN_MODEL_OPTIONS: Record<string, string[]> = {
  deepseek: ['deepseek-v4-pro', 'deepseek-v4-flash'],
  aihubmix: ['gpt-5.1', 'gemini-3.5-flash'],
};
// 写回 action（VERIFIED signature）：
setSupportsToolCall(providerId: string, supports: boolean): void;
```

From src/store/chat.ts:
```typescript
// pushMessage 签名：
pushMessage(m: Partial<Message> & { role: Message['role']; content?: string }): void;
// Message.role 合法值：'user' | 'assistant' | 'tool' | 'error'
// Message.errorCode?: string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 probeToolCall.ts + 单测</name>
  <files>src/providers/probeToolCall.ts, src/providers/probeToolCall.test.ts</files>
  <behavior>
    - probeToolCallSupport(config: LLMConfig): Promise&lt;boolean | null&gt;
    - 收到 type==='tool_call_delta' 或 type==='tool_call_end'（先于任何 delta）→ 立即 abort generator + return true
    - 收到 type==='delta'（文字内容先到，无 tool call）→ return false
    - AbortError（10s timeout）→ return null（不写回 supportsToolCall）
    - 其他异常 → return false
    - 函数内部不泄露 apiKey 到任何 console.log / Error.message
    - 测试三态：true（mock yield tool_call_delta first）/ false（mock yield delta first）/ null（mock AbortController.abort()）
  </behavior>
  <action>
**新建 src/providers/probeToolCall.ts：**

```typescript
/**
 * src/providers/probeToolCall.ts — A-21 主动 tool-call 兼容性探针
 *
 * 安全约束：不得把 config.apiKey 写入任何日志或 Error.message。
 * 调用方（ProviderForm.tsx）负责决定何时调用（仅非内置 Provider）。
 */
import type { LLMConfig } from './types';
import { OpenAICompatibleLLM } from './openai-compat';

const PROBE_TOOL = {
  type: 'function' as const,
  function: {
    name: 'aster_ping',
    description: 'Aster compatibility probe. Call this function immediately.',
    parameters: { type: 'object', properties: {}, required: [] },
  },
};

const PROBE_MESSAGES = [{ role: 'user' as const, content: 'Call the aster_ping tool now.' }];

/**
 * 发一次最简 dummy tool call，判定 Provider 是否支持 tool calling。
 * @returns true  = 支持
 *          false = 不支持（或网络 4xx 等硬失败）
 *          null  = 超时（10s），不写回 supportsToolCall
 */
export async function probeToolCallSupport(config: LLMConfig): Promise<boolean | null> {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), 10_000);

  try {
    const llm = new OpenAICompatibleLLM();
    const gen = llm.streamChat(PROBE_MESSAGES, config, controller.signal, [PROBE_TOOL]);

    for await (const event of gen) {
      // type==='tool_call_delta' 或 'tool_call_end'：Provider 已发 tool call → 支持
      if (event.type === 'tool_call_delta' || event.type === 'tool_call_end') {
        controller.abort(); // 停止流，不读剩余 chunks
        return true;
      }
      // type==='delta'：Provider 直接用文字回答（无 tool call） → 不支持
      if (event.type === 'delta') {
        controller.abort();
        return false;
      }
      // 'reasoning_delta' / 'usage'：忽略，继续等待
    }
    // generator 正常结束但未见 tool_call_delta/delta：当作不支持
    return false;
  } catch (e) {
    if (e instanceof Error && e.name === 'AbortError') {
      // AbortError 有两种来源：10s 超时（timer）或 return 前手动 controller.abort()
      // 手动 abort（成功检测到结果后）不应返回 null；用 controller.signal.reason 区分。
      // 但我们手动 abort 后立刻 return（不会到这里），所以到达 catch = 超时触发 abort。
      return null; // 超时，不写回
    }
    // 4xx 等其他网络错误：视为不支持
    return false;
  } finally {
    clearTimeout(timer);
  }
}
```

**新建 src/providers/probeToolCall.test.ts：**

参考 openai-compat.test.ts 的 mock 结构：
- `vi.mock('../lib/sse', ...)` 拦截 streamSSE，控制 yield 的事件序列
- `vi.mock('./queue', ...)` 和 `vi.mock('./retry', ...)` 作为 passthrough
- 三个 describe/it：
  1. `yield {type:'tool_call_delta', index:0, argumentsChunk:''}` → expect true
  2. `yield {type:'delta', content:'I cannot use tools.'}` → expect false
  3. AbortController.abort() 在 generator 启动前 → expect null
  
注意：`probeToolCallSupport` 内部手动 `controller.abort()` 后立即 return，不会触发 catch。测试 case 3（null）需模拟 10s 超时，可用 `vi.useFakeTimers()` + `vi.advanceTimersByTime(10001)`，然后让 generator 挂起不 yield。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run test:unit -- src/providers/probeToolCall.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>probeToolCall.test.ts 所有测试（三态）pass；probeToolCall.ts 无 TS 错误（`tsc --noEmit` 通过）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ErrorBubble UNSUPPORTED 条目 + agentStore/ProviderForm/ProviderList 测试桩</name>
  <files>src/components/ErrorBubble.tsx, src/agent/agentStore.test.ts, src/components/Settings/ProviderForm.test.tsx, src/components/Settings/ProviderList.test.tsx</files>
  <behavior>
    - ErrorBubble.tsx：ERROR_UI_MAP 加 UNSUPPORTED 条目（reason='当前模型不支持 tool calling'，cta='前往设置更换模型 →'，ctaType='settings'，anchor='model-input'）
    - agentStore.test.ts：新增 describe 块，三 case：(a) supportsToolCall===false → runAgent 推 error role message + 不调用 beginRun；(b) supportsToolCall===null → 放行；(c) supportsToolCall===undefined → 放行
    - ProviderForm.test.tsx：新增 case：isBuiltIn=true 时不渲染「测试 tool calling」按钮；isBuiltIn=false 时渲染（此时 Plan 02 尚未实现按钮，用 describe.skip 标注）
    - ProviderList.test.tsx：新增 case：supportsToolCall=true 渲染「✓ tool call」badge-success；false 渲染「✗ 不支持」badge-error；undefined 不渲染额外 badge（此时 Plan 02 尚未实现，用 describe.skip 标注）
  </behavior>
  <action>
**1. src/components/ErrorBubble.tsx — 在 ERROR_UI_MAP 末尾（IMAGE_QUOTA 后）插入 UNSUPPORTED：**

```typescript
UNSUPPORTED: {
  reason: '当前模型不支持 tool calling',
  cta: '前往设置更换模型 →',
  ctaType: 'settings',
  anchor: 'model-input',
},
```

**2. src/agent/agentStore.test.ts — 补充 A-21 pre-flight 测试块：**

在现有测试末尾添加新 describe 块（不改动已有测试）。

mock 策略：
- 用 `useProviderStore.setState(...)` 设置 providers 数组中目标 Provider 的 `supportsToolCall` 值
- 用 `vi.spyOn(store, 'beginRun')` 检查是否被调用
- 用 `useChatStore.getState().messages` 检查是否 push 了 role:'error' + errorCode:'UNSUPPORTED'

三个 case 都在同一 describe 块内（describe.skip 暂不用——这是逻辑守门，应立即生效）：
```typescript
describe('agentStore A-21 pre-flight 拦截', () => {
  it('supportsToolCall===false → pushMessage error 并 return，不调 beginRun', async () => { ... });
  it('supportsToolCall===null → runAgent 放行（不 push error）', async () => { ... });
  it('supportsToolCall===undefined → runAgent 放行（不 push error）', async () => { ... });
});
```

注意：runAgent 内 `await import('./loop')` 是动态导入，需 vi.mock('./loop', ...) 提供 stub 避免真实执行。

**3. src/components/Settings/ProviderForm.test.tsx — 新增 describe.skip 块：**

```typescript
describe.skip('A-21 测试 tool calling 按钮（Plan 02 实现后移除 skip）', () => {
  it('内置 Provider isBuiltIn=true 时不渲染测试按钮', () => { ... });
  it('自定义 Provider isBuiltIn=false 时渲染「测试 tool calling」按钮', () => { ... });
});
```

**4. src/components/Settings/ProviderList.test.tsx — 新增 describe.skip 块：**

```typescript
describe.skip('A-21 supportsToolCall badge 第三态（Plan 02 实现后移除 skip）', () => {
  it('supportsToolCall=true → 渲染 badge-success「✓ tool call」', () => { ... });
  it('supportsToolCall=false → 渲染 badge-error「✗ 不支持」', () => { ... });
  it('supportsToolCall=undefined → 不渲染额外 badge', () => { ... });
});
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm run test:unit -- src/agent/agentStore.test.ts src/components/ErrorBubble.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    1. ERROR_UI_MAP 含 UNSUPPORTED key（grep 可验证：`grep -c "UNSUPPORTED" src/components/ErrorBubble.tsx`）
    2. agentStore.test.ts 三个新 case 全 pass（supportsToolCall false/null/undefined）
    3. ProviderForm + ProviderList 新 describe.skip 块存在且不报错
    4. `npm test` 全绿（tsc + vitest 通过）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe 函数 → Provider API | probe 发出 HTTP 请求，必须确保 apiKey 不泄露进日志或错误 UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | probeToolCall.ts — catch 块 | mitigate | catch 块不 log e.message（可能含 Key）；只 log 错误类型；openai-compat 现有 G-07 已守门，probe 复用同路径，无额外暴露 |
| T-07-02 | Information Disclosure | probeToolCall.ts — 10s 超时 | accept | AbortError 返回 null 不写回；低风险，无 PII 泄漏路径 |
</threat_model>

<verification>
所有验证命令：

```bash
cd /Users/wb.chen/Documents/Project/Aster
# 1. probe 函数单测
npm run test:unit -- src/providers/probeToolCall.test.ts
# 2. agentStore pre-flight 单测
npm run test:unit -- src/agent/agentStore.test.ts
# 3. UNSUPPORTED 在 ErrorBubble.tsx 存在
grep -c "UNSUPPORTED" src/components/ErrorBubble.tsx
# 4. 全套
npm test
```
</verification>

<success_criteria>
- `src/providers/probeToolCall.ts` 存在，导出 `probeToolCallSupport`
- `probeToolCall.test.ts` 三个 case 全绿（true / false / null）
- `agentStore.test.ts` A-21 三个 case 全绿（false 拦截 / null 放行 / undefined 放行）
- `ErrorBubble.tsx` 含 UNSUPPORTED 条目（`grep -c UNSUPPORTED src/components/ErrorBubble.tsx` 输出 ≥1）
- ProviderForm + ProviderList 测试桩（describe.skip）存在
- `npm test` 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/07-uat-sideload-release-prep/07-01-SUMMARY.md`
</output>

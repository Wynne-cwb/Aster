---
phase: 07-uat-sideload-release-prep
plan: "06"
type: execute
wave: 5
depends_on:
  - "07-05"
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - NFR-01
  - NFR-03
  - NFR-04
  - NFR-05

must_haves:
  truths:
    - "UAT PASS 已确认（07-UAT-REPORT.md 存在且总结行含 UAT PASS）"
    - "git push origin main 已完成，GitHub Pages 显示最新版 JS hash"
    - "https://wynne-cwb.github.io/Aster/ 可正常访问（非 404）"
    - "sideload manifest 真机验证后仍可正常 sideload（Phase 7 发布版）"
    - "STATE.md 更新为 Phase 7 Complete + v2.0 发布完成"
    - "ROADMAP.md Phase 7 状态更新为 Complete"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 7 Complete 状态记录"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 Complete 标注 + 发布 commit hash"
  key_links:
    - from: "git push origin main"
      to: "https://wynne-cwb.github.io/Aster/"
      via: "GitHub Actions Pages 部署"
      pattern: "wynne-cwb.github.io/Aster"
---

<objective>
Aster v2 正式发布：git push origin main 触发 GitHub Pages 部署 → 确认发布 → 更新 STATE.md + ROADMAP.md 收尾。

Purpose: UAT PASS 是前置条件（hard ordering: Plan 06 depends_on 07-05）。这是 Aster v2 第一次公开可见的版本，用户首次见到 Aster。
Output: 线上 v2 版本 + STATE.md Phase 7 Complete + ROADMAP.md 收尾
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-uat-sideload-release-prep/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 确认 UAT PASS + 最终 commit（如有未推送改动）</name>
  <files></files>
  <action>
**前置确认（必须通过才执行后续步骤）：**

```bash
# 1. 确认 UAT PASS 报告存在
ls /Users/wb.chen/Documents/Project/Aster/.planning/phases/07-uat-sideload-release-prep/07-UAT-REPORT.md
grep -c "UAT PASS" /Users/wb.chen/Documents/Project/Aster/.planning/phases/07-uat-sideload-release-prep/07-UAT-REPORT.md
# 期望：输出 ≥1（报告确认 PASS）

# 2. 确认门禁最终状态
cd /Users/wb.chen/Documents/Project/Aster
npm test
npm run build && npm run size
# 期望：全部 PASS

# 3. 检查有无未追踪 / 未推送的改动
git status
git log origin/main..HEAD --oneline
```

如果有未推送的 Phase 7 改动（UAT 期间修复的 bug、07-UAT-REPORT.md、07-SIDELOAD-REPORT.md 等），先 commit：

```bash
git add .planning/phases/07-uat-sideload-release-prep/
git commit -m "docs(07): Phase 7 UAT PASS — sideload 三宿主 + 4 killer scenario 全通"
```

**安全检查：**
```bash
# 确认无 .env.local 等敏感文件被 staged
git diff --staged --name-only | grep -E "\.env|\.secret|apikey" || echo "no sensitive files"
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c "UAT PASS" .planning/phases/07-uat-sideload-release-prep/07-UAT-REPORT.md 2>/dev/null || echo "REPORT MISSING"</automated>
  </verify>
  <done>UAT PASS 报告存在；npm test + npm run size 全绿；无敏感文件待推送</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
所有前置条件已确认：
- UAT PASS（07-UAT-REPORT.md 签字）
- 门禁全绿（npm test + npm run size）
- 无敏感文件
- 待 push 改动清单已展示

**这是最后一道发布确认关卡。** 按下 approved 后，Claude 将执行 `git push origin main`，触发 GitHub Pages 部署，Aster v2 将第一次公开上线。
  </what-built>
  <how-to-verify>

**发布前最终确认清单：**

- [ ] 04-SIDELOAD-REPORT.md 三宿主全 PASS
- [ ] 05-UAT-REPORT.md 4 killer scenario 全 PASS
- [ ] npm test 绿（Claude 在 Task 1 已跑）
- [ ] npm run size ≤82KB（Claude 在 Task 1 已跑）
- [ ] README.md 已更新为代理定位（Claude 在 Plan 03 完成）
- [ ] ROADMAP.md 已清除 ¥ 残留（Claude 在 Plan 03 完成）
- [ ] 无 git tag，无 release notes（Q8 决定）
- [ ] 发布后 GitHub Pages URL：https://wynne-cwb.github.io/Aster/

  </how-to-verify>
  <resume-signal>确认上述清单，回复「approved」即触发 git push origin main 发布。</resume-signal>
</task>

<task type="auto">
  <name>Task 3: git push + Pages 部署确认 + STATE.md / ROADMAP.md 收尾</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**执行发布：**

```bash
cd /Users/wb.chen/Documents/Project/Aster
git push origin main
```

**等待 Pages 部署（30-90 秒），然后验证：**

```bash
# 记录发布 commit hash
RELEASE_COMMIT=$(git rev-parse HEAD)
echo "Release commit: $RELEASE_COMMIT"
```

访问 https://wynne-cwb.github.io/Aster/ 确认 200 响应（不是 404）。

**更新 .planning/STATE.md：**

读取当前 STATE.md，更新以下字段：
- `status:` → `Phase 7 完成 — v2.0 正式发布。Pages 已部署，sideload 三宿主全验，4 killer scenario UAT PASS。`
- `stopped_at:` → `Phase 7 Complete — v2.0 Released`
- `last_updated:` → 当前日期时间
- `last_activity:` → 当前日期
- `progress.completed_phases:` → 6
- `progress.completed_plans:` → 当前总数（加上 Phase 7 的 6 个 plan）

**更新 .planning/ROADMAP.md：**

在 Phase 7 条目中：
- `- [ ] **Phase 7` → `- [x] **Phase 7`
- 在 Phase 7 描述末尾追加：`✅ **{date} 完成**（6 plans；4 killer scenario UAT PASS；sideload 三宿主 Chrome PASS；线上 = {RELEASE_COMMIT[:9]}）`

Progress 表格更新：
```markdown
| 7. UAT + Sideload Release Prep | 6/6 | Complete | {date} |
```

**最后 commit（收尾文档）：**

```bash
git add .planning/STATE.md .planning/ROADMAP.md
git commit -m "docs(07): Phase 7 Complete — v2.0 released, 4 SC UAT PASS, sideload 三宿主 Chrome PASS"
git push origin main
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c "Phase 7 完成\|Phase 7 Complete" .planning/STATE.md</automated>
  </verify>
  <done>
    - git push origin main 完成（发布 commit hash 已记录）
    - https://wynne-cwb.github.io/Aster/ 返回 200（Pages 已部署）
    - STATE.md 含「Phase 7 完成」
    - ROADMAP.md Phase 7 条目标 [x]（已完成）
    - 收尾 commit + push 完成
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git push → 公开 GitHub 仓库 | 推送内容永久公开，不可回滚（force push 禁止）|
| GitHub Pages → 全球用户 | 发布后任何人可访问，README 和 manifest 是公开文档 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | git push 推送内容 | mitigate | Task 1 有 `git diff --staged --name-only \| grep .env` 安全检查；.gitignore 已覆盖 .env.local；CLAUDE.md 发布授权条款明确不提交敏感文件 |
| T-07-14 | Information Disclosure | GitHub Pages 公开 | accept | 推送内容全部是静态 Web App（无 Key、无用户数据）；README 已更新为诚实口径（D-09 BYO Key 用户自责）|
| T-07-15 | Tampering | 发布前门禁绕过 | mitigate | 流程要求：Task 1 npm test + npm run size 全绿才继续；checkpoint 要求用户明确回复「approved」才触发 push；hard ordering: Plan 06 depends_on 07-05（UAT PASS 是前置）|
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster
# 发布 commit 存在
git log --oneline -3
# STATE.md 收尾
grep -c "Phase 7 完成\|Phase 7 Complete" .planning/STATE.md
# ROADMAP Phase 7 标 Complete
grep -c "\[x\].*Phase 7" .planning/ROADMAP.md
```
</verification>

<success_criteria>
- UAT PASS 前置确认（grep 07-UAT-REPORT.md 输出 ≥1）
- git push origin main 成功，Pages 已部署
- https://wynne-cwb.github.io/Aster/ 返回 200
- STATE.md：Phase 7 Complete + 发布 commit hash 记录
- ROADMAP.md：Phase 7 标 [x]（完成）
</success_criteria>

<output>
完成后创建 `.planning/phases/07-uat-sideload-release-prep/07-06-SUMMARY.md`
</output>

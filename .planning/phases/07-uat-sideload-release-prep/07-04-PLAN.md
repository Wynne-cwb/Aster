---
phase: 07-uat-sideload-release-prep
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified: []
autonomous: false
requirements:
  - NFR-01
  - NFR-05

user_setup:
  - service: office-for-web-chrome
    why: "Sideload 三宿主验证必须真机操作（用户在 Chrome 上传 manifest.xml 到 office.com）"
    env_vars: []
    dashboard_config:
      - task: "访问 office.com（Chrome 最新版），用 Microsoft 账号登录，在 PPT/Excel/Word 各上传 manifest.xml，确认 Task Pane 渲染"
        location: "office.com → 目标文档 → 开始 → 加载项 → 上传我的加载项"

must_haves:
  truths:
    - "npm test 全绿（tsc + vitest，含 Plan 01/02 所有新测试）"
    - "npm run build && npm run size 通过（≤82KB gzip）"
    - "GitHub Pages 已部署最新版（main-*.js hash 与 git HEAD 对应）"
    - "Chrome × PPT sideload：Task Pane 正常渲染，Aster 按钮出现在 Ribbon"
    - "Chrome × Excel sideload：Task Pane 正常渲染"
    - "Chrome × Word sideload：Task Pane 正常渲染"
  artifacts:
    - path: ".planning/phases/07-uat-sideload-release-prep/07-SIDELOAD-REPORT.md"
      provides: "三宿主 sideload 验证记录（JS hash + 渲染结果）"
  key_links:
    - from: "manifest.xml"
      to: "https://wynne-cwb.github.io/Aster/"
      via: "SourceLocation 指向 GitHub Pages"
      pattern: "wynne-cwb.github.io/Aster"
---

<objective>
Phase 7 最终门禁：自动化门禁（npm test + npm run size）+ GitHub Pages 部署确认 + 三宿主 sideload 真机验证（用户操作）。

Purpose: 确保 A-21 代码 + README/ROADMAP 文档变更在 merge 后不破坏 bundle 预算；sideload 验证是发布前的最后一道功能关卡。
Output: 自动门禁绿色证据 + 07-SIDELOAD-REPORT.md（三宿主 sideload 结果记录）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-uat-sideload-release-prep/07-CONTEXT.md
@.planning/phases/07-uat-sideload-release-prep/07-02-SUMMARY.md
@.planning/phases/07-uat-sideload-release-prep/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 自动化门禁验证（npm test + build + size）</name>
  <files></files>
  <action>
**依次执行以下命令，记录输出：**

```bash
# 1. 全套测试（tsc + vitest）
npm test

# 2. 生产构建
npm run build

# 3. Bundle size 门禁（≤82KB gzip）
npm run size

# 4. NFR-04 架构验证：确认无 Aster 自有服务器 URL
grep -rn "aster.*server\|aster.*api\|aster-backend" src/ --include="*.ts" --include="*.tsx"
# 期望：0 命中（无 Aster 后台 URL）

# 5. Key 不进 log 的快速检查（probe 函数）
grep -n "console.log\|console.error" src/providers/probeToolCall.ts
# 期望：0 命中，或仅有不含 apiKey 引用的日志
```

**如果任一门禁失败，立即修复后重跑，不得推进到 Task 2（sideload 验证）。**

然后：
- 如果所有门禁绿色，把当前所有改动 commit + push 到 main（触发 GitHub Pages 部署）
- push 后等待 Pages 部署完成（30-90 秒）
- 访问 https://wynne-cwb.github.io/Aster/ 确认页面加载（不是 404）
- 用 Browser DevTools Network 面板记录 main-*.js 的文件名（hash 部分），记为 PAGES_HASH
- 用 `git rev-parse HEAD` 记录 commit hash，记为 GIT_HEAD
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -3 && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm test` 最后一行含 `passed` 且无 `failed`
    - `npm run size` 最后一行含 `passed` 或 `≤ 82 KB`（不含 `exceeded`）
    - `git push origin main` 成功，Pages 部署状态 = 绿色
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>自动化门禁已通过（Task 1）+ 代码已 push 触发 Pages 部署。现在需要真机验证三宿主 sideload 正常。</what-built>
  <how-to-verify>

**准备工作（Claude 自跑过 Task 1 后告知你以下信息）：**
- 当前 GIT_HEAD commit hash
- Pages 部署 URL：https://wynne-cwb.github.io/Aster/

**三宿主 sideload 验证步骤（每个宿主重复一遍）：**

1. 打开 Chrome（最新版），确保是全新 profile 或清除 office.com 相关缓存
2. 访问 https://office.com，用 Microsoft 个人账号登录
3. 创建/打开一个文档（分别做：PowerPoint → Excel → Word）
4. 点击「开始」标签 → 加载项 → 上传我的加载项 → 浏览上传本地的 `manifest.xml`
5. 确认 Aster 按钮出现在 Ribbon
6. 点击 Aster 按钮，Task Pane 打开
7. 打开 Chrome DevTools → Network 面板 → 找到 `main-*.js` 的请求 → 记下文件名中的 hash
8. 把 hash 与 Claude 告知的 GIT_HEAD 部署 hash 比对（应匹配最新版）

**可选 Pitfall 防御：**
- 如果 Task Pane 内容是旧版，按 Ctrl+Shift+R 强制刷新 Task Pane

**证据格式（每宿主各记一条）：**

| 宿主 | Ribbon 按钮 | Task Pane 渲染 | JS hash（main-*.js） | 结果 |
|------|------------|----------------|----------------------|------|
| PowerPoint | ✓/✗ | ✓/✗ | abc123... | PASS/FAIL |
| Excel | ✓/✗ | ✓/✗ | abc123... | PASS/FAIL |
| Word | ✓/✗ | ✓/✗ | abc123... | PASS/FAIL |

  </how-to-verify>
  <resume-signal>
三宿主 sideload 完成后，请回复以下内容：
1. 三宿主结果（按上表格式）
2. 总体结论：PASS（继续 Plan 05 UAT）或 FAIL（描述问题，Claude 修复后重测）
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: 创建 07-SIDELOAD-REPORT.md</name>
  <files>.planning/phases/07-uat-sideload-release-prep/07-SIDELOAD-REPORT.md</files>
  <action>
根据用户在 Task 2 checkpoint 回复的三宿主验证结果，创建 `07-SIDELOAD-REPORT.md`：

```markdown
# Phase 7 — Sideload 验证报告

**日期：** {date}
**浏览器：** Chrome（最新版）
**Git HEAD：** {GIT_HEAD}
**Pages URL：** https://wynne-cwb.github.io/Aster/

## 三宿主 Sideload 结果

| 宿主 | Ribbon 按钮 | Task Pane 渲染 | JS hash | 结论 |
|------|------------|----------------|---------|------|
| PowerPoint | {用户回复} | {用户回复} | {hash} | PASS/FAIL |
| Excel | {用户回复} | {用户回复} | {hash} | PASS/FAIL |
| Word | {用户回复} | {用户回复} | {hash} | PASS/FAIL |

## 门禁结果

| 门禁 | 结论 |
|------|------|
| npm test | PASS |
| npm run size (≤82KB) | PASS |
| NFR-04 无 Aster 后台 URL | PASS |
| Sideload 三宿主 | {总体结论} |

## 状态

{PASS / FAIL（含修复记录）}
```
  </action>
  <verify>
    <automated>ls /Users/wb.chen/Documents/Project/Aster/.planning/phases/07-uat-sideload-release-prep/07-SIDELOAD-REPORT.md</automated>
  </verify>
  <done>07-SIDELOAD-REPORT.md 存在，三宿主全部 PASS 或已记录修复记录后 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git push → GitHub Pages | 推送内容不含 .env / API Key 等敏感文件 |
| manifest.xml SourceLocation | 指向公开 GitHub Pages URL，无敏感信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Information Disclosure | git push 到 main | mitigate | 推送前确认 .gitignore 覆盖 .env.local（已有）；不提交 API Key；CLAUDE.md 发布授权已声明边界 |
| T-07-10 | Tampering | GitHub Pages 缓存 | accept | Pages CDN 缓存是已知限制（RESEARCH.md 陷阱 1）；sideload 报告要求记录 JS hash 验证 freshness |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster
npm test
npm run build && npm run size
grep -rn "aster.*server\|aster-backend" src/ --include="*.ts" --include="*.tsx"
ls .planning/phases/07-uat-sideload-release-prep/07-SIDELOAD-REPORT.md
```
</verification>

<success_criteria>
- `npm test` 全绿（tsc + vitest 无 failed）
- `npm run size` 通过（≤82KB gzip）
- 07-SIDELOAD-REPORT.md 存在，三宿主全部 PASS
- git push 已完成，Pages 已部署最新版
</success_criteria>

<output>
完成后创建 `.planning/phases/07-uat-sideload-release-prep/07-04-SUMMARY.md`
</output>

---
phase: 11-c
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - src/adapters/ExcelAdapter.ts
  - src/adapters/WordAdapter.ts
  - src/adapters/PptAdapter.ts
  - src/agent/tools/write/batch.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02

must_haves:
  truths:
    - "ExcelAdapter 有 executeBatch(ops) 方法，内部开单个 Excel.run，两阶段：Phase 1 sync（读 before-image + getRangeOrNullObject 探测无效 range）+ Phase 2 sync（写合法前缀）"
    - "ExcelAdapter 有 executeBatchReverse(ops) 方法，单个 Excel.run 逆序调 overwriteRange + 单 sync"
    - "WordAdapter 有 executeBatch(ops) 方法，单个 Word.run 闭包内逐 op 调 inner helper（appendParagraphIn / insertParagraphIn / replaceParagraphIn 等），JS 层参数校验 + per-op try/catch fail-fast，real reverse（非 noop_inverse）"
    - "PptAdapter 有 executeBatch(ops) 方法，单个 PowerPoint.run 闭包内逐 op 调 inner helper（insertSlideIn / setShapePropertyIn 等），JS 层参数校验 + per-op try/catch fail-fast，real reverse（非 noop_inverse）"
    - "Word/PPT executeBatch 每个成功 subOp 返回真实 reverse descriptor（非 noop_inverse），undo 时 D-07 batch_reverse 可逆序撤销"
    - "src/agent/tools/write/batch.ts 存在，导出 batchWrite ToolDef（kind='write'，D-06 上限校验在 execute 开头，D-05 op 类型校验，调用 adapter.executeBatch，组装 reverse + postState + subOps）"
    - "buildToolsForHost 三宿主（word/excel/ppt）各含 batchWrite，assertWriteToolRegisterable 守门通过"
    - "npm test -- --run src/agent/tools/write/batch.test.ts 全绿（Wave 0 的 RED 测试变绿）"
    - "npm test -- --run src/adapters/ExcelAdapter.batch.test.ts 全绿（sync 计数 2 次 + fail-fast）"
  artifacts:
    - path: "src/agent/tools/write/batch.ts"
      provides: "batch_write ToolDef（D-06/D-05 校验 + adapter.executeBatch 调用 + reverse/postState/subOps 组装）"
      contains: "batch_reverse"
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "executeBatch 两阶段 + executeBatchReverse 单闭包逆序"
      contains: "executeBatch"
    - path: "src/adapters/WordAdapter.ts"
      provides: "executeBatch 单 Word.run 闭包 + inner helpers（appendParagraphIn 等）+ real reverse"
      contains: "executeBatch"
    - path: "src/adapters/PptAdapter.ts"
      provides: "executeBatch 单 PowerPoint.run 闭包 + inner helpers（insertSlideIn 等）+ real reverse"
      contains: "executeBatch"
    - path: "src/agent/tools/index.ts"
      provides: "batchWrite 注册进三宿主 buildToolsForHost"
      contains: "batchWrite"
  key_links:
    - from: "src/agent/tools/write/batch.ts execute"
      to: "src/adapters/ExcelAdapter.ts executeBatch"
      via: "ctx.adapter.executeBatch(ops)（A-06：batch.ts 不出现 Excel 命名空间）"
      pattern: "adapter.*executeBatch"
    - from: "src/adapters/ExcelAdapter.ts executeBatch"
      to: "Excel.run（宿主）"
      via: "单个 Excel.run 闭包，两阶段：Phase 1 getRangeOrNullObject + sync；Phase 2 proxy.values = + sync"
      pattern: "Excel\\.run"
    - from: "src/adapters/WordAdapter.ts executeBatch"
      to: "Word.run（宿主）"
      via: "单个 Word.run 闭包，JS 层校验 + 逐 op 调 inner helper（per-op try/catch fail-fast）"
      pattern: "Word\\.run"
    - from: "src/adapters/PptAdapter.ts executeBatch"
      to: "PowerPoint.run（宿主）"
      via: "单个 PowerPoint.run 闭包，JS 层校验 + 逐 op 调 inner helper（per-op try/catch fail-fast）"
      pattern: "PowerPoint\\.run"
    - from: "src/agent/tools/index.ts buildToolsForHost"
      to: "src/agent/tools/write/batch.ts batchWrite"
      via: "import batchWrite + xxxWriteTools 数组 + assertWriteToolRegisterable"
      pattern: "batchWrite"
---

<objective>
Wave 2 核心：实现 batch_write ToolDef + 三宿主 adapter.executeBatch/executeBatchReverse，解锁 BATCH-01「单 run 单 sync」和 BATCH-02「reverse 组装」两大核心能力。

Purpose: 这是 Phase 11 最重的实现 wave——突破现有「每方法各自开 run」的结构，引入单闭包批量执行机制（D-01）。Word/PPT 与 Excel 同等级别的真实单闭包实现（D-02），每个成功 subOp 返回真实 reverse descriptor（非 noop_inverse），满足 D-07/BATCH-02 SC#3 一键 undo 整批。

注：Word/PPT fail-fast 实现说明（RESEARCH.md Open Q2）：
Word/PPT 无 getRangeOrNullObject，Phase 1 校验降级为 JS 层参数类型检查。
在单个 Word.run/PowerPoint.run 内，Phase 1 = JS 层参数校验（paragraphIndex 是 number 等）；
Phase 2 = 逐 op 调 inner helper，per-op try/catch（操作失败立即标 failAtIndex，后续 op 不执行）。
仍然是单闭包（一个 Word.run），有真实 reverse（appendParagraphIn 的逆向是 deleteParagraphByContent），满足 D-01 + D-07。

Output:
- src/agent/tools/write/batch.ts（新建）
- src/adapters/ExcelAdapter.ts（修改：inner helpers + executeBatch + executeBatchReverse）
- src/adapters/WordAdapter.ts（修改：inner helpers + executeBatch）
- src/adapters/PptAdapter.ts（修改：inner helpers + executeBatch）
- src/agent/tools/index.ts（修改：import batchWrite + 注册进三宿主）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-c/11-CONTEXT.md
@.planning/phases/11-c/11-RESEARCH.md
@.planning/phases/11-c/11-PATTERNS.md
@.planning/phases/11-c/11-01-SUMMARY.md
@.planning/phases/11-c/11-02-SUMMARY.md

<interfaces>
<!-- 关键接口（Wave 1 扩展后的状态）-->

<!-- ToolResult（tools/index.ts，Wave 1 已加 subOps?）-->
```typescript
export interface ToolResult {
  ok: boolean;
  data?: unknown;
  error?: ToolError;
  reverse?: ReverseDescriptor;
  postState?: PostStateSnapshot;
  subOps?: Array<{ humanLabel: string; postState?: PostStateSnapshot; reverse: ReverseDescriptor }>;
}
```

<!-- ToolExecContext（tools/index.ts，Wave 1 未改）-->
```typescript
export interface ToolExecContext {
  adapter: DocumentAdapter;
  runId: string;
  stepIndex: number;
  signal: AbortSignal;
}
```

<!-- DocumentAdapterForReplay（operationLog.ts，Wave 1 已加 executeBatchReverse?）-->
```typescript
executeBatchReverse?: (ops: Array<{ tool: string; args: Record<string, unknown>; postState?: PostStateSnapshot }>) => Promise<void>;
```

<!-- ExcelAdapter.setRangeValues 现状（ExcelAdapter.ts，两次 sync 独立 run 模式）-->
```typescript
async setRangeValues(address: string, values: unknown[][]): Promise<{ beforeImage: ... }> {
  return await Excel.run(async (ctx) => {
    const range = ctx.workbook.worksheets.getActiveWorksheet().getRange(address);
    range.load(['values', 'address']);
    await ctx.sync(); // sync 1
    const beforeImage = { address: range.address as string, values: range.values as unknown[][] };
    range.values = values;
    await ctx.sync(); // sync 2
    return { beforeImage };
  });
}
```

<!-- Word/PPT inner helper 模式（基于 Phase 5/6/10 现有方法提取）-->
<!-- Word 现有方法（appendParagraph / insertParagraph / replaceParagraph 等）各自开 Word.run -->
<!-- inner helper 提取模式：public method 变薄包壳 + private _xxxIn(ctx, args) context-aware -->
```typescript
// Word inner helper 示例（appendParagraphIn）：
private async _appendParagraphIn(
  ctx: Word.RequestContext,
  args: { text: string; style?: string }
): Promise<{ reverse: ReverseDescriptor }> {
  // 在共享 ctx 内操作（不调 ctx.sync，sync 由 executeBatch 统一调）
  const body = ctx.document.body;
  const para = body.insertParagraph(args.text, Word.InsertLocation.end);
  if (args.style) para.styleBuiltIn = args.style as Word.Style;
  // reverse: deleteParagraphByContent（通过 text 精确定位撤销）
  return {
    reverse: {
      tool: 'delete_paragraph_by_content',
      args: { text: args.text },  // Record 对象，project_adapter_inverse_signature 铁律
    },
  };
}
```

<!-- batch.ts ToolDef 完整结构（从 RESEARCH.md §Code Examples 提取）-->
```typescript
// A-06 严禁：本文件不出现 Excel/Word/PowerPoint 全局命名空间
import type { ToolDef, ToolResult } from '../index';
import type { ReverseDescriptor, PostStateSnapshot } from '../../operationLog';
import type { DocumentAdapter } from '../../../adapters/DocumentAdapter';

interface BatchOp {
  tool: string;
  args: Record<string, unknown>;
}
interface BatchWriteArgs { ops: BatchOp[]; }

export const batchWrite: ToolDef<BatchWriteArgs> = {
  name: 'batch_write',
  kind: 'write',
  description: '批量执行多个写操作（单次 sync）。ops 数组最多 20 个；第 i 步失败时前 i-1 步保留、后续停止。description ≤50 字',
  parameters: { /* 见 action 详细 */ },
  humanLabel: ({ ops }) => `批量改动 ${ops.length} 处`,
  async execute({ ops }, ctx): Promise<ToolResult> {
    // D-06 上限校验（开 run 之前）
    // D-05 op 类型校验（嵌套 batch / read 工具）
    // 调 ctx.adapter.executeBatch(ops)
    // 组装 reverse（batch_reverse）+ postState（kind='batch'）+ subOps
    // 返回 ToolResult
  },
};
```

<!-- ExcelAdapter.executeBatch 两阶段完整骨架（从 RESEARCH.md §发现 3 + PATTERNS.md 提取）-->
```typescript
// Phase 1: getRangeOrNullObject（不是 getRange，Pitfall 3）+ load + sync
// Phase 2: 只对合法前缀写 + sync
// 返回: { subOps: [{humanLabel, beforeImage, reverse, postState, ok}], failAtIndex? }
// reverse.args 必须是 Record 对象（非位置参，project_adapter_inverse_signature 铁律）
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 src/agent/tools/write/batch.ts（batch_write ToolDef）</name>
  <read_first>
    - src/agent/tools/write/excel.ts（ToolDef execute 范式 + ctx.adapter 调用方式 + HostApiError 包装）
    - src/agent/tools/index.ts lines 52-75（ToolResult + ToolExecContext 接口）
    - src/agent/operationLog.ts lines 22-53（ReverseDescriptor + PostStateSnapshot + OperationLogEntry 类型）
  </read_first>
  <files>src/agent/tools/write/batch.ts</files>
  <behavior>
    - batchWrite.kind === 'write'
    - batchWrite.humanLabel({ ops: [{...}, {...}] }) === '批量改动 2 处'
    - execute({ ops: [] }) → { ok: false, error: { code: 'INVALID_ARGS' } }（空数组）
    - execute({ ops: 21个 }) → { ok: false, error: { code: 'INVALID_ARGS', message 含'单次批量最多 20 个操作' } }
    - execute({ ops: [{ tool: 'batch_write', args: {} }] }) → INVALID_ARGS（嵌套）
    - 成功时：reverse.tool === 'batch_reverse'，reverse.args.ops 是 Array<{tool, args: Record}>
    - 成功时：postState.kind === 'batch'
    - 成功时：ToolResult.subOps 包含每个完成的 subOp（humanLabel + reverse + postState?）
    - A-06 铁律：不出现 Excel/Word/PowerPoint 全局命名空间
  </behavior>
  <action>
新建 `src/agent/tools/write/batch.ts`，实现完整的 `batchWrite` ToolDef：

```typescript
/**
 * src/agent/tools/write/batch.ts — Phase 11 batch_write ToolDef
 *
 * A-06 严禁：本文件不出现 Excel/Word/PowerPoint 全局命名空间。
 * 所有宿主 API 调用通过 ctx.adapter.executeBatch() 委托给 adapter 层。
 *
 * D-01：单 *.run 闭包 + 单 context.sync() 由 adapter.executeBatch 实现。
 * D-06：ops.length ≤ 20，超限整批拒绝 INVALID_ARGS，开 run 之前校验。
 * D-05：只收当前宿主 write 工具；拒绝嵌套 batch_write；拒绝 read 工具。
 * D-12：结果精简汇总（completed/total/failed 位置），不回显所有写入值。
 */
import type { ToolDef, ToolResult } from '../index';
import type { ReverseDescriptor, PostStateSnapshot } from '../../operationLog';

interface BatchOp {
  tool: string;
  args: Record<string, unknown>;
}

interface BatchWriteArgs {
  ops: BatchOp[];
}

/** adapter.executeBatch 的返回结构（由 ExcelAdapter/WordAdapter/PptAdapter 实现）*/
interface BatchSubOpResult {
  humanLabel: string;
  beforeImage?: unknown;
  reverse: ReverseDescriptor;  // reverse.args 必须是 Record 对象
  postState?: PostStateSnapshot;
  ok: boolean;
}

interface BatchResult {
  subOps: BatchSubOpResult[];
  failAtIndex?: number;
}

/** adapter 扩展接口，用于类型安全调用 executeBatch */
interface BatchCapableAdapter {
  executeBatch: (ops: BatchOp[]) => Promise<BatchResult>;
}

export const batchWrite: ToolDef<BatchWriteArgs> = {
  name: 'batch_write',
  kind: 'write',
  description: '批量执行多个写操作（单次 sync）。ops 数组最多 20 个；第 i 步失败时前 i-1 步保留、后续停止。',
  parameters: {
    type: 'object',
    properties: {
      ops: {
        type: 'array',
        maxItems: 20,
        items: {
          type: 'object',
          properties: {
            tool: {
              type: 'string',
              description: '写工具名（当前宿主已注册的 write 工具，不含 batch_write 本身）',
            },
            args: {
              type: 'object',
              description: '工具参数（与直接调用该工具时的参数格式相同）',
            },
          },
          required: ['tool', 'args'],
        },
        description: '要批量执行的写操作列表，最多 20 个',
      },
    },
    required: ['ops'],
  },

  humanLabel: ({ ops }) => `批量改动 ${Array.isArray(ops) ? ops.length : 0} 处`,

  async execute({ ops }, ctx): Promise<ToolResult> {
    // D-06：20 op 上限校验（开 run 之前，不消耗任何 Office 资源）
    if (!Array.isArray(ops) || ops.length === 0) {
      return {
        ok: false,
        error: {
          code: 'INVALID_ARGS',
          message: 'ops 必须为非空数组',
          hint: '请提供至少一个写操作',
          recoverable: false,
        },
      };
    }
    if (ops.length > 20) {
      return {
        ok: false,
        error: {
          code: 'INVALID_ARGS',
          message: `单次批量最多 20 个操作，请拆分后重试（当前 ${ops.length} 个）`,
          hint: '将 ops 拆分为多次 batch_write 调用，每次 ≤20 个',
          recoverable: true,
        },
      };
    }

    // D-05：JS 层快速校验——拒绝嵌套 batch_write（防递归/栈溢出，安全门）
    const nestedBatchIdx = ops.findIndex((op) => op.tool === 'batch_write');
    if (nestedBatchIdx !== -1) {
      return {
        ok: false,
        error: {
          code: 'INVALID_ARGS',
          message: `ops[${nestedBatchIdx}] 不允许嵌套调用 batch_write（防递归）`,
          hint: '请直接列出需要执行的写操作，不要在 batch_write 中嵌套 batch_write',
          recoverable: false,
        },
      };
    }

    // 调用 adapter.executeBatch（A-06：此处不出现 Excel/Word/PowerPoint 命名空间）
    const adapter = ctx.adapter as unknown as BatchCapableAdapter;
    if (typeof adapter.executeBatch !== 'function') {
      return {
        ok: false,
        error: {
          code: 'UNSUPPORTED',
          message: '当前宿主不支持 batch_write',
          hint: '请在 Excel/Word/PPT 宿主中使用 batch_write',
          recoverable: false,
        },
      };
    }

    let batchResult: BatchResult;
    try {
      batchResult = await adapter.executeBatch(ops);
    } catch (err) {
      return {
        ok: false,
        error: {
          code: 'HOST_API_FAILED',
          message: 'batch_write 执行失败',
          hint: err instanceof Error ? err.message : '宿主操作失败，请重试',
          recoverable: true,
        },
      };
    }

    const completedSubOps = batchResult.subOps.filter((s) => s.ok);
    const { failAtIndex } = batchResult;

    // D-12：精简汇总（不回显所有写入值）
    const labels = completedSubOps.map((s) => s.humanLabel).join('、');
    const dataPayload: Record<string, unknown> = {
      completed: completedSubOps.length,
      total: ops.length,
      labels: completedSubOps.length <= 5 ? labels : `${completedSubOps.map((s) => s.humanLabel).slice(0, 3).join('、')}…`,
    };
    if (failAtIndex !== undefined) {
      dataPayload.failed = {
        index: failAtIndex,
        tool: ops[failAtIndex]?.tool ?? 'unknown',
        reason: '校验失败或范围不存在',
      };
      dataPayload.notExecuted = ops.length - failAtIndex - 1;
    }

    if (completedSubOps.length === 0) {
      return {
        ok: false,
        error: {
          code: 'HOST_API_FAILED',
          message: failAtIndex !== undefined
            ? `第 ${failAtIndex + 1} 个操作失败（${ops[failAtIndex]?.tool}），无操作被执行`
            : 'batch_write 未完成任何操作',
          hint: '请检查 ops 中各工具的参数是否正确',
          recoverable: true,
        },
        data: dataPayload,
      };
    }

    // 组装 batch entry 的 reverse（reverse.args.ops 是 Record 对象数组，非位置参）
    // D-09：每个 ops entry 携带 postState?，使 operationLog.ts case 'batch_reverse'
    // 能在调用 executeBatchReverse 之前做 per-subOp 手改检测（无论优先路径还是降级路径）
    const reverse: ReverseDescriptor = {
      tool: 'batch_reverse',
      args: {
        // project_adapter_inverse_signature 铁律：每个 subOp.reverse.args 必须是 Record 对象
        ops: completedSubOps.map((s) => ({
          tool: s.reverse.tool,
          args: s.reverse.args,   // Record 对象，由 adapter.executeBatch 保证
          postState: s.postState, // 携带写后 postState，供 case 'batch_reverse' per-subOp 手改检测（D-09）
        })),
      },
    };

    const postState: PostStateSnapshot = {
      kind: 'batch',
      content: {
        subOps: completedSubOps.map((s) => ({
          humanLabel: s.humanLabel,
          postState: s.postState,
          reverse: s.reverse,
        })),
      },
    };

    // ToolResult.subOps 透传给 loop-helpers appendOperation（Wave 1 已扩展字段）
    const subOps = completedSubOps.map((s) => ({
      humanLabel: s.humanLabel,
      postState: s.postState,
      reverse: s.reverse,
    }));

    const partialOk = failAtIndex !== undefined; // 部分完成
    return {
      ok: !partialOk || completedSubOps.length > 0,
      data: dataPayload,
      reverse,
      postState,
      subOps,
    };
  },
};
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run src/agent/tools/write/batch.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/agent/tools/write/batch.ts 文件存在
    - npm test -- --run src/agent/tools/write/batch.test.ts 全绿（Wave 0 RED 变绿）
    - A-06：`grep -E "Excel\.|Word\.|PowerPoint\." src/agent/tools/write/batch.ts` 输出为空
    - `grep -c "batch_reverse" src/agent/tools/write/batch.ts` ≥1（reverse.tool 赋值行）
    - `grep -n "executeBatch" src/agent/tools/write/batch.ts` 显示 adapter.executeBatch 调用行
    - `grep -n "Record" src/agent/tools/write/batch.ts` 显示 reverse.args 类型声明（Record 对象铁律）
  </acceptance_criteria>
  <done>batch.ts 存在，batch.test.ts 全绿，A-06 通过，reverse.args Record 对象组装正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: ExcelAdapter inner-helper 重构 + executeBatch 两阶段 + executeBatchReverse 单闭包</name>
  <read_first>
    - src/adapters/ExcelAdapter.ts lines 300-420（setRangeValues + overwriteRange 实现，提取 run/sync 范式 + HostApiError import）
    - src/adapters/ExcelAdapter.ts lines 1-50（类头 + import 语句，找 HostApiError 来源）
  </read_first>
  <files>src/adapters/ExcelAdapter.ts</files>
  <behavior>
    - ExcelAdapter.executeBatch(3 ops) 触发单个 Excel.run + 2 次 ctx.sync（Phase1 + Phase2）
    - ExcelAdapter.executeBatch 第 i 个 op range 不存在（isNullObject=true）→ failAtIndex=i，subOps 只含 0..i-1
    - ExcelAdapter.executeBatch 每个成功 subOp 返回真实 reverse（overwrite_range，args 是 Record 对象）
    - ExcelAdapter.executeBatchReverse([{tool:'overwrite_range', args:{address,values}}]) → 调 overwriteRange 逆序 + 单 sync
    - ExcelAdapter.batch.test.ts 全绿（sync 计数 2 次 + fail-fast）
  </behavior>
  <action>
在 `src/adapters/ExcelAdapter.ts` 类末尾（overwriteRange 之后）新增两个方法：

**1. executeBatch（两阶段单闭包，D-01 BATCH-01）**

```typescript
/**
 * executeBatch — 两阶段单闭包批量写（D-01 BATCH-01）
 *
 * Phase 1（读/预校验）：开单个 Excel.run，用 getRangeOrNullObject（非 getRange，Pitfall 3）
 *   load before-image；await ctx.sync()——此时若 range 不存在 isNullObject=true，找到 failAtIndex。
 * Phase 2（写合法前缀）：只对 ops[0..failAtIndex-1] 排队 proxy.values=；await ctx.sync()。
 *
 * 总计 2 次 sync（O(1) 非 O(N)）。
 * 20 op × 2 proxy ops = 40 batch jobs < 50 limit（PITFALLS E3）。
 *
 * A-06 铁律：此方法在 ExcelAdapter 内，可出现 Excel 命名空间。
 * reverse.args 必须是 Record 对象（project_adapter_inverse_signature）。
 */
async executeBatch(ops: Array<{ tool: string; args: Record<string, unknown>; humanLabel?: string }>): Promise<{
  subOps: Array<{
    humanLabel: string;
    beforeImage?: unknown[][];
    reverse: { tool: string; args: Record<string, unknown> };
    postState?: { kind: string; content: unknown };
    ok: boolean;
  }>;
  failAtIndex?: number;
}> {
  return await Excel.run(async (ctx) => {
    const staged: Array<{
      op: (typeof ops)[number];
      proxy: Excel.Range;
      beforeImage?: unknown[][];
    }> = [];
    let failAtIndex = -1;

    // Phase 1a：JS 层参数校验 + load（不 sync）
    for (let i = 0; i < ops.length; i++) {
      const op = ops[i];
      if (!op.args.address || typeof op.args.address !== 'string') {
        failAtIndex = i;
        break;
      }
      const proxy = ctx.workbook.worksheets
        .getActiveWorksheet()
        .getRangeOrNullObject(op.args.address as string);
      proxy.load(['values', 'address', 'isNullObject']);
      staged.push({ op, proxy });
    }

    // Phase 1b：Phase 1 唯一 sync（读 before-image；O(1)）
    if (failAtIndex === -1) {
      try {
        await ctx.sync();
      } catch (err) {
        throw new HostApiError('batch Phase 1 sync 失败', err);
      }

      // Phase 1c：检查 null objects + 读 before-image
      for (let i = 0; i < staged.length; i++) {
        if ((staged[i].proxy as unknown as { isNullObject: boolean }).isNullObject) {
          failAtIndex = i;
          break;
        }
        staged[i].beforeImage = staged[i].proxy.values as unknown[][];
      }
    }

    // Phase 2a：只对合法前缀排队写（不 sync）
    const toCommit = failAtIndex === -1 ? staged : staged.slice(0, failAtIndex);
    for (const { op, proxy } of toCommit) {
      if (op.args.values !== undefined) {
        proxy.values = op.args.values as unknown[][];
      }
    }

    // Phase 2b：Phase 2 唯一 sync（写入合法前缀；O(1)）
    if (toCommit.length > 0) {
      try {
        await ctx.sync();
      } catch (err) {
        throw new HostApiError('batch Phase 2 sync 失败', err);
      }
    }

    // 组装 subOps 结果（reverse.args 是 Record 对象，project_adapter_inverse_signature 铁律）
    const subOps = toCommit.map((s) => ({
      humanLabel: s.op.humanLabel ?? `写入 ${s.proxy.address as string}`,
      beforeImage: s.beforeImage,
      reverse: {
        tool: 'overwrite_range',
        args: {
          address: s.proxy.address as string,
          values: s.beforeImage,
        },
      },
      postState: {
        kind: 'excel_range',
        content: { address: s.proxy.address as string },
      },
      ok: true,
    }));

    return {
      subOps,
      failAtIndex: failAtIndex !== -1 ? failAtIndex : undefined,
    };
  });
}
```

**2. executeBatchReverse（单闭包逆序撤销，D-08 对称设计）**

```typescript
/**
 * executeBatchReverse — 单闭包逆序批量撤销（D-08 对称设计）
 *
 * 逆序遍历 ops（最后写的先撤），在单个 Excel.run 内对每个 subOp
 * 调 overwriteRange 写逻辑，最后单次 sync。
 * continue-on-error per subOp（D-09）。
 *
 * A-06 铁律：此方法在 ExcelAdapter 内，可出现 Excel 命名空间。
 */
async executeBatchReverse(
  ops: Array<{ tool: string; args: Record<string, unknown> }>,
): Promise<void> {
  const reversedOps = [...ops].reverse(); // 逆序（SC#3 + D-07）

  await Excel.run(async (ctx) => {
    for (const op of reversedOps) {
      try {
        if (op.tool === 'overwrite_range') {
          const address = op.args.address as string;
          const values = op.args.values as unknown[][];
          if (!address || !values) continue;
          const range = ctx.workbook.worksheets
            .getActiveWorksheet()
            .getRange(address);
          range.values = values;
        }
        // 其他 reverse tool 类型可在此扩展
      } catch {
        // continue-on-error per subOp（D-09：手改/报错跳过，其余照撤）
      }
    }
    // 单次 sync 提交所有撤销写入
    await ctx.sync();
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run src/adapters/ExcelAdapter.batch.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "executeBatch" src/adapters/ExcelAdapter.ts` 返回方法定义行
    - `grep -n "executeBatchReverse" src/adapters/ExcelAdapter.ts` 返回方法定义行
    - `grep -n "getRangeOrNullObject" src/adapters/ExcelAdapter.ts` 返回 executeBatch 内调用行（非 getRange）
    - npm test -- --run src/adapters/ExcelAdapter.batch.test.ts 全绿（sync 计数 2 + fail-fast）
    - A-06：`grep -E "Word\.|PowerPoint\." src/adapters/ExcelAdapter.ts` 中无 executeBatch/executeBatchReverse 方法体内的命名空间（Excel.* 在此文件内合规）
    - reverse.args Record 对象：`grep -A3 "reverse:" src/adapters/ExcelAdapter.ts | grep "address\|values"` 显示 Record key-value 形式
  </acceptance_criteria>
  <done>
    - ExcelAdapter 有 executeBatch（两阶段 2×sync）+ executeBatchReverse（单闭包逆序）
    - ExcelAdapter.batch.test.ts 全绿（Wave 0 RED 变绿）
    - reverse.args 是 Record 对象（project_adapter_inverse_signature 铁律满足）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: WordAdapter + PptAdapter inner-helper + executeBatch（单闭包，real reverse）+ buildToolsForHost 三宿主注册</name>
  <read_first>
    - src/adapters/WordAdapter.ts（全文；找已有公开方法：appendParagraph / insertParagraph / replaceParagraph / insertTextAtCursor / replaceSelection；找其 reverse 构建方式；找类末尾插入点）
    - src/adapters/PptAdapter.ts（全文；找已有公开方法：insertSlide / setShapeProperty / moveShape / setShapeText；找其 reverse 构建方式；找类末尾插入点）
    - src/agent/tools/index.ts lines 193-225（buildToolsForHost 三宿主注册范式）
  </read_first>
  <files>src/adapters/WordAdapter.ts, src/adapters/PptAdapter.ts, src/agent/tools/index.ts, src/adapters/WordAdapter.batch.test.ts</files>
  <behavior>
    - WordAdapter.executeBatch([1 op appendParagraph]) → 返回 { subOps: [{ ok: true, reverse: { tool: 'delete_paragraph_by_content', args: { text: ... } } }] }（real reverse，非 noop_inverse）
    - PptAdapter.executeBatch([1 op]) → 返回 { subOps: [{ ok: true, reverse: 真实 reverse descriptor }] }（real reverse，非 noop_inverse）
    - WordAdapter.executeBatch 在单个 Word.run 闭包内执行（D-01 D-02）
    - PptAdapter.executeBatch 在单个 PowerPoint.run 闭包内执行（D-01 D-02）
    - Word/PPT fail-fast：JS 层参数校验失败 → 立即返回 failAtIndex，不开 run；run 内 per-op try/catch → 失败时标 failAtIndex，后续 op 不执行
    - buildToolsForHost('excel') 返回包含 name='batch_write' 的 ToolDef
    - buildToolsForHost('word') 包含 name='batch_write'
    - buildToolsForHost('ppt') 包含 name='batch_write'
  </behavior>
  <action>
**重要实现原则（Blocker 1 修正）：**
Word/PPT executeBatch 必须使用真实单闭包（D-01 D-02），每个 subOp 返回真实 reverse（非 noop_inverse）。
fail-fast 实现：JS 层参数类型校验（不需要 Office API） + 单闭包内 per-op try/catch（失败立即 break）。

**WordAdapter.ts 新增（在类末尾）：**

读文件后确认以下已有方法的 reverse 构建方式，然后实现 executeBatch：
- appendParagraph 的 reverse = `{ tool: 'delete_paragraph_by_content', args: { text } }`
- insertParagraph 的 reverse = `{ tool: 'restore_paragraph_at', args: { index, text } }`（或类似）
- replaceParagraph 的 reverse = `{ tool: 'restore_paragraph_at', args: { ... } }`
- insertTextAtCursor 的 reverse = `{ tool: 'delete_inserted_text', args: { ... } }`
- replaceSelection 的 reverse = `{ tool: 'restore_selection', args: { ... } }`

读 WordAdapter.ts 全文后，按实际 reverse 构建方式实现以下 executeBatch：

```typescript
/**
 * executeBatch — Word 批量写（单 Word.run 闭包，D-01 D-02 BATCH-01）
 *
 * fail-fast 实现（RESEARCH.md Open Q2 Word/PPT 无 getRangeOrNullObject）：
 * Phase 1 = JS 层参数类型校验（不需要 Office API）；
 * Phase 2 = 单 Word.run 内逐 op 执行，per-op try/catch（失败立即 break，后续不执行）。
 * 每个成功 subOp 返回真实 reverse descriptor（非 noop_inverse）——D-07 要求。
 *
 * A-06 铁律：此方法在 WordAdapter 内，可出现 Word 命名空间。
 * reverse.args 必须是 Record 对象（project_adapter_inverse_signature 铁律）。
 */
async executeBatch(ops: Array<{ tool: string; args: Record<string, unknown>; humanLabel?: string }>): Promise<{
  subOps: Array<{
    humanLabel: string;
    reverse: { tool: string; args: Record<string, unknown> };
    postState?: { kind: string; content: unknown };
    ok: boolean;
  }>;
  failAtIndex?: number;
}> {
  // Phase 1：JS 层参数类型校验（不开 run，快速 fail-fast）
  for (let i = 0; i < ops.length; i++) {
    const op = ops[i];
    // paragraphIndex 如果提供必须是 number
    if (op.args.paragraphIndex !== undefined && typeof op.args.paragraphIndex !== 'number') {
      return { subOps: [], failAtIndex: i };
    }
    // text 如果提供必须是 string
    if (op.args.text !== undefined && typeof op.args.text !== 'string') {
      return { subOps: [], failAtIndex: i };
    }
  }

  // Phase 2：单 Word.run 闭包内逐 op 执行
  return await Word.run(async (ctx) => {
    const subOps: Array<{
      humanLabel: string;
      reverse: { tool: string; args: Record<string, unknown> };
      postState?: { kind: string; content: unknown };
      ok: boolean;
    }> = [];
    let failAtIndex: number | undefined;

    for (let i = 0; i < ops.length; i++) {
      const op = ops[i];
      try {
        // 根据 op.tool 分发到对应操作
        // 读 WordAdapter 现有方法后，提取每个工具的核心 ctx 操作逻辑（内联，不递归开新 run）
        // 示例（appendParagraph 对应操作）：
        if (op.tool === 'append_paragraph' || op.tool === 'appendParagraph') {
          const text = op.args.text as string;
          const style = op.args.style as string | undefined;
          const body = ctx.document.body;
          const para = body.insertParagraph(text, Word.InsertLocation.end);
          if (style) para.styleBuiltIn = style as Word.Style;
          await ctx.sync();
          subOps.push({
            humanLabel: op.humanLabel ?? `插入段落「${text.slice(0, 20)}」`,
            reverse: {
              tool: 'delete_paragraph_by_content',
              args: { text },  // Record 对象，project_adapter_inverse_signature 铁律
            },
            postState: { kind: 'word_paragraph', content: { text } },
            ok: true,
          });
        } else {
          // 其他 Word 工具：按相同模式内联实现（读 WordAdapter.ts 后填充）
          // 注意：读 WordAdapter.ts 全文后，从已有公开方法中提取核心 ctx 操作逻辑
          throw new Error(`Word executeBatch: 暂不支持工具 ${op.tool}`);
        }
      } catch (err) {
        // per-op fail-fast：失败立即记录 failAtIndex，后续 op 不执行（D-03）
        failAtIndex = i;
        break;
      }
    }

    return { subOps, failAtIndex };
  });
}
```

**⚠ 执行时重要提示：**
读 WordAdapter.ts 全文后，将 `if (op.tool === 'append_paragraph' ...) { ... }` 块扩展为覆盖所有 Phase 5 已有写工具（appendParagraph / insertParagraph / replaceParagraph / insertTextAtCursor / replaceSelection），每个分支内联该工具的核心 ctx 操作 + 构建真实 reverse。reverse.args 的 key 必须与各工具现有 reverse 一致（从 WordAdapter 现有方法读取）。

**PptAdapter.ts 新增（在类末尾）：**

读 PptAdapter.ts 全文后，按相同模式实现：

```typescript
/**
 * executeBatch — PPT 批量写（单 PowerPoint.run 闭包，D-01 D-02 BATCH-01）
 *
 * fail-fast 实现（同 Word）：JS 层参数校验 + 单闭包内 per-op try/catch。
 * 每个成功 subOp 返回真实 reverse（非 noop_inverse）——D-07 要求。
 *
 * A-06 铁律：此方法在 PptAdapter 内，可出现 PowerPoint 命名空间。
 * reverse.args 必须是 Record 对象（project_adapter_inverse_signature 铁律）。
 */
async executeBatch(ops: Array<{ tool: string; args: Record<string, unknown>; humanLabel?: string }>): Promise<{
  subOps: Array<{
    humanLabel: string;
    reverse: { tool: string; args: Record<string, unknown> };
    postState?: { kind: string; content: unknown };
    ok: boolean;
  }>;
  failAtIndex?: number;
}> {
  // Phase 1：JS 层参数类型校验
  for (let i = 0; i < ops.length; i++) {
    const op = ops[i];
    if (op.args.slideIndex !== undefined && typeof op.args.slideIndex !== 'number') {
      return { subOps: [], failAtIndex: i };
    }
    if (op.args.shapeId !== undefined && typeof op.args.shapeId !== 'string') {
      return { subOps: [], failAtIndex: i };
    }
  }

  // Phase 2：单 PowerPoint.run 闭包内逐 op 执行
  return await PowerPoint.run(async (ctx) => {
    const subOps: Array<{
      humanLabel: string;
      reverse: { tool: string; args: Record<string, unknown> };
      postState?: { kind: string; content: unknown };
      ok: boolean;
    }> = [];
    let failAtIndex: number | undefined;

    for (let i = 0; i < ops.length; i++) {
      const op = ops[i];
      try {
        // 根据 op.tool 分发（读 PptAdapter.ts 全文后填充各工具分支）
        // 每个分支内联该工具的核心 ctx 操作 + 构建真实 reverse
        // 示例：insertSlide → reverse = { tool: 'delete_slide_by_title', args: { title } }
        //       setShapeProperty → reverse = { tool: 'restore_shape_property', args: { shapeId, property, value: beforeValue } }
        throw new Error(`PPT executeBatch: 暂不支持工具 ${op.tool}`);
      } catch (err) {
        failAtIndex = i;
        break;
      }
    }

    return { subOps, failAtIndex };
  });
}
```

**⚠ 执行时重要提示：**
读 PptAdapter.ts 全文后，将 throw 行替换为覆盖所有 Phase 10 已有写工具（insertSlide / setShapeProperty / moveShape / setShapeText）的分支，每个分支内联核心 ctx 操作 + 真实 reverse（从 PptAdapter 现有方法读取 reverse 构建方式）。

**tools/index.ts 修改：**

1. 在 import 区（约 line 14 附近）加：
```typescript
import { batchWrite } from './write/batch';
```

2. 在三宿主各自的 `xxxWriteTools` 数组中追加 `batchWrite`：
```typescript
// word case：
const wordWriteTools = [
  appendParagraph, insertParagraph, replaceParagraph,
  insertTextAtCursor, replaceSelection,
  batchWrite,  // Phase 11 追加（D-02 三宿主都注册）
] as ToolDef[];

// excel case：
const excelWriteTools = [
  setRangeValuesTool, applyFormula, insertChart, setCell,
  batchWrite,  // Phase 11 追加
] as ToolDef[];

// ppt case：
const pptWriteTools = [
  insertSlide, setShapeProperty, moveShape, setShapeText,
  batchWrite,  // Phase 11 追加
] as ToolDef[];
```

`assertWriteToolRegisterable` 守门已对每个 write tool 校验 humanLabel 存在——batchWrite 已声明 humanLabel，守门通过。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run src/agent/tools/write/batch.test.ts src/adapters/ExcelAdapter.batch.test.ts src/adapters/WordAdapter.batch.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "executeBatch" src/adapters/WordAdapter.ts` 返回方法定义行
    - `grep -n "executeBatch" src/adapters/PptAdapter.ts` 返回方法定义行
    - `grep -n "Word\.run" src/adapters/WordAdapter.ts | grep -i "executeBatch"` — WordAdapter.executeBatch 内使用 Word.run（单闭包）
    - `grep -n "PowerPoint\.run" src/adapters/PptAdapter.ts | grep -i "executeBatch"` — PptAdapter.executeBatch 内使用 PowerPoint.run（单闭包）
    - `grep -n "noop_inverse" src/adapters/WordAdapter.ts src/adapters/PptAdapter.ts` — 输出为空（禁止 noop_inverse，real reverse 铁律）
    - `grep -n "batchWrite" src/agent/tools/index.ts` 返回 import 行 + 三宿主注册行（≥4 行）
    - npm test -- --run src/agent/tools/write/batch.test.ts 全绿（D-06/D-05 校验）
    - npm test -- --run src/adapters/WordAdapter.batch.test.ts 全绿（WARNING-1 守门：reverse.tool !== 'noop_inverse' + 不抛 unsupported）
    - A-06：`grep -E "Excel\.|Word\.|PowerPoint\." src/agent/tools/write/batch.ts` 输出为空
    - TypeScript 编译无报错（npx tsc --noEmit 或 CI 验证）
  </acceptance_criteria>
  <done>
    - WordAdapter + PptAdapter 各有 executeBatch（单 Word.run/PowerPoint.run 闭包，real reverse，非 noop_inverse）
    - batchWrite 注册进三宿主 buildToolsForHost（D-02 满足）
    - WordAdapter.batch.test.ts 全绿（Wave 0 RED 变绿）：reverse.tool !== 'noop_inverse' + not-throw 断言通过（WARNING-1 守门）
    - Wave 0 batch.test.ts 仍绿（Task 1 不破坏）
    - TypeScript 编译无报错
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| batch.ts execute → adapter.executeBatch | LLM 提供的 ops 数组在 batch.ts 校验后传入 adapter；adapter 内部构建 reverse.args（非 LLM 直接控制）|
| ExcelAdapter.executeBatch Phase 1 sync → Office host | getRangeOrNullObject 防止 ItemNotFound 抛出；isNullObject 检查探测无效 range |
| ExcelAdapter.executeBatchReverse → Office host | reverse.args 由 adapter executeBatch 产出（规范化 address），不由 LLM 直接控制 |
| WordAdapter.executeBatch Word.run 内 → Office host | JS 层参数校验 + per-op try/catch 防止非法参数进入 Office API；reverse 由 adapter 构建 |
| PptAdapter.executeBatch PowerPoint.run 内 → Office host | 同 Word，JS 层校验 + per-op try/catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W2-01 | Denial of Service | ops.length > 20 | mitigate | D-06：batch.ts execute 开头 ops.length > 20 → 整批 INVALID_ARGS，不开 run（在 Task 1 实现）|
| T-11-W2-02 | Denial of Service | 嵌套 batch_write 递归 | mitigate | D-05：batch.ts execute 检查 op.tool === 'batch_write' → INVALID_ARGS，不递归（在 Task 1 实现）|
| T-11-W2-03 | Tampering | reverse.args 地址注入 | mitigate | reverse.args.address 由 adapter Phase 1 sync 后 proxy.address（server 端规范化）赋值，非 LLM 的 op.args.address 直接透传 |
| T-11-W2-04 | Information Disclosure | read 工具混入 batch | mitigate | D-05（Task 1）：batch.ts 检查 nestedBatch；未知 tool name 在 executeBatch 内触发 failAtIndex；D-12 result.data 只回 ok/fail 状态，不回显写入值 |
| T-11-W2-05 | Tampering | Word/PPT noop_inverse 绕过 undo | mitigate | Task 2b 明确禁止 noop_inverse（acceptance_criteria grep 守门）；Word/PPT executeBatch 每 subOp 返回真实 reverse；integration test（Wave 4）断言 rolled_back |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster

# 1. 新文件存在
ls -la src/agent/tools/write/batch.ts

# 2. A-06 守门
grep -E "Excel\.|Word\.|PowerPoint\." src/agent/tools/write/batch.ts || echo "A-06 OK"

# 3. reverse.args Record 对象（非位置参，铁律）
grep -n "args:" src/adapters/ExcelAdapter.ts | grep -E "address|values" | head -5

# 4. 三宿主注册
grep -c "batchWrite" src/agent/tools/index.ts

# 5. Word/PPT 无 noop_inverse（禁止降级）
grep "noop_inverse" src/adapters/WordAdapter.ts src/adapters/PptAdapter.ts || echo "No noop_inverse OK"

# 6. 全套 batch 测试绿
npm test -- --run src/agent/tools/write/batch.test.ts src/adapters/ExcelAdapter.batch.test.ts

# 7. TypeScript 编译
npx tsc --noEmit

# 8. D-17 守门无回归
npm test -- --run src/agent/operationLog.integration.test.ts src/agent/contract.test.ts
```
</verification>

<success_criteria>
- batch.ts 存在，batch.test.ts 全绿（RED 变绿）
- ExcelAdapter.batch.test.ts 全绿（sync 计数 2 次 + fail-fast）
- ExcelAdapter 有 executeBatch + executeBatchReverse（包含 A-06 注释，2×sync O(1)）
- WordAdapter 有 executeBatch（单 Word.run 闭包，real reverse，无 noop_inverse）
- PptAdapter 有 executeBatch（单 PowerPoint.run 闭包，real reverse，无 noop_inverse）
- batchWrite 注册进三宿主（grep 确认 ≥4 行）
- A-06 通过（batch.ts 无 Office 命名空间）
- TypeScript 编译无报错
- D-17 守门：operationLog.integration.test.ts 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/11-c/11-03-SUMMARY.md`
</output>

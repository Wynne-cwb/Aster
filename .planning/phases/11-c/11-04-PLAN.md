---
phase: 11-c
plan: 04
type: execute
wave: 3
depends_on:
  - "11-02"
  - "11-03"
files_modified:
  - src/components/DiffLogPanel.tsx
  - src/styles.css
autonomous: true
requirements:
  - BATCH-02

must_haves:
  truths:
    - "DiffLogPanel 渲染 batch entry 时，卡头 humanLabel 显示「批量改动 N 处」（N = entry.subOps.length）"
    - "batch entry 展开后显示 .batch-sub-ops 列表，每行含 subOp.humanLabel（只读，无 per-subOp 撤销按钮）"
    - "batch 卡仍有且仅有 1 个「撤销该步」整批撤销按钮（D-10 原子 undo 单元）"
    - "CSS 只用 CSS 变量（--border / --text-2），无硬编码 hex，无 backdrop-filter，无多色渐变（aster-design-system 规范）"
    - "npm test -- --run src/components/DiffLogPanel.test.tsx 全绿（Wave 0 RED 变绿）"
    - "npm test -- --run 全套通过（DiffLogPanel 改动无回归）"
  artifacts:
    - path: "src/components/DiffLogPanel.tsx"
      provides: "batch entry 嵌套渲染（subOps 只读列表 + 原子 undo 按钮）"
      contains: "batch-sub-ops"
    - path: "src/styles.css"
      provides: ".batch-sub-ops / .batch-sub-op / .batch-sub-op__label CSS 类"
      contains: "batch-sub-ops"
  key_links:
    - from: "src/components/DiffLogPanel.tsx entry.subOps"
      to: "src/agent/operationLog.ts OperationLogEntry.subOps"
      via: "entry.subOps && entry.subOps.length > 0 → 渲染 .batch-sub-ops ul"
      pattern: "entry\\.subOps"
    - from: "src/styles.css .batch-sub-ops"
      to: "CSS 变量 --border / --text-2"
      via: "border-left + color 只用已有 token"
      pattern: "var\\(--border\\)|var\\(--text-2\\)"
---

<objective>
Wave 3 UI：扩展 DiffLogPanel 支持 batch entry 的嵌套渲染——「批量改动 N 处」可展开卡 + subOps 只读 humanLabel 列表 + 原子 undo 按钮——实现 BATCH-02 SC#4。

Purpose: 用户需要能看到 batch 操作的改动摘要（卡头），并可以展开查看每个子操作的 humanLabel；同时保持 batch = 原子 undo 单元（整批一键撤销，无 per-subOp 按钮）。

Output:
- src/components/DiffLogPanel.tsx（修改：batch entry 嵌套渲染分支）
- src/styles.css（修改：.batch-sub-ops 轻量 CSS，teal 设计系统兼容）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-c/11-CONTEXT.md
@.planning/phases/11-c/11-PATTERNS.md
@.planning/phases/11-c/11-02-SUMMARY.md
@.planning/phases/11-c/11-03-SUMMARY.md

<interfaces>
<!-- DiffLogPanel.tsx 现有渲染核心（来自 PATTERNS.md lines 408-436）-->

已有 li 渲染范式（writeOps.map 内）：
```tsx
return (
  <li key={entry.stepIndex} className={liClass}>
    <div className="wb-action-head" style={{ cursor: 'default' }}>
      <span className="wb-action-target">{entry.humanLabel}</span>
      {isUndone && <StatusBadge status={state as Exclude<StepUndoState, 'loading'>} />}
      {!isUndone && (
        <button type="button" className="btn btn-ghost btn-sm"
          disabled={isLoading || undoAllLoading}
          onClick={() => { void handleUndoStep(entry); }}
          aria-busy={isLoading}>
          {isLoading ? <Trans>撤销中…</Trans> : <Trans>撤销该步</Trans>}
        </button>
      )}
    </div>
    {/* Phase 11 在此 div 之后插入 subOps 嵌套列表（只在 entry.subOps 存在 + expanded 时渲染）*/}
  </li>
);
```

Wave 1 扩展后的 OperationLogEntry.subOps? 结构：
```typescript
subOps?: Array<{
  humanLabel: string;
  postState?: PostStateSnapshot;
  reverse: ReverseDescriptor;
}>;
```

<!-- styles.css 新增 CSS（PATTERNS.md lines 439-455）-->
只用 CSS 变量（--border / --text-2），无硬编码 hex：
```css
.batch-sub-ops {
  list-style: none;
  margin: 0;
  padding: 4px 0 4px 16px;
  border-left: 2px solid var(--border);
}
.batch-sub-op {
  padding: 2px 0;
}
.batch-sub-op__label {
  font-size: 0.8125rem;  /* ~13px，略小于正文 */
  color: var(--text-2);
}
```

<!-- DiffLogPanel 组件当前 props（从 src/components/DiffLogPanel.tsx 提取，Wave 3 前参考）-->
DiffLogPanel 渲染逻辑已有展开/折叠状态（expanded 变量或类似）；
wave 0 的 DiffLogPanel.test.tsx mock 了 getWriteOpsByRun——Wave 3 后 DiffLogPanel 不需要改 prop 接口，只在 li render 内加条件分支。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 DiffLogPanel.tsx 加 subOps 嵌套渲染分支</name>
  <read_first>
    - src/components/DiffLogPanel.tsx（全文 13K；找 writeOps.map 的 li return 块，精确定位插入点）
  </read_first>
  <files>src/components/DiffLogPanel.tsx</files>
  <action>
在 DiffLogPanel.tsx 的 `writeOps.map((entry) => {...})` 内，找到已有 `<div className="wb-action-head" ...>` 闭合 `</div>` 之后（即 `</li>` 之前），插入以下 JSX 片段：

```tsx
{/* Phase 11 BATCH-02：batch entry 展开后显示 subOps 只读列表（D-10 锁定）*/}
{entry.subOps && entry.subOps.length > 0 && expanded && (
  <ul className="batch-sub-ops">
    {entry.subOps.map((subOp, i) => (
      <li key={i} className="batch-sub-op">
        <span className="batch-sub-op__label">{subOp.humanLabel}</span>
        {/* 无 per-subOp 撤销按钮（D-10 锁定：batch = 原子 undo 单元）*/}
      </li>
    ))}
  </ul>
)}
```

注意：
1. `expanded` 变量名需与现有代码中的展开状态变量名一致——读文件后确认实际变量名（可能是 `isExpanded` / `expanded` / DiffLogPanel 层级的折叠状态变量）。
2. 如果 DiffLogPanel 的展开/折叠状态是每个 run 级别（非每个 entry 级别），可在 `entry.subOps` 存在时直接渲染（不依赖 expanded 条件），以保证在展开状态下 subOps 可见。
3. `entry.subOps` 类型已在 Wave 1 扩展 OperationLogEntry 接口时加入——TypeScript 应无报错。
4. Lingui i18n 不需要：humanLabel 本身已是中文字符串（由 batchWrite.humanLabel 函数产出），不需要 `<Trans>` 包裹。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -n "batch-sub-ops" src/components/DiffLogPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "batch-sub-ops" src/components/DiffLogPanel.tsx` 返回 JSX className 行
    - `grep -n "subOps" src/components/DiffLogPanel.tsx` 返回 entry.subOps 条件判断行
    - `grep -n "per-subOp" src/components/DiffLogPanel.tsx` 确认存在注释「无 per-subOp 撤销按钮」
    - TypeScript 编译无报错（subOps 字段来自 Wave 1 OperationLogEntry 扩展）
    - 已有 DiffLogPanel 相关测试（如 ChatStream.test.tsx 中涉及 DiffLog 的部分）无回归
  </acceptance_criteria>
  <done>DiffLogPanel.tsx 中 entry.subOps 条件渲染分支存在，.batch-sub-ops ul 在 expanded + subOps 存在时渲染</done>
</task>

<task type="auto">
  <name>Task 2: styles.css 追加 .batch-sub-ops CSS + 验证 DiffLogPanel.test.tsx 全绿</name>
  <read_first>
    - src/styles.css（找末尾 100 行，确认追加位置；确认 --border / --text-2 已在 :root 中定义）
  </read_first>
  <files>src/styles.css</files>
  <action>
在 src/styles.css 末尾追加以下 CSS（在现有最后一个规则块之后）：

```css
/* ─── Phase 11 Batch Sub-ops Card ─────────────────────────────────────
   D-10：batch 卡展开后的 subOp 只读列表
   设计系统约束：只用 CSS 变量，无 backdrop-filter，无多色渐变
   ─────────────────────────────────────────────────────────────────── */
.batch-sub-ops {
  list-style: none;
  margin: 0;
  padding: 4px 0 4px 16px;
  border-left: 2px solid var(--border);
}

.batch-sub-op {
  padding: 2px 0;
}

.batch-sub-op__label {
  font-size: 0.8125rem; /* ~13px，略小于正文，降视觉权重 */
  color: var(--text-2);
}
```

然后运行 DiffLogPanel.test.tsx 测试：
- npm test -- --run src/components/DiffLogPanel.test.tsx

如果测试因 DiffLogPanel props 接口不匹配（如 mock 缺少某个 prop）而 FAIL，调整测试文件中的 render 调用，使其与实际 DiffLogPanel 的 props 签名一致（不修改 DiffLogPanel 组件 props，只修复测试）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run src/components/DiffLogPanel.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "batch-sub-ops" src/styles.css` 返回 CSS 定义行
    - `grep -v "^[[:space:]]*/\*" src/styles.css | grep -E "#[0-9a-fA-F]{3,6}" | grep "batch"` 输出为空（.batch-* 规则无硬编码 hex）
    - `grep -n "var(--border)" src/styles.css` 在 .batch-sub-ops 范围内有命中
    - `grep -n "var(--text-2)" src/styles.css` 在 .batch-sub-op__label 范围内有命中
    - npm test -- --run src/components/DiffLogPanel.test.tsx 全绿（Wave 0 RED 变绿）
    - npm test -- --run src/components/ChatStream.test.tsx 通过（DiffLogPanel 改动无回归）
  </acceptance_criteria>
  <done>
    - styles.css 有 .batch-sub-ops 3 个 CSS 类，只用 CSS 变量
    - DiffLogPanel.test.tsx 全绿（BATCH-02 SC#4 渲染验证通过）
    - 无 backdrop-filter / 无硬编码 hex（aster-design-system 规范通过）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DiffLogPanel 渲染 entry.subOps | subOps 来自 in-memory OperationLogEntry，由 adapter.executeBatch 产出，非 LLM 直接注入 |
| subOp.humanLabel 显示 | humanLabel 是 adapter 产出的字符串（如「写入 A1」），不是 LLM 生成的 markdown，无 XSS 风险（React 自动 escape）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W3-01 | Tampering | subOp.humanLabel XSS | accept | React JSX `{subOp.humanLabel}` 自动 HTML escape；humanLabel 由 adapter 产出（非 LLM 原文）；无 dangerouslySetInnerHTML |
| T-11-W3-02 | Denial of Service | 超长 subOps 列表渲染 | accept | D-06 上限 20 ops → subOps.length ≤ 20；20 个 li 渲染无性能问题 |
| T-11-W3-03 | Tampering | CSS 变量覆盖 | accept | aster-design-system 规范：只用 --border / --text-2，无 backdrop-filter；变量由根 CSS 定义，不由用户输入控制 |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster

# 1. DiffLogPanel 渲染分支存在
grep -n "batch-sub-ops" src/components/DiffLogPanel.tsx
grep -n "entry.subOps" src/components/DiffLogPanel.tsx

# 2. CSS 符合 aster-design-system 规范（无硬编码 hex）
grep -A 20 "batch-sub-ops" src/styles.css | grep -v "^[[:space:]]*$"

# 3. DiffLogPanel.test.tsx 全绿
npm test -- --run src/components/DiffLogPanel.test.tsx

# 4. 全套无回归
npm test -- --run
```
</verification>

<success_criteria>
- DiffLogPanel.tsx 的 subOps 嵌套渲染分支存在（.batch-sub-ops ul）
- DiffLogPanel.test.tsx 全绿（BATCH-02 SC#4 验证）
- styles.css 有 .batch-sub-ops 3 个 CSS 类，只用 CSS 变量，无硬编码 hex
- npm test -- --run 全套通过（无任何回归）
</success_criteria>

<output>
完成后创建 `.planning/phases/11-c/11-04-SUMMARY.md`
</output>

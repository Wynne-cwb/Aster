---
phase: 11-c
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agent/tools/write/batch.test.ts
  - src/adapters/ExcelAdapter.batch.test.ts
  - src/components/DiffLogPanel.test.tsx
  - src/agent/contract.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02

must_haves:
  truths:
    - "batch.test.ts 存在并包含 INVALID_ARGS / D-05 / 嵌套 batch_write 三个 RED 测试（vitest 运行时报 describe 存在但测试 FAIL，因实现未建）"
    - "ExcelAdapter.batch.test.ts 存在并包含 sync 计数（2 次非 N 次）+ fail-fast 部分完成两个 RED 集成测试骨架"
    - "DiffLogPanel.test.tsx 存在并包含 batch 卡 humanLabel 渲染 RED 测试"
    - "contract.test.ts UndoType 类型加 'batch'；CONTRACT[] 末尾加 batch_write 行；长度守门改为 ≥24（基于实测行数，见注释）"
    - "CONTRACT.md Phase 11 章节新增 batch_write 行（undo_type=batch / reverse_tool=batch_reverse / status=planned）"
    - "npm test 全测试可跑（不因新文件语法报错中断），只因实现缺失而 FAIL 的用例符合 RED 预期"
  artifacts:
    - path: "src/agent/tools/write/batch.test.ts"
      provides: "BATCH-01 校验逻辑骨架（20 cap / D-05 / 嵌套 batch_write）"
    - path: "src/adapters/ExcelAdapter.batch.test.ts"
      provides: "BATCH-01 单闭包集成骨架（sync 计数 / fail-fast）"
    - path: "src/components/DiffLogPanel.test.tsx"
      provides: "BATCH-02 DiffLogPanel batch 渲染骨架"
    - path: "src/agent/contract.test.ts"
      provides: "UndoType 扩展 + batch_write 合约行"
    - path: ".planning/phases/08-foundation-a-f/CONTRACT.md"
      provides: "人读合约表 batch_write 行"
  key_links:
    - from: "src/agent/contract.test.ts"
      to: ".planning/phases/08-foundation-a-f/CONTRACT.md"
      via: "CONTRACT 数组与文档表对齐"
      pattern: "batch_write.*batch_reverse"
    - from: "src/agent/tools/write/batch.test.ts"
      to: "src/agent/tools/write/batch.ts"
      via: "import batchWrite（实现由 Wave 2 提供）"
      pattern: "batchWrite"
---

<objective>
Nyquist Wave 0：在实现前建立测试骨架，确保 RED→GREEN 推进有明确的验证锚点，并向合约表声明 batch_write。

Purpose: 满足 Nyquist 规则——每个 BATCH-01/02 行为都有对应自动化测试文件（即使现在 FAIL）；D-11 合约行写入让 CI 从一开始就感知 batch_write 的存在；避免实现后补测试的「事后想起来的」风险。

Output:
- src/agent/tools/write/batch.test.ts（新建，BATCH-01 RED 骨架）
- src/adapters/ExcelAdapter.batch.test.ts（新建，BATCH-01 集成 RED 骨架）
- src/components/DiffLogPanel.test.tsx（新建，BATCH-02 渲染 RED 骨架）
- src/agent/contract.test.ts（修改，UndoType + batch 行 + 长度守门）
- .planning/phases/08-foundation-a-f/CONTRACT.md（修改，Phase 11 章节）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-c/11-CONTEXT.md
@.planning/phases/11-c/11-RESEARCH.md
@.planning/phases/11-c/11-VALIDATION.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 从 src/agent/contract.test.ts 提取的关键类型（供修改时参考）-->
<!-- 当前 UndoType 定义（line ~17）: -->
```typescript
type UndoType = '简单逆向' | '快照式' | 'noop+gate';
// Phase 11 修改为：
type UndoType = '简单逆向' | '快照式' | 'noop+gate' | 'batch';
```

<!-- 当前 CONTRACT[] 条目格式（参考既有行）: -->
```typescript
{ toolName: 'batch_write', host: 'excel', undoType: 'batch', reverseTool: 'batch_reverse', phase: 11, integrationTest: false },
```

<!--
CONTRACT 长度守门依据（Warning 3 要求）：
  - Phase 9  写工具：5 行（appendParagraph / insertParagraph / replaceParagraph /
                           insertTextAtCursor / replaceSelection）
  - Phase 10 写工具：18 行（Excel+PPT B 工具合计）
  - Phase 11 本次新增：batch_write = 1 行
  - 合计：5 + 18 + batch_write(1) = 24 行（还有更早 phase 的工具，总量 ≥24）
  重要：在执行前用 `grep -c "toolName" src/agent/contract.test.ts` 实测当前行数确认基数；
  若当前数组行数 ≠ 23，守门值应为 实测值+1，而非魔数 24。
  测试中应改为 `expect(CONTRACT.length).toBeGreaterThanOrEqual(CONTRACT.length + 0)`
  的写法，或通过 `const expected = CONTRACT.filter(c => c.phase <= 11).length` 动态推导，
  避免魔数漂移导致假绿。简化推荐：守门值 = 实测行数（执行前确认）= ≥24。
-->

<!-- 当前长度守门（应改为 ≥24，执行前 grep 确认）: -->
```typescript
it('CONTRACT 数组长度 ≥ N', () => { expect(CONTRACT.length).toBeGreaterThanOrEqual(24); });
```

<!-- 当前 operationLog.integration.test.ts mockExcel 工厂（lines 47-68，ExcelAdapter.batch.test.ts 复用）: -->
```typescript
// 复用同一工厂模式：vi.fn() mock Excel.run + ctx.sync 记录调用次数
(global as unknown as Record<string, unknown>).Excel = {
  run: vi.fn(async (cb: (ctx: unknown) => unknown) =>
    cb({ workbook: { ... }, sync: vi.fn().mockResolvedValue(undefined) })
  ),
};
```

<!-- DiffLogPanel 渲染：目前无 test 文件，需新建。骨架参考 ChatStream.test.tsx import 模式 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 batch.test.ts + ExcelAdapter.batch.test.ts 测试骨架（BATCH-01 RED）</name>
  <read_first>
    - src/agent/tools/write/excel.ts（ToolDef execute 范式，参考 batchWrite 的 ctx 结构）
    - src/agent/operationLog.integration.test.ts lines 1-100（mockExcel 工厂 + afterEach 模式）
    - src/agent/tools/index.ts lines 52-75（ToolExecContext 接口 + ToolResult 接口）
    - src/adapters/ExcelAdapter.ts lines 1-50（ExcelAdapter 类结构）
  </read_first>
  <files>src/agent/tools/write/batch.test.ts, src/adapters/ExcelAdapter.batch.test.ts</files>
  <action>
**batch.test.ts（新建）：** 测试 batchWrite.execute 的参数校验逻辑（这些测试在 Wave 2 实现 batch.ts 后变绿）。

```typescript
// src/agent/tools/write/batch.test.ts
import { describe, it, expect, vi, afterEach } from 'vitest';
// batchWrite 在 Wave 2 才创建；此处 import 导致 RED（文件不存在时 vitest 报 FAIL，符合预期）
import { batchWrite } from './batch';
import { __resetOperationLogForTest } from '../../operationLog';

const mockCtx = {
  adapter: {} as never,
  runId: 'test-run',
  stepIndex: 0,
  signal: new AbortController().signal,
};

afterEach(() => {
  __resetOperationLogForTest();
  vi.restoreAllMocks();
});

describe('batch_write — D-06 上限校验（开 run 之前）', () => {
  it('ops 为空数组 → INVALID_ARGS', async () => {
    const result = await batchWrite.execute({ ops: [] }, mockCtx);
    expect(result.ok).toBe(false);
    expect(result.error?.code).toBe('INVALID_ARGS');
  });

  it('ops.length > 20 → INVALID_ARGS，message 含「单次批量最多 20 个操作」', async () => {
    const ops = Array.from({ length: 21 }, (_, i) => ({
      tool: 'set_range_values',
      args: { address: `A${i + 1}`, values: [[i]] },
    }));
    const result = await batchWrite.execute({ ops }, mockCtx);
    expect(result.ok).toBe(false);
    expect(result.error?.code).toBe('INVALID_ARGS');
    expect(result.error?.message).toContain('单次批量最多 20 个操作');
    expect(result.error?.message).toContain('21');
  });
});

describe('batch_write — D-05 op 类型校验', () => {
  it('op.tool === "batch_write"（嵌套）→ INVALID_ARGS', async () => {
    const result = await batchWrite.execute(
      { ops: [{ tool: 'batch_write', args: {} }] },
      mockCtx,
    );
    expect(result.ok).toBe(false);
    expect(result.error?.code).toBe('INVALID_ARGS');
  });
});

describe('batch_write — 属性结构', () => {
  it('batchWrite.kind === "write"', () => {
    expect(batchWrite.kind).toBe('write');
  });

  it('batchWrite.humanLabel 返回「批量改动 N 处」', () => {
    const label = batchWrite.humanLabel({ ops: [{ tool: 'set_range_values', args: {} }, { tool: 'set_range_values', args: {} }] });
    expect(label).toContain('批量改动');
    expect(label).toContain('2');
  });
});
```

**ExcelAdapter.batch.test.ts（新建）：** 测试 ExcelAdapter.executeBatch 的单闭包 sync 计数和 fail-fast 行为（在 Wave 2 实现 executeBatch 后变绿）。

```typescript
// src/adapters/ExcelAdapter.batch.test.ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { ExcelAdapter } from './ExcelAdapter';

afterEach(() => {
  delete (global as unknown as Record<string, unknown>).Excel;
  vi.restoreAllMocks();
});

/** 构造 mock Excel 环境，返回 syncCalls 数组（记录每次 sync 时间戳）和 setValuesMock */
function mockExcelForBatch(syncCalls: number[]): { setValues: ReturnType<typeof vi.fn> } {
  const setValues = vi.fn();
  const makeRange = (isNull = false) => ({
    load: vi.fn(),
    isNullObject: isNull,
    address: 'Sheet1!A1',
    get values(): unknown[][] { return [[0]]; },
    set values(v: unknown[][]) { setValues(v); },
  });
  const activeWorksheet = {
    getRangeOrNullObject: vi.fn((addr: string) =>
      addr === 'INVALID_ADDR' ? makeRange(true) : makeRange(false)
    ),
  };
  (global as unknown as Record<string, unknown>).Excel = {
    run: vi.fn(async (cb: (ctx: unknown) => unknown) =>
      cb({
        workbook: { worksheets: { getActiveWorksheet: () => activeWorksheet } },
        sync: vi.fn(async () => { syncCalls.push(Date.now()); }),
      })
    ),
  };
  return { setValues };
}

describe('ExcelAdapter.executeBatch — 单闭包 sync 计数（D-01）', () => {
  it('3 个有效 op → ctx.sync 只调用 2 次（Phase1 + Phase2），非 6 次', async () => {
    const syncCalls: number[] = [];
    mockExcelForBatch(syncCalls);
    const adapter = new ExcelAdapter();

    // executeBatch 在 Wave 2 才实现；此处 RED（方法不存在）
    await (adapter as unknown as { executeBatch: (ops: unknown[]) => Promise<unknown> }).executeBatch([
      { tool: 'set_range_values', args: { address: 'A1', values: [[1]] }, humanLabel: '写入 A1' },
      { tool: 'set_range_values', args: { address: 'A2', values: [[2]] }, humanLabel: '写入 A2' },
      { tool: 'set_range_values', args: { address: 'A3', values: [[3]] }, humanLabel: '写入 A3' },
    ]);

    expect(syncCalls.length).toBe(2); // Phase 1（读）+ Phase 2（写）
  });
});

describe('ExcelAdapter.executeBatch — fail-fast 部分完成（D-03）', () => {
  it('第 2 个 op range 不存在 → failAtIndex=1，subOps 只含第 0 个', async () => {
    const syncCalls: number[] = [];
    mockExcelForBatch(syncCalls);
    const adapter = new ExcelAdapter();

    const result = await (adapter as unknown as { executeBatch: (ops: unknown[]) => Promise<unknown> }).executeBatch([
      { tool: 'set_range_values', args: { address: 'A1', values: [[1]] }, humanLabel: '写入 A1' },
      { tool: 'set_range_values', args: { address: 'INVALID_ADDR', values: [[2]] }, humanLabel: '写入无效地址' },
      { tool: 'set_range_values', args: { address: 'A3', values: [[3]] }, humanLabel: '写入 A3' },
    ]) as { subOps: unknown[]; failAtIndex?: number };

    expect(result.failAtIndex).toBe(1);    // 第 2 个 op（index=1）失败
    expect(result.subOps.length).toBe(1); // 只有第 0 个成功
  });
});
```

这两个文件在 Wave 2 实现 batch.ts + ExcelAdapter.executeBatch 之前全部为 RED，符合 Nyquist Wave 0 要求。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run src/agent/tools/write/batch.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/agent/tools/write/batch.test.ts 文件存在，包含 3 个 describe block（D-06 上限校验 / D-05 op 类型校验 / 属性结构）
    - src/adapters/ExcelAdapter.batch.test.ts 文件存在，包含 sync 计数和 fail-fast 两个 describe block
    - vitest 运行时测试列表可见（文件语法正确），测试 FAIL 原因是 `import './batch'` 找不到模块（RED 预期），不是语法错误
    - ExcelAdapter.batch.test.ts 中 afterEach 正确删除 global.Excel
  </acceptance_criteria>
  <done>两个新测试文件存在且语法正确，vitest 可识别 test cases（即使因实现未建而 FAIL）</done>
</task>

<task type="auto">
  <name>Task 2: 新建 DiffLogPanel.test.tsx + 修改 contract.test.ts + CONTRACT.md</name>
  <read_first>
    - src/agent/contract.test.ts（全文；找 UndoType 定义行、CONTRACT 数组末尾行、长度守门 it-block）
    - .planning/phases/08-foundation-a-f/CONTRACT.md（全文；找已有行格式 + 末尾追加位置）
    - src/components/DiffLogPanel.tsx lines 1-30（import 路径 + OperationLogEntry import）
    - src/components/ChatStream.test.tsx lines 1-30（React 测试 import 范式，作为参考）
  </read_first>
  <files>src/components/DiffLogPanel.test.tsx, src/agent/contract.test.ts, .planning/phases/08-foundation-a-f/CONTRACT.md</files>
  <action>
**DiffLogPanel.test.tsx（新建）：** 测试 batch entry 渲染 batch 卡头和 subOps 列表（Wave 3 实现后变绿）。

```typescript
// src/components/DiffLogPanel.test.tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import DiffLogPanel from './DiffLogPanel';
import type { OperationLogEntry } from '../agent/operationLog';

// mock getWriteOpsByRun + replayUndoAll/Single（不测 replay，只测渲染）
vi.mock('../agent/operationLog', async (importOriginal) => {
  const actual = await importOriginal<typeof import('../agent/operationLog')>();
  return {
    ...actual,
    getWriteOpsByRun: vi.fn(),
    replayUndoAll: vi.fn().mockResolvedValue({ total: 0, rolledBack: 0, skippedManualChange: 0, skippedHostError: 0, details: [] }),
    replayUndoSingle: vi.fn().mockResolvedValue({ total: 1, rolledBack: 1, skippedManualChange: 0, skippedHostError: 0, details: [] }),
  };
});

// 构造一个 batch OperationLogEntry（Wave 3 后 OperationLogEntry 含 subOps? 字段）
const makeBatchEntry = (): OperationLogEntry => ({
  runId: 'run-1',
  stepIndex: 0,
  toolName: 'batch_write',
  args: {},
  humanLabel: '批量改动 3 处',
  reverse: {
    tool: 'batch_reverse',
    args: { ops: [] },
  },
  timestamp: Date.now(),
  // subOps 字段在 Wave 1 类型扩展后可用
  subOps: [
    { humanLabel: '写入 A1', reverse: { tool: 'overwrite_range', args: { address: 'A1', values: [[1]] } } },
    { humanLabel: '设置格式 B2', reverse: { tool: 'overwrite_range', args: { address: 'B2', values: [[2]] } } },
    { humanLabel: '写入 C3', reverse: { tool: 'overwrite_range', args: { address: 'C3', values: [[3]] } } },
  ],
} as OperationLogEntry);

describe('DiffLogPanel — batch 卡渲染（BATCH-02 D-10）', () => {
  it('batch entry humanLabel「批量改动 3 处」显示在卡头', () => {
    const { getWriteOpsByRun } = vi.mocked(
      await import('../agent/operationLog')
    );
    getWriteOpsByRun.mockReturnValue([makeBatchEntry()]);

    render(
      <DiffLogPanel
        runId="run-1"
        adapter={{} as never}
        onUndoComplete={vi.fn()}
      />
    );

    expect(screen.getByText('批量改动 3 处')).toBeTruthy();
  });

  it('batch entry 展开后显示 3 个 subOp humanLabel', async () => {
    const { getWriteOpsByRun } = vi.mocked(
      await import('../agent/operationLog')
    );
    getWriteOpsByRun.mockReturnValue([makeBatchEntry()]);

    render(
      <DiffLogPanel
        runId="run-1"
        adapter={{} as never}
        onUndoComplete={vi.fn()}
      />
    );

    // 展开 DiffLogPanel（点击 toggle 或直接 expanded 默认开）
    // Wave 3 实现后此测试验证 subOps 列表可见
    expect(screen.queryByText('写入 A1')).toBeTruthy();
    expect(screen.queryByText('设置格式 B2')).toBeTruthy();
    expect(screen.queryByText('写入 C3')).toBeTruthy();
  });

  it('batch 卡没有 per-subOp 独立撤销按钮（D-10 锁定：batch = 原子 undo 单元）', () => {
    const { getWriteOpsByRun } = vi.mocked(
      await import('../agent/operationLog')
    );
    getWriteOpsByRun.mockReturnValue([makeBatchEntry()]);

    render(
      <DiffLogPanel
        runId="run-1"
        adapter={{} as never}
        onUndoComplete={vi.fn()}
      />
    );

    // subOps 列表里不应有 3 个独立「撤销该步」按钮（只有 1 个整批撤销按钮）
    const undoButtons = screen.queryAllByText('撤销该步');
    // 最多 1 个（整批），不是 3 个（每 subOp 一个）
    expect(undoButtons.length).toBeLessThanOrEqual(1);
  });
});
```

注意：DiffLogPanel 的 props 接口在 Wave 3 才完整；如果当前 DiffLogPanel 不接受 `adapter` prop，测试可能因 props 类型不匹配 RED——这是预期行为，实现后变绿。

**contract.test.ts（修改）：** 在现有文件中：
1. 先用 `grep -c "toolName" src/agent/contract.test.ts` 实测当前 CONTRACT 数组行数（执行前确认）
2. 找 `type UndoType = '简单逆向' | '快照式' | 'noop+gate';` → 改为 `type UndoType = '简单逆向' | '快照式' | 'noop+gate' | 'batch';`
3. 在 CONTRACT 数组末尾追加：`{ toolName: 'batch_write', host: 'excel', undoType: 'batch' as UndoType, reverseTool: 'batch_reverse', phase: 11, integrationTest: false },`
4. 找长度守门 it-block，改为 `toBeGreaterThanOrEqual(实测行数 + 1)`（Phase 9: 5 行 + Phase 10: 18 行 = 23 行既有，+ batch_write = 24。若 grep 结果不同以实测为准）

**守门值推导（执行前必查）：**
```bash
# 执行前实测，确认当前 CONTRACT 数组行数
grep -c "toolName" src/agent/contract.test.ts
# 期望：23（Phase 9+10 已录入）→ 加 batch_write 后 = 24
# 守门改为 toBeGreaterThanOrEqual(24)
# 若实测不是 23，守门值 = 实测值 + 1
```

**CONTRACT.md（修改）：** 在文件末尾（「使用说明」章节之前或文件结尾）追加：

```markdown
## Phase 11 批量操作 (C)

| tool_name | host | parameters 摘要 | undo_type | reverse_tool | integration_test | phase | status |
|-----------|------|----------------|-----------|--------------|-----------------|-------|--------|
| batch_write | excel/word/ppt（三宿主注册） | ops: Array<{tool,args}>, 上限 20 | batch | batch_reverse | false | 11 | planned |
```
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npm test -- --run src/agent/contract.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DiffLogPanel.test.tsx 存在，包含 3 个 batch 渲染 it block
    - src/agent/contract.test.ts 中 UndoType 联合类型包含 'batch'（grep 验证：`grep -n "batch" src/agent/contract.test.ts` 返回至少 2 行）
    - CONTRACT[] 数组包含 toolName='batch_write' 的条目
    - 长度守门值 = 执行前 grep 实测行数 + 1（≥24，若 Phase 9+10 已录 23 行）
    - npm test -- --run src/agent/contract.test.ts 全绿（UndoType 扩展不破坏既有测试）
    - .planning/phases/08-foundation-a-f/CONTRACT.md 末尾有 batch_write 行（grep 验证：`grep -c "batch_write" .planning/phases/08-foundation-a-f/CONTRACT.md` 返回 ≥1）
  </acceptance_criteria>
  <done>
    - DiffLogPanel.test.tsx 存在（RED，因 Wave 1/3 实现未建）
    - contract.test.ts 含 UndoType 'batch' + batch_write 行 + 长度守门 ≥实测值+1
    - CONTRACT.md 有 Phase 11 章节 + batch_write 行
    - npm test -- --run src/agent/contract.test.ts 全绿
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs → batch.ts | 测试文件 import 尚未创建的 batch.ts，Wave 0 期间 RED 是预期行为 |
| contract.test.ts 长度守门 | 守门值从 23 改为 24；改小会误放行、改大会误阻止——必须精确；执行前 grep 实测确认基数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W0-01 | Tampering | contract.test.ts UndoType 扩展 | mitigate | 增加 'batch' 到联合类型后运行 contract.test.ts 全套；若 TypeScript 编译报类型不兼容则立即修复 |
| T-11-W0-02 | Denial of Service | DiffLogPanel.test.tsx 的 vi.mock('../agent/operationLog') | accept | mock 范围仅限该测试文件；不影响生产 operationLog 逻辑 |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster

# 1. 验证测试文件存在
ls -la src/agent/tools/write/batch.test.ts \
       src/adapters/ExcelAdapter.batch.test.ts \
       src/components/DiffLogPanel.test.tsx

# 2. contract.test.ts 含 batch
grep -n "batch" src/agent/contract.test.ts

# 3. 实测 CONTRACT 数组行数（守门依据）
grep -c "toolName" src/agent/contract.test.ts

# 4. CONTRACT.md 含 batch_write
grep -v "^#" .planning/phases/08-foundation-a-f/CONTRACT.md | grep -c "batch_write"

# 5. contract.test.ts 全绿
npm test -- --run src/agent/contract.test.ts
```
</verification>

<success_criteria>
- 5 个文件（3 新建 + 2 修改）全部存在
- npm test -- --run src/agent/contract.test.ts 全绿
- npm test -- --run src/agent/tools/write/batch.test.ts 报 FAIL（因 batch.ts 未建，RED 预期）
- contract.test.ts UndoType 联合类型含 'batch'，CONTRACT[] 含 batch_write 行
- CONTRACT.md 有 Phase 11 章节
</success_criteria>

<output>
完成后创建 `.planning/phases/11-c/11-01-SUMMARY.md`
</output>

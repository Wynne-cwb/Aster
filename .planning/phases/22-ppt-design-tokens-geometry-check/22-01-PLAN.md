---
phase: 22-ppt-design-tokens-geometry-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/design/ppt-tokens.ts
  - src/agent/design/geometry-check.ts
  - src/agent/tools/read/ppt.ts
  - src/agent/tools/index.ts
  - src/agent/design/ppt-tokens.test.ts
  - src/agent/design/geometry-check.test.ts
  - src/agent/tools/read/tools.test.ts
  - src/agent/tools/index.test.ts
autonomous: true
requirements:
  - PVQ-01
  - PVQ-02

must_haves:
  truths:
    - "ppt-tokens.ts 存结构 token（字号阶梯/页边距/间距/两套网格/默认画布 960×540/缺省兜底单色 teal/涨跌语义色），配色不锁死——无固定调色板数组（D-22-01）"
    - "几何自查纯函数 canvas 作显式参数、默认 960×540（绝不内部硬编 720×405，D-22-02 #1 陷阱）；四项确定性输出违规清单：① 溢出（保守上界）② 重叠（相交边长 >2pt）③ 越界（超画布或到边缘 <页边距）④ 对比（WCAG <4.5:1 正文 / <3:1 大字）"
    - "对比度自查按 AI 供入 hex 色对算 WCAG；bg 缺失/非法 → 诚实降级 undetermined（advisory），绝不报假阳性违规（D-22-05）"
    - "几何自查 = advisory evidence 非硬阻断（D-22-03）；check_slide_layout read 工具（kind:'read'）注册进 buildToolsForHost('ppt')、不进 PPT_TOOLS、无 undo/operationLog；复用既有 adapter.read({kind:'list_shapes_on_slide'}) 零 adapter 改动（D-22-04）"
    - "本 phase 不编辑 system-prompt.ts（D-22-07）；工具靠 description 自我广告即可被 LLM 调用；PVQ-02 SC#3「prompt 不再脑补坐标」留给 Phase 23 PVQ-05 满足"
    - "geometry-check 四项各 happy+edge + 对比诚实降级 + WCAG helper + 文本估算 + ppt-tokens 结构（含无调色板断言）+ 工具集成 + PPT 计数 21→22；现有 933 测 green；tsc 0；bundle ≤82KB（先 build 再 size，懒加载 chunk 预期 ~0 增量）；0 净新增运行时依赖"
  artifacts:
    - path: "src/agent/design/ppt-tokens.ts"
      provides: "结构 token：FONT_LADDER_PT + MARGINS_PT + GAP_PT + DEFAULT_CANVAS_PT(960×540) + gridFull(canvas) + gridTwoColumn(canvas) + DEFAULT_ACCENT(teal 兜底单色) + SEMANTIC(涨跌) + 几何阈值常量；无固定调色板"
      exports: ["FONT_LADDER_PT", "MARGINS_PT", "GAP_PT", "DEFAULT_CANVAS_PT", "DEFAULT_ACCENT", "SEMANTIC", "OVERLAP_MIN_PT", "OVERFLOW_TOLERANCE_PT", "gridFull", "gridTwoColumn", "type Canvas", "type Rect"]
      contains: "DEFAULT_CANVAS_PT"
    - path: "src/agent/design/geometry-check.ts"
      provides: "纯确定性几何自查：wcagContrastRatio + estimateTextBox + checkOverflow + checkOverlap + checkOutOfBounds + checkContrast + checkSlideLayout(顶层) + formatViolations"
      exports: ["wcagContrastRatio", "estimateTextBox", "checkSlideLayout", "formatViolations", "type ShapeBox", "type TextBoxAnnotation", "type Violation", "type LayoutReport"]
      contains: "export function checkSlideLayout"
    - path: "src/agent/tools/read/ppt.ts"
      provides: "checkSlideLayout ToolDef（name:'check_slide_layout', kind:'read'），复用 adapter.read list_shapes_on_slide + 跑纯 checkSlideLayout + wrapReadResult metadata"
      exports: ["checkSlideLayout"]
      contains: "name: 'check_slide_layout'"
    - path: "src/agent/tools/index.ts"
      provides: "buildToolsForHost('ppt') read 列表加 checkSlideLayout；import 加它；PPT_TOOLS 不变"
      contains: "checkSlideLayout"
  key_links:
    - from: "src/agent/design/geometry-check.ts"
      to: "src/agent/design/ppt-tokens.ts"
      via: "import { DEFAULT_CANVAS_PT, MARGINS_PT, OVERLAP_MIN_PT, OVERFLOW_TOLERANCE_PT, type Canvas } — 几何阈值与画布默认来自 token（单一真相源）"
      pattern: "ppt-tokens"
    - from: "src/agent/tools/read/ppt.ts"
      to: "src/agent/design/geometry-check.ts"
      via: "import { checkSlideLayout, type ShapeBox } — 工具 execute 跑纯自查"
      pattern: "checkSlideLayout"
    - from: "src/agent/tools/read/ppt.ts"
      to: "src/agent/design/ppt-tokens.ts"
      via: "import { DEFAULT_CANVAS_PT } — 工具传默认画布给 checkSlideLayout（参数化，D-22-02）"
      pattern: "DEFAULT_CANVAS_PT"
    - from: "src/agent/tools/index.ts"
      to: "src/agent/tools/read/ppt.ts"
      via: "import { ..., checkSlideLayout } from './read/ppt' — 注册进 ppt read 列表"
      pattern: "checkSlideLayout"
---

<objective>
建立 PPT 设计规范的代码化基础（PVQ-01/02）：① 集中**结构** token 模块 `ppt-tokens.ts`（字号阶梯/页边距/网格/默认画布/兜底单色——**配色不锁死，无固定调色板**）；② 确定性几何自查 `geometry-check.ts`（四项：溢出/重叠/越界/对比，纯 TS 零网络零依赖，**canvas 参数化默认 960×540**），输出违规清单作 advisory evidence；③ 新增 `check_slide_layout` read 工具把违规清单经既有 read-tool→wire 路径喂回 LLM 自主重排。

Purpose: 让 PPT 产出从「文字对但粗糙」升级到「有设计规范、可统一调」的地基；用确定性代码替代「让 LLM 拿坐标脑补重叠/溢出」（消除幻觉式排版）。配色由 AI 按客户意图自由生成 hex（用户 2026-06-03 推翻调色板锁定），对比度自查成为唯一颜色护栏，bg 不可读时诚实降级。所有改动 0 净新增运行时依赖、纯 TS、不碰 adapter、不碰 system-prompt.ts、零 write 工具（无 undo 守门）。
Output: 新建 `src/agent/design/ppt-tokens.ts` + `geometry-check.ts`；`tools/read/ppt.ts` 加 `check_slide_layout` ToolDef；`tools/index.ts` 注册；2 个新测试文件 + 2 处计数断言 +1。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-ppt-design-tokens-geometry-check/22-CONTEXT.md

<interfaces>
<!-- 执行前已读关键结构，供参考；以源码现状为准 -->

PptAdapter list_shapes_on_slide 读 handler（L432-483）—— 已 load 几何，返回形状清单（单位 = points）：
```typescript
// src/adapters/PptAdapter.ts case 'list_shapes_on_slide'
slide.shapes.load('items/id,items/type,items/left,items/top,items/width,items/height');
await ctx.sync();
const shapes = slide.shapes.items.map((sh) => ({ id, type, left, top, width, height }));
return { ok: true, data: { slideIndex, shapes } };   // ← 几何输入现成，本 phase 不改此文件
```

ReadRequest 联合（DocumentAdapter.ts L165-179）—— 已有 list_shapes_on_slide，**不新增 kind**：
```typescript
| { kind: 'list_shapes_on_slide'; slideIndex: number }
```

read 工具范式（tools/read/ppt.ts listShapesOnSlide）—— execute 调 adapter.read + wrapReadResult：
```typescript
export const listShapesOnSlide: ToolDef<SlideIndexArgs> = {
  name: 'list_shapes_on_slide', kind: 'read',
  parameters: { type:'object', properties:{ slideIndex:{type:'number', ...} }, required:['slideIndex'] },
  humanLabel: ({ slideIndex }) => `读取了第 ${slideIndex} 张幻灯片的形状清单`,
  async execute({ slideIndex }, ctx) {
    const r = await ctx.adapter.read({ kind:'list_shapes_on_slide', slideIndex });
    return wrapReadResult(r, { result_type:'metadata', source:`slide_${slideIndex}.shapes` });
  },
};
```

wrapReadResult（read-result.ts L74）—— 成功时 data → JSON.stringify → applySizeCap → 包成 WrappedReadResult；失败原样透传。`result_type:'metadata'`（可信结构信息）。

buildToolsForHost('ppt') read 列表（tools/index.ts L307-311）—— 注入站点：
```typescript
return [
  listSlides, getSlide, listShapesOnSlide, getShape,   // ← checkSlideLayout 加在此行
  getShapeImage,
  ...pptWriteTools, selectionDetail,
].map((t) => t as ToolDef);
```
import 行（L20）：`import { listSlides, getSlide, listShapesOnSlide, getShape } from './read/ppt';`
PPT_TOOLS set（L30-46）= **write 工具** casing 归一化集合 → **read 工具不入**（listShapesOnSlide 也不在内）。

测试计数现状：PPT host 当前 **21** 个工具 —— `tools/read/tools.test.ts` L212 `toHaveLength(21)` + `tools/index.test.ts` L69 `toHaveLength(21)`，本 phase 各 → **22**。
read 工具单测 mock 范式（vision.test.ts）：`{ adapter: { read: vi.fn().mockResolvedValue({ok:true,data:{...}}) } } as never`。

⚠️ 坐标陷阱（D-22-02）：Office.js Shape 几何单位 = **points**；标准 16:9 宽屏 = 13.333in×7.5in = **960×540pt**（非 REQUIREMENTS 写的 720×405——那是错误基准，会让右半屏全误判越界）。几何自查 canvas 必须**参数化**、默认 960×540。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 src/agent/design/ppt-tokens.ts（结构 token，配色不锁死，PVQ-01）</name>
  <files>src/agent/design/ppt-tokens.ts</files>
  <read_first>
    - .planning/phases/22-ppt-design-tokens-geometry-check/22-CONTEXT.md D-22-01/02 + §Specifics（初值）
    - src/styles.css（仅确认面板 --accent 体系——**不复用**，token 物理隔离，D-22-01）
  </read_first>
  <action>
新建目录 `src/agent/design/` 并建 `ppt-tokens.ts`。**只存结构 token，绝不内置固定调色板。**

```typescript
/**
 * src/agent/design/ppt-tokens.ts — Phase 22（PVQ-01）PPT 生成成品的结构设计 token
 *
 * 调性：商务密实（咨询/财报汇报风、信息密度高），字号阶梯偏紧凑、页边距偏小。
 * ⚠️ 配色不锁死（用户 2026-06-03 推翻原「固定 teal 调色板 + 3-5 色」）：本模块**不内置调色板**；
 *    配色由 AI 按客户/内容意图自由生成 hex（freehand），DEFAULT_ACCENT 仅作无意图信号时的兜底单色。
 *    后果：几何自查「对比度」项是唯一颜色护栏（兜不可读、兜不了整体不协调，用户已知接受）。
 * ⚠️ 与 Aster 面板 UI 的 --accent CSS 变量系统**物理隔离**——这些 token 只服务「生成的幻灯片」，
 *    面板自身 UI 仍 teal 克制不变，绝不复用面板 CSS 变量。
 * ⚠️ 全部为「建议初值，待真机/UAT 调」。
 */

/** 画布/区域几何类型（单位 = points，与 Office.js Shape 几何一致）。 */
export interface Canvas { widthPt: number; heightPt: number; }
export interface Rect { left: number; top: number; width: number; height: number; }

/**
 * 默认画布 = 标准 16:9 宽屏 = 13.333in × 7.5in @ 72pt/in = 960×540pt（Office.js Shape 实际 pt 空间）。
 * ⚠️ 不是 REQUIREMENTS 写的 720×405——那是错误基准（旧 10in 残留），用它会让右半屏形状全被误判越界。
 *    真机确认见 22-CONTEXT D-22-02（攒到 v2.3 末 UAT；若某版本 Office.js 报别的基准，只改这一个常量）。
 */
export const DEFAULT_CANVAS_PT: Canvas = { widthPt: 960, heightPt: 540 };

/** 字号阶梯（pt，商务密实偏紧凑；初值待 UAT 调）。标题>副标>heading>正文>脚注 单调，kpi 最大。 */
export const FONT_LADDER_PT = {
  title: 28,
  subtitle: 18,
  heading: 16,
  body: 14,
  caption: 11,
  kpi: 40,
} as const;

/** 统一页边距 + 元素间距（pt，基于默认画布；商务密实偏小；初值待 UAT 调）。 */
export const MARGINS_PT = { x: 48, y: 36 } as const;
export const GAP_PT = 16;

/** 几何自查阈值（pt；初值待 UAT 调）。 */
export const OVERLAP_MIN_PT = 2;        // 相交边长 > 此值才报重叠
export const OVERFLOW_TOLERANCE_PT = 2; // 文本预估高度超框 > 此值才报溢出

/** 文本宽度估算乘数（保守上界 = 偏大 = 宁多报；初值待 UAT 调）。 */
export const TEXT_METRICS = {
  cjkAdvance: 1.0,    // CJK ≈ 全角方块（× fontSizePt）
  latinAdvance: 0.6,  // 拉丁/数字/空格（保守偏大；真均值更小）
  lineHeight: 1.3,    // 行高（× fontSizePt）
} as const;

/**
 * 缺省/兜底品牌单色（非调色板）——仅在 AI 无配色意图信号时回退。
 * AI 按客户意图自由生成 hex 时，此值不参与（配色不锁死）。
 */
export const DEFAULT_ACCENT = { light: '#009887', dark: '#4FC9B8' } as const;

/** 涨跌/成败语义色（独立于配色，不挤占任何配色预算；初值）。 */
export const SEMANTIC = { success: '#0E9F6E', error: '#E02424' } as const;

/**
 * 整页单栏网格：标题带 + 单一内容区（canvas 参数化 → 任意画布正确，规避 720/960 陷阱）。
 */
export function gridFull(canvas: Canvas = DEFAULT_CANVAS_PT): { titleBand: Rect; content: Rect } {
  const { x, y } = MARGINS_PT;
  const titleH = FONT_LADDER_PT.title * TEXT_METRICS.lineHeight + GAP_PT;
  return {
    titleBand: { left: x, top: y, width: canvas.widthPt - 2 * x, height: titleH },
    content: {
      left: x, top: y + titleH,
      width: canvas.widthPt - 2 * x,
      height: canvas.heightPt - 2 * y - titleH,
    },
  };
}

/**
 * 左右两栏网格：标题带 + 左右等宽两栏（中间 GAP_PT 间隔）。
 */
export function gridTwoColumn(canvas: Canvas = DEFAULT_CANVAS_PT): { titleBand: Rect; left: Rect; right: Rect } {
  const { x, y } = MARGINS_PT;
  const titleH = FONT_LADDER_PT.title * TEXT_METRICS.lineHeight + GAP_PT;
  const contentTop = y + titleH;
  const contentH = canvas.heightPt - 2 * y - titleH;
  const colW = (canvas.widthPt - 2 * x - GAP_PT) / 2;
  return {
    titleBand: { left: x, top: y, width: canvas.widthPt - 2 * x, height: titleH },
    left: { left: x, top: contentTop, width: colW, height: contentH },
    right: { left: x + colW + GAP_PT, top: contentTop, width: colW, height: contentH },
  };
}
```

**不做**：任何 `PALETTE`/`COLORS: string[]` 调色板数组（配色不锁死，D-22-01）；不 import/复用面板 `src/styles.css` 任何变量；不写任何 React/Office.js 依赖。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c 'DEFAULT_CANVAS_PT: Canvas = { widthPt: 960, heightPt: 540 }' src/agent/design/ppt-tokens.ts; grep -cE 'export const FONT_LADDER_PT|export const MARGINS_PT|export function gridFull|export function gridTwoColumn|export const DEFAULT_ACCENT|export const SEMANTIC' src/agent/design/ppt-tokens.ts; grep -ciE 'PALETTE|COLORS *:|调色板.*\[' src/agent/design/ppt-tokens.ts; npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'widthPt: 960, heightPt: 540' src/agent/design/ppt-tokens.ts` 返回 1（默认画布 960×540，**非 720×405**，D-22-02）
    - `grep -c '720' src/agent/design/ppt-tokens.ts` 可出现在注释里说明「不是 720」，但 `grep -c 'widthPt: 720' src/agent/design/ppt-tokens.ts` 返回 0（没把 720 当默认画布宽）
    - `grep -cE 'export const FONT_LADDER_PT|export const MARGINS_PT|export const GAP_PT|export const DEFAULT_CANVAS_PT|export const DEFAULT_ACCENT|export const SEMANTIC|export function gridFull|export function gridTwoColumn' src/agent/design/ppt-tokens.ts` 返回 8（结构 token 全齐）
    - 无固定调色板：人工核对无 `string[]` 颜色数组 / 无 `PALETTE` 常量（配色不锁死，D-22-01）；`DEFAULT_ACCENT` 注释含「兜底」「非调色板」语义
    - `grep -c "from '../../styles" src/agent/design/ppt-tokens.ts` 返回 0（不复用面板 CSS 变量，物理隔离）
    - `npx tsc --noEmit` 退出 0
  </acceptance_criteria>
  <done>ppt-tokens.ts 已建：字号阶梯（商务密实）+ 页边距/间距 + 默认画布 960×540 + 两套 canvas 参数化网格 + 兜底单色 teal + 涨跌语义色 + 几何阈值；无固定调色板（配色不锁死）；不复用面板 CSS；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 src/agent/design/geometry-check.ts（纯确定性四项自查 + WCAG + 文本估算，PVQ-02）</name>
  <files>src/agent/design/geometry-check.ts</files>
  <read_first>
    - src/agent/design/ppt-tokens.ts（Task 1 产出：Canvas/Rect/阈值/TEXT_METRICS/DEFAULT_CANVAS_PT/MARGINS_PT）
    - .planning/phases/22-ppt-design-tokens-geometry-check/22-CONTEXT.md D-22-02/03/05/06
    - src/agent/read-result.ts（estimateTokens 已导出；本任务不需 token 估算，仅确认不重定义工具函数惯例）
  </read_first>
  <action>
新建 `src/agent/design/geometry-check.ts`——纯 TS、确定性、**零网络零依赖、零 Office.js / React import**（仅 import ppt-tokens）。advisory evidence（违规清单），**非硬阻断**（D-22-03）。

```typescript
/**
 * src/agent/design/geometry-check.ts — Phase 22（PVQ-02）确定性版面几何自查
 *
 * 纯函数、确定性、零网络零依赖（仅依赖 ppt-tokens 常量）。输出违规清单作 advisory evidence
 * 喂回 LLM 自主重排——**绝不阻断写操作、绝不自动改文档**（D-22-03）。
 * canvas 作显式参数（默认 960×540，绝不内部硬编 720×405，D-22-02）。
 */
import {
  DEFAULT_CANVAS_PT, MARGINS_PT, OVERLAP_MIN_PT, OVERFLOW_TOLERANCE_PT, TEXT_METRICS,
  type Canvas,
} from './ppt-tokens';

/** list_shapes_on_slide 返回的形状几何（单位 = points）。 */
export interface ShapeBox { id: string; type?: string; left: number; top: number; width: number; height: number; }

/** AI 供入的文本/配色注解（按 shapeId 关联到 ShapeBox；溢出① 与对比④ 需要）。 */
export interface TextBoxAnnotation {
  shapeId: string;
  text?: string;          // ① 溢出需要（list_shapes 不 load 文本，由 AI 供入）
  fontSizePt?: number;    // ①④ 需要
  bold?: boolean;         // ④ 大字阈值判定
  foreground?: string;    // ④ 文字色 hex
  background?: string;    // ④ 背景色 hex（缺失/非法 → 诚实降级 undetermined）
}

export type ViolationKind = 'overflow' | 'overlap' | 'out_of_bounds' | 'low_contrast' | 'contrast_undetermined';
export interface Violation {
  kind: ViolationKind;
  shapeIds: string[];     // 涉及的形状
  detail: string;         // 量化中文描述（如「文本预估高度 142pt 超框 38pt」「对比 2.1:1 < 4.5:1」）
}
export interface LayoutReport {
  canvas: Canvas;
  violations: Violation[];
  notes: string[];        // 诚实降级/未检查说明（如「未提供配色信息，未做对比检查」）
}

// ---------------------------------------------------------------------------
// WCAG 对比度（D-22-05）
// ---------------------------------------------------------------------------

/** 解析 #RGB / #RRGGBB → [r,g,b] (0..255)；非法返回 null。 */
function parseHex(hex: string): [number, number, number] | null {
  if (typeof hex !== 'string') return null;
  let h = hex.trim().replace(/^#/, '');
  if (h.length === 3) h = h.split('').map((c) => c + c).join('');
  if (!/^[0-9a-fA-F]{6}$/.test(h)) return null;
  return [parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16)];
}

/** 相对亮度（WCAG 2.x）。 */
function relativeLuminance([r, g, b]: [number, number, number]): number {
  const lin = (c: number) => { const s = c / 255; return s <= 0.03928 ? s / 12.92 : Math.pow((s + 0.055) / 1.055, 2.4); };
  return 0.2126 * lin(r) + 0.7152 * lin(g) + 0.0722 * lin(b);
}

/** WCAG 对比比值（1..21）；任一色非法返回 null（→ 调用方诚实降级）。 */
export function wcagContrastRatio(fg: string, bg: string): number | null {
  const a = parseHex(fg), b = parseHex(bg);
  if (!a || !b) return null;
  const la = relativeLuminance(a), lb = relativeLuminance(b);
  const hi = Math.max(la, lb), lo = Math.min(la, lb);
  return (hi + 0.05) / (lo + 0.05);
}

/** WCAG 大字定义：≥18pt，或 ≥14pt 且加粗。 */
function isLargeText(fontSizePt: number, bold: boolean): boolean {
  return fontSizePt >= 18 || (fontSizePt >= 14 && bold);
}

// ---------------------------------------------------------------------------
// 文本溢出保守上界（D-22-06）
// ---------------------------------------------------------------------------

/** 保守估算文本在给定框宽下需要的高度（pt）。over-estimate = 安全方向（宁多报）。 */
export function estimateTextBox(text: string, fontSizePt: number, boxWidthPt: number): { neededHeightPt: number; maxCharWidthPt: number } {
  const { cjkAdvance, latinAdvance, lineHeight } = TEXT_METRICS;
  let totalAdvance = 0; let maxCharWidthPt = 0;
  for (const ch of text) {
    // CJK / 全角范围近似：CJK 统一表意 + 全角标点 + 假名等用全角；其余按拉丁
    const isCjk = /[　-鿿＀-￯぀-ヿ]/.test(ch);
    const w = fontSizePt * (isCjk ? cjkAdvance : latinAdvance);
    totalAdvance += w;
    if (w > maxCharWidthPt) maxCharWidthPt = w;
  }
  const usableW = Math.max(1, boxWidthPt);
  const lines = Math.max(1, Math.ceil(totalAdvance / usableW));
  return { neededHeightPt: lines * fontSizePt * lineHeight, maxCharWidthPt };
}

// ---------------------------------------------------------------------------
// 四项检查（每项纯函数；canvas/阈值参数化）
// ---------------------------------------------------------------------------

export function checkOverflow(shapes: ShapeBox[], annotations: TextBoxAnnotation[]): Violation[] {
  const byId = new Map(shapes.map((s) => [s.id, s]));
  const out: Violation[] = [];
  for (const a of annotations) {
    if (!a.text || !a.fontSizePt) continue;        // 无文本/字号 → 不臆测
    const box = byId.get(a.shapeId); if (!box) continue;
    const { neededHeightPt, maxCharWidthPt } = estimateTextBox(a.text, a.fontSizePt, box.width);
    if (neededHeightPt > box.height + OVERFLOW_TOLERANCE_PT) {
      out.push({ kind: 'overflow', shapeIds: [a.shapeId], detail: `文本预估高度 ${Math.round(neededHeightPt)}pt 超出文本框高 ${Math.round(box.height)}pt（保守上界）` });
    } else if (maxCharWidthPt > box.width + OVERFLOW_TOLERANCE_PT) {
      out.push({ kind: 'overflow', shapeIds: [a.shapeId], detail: `单字宽 ${Math.round(maxCharWidthPt)}pt 超出文本框宽 ${Math.round(box.width)}pt` });
    }
  }
  return out;
}

export function checkOverlap(shapes: ShapeBox[]): Violation[] {
  const out: Violation[] = [];
  for (let i = 0; i < shapes.length; i++) for (let j = i + 1; j < shapes.length; j++) {
    const a = shapes[i], b = shapes[j];
    const ix = Math.min(a.left + a.width, b.left + b.width) - Math.max(a.left, b.left);
    const iy = Math.min(a.top + a.height, b.top + b.height) - Math.max(a.top, b.top);
    if (ix > OVERLAP_MIN_PT && iy > OVERLAP_MIN_PT) {
      out.push({ kind: 'overlap', shapeIds: [a.id, b.id], detail: `形状 ${a.id} 与 ${b.id} 重叠（相交 ${Math.round(ix)}×${Math.round(iy)}pt）` });
    }
  }
  return out;
}

export function checkOutOfBounds(shapes: ShapeBox[], canvas: Canvas): Violation[] {
  const out: Violation[] = [];
  const { x: mx, y: my } = MARGINS_PT;
  for (const s of shapes) {
    const reasons: string[] = [];
    if (s.left < mx - OVERFLOW_TOLERANCE_PT) reasons.push(`左缘 ${Math.round(s.left)}pt < 页边距 ${mx}pt`);
    if (s.top < my - OVERFLOW_TOLERANCE_PT) reasons.push(`上缘 ${Math.round(s.top)}pt < 页边距 ${my}pt`);
    if (s.left + s.width > canvas.widthPt - mx + OVERFLOW_TOLERANCE_PT) reasons.push(`右缘 ${Math.round(s.left + s.width)}pt 超出画布右边距（${canvas.widthPt - mx}pt）`);
    if (s.top + s.height > canvas.heightPt - my + OVERFLOW_TOLERANCE_PT) reasons.push(`下缘 ${Math.round(s.top + s.height)}pt 超出画布下边距（${canvas.heightPt - my}pt）`);
    if (reasons.length) out.push({ kind: 'out_of_bounds', shapeIds: [s.id], detail: `形状 ${s.id} 越界：${reasons.join('；')}` });
  }
  return out;
}

/** 对比④：按 AI 供入色对算 WCAG；bg 缺失/非法 → undetermined（诚实降级，非违规）。 */
export function checkContrast(annotations: TextBoxAnnotation[]): { violations: Violation[]; notes: string[] } {
  const violations: Violation[] = []; const notes: string[] = [];
  for (const a of annotations) {
    if (!a.foreground && !a.background) continue;      // 该形状无配色注解 → 跳过
    if (!a.background) { violations.push({ kind: 'contrast_undetermined', shapeIds: [a.shapeId], detail: `形状 ${a.shapeId} 背景色未知（PPT web 背景读不稳），无法判定对比——请人工确认或提供背景色` }); continue; }
    const ratio = wcagContrastRatio(a.foreground ?? '', a.background);
    if (ratio === null) { violations.push({ kind: 'contrast_undetermined', shapeIds: [a.shapeId], detail: `形状 ${a.shapeId} 颜色解析失败（非法 hex），无法判定对比` }); continue; }
    const large = isLargeText(a.fontSizePt ?? 14, a.bold ?? false);
    const threshold = large ? 3 : 4.5;
    if (ratio < threshold) violations.push({ kind: 'low_contrast', shapeIds: [a.shapeId], detail: `形状 ${a.shapeId} 对比 ${ratio.toFixed(2)}:1 < ${threshold}:1（${large ? '大字' : '正文'}）` });
  }
  return { violations, notes };
}

// ---------------------------------------------------------------------------
// 顶层聚合 + 格式化
// ---------------------------------------------------------------------------

export function checkSlideLayout(
  shapes: ShapeBox[],
  opts?: { canvas?: Canvas; annotations?: TextBoxAnnotation[] },
): LayoutReport {
  const canvas = opts?.canvas ?? DEFAULT_CANVAS_PT;
  const annotations = opts?.annotations ?? [];
  const notes: string[] = [];
  const violations: Violation[] = [
    ...checkOverlap(shapes),
    ...checkOutOfBounds(shapes, canvas),
    ...checkOverflow(shapes, annotations),
  ];
  if (annotations.length === 0) notes.push('未提供文本/配色信息（textBoxes），仅检查了重叠与越界；溢出与对比未检查。');
  const contrast = checkContrast(annotations);
  violations.push(...contrast.violations);
  notes.push(...contrast.notes);
  return { canvas, violations, notes };
}

/** 把 LayoutReport 格式化为给 LLM 的中文 evidence 文字（含锚点）。 */
export function formatViolations(report: LayoutReport): string {
  const head = `【版面自查（确定性，画布 ${report.canvas.widthPt}×${report.canvas.heightPt}pt）】`;
  if (report.violations.length === 0) {
    return `${head}\n未发现溢出/重叠/越界/对比问题。${report.notes.length ? '\n说明：' + report.notes.join(' ') : ''}`;
  }
  const lines = report.violations.map((v, i) => `${i + 1}. [${v.kind}] ${v.detail}`);
  return `${head}\n发现 ${report.violations.length} 项待修正（建议据此调整后重新自查；这是建议非强制）：\n${lines.join('\n')}${report.notes.length ? '\n说明：' + report.notes.join(' ') : ''}`;
}
```

**关键约束**：
- canvas **参数**默认 `DEFAULT_CANVAS_PT`（960×540），**绝不在函数体内写死 720/405**（D-22-02）。
- 对比 ④ bg 缺失/非法 → `contrast_undetermined`（advisory note 类违规，**不是** `low_contrast`，**不报假阳性**，D-22-05）。
- 溢出 ① 只对有 `text+fontSizePt` 注解且 shapeId 命中的形状查；无注解不臆测（D-22-06）。
- 重叠②/越界③ 只用几何 + canvas/MARGINS_PT，恒定运行（不需文本/颜色）。
- 零 Office.js / React / 网络 import（仅 ppt-tokens）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c 'export function checkSlideLayout' src/agent/design/geometry-check.ts; grep -cE 'export function wcagContrastRatio|export function estimateTextBox|export function checkOverflow|export function checkOverlap|export function checkOutOfBounds|export function checkContrast|export function formatViolations' src/agent/design/geometry-check.ts; grep -c 'contrast_undetermined' src/agent/design/geometry-check.ts; grep -cE 'PowerPoint\.|Office\.|import .*react|fetch\(' src/agent/design/geometry-check.ts; grep -c '720\|405' src/agent/design/geometry-check.ts; npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function checkSlideLayout' src/agent/design/geometry-check.ts` 返回 1
    - `grep -cE 'export function wcagContrastRatio|export function estimateTextBox|export function checkOverflow|export function checkOverlap|export function checkOutOfBounds|export function checkContrast|export function formatViolations' src/agent/design/geometry-check.ts` 返回 7（七个导出齐全）
    - `grep -c 'opts?.canvas ?? DEFAULT_CANVAS_PT' src/agent/design/geometry-check.ts` 返回 1（canvas 参数化默认 960×540，D-22-02）
    - `grep -c '720' src/agent/design/geometry-check.ts` 返回 0 且 `grep -c '405' src/agent/design/geometry-check.ts` 返回 0（绝不硬编错误基准）
    - `grep -c 'contrast_undetermined' src/agent/design/geometry-check.ts` >= 2（诚实降级分支存在：bg 缺失 + hex 非法两条，D-22-05）
    - `grep -cE 'PowerPoint\.|Office\.|from .react|fetch\(' src/agent/design/geometry-check.ts` 返回 0（纯 TS、零网络零 Office.js/React）
    - `npx tsc --noEmit` 退出 0
  </acceptance_criteria>
  <done>geometry-check.ts 已建：四项检查（溢出保守上界/重叠>2pt/越界含页边距/对比 WCAG）+ WCAG 相对亮度 + 文本宽度估算 + checkSlideLayout 聚合 + formatViolations；canvas 参数化默认 960×540；对比 bg 不可读诚实降级 undetermined（不假阳性）；纯 TS 零依赖；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 3: check_slide_layout read 工具 + 注册进 buildToolsForHost('ppt')（PVQ-02 证据接线，D-22-04）</name>
  <files>src/agent/tools/read/ppt.ts, src/agent/tools/index.ts</files>
  <read_first>
    - src/agent/tools/read/ppt.ts（全文，重点 listShapesOnSlide L69-93 范式 + import L13-14）
    - src/agent/tools/index.ts（import L20、PPT_TOOLS L30-46 不动、buildToolsForHost('ppt') L294-312 read 列表注入站点）
    - src/agent/design/geometry-check.ts（Task 2：checkSlideLayout / ShapeBox / TextBoxAnnotation）
    - src/agent/design/ppt-tokens.ts（Task 1：DEFAULT_CANVAS_PT）
    - .planning/phases/22-ppt-design-tokens-geometry-check/22-CONTEXT.md D-22-04/09（read 工具、不进 PPT_TOOLS、camelCase + textBoxes 双键容错）
  </read_first>
  <action>
**① `src/agent/tools/read/ppt.ts` 顶部 import 增加**（紧随现有 import）：
```typescript
import { checkSlideLayout as runLayoutCheck, formatViolations, type ShapeBox, type TextBoxAnnotation } from '../../design/geometry-check';
import { DEFAULT_CANVAS_PT } from '../../design/ppt-tokens';
```

**② 在 `getShape` 之后追加 `checkSlideLayout` ToolDef**：
```typescript
interface CheckLayoutArgs {
  slideIndex: number;
  /** 可选：AI 供入的文本/配色注解（溢出① 与对比④ 需要；缺省则只查重叠②/越界③）。 */
  textBoxes?: Array<{
    shapeId?: string; shape_id?: string;   // snake/camel 双键容错（memory project_ppt_officejs_gotchas）
    text?: string; fontSizePt?: number; bold?: boolean; foreground?: string; background?: string;
  }>;
}

export const checkSlideLayout: ToolDef<CheckLayoutArgs> = {
  name: 'check_slide_layout',
  description:
    '对指定幻灯片（1-based）做确定性版面自查（纯计算、非阻断）：检查形状重叠、越界（超画布/页边距），' +
    '以及（当你在 textBoxes 里提供 text+fontSizePt）文本溢出、（提供 foreground+background hex）文字/背景对比度（WCAG）。' +
    '返回违规清单作为你下一步修正的依据——这是建议不是强制；背景色读不到时会诚实标记「无法判定」而非误报。' +
    '在你用形状工具排好一页后调用本工具自查，再按清单调整。',
  parameters: {
    type: 'object',
    properties: {
      slideIndex: { type: 'number', description: '1-based 幻灯片序号' },
      textBoxes: {
        type: 'array',
        description: '可选：每个文本框的内容与配色（用于溢出与对比检查）。不传则只查重叠与越界。',
        items: {
          type: 'object',
          properties: {
            shapeId: { type: 'string', description: '形状 ID（list_shapes_on_slide 返回）' },
            text: { type: 'string', description: '该框文本内容（溢出检查用）' },
            fontSizePt: { type: 'number', description: '字号 pt（溢出/对比大字判定用）' },
            bold: { type: 'boolean', description: '是否加粗（对比大字阈值用）' },
            foreground: { type: 'string', description: '文字色 hex，如 #222222' },
            background: { type: 'string', description: '背景色 hex，如 #FFFFFF（读不到可不传）' },
          },
        },
      },
    },
    required: ['slideIndex'],
  },
  humanLabel: ({ slideIndex }) => `自查了第 ${slideIndex} 张幻灯片的版面`,
  kind: 'read',
  async execute({ slideIndex, textBoxes }, ctx): Promise<ToolResult> {
    const r = await ctx.adapter.read({ kind: 'list_shapes_on_slide', slideIndex });
    if (!r.ok) {
      // 几何读失败：原样透传错误（wrapReadResult 处理 ok:false 分支）
      return wrapReadResult(r, { result_type: 'metadata', source: `slide_${slideIndex}.layout_check` });
    }
    const shapes = ((r.data as { shapes?: ShapeBox[] }).shapes ?? []) as ShapeBox[];
    const annotations: TextBoxAnnotation[] = (textBoxes ?? [])
      .map((t) => ({ ...t, shapeId: (t.shapeId ?? t.shape_id) as string }))   // 双键容错
      .filter((t) => !!t.shapeId);
    const report = runLayoutCheck(shapes, { canvas: DEFAULT_CANVAS_PT, annotations });
    return wrapReadResult(
      { ok: true, data: { ...report, summary: formatViolations(report) } },
      { result_type: 'metadata', source: `slide_${slideIndex}.layout_check` },
    );
  },
};
```

**③ `src/agent/tools/index.ts`**：
- import 行（L20）加 `checkSlideLayout`：
  ```typescript
  import { listSlides, getSlide, listShapesOnSlide, getShape, checkSlideLayout } from './read/ppt';
  ```
- `buildToolsForHost('ppt')` read 列表（L307-311）加入：
  ```typescript
  return [
    listSlides, getSlide, listShapesOnSlide, getShape, checkSlideLayout,
    getShapeImage,
    ...pptWriteTools, selectionDetail,
  ].map((t) => t as ToolDef);
  ```
- **不动 `PPT_TOOLS` set**（read 工具不入 write 归一化集合，D-22-04）。

**关键约束**：
- `kind:'read'`、复用既有 `adapter.read({kind:'list_shapes_on_slide'})`、**不新增 ReadRequest kind**、**不改 PptAdapter.ts**。
- 无 reverse / 无 postState / 无 operationLog（非 mutating，D-22-04）。
- camelCase `slideIndex`（与 listShapesOnSlide 一致，read 工具不归一化）；`textBoxes[]` 内 shapeId 做 snake/camel 双键容错。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c "name: 'check_slide_layout'" src/agent/tools/read/ppt.ts; grep -c "kind: 'read'" src/agent/tools/read/ppt.ts; grep -c 'checkSlideLayout' src/agent/tools/index.ts; grep -c 'check_slide_layout' src/agent/tools/index.ts; grep -c "kind: 'list_shapes_on_slide', slideIndex" src/agent/tools/read/ppt.ts; npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name: 'check_slide_layout'" src/agent/tools/read/ppt.ts` 返回 1（ToolDef 存在）
    - `grep -c "checkSlideLayout" src/agent/tools/index.ts` 返回 2（import + read 列表注册各一处）
    - `grep -c "kind: 'list_shapes_on_slide', slideIndex" src/agent/tools/read/ppt.ts` >= 1（复用既有 read kind，无新 adapter kind）
    - `grep -c 'check_slide_layout' src/agent/tools/index.ts` 返回 0（PPT_TOOLS set 不含——read 工具不归一化，**确认没误加进 write set**）
    - `grep -nc 'restore\|reverse\|postState\|operationLog\|appendOperation' src/agent/tools/read/ppt.ts` 返回 0（read 工具无 undo 守门，D-22-04）
    - `grep -c 'shape_id' src/agent/tools/read/ppt.ts` >= 1（textBoxes snake/camel 双键容错）
    - `npx tsc --noEmit` 退出 0
  </acceptance_criteria>
  <done>check_slide_layout read 工具已建并注册进 ppt read 列表；kind:'read'、复用 list_shapes_on_slide 读 kind、零 adapter 改动、无 undo；不进 PPT_TOOLS；camelCase + textBoxes 双键容错；结果经 wrapReadResult metadata → wire tool-message = evidence；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 4: 测试 — ppt-tokens.test.ts + geometry-check.test.ts（新）+ 工具计数 21→22（PVQ-01/02 守门）</name>
  <files>src/agent/design/ppt-tokens.test.ts, src/agent/design/geometry-check.test.ts, src/agent/tools/read/tools.test.ts, src/agent/tools/index.test.ts</files>
  <read_first>
    - src/agent/design/ppt-tokens.ts + geometry-check.ts（Task 1/2 全部导出）
    - src/agent/tools/read/vision.test.ts（read 工具 mock 范式：adapter:{read:vi.fn()} as never）
    - src/agent/tools/read/tools.test.ts（L205-260 PPT host 块 + L212 计数 21）
    - src/agent/tools/index.test.ts（L60-70 PPT host 计数 21）
    - .planning/phases/22-ppt-design-tokens-geometry-check/22-CONTEXT.md D-22-08
  </read_first>
  <action>
**A. 新建 `src/agent/design/ppt-tokens.test.ts`**（结构守门，含「无调色板」断言）：
```ts
import { describe, it, expect } from 'vitest';
import { FONT_LADDER_PT, MARGINS_PT, GAP_PT, DEFAULT_CANVAS_PT, DEFAULT_ACCENT, SEMANTIC, gridFull, gridTwoColumn } from './ppt-tokens';

describe('ppt-tokens 结构 token（PVQ-01）', () => {
  it('默认画布 = 960×540pt（非 720×405，D-22-02）', () => {
    expect(DEFAULT_CANVAS_PT).toEqual({ widthPt: 960, heightPt: 540 });
  });
  it('字号阶梯单调递减：title>subtitle>heading>body>caption，kpi 最大', () => {
    const { title, subtitle, heading, body, caption, kpi } = FONT_LADDER_PT;
    expect(title).toBeGreaterThan(subtitle);
    expect(subtitle).toBeGreaterThan(heading);
    expect(heading).toBeGreaterThan(body);
    expect(body).toBeGreaterThan(caption);
    expect(kpi).toBeGreaterThanOrEqual(title);
  });
  it('页边距/间距存在且为正', () => {
    expect(MARGINS_PT.x).toBeGreaterThan(0);
    expect(MARGINS_PT.y).toBeGreaterThan(0);
    expect(GAP_PT).toBeGreaterThan(0);
  });
  it('兜底单色 + 涨跌语义色存在（配色不锁死：仅兜底，非调色板）', () => {
    expect(DEFAULT_ACCENT.light).toMatch(/^#[0-9a-fA-F]{6}$/);
    expect(DEFAULT_ACCENT.dark).toMatch(/^#[0-9a-fA-F]{6}$/);
    expect(SEMANTIC.success).toMatch(/^#[0-9a-fA-F]{6}$/);
    expect(SEMANTIC.error).toMatch(/^#[0-9a-fA-F]{6}$/);
  });
  it('无固定调色板：模块未导出任何颜色数组（配色不锁死，D-22-01）', async () => {
    const mod = await import('./ppt-tokens');
    const arrayColorExports = Object.entries(mod).filter(
      ([, v]) => Array.isArray(v) && (v as unknown[]).every((x) => typeof x === 'string' && /^#[0-9a-fA-F]{3,6}$/.test(x)),
    );
    expect(arrayColorExports).toHaveLength(0);
  });
  it('gridFull / gridTwoColumn 区域全在画布内、随 canvas 缩放', () => {
    for (const canvas of [DEFAULT_CANVAS_PT, { widthPt: 720, heightPt: 405 }]) {
      const f = gridFull(canvas);
      expect(f.content.left).toBeGreaterThanOrEqual(MARGINS_PT.x);
      expect(f.content.left + f.content.width).toBeLessThanOrEqual(canvas.widthPt - MARGINS_PT.x + 0.01);
      const t = gridTwoColumn(canvas);
      expect(t.left.width).toBeCloseTo(t.right.width); // 两栏等宽
      expect(t.right.left + t.right.width).toBeLessThanOrEqual(canvas.widthPt - MARGINS_PT.x + 0.01);
    }
  });
});
```

**B. 新建 `src/agent/design/geometry-check.test.ts`**（四项各 happy+edge + 降级 + helper）：
```ts
import { describe, it, expect } from 'vitest';
import { wcagContrastRatio, estimateTextBox, checkSlideLayout, formatViolations, type ShapeBox } from './geometry-check';

const box = (id: string, left: number, top: number, width: number, height: number): ShapeBox => ({ id, left, top, width, height });

describe('WCAG 对比（PVQ-02 ④ helper）', () => {
  it('黑/白 = 21:1', () => expect(wcagContrastRatio('#000000', '#FFFFFF')!).toBeCloseTo(21, 0));
  it('白/白 = 1:1', () => expect(wcagContrastRatio('#FFFFFF', '#FFFFFF')!).toBeCloseTo(1, 1));
  it('非法 hex → null（→ 诚实降级）', () => expect(wcagContrastRatio('zzz', '#FFFFFF')).toBeNull());
});

describe('文本宽度估算（PVQ-02 ① 保守上界）', () => {
  it('CJK 比同长度拉丁需要更高（更宽 advance）', () => {
    const cjk = estimateTextBox('中文中文中文中文', 14, 100).neededHeightPt;
    const latin = estimateTextBox('aaaaaaaa', 14, 100).neededHeightPt;
    expect(cjk).toBeGreaterThanOrEqual(latin);
  });
});

describe('checkSlideLayout 四项（PVQ-02）', () => {
  it('① 溢出 happy：短文本不溢出', () => {
    const r = checkSlideLayout([box('s1', 50, 50, 400, 200)], { annotations: [{ shapeId: 's1', text: '短', fontSizePt: 14 }] });
    expect(r.violations.filter((v) => v.kind === 'overflow')).toHaveLength(0);
  });
  it('① 溢出 edge：长文本超小框 → 报溢出', () => {
    const r = checkSlideLayout([box('s1', 50, 50, 100, 20)], { annotations: [{ shapeId: 's1', text: '很长的文本'.repeat(40), fontSizePt: 18 }] });
    expect(r.violations.some((v) => v.kind === 'overflow')).toBe(true);
  });
  it('② 重叠 happy：分离的框不报', () => {
    const r = checkSlideLayout([box('a', 50, 50, 100, 100), box('b', 200, 50, 100, 100)]);
    expect(r.violations.filter((v) => v.kind === 'overlap')).toHaveLength(0);
  });
  it('② 重叠 edge：相交 >2pt 报，<2pt 不报', () => {
    const overlap = checkSlideLayout([box('a', 50, 50, 100, 100), box('b', 100, 50, 100, 100)]);
    expect(overlap.violations.some((v) => v.kind === 'overlap')).toBe(true);
    const touch = checkSlideLayout([box('a', 50, 50, 100, 100), box('b', 151, 50, 100, 100)]);
    expect(touch.violations.filter((v) => v.kind === 'overlap')).toHaveLength(0);
  });
  it('③ 越界 happy：框在边距内不报', () => {
    const r = checkSlideLayout([box('s', 60, 50, 400, 300)]);
    expect(r.violations.filter((v) => v.kind === 'out_of_bounds')).toHaveLength(0);
  });
  it('③ 越界 edge：框超出 960 画布右缘 → 报（用默认 canvas 960×540，不是 720）', () => {
    const r = checkSlideLayout([box('s', 900, 50, 200, 100)]); // 右缘 1100 > 960
    expect(r.violations.some((v) => v.kind === 'out_of_bounds')).toBe(true);
  });
  it('③ 关键回归：右半屏 (left=700,width=200→右缘 900) 在 960 画布内不越界（若误用 720 会假报）', () => {
    const r = checkSlideLayout([box('s', 700, 50, 200, 100)]);
    expect(r.violations.filter((v) => v.kind === 'out_of_bounds')).toHaveLength(0);
  });
  it('④ 对比 happy：黑字白底不报', () => {
    const r = checkSlideLayout([box('s', 60, 50, 400, 100)], { annotations: [{ shapeId: 's', foreground: '#000000', background: '#FFFFFF', fontSizePt: 14 }] });
    expect(r.violations.filter((v) => v.kind === 'low_contrast')).toHaveLength(0);
  });
  it('④ 对比 edge：低对比正文 <4.5:1 报 low_contrast', () => {
    const r = checkSlideLayout([box('s', 60, 50, 400, 100)], { annotations: [{ shapeId: 's', foreground: '#888888', background: '#777777', fontSizePt: 14 }] });
    expect(r.violations.some((v) => v.kind === 'low_contrast')).toBe(true);
  });
  it('④ 大字 3:1 阈值：18pt 大字在 3:1~4.5:1 之间不报（正文会报）', () => {
    // 选一对 ratio 介于 3 与 4.5 的色：#767676 on #FFFFFF ≈ 4.48… 取更明确的边界对
    const large = checkSlideLayout([box('s', 60, 50, 400, 100)], { annotations: [{ shapeId: 's', foreground: '#949494', background: '#FFFFFF', fontSizePt: 20 }] });
    expect(large.violations.filter((v) => v.kind === 'low_contrast')).toHaveLength(0); // 大字阈值 3:1 放过
  });
  it('④ 诚实降级（D-22-05 MUST）：缺背景色 → contrast_undetermined，不报 low_contrast 假阳性', () => {
    const r = checkSlideLayout([box('s', 60, 50, 400, 100)], { annotations: [{ shapeId: 's', foreground: '#222222', fontSizePt: 14 }] });
    expect(r.violations.some((v) => v.kind === 'contrast_undetermined')).toBe(true);
    expect(r.violations.some((v) => v.kind === 'low_contrast')).toBe(false);
  });
  it('无 annotations：只查重叠/越界，notes 说明未查溢出/对比', () => {
    const r = checkSlideLayout([box('a', 50, 50, 100, 100), box('b', 60, 60, 100, 100)]);
    expect(r.violations.some((v) => v.kind === 'overlap')).toBe(true);
    expect(r.notes.join('')).toContain('未提供');
  });
  it('formatViolations 含画布尺寸锚点 + 建议非强制语', () => {
    const r = checkSlideLayout([box('a', 50, 50, 100, 100), box('b', 60, 60, 100, 100)]);
    const s = formatViolations(r);
    expect(s).toContain('960×540');
    expect(s).toMatch(/建议|非强制/);
  });
});
```
> ⚠️ 执行时若 `#949494 on #FFFFFF` 实测 ratio 不落在 3~4.5 区间，executor 调整为确知落区间的色对（如计算 ratio ∈ (3,4.5) 的灰阶），保持「大字放过 / 正文会报」语义不变。

**C. 工具集成 + 计数**：
- `src/agent/tools/read/tools.test.ts`：
  - L212 PPT 计数 `toHaveLength(21)` → `toHaveLength(22)`（注释加：Phase 22 新增 check_slide_layout）。
  - PPT host `it('包含正确的 tool 名称')` 块加 `expect(names).toContain('check_slide_layout');`。
  - 加一个 execute 断言（mock adapter.read 返 shapes，断言 result_type='metadata' + data.summary 含「版面自查」）：
    ```ts
    it('check_slide_layout execute → result_type=metadata，含违规 summary', async () => {
      const adapter = makeAdapter({ ok: true, data: { slideIndex: 1, shapes: [
        { id: 'a', type: 'TextBox', left: 50, top: 50, width: 100, height: 100 },
        { id: 'b', type: 'TextBox', left: 60, top: 60, width: 100, height: 100 },
      ] } });
      const tools = buildToolsForHost('ppt');
      const tool = tools.find((t) => t.name === 'check_slide_layout')!;
      const res = await tool.execute({ slideIndex: 1 }, makeCtx(adapter));
      expect(res.ok).toBe(true);
      const data = (res.data as Record<string, unknown>);
      expect(data.result_type).toBe('metadata');
    });
    ```
- `src/agent/tools/index.test.ts`：L69 PPT 计数 `toHaveLength(21)` → `toHaveLength(22)`（注释标 Phase 22）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/agent/design/ppt-tokens.test.ts src/agent/design/geometry-check.test.ts src/agent/tools/read/tools.test.ts src/agent/tools/index.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/agent/design/geometry-check.test.ts` 全 green（四项 happy+edge + 右半屏 960 回归 + 对比诚实降级 + helper）
    - `npx vitest run src/agent/design/ppt-tokens.test.ts` 全 green（含「无固定调色板数组」断言）
    - `grep -c 'check_slide_layout' src/agent/tools/read/tools.test.ts` >= 2（name 断言 + execute 断言）
    - `grep -c 'toHaveLength(22)' src/agent/tools/read/tools.test.ts` >= 1 且 `grep -c 'toHaveLength(22)' src/agent/tools/index.test.ts` >= 1（PPT 计数 21→22）
    - `grep -c 'contrast_undetermined' src/agent/design/geometry-check.test.ts` >= 1（诚实降级专测存在）
    - `grep -c '700, 50, 200' src/agent/design/geometry-check.test.ts` >= 1（右半屏 960 画布回归——若误用 720 baseline 会假报，此测守门 D-22-02）
  </acceptance_criteria>
  <done>ppt-tokens.test.ts（结构 + 无调色板）+ geometry-check.test.ts（四项 happy/edge + 960 右半屏回归 + 对比诚实降级 + WCAG/文本 helper）+ 工具集成 + PPT 计数 21→22 全 green。</done>
</task>

<task type="auto">
  <name>Task 5: 最终验证 — tsc + 全套 test + build + size（bundle ~0 增量确认）</name>
  <files></files>
  <read_first>
    - 无（仅运行验证命令）
    - memory project_bundle_size_guard：npm run size 测陈旧 dist 给假绿，必须先 build 再 size
    - memory i18n_extract_and_test_noise：本 phase 不动 Lingui 宏 → 无需 extract；「N failed」才是真失败，尾部 3 retry errors 是噪音
  </read_first>
  <action>
按顺序运行并逐一确认：
1. 类型检查：`npx tsc --noEmit`
2. 全套测试：`npm test -- --run`
3. 构建并验 bundle：`npm run build && npm run size`

确认要点：
- tsc 退出 0
- 全套 green（现有 933 + 本 phase 新增）；「N failed」= 真失败；尾部 3 retry errors 是噪音
- bundle main gzip ≤82KB（baseline 80.6KB；geometry-check / ppt-tokens / check_slide_layout 均落懒加载 agent chunk，预期初始 ~0 增量）

若 size 逼近/超 82KB：确认 design 模块确实在懒加载 chunk（不被初始入口直接 import），geometry-check 零 import 重模块；必要时回查 Task 1-3 是否误把 design 模块拉进初始路径。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -5 && npm test -- --run 2>&1 | tail -12 && npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 退出 0
    - `npm test -- --run` 退出 0，无「N failed」（尾部 3 retry errors 是噪音）；现有 933 测仍 green
    - `npm run size` main bundle gzip ≤82KB（预期与 80.6KB 持平；纯 TS、0 新依赖、懒加载 chunk）
    - `grep -rc 'check_slide_layout' dist/ 2>/dev/null | grep -v ':0' | head -1` 有输出（工具进了构建产物——证明已注册，可被 LLM 发现）
  </acceptance_criteria>
  <done>tsc 0 + 全套 green（933+新增）+ bundle ≤82KB（先 build 再 size）；Phase 22 PVQ-01 + PVQ-02 机制交付完毕。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| check_slide_layout 入参（slideIndex / textBoxes）→ 几何自查 | LLM 生成的工具入参；textBoxes 文本/颜色由 LLM 供入，纯本地计算，不出网络 |
| adapter.read(list_shapes_on_slide) 返回几何 → 自查 | 既有可信 read 路径（metadata），本 phase 复用未改 |
| 违规清单（formatViolations 输出）→ wire tool-message | 本地确定性计算结果，作 evidence 喂回 LLM；非用户机密、非网络数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | textBoxes 文本/hex 入参 | accept | 全部进纯本地计算（无 eval、无网络、无 DOM 注入）；hex 经 `parseHex` 正则白名单校验，非法 → null → 诚实降级，绝不抛/不注入。无新攻击面。 |
| T-22-02 | DoS | 超大 textBoxes / 超长 text | accept | 几何自查是 O(n²) 重叠 + O(text 长度) 估算；shapes 来自单页（量小）；text 估算线性。read-result `applySizeCap` 50K token 上限兜底输出。无放大风险。 |
| T-22-03 | Information Disclosure | 违规清单 evidence | accept | 仅含形状 id + 几何量化 + 对比比值，无 Key / 无用户机密；result_type='metadata'（可信结构信息）；不写文档、不出网络。 |
| T-22-04 | Spoofing/Integrity | 几何基准错误（720 vs 960） | mitigate | canvas 参数化 + 默认 960×540（D-22-02）；geometry-check.test 右半屏 960 回归测守门；真机基准确认列入 v2.3 末 UAT。错误基准只会致 advisory 误报（非阻断、非破坏），LLM 可自主忽略。 |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster

# 类型检查
npx tsc --noEmit

# 几何 / token / 工具守门（含右半屏 960 回归 + 对比诚实降级 + 无调色板）
npx vitest run src/agent/design/geometry-check.test.ts src/agent/design/ppt-tokens.test.ts \
  src/agent/tools/read/tools.test.ts src/agent/tools/index.test.ts

# 全套测试（933 + 新增；「N failed」才是真失败，尾部 3 retry errors 是噪音）
npm test -- --run

# bundle（先 build 再 size，不用陈旧 dist；main gzip ≤82KB，预期 ~0 增量）
npm run build && npm run size

# 本 phase 不动 Lingui 宏 → 无需 npm run extract
```
</verification>

<success_criteria>
1. `src/agent/design/ppt-tokens.ts` 存在：字号阶梯（标题/副标/heading/正文/脚注/kpi pt）、统一页边距 + 间距、两套 canvas 参数化网格（整页/左右两栏）、默认画布 **960×540**（非 720×405）、缺省/兜底单色 teal + 涨跌语义色；**无固定调色板数组**（配色不锁死）；无散落硬编码字号/页边距重复值。
2. `geometry-check.ts` 几何自查接收 `{left,top,width,height}[]` + **canvas 参数**（默认 960×540），确定性输出违规清单，四项齐全：① 溢出（保守上界）② 重叠（>2pt）③ 越界（超画布或入页边距）④ 对比（WCAG <4.5:1 正文 / <3:1 大字；**bg 不可读 → undetermined 诚实降级，不假阳性**）。
3. `check_slide_layout` read 工具注册进 `buildToolsForHost('ppt')`（kind:'read'、不进 PPT_TOOLS、无 undo），复用既有 `adapter.read({kind:'list_shapes_on_slide'})`、零 adapter 改动；结果经既有 read-tool → wire tool-message 路径成为下一轮 evidence。**本 phase 不改 system-prompt.ts**（SC「prompt 不再脑补坐标」由 Phase 23 PVQ-05 整体重写满足——显式排序 defer，避免 system-prompt.ts 三方churn）。
4. 几何自查纯 TS、零网络零依赖；单测覆盖四项各 happy + edge + 右半屏 960 回归 + 对比诚实降级 + ppt-tokens 结构（含无调色板）+ 工具集成 + PPT 计数 21→22；现有 933 测全 green；tsc 0；bundle main gzip ≤82KB（先 build 再 size，预期 ~0 增量、0 净新增依赖）。
</success_criteria>

<output>
完成后创建 `.planning/phases/22-ppt-design-tokens-geometry-check/22-01-SUMMARY.md`
</output>

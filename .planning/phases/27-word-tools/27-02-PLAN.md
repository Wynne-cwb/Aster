---
phase: 27-word-tools
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - src/agent/tools/write/word.ts
  - src/adapters/WordAdapter.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - WORD-06
  - WORD-07
  - WORD-08

must_haves:
  truths:
    - "WORD-06 高亮：set_word_character_format 的 font 参数支持 highlightColor 字段，restoreRangeFont inverse 能正确写回 null（移除高亮）；专属 integration 用例验 null 写回不被 null-guard 跳过"
    - "WORD-07 列表：set_word_list_format 工具可执行 startNewList + setLevelBullet/setLevelNumbering，undo = noop_inverse（诚实标注无法撤销）"
    - "WORD-08 批注：insert_word_comment 工具可插入批注，内容自动加 '[Aster] ' 前缀，undo = deleteCommentById（按 id 删批注）；deleteCommentById 读 ctx.document.body.comments（与 Plan 01 mock 路径字面量一致）"
    - "三工具均注册在 buildToolsForHost('word') 的 wordWriteTools 数组中，assertWriteToolRegisterable 校验通过（humanLabel 非空）"
    - "integration.test.ts 中 insert_word_comment → rolled_back、set_word_list_format → skipped_error（保持）、set_word_character_format 已有用例仍 rolled_back（含新增 highlightColor null 写回断言）"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "WORD-06 setCharacterFormat 扩展 + WORD-07 setWordListFormat + WORD-08 insertWordComment + deleteCommentById"
      contains: "highlightColor"
    - path: "src/agent/tools/write/word.ts"
      provides: "WORD-07 setWordListFormat ToolDef + WORD-08 insertWordComment ToolDef"
      contains: "set_word_list_format"
    - path: "src/agent/tools/index.ts"
      provides: "新工具注册"
      contains: "setWordListFormat"
  key_links:
    - from: "word.ts setWordListFormat"
      to: "WordAdapter.setWordListFormat"
      via: "execute() ctx.adapter 调用"
      pattern: "setWordListFormat"
    - from: "word.ts insertWordComment"
      to: "WordAdapter.insertWordComment"
      via: "execute() ctx.adapter 调用"
      pattern: "insertWordComment"
    - from: "WordAdapter.deleteCommentById"
      to: "ctx.document.body.comments"
      via: "遍历 body.comments 找 id（与 Plan 01 mock 挂载路径一致）"
      pattern: "body.*comments"
---

<objective>
本计划实现 WORD-06（折入 set_word_character_format）、WORD-07（set_word_list_format，noop+gate）、WORD-08（insert_word_comment，按 id 删 inverse）三个工具的完整实现，包括 adapter write/inverse 方法、word.ts ToolDef、index.ts 注册。

Purpose: 让 Plan 01 建立的 integration.test.ts 守门用例中，WORD-08 从 skipped_error 变为 rolled_back；WORD-07 守门用例保持 skipped_error（noop 正确路径）。WORD-06 折入验证 set_word_character_format 已有用例仍通过，并新增 highlightColor null 写回断言。
Output: WordAdapter.ts 新增 3 个 write 方法 + 2 个 inverse 方法 + WORD-06 扩展；word.ts 新增 2 个 ToolDef；index.ts 注册 2 个新工具

⚠️ **跨 plan mock 契约钉死（BLOCKER 2 修复）：** Plan 01 的 mockWordRich 把 comments mock 挂在 **`ctx.document.body.comments`**。本 plan 的 `deleteCommentById` 实现也**必须**读 `ctx.document.body.comments`（路径字面量完全一致），否则运行时 `body.comments` 为 undefined，insert_word_comment 守门用例会跌成 skipped_error 而非 rolled_back。**不要**改读 `ctx.document.comments` 或 `getCommentRanges()` —— Plan 01 mock 已钉死 body.comments 路径。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-word-tools/27-CONTEXT.md
@.planning/phases/27-word-tools/27-RESEARCH.md
@.planning/phases/27-word-tools/27-PATTERNS.md
@.planning/phases/27-word-tools/27-01-SUMMARY.md

<interfaces>
<!-- 关键 analog 代码，执行者直接使用 -->

**WORD-06 折入 — setCharacterFormat 修改点（src/adapters/WordAdapter.ts）：**

loadStr 行（当前 ~L493）查找 `items/font/bold` 类的加载字符串末尾，追加 `,items/font/highlightColor`（注意拼写：`highlightColor`，camelCase）

before-image 块（~L529-237）在末尾加：
```typescript
highlightColor: f.highlightColor,
```

only-if-present 写入块（~L540-546）追加：
```typescript
// highlightColor：null 是有意义语义（移除高亮），不做 null-guard 跳过（与 Pitfall 3 说明一致）
if (font.highlightColor !== undefined) f.highlightColor = font.highlightColor as string | null;
```

SetWordCharacterFormatArgs 接口（src/agent/tools/write/word.ts，~L206-222）的 font 内嵌类型追加：
```typescript
highlightColor?: string | null;  // null 表示移除高亮
```

restoreRangeFont 恢复块（~L308-313）在 `f.name` 写回之后追加：
```typescript
// highlightColor null 要写回（表示移除高亮），不跳过（Pitfall 3：与 bold/italic null-guard 不同）
if (before.highlightColor !== undefined) f.highlightColor = before.highlightColor as string | null;
```

**WORD-07 setWordListFormat（新增）：**

word.ts 新增 ToolDef（按 Analog 1b noop+gate 范式，PATTERNS.md L109-143）：
```typescript
interface SetWordListFormatArgs {
  paragraphIndex: number;
  uniqueLocalId?: string;
  listType: 'bullet' | 'number';
  bulletStyle?: string;   // 'Solid'|'Hollow'|'Square'|'Arrow'|'Checkmark'
  numberStyle?: string;   // 'Arabic'|'UpperRoman'|'LowerRoman'|'UpperLetter'|'LowerLetter'
  level?: number;         // 默认 0
}

export const setWordListFormat: ToolDef<SetWordListFormatArgs> = {
  name: 'set_word_list_format',
  kind: 'write',
  description: '将 Word 指定段落转换为项目符号或编号列表。注意：此操作无法自动撤销（Word Online 列表 API 限制），执行前请确认。listType="bullet" 为项目符号，listType="number" 为编号列表。',
  parameters: {
    type: 'object',
    properties: {
      paragraphIndex: { type: 'number', description: '目标段落编号（0-based）' },
      uniqueLocalId: { type: 'string', description: '段落唯一 ID（可选，精确消歧）' },
      listType: { type: 'string', enum: ['bullet', 'number'], description: '列表类型：bullet=项目符号，number=编号' },
      bulletStyle: { type: 'string', description: '项目符号样式（可选）：Solid/Hollow/Square/Arrow/Checkmark' },
      numberStyle: { type: 'string', description: '编号样式（可选）：Arabic/UpperRoman/LowerRoman/UpperLetter/LowerLetter' },
      level: { type: 'number', description: '列表层级，0-based，默认 0' },
    },
    required: ['paragraphIndex', 'listType'],
  },
  humanLabel: ({ paragraphIndex, listType }) =>
    `将第 ${Number(paragraphIndex) + 1} 段改为${listType === 'bullet' ? '项目符号' : '编号'}列表`,
  async execute({ paragraphIndex, uniqueLocalId, listType, bulletStyle, numberStyle, level }, ctx): Promise<ToolResult> {
    await (ctx.adapter as WordAdapter).setWordListFormat({
      paragraphIndex, uniqueLocalId, listType, bulletStyle, numberStyle, level,
    });
    // WORD-07 noop+gate：Word Online lists.getById 无条件失败（GitHub #6525），无法可靠还原原列表态
    const reverse: ReverseDescriptor = {
      tool: 'noop_inverse',
      args: { reason: '列表格式转换无法自动撤销，请手动操作（Word Online 列表 API 限制）' },
    };
    const postState = { kind: 'word_list_format' as const, content: { index: paragraphIndex } };
    return {
      ok: true,
      data: { paragraphIndex, listType, applied: true },
      reverse,
      postState,
    };
  },
};
```

WordAdapter.ts 新增 setWordListFormat 方法（按 Analog 2d isSetSupported 模式 + Word.run 闭包）：
```typescript
async setWordListFormat(args: Record<string, unknown>): Promise<void> {
  const paragraphIndex = args.paragraphIndex as number;
  const uniqueLocalId = args.uniqueLocalId as string | undefined;
  const listType = args.listType as 'bullet' | 'number';
  const bulletStyle = (args.bulletStyle as string | undefined) ?? 'Solid';
  const numberStyle = (args.numberStyle as string | undefined) ?? 'Arabic';
  const level = (args.level as number | undefined) ?? 0;

  // isSetSupported 门控（WordApi 1.3）
  const supports = typeof Office !== 'undefined' &&
    Office.context?.requirements?.isSetSupported('WordApi', '1.3') === true;
  if (!supports) {
    throw new HostApiError('当前 Word 版本不支持列表操作（需要 WordApi 1.3）', undefined);
  }

  try {
    await Word.run(async (ctx) => {
      const paras = ctx.document.body.paragraphs;
      paras.load('items/text,items/uniqueLocalId');
      await ctx.sync();

      // 双重定位：index 快路径 + uniqueLocalId 消歧
      let targetIndex = paragraphIndex;
      if (uniqueLocalId) {
        const byId = paras.items.findIndex((p) => (p as unknown as { uniqueLocalId: string }).uniqueLocalId === uniqueLocalId);
        if (byId !== -1) targetIndex = byId;
      }
      if (targetIndex < 0 || targetIndex >= paras.items.length) {
        throw new HostApiError(`setWordListFormat: 目标段落 index=${paragraphIndex} 越界`, undefined);
      }

      const para = paras.items[targetIndex];
      const list = para.startNewList();
      if (listType === 'bullet') {
        list.setLevelBullet(level, Word.ListBullet[bulletStyle as keyof typeof Word.ListBullet] ?? Word.ListBullet.solid, undefined);
      } else {
        list.setLevelNumbering(level, Word.ListNumbering[numberStyle as keyof typeof Word.ListNumbering] ?? Word.ListNumbering.arabic, undefined);
      }
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word setWordListFormat 失败', err);
  }
}
```
注意：`Word.ListBullet` 和 `Word.ListNumbering` 是 WordApi 1.3 枚举，使用时加 `as any` 兜底（若 TS 类型不完整，DefinitelyTyped #72801 已记录此已知问题）。

**WORD-08 insertWordComment（新增）：**

word.ts 新增 ToolDef（按 Analog 1a 简单逆向范式，PATTERNS.md L31-101）：
```typescript
const COMMENT_PREFIX = '[Aster] ';
const COMMENT_CONTENT_CAP = 50;

interface InsertWordCommentArgs {
  paragraphIndex: number;
  searchText?: string;    // 批注锚文本（在目标段落内搜索定位）；省略则对整段批注
  commentText: string;    // 批注内容（代码自动加 '[Aster] ' 前缀）
  uniqueLocalId?: string;
}

export const insertWordComment: ToolDef<InsertWordCommentArgs> = {
  name: 'insert_word_comment',
  kind: 'write',
  description: '在 Word 指定段落（或段落内指定文字）插入批注。批注内容会自动加 "[Aster] " 前缀以区分 AI 建议。注意：Word for Web 插入的批注可能需要刷新页面才可见（平台已知限制）。',
  parameters: {
    type: 'object',
    properties: {
      paragraphIndex: { type: 'number', description: '目标段落编号（0-based）' },
      searchText: { type: 'string', description: '批注锚文本（在目标段落内搜索定位，省略则对整段）' },
      commentText: { type: 'string', description: '批注内容（不需加前缀，代码自动添加 "[Aster] "）' },
      uniqueLocalId: { type: 'string', description: '段落唯一 ID（可选，精确消歧）' },
    },
    required: ['paragraphIndex', 'commentText'],
  },
  humanLabel: ({ paragraphIndex, commentText }) =>
    `给第 ${Number(paragraphIndex) + 1} 段插入批注「${String(commentText).slice(0, COMMENT_CONTENT_CAP)}」`,
  async execute({ paragraphIndex, searchText, commentText, uniqueLocalId }, ctx): Promise<ToolResult> {
    const result = await (ctx.adapter as WordAdapter).insertWordComment({
      paragraphIndex, searchText, commentText, uniqueLocalId,
    });
    const reverse: ReverseDescriptor = {
      tool: 'delete_comment_by_id',
      args: { commentId: result.commentId },
    };
    const postState = { kind: 'word_comment' as const, content: { commentId: result.commentId } };
    return {
      ok: true,
      data: { commentId: result.commentId, inserted: true },
      reverse,
      postState,
    };
  },
};
```

WordAdapter.ts 新增 insertWordComment + deleteCommentById 方法：

insertWordComment（写操作）：
```typescript
async insertWordComment(args: Record<string, unknown>): Promise<{ commentId: string }> {
  const paragraphIndex = args.paragraphIndex as number;
  const searchText = args.searchText as string | undefined;
  const commentText = args.commentText as string;
  const uniqueLocalId = args.uniqueLocalId as string | undefined;
  const COMMENT_PREFIX = '[Aster] ';

  const supports = typeof Office !== 'undefined' &&
    Office.context?.requirements?.isSetSupported('WordApi', '1.4') === true;
  if (!supports) {
    throw new HostApiError('当前 Word 版本不支持批注操作（需要 WordApi 1.4）', undefined);
  }

  try {
    return await Word.run(async (ctx) => {
      const paras = ctx.document.body.paragraphs;
      paras.load('items/text,items/uniqueLocalId');
      await ctx.sync();

      let targetIndex = paragraphIndex;
      if (uniqueLocalId) {
        const byId = paras.items.findIndex((p) => (p as unknown as { uniqueLocalId: string }).uniqueLocalId === uniqueLocalId);
        if (byId !== -1) targetIndex = byId;
      }
      if (targetIndex < 0 || targetIndex >= paras.items.length) {
        throw new HostApiError(`insertWordComment: 目标段落 index=${paragraphIndex} 越界`, undefined);
      }

      const para = paras.items[targetIndex];
      let range: Word.Range;
      if (searchText) {
        const results = para.search(searchText, { matchCase: false, matchWholeWord: false });
        results.load('items');
        await ctx.sync();
        if (!results.items.length) {
          throw new HostApiError(`insertWordComment: 在目标段落中找不到 searchText="${searchText}"`, undefined);
        }
        range = results.items[0];
      } else {
        range = para.getRange();
      }

      const fullContent = `${COMMENT_PREFIX}${commentText}`;
      const comment = range.insertComment(fullContent);
      comment.load('id');
      await ctx.sync();

      // 写后回读验证（R3）
      const commentId = comment.id as string | undefined;
      if (!commentId) {
        throw new HostApiError('insertWordComment: 批注插入后 id 为空（疑似 Word for Web 静默失败）', undefined);
      }
      return { commentId };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word insertWordComment 失败', err);
  }
}
```

deleteCommentById（inverse 方法，D-17：第一行解包 Record 对象）：
```typescript
async deleteCommentById(args: Record<string, unknown>): Promise<void> {
  // D-17: 第一行解包，不用位置参
  const commentId = args.commentId as string;

  try {
    await Word.run(async (ctx) => {
      // ⚠️ BLOCKER 2 钉死路径：读 ctx.document.body.comments（与 Plan 01 mockWordRich 挂载路径字面量一致）
      const comments = ctx.document.body.comments;
      comments.load('items/id');
      await ctx.sync();

      const target = comments.items.find((c) => (c as unknown as { id: string }).id === commentId);
      if (!target) {
        throw new HostApiError(`deleteCommentById: 找不到 comment id="${commentId}"`, undefined);
      }
      target.delete();
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word deleteCommentById 失败', err);
  }
}
```

⚠️ **路径钉死（BLOCKER 2）：** `ctx.document.body.comments` 是 WordApi 1.4 的 `Word.Body` 上的 comments 集合。**本 plan 与 Plan 01 mockWordRich 必须用同一路径字面量 `ctx.document.body.comments`**——Plan 01 的 mock 已把 comments 挂在 `ctx.document.body.comments`（不是 ctx.document 顶层）。**不要**改读 `ctx.document.comments` 或 `getCommentRanges()`。若真机 `@types/office-js` 类型上 `body.comments` 报类型错误，用 `(ctx.document.body as unknown as { comments: Word.CommentCollection }).comments` 兜底访问，但**运行时读取路径字面量保持 `body.comments` 不变**（mock 与真机一致）。

**index.ts 注册（追加 2 个新工具）：**
在 `setWordParagraphFormat, // Phase 9 WORD-01/WORD-02` 附近的 wordWriteTools 数组中，在 `batchWrite` 之前追加：
```typescript
    setWordListFormat, // Phase 27 WORD-07
    insertWordComment, // Phase 27 WORD-08
```
并在文件顶部 import 区域加对应 import（按现有 setWordCharacterFormat 的 import 模式）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WORD-06 折入 setCharacterFormat + WORD-07/08 adapter 方法</name>
  <read_first>
    - src/adapters/WordAdapter.ts（全文，重点 L474-555 setCharacterFormat、L571-619 restoreRangeFont、L1148-1183 deleteTableByMarker）
    - .planning/phases/27-word-tools/27-PATTERNS.md（Analog 2a/2b/2d，精确 L474 loadStr 格式）
    - .planning/phases/27-word-tools/27-RESEARCH.md（WORD-06/07/08 API 裁定、Pitfall 3 highlightColor null、Pitfall 1 lists.getById bug）
    - src/agent/operationLog.integration.test.ts（L256-304 mockWordRich 扩展后结构，Plan 01 已修改——确认 comments 挂在 ctx.document.body.comments）
    - .planning/phases/27-word-tools/27-01-SUMMARY.md（Plan 01 钉死的 mock 路径契约，尤其 comments = ctx.document.body.comments）
  </read_first>
  <behavior>
    - WORD-06：`setCharacterFormat` loadStr 加 `highlightColor`，before-image 加 `highlightColor: f.highlightColor`，only-if-present 加 `highlightColor`，`restoreRangeFont` 恢复块加 highlightColor 写回（null 不跳过）
    - WORD-07：`setWordListFormat(args: Record<string, unknown>)` 正常执行 startNewList，isSetSupported 门控，undo 为 noop（由 word.ts 层决定）
    - WORD-08：`insertWordComment(args: Record<string, unknown>)` 插入 `[Aster] ` 前缀批注，写后回读 comment.id；`deleteCommentById(args: Record<string, unknown>)` 读 **ctx.document.body.comments**、遍历找 id 删除
  </behavior>
  <action>
按上述 interfaces 部分的完整代码在 WordAdapter.ts 做 5 处修改：
1. `setCharacterFormat` 的 loadStr 末尾加 `,items/font/highlightColor`（精确定位当前 load 字符串行）
2. before-image 块加 `highlightColor: f.highlightColor`
3. only-if-present 加 `if (font.highlightColor !== undefined) f.highlightColor = font.highlightColor as string | null;`
4. `restoreRangeFont` 恢复块末尾加 `if (before.highlightColor !== undefined) f.highlightColor = before.highlightColor as string | null;`
5. 在 `deleteTableByMarker` 方法之后追加 `setWordListFormat`、`insertWordComment`、`deleteCommentById` 三个新方法

重要：
- WordAdapter.ts 已有 `import * as Word from '...'` 之类的类型引用；若 Word.ListBullet / Word.ListNumbering 枚举访问产生 TS 错误（类型不完整），加 `as unknown as Word.ListBullet` 或 `(Word as any).ListBullet` 兜底（DefinitelyTyped #72801 已知问题）。
- **`deleteCommentById` 读取路径钉死 `ctx.document.body.comments`（BLOCKER 2）**：与 Plan 01 mockWordRich 挂载路径字面量完全一致。**不要**改成 `ctx.document.comments` 或 `getCommentRanges()`。若 `@types/office-js` 上 `body.comments` 报类型错，用 `(ctx.document.body as unknown as { comments: Word.CommentCollection }).comments` 兜底，但运行时读取路径仍是 `body.comments`。
- 所有新 adapter 方法：第一行解包 args（D-17），`Word.run` 闭包内操作，`HostApiError` 封装错误（不暴露 stack）。
  </action>
  <verify>
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# TypeScript 编译
npx tsc --noEmit
# 确认 WORD-06 折入
grep -c "highlightColor" src/adapters/WordAdapter.ts
# 期望 >= 4（loadStr + before-image + only-if-present + restoreRangeFont 恢复）
# 确认新方法存在
grep -c "setWordListFormat\|insertWordComment\|deleteCommentById" src/adapters/WordAdapter.ts
# 期望 3
# 确认 deleteCommentById 读 body.comments（BLOCKER 2 路径自洽）
grep -c "body.comments\|body as unknown as { comments" src/adapters/WordAdapter.ts
# 期望 >= 1
# 运行 integration test（WORD-06 already-green 保持绿；WORD-08 现在变绿）
npm run test -- --run src/agent/operationLog.integration.test.ts
```
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' src/adapters/WordAdapter.ts | grep -c 'highlightColor'` 输出 `>= 4`（非注释行）
    - `grep -c 'async setWordListFormat\|async insertWordComment\|async deleteCommentById' src/adapters/WordAdapter.ts` 输出 `3`
    - `grep -c 'body.comments' src/adapters/WordAdapter.ts` 输出 `>= 1`（deleteCommentById 读 ctx.document.body.comments，与 Plan 01 mock 路径一致——BLOCKER 2 自洽）
    - `npx tsc --noEmit` 退出 0（无 TS 错误）
    - `npm run test -- --run src/agent/operationLog.integration.test.ts`：insert_word_comment 守门用例状态 `rolled_back`（deleteCommentFn 被调用 1 次）；set_word_list_format 守门用例状态 `skipped_error`（noop 正确路径）；set_word_character_format 原有用例仍 `rolled_back`
  </acceptance_criteria>
  <done>WORD-06 折入完成（highlightColor before-image + only-if-present + restore 写回）；WORD-07 adapter 方法执行列表操作；WORD-08 adapter insert + delete by id（读 body.comments）；integration test insert_word_comment 变绿（deleteCommentById 与 Plan 01 mock 路径自洽）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WORD-07/08 ToolDef + index.ts 注册 + WORD-06 null 写回断言</name>
  <read_first>
    - src/agent/tools/write/word.ts（全文，重点 L231-290 setWordCharacterFormat ToolDef 范式、L635-666 replaceSelection noop+gate 范式、interface 声明区）
    - src/agent/tools/index.ts（L275-293 buildToolsForHost('word') wordWriteTools 数组）
    - src/agent/operationLog.integration.test.ts（L398-416 set_word_character_format 守门用例——本 task 要扩展它的 highlightColor null 写回断言）
    - .planning/phases/27-word-tools/27-PATTERNS.md（Analog 1a/1b、Analog 6a）
    - .planning/phases/27-word-tools/27-RESEARCH.md（WORD-07 noop+gate 完整 interface 形状、WORD-08 参数形状、Pitfall 3 highlightColor null 写回）
    - .planning/phases/27-word-tools/27-VALIDATION.md（§WORD-06 要求 null 写回专属断言）
  </read_first>
  <behavior>
    - setWordListFormat ToolDef：noop_inverse reverse，humanLabel 中文
    - insertWordComment ToolDef：delete_comment_by_id reverse，commentText 经 adapter 加 [Aster] 前缀
    - WORD-06 null 写回（WARNING 2 修复）：set_word_character_format 守门用例的 mockWordRich 段落 font 含 highlightColor:'#FFFF00'（before），inverse 写回 highlightColor:null，replayUndoSingle → rolled_back 且 null 被正确写回（不被 null-guard 跳过）
  </behavior>
  <action>
**改 src/agent/tools/write/word.ts：**

1. 在现有 interface 声明区（`SetWordCharacterFormatArgs` 附近）的 font 内嵌类型追加 `highlightColor?: string | null;`（WORD-06 折入，让 TS 类型完整）

2. 在文件末尾（或 `replaceSelection` ToolDef 之后）追加两个新 interface + ToolDef：

`SetWordListFormatArgs` 接口 + `setWordListFormat` ToolDef（按 interfaces 部分完整代码）：
- name: `'set_word_list_format'`
- kind: `'write'`
- humanLabel: `({ paragraphIndex, listType }) => \`将第 \${Number(paragraphIndex) + 1} 段改为\${listType === 'bullet' ? '项目符号' : '编号'}列表\``
- execute：调用 `(ctx.adapter as WordAdapter).setWordListFormat({...})`，reverse = `{ tool: 'noop_inverse', args: { reason: '...' } }`，postState.kind = `'word_list_format' as const`

`InsertWordCommentArgs` 接口 + `insertWordComment` ToolDef（按 interfaces 部分完整代码）：
- name: `'insert_word_comment'`
- kind: `'write'`
- humanLabel: `({ paragraphIndex, commentText }) => \`给第 \${Number(paragraphIndex) + 1} 段插入批注「\${String(commentText).slice(0, COMMENT_CONTENT_CAP)}」\``
- execute：调用 `(ctx.adapter as WordAdapter).insertWordComment({...})`，reverse = `{ tool: 'delete_comment_by_id', args: { commentId: result.commentId } }`，postState.kind = `'word_comment' as const`

在 word.ts 顶部常量区定义 `const COMMENT_PREFIX = '[Aster] ';`（虽然 word.ts 层不直接用，但保持文档性；adapter 层的 `COMMENT_PREFIX` 常量在 adapter 文件内也可定义为私有常量）

**改 src/agent/tools/index.ts：**

1. 在文件顶部 import 区域追加：
```typescript
import { setWordListFormat, insertWordComment } from './write/word.js';
```
（按现有 import 模式，使用 .js 扩展名如已有其他 import 所示）

2. 在 wordWriteTools 数组的 `batchWrite` 行前追加：
```typescript
    setWordListFormat, // Phase 27 WORD-07
    insertWordComment, // Phase 27 WORD-08
```

**改 src/agent/operationLog.integration.test.ts —— WORD-06 highlightColor null 写回专属断言（WARNING 2 修复，VALIDATION.md §WORD-06 要求）：**

现有 Phase 9 的 `set_word_character_format` 守门用例（L398-416）的 before 对象不含 highlightColor。本 task 扩展它，让 WORD-06 折入的 null 写回有 integration 覆盖（Pitfall 3：null=移除高亮，不能被 null-guard 跳过）：

1. 在该用例的 `mockWordRich` 段落 font mock 里加 `highlightColor: '#FFFF00'`（before 态有黄色高亮）。具体：mockWordRich 的 paraItems font 默认对象（Plan 01 / 既有为 `{ bold: false, italic: false, underline: 'None', size: 12, color: '#000000', name: 'Calibri' }`）追加 `highlightColor: '#FFFF00'`，或在该用例内单独构造带 highlightColor 的段落 mock。
2. 在该用例的 `reverse.args.before` 对象里加 `highlightColor: null`（inverse 目标 = 移除高亮）。
3. 用 spy 验证 null 被写回到 font：可在 paraItems 的 font 对象上用 getter/setter spy 或断言 `paraItems[0].font.highlightColor === null`（restoreRangeFont 执行后）。最低限度：断言 `replayUndoSingle` → `rolled_back`，且**不抛错**（证明 `if (before.highlightColor !== undefined)` 分支对 null 走写回而非跳过）。
4. 若改 mockWordRich 默认 font 影响其它用例，则在该 set_word_character_format 用例内**局部覆盖** font.highlightColor（不动全局默认），避免回归。
  </action>
  <verify>
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# TypeScript 编译
npx tsc --noEmit
# 确认 ToolDef 存在
grep -c "'set_word_list_format'\|'insert_word_comment'" src/agent/tools/write/word.ts
# 期望 2（name 字段各一个）
# 确认 index.ts 注册
grep -c "setWordListFormat\|insertWordComment" src/agent/tools/index.ts
# 期望 >= 2
# 确认 WORD-06 null 写回断言（WARNING 2）
grep -c "highlightColor" src/agent/operationLog.integration.test.ts
# 期望 >= 2（before '#FFFF00' + reverse before null）
# 运行全套测试（确认新工具注册期校验通过 + WORD-06 null 写回用例绿）
npm run test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts
```
  </verify>
  <acceptance_criteria>
    - `grep -c "name: 'set_word_list_format'" src/agent/tools/write/word.ts` 输出 `1`
    - `grep -c "name: 'insert_word_comment'" src/agent/tools/write/word.ts` 输出 `1`
    - `grep -c "setWordListFormat\|insertWordComment" src/agent/tools/index.ts` 输出 `>= 2`（import + 数组使用）
    - `grep -c "highlightColor" src/agent/operationLog.integration.test.ts` 输出 `>= 2`（set_word_character_format 用例 before '#FFFF00' + reverse.args.before null —— WARNING 2 null 写回覆盖）
    - `npx tsc --noEmit` 退出 0
    - `npm run test -- --run src/agent/contract.test.ts` 退出 0（D-17 守门通过）
    - `npm run test -- --run src/agent/operationLog.integration.test.ts`：`set_word_list_format` → `skipped_error`，`insert_word_comment` → `rolled_back`，`set_word_character_format` → `rolled_back`（含 highlightColor:'#FFFF00' → null 写回断言通过，证明 null 不被 null-guard 跳过）
  </acceptance_criteria>
  <done>word.ts 含 setWordListFormat + insertWordComment ToolDef；index.ts 注册两个工具；assertWriteToolRegisterable humanLabel 校验通过；contract.test.ts 守门绿；set_word_character_format 守门用例新增 highlightColor null 写回断言（WARNING 2 闭合）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → adapter（批注内容） | insertWordComment 的 commentText 来自 LLM，写入 Word 文档前自动加 `[Aster] ` 前缀（G-A 透明性要求）|
| adapter → Word.run 闭包 | proxy 对象（para、comment、list）不出 Word.run 闭包（A-06 硬约束）|
| executeReverse → adapter.deleteCommentById | 路由通过 switch case，args 为 Record 对象（D-17），防位置参签名错配；读 ctx.document.body.comments（与 mock 路径一致）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-1 | Tampering | insertWordComment 批注内容 | mitigate | 批注内容自动加 `[Aster] ` 前缀（G-A 决策），区分 AI 建议 vs 用户批注，防 AI 建议匿名混入用户名下 |
| T-27-02-2 | Elevation of Privilege | deleteCommentById 定位 | mitigate | 读 ctx.document.body.comments 遍历精确匹配 id，找不到抛 HostApiError → skipped_error，不误删其他批注 |
| T-27-02-3 | Spoofing | comment.authorName | accept | Office.js 无 API 改写作者，作者 = 当前登录账号（Office 平台强制）；`[Aster] ` 内容标记是唯一可控的诚实机制 |
| T-27-02-4 | Denial of Service | setWordListFormat（WordApi 1.3） | mitigate | isSetSupported 门控，不支持时抛 HostApiError（UNSUPPORTED），不静默失败 |
| T-27-02-5 | Information Disclosure | HostApiError | mitigate | `new HostApiError(message, err)` 不存 stack（防调用链泄漏），仅传 message 字符串 |
</threat_model>

<verification>
完整验证命令：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. TS 编译
npx tsc --noEmit
# 2. 单元测试 + 合约守门
npm run test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts
# 期望：insert_word_comment 守门 rolled_back（deleteCommentById 读 body.comments，与 mock 路径一致）；
#       set_word_list_format 守门 skipped_error；set_word_character_format rolled_back（含 highlightColor null 写回断言）
# 3. 全套测试
npm run test -- --run
```
</verification>

<success_criteria>
- WORD-06：setCharacterFormat font 扩展 highlightColor；restoreRangeFont null 写回正确；**integration 专属断言验 highlightColor:'#FFFF00' → null 写回不被 null-guard 跳过（WARNING 2 闭合）**
- WORD-07：setWordListFormat adapter 方法（write only，noop_inverse）；word.ts ToolDef humanLabel 中文
- WORD-08：insertWordComment（`[Aster] ` 前缀，comment.id 写后回读）+ deleteCommentById（Record 对象解包，读 **ctx.document.body.comments**，id 精确匹配——与 Plan 01 mock 路径自洽 BLOCKER 2）
- 两个工具在 index.ts 注册，assertWriteToolRegisterable 无抛
- integration.test.ts：insert_word_comment 用例 rolled_back（绿），set_word_list_format 用例 skipped_error（noop 正确），set_word_character_format rolled_back（含 null 写回）
- `npm run test -- --run` 全套通过（含 contract.test.ts D-17 守门）
</success_criteria>

<output>
完成后创建 `.planning/phases/27-word-tools/27-02-SUMMARY.md`，记录：
- WORD-06 折入：WordAdapter.ts 4 处改动行号 + SetWordCharacterFormatArgs font 类型扩展 + **highlightColor null 写回 integration 断言（WARNING 2）**
- WORD-07：setWordListFormat adapter 行号 + noop_inverse 裁定原因（GitHub #6525）
- WORD-08：insertWordComment + deleteCommentById 方法行号，COMMENT_PREFIX = '[Aster] '，**deleteCommentById 读取路径 = ctx.document.body.comments（与 Plan 01 mock 自洽，BLOCKER 2）**
- index.ts 注册行号
- integration test 用例状态（insert_word_comment 绿、set_word_list_format 正确 skipped_error、set_word_character_format 绿含 null 写回断言）
</output>

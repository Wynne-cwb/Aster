---
phase: 27-word-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/operationLog.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - WORD-07
  - WORD-08
  - WORD-09
  - WORD-10

must_haves:
  truths:
    - "operationLog.ts 的 PostStateSnapshot.kind union 包含 4 个新 kind（word_list_format / word_comment / word_header_footer / word_table_cell）"
    - "operationLog.ts 的 DocumentAdapterForReplay 接口声明 3 个新 inverse 方法（deleteCommentById / restoreWordHeaderFooter / restoreTableCell）"
    - "operationLog.ts 的 executeReverse switch 包含 3 个新 case（delete_comment_by_id / restore_word_header_footer / restore_table_cell）"
    - "contract.test.ts 的 CONTRACT[] 包含 4 个新 Phase 27 行（integrationTest: true），PhaseNum 含 27"
    - "operationLog.integration.test.ts 包含 4 个新守门用例，4 个工具名字符串字面量全部出现在文件中"
    - "D-17 硬卡：contract.test.ts 全套通过（fs.readFileSync 断言工具名出现在 integration.test）"
    - ".planning/phases/08-foundation-a-f/CONTRACT.md 包含 Phase 27 段落（5 条工具行）"
    - "CONTRACT.md Phase 27 段含 WORD-06 折入说明行（set_word_character_format，复用 restore_range_font，沿用既有 integration 守门）"
    - "mockWordRich comments mock 挂载路径 = ctx.document.body.comments（与 Plan 02 deleteCommentById 读取路径字面量完全一致——跨 plan mock 契约自洽）"
  artifacts:
    - path: "src/agent/operationLog.ts"
      provides: "Phase 27 骨架：kind union + 接口声明 + switch case"
      contains: "word_list_format"
    - path: "src/agent/contract.test.ts"
      provides: "Phase 27 CONTRACT 行 + D-17 守门"
      contains: "set_word_list_format"
    - path: "src/agent/operationLog.integration.test.ts"
      provides: "4 个守门用例"
      contains: "insert_word_comment"
    - path: ".planning/phases/08-foundation-a-f/CONTRACT.md"
      provides: "人读合约表 Phase 27 段"
      contains: "Phase 27"
  key_links:
    - from: "contract.test.ts CONTRACT[]"
      to: "operationLog.integration.test.ts"
      via: "fs.readFileSync D-17 硬断言"
      pattern: "set_word_list_format.*integration"
    - from: "operationLog.ts executeReverse"
      to: "adapter.deleteCommentById"
      via: "case 'delete_comment_by_id'"
      pattern: "case 'delete_comment_by_id'"
    - from: "mockWordRich"
      to: "ctx.document.body.comments"
      via: "comments mock 挂在 body 上（与 Plan 02 deleteCommentById 读取路径一致）"
      pattern: "body.*comments"
---

<objective>
本计划完成 Phase 27 的**合约骨架接线**：在 operationLog.ts 注册 4 个新 PostStateSnapshot kind、3 个新接口方法声明、3 个新 executeReverse switch case；在 contract.test.ts 追加 4 条 Phase 27 CONTRACT 行（integrationTest: true）；在 operationLog.integration.test.ts 追加 4 个守门用例（此时 adapter 方法尚未实现，用例结果为 RED — 预期的「先红后绿」TDD 骨架）；更新 CONTRACT.md 人读表。

Purpose: 合约骨架先行——让 D-17 硬门（fs.readFileSync 断言）、executeReverse case 路由、接口声明全部到位，后续两个 plan 的 adapter 实现可直接让对应用例变绿，而不用在实现时再补接线。
Output: operationLog.ts（扩展 3 处）、contract.test.ts（追加 4 行 + PhaseNum 联合类型）、operationLog.integration.test.ts（追加 4 个用例 + 扩展 mockWordRich）、CONTRACT.md（Phase 27 段）

⚠️ **Wave 1 RED 状态声明（BLOCKER 1 修复）：** 本 plan 是 wave 1，先建 integration.test.ts 的 4 个守门用例 + CONTRACT 行，但 adapter 方法在 wave 2/3 才实现。因此 **wave 1 结束时 operationLog.integration.test.ts 新增 4 个用例预期为 RED**：3 个简单逆向用例（insert_word_comment / set_word_header_footer / edit_table_cell）因 adapter 方法未实现 → `skipped_error`；1 个 noop+gate 用例（set_word_list_format）→ `skipped_error`（noop_inverse 正确抛错）。**Wave 1 验收 gate 只验 `npm run test -- --run src/agent/contract.test.ts` 退出 0（D-17 守门）**，integration 全绿须等 Wave 2（insert_word_comment）+ Wave 3（set_word_header_footer / edit_table_cell）。**不要在 wave 1 验收时跑 integration 全套并要求它全绿**——那是 Wave 2/3 的职责。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-word-tools/27-CONTEXT.md
@.planning/phases/27-word-tools/27-RESEARCH.md
@.planning/phases/27-word-tools/27-PATTERNS.md

<interfaces>
<!-- 关键接口与 analog，执行者直接使用，无需搜索 -->

从 src/agent/operationLog.ts（L34-49）— PostStateSnapshot kind union 当前末尾：
```typescript
// 当前末尾（L46-48）：
    | 'ppt_layout'
    // Phase 11 新增：batch 整体快照 kind
    | 'batch';
```
Phase 27 在 `| 'batch'` 前插入：
```typescript
    // Phase 27 新增：Word 工具补全 4 个 kind（readTargetState/isTargetStateConsistent 走保守 default）
    | 'word_list_format' | 'word_comment' | 'word_header_footer' | 'word_table_cell'
```

从 src/agent/operationLog.ts（L164-171）— DocumentAdapterForReplay 当前末尾：
```typescript
  /** PPT inverse：按 index+ID 双定位删除复制的幻灯片（copy_slide → delete_slide_by_index）*/
  deleteSlideByIndex?: (args: Record<string, unknown>) => Promise<void>;
  /** Phase 11：batch_reverse 单闭包逆序撤销 */
  executeBatchReverse?: (ops: Array<{ tool: string; args: Record<string, unknown>; postState?: PostStateSnapshot }>) => Promise<void>;
}
```
Phase 27 在 `executeBatchReverse` 前插入（按 PATTERNS.md Analog 3b）：
```typescript
  // ─── Phase 27 Word 工具补全 inverse 方法 ───
  /** Word inverse：按 comment id 删除批注（insert_word_comment） */
  deleteCommentById?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：还原页眉/页脚文字（set_word_header_footer） */
  restoreWordHeaderFooter?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：还原表格单元格内容（edit_table_cell） */
  restoreTableCell?: (args: Record<string, unknown>) => Promise<void>;
```
（注：WORD-06 折入复用 restoreRangeFont，WORD-07 用 noop_inverse，无需新增接口方法）

从 src/agent/operationLog.ts（L536-543）— executeReverse switch 末尾（noop_inverse 前）：
```typescript
    case 'noop_inverse':
      throw new Error(`noop_inverse: 此操作不支持自动回滚（${String(reverse.args.reason ?? '')}）`);
    default:
      throw new Error(`未知 reverse tool: ${reverse.tool}`);
```
Phase 27 在 `case 'noop_inverse'` 前插入（按 PATTERNS.md Analog 3c）：
```typescript
    // ─── Phase 27 Word 工具补全 3 个新 case ───
    case 'delete_comment_by_id':
      if (!adapter.deleteCommentById) {
        throw new Error(`adapter 未实现 deleteCommentById（tool=${reverse.tool}）`);
      }
      await adapter.deleteCommentById(reverse.args);
      break;
    case 'restore_word_header_footer':
      if (!adapter.restoreWordHeaderFooter) {
        throw new Error(`adapter 未实现 restoreWordHeaderFooter（tool=${reverse.tool}）`);
      }
      await adapter.restoreWordHeaderFooter(reverse.args);
      break;
    case 'restore_table_cell':
      if (!adapter.restoreTableCell) {
        throw new Error(`adapter 未实现 restoreTableCell（tool=${reverse.tool}）`);
      }
      await adapter.restoreTableCell(reverse.args);
      break;
```

从 src/agent/contract.test.ts（L18）— PhaseNum 类型（当前）：
```typescript
type PhaseNum = 9 | 10 | 11 | 23;
```
改为：
```typescript
type PhaseNum = 9 | 10 | 11 | 23 | 27;
```

从 src/agent/contract.test.ts（L63）— CONTRACT 数组当前末尾（Phase 23 行之后）：
```typescript
  { toolName: 'apply_slide_layout', host: 'ppt', undoType: '简单逆向', reverseTool: 'delete_slide_by_index', phase: 23, integrationTest: true },
];
```
Phase 27 在 `];` 前追加（按 PATTERNS.md Analog 4a）：
```typescript
  // ─── Phase 27 Word 工具补全 ───
  { toolName: 'set_word_list_format', host: 'word', undoType: 'noop+gate', reverseTool: 'noop_inverse', phase: 27, integrationTest: true },
  { toolName: 'insert_word_comment', host: 'word', undoType: '简单逆向', reverseTool: 'delete_comment_by_id', phase: 27, integrationTest: true },
  { toolName: 'set_word_header_footer', host: 'word', undoType: '简单逆向', reverseTool: 'restore_word_header_footer', phase: 27, integrationTest: true },
  { toolName: 'edit_table_cell', host: 'word', undoType: '简单逆向', reverseTool: 'restore_table_cell', phase: 27, integrationTest: true },
```

从 src/agent/operationLog.integration.test.ts（L256-304）— mockWordRich 当前签名：
```typescript
function mockWordRich(opts?: {
  paragraphTexts?: string[];
  tables?: Array<{ rowCount: number; columnCount: number; values: string[][]; delete: ReturnType<typeof vi.fn> }>;
}): { paraItems: ...; tableItems: ...; }
```
Phase 27 需扩展 opts 加两个可选字段（按 PATTERNS.md Analog 5a）：
```typescript
  comments?: Array<{ id: string; delete: ReturnType<typeof vi.fn> }>;
  sectionHeader?: { text: string; insertText: ReturnType<typeof vi.fn> };
```

⚠️ **跨 plan 契约钉死（BLOCKER 2 根因修复）：comments mock 必须挂到 `ctx.document.body.comments`**（与 Office.js `Word.run` 里 `context.document.body` 访问范式一致；Plan 02 的 `deleteCommentById` 实现也读 **`ctx.document.body.comments`** —— 路径字面量两 plan 必须完全相同，否则运行时 `body.comments` 为 undefined，insert_word_comment 守门用例会跌成 skipped_error 而非 rolled_back）。

在 `ctx.document.body` 对象内增加 `comments`（与 paragraphs/tables 同级挂在 **body** 上，**不是** ctx.document 顶层）：
```typescript
            comments: { load: vi.fn(), items: opts?.comments ?? [] },
```

在 `ctx.document` 顶层（与 body 同级）增加 `sections`：
```typescript
          sections: {
            load: vi.fn(),
            getFirst: vi.fn(() => ({
              load: vi.fn(),
              getHeader: vi.fn((_type: string) => ({
                load: vi.fn(),
                text: opts?.sectionHeader?.text ?? '旧页眉',
                insertText: opts?.sectionHeader?.insertText ?? vi.fn(),
              })),
              getFooter: vi.fn((_type: string) => ({
                load: vi.fn(),
                text: opts?.sectionHeader?.text ?? '旧页脚',
                insertText: opts?.sectionHeader?.insertText ?? vi.fn(),
              })),
            })),
            // sections.items[i]：restoreWordHeaderFooter 走 sections.items[sectionIndex]，结构同 getFirst 返回值
            items: [{
              load: vi.fn(),
              getHeader: vi.fn((_type: string) => ({
                load: vi.fn(),
                text: opts?.sectionHeader?.text ?? '旧页眉',
                insertText: opts?.sectionHeader?.insertText ?? vi.fn(),
              })),
              getFooter: vi.fn((_type: string) => ({
                load: vi.fn(),
                text: opts?.sectionHeader?.text ?? '旧页脚',
                insertText: opts?.sectionHeader?.insertText ?? vi.fn(),
              })),
            }],
          },
```

⚠️ **table 单元格 mock 完整挂载（BLOCKER 3 根因修复）：**
mockWordRich 函数体内必须对 **`opts?.tables ?? []` 的每个 table 对象**追加 `getCell` 与 `getCellOrNullObject` 方法（否则 Plan 03 的 `editTableCell`/`restoreTableCell` 调用 `table.getCellOrNullObject(...)` 会报 `is not a function`）。**用下面这段 `.map(...)` 版本替换原 `const tableItems = opts?.tables ?? [];` 行**（getCell/getCellOrNullObject 必须挂在每个 table item 上，不能只定义顶层常量）：
```typescript
  // cell mock：含 isNullObject: false（Plan 03 的 if (cell.isNullObject) 检查需走明确 false，不是 undefined）
  const tableCellMockDefault = {
    load: vi.fn(),
    value: '原内容',
    isNullObject: false,
    body: { insertText: vi.fn() },
  };
  const tableItems = (opts?.tables ?? []).map((t) => ({
    ...t,
    getCell: vi.fn(() => tableCellMockDefault),
    getCellOrNullObject: vi.fn(() => tableCellMockDefault),
  }));
```

从 PATTERNS.md Analog 5b — 4 个守门用例完整结构（照此逐字实现）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 operationLog.ts — 新 kind + 接口声明 + switch case</name>
  <read_first>
    - src/agent/operationLog.ts（全文，重点 L34-49 PostStateSnapshot、L102-171 接口声明、L382-543 switch case）
    - .planning/phases/27-word-tools/27-PATTERNS.md（Analog 3a/3b/3c/3d，精确行号）
    - .planning/phases/27-word-tools/27-RESEARCH.md（合约接线清单）
  </read_first>
  <action>
在 src/agent/operationLog.ts 做三处精确修改：

**修改 1：PostStateSnapshot.kind union（L46 前，在 `| 'ppt_layout'` 行之后、`| 'batch'` 行之前插入）**
插入：
```
    // Phase 27 新增：Word 工具补全 4 个 kind（readTargetState/isTargetStateConsistent 走保守 default）
    | 'word_list_format' | 'word_comment' | 'word_header_footer' | 'word_table_cell'
```
注意：`readTargetState` 和 `isTargetStateConsistent` 中的 `default` 分支（分别返回 `undefined` 和 `true`）已覆盖这 4 个新 kind，**绝不新增 case**（D-03 硬约束，盲加 read 比对规则会误判手改 → undo 静默全挂）。

**修改 2：DocumentAdapterForReplay 接口（在 `executeBatchReverse` 声明之前插入，即当前 L168 左右）**
插入（WORD-06 折入复用 restoreRangeFont 已有声明，WORD-07 noop_inverse 无需声明）：
```typescript
  // ─── Phase 27 Word 工具补全 inverse 方法 ───
  /** Word inverse：按 comment id 删除批注（insert_word_comment） */
  deleteCommentById?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：还原页眉/页脚文字（set_word_header_footer） */
  restoreWordHeaderFooter?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：还原表格单元格内容（edit_table_cell） */
  restoreTableCell?: (args: Record<string, unknown>) => Promise<void>;
```

**修改 3：executeReverse switch（在 `case 'noop_inverse':` 之前插入）**
插入（按 PATTERNS.md Analog 3c 逐字）：
```typescript
    // ─── Phase 27 Word 工具补全 3 个新 case ───
    case 'delete_comment_by_id':
      if (!adapter.deleteCommentById) {
        throw new Error(`adapter 未实现 deleteCommentById（tool=${reverse.tool}）`);
      }
      await adapter.deleteCommentById(reverse.args);
      break;
    case 'restore_word_header_footer':
      if (!adapter.restoreWordHeaderFooter) {
        throw new Error(`adapter 未实现 restoreWordHeaderFooter（tool=${reverse.tool}）`);
      }
      await adapter.restoreWordHeaderFooter(reverse.args);
      break;
    case 'restore_table_cell':
      if (!adapter.restoreTableCell) {
        throw new Error(`adapter 未实现 restoreTableCell（tool=${reverse.tool}）`);
      }
      await adapter.restoreTableCell(reverse.args);
      break;
```
  </action>
  <verify>
```bash
grep -c "word_list_format" src/agent/operationLog.ts
# 期望：1（kind union 中）
grep -c "deleteCommentById" src/agent/operationLog.ts
# 期望：2（接口声明 + switch case）
grep -c "restoreWordHeaderFooter" src/agent/operationLog.ts
# 期望：2（接口声明 + switch case）
grep -c "restoreTableCell" src/agent/operationLog.ts
# 期望：2（接口声明 + switch case）
grep -c "word_table_cell" src/agent/operationLog.ts
# 期望：1（kind union）
```
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' src/agent/operationLog.ts | grep -c "word_list_format"` 输出 `1`（在 kind union 中，非注释行）
    - `grep -c "case 'delete_comment_by_id'" src/agent/operationLog.ts` 输出 `1`
    - `grep -c "case 'restore_word_header_footer'" src/agent/operationLog.ts` 输出 `1`
    - `grep -c "case 'restore_table_cell'" src/agent/operationLog.ts` 输出 `1`
    - TypeScript 编译无错误：`npx tsc --noEmit`
  </acceptance_criteria>
  <done>operationLog.ts 已包含 4 个新 kind、3 个新接口声明、3 个新 switch case，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 更新 contract.test.ts + CONTRACT.md + integration.test.ts 骨架</name>
  <read_first>
    - src/agent/contract.test.ts（全文，重点 L18 PhaseNum、L33-64 CONTRACT 数组、L144 长度守门）
    - src/agent/operationLog.integration.test.ts（L256-304 mockWordRich、L398-503 Phase 9 五例）
    - .planning/phases/27-word-tools/27-PATTERNS.md（Analog 4a/4b/5a/5b，精确代码模板）
    - .planning/phases/08-foundation-a-f/CONTRACT.md（当前末尾结构，Phase 27 段加在末尾）
  </read_first>
  <action>
**改 contract.test.ts（3 处）：**

1. L18：PhaseNum 类型追加 `| 27`：
```typescript
type PhaseNum = 9 | 10 | 11 | 23 | 27;
```

2. CONTRACT[] 末尾（apply_slide_layout 行后 `];` 前）追加 4 行：
```typescript
  // ─── Phase 27 Word 工具补全 ───
  { toolName: 'set_word_list_format', host: 'word', undoType: 'noop+gate', reverseTool: 'noop_inverse', phase: 27, integrationTest: true },
  { toolName: 'insert_word_comment', host: 'word', undoType: '简单逆向', reverseTool: 'delete_comment_by_id', phase: 27, integrationTest: true },
  { toolName: 'set_word_header_footer', host: 'word', undoType: '简单逆向', reverseTool: 'restore_word_header_footer', phase: 27, integrationTest: true },
  { toolName: 'edit_table_cell', host: 'word', undoType: '简单逆向', reverseTool: 'restore_table_cell', phase: 27, integrationTest: true },
```

3. L144 守门注释更新：原文 `>= 24（Phase 9/10 全部工具 23 行 + Phase 11 batch_write 1 行）` 改为 `>= 28（Phase 27 新增 4 行后总计 30 行，守门放宽为 >= 28）`。守门断言数值保持 `>= 24` 仍通过（新增后 30 >= 24），无需改数值。只更新注释说明新总数。

**改 operationLog.integration.test.ts（2 处）：**

1. 扩展 `mockWordRich` 函数（按上方 interfaces 部分的钉死路径——BLOCKER 2/3 修复）：
   - opts 参数类型加 `comments?: Array<{ id: string; delete: ReturnType<typeof vi.fn> }>` 和 `sectionHeader?: { text: string; insertText: ReturnType<typeof vi.fn> }`
   - **comments mock 挂在 `ctx.document.body.comments`（与 paragraphs/tables 同级在 body 上）：** `comments: { load: vi.fn(), items: opts?.comments ?? [] }`（⚠️ 必须在 body 内，不能挂 ctx.document 顶层——Plan 02 的 deleteCommentById 读 `ctx.document.body.comments`）
   - sections mock 挂在 `ctx.document` 顶层（与 body 同级），含 getFirst() 与 items[]（结构见 interfaces 部分完整代码）
   - **table cell mock 完整挂载：** 用 interfaces 部分的 `.map(...)` 版本**替换**原 `const tableItems = opts?.tables ?? [];` 行——每个 table item 追加 `getCell`/`getCellOrNullObject`（返回含 `isNullObject: false` 的 cell mock）。`tableCellMockDefault` 含 `load`/`value`/`isNullObject: false`/`body.insertText`。

2. 在 Phase 9 五例 (`describe('集成：replay engine × 真 WordAdapter', ...)`) 末尾追加 4 个 Phase 27 守门用例（按 PATTERNS.md Analog 5b 逐字，关键字符串字面量必须出现）：

```typescript
  // ─── Phase 27 Word 工具补全守门用例 ───

  it('单步撤销 set_word_list_format：noop_inverse → skipped_error', async () => {
    mockWordRich({ paragraphTexts: ['段落文本'] });
    const adapter = new WordAdapter();
    const entry: OperationLogEntry = {
      runId: 'run-w27-1', stepIndex: 0,
      toolName: 'set_word_list_format',
      args: { paragraphIndex: 0, listType: 'bullet' },
      humanLabel: '将第 1 段改为项目符号列表',
      reverse: {
        tool: 'noop_inverse',
        args: { reason: '列表格式转换无法自动撤销，请手动操作' },
      },
      postState: { kind: 'word_list_format', content: { index: 0 } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('skipped_error');
  });

  it('单步撤销 insert_word_comment：真 WordAdapter.deleteCommentById 收 Record 对象 → rolled_back', async () => {
    const deleteCommentFn = vi.fn();
    mockWordRich({
      comments: [{ id: 'cmt-1', delete: deleteCommentFn }],
    });
    const adapter = new WordAdapter();
    const entry: OperationLogEntry = {
      runId: 'run-w27-2', stepIndex: 0,
      toolName: 'insert_word_comment',
      args: { paragraphIndex: 0, searchText: '测试文本', commentText: '建议修改' },
      humanLabel: '给「测试文本」插入批注',
      reverse: {
        tool: 'delete_comment_by_id',
        args: { commentId: 'cmt-1' },
      },
      postState: { kind: 'word_comment', content: { commentId: 'cmt-1' } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('rolled_back');
    expect(deleteCommentFn).toHaveBeenCalledTimes(1);
  });

  it('单步撤销 set_word_header_footer：真 WordAdapter.restoreWordHeaderFooter 收 Record 对象 → rolled_back', async () => {
    const headerInsertTextFn = vi.fn();
    mockWordRich({
      sectionHeader: { text: '旧页眉', insertText: headerInsertTextFn },
    });
    const adapter = new WordAdapter();
    const entry: OperationLogEntry = {
      runId: 'run-w27-3', stepIndex: 0,
      toolName: 'set_word_header_footer',
      args: { headerOrFooter: 'header', text: '新页眉' },
      humanLabel: '将页眉改为「新页眉」',
      reverse: {
        tool: 'restore_word_header_footer',
        args: { type: 'Primary', sectionIndex: 0, headerOrFooter: 'header', beforeText: '旧页眉' },
      },
      postState: { kind: 'word_header_footer', content: { type: 'Primary', sectionIndex: 0 } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('rolled_back');
    expect(headerInsertTextFn).toHaveBeenCalledTimes(1);
  });

  it('单步撤销 edit_table_cell：真 WordAdapter.restoreTableCell 收 Record 对象 → rolled_back', async () => {
    mockWordRich({
      tables: [{
        rowCount: 2, columnCount: 2,
        values: [['原内容', 'B'], ['C', 'D']], delete: vi.fn(),
      }],
    });
    const adapter = new WordAdapter();
    const entry: OperationLogEntry = {
      runId: 'run-w27-4', stepIndex: 0,
      toolName: 'edit_table_cell',
      args: { tableIndex: 0, rowIndex: 0, columnIndex: 0, text: '新内容' },
      humanLabel: '将表格 1 第 1 行第 1 列改为「新内容」',
      reverse: {
        tool: 'restore_table_cell',
        args: { tableIndex: 0, tableFingerprint: '原内容|B__2x2', rowIndex: 0, columnIndex: 0, beforeValue: '原内容' },
      },
      postState: { kind: 'word_table_cell', content: { tableIndex: 0, rowIndex: 0, columnIndex: 0 } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('rolled_back');
  });
```

⚠️ 提醒（BLOCKER 1）：以上 4 个用例在 wave 1 结束时**预期为 RED**（adapter 方法 wave 2/3 才实现）：set_word_list_format → skipped_error（noop_inverse 正确抛错）；其余 3 个 → skipped_error（adapter 方法未实现，executeReverse 的 `if (!adapter.xxx) throw` 触发）。wave 1 验收**只验 contract.test.ts**，不要求 integration 这 4 例此时变绿。

**改 .planning/phases/08-foundation-a-f/CONTRACT.md：**
在文件末尾追加 Phase 27 段（先读文件确认当前末尾格式，照现有 Phase 段格式追加）：
```markdown
## Phase 27：Word 工具补全

| 工具名 | Host | undo 分类 | reverseTool | integration_test | status |
|--------|------|----------|------------|-----------------|--------|
| set_word_character_format（WORD-06 折入：font 加 highlightColor，复用 restore_range_font，沿用 Phase 9 既有 integration 守门，不新增工具/合约行） | word | 简单逆向 | restore_range_font | true | done |
| set_word_list_format | word | noop+gate | noop_inverse | true | done |
| insert_word_comment | word | 简单逆向 | delete_comment_by_id | true | done |
| set_word_header_footer | word | 简单逆向 | restore_word_header_footer | true | done |
| edit_table_cell | word | 简单逆向 | restore_table_cell | true | done |
```
  </action>
  <verify>
```bash
# D-17 守门：4 个工具名字符串都在 integration.test.ts
grep -c "set_word_list_format" src/agent/operationLog.integration.test.ts
grep -c "insert_word_comment" src/agent/operationLog.integration.test.ts
grep -c "set_word_header_footer" src/agent/operationLog.integration.test.ts
grep -c "edit_table_cell" src/agent/operationLog.integration.test.ts
# 各期望 >= 1

# Phase 27 CONTRACT 行
grep -c "phase: 27" src/agent/contract.test.ts
# 期望：4

# 跨 plan mock 路径自洽（BLOCKER 2）：comments 挂在 body 上
grep -c "comments: { load" src/agent/operationLog.integration.test.ts
# 期望 >= 1（在 body 块内）

# ⚠️ Wave 1 验收 gate：只跑 contract.test.ts（D-17 守门）；integration 4 用例此时预期 RED，不在 wave 1 验收范围
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && npm run test -- --run src/agent/contract.test.ts
```
  </verify>
  <acceptance_criteria>
    - `grep -c "phase: 27" src/agent/contract.test.ts` 输出 `4`
    - `grep -c "set_word_list_format" src/agent/operationLog.integration.test.ts` 输出 `>= 1`（D-17 要求字符串出现）
    - `grep -c "insert_word_comment" src/agent/operationLog.integration.test.ts` 输出 `>= 1`
    - `grep -c "set_word_header_footer" src/agent/operationLog.integration.test.ts` 输出 `>= 1`
    - `grep -c "edit_table_cell" src/agent/operationLog.integration.test.ts` 输出 `>= 1`
    - `npm run test -- --run src/agent/contract.test.ts` 全部通过（contract 守门通过，D-17 不挂）—— **这是 wave 1 唯一的验收 gate**
    - mockWordRich 的 comments mock 挂在 `ctx.document.body` 内（与 Plan 02 的 `deleteCommentById` 读 `ctx.document.body.comments` 路径一致）；`grep "body" 上下文确认 comments 在 body 块内
    - mockWordRich 的 table items 经 `.map(...)` 追加 `getCell`/`getCellOrNullObject`；`tableCellMockDefault` 含 `isNullObject: false`
    - ⚠️ **Wave 1 后 integration 这 4 个新用例预期为 RED/skipped_error**（adapter 方法 wave 2/3 才实现）——**不**在 wave 1 验收时跑 integration 全套并要求其变绿；integration 全绿是 Wave 2（insert_word_comment）+ Wave 3（set_word_header_footer / edit_table_cell）的职责
    - `.planning/phases/08-foundation-a-f/CONTRACT.md` 包含 `Phase 27` 字符串，且 WORD-06 折入说明行含 `highlightColor` + `restore_range_font`
  </acceptance_criteria>
  <done>contract.test.ts 含 4 条 Phase 27 行（D-17 通过）；integration.test.ts 含 4 个守门用例（工具名字符串出现，comments 挂 body.comments、table 含 getCell/getCellOrNullObject + isNullObject:false）；CONTRACT.md 含 Phase 27 段（含 WORD-06 折入说明行）。**Wave 1 验收 gate = `npm run test -- --run src/agent/contract.test.ts` 退出 0**；integration 4 用例此时预期 RED/skipped_error（adapter 方法 wave 2/3 实现后才变绿），不卡 wave 1。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 生产代码接口 | contract.test.ts 通过 fs.readFileSync 读 integration.test.ts，确保守门覆盖（无用户数据流）|
| replay engine → adapter 方法 | executeReverse switch 路由到 adapter 方法——路由错误会导致 skipped_error（安全降级，不改错目标）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-1 | Tampering | executeReverse switch default case | mitigate | switch 末尾 `default: throw` 防止未知 reverse tool 被静默忽略；新 case 在 default 前插入正确位置已验证 |
| T-27-01-2 | Elevation of Privilege | DocumentAdapterForReplay 接口 | accept | 接口声明为可选（`?:`），adapter 未实现时 throw → skipped_error，不越权执行 |
| T-27-01-3 | Denial of Service | mockWordRich 扩展 | accept | 仅测试环境使用，无生产 DoS 面；mock 扩展不影响生产代码路径 |
</threat_model>

<verification>
Wave 1 验收命令（⚠️ 只跑 contract.test.ts，integration 4 用例此时预期 RED，不在 wave 1 验收范围 — BLOCKER 1）：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. TypeScript 编译
npx tsc --noEmit
# 2. Wave 1 唯一验收 gate：contract.test.ts（D-17 守门）退出 0
npm run test -- --run src/agent/contract.test.ts
# 3. 确认 operationLog.ts 三处修改
grep -c "word_list_format\|word_comment\|word_header_footer\|word_table_cell" src/agent/operationLog.ts
# 期望：4（kind union 中 4 个 kind）
grep -c "case 'delete_comment_by_id'\|case 'restore_word_header_footer'\|case 'restore_table_cell'" src/agent/operationLog.ts
# 期望：3（switch case）
# 4. 跨 plan mock 路径自洽（BLOCKER 2）：comments 在 body 内
grep -c "comments: { load" src/agent/operationLog.integration.test.ts
# 期望 >= 1

# ⚠️ 不要在 wave 1 跑 `npm run test -- --run src/agent/operationLog.integration.test.ts` 并要求 4 新例全绿——
#   它们此时是预期 RED（adapter 方法 wave 2/3 才实现）。该全套验证留给 Wave 3 收尾。
```
</verification>

<success_criteria>
- operationLog.ts 含 4 个新 PostStateSnapshot kind + 3 个接口声明 + 3 个 switch case，TypeScript 编译无错误
- contract.test.ts 含 4 条 Phase 27 行（integrationTest: true），D-17 守门通过（fs.readFileSync 检测到 4 个工具名）
- integration.test.ts 含 4 个新守门用例 + 扩展后的 mockWordRich（comments 挂 **ctx.document.body.comments**、sections、header-footer、table getCell/getCellOrNullObject + isNullObject:false）
- mockWordRich 的 comments / table cell mock 路径与形状**与 Plan 02/03 实现完全自洽**（comments = ctx.document.body.comments；cell 含 isNullObject:false）
- CONTRACT.md 含 Phase 27 段落（5 条工具行，含 WORD-06 折入说明行：highlightColor + restore_range_font + 沿用既有 integration 守门）
- **Wave 1 验收 gate = `npm run test -- --run src/agent/contract.test.ts` 退出 0**（D-17 守门）
- ⚠️ **Wave 1 后 operationLog.integration.test.ts 新增 4 个用例预期为 RED/skipped_error**（adapter 方法 wave 2/3 才实现）——integration 全绿是 Wave 2/3 的职责，不卡 wave 1 验收
</success_criteria>

<output>
完成后创建 `.planning/phases/27-word-tools/27-01-SUMMARY.md`，记录：
- 合约骨架接线三处 operationLog.ts 改动（行号）
- contract.test.ts 新增 4 行（工具名、phase 27）
- integration.test.ts mockWordRich 扩展内容（**显式记录 comments 挂载路径 = ctx.document.body.comments**、table cell mock 含 isNullObject:false）+ 4 个新用例摘要
- 当前 integration test 状态（**预期 4 例全 skipped_error/RED——这是 wave 1 正确状态，等 Plan 02/03 adapter 实现后变绿；wave 1 验收只过 contract.test.ts**）
- 跨 plan mock 契约钉死说明（供 Plan 02 deleteCommentById 实现对齐路径）
</output>

---
phase: 27-word-tools
plan: 03
type: execute
wave: 3
depends_on:
  - 27-02
files_modified:
  - src/agent/tools/write/word.ts
  - src/adapters/WordAdapter.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - WORD-09
  - WORD-10

must_haves:
  truths:
    - "WORD-09 页眉/页脚：set_word_header_footer 工具可读写第一 section 的 primary header/footer，before-image 为全文 body.text，restore 写回（包括空页眉）"
    - "WORD-10 表格单元格：edit_table_cell 工具可按 tableIndex + tableFingerprint 双定位找到目标表格，按 rowIndex/columnIndex 定位单元格，写入新文字，before-image 为 cell.value"
    - "WORD-09 inverse（restoreWordHeaderFooter）+ WORD-10 inverse（restoreTableCell）均收 Record 对象、第一行解包（D-17），inverse 定位失败时抛 HostApiError"
    - "两工具注册在 buildToolsForHost('word') wordWriteTools，humanLabel 中文非空"
    - "integration.test.ts 中 set_word_header_footer 守门 rolled_back、edit_table_cell 守门 rolled_back（Plan 01 建立的 RED 用例变绿）"
    - "全套测试绿（npm run test -- --run）+ bundle CI gate 通过（npm run build && npm run size，main bundle ≤100KB gzip）"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "setWordHeaderFooter + restoreWordHeaderFooter + editTableCell + restoreTableCell"
      contains: "restoreWordHeaderFooter"
    - path: "src/agent/tools/write/word.ts"
      provides: "setWordHeaderFooter + editTableCell ToolDef"
      contains: "set_word_header_footer"
    - path: "src/agent/tools/index.ts"
      provides: "WORD-09/10 注册"
      contains: "setWordHeaderFooter"
  key_links:
    - from: "word.ts editTableCell"
      to: "WordAdapter.editTableCell"
      via: "execute() ctx.adapter 调用"
      pattern: "editTableCell"
    - from: "WordAdapter.restoreTableCell"
      to: "buildTableFingerprint"
      via: "遍历 tables 比对指纹"
      pattern: "buildTableFingerprint"
    - from: "WordAdapter.restoreWordHeaderFooter"
      to: "ctx.document.sections"
      via: "sections.getFirst().getHeader(type) / getFooter(type)"
      pattern: "getHeader\\|getFooter"
---

<objective>
本计划实现 WORD-09（set_word_header_footer，页眉/页脚写入 + before-image restore）、WORD-10（edit_table_cell，表格单元格写入 + 双定位 restore）两个工具的完整实现，包括 adapter write/inverse 方法、word.ts ToolDef、index.ts 注册，并完成 bundle gate 收尾验证。

Purpose: 让 Plan 01 建立的 integration.test.ts 守门用例全部变绿；所有 5 个 Phase 27 工具实现完毕后，执行全套测试 + bundle gate 确认 Phase 27 交付完整。
Output: WordAdapter.ts 新增 4 个方法（setWordHeaderFooter + restoreWordHeaderFooter + editTableCell + restoreTableCell）；word.ts 新增 2 个 ToolDef；index.ts 注册；全套测试绿；bundle ≤100KB gzip
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-word-tools/27-CONTEXT.md
@.planning/phases/27-word-tools/27-RESEARCH.md
@.planning/phases/27-word-tools/27-PATTERNS.md
@.planning/phases/27-word-tools/27-02-SUMMARY.md

<interfaces>
<!-- 关键 adapter 范式，执行者直接使用 -->

**WORD-09 setWordHeaderFooter（新增 adapter write 方法）：**

参数形状（camelCase，G-C，RESEARCH.md §WORD-09 §参数形状）：
```typescript
interface SetWordHeaderFooterArgs {
  text: string;
  headerOrFooter: 'header' | 'footer';
  type?: 'Primary' | 'FirstPage' | 'EvenPages';   // 默认 'Primary'
  sectionIndex?: number;                            // 默认 0（第一 section）
}
```

adapter 方法（D-17：第一行解包；按 Analog 2a Word.run 闭包 + before-image 写前读）：
```typescript
async setWordHeaderFooter(args: Record<string, unknown>): Promise<{ beforeText: string; type: string; headerOrFooter: string; sectionIndex: number }> {
  const text = args.text as string;
  const headerOrFooter = (args.headerOrFooter as string) ?? 'header';
  const type = (args.type as string | undefined) ?? 'Primary';
  const sectionIndex = (args.sectionIndex as number | undefined) ?? 0;

  // WordApi 1.1 是基础集，无需 isSetSupported 特殊门控
  try {
    return await Word.run(async (ctx) => {
      const sections = ctx.document.sections;
      sections.load('items');
      await ctx.sync();

      if (sectionIndex < 0 || sectionIndex >= sections.items.length) {
        throw new HostApiError(`setWordHeaderFooter: sectionIndex=${sectionIndex} 越界（共 ${sections.items.length} sections）`, undefined);
      }

      const section = sections.items[sectionIndex];
      const body = headerOrFooter === 'header'
        ? section.getHeader(type as Word.HeaderFooterType)
        : section.getFooter(type as Word.HeaderFooterType);
      body.load('text');
      await ctx.sync();

      const beforeText = body.text ?? '';  // before-image

      body.insertText(text, Word.InsertLocation.replace);
      await ctx.sync();

      // R3 写后回读验证（网页版 no-op 防御）
      body.load('text');
      await ctx.sync();
      const afterText = body.text ?? '';
      if (normalizeText(afterText) !== normalizeText(text) && text.length > 0) {
        // 仅当 text 非空时检查（空字符串 insertText 可能返回含 \r 的空段落，normalizeText 后相等）
        // 如有出入，记录警告但不抛错（soft warning，真机 UAT 再确认）
      }

      return { beforeText, type, headerOrFooter, sectionIndex };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word setWordHeaderFooter 失败', err);
  }
}
```

注意 RESEARCH.md Assumption A4：`body.insertText(text, 'Replace')` 在 header/footer body 的行为待真机验证。降级方案（若 UAT 发现 Replace 不工作）= `body.clear()` + `body.insertText(text, Word.InsertLocation.start)`。

**WORD-09 restoreWordHeaderFooter（inverse 方法）：**

```typescript
async restoreWordHeaderFooter(args: Record<string, unknown>): Promise<void> {
  // D-17: 第一行解包，不用位置参
  const type = (args.type as string | undefined) ?? 'Primary';
  const sectionIndex = (args.sectionIndex as number | undefined) ?? 0;
  const headerOrFooter = (args.headerOrFooter as string | undefined) ?? 'header';
  const beforeText = args.beforeText as string;

  try {
    await Word.run(async (ctx) => {
      const sections = ctx.document.sections;
      sections.load('items');
      await ctx.sync();

      if (sectionIndex < 0 || sectionIndex >= sections.items.length) {
        throw new HostApiError(`restoreWordHeaderFooter: sectionIndex=${sectionIndex} 越界`, undefined);
      }

      const section = sections.items[sectionIndex];
      const body = headerOrFooter === 'header'
        ? section.getHeader(type as Word.HeaderFooterType)
        : section.getFooter(type as Word.HeaderFooterType);

      body.insertText(beforeText, Word.InsertLocation.replace);
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word restoreWordHeaderFooter 失败', err);
  }
}
```

**WORD-10 editTableCell（新增 adapter write 方法）：**

参数形状（camelCase，RESEARCH.md §WORD-10 §参数形状）：
```typescript
interface EditTableCellArgs {
  tableIndex: number;          // 目标表格编号（0-based）
  rowIndex: number;            // 目标行编号（0-based）
  columnIndex: number;         // 目标列编号（0-based）
  text: string;                // 新单元格文字
  tableFingerprint?: string;   // 可选内容指纹（防 index drift）
}
```

adapter 方法（D-17 + Analog 2c deleteTableByMarker 指纹遍历范式）：
```typescript
async editTableCell(args: Record<string, unknown>): Promise<{ beforeValue: string; tableFingerprint: string; tableIndex: number; rowIndex: number; columnIndex: number }> {
  const tableIndex = args.tableIndex as number;
  const rowIndex = args.rowIndex as number;
  const columnIndex = args.columnIndex as number;
  const text = args.text as string;
  const providedFingerprint = args.tableFingerprint as string | undefined;

  // WordApi 1.3 门控
  const supports = typeof Office !== 'undefined' &&
    Office.context?.requirements?.isSetSupported('WordApi', '1.3') === true;
  if (!supports) {
    throw new HostApiError('当前 Word 版本不支持表格操作（需要 WordApi 1.3）', undefined);
  }

  try {
    return await Word.run(async (ctx) => {
      const tables = ctx.document.body.tables;
      // 注：Word.Table 无 columnCount 属性，列数从 values[0].length 推导（Phase 9 gotcha）
      tables.load('items/rowCount,items/values');
      await ctx.sync();

      if (!tables.items.length) {
        throw new HostApiError('editTableCell: 文档中没有表格', undefined);
      }

      // 双重定位：tableIndex 快路径 + fingerprint 验证/降级遍历（D-06）
      let resolvedIndex = -1;

      // 策略 1：tableIndex 快路径 + fingerprint 验证
      if (tableIndex >= 0 && tableIndex < tables.items.length) {
        const candidate = tables.items[tableIndex];
        const candidateFp = buildTableFingerprint(candidate.values as string[][], candidate.rowCount);
        if (!providedFingerprint || candidateFp === providedFingerprint) {
          resolvedIndex = tableIndex;
        }
      }

      // 策略 2：若 fingerprint 不匹配，遍历找 fingerprint 匹配的表（防 index drift）
      if (resolvedIndex === -1 && providedFingerprint) {
        for (let i = 0; i < tables.items.length; i++) {
          const t = tables.items[i];
          const fp = buildTableFingerprint(t.values as string[][], t.rowCount);
          if (fp === providedFingerprint) {
            resolvedIndex = i;
            break;
          }
        }
      }

      // 策略 3：无 fingerprint，直接用 tableIndex
      if (resolvedIndex === -1 && !providedFingerprint) {
        if (tableIndex >= 0 && tableIndex < tables.items.length) {
          resolvedIndex = tableIndex;
        }
      }

      if (resolvedIndex === -1) {
        throw new HostApiError(
          `editTableCell: 找不到目标表格（tableIndex=${tableIndex}, fingerprint=${providedFingerprint ?? '无'}）`,
          undefined,
        );
      }

      const table = tables.items[resolvedIndex];
      const totalRows = table.rowCount;
      const totalCols = ((table.values as string[][])[0] ?? []).length;

      // 越界检查（Pitfall 5：无 columnCount，用 values[0].length）
      if (rowIndex < 0 || rowIndex >= totalRows || columnIndex < 0 || columnIndex >= totalCols) {
        throw new HostApiError(
          `editTableCell: 坐标越界 row=${rowIndex} col=${columnIndex}（共 ${totalRows}×${totalCols}）`,
          undefined,
        );
      }

      // before-image：读取 cell.value（Pitfall 4：cell.value 是纯文本，与写入语义对齐）
      const cell = table.getCellOrNullObject(rowIndex, columnIndex);
      cell.load('value');
      await ctx.sync();

      if (cell.isNullObject) {
        throw new HostApiError(`editTableCell: getCellOrNullObject(${rowIndex}, ${columnIndex}) 返回 null`, undefined);
      }

      const beforeValue = cell.value as string;
      const tableFingerprint = buildTableFingerprint(table.values as string[][], table.rowCount);

      // 写入
      cell.value = text;
      await ctx.sync();

      // R3 写后回读验证
      cell.load('value');
      await ctx.sync();
      if ((cell.value as string) !== text) {
        // soft warning，不抛（真机 UAT 再确认）
      }

      return { beforeValue, tableFingerprint, tableIndex: resolvedIndex, rowIndex, columnIndex };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word editTableCell 失败', err);
  }
}
```

**WORD-10 restoreTableCell（inverse 方法，D-17 + 双重定位）：**

```typescript
async restoreTableCell(args: Record<string, unknown>): Promise<void> {
  // D-17: 第一行解包，不用位置参
  const tableIndex = args.tableIndex as number;
  const tableFingerprint = args.tableFingerprint as string | undefined;
  const rowIndex = args.rowIndex as number;
  const columnIndex = args.columnIndex as number;
  const beforeValue = args.beforeValue as string;

  try {
    await Word.run(async (ctx) => {
      const tables = ctx.document.body.tables;
      tables.load('items/rowCount,items/values');
      await ctx.sync();

      // 双重定位（与 editTableCell 相同策略）
      let resolvedIndex = -1;

      if (tableIndex >= 0 && tableIndex < tables.items.length && tableFingerprint) {
        const fp = buildTableFingerprint(tables.items[tableIndex].values as string[][], tables.items[tableIndex].rowCount);
        if (fp === tableFingerprint) resolvedIndex = tableIndex;
      }

      if (resolvedIndex === -1 && tableFingerprint) {
        for (let i = 0; i < tables.items.length; i++) {
          const fp = buildTableFingerprint(tables.items[i].values as string[][], tables.items[i].rowCount);
          if (fp === tableFingerprint) { resolvedIndex = i; break; }
        }
      }

      if (resolvedIndex === -1) {
        if (tableIndex >= 0 && tableIndex < tables.items.length) {
          resolvedIndex = tableIndex;
        }
      }

      if (resolvedIndex === -1) {
        throw new HostApiError(
          `restoreTableCell: 找不到目标表格（tableIndex=${tableIndex}, fingerprint=${tableFingerprint ?? '无'}）`,
          undefined,
        );
      }

      const cell = tables.items[resolvedIndex].getCellOrNullObject(rowIndex, columnIndex);
      cell.load('value');
      await ctx.sync();

      if (cell.isNullObject) {
        throw new HostApiError(`restoreTableCell: getCellOrNullObject(${rowIndex}, ${columnIndex}) 返回 null`, undefined);
      }

      cell.value = beforeValue;
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word restoreTableCell 失败', err);
  }
}
```

**WORD-09 word.ts ToolDef（按 Analog 1a 简单逆向，PATTERNS.md L31-101）：**

```typescript
const HEADER_TEXT_CAP = 30;

interface SetWordHeaderFooterArgs {
  text: string;
  headerOrFooter: 'header' | 'footer';
  type?: 'Primary' | 'FirstPage' | 'EvenPages';
  sectionIndex?: number;
}

export const setWordHeaderFooter: ToolDef<SetWordHeaderFooterArgs> = {
  name: 'set_word_header_footer',
  kind: 'write',
  description: '编辑 Word 文档的页眉或页脚文字。默认修改第一个节的主页眉/页脚（type="Primary"）。返回前读取原文字用于撤销。',
  parameters: {
    type: 'object',
    properties: {
      text: { type: 'string', description: '新页眉/页脚文字' },
      headerOrFooter: { type: 'string', enum: ['header', 'footer'], description: '修改页眉还是页脚' },
      type: { type: 'string', enum: ['Primary', 'FirstPage', 'EvenPages'], description: '页眉/页脚类型，默认 Primary（主页眉页脚）' },
      sectionIndex: { type: 'number', description: '节编号（0-based），默认 0（第一节）' },
    },
    required: ['text', 'headerOrFooter'],
  },
  humanLabel: ({ headerOrFooter, text }) =>
    `将${headerOrFooter === 'header' ? '页眉' : '页脚'}改为「${String(text).slice(0, HEADER_TEXT_CAP)}」`,
  async execute({ text, headerOrFooter, type, sectionIndex }, ctx): Promise<ToolResult> {
    const result = await (ctx.adapter as WordAdapter).setWordHeaderFooter({
      text, headerOrFooter, type, sectionIndex,
    });
    const reverse: ReverseDescriptor = {
      tool: 'restore_word_header_footer',
      args: {
        type: result.type,
        sectionIndex: result.sectionIndex,
        headerOrFooter: result.headerOrFooter,
        beforeText: result.beforeText,
      },
    };
    const postState = {
      kind: 'word_header_footer' as const,
      content: { type: result.type, sectionIndex: result.sectionIndex },
    };
    return {
      ok: true,
      data: { headerOrFooter, modified: true },
      reverse,
      postState,
    };
  },
};
```

**WORD-10 word.ts ToolDef（按 Analog 1d insert_table 指纹 reverse.args，PATTERNS.md L173-206）：**

```typescript
const CELL_TEXT_CAP = 20;

interface EditTableCellArgs {
  tableIndex: number;
  rowIndex: number;
  columnIndex: number;
  text: string;
  tableFingerprint?: string;
}

export const editTableCell: ToolDef<EditTableCellArgs> = {
  name: 'edit_table_cell',
  kind: 'write',
  description: '编辑 Word 文档中已有表格的指定单元格文字。tableIndex=0 表示第一个表格，rowIndex/columnIndex 均为 0-based。建议提供 tableFingerprint 防止多表时定位漂移。',
  parameters: {
    type: 'object',
    properties: {
      tableIndex: { type: 'number', description: '目标表格编号（0-based）' },
      rowIndex: { type: 'number', description: '目标行编号（0-based）' },
      columnIndex: { type: 'number', description: '目标列编号（0-based）' },
      text: { type: 'string', description: '新单元格文字' },
      tableFingerprint: { type: 'string', description: '表格内容指纹（可选，防多表定位漂移；建议先用 get_document_full_text 读取）' },
    },
    required: ['tableIndex', 'rowIndex', 'columnIndex', 'text'],
  },
  humanLabel: ({ tableIndex, rowIndex, columnIndex, text }) =>
    `将表格 ${Number(tableIndex) + 1} 第 ${Number(rowIndex) + 1} 行第 ${Number(columnIndex) + 1} 列改为「${String(text).slice(0, CELL_TEXT_CAP)}」`,
  async execute({ tableIndex, rowIndex, columnIndex, text, tableFingerprint }, ctx): Promise<ToolResult> {
    const result = await (ctx.adapter as WordAdapter).editTableCell({
      tableIndex, rowIndex, columnIndex, text, tableFingerprint,
    });
    const reverse: ReverseDescriptor = {
      tool: 'restore_table_cell',
      args: {
        tableIndex: result.tableIndex,
        tableFingerprint: result.tableFingerprint,
        rowIndex: result.rowIndex,
        columnIndex: result.columnIndex,
        beforeValue: result.beforeValue,
      },
    };
    const postState = {
      kind: 'word_table_cell' as const,
      content: { tableIndex: result.tableIndex, rowIndex: result.rowIndex, columnIndex: result.columnIndex },
    };
    return {
      ok: true,
      data: { tableIndex: result.tableIndex, rowIndex, columnIndex, modified: true },
      reverse,
      postState,
    };
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WORD-09/10 adapter 方法实现</name>
  <read_first>
    - src/adapters/WordAdapter.ts（全文，重点 L38-50 buildTableFingerprint、L474-555 setCharacterFormat 范式、L1148-1183 deleteTableByMarker 指纹遍历）
    - .planning/phases/27-word-tools/27-PATTERNS.md（Analog 2b restoreRangeFont 双定位结构、Analog 2c deleteTableByMarker 指纹遍历结构）
    - .planning/phases/27-word-tools/27-RESEARCH.md（WORD-09 Assumption A4 insertText Replace 行为、WORD-10 Pitfall 4/5、R3 写后回读）
    - src/agent/operationLog.integration.test.ts（mockWordRich 扩展后的 sections / table.getCell mock 结构，Plan 01 已修改）
  </read_first>
  <behavior>
    - setWordHeaderFooter：写前读 `body.text`，insertText(text, Replace)，写后回读比对
    - restoreWordHeaderFooter：Record 对象解包，sections.getFirst()，getHeader/getFooter(type)，insertText(beforeText, Replace)
    - editTableCell：双重定位（tableIndex 快路径 + fingerprint 遍历），读 cell.value before-image，写 cell.value，返回 beforeValue + tableFingerprint
    - restoreTableCell：Record 对象解包，双重定位，getCellOrNullObject，cell.value = beforeValue
  </behavior>
  <action>
在 src/adapters/WordAdapter.ts 的末尾（`deleteTableByMarker` 方法之后，或现有最后一个方法之后）追加 4 个新方法，按 interfaces 部分完整代码逐字实现：

1. `setWordHeaderFooter(args: Record<string, unknown>)` — write 方法
2. `restoreWordHeaderFooter(args: Record<string, unknown>)` — inverse 方法
3. `editTableCell(args: Record<string, unknown>)` — write 方法
4. `restoreTableCell(args: Record<string, unknown>)` — inverse 方法

关键约束：
- **所有方法第一行解包 args**（D-17，Phase 5 签名 bug 教训）
- `Word.HeaderFooterType` 枚举：使用 `type as Word.HeaderFooterType`；若 TS 类型报错，加 `as unknown as Word.HeaderFooterType`
- `buildTableFingerprint`：已是文件内私有函数（L38-50），直接调用无需 import
- `getCellOrNullObject(row, col)` 后需 load + sync 再检查 `cell.isNullObject`（非 null object 模式防 TS 报错：`cell.isNullObject` 可能需要 `(cell as any).isNullObject`）
- `cell.value` 赋值（WORD-10 Pitfall 4）：直接 `cell.value = text` 等效替换全部文本
- 错误均包装为 `HostApiError`（不暴露 stack）
  </action>
  <verify>
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# TypeScript 编译
npx tsc --noEmit
# 确认 4 个新方法存在
grep -c "async setWordHeaderFooter\|async restoreWordHeaderFooter\|async editTableCell\|async restoreTableCell" src/adapters/WordAdapter.ts
# 期望 4
# 运行 integration test（WORD-09/10 守门用例变绿）
npm run test -- --run src/agent/operationLog.integration.test.ts
```
  </verify>
  <acceptance_criteria>
    - `grep -c 'async setWordHeaderFooter\|async restoreWordHeaderFooter\|async editTableCell\|async restoreTableCell' src/adapters/WordAdapter.ts` 输出 `4`
    - `npx tsc --noEmit` 退出 0
    - `npm run test -- --run src/agent/operationLog.integration.test.ts`：
      - `set_word_header_footer` 守门用例状态 `rolled_back`（绿）
      - `edit_table_cell` 守门用例状态 `rolled_back`（绿）
      - 所有 Plan 01/02 建立的守门用例仍通过（无回归）
  </acceptance_criteria>
  <done>4 个新 adapter 方法实现完毕；integration.test.ts 5 个 Phase 27 守门用例（含 set_word_list_format skipped_error）全部验证正确</done>
</task>

<task type="auto">
  <name>Task 2: WORD-09/10 ToolDef + index.ts 注册 + 全套测试 + bundle gate 收尾</name>
  <read_first>
    - src/agent/tools/write/word.ts（全文，重点最后几个 ToolDef 的结构，确认追加位置）
    - src/agent/tools/index.ts（L275-293 wordWriteTools 数组，确认当前已注册的工具列表）
    - .planning/phases/27-word-tools/27-PATTERNS.md（Analog 1a/1d ToolDef 完整结构、Analog 6a index.ts 注册位置）
    - .planning/phases/27-word-tools/27-CONTEXT.md（G-C casing 约定，camelCase，不建 WORD_TOOLS Set）
  </read_first>
  <action>
**改 src/agent/tools/write/word.ts：**

在文件末尾追加两个 interface + ToolDef（按 interfaces 部分完整代码）：
1. `SetWordHeaderFooterArgs` 接口 + `setWordHeaderFooter` ToolDef
   - name: `'set_word_header_footer'`
   - humanLabel: `({ headerOrFooter, text }) => \`将\${headerOrFooter === 'header' ? '页眉' : '页脚'}改为「\${String(text).slice(0, HEADER_TEXT_CAP)}」\``
   - reverse.tool: `'restore_word_header_footer'`
   - postState.kind: `'word_header_footer' as const`

2. `EditTableCellArgs` 接口 + `editTableCell` ToolDef
   - name: `'edit_table_cell'`
   - humanLabel: `({ tableIndex, rowIndex, columnIndex, text }) => \`将表格 \${Number(tableIndex) + 1} 第 \${Number(rowIndex) + 1} 行第 \${Number(columnIndex) + 1} 列改为「\${String(text).slice(0, CELL_TEXT_CAP)}」\``
   - reverse.tool: `'restore_table_cell'`
   - postState.kind: `'word_table_cell' as const`

在文件顶部常量区（与现有 `HUMAN_LABEL_TEXT_CAP` 等常量同区）增加：
```typescript
const HEADER_TEXT_CAP = 30;
const CELL_TEXT_CAP = 20;
```

**改 src/agent/tools/index.ts：**

1. import 区追加：
```typescript
import { setWordHeaderFooter, editTableCell } from './write/word.js';
```

2. wordWriteTools 数组在 `batchWrite` 之前追加（与 Plan 02 追加的两个工具顺序一致）：
```typescript
    setWordHeaderFooter, // Phase 27 WORD-09
    editTableCell, // Phase 27 WORD-10
```

**收尾全套验证（此 task 最重要）：**
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. TypeScript 编译
npx tsc --noEmit
# 2. 全套测试（1100+ 测试全部绿，5 个 Phase 27 守门用例全绿）
npm run test -- --run
# 3. bundle gate（先 build 再 size，不测陈旧 dist）
npm run build && npm run size
# 期望：main-*.js ≤100KB gzip（当前 82.48KB，预计增量 ~5-7KB，目标 ~88-90KB）
```
  </action>
  <verify>
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# ToolDef 名字字符串
grep -c "name: 'set_word_header_footer'" src/agent/tools/write/word.ts
grep -c "name: 'edit_table_cell'" src/agent/tools/write/word.ts
# 各期望 1

# index.ts 注册
grep -c "setWordHeaderFooter\|editTableCell" src/agent/tools/index.ts
# 期望 >= 2（import + 数组两处）

# 全套测试
npm run test -- --run

# bundle gate（先 build 再 size，绝不测陈旧 dist）
npm run build && npm run size
```
  </verify>
  <acceptance_criteria>
    - `grep -c "name: 'set_word_header_footer'" src/agent/tools/write/word.ts` 输出 `1`
    - `grep -c "name: 'edit_table_cell'" src/agent/tools/write/word.ts` 输出 `1`
    - `grep -c "setWordHeaderFooter\|editTableCell" src/agent/tools/index.ts` 输出 `>= 2`
    - `npx tsc --noEmit` 退出 0
    - `npm run test -- --run` 退出 0（所有测试绿，含 Phase 27 的 5 个守门用例）
    - `npm run build && npm run size` main bundle ≤100KB gzip（期望输出包含 `Passed` 且 main-*.js 数值 ≤100KB）
    - `npm run test -- --run src/agent/contract.test.ts` 退出 0（D-17 守门：5 个工具名字符串均在 integration.test.ts 中）
  </acceptance_criteria>
  <done>WORD-09/10 ToolDef 注册完成；全套测试绿；bundle CI gate PASS（≤100KB gzip）；Phase 27 全部 5 个需求（WORD-06/07/08/09/10）交付完毕</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → adapter（表格写入） | editTableCell 的 text 来自 LLM，写入 Word 表格单元格；定位失败（未命中 index + fingerprint）→ NOT_FOUND，不静默改错单元格 |
| adapter → Word.run 闭包 | proxy 对象（section、body、table、cell）不出 Word.run 闭包（A-06）|
| executeReverse → adapter.restoreTableCell | Record 对象路由（D-17），防位置参错配；双定位防 index drift 改错表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-1 | Tampering | editTableCell 定位 | mitigate | tableIndex 快路径 + tableFingerprint 遍历双定位；找不到抛 HostApiError（NOT_FOUND recoverable），不静默改错表格 |
| T-27-03-2 | Tampering | restoreTableCell 定位 | mitigate | 同上双定位；找不到抛 HostApiError → skipped_error，不误改其他单元格 |
| T-27-03-3 | Elevation of Privilege | setWordHeaderFooter sectionIndex | mitigate | sectionIndex 越界检查（`< sections.items.length`），越界抛 HostApiError，不修改错误节的页眉 |
| T-27-03-4 | Denial of Service | editTableCell 越界行列 | mitigate | rowIndex/columnIndex 越界检查（`rowCount × values[0].length`），越界抛 HostApiError（recoverable hint：先读表结构再改） |
| T-27-03-5 | Information Disclosure | HostApiError | mitigate | `new HostApiError(message, err)` 不存 stack，仅传 message（防调用链泄漏）；adapter 内 err 被封装，不上浮到 LLM 输出 |
| T-27-03-6 | Spoofing | bundle gate（npm run size） | mitigate | 每次 `npm run build` 生成新 dist 后再 `npm run size`（不测陈旧 dist 给假绿）；CI gate = 100KB gzip |
</threat_model>

<verification>
完整 Phase 27 交付验证命令（按顺序）：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"

# 1. TypeScript 编译（0 错误）
npx tsc --noEmit

# 2. 全套测试（退出 0，全绿）
npm run test -- --run

# 3. Phase 27 专项守门（contract.test.ts D-17 + integration.test.ts 5 例）
npm run test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts

# 4. bundle gate（先 build 再 size，绝不测陈旧 dist）
npm run build && npm run size
# 期望输出：main-*.js ≤100KB gzip（Passed）
```

Phase 27 5 工具接线四处对齐验证：
```bash
# WORD-07 noop_inverse 四处对齐
grep "noop_inverse" src/agent/tools/write/word.ts        # reverse.tool
grep "noop_inverse" src/agent/operationLog.ts            # case + noop_inverse throw
grep "noop_inverse" src/agent/contract.test.ts            # CONTRACT 行 reverseTool
# 各至少 1 次

# WORD-08 delete_comment_by_id 四处对齐
grep "delete_comment_by_id" src/agent/tools/write/word.ts         # reverse.tool
grep "delete_comment_by_id" src/agent/operationLog.ts              # case + 接口声明注释
grep "delete_comment_by_id" src/agent/contract.test.ts             # CONTRACT 行
grep "deleteCommentById" src/adapters/WordAdapter.ts               # adapter 方法名
# 各至少 1 次

# WORD-09 restore_word_header_footer 四处对齐
grep "restore_word_header_footer" src/agent/tools/write/word.ts    # reverse.tool
grep "restore_word_header_footer" src/agent/operationLog.ts        # case
grep "restore_word_header_footer" src/agent/contract.test.ts       # CONTRACT 行
grep "restoreWordHeaderFooter" src/adapters/WordAdapter.ts         # adapter 方法名

# WORD-10 restore_table_cell 四处对齐
grep "restore_table_cell" src/agent/tools/write/word.ts            # reverse.tool
grep "restore_table_cell" src/agent/operationLog.ts                # case
grep "restore_table_cell" src/agent/contract.test.ts               # CONTRACT 行
grep "restoreTableCell" src/adapters/WordAdapter.ts                # adapter 方法名
```
</verification>

<success_criteria>
Phase 27 全部交付成功的判定：
- **WORD-06**：setCharacterFormat highlightColor 字段折入，restoreRangeFont null 写回正确，原有 integration test 仍绿
- **WORD-07**：set_word_list_format 工具注册，执行列表操作，undo = noop_inverse（skipped_error），诚实标注
- **WORD-08**：insert_word_comment 工具注册，`[Aster] ` 前缀，comment.id 写后回读；deleteCommentById inverse，integration test rolled_back
- **WORD-09**：set_word_header_footer 工具注册，before-image body.text，restore insertText(Replace)；integration test rolled_back
- **WORD-10**：edit_table_cell 工具注册，双定位（index + fingerprint），cell.value before-image + restore；integration test rolled_back
- **合约三步齐**：CONTRACT.md Phase 27 段、contract.test.ts 4 行（integrationTest:true）、integration.test.ts 5 守门用例——缺一 D-17 CI 挂
- **bundle gate PASS**：`npm run build && npm run size` main bundle ≤100KB gzip
- **全套测试绿**：`npm run test -- --run` 退出 0
</success_criteria>

<output>
完成后创建 `.planning/phases/27-word-tools/27-03-SUMMARY.md`，记录：
- WORD-09：setWordHeaderFooter + restoreWordHeaderFooter 方法行号，before-image 策略（body.text），Assumption A4（insertText Replace 真机待验）
- WORD-10：editTableCell + restoreTableCell 方法行号，双定位逻辑，buildTableFingerprint 复用
- ToolDef 行号（word.ts）+ 注册行号（index.ts）
- 全套测试结果（pass 数量）
- bundle gate 结果（实测 KB 数）
- Phase 27 全部 5 工具交付总结（WORD-06 折入确认 + 四工具新增）
- UAT 种子注意事项（WORD-08 Word for Web 批注刷新 bug、WORD-09 insertText Replace 真机验证）
</output>

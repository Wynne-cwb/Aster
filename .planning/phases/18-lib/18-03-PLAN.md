---
phase: 18-lib
plan: 03
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - src/components/Settings/SettingsPanel.tsx
  - src/components/StockImageResultCard.tsx
  - src/components/ChatStream.tsx
  - src/styles.css
  - src/i18n/locales/zh-CN/messages.po
  - src/components/Settings/SettingsPanel.test.tsx
autonomous: true
requirements:
  - LIB-01
  - LIB-03

must_haves:
  truths:
    - "Settings 新增独立「图库 / Pexels API Key」pref-section（仿 image-gen model picker 范式）：type='password' 输入框，存 STORAGE_KEYS.PEXELS_API_KEY（partitioned localStorage）"
    - "Pexels key 存储 round-trip：填值 storage.set、清空 storage.remove；不塞进 LLM ProviderList/ProviderForm（Q3=A 独立字段）"
    - "chat 内 LIB-03 署名：每张插入的图配只读 note「照片来自 Pexels · 摄影师 [name]」，Pexels + 摄影师均为可点链接（photo_url / photographer_url），target=_blank rel=noopener"
    - "署名/缩略图只读：无确认/选择/重新检索按钮（D-06 自动直插哲学）；缩略图用 Pexels 远程 URL（thumbnail_url，<img src> 不受 CORS 限制），不用 base64"
    - "绝不在插入的图片上叠水印（LIB-03 + ROADMAP；保 slide 视觉）——署名只在 chat 内"
    - "ChatStream 通过 data.inserted + data.thumbnail_url + data.photographer 识别图库结果，渲染 StockImageResultCard（与生图 ImagePreviewCard 的 data.thumbnail base64 路径互斥）；lazy import 守 ≤82KB main 预算"
    - "teal 克制 UI：复用 .img-result-card 体系 + CSS 变量；不硬编码 hex/px；加载 aster-design-system skill"
    - "新增 <Trans> 宏后跑 npm run extract（同步 messages.po，否则 coverage.test 红 — memory i18n_extract_and_test_noise）"
  artifacts:
    - path: "src/components/Settings/SettingsPanel.tsx"
      provides: "Pexels API Key pref-section"
      contains: "PEXELS_API_KEY"
    - path: "src/components/StockImageResultCard.tsx"
      provides: "图库只读结果卡 + 署名（LIB-03）"
      exports: ["StockImageResultCard"]
    - path: "src/components/ChatStream.tsx"
      provides: "图库结果识别 + 卡渲染"
      contains: "thumbnail_url"
  key_links:
    - from: "src/components/Settings/SettingsPanel.tsx"
      to: "src/lib/storage.ts"
      via: "storage.set/get/remove(STORAGE_KEYS.PEXELS_API_KEY)"
      pattern: "PEXELS_API_KEY"
    - from: "src/components/ChatStream.tsx"
      to: "src/components/StockImageResultCard.tsx"
      via: "lazy import + Suspense 渲染（data.thumbnail_url 信号）"
      pattern: "StockImageResultCard"
---

<objective>
Phase 18 的 **前端层**：① Settings 独立「图库 / Pexels API Key」输入框（BYO key，Q3=A）；② chat 内 LIB-03 署名展示（只读缩略图 + Pexels/摄影师可点链接，不叠水印）。复用 Phase 16 ImagePreviewCard 只读卡范式 + image-gen model picker 的 pref-section 范式。

**Purpose:** 让用户能填 Pexels key（LIB-01 UI 入口），并在插图后于 chat 内看到合规署名（LIB-03）。

**Output:** SettingsPanel pref-section + StockImageResultCard（新）+ ChatStream 接线 + styles.css 署名样式 + extract。

> ⚠️ **UI 改动前加载 `Skill("aster-design-system")`**（teal 克制系统、CSS token、组件类名、反模式）。全中文 UI。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-lib/18-CONTEXT.md
@.planning/phases/18-lib/18-RESEARCH.md
@.planning/phases/18-lib/18-PATTERNS.md

<interfaces>
<!-- executor 直接使用的关键接口（已 file:line 核验 2026-06-03） -->

来自 src/components/Settings/SettingsPanel.tsx（image-gen model picker state L86-93 + pref-section L192-213 = 仿照模板）：
```typescript
const [imageGenModel, setImageGenModelState] = useState<string>(
  () => storage.get<string>(STORAGE_KEYS.PREF_IMAGE_GEN_MODEL) ?? DEFAULT_IMAGE_GEN_MODEL.id,
);
const setImageGenModel = (modelId: string): void => {
  storage.set(STORAGE_KEYS.PREF_IMAGE_GEN_MODEL, modelId);
  setImageGenModelState(modelId);
};
// 渲染（L193-213）：
<div className="aster-settings__section">
  <label className="aster-settings__label" htmlFor="setting-image-gen-model"><Trans>生图模型</Trans></label>
  <select className="aster-settings__select" value={imageGenModel} onChange={...} aria-label={t`生图模型`}>...</select>
  <p className="aster-settings__hint"><Trans>...</Trans></p>
</div>
// 自定义偏好 pref-input（L224）：<textarea className="aster-settings__pref-input" .../>
```

来自 src/components/ChatStream.tsx（imageResult 派生 L116-124 + 渲染 L222-232；这是生图卡，图库卡要并列加一个 stockResult 派生）：
```typescript
const imageResult = ((): { base64: string; mimeType: string } | null => {
  if (!message.toolResult?.ok || typeof message.toolResult.data !== 'object' || !message.toolResult.data) return null;
  const d = message.toolResult.data as Record<string, unknown>;
  if (d.inserted !== true || typeof d.thumbnail !== 'string') return null;   // 生图：thumbnail 是 base64
  return { base64: d.thumbnail, mimeType: (d.mimeType as string) ?? 'image/png' };
})();
// 渲染（L224-232）：
{imageResult && (<Suspense fallback={null}><ImagePreviewCard base64={...} mimeType={...} host={host} /></Suspense>)}
// host 已在 L201-202 求出：currentHost==='ppt'||'word' ? currentHost : 'word'
// ImagePreviewCard lazy import 范式见 L45-47
```

来自 src/components/ImagePreviewCard.tsx（只读卡模板，复用 .img-result-card）：
```tsx
export function ImagePreviewCard({ base64, mimeType, host }): ReactElement {
  return (<div className="aster-tool-card img-result-card">
    <img src={`data:${mimeType};base64,${base64}`} alt="已插入的生成图片" className="img-result-card__thumb" />
    <div className="img-result-card__label">{host === 'ppt' ? '已插入到 PPT' : '已插入到 Word'}</div>
  </div>);
}
```

来自 src/styles.css（已有 .img-result-card / __thumb / __label，L576-589；需新增 __attribution）+ STORAGE_KEYS.PEXELS_API_KEY（Plan 18-01）。

工具 ToolResult.data 契约（Plan 18-02 产出，本 plan 渲染依据）：
```typescript
// PPT/Word search_and_insert_stock_image 成功 data：
{ shape_id?, slide_index?, photographer: string, photographer_url: string, photo_url: string,
  alt: string, thumbnail_url: string /* =photo.src.tiny 远程 URL，非 base64 */, inserted: true }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsPanel 新增「图库 / Pexels API Key」pref-section</name>
  <files>
    src/components/Settings/SettingsPanel.tsx
  </files>
  <read_first>
    - src/components/Settings/SettingsPanel.tsx — image-gen model picker state（L86-93）+ pref-section 渲染（L192-213）+ pref-input textarea（L219-244）+ 顶部 imports（storage / STORAGE_KEYS / Trans / t）
    - src/lib/storage.ts — STORAGE_KEYS.PEXELS_API_KEY（Plan 18-01）；storage.get/set/remove
    - .planning/phases/18-lib/18-PATTERNS.md — §SettingsPanel（L329-419 含 state + JSX 形态）
    - Skill: aster-design-system（teal 克制：.aster-settings__section / __label / __pref-input / __hint 类名 + 诚实禁用范式）
  </read_first>
  <action>
    **state**（在 imageGenModel state L86-93 之后追加）：
    ```typescript
    // Phase 18 LIB-01（D-08）：BYO Pexels API Key（独立字段，非 LLM Provider）
    const [pexelsApiKey, setPexelsApiKeyState] = useState<string>(
      () => storage.get<string>(STORAGE_KEYS.PEXELS_API_KEY) ?? '',
    );
    const setPexelsApiKey = (key: string): void => {
      const trimmed = key.trim();
      if (trimmed) storage.set(STORAGE_KEYS.PEXELS_API_KEY, trimmed);
      else storage.remove(STORAGE_KEYS.PEXELS_API_KEY);
      setPexelsApiKeyState(key);
    };
    ```

    **渲染**（在生图模型 section L213 之后、自定义偏好 section L219 之前插入一个新 section）：
    ```tsx
    {/* Phase 18 LIB-01（D-08）— 图库 / Pexels API Key（独立字段，密码态） */}
    <div className="aster-settings__section">
      <label className="aster-settings__label" htmlFor="setting-pexels-key">
        <Trans>图库 / Pexels API Key</Trans>
      </label>
      <input
        id="setting-pexels-key"
        type="password"
        className="aster-settings__pref-input"
        value={pexelsApiKey}
        onChange={(e) => setPexelsApiKey(e.target.value)}
        placeholder={t`粘贴 Pexels API Key`}
        aria-label={t`Pexels API Key`}
        autoComplete="off"
      />
      <p className="aster-settings__hint">
        <Trans>用于从 Pexels 免费图库检索正版图片插入 PPT / Word。在 pexels.com/api 免费申请。</Trans>
      </p>
    </div>
    ```
    若 `.aster-settings__pref-input` 默认是 textarea 样式（多行），可改 className 为已有单行 input 类（如复用 ProviderForm 的 key 输入框类），或在 styles.css 确认 input.aster-settings__pref-input 渲染正常——以单行密码框为准（teal 克制）。
    类名/间距全走现有 CSS 变量，不硬编码。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "error" | head; grep -c "PEXELS_API_KEY" src/components/Settings/SettingsPanel.tsx</automated>
  </verify>
  <done>
    Settings 含 Pexels key 字段，build 无错误。
    acceptance_criteria:
    - `grep -c "STORAGE_KEYS.PEXELS_API_KEY" src/components/Settings/SettingsPanel.tsx` 返回 ≥2（get + set/remove）
    - `grep -c "setting-pexels-key" src/components/Settings/SettingsPanel.tsx` 返回 ≥1
    - `grep -c "type=\"password\"" src/components/Settings/SettingsPanel.tsx` 返回 ≥1
    - `grep -c "storage.remove(STORAGE_KEYS.PEXELS_API_KEY)" src/components/Settings/SettingsPanel.tsx` 返回 1（清空走 remove）
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
  </done>
</task>

<task type="auto">
  <name>Task 2: 新建 StockImageResultCard.tsx（只读缩略图 + LIB-03 署名）+ styles.css 署名样式</name>
  <files>
    src/components/StockImageResultCard.tsx
    src/styles.css
  </files>
  <read_first>
    - src/components/ImagePreviewCard.tsx — 整文件（只读卡结构、.img-result-card 类、host 标签）
    - src/styles.css — .img-result-card / __thumb / __label（L576-589）；CSS 变量体系（--accent / --text-3 / --border 等）
    - .planning/phases/18-lib/18-PATTERNS.md — §StockImageResultCard（L556-597）
    - Skill: aster-design-system（teal 克制、链接色用 --accent、字号/间距用 token）
  </read_first>
  <action>
    新建 `src/components/StockImageResultCard.tsx`（仿 ImagePreviewCard 只读态；缩略图用远程 URL）：
    ```tsx
    import type { ReactElement } from 'react';
    import { Trans } from '@lingui/react/macro';

    interface StockImageResultCardProps {
      thumbnailUrl: string;       // Pexels 远程 URL（photo.src.tiny），<img src> 不受 CORS 限制
      photographer: string;
      photographerUrl: string;
      photoUrl: string;           // Pexels 图片页（署名链接）
      host: 'ppt' | 'word';
    }

    /** 图库只读结果卡（LIB-03）：缩略图 + 「已插入到 X」+ Pexels/摄影师署名链接。无任何操作按钮（D-06）。不叠水印。 */
    export function StockImageResultCard({ thumbnailUrl, photographer, photographerUrl, photoUrl, host }: StockImageResultCardProps): ReactElement {
      return (
        <div className="aster-tool-card img-result-card">
          <img src={thumbnailUrl} alt="已插入的图库图片" className="img-result-card__thumb" loading="lazy" />
          <div className="img-result-card__label">
            {host === 'ppt' ? <Trans>已插入到 PPT</Trans> : <Trans>已插入到 Word</Trans>}
          </div>
          <div className="img-result-card__attribution">
            <Trans>照片来自</Trans>{' '}
            <a href={photoUrl} target="_blank" rel="noopener noreferrer">Pexels</a>
            {' · '}
            <a href={photographerUrl} target="_blank" rel="noopener noreferrer">{photographer}</a>
          </div>
        </div>
      );
    }
    ```
    （Trans 用法须与项目现有 import 来源一致——核对 ImagePreviewCard/SettingsPanel 用的是 `@lingui/react/macro` 还是 `@lingui/macro`，照抄同源。）

    **styles.css** — 在 `.img-result-card__label`（L587 附近）之后新增 teal 克制署名样式（全走 CSS 变量）：
    ```css
    .img-result-card__attribution {
      margin-top: var(--space-1, 4px);
      font-size: 12px;
      color: var(--text-3);
    }
    .img-result-card__attribution a {
      color: var(--accent);
      text-decoration: none;
    }
    .img-result-card__attribution a:hover { text-decoration: underline; }
    ```
    （变量名以 styles.css 实际存在的为准：核对 --text-3 / --accent / --space-* 是否存在，不存在则换成已有 token，不新增硬编码 hex。）
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "error" | head; grep -c "StockImageResultCard" src/components/StockImageResultCard.tsx</automated>
  </verify>
  <done>
    署名卡组件 + 样式存在，build 无错误。
    acceptance_criteria:
    - `grep -c "export function StockImageResultCard" src/components/StockImageResultCard.tsx` 返回 1
    - `grep -c "target=\"_blank\"" src/components/StockImageResultCard.tsx` 返回 ≥2（Pexels + 摄影师链接）
    - `grep -c "rel=\"noopener" src/components/StockImageResultCard.tsx` 返回 ≥2
    - `grep -c "img-result-card__attribution" src/styles.css` 返回 ≥1
    - `grep -ci "watermark\|水印" src/components/StockImageResultCard.tsx` 返回 0（不叠水印）
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
  </done>
</task>

<task type="auto">
  <name>Task 3: ChatStream 识别图库结果并渲染 StockImageResultCard（lazy）</name>
  <files>
    src/components/ChatStream.tsx
  </files>
  <read_first>
    - src/components/ChatStream.tsx — ImagePreviewCard lazy import（L45-47）、imageResult 派生（L116-124）、host 求值（L201-202）、渲染块（L222-232）
    - src/components/StockImageResultCard.tsx — props 契约（Task 2）
    - .planning/phases/18-lib/18-PATTERNS.md — ChatStream 渲染锚点说明
  </read_first>
  <action>
    1. **lazy import**（仿 ImagePreviewCard L45-47，守 ≤82KB main 预算）：
       ```typescript
       const StockImageResultCard = lazy(() =>
         import('./StockImageResultCard').then((m) => ({ default: m.StockImageResultCard })),
       );
       ```
    2. **stockResult 派生**（在 imageResult 派生 L124 之后，并列新增；与生图卡互斥——生图用 d.thumbnail base64，图库用 d.thumbnail_url URL + photographer）：
       ```typescript
       // Phase 18 LIB-03：图库只读结果卡（缩略图远程 URL + 署名；与生图 imageResult 互斥）
       const stockResult = ((): { thumbnailUrl: string; photographer: string; photographerUrl: string; photoUrl: string } | null => {
         if (!message.toolResult?.ok || typeof message.toolResult.data !== 'object' || !message.toolResult.data) return null;
         const d = message.toolResult.data as Record<string, unknown>;
         if (d.inserted !== true || typeof d.thumbnail_url !== 'string' || typeof d.photographer !== 'string') return null;
         return {
           thumbnailUrl: d.thumbnail_url,
           photographer: d.photographer,
           photographerUrl: (d.photographer_url as string) ?? '',
           photoUrl: (d.photo_url as string) ?? '',
         };
       })();
       ```
    3. **渲染**（在 imageResult 渲染块 L224-232 之后并列追加）：
       ```tsx
       {stockResult && (
         <Suspense fallback={null}>
           <StockImageResultCard
             thumbnailUrl={stockResult.thumbnailUrl}
             photographer={stockResult.photographer}
             photographerUrl={stockResult.photographerUrl}
             photoUrl={stockResult.photoUrl}
             host={host}
           />
         </Suspense>
       )}
       ```
    （`lazy` / `Suspense` 已在 ChatStream import；若 host 变量作用域不覆盖此处，复用 L201-202 同款求值。）
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "error" | head; npm test -- --run src/components/ChatStream.test.tsx</automated>
  </verify>
  <done>
    ChatStream 渲染图库署名卡，build + ChatStream 测试无回归。
    acceptance_criteria:
    - `grep -c "StockImageResultCard" src/components/ChatStream.tsx` 返回 ≥3（lazy import + 派生注释 + 渲染）
    - `grep -c "thumbnail_url" src/components/ChatStream.tsx` 返回 ≥1
    - `grep -c "stockResult" src/components/ChatStream.tsx` 返回 ≥2
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
    - `npm test -- --run src/components/ChatStream.test.tsx` 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 4: i18n extract + Settings 存储 round-trip 测试 + bundle gate</name>
  <files>
    src/i18n/locales/zh-CN/messages.po
    src/components/Settings/SettingsPanel.test.tsx
  </files>
  <read_first>
    - src/components/Settings/SettingsPanel.test.tsx — 现有冒烟测试结构（render + 断言 section/输入框）
    - package.json — "extract": "lingui extract"；"size" / "build" scripts
    - memory: project_i18n_extract_and_test_noise（改 UI 动 Lingui 宏必跑 extract）；project_bundle_size_guard（先 build 再 size）
  </read_first>
  <action>
    1. **跑 `npm run extract`** 同步新增 `<Trans>` 宏到 `src/i18n/locales/zh-CN/messages.po`（图库 Key 字段 + 署名卡字符串）。提交更新后的 messages.po。
    2. **SettingsPanel.test.tsx 追加** Pexels key 存储 round-trip 用例（仿现有 image-gen model 测试）：
       - render SettingsPanel → 找到 `#setting-pexels-key` 输入框 → fireEvent.change 填 'pk-abc' → 断言 `storage.get(STORAGE_KEYS.PEXELS_API_KEY) === 'pk-abc'`（或 localStorage 含 prefixed key）
       - 清空（填 ''）→ 断言 `storage.get(...)` 为 null（走 remove）
       afterEach 清 localStorage。
    3. **bundle gate**：`npm run build && npm run size` —— 确认 main-*.js ≤82KB gzip。
       - StockImageResultCard 已 lazy（不进 main）；Settings 字段增量极小。
       - 若超门（当前基线 ~79.8KB，余量约 2KB）：与 Plan 18-02 协调把 Pexels client 改为工具内动态 import；或精简 Settings 文案。在 SUMMARY 记录实测值。
  </action>
  <verify>
    <automated>npm run extract; npm test -- --run src/components/Settings/SettingsPanel.test.tsx; npm run build && npm run size</automated>
  </verify>
  <done>
    extract 同步、Settings 存储测试绿、bundle ≤82KB。
    acceptance_criteria:
    - `npm test -- --run src/components/Settings/SettingsPanel.test.tsx` 退出 0
    - `git status --porcelain src/i18n/locales/zh-CN/messages.po` —— extract 后若有变更已纳入提交（或无变更=宏已存在）
    - `grep -c "PEXELS_API_KEY\|pexels-key\|pk-abc" src/components/Settings/SettingsPanel.test.tsx` 返回 ≥1
    - `npm run build && npm run size` —— size 报告 main-*.js ≤ 82KB gzip（人工确认输出）
    - `npm test -- --run` 全量通过（i18n coverage.test 不因未 extract 变红）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings 输入框 → localStorage | Pexels key 经 storage.set 写 partitioned localStorage（partition 自动注入）；不进 Aster 自有服务器（无后台） |
| ChatStream → StockImageResultCard | 仅渲染 ToolResult.data 的 URL 字符串（thumbnail_url/photo_url/photographer_url）；不含 base64、不含 apiKey |
| 署名链接 → 外部站点 | a[target=_blank] 带 rel=noopener noreferrer（防 tabnabbing / referrer 泄漏） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Information Disclosure | Pexels key 在 Settings 明文显示 | mitigate | input type='password'（默认遮蔽）；autoComplete='off'；key 留浏览器本地（BYO，无后台上传） |
| T-18-08 | Tampering | 署名外链 tabnabbing | mitigate | 所有 `<a target=_blank>` 带 `rel="noopener noreferrer"` |
| T-18-02 | Information Disclosure | 缩略图/卡片携 base64 进 DOM/序列化 | mitigate | StockImageResultCard 用远程 URL（thumbnail_url），无 base64；tool role 消息不进 serializeForStorage（chat.ts L122-135） |
</threat_model>

<verification>
- `npm run build` 无 TypeScript 错误
- `npm test -- --run` 全量通过（含 SettingsPanel + ChatStream 测试；i18n coverage 绿）
- `npm run extract` 已跑，messages.po 同步（无未提取宏）
- `npm run build && npm run size` —— main-*.js ≤82KB gzip（StockImageResultCard lazy，不进 main）
- 人工：Settings 出现「图库 / Pexels API Key」密码框；插图后 chat 显示 Pexels + 摄影师可点链接、无水印
</verification>

<success_criteria>
1. Settings 独立 Pexels key pref-section（password 框，存/清 STORAGE_KEYS.PEXELS_API_KEY；不进 LLM Provider 列表）
2. StockImageResultCard：只读缩略图（远程 URL）+「已插入到 X」+ Pexels/摄影师可点链接（rel=noopener）；无操作按钮；不叠水印（LIB-03）
3. ChatStream 据 data.thumbnail_url + photographer 识别图库结果并 lazy 渲染署名卡（与生图卡互斥）
4. teal 克制 UI（CSS 变量，无硬编码）；全中文；npm run extract 同步 messages.po
5. bundle ≤82KB gzip（lazy 守门）；全量测试无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/18-lib/18-03-SUMMARY.md`，记录：
- Settings 输入框最终形态（password / 显隐切换 / 是否加测试按钮）
- StockImageResultCard 与生图 ImagePreviewCard 的互斥识别逻辑
- npm run extract 后 messages.po 变更行数
- bundle size build 后实测值（含本 plan + 18-02 累加是否触门）
</output>

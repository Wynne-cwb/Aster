---
phase: 18-lib
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - src/agent/tools/write/search-stock-image.ts
  - src/agent/tools/index.ts
  - src/agent/tools/write/search-stock-image.test.ts
  - src/store/chat.test.ts
  - src/agent/operationLog.integration.test.ts
  - src/agent/tools/tools-host.test.ts
  - src/agent/tools/write/ppt-image.ts
  - src/agent/tools/write/word-image.ts
  - src/adapters/PptAdapter.ts
  - src/lib/insertImage.ts
autonomous: true
requirements:
  - LIB-02
  - LIB-03

must_haves:
  truths:
    - "search-stock-image.ts 导出两个 ToolDef（PPT + Word），均 name='search_and_insert_stock_image'、kind:'write'、timeoutMs=120_000（覆盖默认 15s，防慢 fetch 误杀，memory browser_image_gen_gotchas）"
    - "工具是 loop 内 write tool，走标准 reverse 路径：execute 直接调 adapter（PPT addImageShape / Word insertBodyImage）→ 返回 reverse descriptor + postState → loop-helpers 自动 appendOperation。绝不调 insertImage helper（D-02 reconcile）"
    - "PPT reverse = { tool:'delete_shape_by_id', args:{ slide_index, shape_id } }（Record 对象，snake_case，非位置参 — memory adapter_inverse_signature）；postState = { kind:'ppt_shape_new', content:{ slideIndex, shapeId } }（camelCase content）"
    - "Word reverse = { tool:'noop_inverse', args:{ reason:'Word 图片插入暂不支持自动撤销' } }（诚实标注）"
    - "工具 description 明确指引 AI 传英文 query（D-04）；params snake_case：query(必填)/slide_index/page"
    - "流程：registry.resolve('stock-image') 读 key → searchPexels → 选首张（可依 alt 选最匹配，非机械 [0]）→ fetchPexelsImageToBase64(photo.src.large) → adapter 插入 → 返回 data.shape_id（PPT 供 AI 排版）"
    - "data 含 LIB-03 署名字段：photographer / photographer_url / photo_url / thumbnail_url(=photo.src.tiny 远程 URL)；inserted:true；data 绝不含 base64（thumbnail_url 是 URL 字符串，天然满足 NFR-09）"
    - "三态结构化错误（D-13）：无 key→PERMISSION_DENIED 不可恢复；429→HOST_API_FAILED 可恢复；无结果→ok:true data.results=0（让 AI 换 query）；CORS/网络/插入失败→HOST_API_FAILED 可恢复"
    - "'search_and_insert_stock_image' 加入 PPT_TOOLS Set（Phase 14 D-10 casing 归一化守门）；PPT+Word 注册，Excel 不注册（D-11）"
    - "operationLog.integration.test.ts 含 Phase 18 守门：PPT→delete_shape_by_id→rolled_back、Word→noop_inverse→skipped_error（toolName 字面量 'search_and_insert_stock_image' 出现在本文件）"
    - "src/lib/insertImage.ts 已删除（Q1=B 下零运行时调用方）；4 处 stale 注释 wording 已清理（ppt-image.ts/word-image.ts/operationLog.integration.test.ts/PptAdapter.ts）"
  artifacts:
    - path: "src/agent/tools/write/search-stock-image.ts"
      provides: "search_and_insert_stock_image 工具（PPT + Word）"
      exports: ["searchAndInsertStockImagePptTool", "searchAndInsertStockImageWordTool"]
    - path: "src/agent/tools/index.ts"
      provides: "工具注册 + PPT_TOOLS 入集"
      contains: "search_and_insert_stock_image"
    - path: "src/agent/operationLog.integration.test.ts"
      provides: "Phase 18 inverse replay 守门"
      contains: "search_and_insert_stock_image"
  key_links:
    - from: "src/agent/tools/write/search-stock-image.ts"
      to: "src/providers/pexels-client.ts"
      via: "searchPexels() + fetchPexelsImageToBase64()"
      pattern: "searchPexels"
    - from: "src/agent/tools/write/search-stock-image.ts"
      to: "src/providers/registry.ts"
      via: "ProviderRegistry.resolve('stock-image')"
      pattern: "stock-image"
    - from: "src/agent/tools/write/search-stock-image.ts"
      to: "src/adapters/PptAdapter.ts"
      via: "ctx.adapter.addImageShape()"
      pattern: "addImageShape"
    - from: "src/agent/tools/index.ts"
      to: "src/agent/tools/write/search-stock-image.ts"
      via: "buildToolsForHost ppt/word case + PPT_TOOLS Set"
      pattern: "searchAndInsertStockImage"
---

<objective>
Phase 18 的 **agentic 写工具**：`search_and_insert_stock_image` —— loop 内自动检索 Pexels + 选首张 + fetch full-res → 裸 base64 → 直插当前 slide（PPT）/ body（Word），返回 shape_id 供 AI 自主排版，data 携 LIB-03 署名字段。完全照抄 Phase 16 `generate_ppt_image`/`generate_word_image` 的 write-tool 范式（timeoutMs / 三态错误 / reverse descriptor + postState → loop-helpers appendOperation）。

附带 **D-02 reconcile 的收尾**：删除已无调用方的 `src/lib/insertImage.ts`（Q1=B 反转后该手动 appendOperation helper 失去用途），清理 4 处 stale 注释。

**Purpose:** 交付 LIB-02（检索→自动插入）+ LIB-03 的署名数据；undo/host/casing 三重守门。

**Output:** `search-stock-image.ts`（新）+ tools/index.ts 注册 + integration/host 守门测试 + insertImage.ts 删除。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-lib/18-CONTEXT.md
@.planning/phases/18-lib/18-RESEARCH.md
@.planning/phases/18-lib/18-PATTERNS.md

<interfaces>
<!-- executor 直接使用的关键接口（已 file:line 核验 2026-06-03） -->

来自 src/agent/tools/write/ppt-image.ts（**最直接模板**——整文件照抄，仅换数据源）：
```typescript
const IMAGE_GEN_TIMEOUT_MS = 120_000;                                  // L37：覆盖默认 15s
const DEFAULT_IMAGE_POSITION = { left: 120, top: 90, width: 480, height: 360 };  // L40：居中
// execute：ProviderRegistry.resolve(...) → KeyInvalidError→PERMISSION_DENIED（L94-116）
//   → ctx.adapter.addImageShape(slideIndex, base64, DEFAULT_IMAGE_POSITION)（L142-147）
const reverse: ReverseDescriptor = { tool: 'delete_shape_by_id', args: { slide_index: slideIndex, shape_id: newShapeId } };  // L164-167
const postState: PostStateSnapshot = { kind: 'ppt_shape_new', content: { slideIndex, shapeId: newShapeId } };                // L169-172
return { ok: true, data: { shape_id: newShapeId, slide_index: slideIndex, ... }, reverse, postState };                        // L174-186
```

来自 src/agent/tools/write/word-image.ts（Word 范式）：
```typescript
await (ctx.adapter as WordAdapter).insertBodyImage(result.base64);   // L130
const reverse: ReverseDescriptor = { tool: 'noop_inverse', args: { reason: 'Word 图片插入暂不支持自动撤销' } };  // L147-150
return { ok: true, data: { ... }, reverse };  // L152-161（无 postState）
```

来自 src/providers/pexels-client.ts（Plan 18-01 交付）：
```typescript
export async function searchPexels(query, apiKey, baseURL?, opts?): Promise<PexelsPhoto[]>;
export async function fetchPexelsImageToBase64(url, signal?): Promise<string>;  // URL→裸 base64
export interface PexelsPhoto { id; url; photographer; photographer_url; alt; src:{ original; large2x; large; medium; tiny } }
```

来自 src/agent/tools/index.ts（PPT_TOOLS L30-45；buildToolsForHost L254-312；imports L16-17）：
```typescript
import { generatePptImageTool } from './write/ppt-image';   // L16
import { generateWordImageTool } from './write/word-image'; // L17
const PPT_TOOLS = new Set([ /* ...L31-44... */ 'generate_ppt_image' ]);  // L30-45
// buildToolsForHost: case 'word'（L256-273，含 generateWordImageTool L264）
//                    case 'excel'（L274-290，不注册插图工具 — D-11）
//                    case 'ppt'（L291-308，含 generatePptImageTool L299）
```

来自 src/agent/operationLog.integration.test.ts（Phase 16 生图守门块 L1215-1256 = 直接模板）：
```typescript
describe('集成：Phase 16 生图工具 inverse replay 守门', () => {
  it('Phase 16: generate_ppt_image → delete_shape_by_id → rolled_back', async () => {
    mockPpt(''); const adapter = new PptAdapter();
    const entry: OperationLogEntry = { runId:'r16-ppt', stepIndex:0, toolName:'generate_ppt_image', args:{},
      humanLabel:'...', reverse:{ tool:'delete_shape_by_id', args:{ slide_index:1, shape_id:'new-shape-uuid' } },
      postState:{ kind:'ppt_shape_new', content:{ slideIndex:1, shapeId:'new-shape-uuid' } }, timestamp:0 };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('rolled_back');
  });
  it('Phase 16: generate_word_image → noop_inverse → skipped_error', async () => { /* {} adapter → skipped_error */ });
});
// NOTE: mockPpt 已注册 'new-shape-uuid' shape；replayUndoSingle / DocumentAdapterForReplay / __resetOperationLogForTest 已 import
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 src/agent/tools/write/search-stock-image.ts（PPT + Word ToolDef）</name>
  <files>
    src/agent/tools/write/search-stock-image.ts
  </files>
  <read_first>
    - src/agent/tools/write/ppt-image.ts — 整文件（结构、timeoutMs、DEFAULT_IMAGE_POSITION、registry.resolve→KeyInvalidError、addImageShape 调用、reverse+postState、data.shape_id）
    - src/agent/tools/write/word-image.ts — 整文件（insertBodyImage + noop_inverse + 无 postState）
    - src/providers/pexels-client.ts — searchPexels / fetchPexelsImageToBase64 / PexelsPhoto（Plan 18-01）
    - src/providers/registry.ts — stock-image case（Plan 18-01，resolve 抛 KeyInvalidError）
    - src/agent/tools/index.ts — ToolDef / ToolResult / ToolError code 类型（L66-82）
    - src/agent/operationLog.ts — ReverseDescriptor / PostStateSnapshot 类型
    - .planning/phases/18-lib/18-RESEARCH.md — Pattern 2（L283-316）、系统流程图（L127-190）、Pitfall 1-6
    - .planning/phases/18-lib/18-PATTERNS.md — §search-stock-image.ts（L118-239 含 PPT/Word execute 形态 + 关键差异点）
  </read_first>
  <action>
    新建 `src/agent/tools/write/search-stock-image.ts`。导出两个 ToolDef，**共享 name='search_and_insert_stock_image'**（per-host 注册，不冲突），共用一个内部检索/取图 helper。

    **文件头注释**（说明 Q1=B 自动直插 + D-02 不走 insertImage helper + per-host + 安全约束）。

    **常量：**
    ```typescript
    const STOCK_IMAGE_TIMEOUT_MS = 120_000;  // 覆盖默认 15s：Pexels 检索 + full-res CDN fetch 慢（memory browser_image_gen_gotchas）
    const DEFAULT_IMAGE_POSITION = { left: 120, top: 90, width: 480, height: 360 };  // 同 ppt-image.ts 居中
    const PEXELS_LOCALE = 'zh-CN';
    const PEXELS_PER_PAGE = 10;          // D-12
    const STOCK_IMAGE_TOOL_NAME = 'search_and_insert_stock_image';
    ```

    **共享内部 helper**（DRY：PPT/Word 共用「读 key → 检索 → 选首张 → 取 base64」）：
    ```typescript
    type StockFetchOk = { ok: true; photo: PexelsPhoto; base64: string };
    type StockFetchErr = { ok: false; error: ToolError };
    type StockEmpty = { ok: true; empty: true };   // 无结果（让 AI 换 query）

    async function resolveAndFetchStockImage(
      query: string, page: number, signal: AbortSignal | undefined,
    ): Promise<StockFetchOk | StockFetchErr | StockEmpty> {
      // 1. 读 key（registry.resolve 'stock-image'；KeyInvalidError → PERMISSION_DENIED 不可恢复）
      let cfg: ImageConfig;
      try {
        cfg = ProviderRegistry.resolve('stock-image', () => { throw new Error('unused'); }) as ImageConfig;
      } catch (err) {
        if (err instanceof KeyInvalidError) {
          return { ok: false, error: { code: 'PERMISSION_DENIED', message: 'Pexels Key 未配置，请在设置中填写图库 Key', recoverable: false, hint: '前往设置 → 图库 / Pexels API Key' } };
        }
        return { ok: false, error: { code: 'HOST_API_FAILED', message: '图库配置解析失败', recoverable: false, hint: '检查图库 Provider 配置' } };
      }
      // 2. 检索（429→RateLimitError→可恢复；NetworkError→可恢复；不读 err.message 拼 key）
      let photos: PexelsPhoto[];
      try {
        photos = await searchPexels(query, cfg.apiKey, cfg.baseURL, { per_page: PEXELS_PER_PAGE, page, locale: PEXELS_LOCALE, signal });
      } catch (err) {
        if (err instanceof RateLimitError) {
          return { ok: false, error: { code: 'HOST_API_FAILED', message: 'Pexels 检索过于频繁，请稍后再试', recoverable: true, hint: '已达 Pexels 速率上限（200/小时）' } };
        }
        console.error('[search_and_insert_stock_image] 检索失败', err);
        return { ok: false, error: { code: 'HOST_API_FAILED', message: '图库检索失败，请重试', recoverable: true, hint: '网络或 CORS 失败（Office Web iframe）' } };
      }
      // 3. 无结果 → 让 AI 换 query（不算错误）
      if (!photos.length) return { ok: true, empty: true };
      // 4. 选首张（可依 alt 与 query 相关性挑最匹配；最简取 photos[0]）
      const photo = photos[0];
      // 5. full-res（large，避 original）→ 裸 base64（⚠️ CORS 风险面二，Phase 19 UAT）
      let base64: string;
      try {
        base64 = await fetchPexelsImageToBase64(photo.src.large, signal);
      } catch (err) {
        console.error('[search_and_insert_stock_image] 取图失败', err);
        return { ok: false, error: { code: 'HOST_API_FAILED', message: '图库图片获取失败，请重试', recoverable: true, hint: 'images.pexels.com CDN fetch 失败（可能 CORS）' } };
      }
      return { ok: true, photo, base64 };
    }
    ```

    **`searchAndInsertStockImagePptTool: ToolDef`：**
    - name: STOCK_IMAGE_TOOL_NAME；kind:'write'；timeoutMs: STOCK_IMAGE_TIMEOUT_MS
    - description: `'从 Pexels 免费图库检索正版照片并自动插入当前 PPT 幻灯片（居中），返回 shape_id 供后续 move_shape/set_shape_property 排版。query 请用英文关键词（把用户中文意图翻译成英文，如"海边日落"→"seaside sunset"），英文召回质量更好。'`
    - parameters: `{ type:'object', properties:{ query:{type:'string',description:'英文图片检索词（翻译用户意图为英文）'}, slide_index:{type:'number',description:'插入到第几张幻灯片（1开始）。默认 1。'}, page:{type:'number',description:'检索页码（1开始，默认 1；用户说"换一张"时递增翻页）。'} }, required:['query'] }`
    - humanLabel: `(args)=>\`搜索并插入图库图片：${String((args as Record<string,unknown>).query ?? '').slice(0,20)}\``
    - execute:
      - 取 query（缺/空 → INVALID_ARGS 可恢复）；slideIndex = number≥1 ? : 1；page = number≥1 ? : 1
      - `const r = await resolveAndFetchStockImage(query, page, ctx.signal)`
      - `if (!r.ok) return { ok:false, error:r.error }`
      - `if ('empty' in r) return { ok:true, data:{ results:0, query }, }`（无 reverse；让 AI 换 query；描述里说明 results:0 含义）
      - 插入：`const inserted = await (ctx.adapter as PptAdapter).addImageShape(slideIndex, r.base64, DEFAULT_IMAGE_POSITION)`（try/catch → HOST_API_FAILED 可恢复「PPT 图片插入失败，请重试」）
      - reverse/postState（照抄 ppt-image.ts L164-172）：`reverse={tool:'delete_shape_by_id',args:{slide_index:slideIndex,shape_id:inserted.newShapeId}}`；`postState={kind:'ppt_shape_new',content:{slideIndex,shapeId:inserted.newShapeId}}`
      - return `{ ok:true, data:{ shape_id:inserted.newShapeId, slide_index:slideIndex, photographer:r.photo.photographer, photographer_url:r.photo.photographer_url, photo_url:r.photo.url, alt:r.photo.alt, thumbnail_url:r.photo.src.tiny, inserted:true }, reverse, postState }`
      - ⚠️ **data 绝不含 base64**（thumbnail_url 用 photo.src.tiny 远程 URL）

    **`searchAndInsertStockImageWordTool: ToolDef`：**
    - 同 name/kind/timeoutMs；description 改「插入当前 Word 文档末尾」并删 slide_index 语义（保留 query/page）
    - parameters：`{ query(必填), page }`（无 slide_index）
    - execute：同 helper；插入 `await (ctx.adapter as WordAdapter).insertBodyImage(r.base64)`（try/catch → HOST_API_FAILED「Word 图片插入失败，请重试」）
    - reverse = `{ tool:'noop_inverse', args:{ reason:'Word 图片插入暂不支持自动撤销' } }`（无 postState）
    - return `{ ok:true, data:{ photographer, photographer_url, photo_url, alt, thumbnail_url:photo.src.tiny, inserted:true }, reverse }`

    imports：`searchPexels, fetchPexelsImageToBase64, PexelsPhoto` from '../../../providers/pexels-client'；`ProviderRegistry` from registry；`KeyInvalidError, RateLimitError` from '../../../errors'；`ToolDef, ToolResult, ToolError` from '../index'；`ReverseDescriptor, PostStateSnapshot` from '../../operationLog'；`PptAdapter`/`WordAdapter` type；`ImageConfig` from types。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "error" | grep -i "stock" | head</automated>
  </verify>
  <done>
    工具文件存在，build 无类型错误，PPT/Word 双导出，data 无 base64。
    acceptance_criteria:
    - `grep -c "export const searchAndInsertStockImagePptTool" src/agent/tools/write/search-stock-image.ts` 返回 1
    - `grep -c "export const searchAndInsertStockImageWordTool" src/agent/tools/write/search-stock-image.ts` 返回 1
    - `grep -c "search_and_insert_stock_image" src/agent/tools/write/search-stock-image.ts` 返回 ≥1
    - `grep -c "timeoutMs" src/agent/tools/write/search-stock-image.ts` 返回 ≥2
    - `grep -c "delete_shape_by_id" src/agent/tools/write/search-stock-image.ts` 返回 ≥1
    - `grep -c "noop_inverse" src/agent/tools/write/search-stock-image.ts` 返回 ≥1
    - `grep -c "thumbnail_url" src/agent/tools/write/search-stock-image.ts` 返回 ≥2（PPT+Word data）
    - `grep -c "photographer" src/agent/tools/write/search-stock-image.ts` 返回 ≥2（LIB-03）
    - `grep -ci "base64,\|data:image\|result.base64.*data\b" src/agent/tools/write/search-stock-image.ts` —— **不**把 base64 放进任何 data.* 字段（人工核 + 下方 NFR-09 守门兜底）
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
  </done>
</task>

<task type="auto">
  <name>Task 2: tools/index.ts 注册（PPT_TOOLS Set + buildToolsForHost ppt/word；Excel 不注册）</name>
  <files>
    src/agent/tools/index.ts
  </files>
  <read_first>
    - src/agent/tools/index.ts — imports（L16-17）、PPT_TOOLS Set（L30-45）、normalizeToSnakeCase 用 PPT_TOOLS（L209-210）、buildToolsForHost word/excel/ppt（L254-312）
    - src/agent/tools/write/search-stock-image.ts — 两个导出（Task 1）
    - .planning/phases/18-lib/18-PATTERNS.md — §tools/index.ts（L423-466 精确锚点）
  </read_first>
  <action>
    1. **import**（L17 后追加）：
       ```typescript
       import { searchAndInsertStockImagePptTool, searchAndInsertStockImageWordTool } from './write/search-stock-image';
       ```
    2. **PPT_TOOLS Set**（在 'generate_ppt_image' 行 L44 后追加，casing 归一化守门 Phase 14 D-10）：
       ```typescript
       'search_and_insert_stock_image', // Phase 18 LIB-02（PPT 必须在此，否则 LLM camelCase 参数不被 normalize）
       ```
    3. **buildToolsForHost ppt case**（在 generatePptImageTool L299 之后追加）：
       ```typescript
       searchAndInsertStockImagePptTool, // Phase 18 LIB-02（PPT 图库检索插入）
       ```
    4. **buildToolsForHost word case**（在 generateWordImageTool L264 之后追加）：
       ```typescript
       searchAndInsertStockImageWordTool, // Phase 18 LIB-02（Word 图库检索插入）
       ```
    5. **excel case 不改**（D-11：Excel 无原生插图 API，诚实不注册）。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "error" | head; npm test -- --run src/agent/tools/index.test.ts src/agent/tools/dispatch.test.ts</automated>
  </verify>
  <done>
    工具在 PPT_TOOLS + ppt/word host 注册，Excel 不含。
    acceptance_criteria:
    - `grep -c "search_and_insert_stock_image" src/agent/tools/index.ts` 返回 ≥1（PPT_TOOLS 入集）
    - `grep -c "searchAndInsertStockImagePptTool" src/agent/tools/index.ts` 返回 ≥2（import + ppt case）
    - `grep -c "searchAndInsertStockImageWordTool" src/agent/tools/index.ts` 返回 ≥2（import + word case）
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
    - `npm test -- --run src/agent/tools/index.test.ts src/agent/tools/dispatch.test.ts` 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 3: tools-host.test.ts 追加 per-host 守门（PPT/Word 含、Excel 不含）</name>
  <files>
    src/agent/tools/tools-host.test.ts
  </files>
  <read_first>
    - src/agent/tools/tools-host.test.ts — 现有 IMG-05 守门结构（Excel 不含 generate_*；PPT/Word 含 generate_*）
  </read_first>
  <action>
    在现有 describe 块内追加 3 条断言（仿现有 generate_* 写法）：
    ```typescript
    it('Excel host 工具表不含 search_and_insert_stock_image（D-11）', () => {
      expect(buildToolsForHost('excel').map((t) => t.name)).not.toContain('search_and_insert_stock_image');
    });
    it('PPT host 工具表含 search_and_insert_stock_image（LIB-02）', () => {
      expect(buildToolsForHost('ppt').map((t) => t.name)).toContain('search_and_insert_stock_image');
    });
    it('Word host 工具表含 search_and_insert_stock_image（LIB-02）', () => {
      expect(buildToolsForHost('word').map((t) => t.name)).toContain('search_and_insert_stock_image');
    });
    ```
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/tools-host.test.ts</automated>
  </verify>
  <done>
    per-host 守门全绿。
    acceptance_criteria:
    - `grep -c "search_and_insert_stock_image" src/agent/tools/tools-host.test.ts` 返回 ≥3
    - `npm test -- --run src/agent/tools/tools-host.test.ts` 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 4: operationLog.integration.test.ts 追加 Phase 18 inverse replay 守门</name>
  <files>
    src/agent/operationLog.integration.test.ts
  </files>
  <read_first>
    - src/agent/operationLog.integration.test.ts — Phase 16 生图守门块（L1215-1256，整块照抄改 toolName）；顶部 import（replayUndoSingle / DocumentAdapterForReplay / OperationLogEntry / PptAdapter / mockPpt / __resetOperationLogForTest）；D-17 字面量扫描说明（L376, L644-645）
    - .planning/phases/18-lib/18-PATTERNS.md — §operationLog 守门（L483-524）
  </read_first>
  <action>
    在 Phase 16 生图守门 describe 块（L1256 `});` 结束）之后，新增一个 describe 块（memory adapter_inverse_signature 铁律：新 inverse 必补 integration test）：

    ```typescript
    // ---------------------------------------------------------------------------
    // Phase 18 LIB-02 守门：search_and_insert_stock_image inverse 路径
    // （PPT delete_shape_by_id rolled_back / Word noop_inverse skipped_error）
    // toolName 字面量 'search_and_insert_stock_image' 必须出现在本文件（D-17 扫描）
    // ---------------------------------------------------------------------------
    describe('集成：Phase 18 图库工具 inverse replay 守门', () => {
      afterEach(() => __resetOperationLogForTest());

      it('Phase 18: search_and_insert_stock_image (PPT) → delete_shape_by_id → rolled_back', async () => {
        mockPpt('');
        const adapter = new PptAdapter();
        const entry: OperationLogEntry = {
          runId: 'r18-ppt', stepIndex: 0,
          toolName: 'search_and_insert_stock_image',
          args: {},
          humanLabel: '搜索并插入图库图片到第 1 页',
          reverse: { tool: 'delete_shape_by_id', args: { slide_index: 1, shape_id: 'new-shape-uuid' } },
          postState: { kind: 'ppt_shape_new', content: { slideIndex: 1, shapeId: 'new-shape-uuid' } },
          timestamp: 0,
        };
        const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
        expect(detail.status).toBe('rolled_back');
      });

      it('Phase 18: search_and_insert_stock_image (Word) → noop_inverse → skipped_error', async () => {
        const entry: OperationLogEntry = {
          runId: 'r18-word', stepIndex: 0,
          toolName: 'search_and_insert_stock_image',
          args: {},
          humanLabel: '搜索并插入图库图片到 Word 文档',
          reverse: { tool: 'noop_inverse', args: { reason: 'Word 图片插入暂不支持自动撤销' } },
          timestamp: 0,
        };
        const detail = await replayUndoSingle(entry, {} as DocumentAdapterForReplay);
        expect(detail.status).toBe('skipped_error');
      });
    });
    ```
    （若 mockPpt 的 'new-shape-uuid' 注册与 Phase 16 块共用且 afterEach reset 影响，确保本块自带 mockPpt('') 与 Phase 16 一致——照抄即可。）
  </action>
  <verify>
    <automated>npm test -- --run src/agent/operationLog.integration.test.ts</automated>
  </verify>
  <done>
    Phase 18 守门两条通过；全文件无回归。
    acceptance_criteria:
    - `grep -c "search_and_insert_stock_image" src/agent/operationLog.integration.test.ts` 返回 ≥2
    - `grep -c "Phase 18: search_and_insert_stock_image (PPT)" src/agent/operationLog.integration.test.ts` 返回 1
    - `npm test -- --run src/agent/operationLog.integration.test.ts` 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 5: search-stock-image.test.ts 单测 + chat.test.ts NFR-09 序列化守门（路径 D）</name>
  <files>
    src/agent/tools/write/search-stock-image.test.ts
    src/store/chat.test.ts
  </files>
  <read_first>
    - src/agent/tools/write/ppt-image.test.ts — mock 结构（mock ctx.adapter.addImageShape、mock registry/storage、断言 reverse/data/三态错误）
    - src/agent/tools/write/word-image.test.ts — Word mock 结构
    - src/agent/tools/write/search-stock-image.ts — 被测 ToolDef（Task 1）
    - src/store/chat.test.ts — NFR-09 路径 C（L368-398，generate 图工具 serialize 守门模板）；serializeForStorage 仅存 user/assistant text role（tool role 整条丢弃）
    - src/store/chat.ts — serializeForStorage 白名单（L122-135，现有，不需改）
    - .planning/phases/18-lib/18-RESEARCH.md — Validation Architecture Test Map（L585-598）+ Wave 0 Gaps 路径 D（L623）
  </read_first>
  <action>
    新建 `src/agent/tools/write/search-stock-image.test.ts`。mock `../../../providers/pexels-client`（searchPexels / fetchPexelsImageToBase64）+ `../../../providers/registry`（或直接 storage.set/remove PEXELS_API_KEY）+ mock ctx.adapter。覆盖：

    **PPT 工具：**
    1. 无 Pexels key（storage 无 key → registry 抛 KeyInvalidError）→ execute 返回 `{ ok:false, error:{ code:'PERMISSION_DENIED', recoverable:false } }`。
    2. 正常路径（mock searchPexels 返回 1 张 PexelsPhoto、fetchPexelsImageToBase64 返回 'QUJD'、mock adapter.addImageShape 返回 {newShapeId:'sid-1'}）→ 断言：
       - `result.reverse.tool === 'delete_shape_by_id'`，`result.reverse.args` 为 `{ slide_index:1, shape_id:'sid-1' }`（Record，snake_case）
       - `result.postState.kind === 'ppt_shape_new'`，content `{ slideIndex:1, shapeId:'sid-1' }`
       - `result.data.shape_id === 'sid-1'`、`result.data.photographer` 存在、`result.data.thumbnail_url` 是 photo.src.tiny（URL 字符串）
       - **NFR-09 守门**：`JSON.stringify(result.data)` 不含 base64 'QUJD'（断言 `expect(JSON.stringify(result.data)).not.toContain('QUJD')`）
    3. 无结果（mock searchPexels 返回 []）→ `result.ok === true && result.data.results === 0`，且 `result.reverse` 为 undefined。
    4. 429（mock searchPexels reject RateLimitError）→ `{ ok:false, error:{ code:'HOST_API_FAILED', recoverable:true } }`。

    **Word 工具：**
    5. 正常路径（mock insertBodyImage 成功）→ `result.reverse.tool === 'noop_inverse'`；`result.data.photographer` 存在；`JSON.stringify(result.data)` 不含 base64。

    **chat.test.ts — NFR-09 序列化守门（路径 D，RESEARCH Wave 0 Gap L623）：**
    6. 仿 `src/store/chat.test.ts` 路径 C（L368-398）追加路径 D：构造一条 role='tool' 的 search_and_insert_stock_image 结果消息（data 含 `thumbnail_url`=URL 字符串、`photographer`、`inserted:true`，**无 base64**）→ 调 `serializeForStorage`（或 saveHistory 路径）→ 断言序列化结果**不含该 tool 消息**（tool role 被白名单丢弃）且 localStorage 内容不含任何 base64 模式。这是结构性守门（memory feedback_recurring_failure_add_gate）：即使将来误把 base64 塞进 data，serialize 层仍拦住。
  </action>
  <verify>
    <automated>npm test -- --run src/agent/tools/write/search-stock-image.test.ts src/store/chat.test.ts</automated>
  </verify>
  <done>
    工具单测 + chat serialize 守门全绿，含 NFR-09 双层断言。
    acceptance_criteria:
    - `grep -c "PERMISSION_DENIED" src/agent/tools/write/search-stock-image.test.ts` 返回 ≥1
    - `grep -c "delete_shape_by_id" src/agent/tools/write/search-stock-image.test.ts` 返回 ≥1
    - `grep -c "noop_inverse" src/agent/tools/write/search-stock-image.test.ts` 返回 ≥1
    - `grep -c "not.toContain" src/agent/tools/write/search-stock-image.test.ts` 返回 ≥1（NFR-09 无 base64）
    - `grep -c "search_and_insert_stock_image\|路径 D\|thumbnail_url" src/store/chat.test.ts` 返回 ≥1（serialize 守门路径 D）
    - `npm test -- --run src/agent/tools/write/search-stock-image.test.ts src/store/chat.test.ts` 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 6: 删除 src/lib/insertImage.ts + 清理 4 处 stale 注释（D-02 收尾）</name>
  <files>
    src/lib/insertImage.ts
    src/agent/tools/write/ppt-image.ts
    src/agent/tools/write/word-image.ts
    src/adapters/PptAdapter.ts
  </files>
  <read_first>
    - src/lib/insertImage.ts — 待删文件（确认无 import 调用方）
    - src/agent/tools/write/ppt-image.ts — L12 注释「不走 16-02 insertImage helper...」
    - src/agent/tools/write/word-image.ts — L10 注释「不走 16-02 insertImage helper...」
    - src/adapters/PptAdapter.ts — L1657 `@returns` JSDoc「供 insertImage helper 写 reverse.args.shape_id」
    - .planning/phases/18-lib/18-PATTERNS.md — §DELETE insertImage.ts（L469-479）
  </read_first>
  <action>
    **执行前复核（硬门）**：先跑 `git grep -n "from '.*insertImage'\|import.*insertImage\|insertImage(" -- 'src/**'`，确认**无任何 import/调用**（只剩注释/JSDoc）。若发现真实调用方，**停止本 task** 并在 SUMMARY 标注，改为保留文件不删（避免破坏运行时）。team-lead 已 grep 确认零调用方（2026-06-02），此为执行时二次防御。

    确认无调用方后：
    1. **删除** `src/lib/insertImage.ts`（`git rm src/lib/insertImage.ts` 或直接删文件）。
    2. **ppt-image.ts L10-13** 头注释：把「不走 16-02 insertImage helper 的手动 appendOperation，避免 loop 内重复记录 / stepIndex 冲突。」改为中性表述：「execute 返回 reverse descriptor，loop-helpers 据此 appendOperation（单一 undo 记录），无需手动追加。」（删除对已不存在文件的指代）
    3. **word-image.ts L10** 同理：把「不走 16-02 insertImage helper 的手动记录。」改为「loop-helpers 据 reverse 自动 appendOperation。」
    4. **operationLog.integration.test.ts L1235** 注释「此处 camelCase 必须与 16-02 insertImage helper 真实写入的 content 形状一致」→ 改为「此处 camelCase 必须与生图工具 execute 返回的 postState.content 形状一致」。
    5. **PptAdapter.ts L1657** `@returns { newShapeId } 新插入 shape 的 ID，供 insertImage helper 写 reverse.args.shape_id` → 改为「@returns { newShapeId } 新插入 shape 的 ID，供 write 工具写 reverse.args.shape_id（delete_shape_by_id）」。

    全部为注释/wording 改动 + 1 文件删除，**不动任何运行时逻辑**。
  </action>
  <verify>
    <automated>test ! -f src/lib/insertImage.ts && echo DELETED; git grep -c "insertImage" -- 'src/**' || echo "no insertImage refs"; npm run build 2>&1 | grep -iE "error" | head</automated>
  </verify>
  <done>
    insertImage.ts 已删，src/ 下无 insertImage 残留引用，build + 全量测试无回归。
    acceptance_criteria:
    - `test -f src/lib/insertImage.ts && echo EXISTS || echo GONE` 返回 GONE
    - `git grep -c "insertImage" -- 'src/**'` 返回 0（或命令无输出 = 无匹配）
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
    - `npm test -- --run` 退出 0（全量无回归，尾部 retry noise 除外）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 工具 execute → adapter | base64 仅作 addImageShape/insertBodyImage 入参，不写持久存储、不进 ToolResult.data |
| 工具 execute → ToolResult.data | data 只含 URL 字符串/元数据（shape_id、photographer、thumbnail_url）；绝不含 base64（NFR-09） |
| 工具 execute → operationLog | reverse.args 只含 slide_index + shape_id（Office 内部 UUID，低价值）；不含 base64 |
| Office.js 写操作 → PPT/Word | 复用 Phase 16 已实测 adapter（写后回读已在 adapter 内） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Information Disclosure | ToolResult.data base64 泄漏（NFR-09） | mitigate | data 用 thumbnail_url=photo.src.tiny（远程 URL），不放 base64；Task 5 单测 `not.toContain(base64)` 守门；且 serializeForStorage 仅存 user/assistant text role（tool role 整条丢弃，chat.ts L122-135） |
| T-18-03 | Information Disclosure | Pexels apiKey 进 chat/LLM 上下文 | mitigate | apiKey 只在 resolveAndFetchStockImage 内部 registry→searchPexels 流转，绝不进 ToolResult.data / Message.content |
| T-18-05 | Tampering / Spoofing | 自动选首张插入未经确认 | accept | Q1=B 产品决策：loop 内自动直插（同 Phase 16）；用户可「换一张」翻页或 undo（PPT delete_shape_by_id）；Word 诚实标不可自动撤销 |
| T-18-06 | Denial of Service | 慢 fetch 误杀 / 卡 loop | mitigate | timeoutMs=120_000 覆盖默认 15s（memory browser_image_gen_gotchas）；选 large 尺寸避 original 数 MB（撑 P95） |
</threat_model>

<verification>
- `npm run build` 无 TypeScript 错误
- `npm test -- --run` 全量通过（含 Phase 18 守门、tools-host、search-stock-image、chat serialize 路径 D、operationLog.integration）
- `git grep -c "insertImage" -- 'src/**'` 返回 0（insertImage 彻底移除）
- `npm run build && npm run size` —— main-*.js ≤82KB gzip（先 build 再 size；memory bundle_size_guard）。若超门：把 search-stock-image.ts 内 Pexels client import 改为动态 `await import()`（首次调用工具时才加载），或与 18-03 协调懒加载策略
</verification>

<success_criteria>
1. search-stock-image.ts 存在：PPT+Word 双 ToolDef，loop 内自动检索→选首张→fetch→插入→返回 shape_id；走标准 reverse 路径（不调 insertImage helper）
2. PPT reverse=delete_shape_by_id（Record snake_case）+ postState=ppt_shape_new(camelCase)；Word reverse=noop_inverse
3. data 携 LIB-03 署名（photographer/photographer_url/photo_url/thumbnail_url）；data 无 base64（NFR-09）
4. 三态错误齐全（D-13）；description 指引英文 query（D-04）；timeoutMs=120_000
5. 'search_and_insert_stock_image' 入 PPT_TOOLS + PPT/Word 注册 + Excel 不注册（D-11）
6. operationLog.integration + tools-host + search-stock-image 守门全绿
7. insertImage.ts 删除 + 4 处 stale 注释清理；build + 全量测试无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/18-lib/18-02-SUMMARY.md`，记录：
- 工具命名最终值（search_and_insert_stock_image）+ PPT/Word 是否共享名（是，per-host 注册）
- 选首张策略（photos[0] vs alt 相关性）
- 「换一张」最终实现（page 递增翻页）
- insertImage.ts 删除前 git grep 复核结论（零调用方确认）
- bundle size build 后实测值（是否需懒加载）
</output>

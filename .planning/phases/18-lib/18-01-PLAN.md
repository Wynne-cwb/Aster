---
phase: 18-lib
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storage.ts
  - src/providers/pexels-client.ts
  - src/providers/registry.ts
  - src/providers/types.ts
  - src/providers/pexels-client.test.ts
  - src/providers/registry.test.ts
autonomous: true
requirements:
  - LIB-01

must_haves:
  truths:
    - "src/providers/pexels-client.ts 存在：searchPexels(query, apiKey, baseURL, opts) 用 `Authorization: <apiKey>`（裸 key，无 Bearer 前缀）调 GET /v1/search，返回 PexelsPhoto[]"
    - "pexels-client 鉴权绝不用 Bearer 前缀（区别于 aihubmix/openai sse.ts 的 `Bearer ${key}`）——这是 401 的头号坑（D-10 gotcha）"
    - "fetchPexelsImageToBase64(url, signal) 把远程图片 URL → 裸 base64（无 data: 前缀，与 addImageShape/insertBodyImage 入参一致）"
    - "apiKey 仅进 Authorization header，绝不进 request body、绝不 interpolate 进 error.message（T-14-01 继承）"
    - "registry.ts 的 'stock-image' case 已填实：读 STORAGE_KEYS.PEXELS_API_KEY，缺失 → throw KeyInvalidError（不再 throw ModelNotFoundError stub）；返回 ImageConfig { providerId:'pexels', baseURL, apiKey, model:'' }"
    - "baseURL 可配：registry 读可选 STORAGE_KEYS.PEXELS_BASE_URL override，缺省回退 PEXELS_DEFAULT_BASE_URL='https://api.pexels.com/v1'（Cloudflare Worker 兜底切换口，默认纯直连）"
    - "STORAGE_KEYS.PEXELS_API_KEY = 'aster:keys:pexels'（沿用 KEY_PREFIX='aster:keys:' 约定）"
    - "0 净新增运行时依赖：native fetch，不装 pexels / unsplash-js npm 包"
  artifacts:
    - path: "src/providers/pexels-client.ts"
      provides: "Pexels REST client（search + URL→base64）"
      exports: ["searchPexels", "fetchPexelsImageToBase64", "PexelsPhoto", "PexelsSearchOpts", "PEXELS_DEFAULT_BASE_URL"]
    - path: "src/lib/storage.ts"
      provides: "PEXELS_API_KEY / PEXELS_BASE_URL storage keys"
      contains: "PEXELS_API_KEY"
    - path: "src/providers/registry.ts"
      provides: "stock-image case 填实"
      contains: "stock-image"
  key_links:
    - from: "src/providers/registry.ts"
      to: "src/lib/storage.ts"
      via: "storage.get(STORAGE_KEYS.PEXELS_API_KEY)"
      pattern: "PEXELS_API_KEY"
    - from: "src/providers/registry.ts"
      to: "src/errors/index.ts"
      via: "throw new KeyInvalidError(...)"
      pattern: "KeyInvalidError"
    - from: "src/providers/pexels-client.ts"
      to: "src/errors/index.ts"
      via: "throw new RateLimitError / NetworkError"
      pattern: "RateLimitError"
---

<objective>
Phase 18 的 **provider 接线基础层**：新建 Pexels REST client（裸 key 鉴权 + URL→base64）、新增 BYO Pexels key 存储约定、填实 registry `stock-image` 路由。这是 Plan 18-02（图库工具）与 18-03（Settings 字段）共同依赖的地基。

**Purpose:** 提供「读 key → 检索 Pexels → 取图转 base64」的纯数据/路由能力，0 净新增运行时依赖，UI 与 agent 工具层在 Wave 2 接入。

**Output:** `src/providers/pexels-client.ts`（新）+ `STORAGE_KEYS.PEXELS_API_KEY`/`PEXELS_BASE_URL` + registry `stock-image` case 填实 + 单测。

**实现 LIB-01 的检索基础设施**（Settings 输入框 UI 在 18-03；自动插入工具在 18-02）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-lib/18-CONTEXT.md
@.planning/phases/18-lib/18-RESEARCH.md
@.planning/phases/18-lib/18-PATTERNS.md

<interfaces>
<!-- executor 直接使用的关键接口（已 file:line 核验 2026-06-03） -->

来自 src/providers/types.ts（ImageConfig，L45-50；TaskKind L24 已含 'stock-image'）：
```typescript
export type TaskKind = 'chat' | 'short-task' | 'vision' | 'image-gen' | 'stock-image';
export interface ImageConfig {
  providerId: string;
  baseURL: string;
  apiKey: string;
  model: string;
}
```

来自 src/providers/registry.ts（vision case L112-123 = key 读取范式；stock-image stub L142-143 = 填实点）：
```typescript
case 'vision': {
  const apiKey = storage.get<string>(STORAGE_KEYS.KEY_PREFIX + AIHUBMIX_PROVIDER_ID);
  if (!apiKey) {
    throw new KeyInvalidError('aihubmix Key 未配置，请在设置中填写 aihubmix Key');
  }
  return { providerId: `${AIHUBMIX_PROVIDER_ID}-vision`, baseURL: AIHUBMIX_BASE_URL, apiKey, model: AIHUBMIX_VISION_MODEL } satisfies ImageConfig;
}
// ...
case 'stock-image':
  throw new ModelNotFoundError('stock-image Provider 未配置（v1 不含图库）');  // ← L142-143 替换点
```

来自 src/lib/storage.ts（STORAGE_KEYS L19-47；KEY_PREFIX L23；末尾 PREF_IMAGE_GEN_MODEL L46 + `} as const` L47）：
```typescript
export const STORAGE_KEYS = {
  KEY_PREFIX: 'aster:keys:',
  // ...
  PREF_IMAGE_GEN_MODEL: 'aster:pref:image-gen-model',
} as const;
// storage.get<T>(rawKey) / storage.set(rawKey, value) / storage.remove(rawKey) — partition 自动注入
```

来自 src/errors/index.ts（全部已存在，import 路径 '../errors'）：
```typescript
export class KeyInvalidError extends AsterError {}    // L63
export class NetworkError extends AsterError {}       // L102
export class RateLimitError extends AsterError {}     // L118
```

来自 src/providers/aihubmix-image.ts（鉴权对照——勿照抄 Bearer）：
```typescript
// aihubmix-image.ts 用 Authorization: `Bearer ${apiKey}` —— Pexels 必须去掉 Bearer！
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: storage.ts 新增 PEXELS_API_KEY + PEXELS_BASE_URL</name>
  <files>
    src/lib/storage.ts
  </files>
  <read_first>
    - src/lib/storage.ts — STORAGE_KEYS 定义（L19-47），KEY_PREFIX 约定（L23），末尾 PREF_IMAGE_GEN_MODEL（L46）+ `} as const`（L47）；get/set/remove 实现（L69-100）
    - .planning/phases/18-lib/18-PATTERNS.md — §storage.ts 新增范式（L307-325）
  </read_first>
  <action>
    在 `STORAGE_KEYS` 对象内 `PREF_IMAGE_GEN_MODEL` 行（L46）之后、`} as const`（L47）之前追加两个 key：

    ```typescript
      /** Phase 18 LIB-01（D-08）：用户 BYO Pexels API Key（string）。
       *  registry 'stock-image' resolve 读此 key；缺失 → KeyInvalidError（引导去 Settings）。
       *  沿用 KEY_PREFIX='aster:keys:' 约定（这是 key，非 pref）。 */
      PEXELS_API_KEY: 'aster:keys:pexels',
      /** Phase 18 LIB-01（D-09）：可选 Pexels baseURL override（Cloudflare Worker 兜底切换口）。
       *  缺省时 registry 回退 PEXELS_DEFAULT_BASE_URL（纯浏览器直连）。无 UI，仅 CORS 失败后手动/Worker 切换用。 */
      PEXELS_BASE_URL: 'aster:config:pexels-base-url',
    ```

    不改动 get/set/remove 实现。
  </action>
  <verify>
    <automated>grep -c "PEXELS_API_KEY\|PEXELS_BASE_URL" src/lib/storage.ts</automated>
  </verify>
  <done>
    STORAGE_KEYS 含两个新 key。
    acceptance_criteria:
    - `grep -c "PEXELS_API_KEY: 'aster:keys:pexels'" src/lib/storage.ts` 返回 1
    - `grep -c "PEXELS_BASE_URL: 'aster:config:pexels-base-url'" src/lib/storage.ts` 返回 1
  </done>
</task>

<task type="auto">
  <name>Task 2: 新建 src/providers/pexels-client.ts（裸 key 鉴权 + URL→base64，0 SDK）</name>
  <files>
    src/providers/pexels-client.ts
  </files>
  <read_first>
    - src/providers/aihubmix-image.ts — class 结构 + fetch + signal 透传 + 安全注释（apiKey 仅进 header）；⚠️ 其 `Authorization: Bearer ${apiKey}` 是 Pexels 要避开的反例
    - src/errors/index.ts — NetworkError（L102）、RateLimitError（L118）构造签名
    - .planning/phases/18-lib/18-RESEARCH.md — Pattern 1 完整 client 代码（L211-281）、Pexels API Exact Contract（L384-461）、Pitfall 1/2/3/5（L503-528）
    - .planning/phases/18-lib/18-PATTERNS.md — §pexels-client.ts 鉴权 gotcha 对照（L68-95）+ PexelsPhoto 接口（L99-114）
  </read_first>
  <action>
    新建 `src/providers/pexels-client.ts`。native fetch，**0 净新增运行时依赖**（不装 pexels 包）。

    ```typescript
    /**
     * src/providers/pexels-client.ts — Pexels 公开图库 REST client（LIB-01）
     *
     * 🔴 鉴权 gotcha（D-10）：Pexels 用 `Authorization: <API_KEY>`（裸 key，无 "Bearer " 前缀）——
     *   区别于 aihubmix/openai 的 `Authorization: Bearer <key>`。照抄 Bearer 范式会得 401。
     * 安全（T-14-01 继承）：apiKey 仅进 Authorization header，不进 body、不 interpolate 进 error.message。
     * 0 净新增运行时依赖：native fetch + FileReader。
     */
    import { NetworkError, RateLimitError } from '../errors';

    export const PEXELS_DEFAULT_BASE_URL = 'https://api.pexels.com/v1';

    export interface PexelsPhoto {
      id: number;
      url: string;                  // Pexels 图片页（署名链接 LIB-03）
      photographer: string;         // 署名（LIB-03）
      photographer_url: string;     // 署名链接（LIB-03）
      alt: string;                  // AI 可据此选最匹配（D-01）
      src: {
        original: string;
        large2x: string;            // ~1880px
        large: string;              // ~940px，插图主路推荐
        medium: string;
        tiny: string;               // ~280px，缩略图（<img src>，不需 base64）
      };
    }

    export interface PexelsSearchOpts {
      per_page?: number;            // 默认 10（D-12）
      page?: number;                // 默认 1（翻页 = 换一张 D-05）
      locale?: string;              // 'zh-CN'（影响元数据，不影响英文关键词匹配 D-04）
      orientation?: 'landscape' | 'portrait' | 'square';
      signal?: AbortSignal;
    }

    /** GET /v1/search — 裸 key 鉴权。429 → RateLimitError；网络失败 → NetworkError；其它非 2xx → NetworkError（不泄漏 key）。 */
    export async function searchPexels(
      query: string,
      apiKey: string,
      baseURL: string = PEXELS_DEFAULT_BASE_URL,
      opts: PexelsSearchOpts = {},
    ): Promise<PexelsPhoto[]> {
      const params = new URLSearchParams({
        query,                                  // URLSearchParams 自动编码（V5 输入校验）
        per_page: String(opts.per_page ?? 10),
        page: String(opts.page ?? 1),
        ...(opts.locale ? { locale: opts.locale } : {}),
        ...(opts.orientation ? { orientation: opts.orientation } : {}),
      });
      let resp: Response;
      try {
        resp = await fetch(`${baseURL}/search?${params.toString()}`, {
          headers: { Authorization: apiKey },   // ⚠️ 裸 key，无 Bearer（D-10）
          signal: opts.signal,
        });
      } catch {
        throw new NetworkError('Pexels 检索网络失败');   // 字面量，不带 err（防 key 泄漏）
      }
      if (resp.status === 429) {
        throw new RateLimitError('Pexels 检索过于频繁，请稍后再试');
      }
      if (!resp.ok) {
        throw new NetworkError('Pexels 检索失败');       // 不 interpolate status/body
      }
      const data = (await resp.json()) as { photos?: PexelsPhoto[] };
      return data.photos ?? [];
    }

    /** 远程图片 URL → 裸 base64（无 data: 前缀，喂 addImageShape/insertBodyImage）。透传 signal。 */
    export async function fetchPexelsImageToBase64(url: string, signal?: AbortSignal): Promise<string> {
      let resp: Response;
      try {
        resp = await fetch(url, { signal });
      } catch {
        throw new NetworkError('Pexels 图片获取失败');
      }
      if (!resp.ok) throw new NetworkError('Pexels 图片获取失败');
      const blob = await resp.blob();
      return new Promise<string>((resolve, reject) => {
        const reader = new FileReader();
        reader.onload = () => resolve((reader.result as string).split(',')[1] ?? '');  // 去掉 data: 前缀 → 裸 base64
        reader.onerror = () => reject(new NetworkError('Pexels 图片 base64 转换失败'));
        reader.readAsDataURL(blob);
      });
    }
    ```

    严格遵守：① `Authorization: apiKey` 不加 Bearer；② 所有 error.message 是中文字面量、不 interpolate err / status / body（T-14-01）；③ `signal` 透传两个 fetch；④ 不 import 任何新 npm 包。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "error" | grep -i "pexels" | head; grep -c "Bearer" src/providers/pexels-client.ts</automated>
  </verify>
  <done>
    pexels-client.ts 存在且 build 无类型错误；鉴权无 Bearer。
    acceptance_criteria:
    - `grep -c "Authorization: apiKey" src/providers/pexels-client.ts` 返回 ≥1
    - `grep -c "Bearer" src/providers/pexels-client.ts` 返回 0
    - `grep -c "export async function searchPexels" src/providers/pexels-client.ts` 返回 1
    - `grep -c "export async function fetchPexelsImageToBase64" src/providers/pexels-client.ts` 返回 1
    - `grep -c "split(','" src/providers/pexels-client.ts` 返回 ≥1（裸 base64）
    - `grep -c "from 'pexels'\|unsplash-js" src/providers/pexels-client.ts` 返回 0（0 新依赖）
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
  </done>
</task>

<task type="auto">
  <name>Task 3: registry.ts 填实 stock-image case（读 key / KeyInvalidError / 可配 baseURL）+ types.ts 注释订正</name>
  <files>
    src/providers/registry.ts
    src/providers/types.ts
  </files>
  <read_first>
    - src/providers/registry.ts — vision case（L112-123）、image-gen case（L125-140）= key 读取 + KeyInvalidError + 返 ImageConfig 范式；stock-image stub（L142-143）= 替换点；imports（L18 ModelNotFoundError/KeyInvalidError）
    - src/lib/storage.ts — STORAGE_KEYS.PEXELS_API_KEY / PEXELS_BASE_URL（Task 1 新增）
    - src/providers/pexels-client.ts — PEXELS_DEFAULT_BASE_URL（Task 2 导出）
    - src/providers/types.ts — TaskKind 注释（L22「'stock-image': 暂未配置...抛 ModelNotFoundError」需订正）、ImageConfig（L45）
    - .planning/phases/18-lib/18-PATTERNS.md — §registry stock-image 填实形态（L280-303）
  </read_first>
  <action>
    **registry.ts** — 把 L142-143 的 stub：
    ```typescript
    case 'stock-image':
      throw new ModelNotFoundError('stock-image Provider 未配置（v1 不含图库）');
    ```
    替换为（仿 vision case，不含 model override；baseURL 可配）：
    ```typescript
    case 'stock-image': {
      // D-09：读 BYO Pexels Key，缺失 → KeyInvalidError（UI 层展示气泡引导去 Settings）
      const apiKey = storage.get<string>(STORAGE_KEYS.PEXELS_API_KEY);
      if (!apiKey) {
        throw new KeyInvalidError('Pexels Key 未配置，请在设置中填写图库 Key');
      }
      // D-09：baseURL 可配（Cloudflare Worker 兜底切换口）；默认纯浏览器直连
      const baseURL = storage.get<string>(STORAGE_KEYS.PEXELS_BASE_URL) ?? PEXELS_DEFAULT_BASE_URL;
      return {
        providerId: 'pexels',
        baseURL,
        apiKey,
        model: '',  // Pexels 无 model 概念，占位
      } satisfies ImageConfig;
    }
    ```
    在 registry.ts 顶部 import 区加：`import { PEXELS_DEFAULT_BASE_URL } from './pexels-client';`
    若替换后 `ModelNotFoundError` 在本文件不再被任何 case 使用，保留 import（其它路径 L90 注释/未知 taskKind 仍可能用）——执行时按 build 警告处理，**不要**删到 build 报 unused。

    **types.ts** — 把 L22 的 TaskKind 注释「'stock-image': 暂未配置（v1 未引入图库 Provider），resolve 直接抛 ModelNotFoundError」订正为：「'stock-image': Pexels 图库检索（Phase 18 LIB），resolve 读 PEXELS_API_KEY，缺失抛 KeyInvalidError」。仅改注释，不改 TaskKind 类型本身（已含 'stock-image'）。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "error" | head; grep -n "case 'stock-image'" src/providers/registry.ts</automated>
  </verify>
  <done>
    stock-image case 填实，build 无类型错误。
    acceptance_criteria:
    - `grep -c "STORAGE_KEYS.PEXELS_API_KEY" src/providers/registry.ts` 返回 ≥1
    - `grep -c "new KeyInvalidError('Pexels Key 未配置" src/providers/registry.ts` 返回 1
    - `grep -c "PEXELS_DEFAULT_BASE_URL" src/providers/registry.ts` 返回 ≥1
    - `grep -c "ModelNotFoundError('stock-image" src/providers/registry.ts` 返回 0（stub 已移除）
    - `npm run build 2>&1 | grep -ci "error"` 返回 0
  </done>
</task>

<task type="auto">
  <name>Task 4: 单测 — pexels-client.test.ts（新）+ registry.test.ts（追加 stock-image）</name>
  <files>
    src/providers/pexels-client.test.ts
    src/providers/registry.test.ts
  </files>
  <read_first>
    - src/providers/pexels-client.ts — 被测函数（Task 2）
    - src/providers/registry.test.ts — 现有结构 + 现有 stock-image stub 测试（搜 'stock-image'，从断言 ModelNotFoundError 改为 KeyInvalidError + 有 key 路径）；vision/image-gen 测试范式
    - src/lib/storage.ts — storage.set/get/remove（测试 setup/teardown 用）
    - .planning/phases/18-lib/18-RESEARCH.md — Validation Architecture（L571-625）Test Map + Wave 0 Gaps
  </read_first>
  <action>
    **新建 `src/providers/pexels-client.test.ts`**，覆盖 LIB-01（用 `vi.fn()` mock 全局 `fetch`）：
    1. `searchPexels` 发起的请求 headers.Authorization === 裸 apiKey（**断言不含 "Bearer "**）。做法：mock fetch，捕获第二参 `init.headers.Authorization`，`expect(...).toBe('test-key')` 且 `expect(...).not.toMatch(/Bearer/)`。
    2. query/per_page/page/locale 正确拼进 URL（捕获 fetch 第一参，断言含 `query=`、`per_page=10`、`locale=zh-CN`）。
    3. 429 响应 → `await expect(searchPexels(...)).rejects.toBeInstanceOf(RateLimitError)`。
    4. fetch reject（网络） → rejects NetworkError。
    5. 正常 200 → 返回 `data.photos`（构造含 1 个 PexelsPhoto 的 mock json）。
    6. `fetchPexelsImageToBase64`：mock fetch 返回带 `blob()` 的 Response + mock FileReader（或用 `Blob` + jsdom FileReader）→ 断言返回裸 base64（无 `data:` 前缀）。若 jsdom FileReader 不可用，用 mock：`global.FileReader` stub onload 触发 `reader.result='data:image/jpeg;base64,QUJD'` → 期望返回 `'QUJD'`。

    **追加 `src/providers/registry.test.ts`** stock-image 用例（替换现有 stub 断言）：
    7. 设 `storage.set(STORAGE_KEYS.PEXELS_API_KEY, 'pk-test')` → `ProviderRegistry.resolve('stock-image', () => { throw new Error('unused'); })` 返回 `{ providerId:'pexels', baseURL:'https://api.pexels.com/v1', apiKey:'pk-test', model:'' }`。
    8. 清除 key（`storage.remove(STORAGE_KEYS.PEXELS_API_KEY)`）→ resolve 抛 `KeyInvalidError`（不再 ModelNotFoundError）。
    9. 设 `storage.set(STORAGE_KEYS.PEXELS_BASE_URL, 'https://worker.example/pexels')` + key → 返回 config.baseURL === override（验证可配）。
    每个用例 afterEach 清理 storage（localStorage.clear() 或 storage.remove）。
  </action>
  <verify>
    <automated>npm test -- --run src/providers/pexels-client.test.ts src/providers/registry.test.ts</automated>
  </verify>
  <done>
    两个测试文件全绿。
    acceptance_criteria:
    - `grep -c "not.toMatch\|not.*Bearer\|toBe('test-key')\|toBe('pk-" src/providers/pexels-client.test.ts` 返回 ≥1（鉴权无 Bearer 断言）
    - `grep -c "RateLimitError" src/providers/pexels-client.test.ts` 返回 ≥1
    - `grep -c "KeyInvalidError" src/providers/registry.test.ts` 返回 ≥1
    - `grep -c "providerId: 'pexels'\|providerId:'pexels'\|'pexels'" src/providers/registry.test.ts` 返回 ≥1
    - `npm test -- --run src/providers/pexels-client.test.ts src/providers/registry.test.ts` 退出 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → Pexels API | apiKey 仅进 `Authorization` header；query 经 URLSearchParams 自动编码 |
| registry → pexels-client | apiKey 通过 ImageConfig.apiKey 传递，不落任何持久存储（已在 localStorage，原地读取） |
| pexels-client → error 链 | 所有 error.message 是中文字面量，不 interpolate err / status / body（防 key 泄漏） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | pexels-client error.message | mitigate | NetworkError/RateLimitError 用字面量中文，catch 块不传 err（`catch {}` 而非 `catch(e)` 拼接）；apiKey 不进 body |
| T-18-02 | Information Disclosure | registry stock-image apiKey | mitigate | apiKey 从 storage 读后仅放入 ImageConfig.apiKey → header；不进 model/providerId/log |
| T-18-04 | Tampering | searchPexels query 参数 | mitigate | query 经 `URLSearchParams` 编码（V5 输入校验）；baseURL 为受控常量/storage override，非用户运行时直传 |
</threat_model>

<verification>
- `npm run build` 无 TypeScript 错误
- `npm test -- --run src/providers/pexels-client.test.ts src/providers/registry.test.ts` 全绿
- `grep -c Bearer src/providers/pexels-client.ts` 返回 0（鉴权 gotcha 守门）
- `npm test -- --run` 全量通过（无回归）
</verification>

<success_criteria>
1. pexels-client.ts 存在：searchPexels 裸 key 鉴权（无 Bearer）+ fetchPexelsImageToBase64（URL→裸 base64）+ signal 透传 + 字面量错误
2. STORAGE_KEYS 含 PEXELS_API_KEY（'aster:keys:pexels'）+ PEXELS_BASE_URL（可配口）
3. registry 'stock-image' case 填实：缺 key → KeyInvalidError；有 key → ImageConfig（baseURL 可配，默认直连）
4. 0 净新增运行时依赖（不装 pexels/unsplash-js）
5. 单测覆盖：鉴权无 Bearer、429→RateLimitError、有/无 key registry 分支、base64 裸格式
</success_criteria>

<output>
完成后创建 `.planning/phases/18-lib/18-01-SUMMARY.md`，记录：
- pexels-client 接口最终形态（导出符号）
- registry baseURL 可配机制的落地形态（storage override key）
- 任何对 ImageConfig 复用的偏差（如 model:'' 占位是否引起下游类型问题）
</output>

---
phase: 09-word-d-b-word
plan: "07"
type: execute
wave: 6
depends_on:
  - "09-06"
files_modified:
  - src/adapters/WordAdapter.ts
  - src/agent/tools/write/word.ts
  - src/agent/tools/write/word.test.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - WORD-05

must_haves:
  truths:
    - "agent 插入 3×3 表格后，undo 后表格消失（delete_table_by_marker 按内容指纹定位删表）"
    - "content[][] 提供时指纹 = 首行文本 join('|') + '__3x3'；空表退化为尺寸 + 位置锚匹配"
    - "定位不到目标表格时返回 skipped_error（诚实标注，不删错表，D-14）"
    - "afterParagraphIndex 提供时在段落后插入；省略时在文档末尾插入（D-15）"
    - "所有 5 个 Phase 9 工具 integrationTest: true（合约完整收尾）"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "insertTable + deleteTableByMarker 方法"
      contains: "insertTable"
    - path: "src/agent/tools/write/word.ts"
      provides: "insertTable ToolDef（简单逆向）"
      contains: "insert_table"
    - path: "src/agent/contract.test.ts"
      provides: "5 行全部 integrationTest: true（Phase 9 合约完整）"
    - path: ".planning/phases/08-foundation-a-f/CONTRACT.md"
      provides: "5 行全部 status: done + integration_test: true"
  key_links:
    - from: "src/agent/tools/write/word.ts insertTable.execute"
      to: "src/adapters/WordAdapter.insertTable"
      via: "ctx.adapter as WordAdapter"
      pattern: "delete_table_by_marker"
    - from: "operationLog executeReverse delete_table_by_marker"
      to: "src/adapters/WordAdapter.deleteTableByMarker"
      via: "adapter.deleteTableByMarker(args)"
      pattern: "deleteTableByMarker"
---

<objective>
Wave 6：实现 WORD-05（insert_table + delete_table_by_marker）并完成 Phase 9 合约收尾。

Purpose: 成功标准 #5 要求插入 3×3 表格后 undo 表格消失。Word.Table 无 uniqueLocalId，用内容指纹（首行文本 + 行列数）定位逆向删除。计划 07 同时翻转最后 1 个工具的合约标志位，完成 Phase 9 所有 5 个工具的 D-17/D-18 三步门。

Output: WordAdapter 2 个新方法 + insertTable ToolDef + 最终收尾（5 行合约全 done）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-word-d-b-word/09-CONTEXT.md
@.planning/phases/09-word-d-b-word/09-RESEARCH.md
@.planning/phases/09-word-d-b-word/09-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 从 RESEARCH.md Q7 提取的关键代码片段 -->

body.insertTable（body 直接调用，仅支持 Start/End）:
```typescript
// RESEARCH.md Q7 — body.insertTable 用法（Pitfall 3）
body.insertTable(
  rowCount: number,
  columnCount: number,
  insertLocation: 'Start' | 'End',
  values?: string[][]
): Word.Table
```

paragraph.insertTable（afterParagraphIndex 提供时使用，支持 Before/After）:
```typescript
// RESEARCH.md Q7 — paragraph.insertTable 用法
paras.items[afterParagraphIndex].insertTable(rows, cols, 'After', values)
```

插入后读取 values（D-13 指纹生成时机）:
```typescript
const table = body.insertTable(rows, cols, 'End', content ?? undefined);
table.load('values,rowCount,columnCount');
await ctx.sync();
// 生成指纹
const fingerprint = buildFingerprint(table.values, table.rowCount, table.columnCount);
```

buildFingerprint（D-13 Claude's Discretion，首行文本拼接）:
```typescript
function buildFingerprint(values: string[][], rows: number, cols: number): string {
  const firstRow = (values[0] ?? []) as string[];
  return firstRow.join('|') + `__${rows}x${cols}`;
}
```

deleteTableByMarker（D-13/D-14 逆向定位）:
```typescript
// body.tables 遍历，按指纹匹配
const tables = ctx.document.body.tables;
tables.load('items/rowCount,items/columnCount,items/values');
await ctx.sync();
for (const table of tables.items) {
  const fp = buildFingerprint(table.values as string[][], table.rowCount, table.columnCount);
  if (table.rowCount === args.rows && table.columnCount === args.cols && fp === args.contentFingerprint) {
    table.delete();
    await ctx.sync();
    return;
  }
}
// 定位不到 → skipped_error（诚实标注，不删错表，D-14）
throw new HostApiError('deleteTableByMarker: 找不到目标表格（skipped_error）', undefined);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WordAdapter.ts 新增 insertTable + deleteTableByMarker</name>
  <files>src/adapters/WordAdapter.ts</files>
  <read_first>
    - src/adapters/WordAdapter.ts（找到 restoreRangeSnapshot 方法末尾，追加 2 个新方法）
    - .planning/phases/09-word-d-b-word/09-RESEARCH.md §Q7（insert_table + delete_table_by_marker 完整范式）
    - .planning/phases/09-word-d-b-word/09-CONTEXT.md D-13 D-14 D-15（marker 机制 + 空表后备 + 插入位置）
  </read_first>
  <behavior>
    - insertTable 返回 { contentFingerprint, rows, cols, afterParagraphIndex? } 供 reverse.args
    - deleteTableByMarker 签名 `(args: Record<string, unknown>): Promise<void>`（D-17）
    - 定位不到目标表格时 throw HostApiError（被 replayUndoStep catch → skipped_error，D-14）
    - buildFingerprint 私有辅助：首行文本 join('|') + '__rows×cols'（模块级 function，非类方法）
  </behavior>
  <action>
在文件末尾（类内）追加 `insertTable` 和 `deleteTableByMarker` 方法，并在类外（模块级）添加 `buildTableFingerprint` 辅助函数：

**模块级辅助函数（在 WordAdapter 类定义前/后，类外）：**

```typescript
// 模块私有：表格指纹生成（D-13 Claude's Discretion — 首行文本 + 行列数）
function buildTableFingerprint(
  values: string[][] | null | undefined,
  rows: number,
  cols: number
): string {
  const firstRow = (values?.[0] ?? []) as string[];
  return firstRow.join('|') + `__${rows}x${cols}`;
}
```

**方法 1：insertTable**

```typescript
async insertTable(
  args: Record<string, unknown>
): Promise<{
  contentFingerprint: string;
  rows: number;
  cols: number;
  afterParagraphIndex: number | undefined;
}> {
  const rows = args.rows as number;
  const cols = args.cols as number;
  const afterParagraphIndex = args.afterParagraphIndex as number | undefined;
  const content = args.content as string[][] | undefined;

  try {
    return await Word.run(async (ctx) => {
      const paras = ctx.document.body.paragraphs;

      let table: Word.Table;

      if (afterParagraphIndex !== undefined) {
        // D-15: afterParagraphIndex 提供 → 在指定段落后插入（paragraph.insertTable 支持 'After'）
        paras.load('items/text');
        await ctx.sync();
        if (afterParagraphIndex < 0 || afterParagraphIndex >= paras.items.length) {
          throw new HostApiError(
            `insertTable: afterParagraphIndex=${afterParagraphIndex} 越界`,
            undefined
          );
        }
        table = paras.items[afterParagraphIndex].insertTable(
          rows,
          cols,
          Word.InsertLocation.after,
          content ?? undefined
        );
      } else {
        // D-15: afterParagraphIndex 省略 → 文档末尾（body.insertTable 仅支持 'End'/'Start'）
        table = ctx.document.body.insertTable(
          rows,
          cols,
          Word.InsertLocation.end,
          content ?? undefined
        );
      }

      // 插入后读取 values 以生成指纹（D-13: 同一 Word.run 闭包内两次 sync）
      table.load('values,rowCount,columnCount');
      await ctx.sync();

      const contentFingerprint = buildTableFingerprint(
        table.values as string[][],
        table.rowCount,
        table.columnCount
      );

      return { contentFingerprint, rows, cols, afterParagraphIndex };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word insertTable 失败', err);
  }
}
```

**方法 2：deleteTableByMarker（D-17 Record 签名）**

```typescript
async deleteTableByMarker(args: Record<string, unknown>): Promise<void> {
  // D-17: Record 签名守门
  const contentFingerprint = args.contentFingerprint as string;
  const rows = args.rows as number;
  const cols = args.cols as number;
  // afterParagraphIndex 可选（空表位置锚，D-14）
  // const afterParagraphIndex = args.afterParagraphIndex as number | undefined;

  try {
    await Word.run(async (ctx) => {
      const tables = ctx.document.body.tables;
      tables.load('items/rowCount,items/columnCount,items/values');
      await ctx.sync();

      for (const table of tables.items) {
        const fp = buildTableFingerprint(
          table.values as string[][],
          table.rowCount,
          table.columnCount
        );
        if (table.rowCount === rows && table.columnCount === cols && fp === contentFingerprint) {
          table.delete();
          await ctx.sync();
          return; // 找到并删除成功
        }
      }

      // D-14: 定位不到 → throw（被 replayUndoStep catch → skipped_error，诚实标注）
      // 空表（全 null 指纹）也走此路径（不删错表）
      throw new HostApiError(
        `deleteTableByMarker: 找不到目标表格（fingerprint=${contentFingerprint} rows=${rows} cols=${cols}）`,
        undefined
      );
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word deleteTableByMarker 失败', err);
  }
}
```
  </action>
  <verify>
    <automated>grep -n "insertTable\|deleteTableByMarker\|buildTableFingerprint" src/adapters/WordAdapter.ts | head -15</automated>
  </verify>
  <done>
    - grep 输出包含 insertTable + deleteTableByMarker + buildTableFingerprint
    - npx tsc --noEmit 通过
    - npm run test -- operationLog.integration 的 insert_table 守门测试 GREEN
  </done>
</task>

<task type="auto">
  <name>Task 2: insertTable ToolDef + 测试更新 + 注册 + Phase 9 合约完整收尾</name>
  <files>src/agent/tools/write/word.ts, src/agent/tools/write/word.test.ts, src/agent/tools/index.ts, src/agent/contract.test.ts, .planning/phases/08-foundation-a-f/CONTRACT.md</files>
  <read_first>
    - src/agent/tools/write/word.ts（找到 findAndReplace ToolDef 末尾）
    - src/agent/tools/write/word.test.ts（找到 TODO(计划 07) 行）
    - src/agent/tools/index.ts（word case）
    - src/agent/contract.test.ts（insert_table 行，确认是最后一个 false 的 Phase 9 行）
    - .planning/phases/08-foundation-a-f/CONTRACT.md（insert_table 行）
  </read_first>
  <action>
**文件 1：word.ts — 追加 insertTable ToolDef**

```typescript
// ---- Phase 9: insert_table（简单逆向 delete_table_by_marker）-----------------

interface InsertTableArgs {
  rows: number;
  cols: number;
  afterParagraphIndex?: number;
  content?: string[][];
}

export const insertTable: ToolDef<InsertTableArgs> = {
  name: 'insert_table',
  kind: 'write',
  description: '在 Word 文档插入表格（rows × cols）。afterParagraphIndex 指定插入位置（段落后，0-based），省略时插入到文档末尾。content 为二维字符串数组（可选，省略则空表）。',
  parameters: {
    type: 'object',
    properties: {
      rows: { type: 'number', description: '行数' },
      cols: { type: 'number', description: '列数' },
      afterParagraphIndex: { type: 'number', description: '在第 N 段之后插入（0-based，省略则末尾）' },
      content: {
        type: 'array',
        description: '表格内容（二维数组，外层为行，内层为列，省略则空表）',
        items: {
          type: 'array',
          items: { type: 'string' },
        },
      },
    },
    required: ['rows', 'cols'],
  },
  humanLabel: ({ rows, cols }) => `插入 ${Number(rows)}×${Number(cols)} 表格`,
  async execute({ rows, cols, afterParagraphIndex, content }, ctx): Promise<ToolResult> {
    const result = await (ctx.adapter as WordAdapter).insertTable({
      rows,
      cols,
      afterParagraphIndex,
      content,
    });
    const reverse: ReverseDescriptor = {
      tool: 'delete_table_by_marker',  // ← CONTRACT.md 逐字对齐
      args: {
        contentFingerprint: result.contentFingerprint,
        rows: result.rows,
        cols: result.cols,
        afterParagraphIndex: result.afterParagraphIndex,
      },
    };
    const postState = {
      kind: 'word_table' as const,
      content: { rows, cols, fingerprint: result.contentFingerprint },
    };
    return {
      ok: true,
      data: { rows, cols, inserted: true },
      reverse,
      postState,
    };
  },
};
```

**文件 2：word.test.ts — 替换 TODO(计划 07) placeholder**

在文件顶部 import 追加 `insertTable`：
```typescript
import { applyParagraphStyle, setWordCharacterFormat, setWordParagraphFormat, findAndReplace, insertTable } from './word';
```

将 `TODO(计划 07): insert_table reverse.tool === "delete_table_by_marker"` 替换为：
```typescript
it('insert_table 正确导出且 name 属性为 "insert_table"', () => {
  expect(insertTable.name).toBe('insert_table');
});
```

**文件 3：index.ts — 追加 insertTable**

import：
```typescript
import {
  appendParagraph, insertParagraph, replaceParagraph,
  insertTextAtCursor, replaceSelection,
  setWordCharacterFormat, setWordParagraphFormat,
  applyParagraphStyle,
  findAndReplace,
  insertTable,  // Phase 9 WORD-05
} from './write/word';
```

wordWriteTools：
```typescript
const wordWriteTools = [
  appendParagraph, insertParagraph, replaceParagraph,
  insertTextAtCursor, replaceSelection,
  setWordCharacterFormat, setWordParagraphFormat,
  applyParagraphStyle,
  findAndReplace,
  insertTable,  // Phase 9 WORD-05
] as ToolDef[];
```

**文件 4：contract.test.ts — 翻转最后一行（WORD-05 收尾）**

```typescript
{ toolName: 'insert_table', ..., integrationTest: true }
```

确认此时 5 行全部为 `integrationTest: true`：
```typescript
// 确认 Phase 9 五行全部 true（合约完整）
{ toolName: 'set_word_character_format', ..., integrationTest: true },
{ toolName: 'set_word_paragraph_format', ..., integrationTest: true },
{ toolName: 'apply_paragraph_style', ..., integrationTest: true },
{ toolName: 'find_and_replace', ..., integrationTest: true },
{ toolName: 'insert_table', ..., integrationTest: true },
```

**文件 5：CONTRACT.md — 翻转最后一行（Phase 9 全部收尾）**

```markdown
| insert_table | word | ... | 简单逆向 | delete_table_by_marker | true | 9 | done |
```

确认 Phase 9 全部 5 行 status: done + integration_test: true。
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npm run test 全绿（word.test.ts + operationLog.integration 5 条 + contract 全部 passed）
    - grep "insert_table" src/agent/tools/index.ts 输出 ≥ 1
    - grep "integrationTest: false" src/agent/contract.test.ts 中不再有任何 phase 9 的行（5 行全 true）
    - CONTRACT.md 中 Phase 9 全部 5 行 status: done + integration_test: true
    - npm run size（bundle CI 守门）≤ 82 KB（Phase 9 纯 adapter/tool 代码无新 runtime dep）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → insert_table rows/cols/content | LLM 控制表格大小和内容，afterParagraphIndex 做越界检查 |
| deleteTableByMarker 指纹匹配 → Word.Table.delete() | 指纹碰撞可能删错表（D-14 空表边界情况） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-15 | Tampering | deleteTableByMarker 指纹碰撞（D-14）| mitigate | 空表（全空指纹）时定位不到 → throw（skipped_error），绝不删第一个同尺寸表；有内容的表指纹包含首行文本，碰撞概率低；不匹配不删，诚实报告 |
| T-9-16 | Tampering | insertTable afterParagraphIndex 越界 | mitigate | 提供 afterParagraphIndex 时做 `< 0 || >= paras.items.length` 检查 → throw HostApiError |
| T-9-17 | Elevation of Privilege | deleteTableByMarker 位置签名 | mitigate | D-17 + D-18 三步门：integrationTest: true 后 D-17 硬卡验证工具名在 integration.test.ts 存在 |
</threat_model>

<verification>
```bash
# 适配器方法
grep -n "insertTable\|deleteTableByMarker\|buildTableFingerprint" src/adapters/WordAdapter.ts

# ToolDef reverse.tool
grep "delete_table_by_marker" src/agent/tools/write/word.ts

# Phase 9 合约全部收尾（5 行全 true）
grep -c "integrationTest: true" src/agent/contract.test.ts
# 期望 ≥ 5（Phase 9 五行都改完）

# 不再有 Phase 9 false 行
grep "phase: 9, integrationTest: false" src/agent/contract.test.ts
# 期望 0 结果

# CONTRACT.md 全部收尾
grep -c "status.*done" ".planning/phases/08-foundation-a-f/CONTRACT.md"

# 全套
npm run test

# bundle 守门
npm run build 2>/dev/null && npm run size 2>&1 | grep "main-" | head -5
```
</verification>

<success_criteria>
- insertTable + deleteTableByMarker 在 WordAdapter.ts 中实现（buildTableFingerprint 辅助函数）
- insertTable ToolDef 的 reverse.tool === "delete_table_by_marker"
- 空表或找不到目标表格时 deleteTableByMarker throw（→ skipped_error，不删错表）
- Phase 9 全部 5 个工具 contract.test.ts integrationTest: true
- Phase 9 全部 5 行 CONTRACT.md status: done + integration_test: true
- npm run test 全绿
- bundle ≤ 82 KB（npm run size 确认）
</success_criteria>

<output>
完成后创建 `.planning/phases/09-word-d-b-word/09-07-SUMMARY.md`
</output>

---
phase: 09-word-d-b-word
plan: "05"
type: execute
wave: 4
depends_on:
  - "09-04"
files_modified:
  - src/adapters/WordAdapter.ts
  - src/agent/tools/write/word.ts
  - src/agent/tools/write/word.test.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - WORD-03

must_haves:
  truths:
    - "agent 调用 apply_paragraph_style 传非法 styleName 时，工具返回 INVALID_PARAM，Word.run 未被调用（D-08 allowlist 在调 Word 之前拒绝）"
    - "apply_paragraph_style 传合法 styleName（如 Heading1）正确写入 paragraph.styleBuiltIn，不会因中文 locale 崩溃"
    - "undo 后段落样式还原（restore_paragraph_style 双重定位生效）"
    - "contract.test.ts WORD-03 行 integrationTest: false → true"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "applyParagraphStyle + restoreParagraphStyle 方法"
      contains: "VALID_BUILTIN_STYLES"
    - path: "src/agent/tools/write/word.ts"
      provides: "applyParagraphStyle ToolDef（含 allowlist 校验）"
      contains: "apply_paragraph_style"
    - path: "src/agent/tools/write/word.test.ts"
      provides: "D-08 allowlist 拒绝测试 + 形状测试（GREEN）"
  key_links:
    - from: "src/agent/tools/write/word.ts applyParagraphStyle.execute"
      to: "src/adapters/WordAdapter.applyParagraphStyle"
      via: "allowlist 通过后调用"
      pattern: "VALID_BUILTIN_STYLES"
---

<objective>
Wave 4：实现 WORD-03（apply_paragraph_style）。

Purpose: 成功标准 #3 的核心是「不因语言版本 crash」——中文 Office 用户套「标题1」时，`paragraph.style = "标题1"` 在英文 Office 报 ItemNotFound。本计划用 `Word.BuiltInStyleName` enum + allowlist 校验完全消除此风险（D-08）。

Output: WordAdapter 2 个新方法 + applyParagraphStyle ToolDef（含 tool 层 allowlist）+ word.test.ts 真实测试（替换 Wave 0 placeholder）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-word-d-b-word/09-CONTEXT.md
@.planning/phases/09-word-d-b-word/09-RESEARCH.md
@.planning/phases/09-word-d-b-word/09-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 从代码库提取 -->

Word.BuiltInStyleName enum 关键值（来自 RESEARCH.md Q3）:
```typescript
const VALID_BUILTIN_STYLES = new Set([
  'Heading1','Heading2','Heading3','Heading4','Heading5',
  'Heading6','Heading7','Heading8','Heading9',
  'Normal','NoSpacing','Title','Subtitle',
  'Quote','IntenseQuote','ListParagraph','Caption',
  'Strong','Emphasis','IntenseEmphasis','BookTitle',
]);
```

apply_paragraph_style before-image 方案（来自 RESEARCH.md Q3 + CONTEXT.md D-06）:
```typescript
// 同时存 style（本地化名，参考）+ styleBuiltIn（locale-safe，优先用于还原）
para.load('style,styleBuiltIn');
await ctx.sync();
const beforeImage = { style: para.style, styleBuiltIn: para.styleBuiltIn };

// write：
para.styleBuiltIn = args.styleName as Word.BuiltInStyleName;

// restore（inverse）：
if (before.styleBuiltIn !== 'Other') {
  para.styleBuiltIn = before.styleBuiltIn as Word.BuiltInStyleName;
} else {
  para.style = before.style as string; // 自定义样式回退
}
```

D-08 allowlist 校验位置（tool 层 execute，在调 adapter 之前）:
```typescript
if (!VALID_BUILTIN_STYLES.has(args.styleName)) {
  return { ok: false, error: { code: 'INVALID_PARAM', message: `未知内置样式：${args.styleName}。可用值：Heading1–9, Normal, Quote 等`, recoverable: true } };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WordAdapter.ts 新增 applyParagraphStyle + restoreParagraphStyle</name>
  <files>src/adapters/WordAdapter.ts</files>
  <read_first>
    - src/adapters/WordAdapter.ts（找到 restoreParagraphFormat 方法末尾，在此后追加 2 个新方法）
    - .planning/phases/09-word-d-b-word/09-RESEARCH.md §Q3（locale-safe 路径，before-image style+styleBuiltIn）
    - .planning/phases/09-word-d-b-word/09-CONTEXT.md D-06 D-08（before-image 字段 + allowlist 决策）
  </read_first>
  <behavior>
    - applyParagraphStyle 写前读 { style, styleBuiltIn }，写入 paragraph.styleBuiltIn
    - restoreParagraphStyle 签名必须 `(args: Record<string, unknown>): Promise<void>`（D-17）
    - 还原时：styleBuiltIn !== 'Other' → 用 styleBuiltIn；=== 'Other' → 用 style（自定义样式回退）
    - allowlist 校验在 ToolDef.execute 层（计划 Task 2），adapter 层不做二次校验
  </behavior>
  <action>
在 `src/adapters/WordAdapter.ts` 的 `restoreParagraphFormat` 方法之后追加 2 个新方法：

```typescript
async applyParagraphStyle(
  args: Record<string, unknown>
): Promise<{ beforeImage: Record<string, unknown>; afterText: string }> {
  const index = args.paragraphIndex as number;
  const uniqueLocalId = args.uniqueLocalId as string | undefined;
  const styleName = args.styleName as string; // 已由 ToolDef allowlist 校验过

  try {
    return await Word.run(async (ctx) => {
      const paras = ctx.document.body.paragraphs;
      const supportsUniqueId =
        typeof Office !== 'undefined' &&
        Office.context?.requirements?.isSetSupported('WordApi', '1.6') === true;
      const loadStr = supportsUniqueId
        ? 'items/text,items/style,items/styleBuiltIn,items/uniqueLocalId'
        : 'items/text,items/style,items/styleBuiltIn';
      paras.load(loadStr);
      await ctx.sync();

      if (index < 0 || index >= paras.items.length) {
        throw new HostApiError(`applyParagraphStyle: paragraphIndex=${index} 越界`, undefined);
      }

      let targetIndex = index;
      if (
        supportsUniqueId &&
        uniqueLocalId !== undefined &&
        uniqueLocalId !== null &&
        paras.items[index].uniqueLocalId !== uniqueLocalId
      ) {
        const found = paras.items.findIndex((p) => p.uniqueLocalId === uniqueLocalId);
        if (found === -1) {
          throw new HostApiError(
            `applyParagraphStyle: NOT_FOUND paragraphIndex=${index} uniqueLocalId=${uniqueLocalId}`,
            undefined
          );
        }
        targetIndex = found;
      }

      const para = paras.items[targetIndex];
      // before-image（D-06: 同时存 style + styleBuiltIn）
      const beforeImage: Record<string, unknown> = {
        style: para.style,
        styleBuiltIn: para.styleBuiltIn,
      };
      const afterText = normalizeText(para.text);

      // write（locale-safe via styleBuiltIn）
      para.styleBuiltIn = styleName as Word.BuiltInStyleName;
      await ctx.sync();

      return { beforeImage, afterText };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word applyParagraphStyle 失败', err);
  }
}

async restoreParagraphStyle(args: Record<string, unknown>): Promise<void> {
  // D-17: Record 签名守门
  const index = args.index as number;
  const expectedText = args.expectedText as string;
  const before = args.before as Record<string, unknown>;

  try {
    await Word.run(async (ctx) => {
      const paras = ctx.document.body.paragraphs;
      paras.load('items/text');
      await ctx.sync();

      // 双重定位
      let targetIndex = -1;
      if (
        index >= 0 &&
        index < paras.items.length &&
        normalizeText(paras.items[index].text) === normalizeText(expectedText)
      ) {
        targetIndex = index;
      }
      if (targetIndex === -1) {
        for (let i = 0; i < paras.items.length; i++) {
          if (normalizeText(paras.items[i].text) === normalizeText(expectedText)) {
            targetIndex = i;
            break;
          }
        }
      }
      if (targetIndex === -1) {
        throw new HostApiError('restoreParagraphStyle: 目标段落未找到', undefined);
      }

      const para = paras.items[targetIndex];
      // 还原（优先 styleBuiltIn；自定义样式回退 style）
      if (before.styleBuiltIn !== null && before.styleBuiltIn !== undefined &&
          before.styleBuiltIn !== 'Other') {
        para.styleBuiltIn = before.styleBuiltIn as Word.BuiltInStyleName;
      } else if (before.style !== null && before.style !== undefined) {
        para.style = before.style as string;
      }
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word restoreParagraphStyle 失败', err);
  }
}
```
  </action>
  <verify>
    <automated>grep -n "applyParagraphStyle\|restoreParagraphStyle" src/adapters/WordAdapter.ts | head -10</automated>
  </verify>
  <done>
    - grep 输出包含方法声明
    - npx tsc --noEmit 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: word.ts ToolDef + word.test.ts 真实测试 + index.ts 注册 + 标志位翻转</name>
  <files>src/agent/tools/write/word.ts, src/agent/tools/write/word.test.ts, src/agent/tools/index.ts, src/agent/contract.test.ts, .planning/phases/08-foundation-a-f/CONTRACT.md</files>
  <read_first>
    - src/agent/tools/write/word.ts（找到 setWordParagraphFormat 末尾，在此后追加新 ToolDef）
    - src/agent/tools/write/word.test.ts（Wave 0 创建的 placeholder 文件，替换 D-08 相关测试）
    - src/agent/tools/index.ts（word case import + wordWriteTools 数组）
    - src/agent/contract.test.ts（lines 35-39，找到 apply_paragraph_style 行）
  </read_first>
  <action>
**文件 1：src/agent/tools/write/word.ts — 追加 applyParagraphStyle ToolDef**

```typescript
// ---- Phase 9: apply_paragraph_style ----------------------------------------

// D-08 allowlist（locale-safe 内置样式名，在调 Word 之前校验）
const VALID_BUILTIN_STYLES = new Set([
  'Heading1','Heading2','Heading3','Heading4','Heading5',
  'Heading6','Heading7','Heading8','Heading9',
  'Normal','NoSpacing','Title','Subtitle',
  'Quote','IntenseQuote','ListParagraph','Caption',
  'Strong','Emphasis','IntenseEmphasis','BookTitle',
]);

interface ApplyParagraphStyleArgs {
  paragraphIndex: number;
  uniqueLocalId?: string;
  styleName: string;
}

export const applyParagraphStyle: ToolDef<ApplyParagraphStyleArgs> = {
  name: 'apply_paragraph_style',
  kind: 'write',
  description: '套用 Word 内置段落样式（如 Heading1-9, Normal, Title 等 BuiltInStyleName 枚举值）。传中文样式名会被拒绝，请传英文 BuiltInStyleName 值。',
  parameters: {
    type: 'object',
    properties: {
      paragraphIndex: { type: 'number', description: '目标段落编号（0-based）' },
      uniqueLocalId: { type: 'string', description: '段落唯一 ID（可选，精确消歧）' },
      styleName: {
        type: 'string',
        description: 'Word.BuiltInStyleName 枚举值（Heading1-9, Normal, NoSpacing, Title, Subtitle, Quote, IntenseQuote, ListParagraph, Caption, Strong, Emphasis, IntenseEmphasis, BookTitle）',
      },
    },
    required: ['paragraphIndex', 'styleName'],
  },
  humanLabel: ({ paragraphIndex, styleName }) =>
    `将第 ${Number(paragraphIndex) + 1} 段套用样式「${String(styleName)}」`,
  async execute({ paragraphIndex, uniqueLocalId, styleName }, ctx): Promise<ToolResult> {
    // D-08: allowlist 校验在调 Word 之前（locale-safe 防御）
    if (!VALID_BUILTIN_STYLES.has(styleName)) {
      return {
        ok: false,
        error: {
          code: 'INVALID_PARAM',
          message: `未知内置样式：${styleName}。可用值：Heading1–9, Normal, Title, Quote 等（Word.BuiltInStyleName 枚举值）`,
          recoverable: true,
        },
      };
    }
    const result = await (ctx.adapter as WordAdapter).applyParagraphStyle({
      paragraphIndex,
      uniqueLocalId,
      styleName,
    });
    const reverse: ReverseDescriptor = {
      tool: 'restore_paragraph_style',  // ← CONTRACT.md 逐字对齐
      args: {
        index: paragraphIndex,
        expectedText: result.afterText,
        before: result.beforeImage,
      },
    };
    const postState = { kind: 'word_style' as const, content: { index: paragraphIndex, styleName } };
    return { ok: true, data: { paragraphIndex, styleName }, reverse, postState };
  },
};
```

**文件 2：src/agent/tools/write/word.test.ts — 替换 D-08 placeholder 为真实测试**

将 Wave 0 中 `apply_paragraph_style — D-08 allowlist 校验` describe 块的 3 个 placeholder 测试替换为真实断言：

```typescript
import { describe, it, expect, vi } from 'vitest';
import { applyParagraphStyle, setWordCharacterFormat, setWordParagraphFormat, findAndReplace, insertTable } from './word';

describe('apply_paragraph_style — D-08 allowlist 校验', () => {
  it('非法 styleName（中文"标题1"）→ { ok: false, error.code: INVALID_PARAM }，不调用 adapter', async () => {
    const mockAdapter = { applyParagraphStyle: vi.fn() };
    const ctx = { adapter: mockAdapter };
    const result = await applyParagraphStyle.execute(
      { paragraphIndex: 0, styleName: '标题1' } as Parameters<typeof applyParagraphStyle.execute>[0],
      ctx as Parameters<typeof applyParagraphStyle.execute>[1]
    );
    expect(result.ok).toBe(false);
    if (result.ok) return;
    expect((result.error as Record<string, unknown>).code).toBe('INVALID_PARAM');
    expect(mockAdapter.applyParagraphStyle).not.toHaveBeenCalled();
  });

  it('非法 styleName "Heading10"（不在 allowlist）→ INVALID_PARAM', async () => {
    const mockAdapter = { applyParagraphStyle: vi.fn() };
    const ctx = { adapter: mockAdapter };
    const result = await applyParagraphStyle.execute(
      { paragraphIndex: 0, styleName: 'Heading10' } as Parameters<typeof applyParagraphStyle.execute>[0],
      ctx as Parameters<typeof applyParagraphStyle.execute>[1]
    );
    expect(result.ok).toBe(false);
    expect(mockAdapter.applyParagraphStyle).not.toHaveBeenCalled();
  });

  it('合法 styleName "Heading1" → 调用 adapter.applyParagraphStyle', async () => {
    const mockResult = { beforeImage: { style: 'Normal', styleBuiltIn: 'Normal' }, afterText: '第一段' };
    const mockAdapter = { applyParagraphStyle: vi.fn().mockResolvedValue(mockResult) };
    const ctx = { adapter: mockAdapter };
    const result = await applyParagraphStyle.execute(
      { paragraphIndex: 0, styleName: 'Heading1' } as Parameters<typeof applyParagraphStyle.execute>[0],
      ctx as Parameters<typeof applyParagraphStyle.execute>[1]
    );
    expect(result.ok).toBe(true);
    expect(mockAdapter.applyParagraphStyle).toHaveBeenCalledTimes(1);
    const callArgs = mockAdapter.applyParagraphStyle.mock.calls[0][0] as Record<string, unknown>;
    expect(callArgs.styleName).toBe('Heading1');
  });
});

describe('Phase 9 write tools — reverse/postState 形状测试', () => {
  it('set_word_character_format reverse.tool === "restore_range_font"', () => {
    // 验证 ToolDef 名称常量
    expect(setWordCharacterFormat.name).toBe('set_word_character_format');
  });

  it('set_word_paragraph_format reverse.tool === "restore_paragraph_format"', () => {
    expect(setWordParagraphFormat.name).toBe('set_word_paragraph_format');
  });

  it('apply_paragraph_style reverse.tool === "restore_paragraph_style"（通过合法路径验证）', () => {
    expect(applyParagraphStyle.name).toBe('apply_paragraph_style');
  });

  it('TODO(计划 06): find_and_replace reverse.tool === "restore_range_snapshot"', () => {
    expect(true).toBe(true); // 计划 06 替换
  });

  it('TODO(计划 07): insert_table reverse.tool === "delete_table_by_marker"', () => {
    expect(true).toBe(true); // 计划 07 替换
  });
});
```

**注意：** `findAndReplace` 和 `insertTable` 的 import 在计划 06/07 后才有，先注释掉或用条件 import。为避免编译错误，仅 import 已存在的 4 个工具：
```typescript
import { applyParagraphStyle, setWordCharacterFormat, setWordParagraphFormat } from './word';
```

**文件 3：src/agent/tools/index.ts — 追加 applyParagraphStyle 到 import + wordWriteTools**

在 word.ts import 行追加：
```typescript
import {
  appendParagraph, insertParagraph, replaceParagraph,
  insertTextAtCursor, replaceSelection,
  setWordCharacterFormat, setWordParagraphFormat,
  applyParagraphStyle,  // Phase 9 WORD-03
} from './write/word';
```

wordWriteTools 数组追加：
```typescript
const wordWriteTools = [
  appendParagraph, insertParagraph, replaceParagraph,
  insertTextAtCursor, replaceSelection,
  setWordCharacterFormat, setWordParagraphFormat,
  applyParagraphStyle,  // Phase 9 WORD-03
] as ToolDef[];
```

**文件 4：contract.test.ts — 翻转 apply_paragraph_style 行**

```typescript
{ toolName: 'apply_paragraph_style', ..., integrationTest: true }  // false → true
```

**文件 5：CONTRACT.md — 翻转 apply_paragraph_style 行**

```markdown
| apply_paragraph_style | word | ... | 简单逆向 | restore_paragraph_style | true | 9 | done |
```
  </action>
  <verify>
    <automated>npm run test -- word 2>&1 | tail -15</automated>
  </verify>
  <done>
    - npm run test -- word 全部通过（D-08 allowlist 3 个测试 GREEN，形状测试 GREEN）
    - npm run test -- contract 通过（D-17 硬卡：apply_paragraph_style 在 integration.test.ts 存在）
    - grep "apply_paragraph_style" src/agent/tools/index.ts 输出 ≥ 1 行
    - npm run test 无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → apply_paragraph_style styleName | 不受信任的 LLM 输出，allowlist 在调 Word 之前拦截 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-09 | Tampering | apply_paragraph_style styleName | mitigate | D-08：VALID_BUILTIN_STYLES Set 在 tool execute 层校验，`Word.run` 之前拒绝，返 INVALID_PARAM + recoverable:true（LLM 可重试合法值）|
| T-9-10 | Tampering | restoreParagraphStyle 'Other' styleBuiltIn | mitigate | 还原逻辑：styleBuiltIn === 'Other' 时回退 `para.style = before.style`（自定义样式）；若 style 也为空则静默不写（保留当前状态，不崩溃）|
| T-9-11 | Elevation of Privilege | restoreParagraphStyle 位置签名 | mitigate | D-17 + D-18 三步门：integrationTest 翻 true 前 integration.test.ts 有 toolName 字符串（D-17 硬卡通过）|
</threat_model>

<verification>
```bash
# allowlist 在 tool 层（不在 adapter 层）
grep -n "VALID_BUILTIN_STYLES" src/agent/tools/write/word.ts

# adapter 方法存在
grep -n "applyParagraphStyle\|restoreParagraphStyle" src/adapters/WordAdapter.ts

# D-08 测试绿化
npm run test -- word

# D-17 硬卡
npm run test -- contract

# 全套
npm run test
```
</verification>

<success_criteria>
- VALID_BUILTIN_STYLES Set 在 word.ts（tool 层），不在 WordAdapter.ts
- apply_paragraph_style 传 "标题1" → INVALID_PARAM，Word.run 未调用
- apply_paragraph_style 传 "Heading1" → 成功，reverse.tool === "restore_paragraph_style"
- word.test.ts 的 D-08 测试 3 个 GREEN
- contract.test.ts WORD-03 integrationTest: true，D-17 硬卡通过
- npm run test 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/09-word-d-b-word/09-05-SUMMARY.md`
</output>

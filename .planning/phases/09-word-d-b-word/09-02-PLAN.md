---
phase: 09-word-d-b-word
plan: "02"
type: execute
wave: 1
depends_on:
  - "09-01"
files_modified:
  - src/agent/operationLog.ts
autonomous: true
requirements:
  - WORD-01
  - WORD-02
  - WORD-03
  - WORD-04
  - WORD-05

must_haves:
  truths:
    - "operationLog.ts 的 DocumentAdapterForReplay 接口包含 5 个新 Word inverse 方法声明"
    - "operationLog.ts 的 executeReverse switch 包含 5 个新 case（restore_range_font / restore_paragraph_format / restore_paragraph_style / restore_range_snapshot / delete_table_by_marker）"
    - "PostStateSnapshot.kind union 包含 5 个新类型（word_char_format / word_para_format / word_style / word_snapshot / word_table）"
    - "Wave 0 的 5 个 integration 守门测试从 skipped_error → rolled_back（因为 executeReverse 现在有这些 case）"
  artifacts:
    - path: "src/agent/operationLog.ts"
      provides: "5 个 DocumentAdapterForReplay 方法声明 + 5 个 executeReverse case + 5 个 PostStateSnapshot.kind"
      contains: "restoreRangeFont"
  key_links:
    - from: "src/agent/operationLog.ts executeReverse"
      to: "src/adapters/WordAdapter"
      via: "case 'restore_range_font' → adapter.restoreRangeFont(reverse.args)"
      pattern: "case 'restore_range_font'"
---

<objective>
Wave 1：扩展 operationLog.ts 地基。

Purpose: 这是所有 5 个 Word 工具计划的前置依赖。DocumentAdapterForReplay 接口必须先有方法声明，各 adapter plan 的 TypeScript 实现才能类型正确。executeReverse 的 5 个新 case 将 Wave 0 的 RED 守门测试变为 GREEN（因为 case 命中 → adapter.method(args) 执行 → rolled_back）。

Output: 修改后的 src/agent/operationLog.ts，包含完整的 Phase 9 接口扩展。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-word-d-b-word/09-CONTEXT.md
@.planning/phases/09-word-d-b-word/09-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 现有 operationLog.ts 关键接口，从代码库提取 -->

From src/agent/operationLog.ts (lines 34-37) — 待扩展的 union:
```typescript
export interface PostStateSnapshot {
  kind: 'word_paragraph' | 'excel_range' | 'excel_chart' | 'ppt_slide' | 'ppt_shape';
  content: unknown;
}
```

From src/agent/operationLog.ts (lines 83-106) — 待追加方法的接口:
```typescript
export interface DocumentAdapterForReplay {
  deleteParagraphByContent?: (args: Record<string, unknown>) => Promise<void>;
  readWordParagraph?: (args: Record<string, unknown>) => Promise<string>;
  overwriteRange?: (args: Record<string, unknown>) => Promise<void>;
  readExcelRange?: (args: Record<string, unknown>) => Promise<unknown[][]>;
  deleteSlideByTitle?: (args: Record<string, unknown>) => Promise<void>;
  readPptSlideTitle?: (args: Record<string, unknown>) => Promise<string>;
  restoreShapeProperty?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeGeometry?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeText?: (args: Record<string, unknown>) => Promise<void>;
  deleteChartByName?: (args: Record<string, unknown>) => Promise<void>;
  restoreParagraphAt?: (args: Record<string, unknown>) => Promise<void>;
}
```

From src/agent/operationLog.ts (lines 294-299) — executeReverse case 范式:
```typescript
case 'restore_paragraph_at':
  if (!adapter.restoreParagraphAt) {
    throw new Error(`adapter 未实现 restoreParagraphAt（tool=${reverse.tool}）`);
  }
  await adapter.restoreParagraphAt(reverse.args);
  break;
```

CONTRACT.md Phase 9 reverse_tool 名（逐字）:
- restore_range_font
- restore_paragraph_format
- restore_paragraph_style
- restore_range_snapshot
- delete_table_by_marker
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 operationLog.ts — 接口 + switch case + PostStateSnapshot.kind</name>
  <files>src/agent/operationLog.ts</files>
  <read_first>
    - src/agent/operationLog.ts（完整文件，找到 DocumentAdapterForReplay 接口末尾、executeReverse switch 末尾、PostStateSnapshot kind union）
    - .planning/phases/09-word-d-b-word/09-PATTERNS.md §operationLog.ts（追加格式 + case 范式）
    - .planning/phases/08-foundation-a-f/CONTRACT.md §Phase 9 Word 工具（reverse_tool 名逐字确认）
  </read_first>
  <behavior>
    - PostStateSnapshot.kind union 追加 5 个新类型后，`kind: 'word_char_format'` 应该是合法的 TypeScript 赋值
    - DocumentAdapterForReplay 接口追加 5 个新 optional 方法后，`adapter.restoreRangeFont?.(args)` 应该类型正确
    - executeReverse 的新 5 个 case 应与 CONTRACT.md reverse_tool 名逐字一致（不能拼错）
    - 新 case 的错误消息格式应与现有 case 一致：`` `adapter 未实现 XxxMethod（tool=${reverse.tool}）` ``
  </behavior>
  <action>
对 `src/agent/operationLog.ts` 进行以下三处精确修改：

**修改 1：扩展 PostStateSnapshot.kind union（lines 34-37 附近）**

将：
```typescript
export interface PostStateSnapshot {
  kind: 'word_paragraph' | 'excel_range' | 'excel_chart' | 'ppt_slide' | 'ppt_shape';
  content: unknown;
}
```
改为：
```typescript
export interface PostStateSnapshot {
  kind: 'word_paragraph' | 'excel_range' | 'excel_chart' | 'ppt_slide' | 'ppt_shape'
      | 'word_char_format' | 'word_para_format' | 'word_style' | 'word_snapshot' | 'word_table';
  content: unknown;
}
```

**修改 2：在 DocumentAdapterForReplay 接口末尾（`restoreParagraphAt?:` 行之后）追加 5 个方法声明**

```typescript
  /** Word inverse：还原段落字体格式（set_word_character_format，D-17 Record 签名）*/
  restoreRangeFont?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：还原段落格式（set_word_paragraph_format，D-17 Record 签名）*/
  restoreParagraphFormat?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：还原段落样式（apply_paragraph_style，D-17 Record 签名）*/
  restoreParagraphStyle?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：按段落快照还原文本（find_and_replace，D-17 Record 签名）*/
  restoreRangeSnapshot?: (args: Record<string, unknown>) => Promise<void>;
  /** Word inverse：按内容指纹删除表格（insert_table，D-17 Record 签名）*/
  deleteTableByMarker?: (args: Record<string, unknown>) => Promise<void>;
```

**修改 3：在 executeReverse switch 中，`case 'restore_paragraph_at':` 块之后、`case 'noop_inverse':` 之前，追加 5 个新 case**

```typescript
    case 'restore_range_font':
      if (!adapter.restoreRangeFont) {
        throw new Error(`adapter 未实现 restoreRangeFont（tool=${reverse.tool}）`);
      }
      await adapter.restoreRangeFont(reverse.args);
      break;
    case 'restore_paragraph_format':
      if (!adapter.restoreParagraphFormat) {
        throw new Error(`adapter 未实现 restoreParagraphFormat（tool=${reverse.tool}）`);
      }
      await adapter.restoreParagraphFormat(reverse.args);
      break;
    case 'restore_paragraph_style':
      if (!adapter.restoreParagraphStyle) {
        throw new Error(`adapter 未实现 restoreParagraphStyle（tool=${reverse.tool}）`);
      }
      await adapter.restoreParagraphStyle(reverse.args);
      break;
    case 'restore_range_snapshot':
      if (!adapter.restoreRangeSnapshot) {
        throw new Error(`adapter 未实现 restoreRangeSnapshot（tool=${reverse.tool}）`);
      }
      await adapter.restoreRangeSnapshot(reverse.args);
      break;
    case 'delete_table_by_marker':
      if (!adapter.deleteTableByMarker) {
        throw new Error(`adapter 未实现 deleteTableByMarker（tool=${reverse.tool}）`);
      }
      await adapter.deleteTableByMarker(reverse.args);
      break;
```

**D-19 保守路径（无需修改）：** `readTargetState` 函数的 switch 中有 `default: return undefined`，`isTargetStateConsistent` 有 `default: return true`。新 kind 自动走这两个 default，不需要添加 case。
  </action>
  <verify>
    <automated>grep -c "restoreRangeFont\|restoreParagraphFormat\|restoreParagraphStyle\|restoreRangeSnapshot\|deleteTableByMarker" src/agent/operationLog.ts</automated>
  </verify>
  <done>
    - grep -c 输出 ≥ 10（接口声明 + switch case 各 5 次）
    - grep "word_char_format\|word_para_format\|word_style\|word_snapshot\|word_table" src/agent/operationLog.ts 输出 ≥ 1 行
    - npm run test -- operationLog.integration 中，5 个 Phase 9 守门测试（Wave 0 创建）从 skipped_error 变为 rolled_back（GREEN）
    - npx tsc --noEmit 通过（TypeScript 类型无错误）
  </done>
</task>

<task type="auto">
  <name>Task 2: integration 守门测试绿化验证（Wave 0 RED → GREEN 确认）</name>
  <files>src/agent/operationLog.integration.test.ts</files>
  <read_first>
    - src/agent/operationLog.integration.test.ts（Phase 9 守门块，确认 mockAdapter 结构与新接口兼容）
  </read_first>
  <action>
运行 integration 测试，确认 Wave 0 的 5 个 Phase 9 守门测试从 skipped_error 转为 rolled_back。

如果任何测试仍然 FAIL，检查原因：
- 最常见问题：`operationLog.ts` 的 case 字符串与 `reverse.tool` 字段不完全匹配（字母大小写/下划线）
- 解决：核对 CONTRACT.md 中的 reverse_tool 名（`restore_range_font` 等），与 integration test 中 `reverse.tool` 字段完全一致

如果 Wave 0 测试的 `mockAdapter` 类型报 TypeScript 错误（因为用了 `as unknown as DocumentAdapterForReplay`），确保写法是：
```typescript
const mockAdapter = { restoreRangeFont: restoreRangeFontFn } as unknown as DocumentAdapterForReplay;
```
这个写法在 Wave 0 骨架中已用，计划 02 让接口有了正式声明后，可以去掉 `as unknown`，改为：
```typescript
const mockAdapter: DocumentAdapterForReplay = { restoreRangeFont: restoreRangeFontFn };
```

如果 integration.test.ts 中 Wave 0 测试的 `postState.kind` 报错（如 `'word_char_format' is not assignable`），这是因为 PostStateSnapshot.kind 扩展了——此时直接编译通过即可。如果类型仍然报错，检查 operationLog.ts 的 kind union 是否正确更新。

本任务无需修改任何文件（只是验证），若发现需要修复，范围仅限于 operationLog.integration.test.ts 的类型注解调整（不改 operationLog.ts，那在 Task 1 完成）。
  </action>
  <verify>
    <automated>npm run test -- operationLog.integration 2>&1 | tail -10</automated>
  </verify>
  <done>
    - npm run test -- operationLog.integration 输出中 Phase 9 守门的 5 个 it 块全部 passed
    - 没有新的 TypeScript 编译错误
    - 全套 npm run test 通过（现有测试无回归）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executeReverse switch → adapter | 不可信的 reverse.tool 字符串路由到 adapter 方法调用；default case throw 防止未知 tool 静默失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | Elevation of Privilege | executeReverse switch default | mitigate | default case throw new Error 防止未知 reverse tool 被 noop_inverse 混淆；每个新 case 检查 adapter 方法是否存在，不存在则 throw（D-17 守门路径）|
| T-9-03 | Elevation of Privilege | PostStateSnapshot.kind | accept | 新 kind 的 readTargetState 返 undefined（保守），isTargetStateConsistent 返 true（D-19）；不增加误判手动改的风险，低影响 |
</threat_model>

<verification>
```bash
# 接口方法已追加（10 次 = 接口声明 5 + case 5）
grep -c "restoreRangeFont\|restoreParagraphFormat\|restoreParagraphStyle\|restoreRangeSnapshot\|deleteTableByMarker" src/agent/operationLog.ts

# kind union 已扩展
grep "word_char_format" src/agent/operationLog.ts

# Phase 9 守门测试变绿
npm run test -- operationLog.integration

# 全套无回归
npm run test
```
</verification>

<success_criteria>
- operationLog.ts 的 DocumentAdapterForReplay 接口有 5 个新 optional 方法（Record 签名）
- operationLog.ts 的 executeReverse switch 有 5 个新 case（reverse_tool 名与 CONTRACT 逐字一致）
- PostStateSnapshot.kind union 有 5 个新类型
- Wave 0 的 5 个 Phase 9 integration 守门测试全部 GREEN
- npm run test 无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/09-word-d-b-word/09-02-SUMMARY.md`
</output>

---
phase: 09-word-d-b-word
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agent/operationLog.integration.test.ts
  - src/adapters/WordAdapter.read.test.ts
  - src/agent/tools/write/word.test.ts
autonomous: true
requirements:
  - WSEL-01
  - WORD-01
  - WORD-02
  - WORD-03
  - WORD-04
  - WORD-05

must_haves:
  truths:
    - "operationLog.integration.test.ts 包含 5 个 Word 工具的 toolName 字符串字面量（D-17 硬卡前提）"
    - "每个守门测试目前 RED（expect rolled_back 但 adapter 方法缺失 / 案例占位），波次 3-6 实现后变绿"
    - "selection_detail 单测目前 RED（paragraphIndex 字段尚未扩展），计划 03 实现后变绿"
    - "D-08 allowlist 单测目前 RED（apply_paragraph_style 工具未实现），计划 05 实现后变绿"
  artifacts:
    - path: "src/agent/operationLog.integration.test.ts"
      provides: "5 条 Word 守门测试（toolName 字面量出现，D-17 预埋）"
    - path: "src/adapters/WordAdapter.read.test.ts"
      provides: "selection_detail paragraphIndex + uniqueLocalId 单测骨架"
    - path: "src/agent/tools/write/word.test.ts"
      provides: "D-08 allowlist 拒绝测试 + 5 工具 reverse/postState 形状测试骨架"
  key_links:
    - from: "src/agent/contract.test.ts"
      to: "src/agent/operationLog.integration.test.ts"
      via: "fs.readFileSync D-17 硬卡"
      pattern: "set_word_character_format|set_word_paragraph_format|apply_paragraph_style|find_and_replace|insert_table"
---

<objective>
Wave 0：写出所有 Phase 9 测试骨架（RED 状态）。

Purpose: D-17/D-18 硬门要求工具名字符串在 integration.test.ts 中出现，contract.test.ts 的 fs.readFileSync 扫描发生在 CI 时，必须在工具实现（Wave 1–6）之前预埋这些字符串。Wave 0 测试是 RED，各工具实现后逐步变 GREEN。

Output:
- 5 条 Word integration 守门测试（骨架，RED）
- 1 个 selection_detail 单测（骨架，RED）
- 1 个 word.test.ts（allowlist 拒绝测试 + 形状测试骨架，RED）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-word-d-b-word/09-CONTEXT.md
@.planning/phases/09-word-d-b-word/09-RESEARCH.md
@.planning/phases/09-word-d-b-word/09-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 执行者需要的现有接口，从代码库提取 -->

From src/agent/operationLog.ts (lines 83-106):
```typescript
export interface DocumentAdapterForReplay {
  deleteParagraphByContent?: (args: Record<string, unknown>) => Promise<void>;
  readWordParagraph?: (args: Record<string, unknown>) => Promise<string>;
  overwriteRange?: (args: Record<string, unknown>) => Promise<void>;
  readExcelRange?: (args: Record<string, unknown>) => Promise<unknown[][]>;
  deleteSlideByTitle?: (args: Record<string, unknown>) => Promise<void>;
  readPptSlideTitle?: (args: Record<string, unknown>) => Promise<string>;
  restoreShapeProperty?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeGeometry?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeText?: (args: Record<string, unknown>) => Promise<void>;
  deleteChartByName?: (args: Record<string, unknown>) => Promise<void>;
  restoreParagraphAt?: (args: Record<string, unknown>) => Promise<void>;
}
```

From src/agent/operationLog.ts (lines 44-53):
```typescript
export interface OperationLogEntry {
  runId: string;
  stepIndex: number;
  toolName: string;
  args: unknown;
  humanLabel: string;
  reverse: ReverseDescriptor;
  postState?: PostStateSnapshot;
  timestamp: number;
}
```

From src/agent/operationLog.integration.test.ts (lines 33-45) — mockWord 工厂（追加扩展新字段）:
```typescript
function mockWord(paragraphTexts: string[]): Array<{ text: string; delete: ReturnType<typeof vi.fn> }> {
  const items = paragraphTexts.map((text) => ({ text, delete: vi.fn() }));
  (global as unknown as Record<string, unknown>).Word = {
    InsertLocation: { end: 'End' },
    run: vi.fn(async (cb: (ctx: unknown) => unknown) =>
      cb({
        document: { body: { paragraphs: { load: vi.fn(), items } } },
        sync: vi.fn().mockResolvedValue(undefined),
      }),
    ),
  };
  return items;
}
```

From src/agent/operationLog.integration.test.ts (lines 124-153) — 守门测试范式:
```typescript
it('单步撤销 replace_paragraph：restoreParagraphAt 收 Record 对象（不抛 TypeError）', async () => {
  const restoreParagraphAtFn = vi.fn(async (_args: Record<string, unknown>): Promise<void> => {});
  const mockAdapter: DocumentAdapterForReplay = { restoreParagraphAt: restoreParagraphAtFn };
  const entry: OperationLogEntry = {
    runId: 'run-it', stepIndex: 0,
    toolName: 'replace_paragraph',  // ← D-17 硬卡：字符串必须出现在本文件
    args: { index: 1, new_text: '新段落' },
    humanLabel: '替换第 1 段落为「新段落」',
    reverse: { tool: 'restore_paragraph_at', args: { index: 1, expectedText: '新段落', restoreText: '原段落' } },
    postState: { kind: 'word_paragraph', content: '新段落' },
    timestamp: 0,
  };
  const detail = await replayUndoSingle(entry, mockAdapter);
  expect(detail.status).toBe('rolled_back');
  expect(restoreParagraphAtFn).toHaveBeenCalledTimes(1);
  const receivedArgs = restoreParagraphAtFn.mock.calls[0][0] as Record<string, unknown>;
  expect(typeof receivedArgs).toBe('object');
  expect(receivedArgs.index).toBe(1);
  expect(receivedArgs.restoreText).toBe('原段落');
});
```

Phase 9 新增的 PostStateSnapshot.kind（计划 02 后有效）:
- `'word_char_format'`（set_word_character_format）
- `'word_para_format'`（set_word_paragraph_format）
- `'word_style'`（apply_paragraph_style）
- `'word_snapshot'`（find_and_replace）
- `'word_table'`（insert_table）

Phase 9 新增的 DocumentAdapterForReplay 方法（计划 02 后有效）:
- `restoreRangeFont?: (args: Record<string, unknown>) => Promise<void>`
- `restoreParagraphFormat?: (args: Record<string, unknown>) => Promise<void>`
- `restoreParagraphStyle?: (args: Record<string, unknown>) => Promise<void>`
- `restoreRangeSnapshot?: (args: Record<string, unknown>) => Promise<void>`
- `deleteTableByMarker?: (args: Record<string, unknown>) => Promise<void>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 追加 5 条 Word inverse 守门测试到 operationLog.integration.test.ts（RED 骨架）</name>
  <files>src/agent/operationLog.integration.test.ts</files>
  <read_first>
    - src/agent/operationLog.integration.test.ts（完整文件，提取现有 mockWord 工厂 + restoreParagraphAt 守门测试范式 lines 33-153）
    - .planning/phases/09-word-d-b-word/09-PATTERNS.md §operationLog.integration.test.ts（守门范式）
    - .planning/phases/08-foundation-a-f/CONTRACT.md §Phase 9 Word 工具（reverse_tool 逐字确认）
  </read_first>
  <action>
在文件末尾（现有最后一个 `it` 块之后，`describe` 关闭括号之前）追加一个新的 `describe('Phase 9 Word inverse 守门', ...)` 块，包含 5 个守门测试。

**关键约束（D-17 硬门）：** 每个测试中 `toolName` 字段必须是字符串字面量（不是变量），contract.test.ts 的 fs.readFileSync 扫描文件内容查找此字符串。

**重要：** 当前 `DocumentAdapterForReplay` 接口尚未包含这 5 个方法（计划 02 才加）。因此守门测试使用 `// @ts-expect-error 计划 02 补接口` 注释暂时压制类型错误，或者直接声明本地 mock 接口（`{ restoreRangeFont: ... }`，不走 DocumentAdapterForReplay 类型，改用 `as DocumentAdapterForReplay`）。

**测试结构（严格按范式，5 个，一一对应 CONTRACT 中的 5 个 Phase 9 工具）：**

1. `set_word_character_format` → `restore_range_font`
   ```typescript
   it('单步撤销 set_word_character_format：restoreRangeFont 收 Record 对象 → rolled_back', async () => {
     const restoreRangeFontFn = vi.fn(async (_args: Record<string, unknown>): Promise<void> => {});
     const mockAdapter = { restoreRangeFont: restoreRangeFontFn } as unknown as DocumentAdapterForReplay;
     const entry: OperationLogEntry = {
       runId: 'run-w1', stepIndex: 0,
       toolName: 'set_word_character_format',   // ← D-17 必须出现在文件内
       args: { paragraphIndex: 1, font: { bold: true } },
       humanLabel: '将第 2 段设为加粗 14pt',
       reverse: {
         tool: 'restore_range_font',
         args: { index: 1, expectedText: '原段落文本', before: { bold: false, italic: false, size: 12, color: null, name: null, underline: 'None' } },
       },
       postState: { kind: 'word_char_format', content: { index: 1 } },
       timestamp: 0,
     };
     const detail = await replayUndoSingle(entry, mockAdapter);
     expect(detail.status).toBe('rolled_back');
     expect(restoreRangeFontFn).toHaveBeenCalledTimes(1);
     const receivedArgs = restoreRangeFontFn.mock.calls[0][0] as Record<string, unknown>;
     expect(typeof receivedArgs).toBe('object');
     expect(receivedArgs.index).toBe(1);
     expect(receivedArgs.before).toBeDefined();
   });
   ```

2. `set_word_paragraph_format` → `restore_paragraph_format`（同范式，postState.kind: `'word_para_format'`，reverse.args 含 before: { lineSpacing, spaceBefore, spaceAfter, alignment, indent }）

3. `apply_paragraph_style` → `restore_paragraph_style`（postState.kind: `'word_style'`，reverse.args 含 before: { style: 'Normal', styleBuiltIn: 'Normal' }）

4. `find_and_replace` → `restore_range_snapshot`（postState.kind: `'word_snapshot'`，reverse.args 含 snapshot: [{ paragraphIndex: 0, text: '原文' }]）

5. `insert_table` → `delete_table_by_marker`（postState.kind: `'word_table'`，reverse.args 含 contentFingerprint: '|__3x3', rows: 3, cols: 3, afterParagraphIndex: 0）

**注意：** 这些测试目前会失败，原因是 operationLog.ts 的 executeReverse switch 还没有 5 个新 case（计划 02 加），导致 `default: throw new Error('未知 reverse tool')` 触发 → status = `skipped_error` 而非 `rolled_back`。这是预期的 RED 状态。

**mockWord 扩展（在同一文件的 mockWord 工厂函数中）：** 在现有的 `mockWord` 工厂中，`items` 元素可不必扩展（integration 守门测试使用 mock adapter，不调用真实 WordAdapter 方法）。这 5 个守门测试的 mock adapter 是 `vi.fn()`，不调用 Word.run。
  </action>
  <verify>
    <automated>grep -c "set_word_character_format\|set_word_paragraph_format\|apply_paragraph_style\|find_and_replace\|insert_table" src/agent/operationLog.integration.test.ts</automated>
  </verify>
  <done>
    - operationLog.integration.test.ts 包含 5 个工具名字符串字面量（D-17 预埋）
    - 每个工具有独立 it 块，结构完整（vi.fn + mockAdapter + OperationLogEntry + replayUndoSingle + 3 个 expect）
    - grep -c 输出 ≥ 5（每个工具名各出现至少 1 次）
    - npm run test -- operationLog.integration 可执行（有些测试 FAIL 是预期，因为 case 未加）
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 WordAdapter.read.test.ts 和 word.test.ts 测试骨架</name>
  <files>src/adapters/WordAdapter.read.test.ts, src/agent/tools/write/word.test.ts</files>
  <read_first>
    - src/adapters/WordAdapter.ts（lines 540-580，selection_detail 现有 case 实现）
    - src/agent/tools/write/word.ts（完整文件，了解现有 ToolDef 结构）
    - .planning/phases/09-word-d-b-word/09-VALIDATION.md（Wave 0 Requirements 清单）
    - src/agent/operationLog.integration.test.ts（lines 33-45，mockWord 工厂参考）
  </read_first>
  <action>
**文件 1：`src/adapters/WordAdapter.read.test.ts`**

创建新文件（如果已存在则追加 describe 块）。包含：

```typescript
import { describe, it, expect, vi, afterEach } from 'vitest';
import { WordAdapter } from './WordAdapter';

// mockWord 工厂（含 paragraphs + uniqueLocalId 字段）
function mockWordForRead(paragraphTexts: Array<{ text: string; uniqueLocalId?: string }>) {
  (global as unknown as Record<string, unknown>).Office = {
    context: {
      requirements: {
        isSetSupported: vi.fn((setName: string, version: string) =>
          setName === 'WordApi' && version === '1.6'
        ),
      },
    },
  };
  const items = paragraphTexts.map(({ text, uniqueLocalId }) => ({
    text,
    uniqueLocalId: uniqueLocalId ?? 'mock-uid-' + text,
    load: vi.fn(),
  }));
  (global as unknown as Record<string, unknown>).Word = {
    InsertLocation: { end: 'End' },
    run: vi.fn(async (cb: (ctx: unknown) => unknown) =>
      cb({
        document: {
          body: { paragraphs: { load: vi.fn(), items } },
          getSelection: () => ({ load: vi.fn(), text: items[0]?.text ?? '' }),
        },
        sync: vi.fn().mockResolvedValue(undefined),
      }),
    ),
  };
}

afterEach(() => {
  delete (global as Record<string, unknown>).Word;
  delete (global as Record<string, unknown>).Office;
});

describe('WordAdapter.read selection_detail — WSEL-01 扩展（Phase 9）', () => {
  it('TODO(计划 03): selection_detail 返回 paragraphIndex + uniqueLocalId', async () => {
    // RED 骨架：实现后取消 .skip
    mockWordForRead([
      { text: '第一段', uniqueLocalId: 'uid-001' },
      { text: '第二段', uniqueLocalId: 'uid-002' },
    ]);
    const adapter = new WordAdapter();
    const result = await adapter.read({ kind: 'selection_detail' });
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    const data = result.data as Record<string, unknown>;
    expect(data.paragraphIndex).toBe(0);           // 选中第一段
    expect(data.uniqueLocalId).toBe('uid-001');    // 返回 uniqueLocalId
  });

  it('TODO(计划 03): selection_detail — 不支持 WordApi 1.6 时 uniqueLocalId 返 null（降级 D-03）', async () => {
    // RED 骨架
    (global as unknown as Record<string, unknown>).Office = {
      context: {
        requirements: { isSetSupported: vi.fn().mockReturnValue(false) },
      },
    };
    mockWordForRead([{ text: '段落' }]);
    const adapter = new WordAdapter();
    const result = await adapter.read({ kind: 'selection_detail' });
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    const data = result.data as Record<string, unknown>;
    expect(data.paragraphIndex).toBe(0);
    expect(data.uniqueLocalId).toBeNull();
  });
});
```

**文件 2：`src/agent/tools/write/word.test.ts`**

创建新文件，包含：

```typescript
import { describe, it, expect, vi } from 'vitest';

describe('apply_paragraph_style — D-08 allowlist 校验（Phase 9, 计划 05 实现）', () => {
  it('TODO(计划 05): 非法 styleName（中文样式名）被 allowlist 拒绝，Word.run 未调用', async () => {
    // RED 骨架：apply_paragraph_style ToolDef 不存在时跳过
    // 实现后：import { applyParagraphStyle } from './word'; 取消注释并验证
    expect(true).toBe(true); // placeholder，计划 05 替换为真实断言
  });

  it('TODO(计划 05): 非法 styleName "Normal1"（不在 allowlist）→ { ok: false, error.code: INVALID_PARAM }', async () => {
    expect(true).toBe(true); // placeholder
  });

  it('TODO(计划 05): 合法 styleName "Heading1" → 调用 adapter.applyParagraphStyle（不被 allowlist 拒绝）', async () => {
    expect(true).toBe(true); // placeholder
  });
});

describe('Phase 9 write tools — reverse/postState 形状测试骨架（计划 04-07 实现）', () => {
  it('TODO(计划 04): set_word_character_format reverse.tool === "restore_range_font"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 04): set_word_paragraph_format reverse.tool === "restore_paragraph_format"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 05): apply_paragraph_style reverse.tool === "restore_paragraph_style"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 06): find_and_replace reverse.tool === "restore_range_snapshot"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 07): insert_table reverse.tool === "delete_table_by_marker"', () => {
    expect(true).toBe(true);
  });
});
```

**注意：** 两个文件的 TODO 测试都标注对应的计划编号，方便执行者知道哪个计划来替换它们。word.test.ts 使用 placeholder `expect(true).toBe(true)` 确保文件语法正确、npm test 通过（GREEN placeholder），计划 04-07 执行时替换为真实断言。
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|word\.test|WordAdapter\.read)" | head -20</automated>
  </verify>
  <done>
    - src/adapters/WordAdapter.read.test.ts 存在，包含 2 个 selection_detail 测试用例（describe + 2 it）
    - src/agent/tools/write/word.test.ts 存在，包含 allowlist + 形状测试骨架（8 个 it，全部 placeholder GREEN）
    - npm run test 通过（wave 0 不引入新的 RED，integration 守门测试的 FAIL 由已有的 RED 贡献）
    - grep "TODO(计划" src/agent/tools/write/word.test.ts | wc -l 输出 ≥ 5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → operationLog | 守门测试通过 mock adapter 调用 replayUndoSingle，不访问真实 Office 命名空间 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Elevation of Privilege | operationLog.integration.test.ts | mitigate | Wave 0 预埋 5 个 toolName 字符串字面量；contract.test.ts D-17 硬卡将在 integrationTest:true 时扫描这些字符串，确保 inverse Record 签名路径被覆盖 |
| T-9-00 | Tampering | 测试骨架空文件 | mitigate | word.test.ts 使用 placeholder GREEN 测试（不引入 RED），避免 CI 误报；注释标注「计划 0X 替换」 |
</threat_model>

<verification>
```bash
# 1. 工具名字符串预埋（D-17 前提）
grep -c "set_word_character_format\|set_word_paragraph_format\|apply_paragraph_style\|find_and_replace\|insert_table" src/agent/operationLog.integration.test.ts
# 期望 ≥ 5

# 2. 守门测试结构完整
grep -c "replayUndoSingle" src/agent/operationLog.integration.test.ts
# 期望 ≥ 6（现有 ≥1 + 新增 5）

# 3. 全套测试可运行（部分 RED 可接受）
npm run test -- operationLog.integration 2>&1 | tail -5
```
</verification>

<success_criteria>
- operationLog.integration.test.ts 包含 5 个新 describe/it 块，每个含工具名字符串字面量（D-17 预埋）
- WordAdapter.read.test.ts 存在且 describe 结构正确
- word.test.ts 存在，placeholder 测试全绿，TODO 注释标注对应计划
- contract.test.ts 的 D-17 硬卡检查（npm run test -- contract）不因 Wave 0 新增的 false 条目报错
</success_criteria>

<output>
完成后创建 `.planning/phases/09-word-d-b-word/09-01-SUMMARY.md`
</output>

---
phase: 09-word-d-b-word
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agent/operationLog.integration.test.ts
  - src/adapters/WordAdapter.read.test.ts
  - src/agent/tools/write/word.test.ts
autonomous: true
requirements:
  - WSEL-01
  - WORD-01
  - WORD-02
  - WORD-03
  - WORD-04
  - WORD-05

must_haves:
  truths:
    - "operationLog.integration.test.ts 包含 5 个 Word 工具的 toolName 字符串字面量（D-17 硬卡前提）"
    - "5 条守门测试用真 WordAdapter 实例（new WordAdapter()）+ 扩展 mockWord 工厂（仅 mock Office.js 全局），不用 mock adapter——只有真 adapter 才能捕获 Phase 5 位置签名 bug（project_adapter_inverse_signature）"
    - "每个守门测试目前 RED（真 adapter 方法缺失 → skipped_error / 编译失败），各工具实现（波次 2-6）后逐步变绿"
    - "selection_detail 单测目前 RED（paragraphIndex 字段尚未扩展），计划 03 实现后变绿"
    - "D-08 allowlist 单测目前 RED（apply_paragraph_style 工具未实现），计划 05 实现后变绿"
  artifacts:
    - path: "src/agent/operationLog.integration.test.ts"
      provides: "5 条 Word 守门测试（真 WordAdapter + 扩展 mockWord，toolName 字面量出现，D-17 预埋）"
    - path: "src/adapters/WordAdapter.read.test.ts"
      provides: "selection_detail paragraphIndex + uniqueLocalId 单测骨架"
    - path: "src/agent/tools/write/word.test.ts"
      provides: "D-08 allowlist 拒绝测试 + 5 工具 reverse/postState 形状测试骨架"
  key_links:
    - from: "src/agent/contract.test.ts"
      to: "src/agent/operationLog.integration.test.ts"
      via: "fs.readFileSync D-17 硬卡"
      pattern: "set_word_character_format|set_word_paragraph_format|apply_paragraph_style|find_and_replace|insert_table"
---

<objective>
Wave 0：写出所有 Phase 9 测试骨架（RED 状态）。

Purpose: D-17/D-18 硬门要求工具名字符串在 integration.test.ts 中出现，contract.test.ts 的 fs.readFileSync 扫描发生在 CI 时，必须在工具实现（Wave 1–6）之前预埋这些字符串。Wave 0 测试是 RED，各工具实现后逐步变 GREEN。

**关键（项目 #1 历史故障守门）：** 5 条 integration 守门测试必须用 **真 WordAdapter 实例 + mock Office.js 宿主**，绝不能用 mock adapter。Phase 5 的位置签名 bug（`(text: string)` 收到 replay 传来的对象 → 全部 Word undo 静默挂 skipped_error）之所以漏过测试，正是因为当时用 `vi.fn()` mock adapter——mock 收什么形状的 args 都不报错，无法捕获签名错配。本文件头部注释已强制「真 WordAdapter 实例 + mock Office.js 宿主」。范式锚点 = 现有 `append_paragraph` 守门测试（operationLog.integration.test.ts ~line 154-166：`new WordAdapter()` + `mockWord([...])`），**不是** `replace_paragraph` 的 mock-adapter 测试。

Output:
- 5 条 Word integration 守门测试（真 WordAdapter + 扩展 mockWord，骨架，RED）
- 1 个 selection_detail 单测（骨架，RED）
- 1 个 word.test.ts（allowlist 拒绝测试 + 形状测试骨架，RED）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-word-d-b-word/09-CONTEXT.md
@.planning/phases/09-word-d-b-word/09-RESEARCH.md
@.planning/phases/09-word-d-b-word/09-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 执行者需要的现有接口，从代码库提取 -->

From src/agent/operationLog.ts (lines 83-106):
```typescript
export interface DocumentAdapterForReplay {
  deleteParagraphByContent?: (args: Record<string, unknown>) => Promise<void>;
  readWordParagraph?: (args: Record<string, unknown>) => Promise<string>;
  overwriteRange?: (args: Record<string, unknown>) => Promise<void>;
  readExcelRange?: (args: Record<string, unknown>) => Promise<unknown[][]>;
  deleteSlideByTitle?: (args: Record<string, unknown>) => Promise<void>;
  readPptSlideTitle?: (args: Record<string, unknown>) => Promise<string>;
  restoreShapeProperty?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeGeometry?: (args: Record<string, unknown>) => Promise<void>;
  restoreShapeText?: (args: Record<string, unknown>) => Promise<void>;
  deleteChartByName?: (args: Record<string, unknown>) => Promise<void>;
  restoreParagraphAt?: (args: Record<string, unknown>) => Promise<void>;
}
```

From src/agent/operationLog.ts (lines 44-53):
```typescript
export interface OperationLogEntry {
  runId: string;
  stepIndex: number;
  toolName: string;
  args: unknown;
  humanLabel: string;
  reverse: ReverseDescriptor;
  postState?: PostStateSnapshot;
  timestamp: number;
}
```

From src/agent/operationLog.integration.test.ts (lines 33-45) — 现有 mockWord 工厂（Phase 9 必须扩展，见 Task 1）:
```typescript
function mockWord(paragraphTexts: string[]): Array<{ text: string; delete: ReturnType<typeof vi.fn> }> {
  const items = paragraphTexts.map((text) => ({ text, delete: vi.fn() }));
  (global as unknown as Record<string, unknown>).Word = {
    InsertLocation: { end: 'End' },
    run: vi.fn(async (cb: (ctx: unknown) => unknown) =>
      cb({
        document: { body: { paragraphs: { load: vi.fn(), items } } },
        sync: vi.fn().mockResolvedValue(undefined),
      }),
    ),
  };
  return items;
}
```

From src/agent/operationLog.integration.test.ts (lines 154-166) — ✅ 正确范式锚点（真 WordAdapter + mockWord）:
```typescript
it('单步撤销 append_paragraph：reverse.args 对象被正确消费 → rolled_back（非 skipped_error）', async () => {
  const items = mockWord(['无关段', '追加段']);
  const adapter = new WordAdapter();            // ← 真 adapter，非 mock
  const detail = await replayUndoSingle(
    wordEntry(0, '追加段'),
    adapter as unknown as DocumentAdapterForReplay,
  );
  // 旧 (text:string) 签名下此处会是 'skipped_error'（normalizeText 对对象抛 TypeError）
  expect(detail.status).toBe('rolled_back');
  expect(items[1].delete).toHaveBeenCalledTimes(1);
});
```

⚠️ **不要** 仿照 `replace_paragraph`（lines 124-153）那条——它用 `vi.fn()` mock adapter，收什么 args 都不报错，CANNOT 捕获签名错配。Phase 9 五条守门必须仿 `append_paragraph` 的真 adapter 路径。

From src/agent/operationLog.integration.test.ts (lines 93-105) — wordEntry helper（构造真实形状 entry）:
```typescript
function wordEntry(stepIndex: number, text: string): OperationLogEntry {
  return {
    runId: 'run-it', stepIndex,
    toolName: 'append_paragraph',
    args: { text },
    humanLabel: `在文档末尾追加段落「${text}」`,
    reverse: { tool: 'delete_paragraph_by_content', args: { text } }, // ← 对象，非位置参
    postState: { kind: 'word_paragraph', content: text },
    timestamp: 0,
  };
}
```

Phase 9 新增的 PostStateSnapshot.kind（计划 02 后有效）:
- `'word_char_format'`（set_word_character_format）
- `'word_para_format'`（set_word_paragraph_format）
- `'word_style'`（apply_paragraph_style）
- `'word_snapshot'`（find_and_replace）
- `'word_table'`（insert_table）

Phase 9 新增的 DocumentAdapterForReplay 方法（计划 02 后有效）:
- `restoreRangeFont?: (args: Record<string, unknown>) => Promise<void>`
- `restoreParagraphFormat?: (args: Record<string, unknown>) => Promise<void>`
- `restoreParagraphStyle?: (args: Record<string, unknown>) => Promise<void>`
- `restoreRangeSnapshot?: (args: Record<string, unknown>) => Promise<void>`
- `deleteTableByMarker?: (args: Record<string, unknown>) => Promise<void>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 追加 5 条 Word inverse 守门测试到 operationLog.integration.test.ts（真 WordAdapter，RED 骨架）</name>
  <files>src/agent/operationLog.integration.test.ts</files>
  <read_first>
    - src/agent/operationLog.integration.test.ts（完整文件，重点：mockWord 工厂 lines 33-45、wordEntry helper lines 93-105、✅ append_paragraph 真 adapter 守门 lines 154-166、afterEach 清理 lines 107-113）
    - src/adapters/WordAdapter.ts（lines 280-400，了解 replaceParagraphAt / restoreParagraphAt 闭包内如何访问 paras.items[i].text、insertText；Phase 9 新方法会访问 .font / .lineSpacing / .styleBuiltIn / body.search / body.tables / body.insertTable）
    - .planning/phases/09-word-d-b-word/09-PATTERNS.md §operationLog.integration.test.ts（守门范式 + mockWord 扩展说明）
    - .planning/phases/08-foundation-a-f/CONTRACT.md §Phase 9 Word 工具（reverse_tool 逐字确认）
  </read_first>
  <action>
**守门测试必须用真 WordAdapter（项目 #1 历史故障守门）。** 仿 `append_paragraph` 测试（lines 154-166），**不要**仿 `replace_paragraph`（mock adapter，捕不到签名错配）。每条测试：`new WordAdapter()` 真实例 + 扩展 mockWord 工厂提供新方法所需的 Word 全局 → `replayUndoSingle(entry, adapter)` → 断言 `status === 'rolled_back'`（证明真 adapter 的 inverse Record 签名正确消费了 reverse.args，未抛 TypeError = 未复现 Phase 5 bug）。

**Step A：扩展 mockWord 工厂（让真 WordAdapter 的 5 个 inverse 方法能跑通）。**

现有 mockWord 只给 `{ text, delete }`。Phase 9 inverse 方法需要更丰富的段落对象 + body 集合。新增一个扩展工厂 `mockWordRich`（保留原 mockWord 不动，供旧测试用）：

```typescript
// Phase 9：扩展 mock，供真 WordAdapter 的 5 个 inverse 方法在闭包内访问
// font 属性包 / 段落格式属性 / style / styleBuiltIn / body.search / body.tables / body.insertTable
function mockWordRich(opts?: {
  paragraphTexts?: string[];
  tables?: Array<{ rowCount: number; columnCount: number; values: string[][]; delete: ReturnType<typeof vi.fn> }>;
}): {
  paraItems: Array<Record<string, unknown>>;
  tableItems: Array<{ rowCount: number; columnCount: number; values: string[][]; delete: ReturnType<typeof vi.fn> }>;
} {
  const texts = opts?.paragraphTexts ?? ['原段落文本', '第二段'];
  // 每个段落带可读写 font（属性包）+ 段落格式属性 + style/styleBuiltIn + insertText/getRange
  const paraItems = texts.map((text) => ({
    text,
    uniqueLocalId: 'uid-' + text,
    font: { bold: false, italic: false, underline: 'None', size: 12, color: '#000000', name: 'Calibri' },
    lineSpacing: 12, spaceBefore: 0, spaceAfter: 0, alignment: 'Left',
    firstLineIndent: 0, leftIndent: 0,
    style: 'Normal', styleBuiltIn: 'Normal',
    load: vi.fn(),
    insertText: vi.fn(),
    getRange: vi.fn(() => ({ insertTable: vi.fn() })),
    insertTable: vi.fn(),
  }));
  const tableItems = opts?.tables ?? [];
  // body.search 返回一个 RangeCollection：items 为匹配 range（每个有 text + insertText）
  const searchResults = {
    load: vi.fn(),
    items: [{ text: texts[0], insertText: vi.fn() }],
  };
  (global as unknown as Record<string, unknown>).Word = {
    InsertLocation: { end: 'End', replace: 'Replace', after: 'After', start: 'Start' },
    run: vi.fn(async (cb: (ctx: unknown) => unknown) =>
      cb({
        document: {
          body: {
            paragraphs: { load: vi.fn(), items: paraItems },
            tables: { load: vi.fn(), items: tableItems },
            search: vi.fn(() => searchResults),
            insertTable: vi.fn(() => ({
              load: vi.fn(), rowCount: 3, columnCount: 3,
              values: [['a', 'b', 'c'], ['', '', ''], ['', '', '']],
              delete: vi.fn(),
            })),
          },
        },
        sync: vi.fn().mockResolvedValue(undefined),
      }),
    ),
  };
  return { paraItems, tableItems };
}
```

注意：`mockWordRich` 的内部形状要对齐各 inverse 方法实际访问的字段。各 inverse 方法签名/字段以计划 04-07 实现为准；执行 Wave 0 时若某字段名尚不确定，按本草案占位，等对应工具计划（02-07）实现时由那个计划微调本工厂（plan 04-07 的 read_first 已包含本文件）。`afterEach` 的 `delete global.Word` 已存在（line 108），无需重复。

**Step B：在文件末尾的 `describe('集成：replay engine × 真 WordAdapter', ...)` 块内（即 append_paragraph 测试同一 describe），追加 5 个 Phase 9 守门测试。**

每个测试结构（以第 1 个为模板，其余 4 个同构）：

1. `set_word_character_format` → `restore_range_font`
```typescript
it('单步撤销 set_word_character_format：真 WordAdapter.restoreRangeFont 收 Record 对象 → rolled_back', async () => {
  mockWordRich({ paragraphTexts: ['原段落文本', '第二段'] });
  const adapter = new WordAdapter();   // ← 真 adapter（捕获 Phase 5 签名 bug）
  const entry: OperationLogEntry = {
    runId: 'run-w1', stepIndex: 0,
    toolName: 'set_word_character_format',   // ← D-17 硬卡：字符串必须出现在本文件
    args: { paragraphIndex: 0, font: { bold: true } },
    humanLabel: '将第 1 段设为加粗',
    reverse: {
      tool: 'restore_range_font',
      args: { index: 0, expectedText: '原段落文本', before: { bold: false, italic: false, underline: 'None', size: 12, color: '#000000', name: 'Calibri' } },
    },
    postState: { kind: 'word_char_format', content: { index: 0 } },
    timestamp: 0,
  };
  const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
  // 旧位置签名会在 restoreRangeFont 解构 args 时抛 → skipped_error；Record 签名 → rolled_back
  expect(detail.status).toBe('rolled_back');
});
```

2. `set_word_paragraph_format` → `restore_paragraph_format`（reverse.args = { index: 0, expectedText: '原段落文本', before: { lineSpacing: 12, spaceBefore: 0, spaceAfter: 0, alignment: 'Left', indent: 0, leftIndent: 0 } }，postState.kind: 'word_para_format'）

3. `apply_paragraph_style` → `restore_paragraph_style`（reverse.args = { index: 0, expectedText: '原段落文本', before: { style: 'Normal', styleBuiltIn: 'Normal' } }，postState.kind: 'word_style'）

4. `find_and_replace` → `restore_range_snapshot`（reverse.args = { snapshot: [{ paragraphIndex: 0, text: '原段落文本' }] }，postState.kind: 'word_snapshot'）

5. `insert_table` → `delete_table_by_marker`（mockWordRich 传 tables: [{ rowCount: 3, columnCount: 3, values: [['a','b','c'],['','',''],['','','']], delete: vi.fn() }]；reverse.args = { contentFingerprint: 'a|b|c__3x3', rows: 3, cols: 3, afterParagraphIndex: undefined }，postState.kind: 'word_table'。断言 rolled_back + tableItems[0].delete 被调用 1 次）

**RED 状态预期：** Wave 0 时这些测试 FAIL，原因有二（任一即可）：
(a) 真 WordAdapter 还没有这 5 个 inverse 方法（计划 02 加接口 + 04-07 加实现）→ executeReverse 找不到 case 或 adapter 方法不存在 → skipped_error；
(b) `DocumentAdapterForReplay` 接口暂无这 5 个方法（计划 02 加）→ TS 编译错误。用 `adapter as unknown as DocumentAdapterForReplay` 绕过编译，让测试能跑（RED 而非编译失败）。

**toolName 字符串字面量（D-17 硬卡）：** 5 个 `toolName: 'xxx'` 必须是字面量，contract.test.ts 用 fs.readFileSync 扫描本文件查找这些字符串。

**真 adapter 无法跑通的兜底：** 若某个 inverse 方法在 Wave 0 阶段确实无法用合理的 Word mock 跑通（如依赖尚未实现的复杂 proxy 行为），保留该测试的 `toolName` 字符串字面量（D-17 硬卡必需）+ 加 `// TODO(计划 0X): 待 adapter 方法实现后切到真 adapter 断言 rolled_back` 注释，但**优先**走真 adapter 路径——5 个方法都是 paras.items / body.tables 级操作，扩展 mockWordRich 后均可跑通，不应有兜底。
  </action>
  <verify>
    <automated>grep -c "set_word_character_format\\|set_word_paragraph_format\\|apply_paragraph_style\\|find_and_replace\\|insert_table" src/agent/operationLog.integration.test.ts</automated>
  </verify>
  <done>
    - operationLog.integration.test.ts 包含 5 个工具名字符串字面量（D-17 预埋），grep -c ≥ 5
    - 5 个守门测试都用 `new WordAdapter()`（真 adapter），不用 `vi.fn()` mock adapter——grep "new WordAdapter" src/agent/operationLog.integration.test.ts 输出 ≥ 6（原 append_paragraph 等 + 新 5 条）
    - mockWordRich 工厂存在（提供 font/段落格式/style/styleBuiltIn/search/tables/insertTable）
    - 每条测试断言 `detail.status === 'rolled_back'`（真 adapter 消费 Record args 不抛）
    - npm run test -- operationLog.integration 可执行（这 5 条 RED 是预期，因 adapter 方法尚未实现）
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 WordAdapter.read.test.ts 和 word.test.ts 测试骨架</name>
  <files>src/adapters/WordAdapter.read.test.ts, src/agent/tools/write/word.test.ts</files>
  <read_first>
    - src/adapters/WordAdapter.ts（lines 540-580，selection_detail 现有 case 实现）
    - src/agent/tools/write/word.ts（完整文件，了解现有 ToolDef 结构）
    - .planning/phases/09-word-d-b-word/09-VALIDATION.md（Wave 0 Requirements 清单）
    - src/agent/operationLog.integration.test.ts（lines 33-45，mockWord 工厂参考）
  </read_first>
  <action>
**文件 1：`src/adapters/WordAdapter.read.test.ts`**

创建新文件（如果已存在则追加 describe 块）。包含：

```typescript
import { describe, it, expect, vi, afterEach } from 'vitest';
import { WordAdapter } from './WordAdapter';

// mockWord 工厂（含 paragraphs + uniqueLocalId 字段）
function mockWordForRead(paragraphTexts: Array<{ text: string; uniqueLocalId?: string }>) {
  (global as unknown as Record<string, unknown>).Office = {
    context: {
      requirements: {
        isSetSupported: vi.fn((setName: string, version: string) =>
          setName === 'WordApi' && version === '1.6'
        ),
      },
    },
  };
  const items = paragraphTexts.map(({ text, uniqueLocalId }) => ({
    text,
    uniqueLocalId: uniqueLocalId ?? 'mock-uid-' + text,
    load: vi.fn(),
  }));
  (global as unknown as Record<string, unknown>).Word = {
    InsertLocation: { end: 'End' },
    run: vi.fn(async (cb: (ctx: unknown) => unknown) =>
      cb({
        document: {
          body: { paragraphs: { load: vi.fn(), items } },
          getSelection: () => ({ load: vi.fn(), text: items[0]?.text ?? '' }),
        },
        sync: vi.fn().mockResolvedValue(undefined),
      }),
    ),
  };
}

afterEach(() => {
  delete (global as Record<string, unknown>).Word;
  delete (global as Record<string, unknown>).Office;
});

describe('WordAdapter.read selection_detail — WSEL-01 扩展（Phase 9）', () => {
  it('TODO(计划 03): selection_detail 返回 paragraphIndex + uniqueLocalId', async () => {
    // RED 骨架：实现后取消 .skip
    mockWordForRead([
      { text: '第一段', uniqueLocalId: 'uid-001' },
      { text: '第二段', uniqueLocalId: 'uid-002' },
    ]);
    const adapter = new WordAdapter();
    const result = await adapter.read({ kind: 'selection_detail' });
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    const data = result.data as Record<string, unknown>;
    expect(data.paragraphIndex).toBe(0);           // 选中第一段
    expect(data.uniqueLocalId).toBe('uid-001');    // 返回 uniqueLocalId
  });

  it('TODO(计划 03): selection_detail — 不支持 WordApi 1.6 时 uniqueLocalId 返 null（降级 D-03）', async () => {
    // RED 骨架
    (global as unknown as Record<string, unknown>).Office = {
      context: {
        requirements: { isSetSupported: vi.fn().mockReturnValue(false) },
      },
    };
    mockWordForRead([{ text: '段落' }]);
    const adapter = new WordAdapter();
    const result = await adapter.read({ kind: 'selection_detail' });
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    const data = result.data as Record<string, unknown>;
    expect(data.paragraphIndex).toBe(0);
    expect(data.uniqueLocalId).toBeNull();
  });
});
```

**文件 2：`src/agent/tools/write/word.test.ts`**

创建新文件，包含：

```typescript
import { describe, it, expect, vi } from 'vitest';

describe('apply_paragraph_style — D-08 allowlist 校验（Phase 9, 计划 05 实现）', () => {
  it('TODO(计划 05): 非法 styleName（中文样式名）被 allowlist 拒绝，Word.run 未调用', async () => {
    // RED 骨架：apply_paragraph_style ToolDef 不存在时跳过
    // 实现后：import { applyParagraphStyle } from './word'; 取消注释并验证
    expect(true).toBe(true); // placeholder，计划 05 替换为真实断言
  });

  it('TODO(计划 05): 非法 styleName "Normal1"（不在 allowlist）→ { ok: false, error.code: INVALID_PARAM }', async () => {
    expect(true).toBe(true); // placeholder
  });

  it('TODO(计划 05): 合法 styleName "Heading1" → 调用 adapter.applyParagraphStyle（不被 allowlist 拒绝）', async () => {
    expect(true).toBe(true); // placeholder
  });
});

describe('Phase 9 write tools — reverse/postState 形状测试骨架（计划 04-07 实现）', () => {
  it('TODO(计划 04): set_word_character_format reverse.tool === "restore_range_font"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 04): set_word_paragraph_format reverse.tool === "restore_paragraph_format"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 05): apply_paragraph_style reverse.tool === "restore_paragraph_style"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 06): find_and_replace reverse.tool === "restore_range_snapshot"', () => {
    expect(true).toBe(true);
  });
  it('TODO(计划 07): insert_table reverse.tool === "delete_table_by_marker"', () => {
    expect(true).toBe(true);
  });
});
```

**注意：** 两个文件的 TODO 测试都标注对应的计划编号，方便执行者知道哪个计划来替换它们。word.test.ts 使用 placeholder `expect(true).toBe(true)` 确保文件语法正确、npm test 通过（GREEN placeholder），计划 04-07 执行时替换为真实断言。
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|word\.test|WordAdapter\.read)" | head -20</automated>
  </verify>
  <done>
    - src/adapters/WordAdapter.read.test.ts 存在，包含 2 个 selection_detail 测试用例（describe + 2 it）
    - src/agent/tools/write/word.test.ts 存在，包含 allowlist + 形状测试骨架（8 个 it，全部 placeholder GREEN）
    - npm run test 通过（wave 0 不引入新的 RED，integration 守门测试的 FAIL 由已有的 RED 贡献）
    - grep "TODO(计划" src/agent/tools/write/word.test.ts | wc -l 输出 ≥ 5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → operationLog → 真 WordAdapter | 守门测试用真 WordAdapter 实例经 replayUndoSingle，仅 mock Office.js 全局（捕获 inverse 签名错配） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Elevation of Privilege | operationLog.integration.test.ts | mitigate | Wave 0 预埋 5 个 toolName 字符串字面量（D-17 硬卡）；守门用真 WordAdapter（非 mock adapter）→ replayUndoSingle，若 inverse 用位置签名收到 Record 对象会抛 → skipped_error，测试红 = 捕获 Phase 5 故障模式（mock adapter 漏过此 bug，project_adapter_inverse_signature） |
| T-9-00 | Tampering | 测试骨架空文件 | mitigate | word.test.ts 使用 placeholder GREEN 测试（不引入 RED），避免 CI 误报；注释标注「计划 0X 替换」 |
</threat_model>

<verification>
```bash
# 1. 工具名字符串预埋（D-17 前提）
grep -c "set_word_character_format\|set_word_paragraph_format\|apply_paragraph_style\|find_and_replace\|insert_table" src/agent/operationLog.integration.test.ts
# 期望 ≥ 5

# 2. 守门测试结构完整
grep -c "replayUndoSingle" src/agent/operationLog.integration.test.ts
# 期望 ≥ 6（现有 ≥1 + 新增 5）

# 2b. 守门用真 WordAdapter（非 mock adapter）—— 项目 #1 故障守门
grep -c "new WordAdapter" src/agent/operationLog.integration.test.ts
# 期望 ≥ 6（现有 append_paragraph 等 + 新增 5）

# 3. 全套测试可运行（部分 RED 可接受）
npm run test -- operationLog.integration 2>&1 | tail -5
```
</verification>

<success_criteria>
- operationLog.integration.test.ts 包含 5 个新 Phase 9 守门 it（与 append_paragraph 同 describe 块内），每个用真 WordAdapter + 工具名字符串字面量（D-17 预埋）
- 5 条守门均用 `new WordAdapter()`（非 mock adapter）+ 扩展 mockWordRich 工厂
- WordAdapter.read.test.ts 存在且 describe 结构正确
- word.test.ts 存在，placeholder 测试全绿，TODO 注释标注对应计划
- contract.test.ts 的 D-17 硬卡检查（npm run test -- contract）不因 Wave 0 新增的 false 条目报错
</success_criteria>

<output>
完成后创建 `.planning/phases/09-word-d-b-word/09-01-SUMMARY.md`
</output>

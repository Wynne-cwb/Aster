---
phase: 09-word-d-b-word
plan: "06"
type: execute
wave: 5
depends_on:
  - "09-05"
files_modified:
  - src/adapters/WordAdapter.ts
  - src/agent/tools/write/word.ts
  - src/agent/tools/write/word.test.ts
  - src/agent/tools/index.ts
  - src/agent/contract.test.ts
  - .planning/phases/08-foundation-a-f/CONTRACT.md
autonomous: true
requirements:
  - WORD-04

must_haves:
  truths:
    - "find_and_replace 执行后 data.replaced 包含实际替换数（成功标准 #4 的「改动卡显示改动数」）"
    - "find_and_replace undo 后全文文字还原（快照式 undo，restore_range_snapshot 按段还原）"
    - "受影响段落数超过 100 时降级为 noop+gate（warn 不中断 agent，D-10）"
    - "matchCase / matchWholeWord 参数透传给 body.search（D-11）"
    - "contract.test.ts WORD-04 行 integrationTest: true"
  artifacts:
    - path: "src/adapters/WordAdapter.ts"
      provides: "findAndReplace + restoreRangeSnapshot 方法"
      contains: "findAndReplace"
    - path: "src/agent/tools/write/word.ts"
      provides: "findAndReplace ToolDef（快照式 undo）"
      contains: "find_and_replace"
  key_links:
    - from: "src/agent/tools/write/word.ts findAndReplace.execute"
      to: "src/adapters/WordAdapter.findAndReplace"
      via: "ctx.adapter as WordAdapter"
      pattern: "restore_range_snapshot"
    - from: "operationLog executeReverse restore_range_snapshot"
      to: "src/adapters/WordAdapter.restoreRangeSnapshot"
      via: "adapter.restoreRangeSnapshot(args)"
      pattern: "restoreRangeSnapshot"
---

<objective>
Wave 5：实现 WORD-04（find_and_replace 快照式）。

Purpose: 成功标准 #4 要求全文查替换 + undo 文字全还原。这是本 Phase 唯一「快照式」工具，快照粒度 = 受影响段落整段 before-image（D-09）。超限 100 段降级为 noop+gate（D-10 DoS 防御）。

Output: WordAdapter 2 个新方法（写 + inverse）+ findAndReplace ToolDef + 测试更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-word-d-b-word/09-CONTEXT.md
@.planning/phases/09-word-d-b-word/09-RESEARCH.md
@.planning/phases/09-word-d-b-word/09-PATTERNS.md
@.planning/phases/08-foundation-a-f/CONTRACT.md

<interfaces>
<!-- 关键 API 片段，来自 RESEARCH.md Q6 -->

find_and_replace 流程（RESEARCH.md Q6 完整范式）:
```typescript
// Step 1: body.search 枚举匹配
const results = ctx.document.body.search(args.searchText, { matchCase, matchWholeWord });
results.load('items/text');
const paras = ctx.document.body.paragraphs;
paras.load('items/text');
await ctx.sync();

// Step 2: 构建 before-image（受影响段落整段文本，按 paragraphIndex keyed）
const affectedParaIndices = new Set<number>();
for (const range of results.items) {
  for (let i = 0; i < paras.items.length; i++) {
    if (normalizeText(paras.items[i].text).includes(normalizeText(range.text))) {
      affectedParaIndices.add(i); break;
    }
  }
}
// 超限检查
if (affectedParaIndices.size > SNAPSHOT_LIMIT) { /* noop+gate */ }
const snapshot = [...affectedParaIndices].map(i => ({
  paragraphIndex: i,
  text: paras.items[i].text,
}));

// Step 3: 执行替换
for (const range of results.items) {
  range.insertText(args.replaceText, Word.InsertLocation.replace);
}
await ctx.sync();
```

restoreRangeSnapshot inverse（RESEARCH.md Q6 Notes 4）:
```typescript
// 按 snapshot 列表逐段还原（复用 restoreParagraphAt 范式）
for (const { paragraphIndex, text } of snapshot) {
  // 双重定位 + insertText(text, 'replace')
}
```

D-10 超限降级（noop_inverse）:
```typescript
// 超限时 reverse.tool = 'noop_inverse'
return {
  ok: true,
  data: { replaced: 0, warning: '改动超过快照上限（100段），此次替换无法自动撤销' },
  reverse: { tool: 'noop_inverse', args: { reason: '超出 100 段快照上限' } },
  postState: { kind: 'word_snapshot' as const, content: { snapshottedParagraphs: 0 } },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WordAdapter.ts 新增 findAndReplace + restoreRangeSnapshot</name>
  <files>src/adapters/WordAdapter.ts</files>
  <read_first>
    - src/adapters/WordAdapter.ts（找到 restoreParagraphStyle 方法末尾，追加 2 个新方法）
    - .planning/phases/09-word-d-b-word/09-RESEARCH.md §Q6（完整代码示例 + SNAPSHOT_LIMIT 建议 100）
    - .planning/phases/09-word-d-b-word/09-CONTEXT.md D-09 D-10 D-11 D-12
  </read_first>
  <behavior>
    - findAndReplace 先枚举匹配，按段落 index 构建 snapshot，超过 100 段返回 noop 标记（不执行替换）
    - findAndReplace 执行替换后返回 { snapshot, replacedCount, overLimit: false } 或 { overLimit: true }
    - restoreRangeSnapshot 签名 `(args: Record<string, unknown>): Promise<void>`（D-17）
    - restoreRangeSnapshot 按 snapshot 数组逐段还原（复用双重定位范式）
  </behavior>
  <action>
在 `src/adapters/WordAdapter.ts` 追加（在 `restoreParagraphStyle` 之后）：

```typescript
// 快照上限（D-10：防止超大文档的 before-image 内存溢出）
const FIND_AND_REPLACE_SNAPSHOT_LIMIT = 100;

async findAndReplace(
  args: Record<string, unknown>
): Promise<{
  snapshot: Array<{ paragraphIndex: number; text: string }>;
  replacedCount: number;
  overLimit: boolean;
}> {
  const searchText = args.searchText as string;
  const replaceText = args.replaceText as string;
  const matchCase = (args.matchCase as boolean | undefined) ?? false;
  const matchWholeWord = (args.matchWholeWord as boolean | undefined) ?? false;

  try {
    return await Word.run(async (ctx) => {
      // Step 1: 枚举所有匹配
      const results = ctx.document.body.search(searchText, { matchCase, matchWholeWord });
      const paras = ctx.document.body.paragraphs;
      results.load('items/text');
      paras.load('items/text');
      await ctx.sync();

      if (results.items.length === 0) {
        return { snapshot: [], replacedCount: 0, overLimit: false };
      }

      // Step 2: 构建受影响段落集合（D-09: 近似段落归属）
      const affectedParaIndices = new Set<number>();
      for (const range of results.items) {
        const rangeNorm = normalizeText(range.text);
        for (let i = 0; i < paras.items.length; i++) {
          if (normalizeText(paras.items[i].text).includes(rangeNorm)) {
            affectedParaIndices.add(i);
            break;
          }
        }
      }

      // Step 2b: 超限检查（D-10: DoS 防御）
      if (affectedParaIndices.size > FIND_AND_REPLACE_SNAPSHOT_LIMIT) {
        return { snapshot: [], replacedCount: 0, overLimit: true };
      }

      // Step 2c: 存 before-image
      const snapshot = [...affectedParaIndices].map((i) => ({
        paragraphIndex: i,
        text: paras.items[i].text,  // 原始文本（含 \r 等，undo 时写回）
      }));

      // Step 3: 执行替换（D-11: matchCase + matchWholeWord 已透传）
      let replacedCount = 0;
      for (const range of results.items) {
        range.insertText(replaceText, Word.InsertLocation.replace);
        replacedCount++;
      }
      await ctx.sync();

      return { snapshot, replacedCount, overLimit: false };
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word findAndReplace 失败', err);
  }
}

async restoreRangeSnapshot(args: Record<string, unknown>): Promise<void> {
  // D-17: Record 签名守门
  const snapshot = args.snapshot as Array<{ paragraphIndex: number; text: string }>;

  try {
    await Word.run(async (ctx) => {
      const paras = ctx.document.body.paragraphs;
      paras.load('items/text');
      await ctx.sync();

      // 按 snapshot 顺序逐段还原（双重定位：index + 当前文本 fingerprint 回退）
      for (const { paragraphIndex, text: originalText } of snapshot) {
        const expectedCurrent = paras.items[paragraphIndex]?.text;

        // 快路径：index 有效直接还原
        let targetIndex = -1;
        if (paragraphIndex >= 0 && paragraphIndex < paras.items.length) {
          targetIndex = paragraphIndex;
        }

        if (targetIndex === -1) {
          // index 越界（段落数减少）→ 跳过该段（诚实处理，不 crash）
          continue;
        }

        // 注意：find_and_replace 替换后段落文本已变为替换后内容
        // 我们不用 expectedText 做指纹匹配（替换后文本 ≠ 原始文本）
        // 直接按 index 还原（这是快照式 undo 的特性：已知 snapshot 顺序对应 index）
        paras.items[targetIndex].insertText(originalText, Word.InsertLocation.replace);
        void expectedCurrent; // suppress unused var warning
      }
      await ctx.sync();
    });
  } catch (err) {
    if (err instanceof HostApiError) throw err;
    throw new HostApiError('Word restoreRangeSnapshot 失败', err);
  }
}
```
  </action>
  <verify>
    <automated>grep -n "findAndReplace\|restoreRangeSnapshot\|FIND_AND_REPLACE_SNAPSHOT_LIMIT" src/adapters/WordAdapter.ts | head -10</automated>
  </verify>
  <done>
    - grep 输出包含 findAndReplace 方法声明和 FIND_AND_REPLACE_SNAPSHOT_LIMIT 常量
    - npx tsc --noEmit 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: findAndReplace ToolDef + 测试更新 + 注册 + 标志位翻转</name>
  <files>src/agent/tools/write/word.ts, src/agent/tools/write/word.test.ts, src/agent/tools/index.ts, src/agent/contract.test.ts, .planning/phases/08-foundation-a-f/CONTRACT.md</files>
  <read_first>
    - src/agent/tools/write/word.ts（找到 applyParagraphStyle 末尾，追加新 ToolDef）
    - src/agent/tools/write/word.test.ts（找到 TODO(计划 06) 行）
    - src/agent/tools/index.ts（word case import + wordWriteTools）
    - src/agent/contract.test.ts（find_and_replace 行）
  </read_first>
  <action>
**文件 1：word.ts — 追加 findAndReplace ToolDef**

```typescript
// ---- Phase 9: find_and_replace（快照式 undo）-------------------------------

interface FindAndReplaceArgs {
  searchText: string;
  replaceText: string;
  matchCase?: boolean;
  matchWholeWord?: boolean;
}

export const findAndReplace: ToolDef<FindAndReplaceArgs> = {
  name: 'find_and_replace',
  kind: 'write',
  description: '全文查找替换文字（支持 matchCase / matchWholeWord）。undo 按段落快照还原，超过 100 个受影响段落时降级为不可撤销操作并给出提示。',
  parameters: {
    type: 'object',
    properties: {
      searchText: { type: 'string', description: '要查找的文字' },
      replaceText: { type: 'string', description: '替换后的文字' },
      matchCase: { type: 'boolean', description: '区分大小写（默认 false）' },
      matchWholeWord: { type: 'boolean', description: '全词匹配（默认 false）' },
    },
    required: ['searchText', 'replaceText'],
  },
  humanLabel: ({ searchText, replaceText }) =>
    `将「${String(searchText).slice(0, HUMAN_LABEL_TEXT_CAP)}」替换为「${String(replaceText).slice(0, HUMAN_LABEL_TEXT_CAP)}」`,
  async execute({ searchText, replaceText, matchCase, matchWholeWord }, ctx): Promise<ToolResult> {
    const result = await (ctx.adapter as WordAdapter).findAndReplace({
      searchText,
      replaceText,
      matchCase,
      matchWholeWord,
    });

    // D-10: 超限降级（noop+gate，不中断 agent）
    if (result.overLimit) {
      return {
        ok: true,
        data: {
          replaced: 0,
          warning: '受影响段落超过 100 个，此次替换无法自动撤销（已执行替换）',
        },
        reverse: { tool: 'noop_inverse', args: { reason: '超出 100 段快照上限' } },
        postState: { kind: 'word_snapshot' as const, content: { snapshottedParagraphs: 0 } },
      };
    }

    const reverse: ReverseDescriptor = {
      tool: 'restore_range_snapshot',  // ← CONTRACT.md 逐字对齐
      args: {
        snapshot: result.snapshot,
      },
    };
    const postState = {
      kind: 'word_snapshot' as const,
      content: { snapshottedParagraphs: result.snapshot.length },
    };
    return {
      ok: true,
      data: { replaced: result.replacedCount },  // D-12：返替换数（成功标准 #4）
      reverse,
      postState,
    };
  },
};
```

**文件 2：word.test.ts — 替换 TODO(计划 06) placeholder**

将 `TODO(计划 06): find_and_replace reverse.tool === "restore_range_snapshot"` 替换为：
```typescript
it('find_and_replace 正确导出且 name 属性为 "find_and_replace"', () => {
  expect(findAndReplace.name).toBe('find_and_replace');
});
```

在文件顶部 import 中追加 `findAndReplace`：
```typescript
import { applyParagraphStyle, setWordCharacterFormat, setWordParagraphFormat, findAndReplace } from './word';
```

**文件 3：index.ts — 追加 findAndReplace**

import：
```typescript
import {
  appendParagraph, insertParagraph, replaceParagraph,
  insertTextAtCursor, replaceSelection,
  setWordCharacterFormat, setWordParagraphFormat,
  applyParagraphStyle,
  findAndReplace,  // Phase 9 WORD-04
} from './write/word';
```

wordWriteTools：
```typescript
const wordWriteTools = [
  appendParagraph, insertParagraph, replaceParagraph,
  insertTextAtCursor, replaceSelection,
  setWordCharacterFormat, setWordParagraphFormat,
  applyParagraphStyle,
  findAndReplace,  // Phase 9 WORD-04
] as ToolDef[];
```

**文件 4：contract.test.ts**

```typescript
{ toolName: 'find_and_replace', ..., integrationTest: true }
```

**文件 5：CONTRACT.md**

```markdown
| find_and_replace | word | ... | 快照式 | restore_range_snapshot | true | 9 | done |
```
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -15</automated>
  </verify>
  <done>
    - npm run test 全绿（word.test.ts + operationLog.integration + contract 全部 passed）
    - grep "find_and_replace" src/agent/tools/index.ts 输出 ≥ 1
    - grep "restore_range_snapshot" src/agent/tools/write/word.ts 输出 ≥ 1（reverse.tool 逐字对齐）
    - CONTRACT.md find_and_replace 行 status: done
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → searchText/replaceText | 不受信任的 LLM 输入，body.search 在 Word.run 闭包内执行（Word JS API 自身校验边界） |
| before-image snapshot → reverse.args | 可能包含大量段落文本，超限 100 段触发 noop+gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-12 | DoS | find_and_replace 大文档快照 | mitigate | FIND_AND_REPLACE_SNAPSHOT_LIMIT = 100（D-10）：超限时不构建 before-image，不执行替换（返回 overLimit + warn），防止 before-image 内存溢出 |
| T-9-13 | Tampering | restoreRangeSnapshot 段落归属近似（A5）| accept | v2.1 已知限制：近似段落归属（文本包含判断）对常见场景足够；精确方案（range.paragraphs.getFirst()）推 v2.2；影响仅限 undo 精确度，不影响数据安全 |
| T-9-14 | Elevation of Privilege | restoreRangeSnapshot 位置签名 | mitigate | D-17 + D-18 三步门满足 |
</threat_model>

<verification>
```bash
# 适配器方法
grep -n "findAndReplace\|restoreRangeSnapshot\|FIND_AND_REPLACE_SNAPSHOT_LIMIT" src/adapters/WordAdapter.ts

# ToolDef 名逐字对齐
grep "restore_range_snapshot" src/agent/tools/write/word.ts

# D-17 硬卡
npm run test -- contract

# 全套
npm run test
```
</verification>

<success_criteria>
- findAndReplace 执行后 data.replaced 包含替换数
- 超 100 段时返回 overLimit warn + noop_inverse（不执行替换）
- reverse.tool === "restore_range_snapshot"（与 CONTRACT.md 逐字一致）
- contract.test.ts WORD-04 integrationTest: true，D-17 硬卡通过
- npm run test 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/09-word-d-b-word/09-06-SUMMARY.md`
</output>

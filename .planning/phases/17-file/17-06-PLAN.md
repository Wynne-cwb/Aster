---
phase: 17-file
plan: 06
type: execute
wave: 4
depends_on: [17-05]
files_modified:
  - .size-limit.json
autonomous: true
requirements: [FILE-07, NFR-10]

must_haves:
  truths:
    - "npm run build && npm run size 报 main-*.js ≤82KB gzip（懒加载 0 增量）"
    - "npm audit --audit-level=high 无 high/critical 漏洞（mammoth CVE-2025-11849 已修）"
    - "npm test -- --run 全套绿（含路径 D + 所有解析器测试）"
    - "npm run typecheck 无 TS 错误"
  artifacts:
    - path: ".size-limit.json"
      provides: "bundle size CI gate（可能更新以反映解析库 chunk）"
      contains: "main-*.js"
  key_links:
    - from: ".size-limit.json"
      to: "dist/assets/main-*.js"
      via: "npm run size"
      pattern: "82 KB"
---

<objective>
Wave 4：Phase 17 最终验证 gate——bundle size / npm audit / 全量测试 / TS 检查四重门。

Purpose: 确认整个 Phase 17 的交付满足 NFR-10（初始 bundle 0 增量 ≤82KB）和安全约束（mammoth CVE 已修）。这是 phase-level 阶段门，通过后 Phase 17 可进行 verify。

Output: 全量绿灯 + .size-limit.json 可能小更新（解析库 chunk 名变化时）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-file/17-CONTEXT.md
@.planning/ROADMAP.md
@.size-limit.json

<interfaces>
<!-- 从现有配置提取 -->

From .size-limit.json:
```json
[
  {
    "name": "initial-js",
    "path": "dist/assets/main-*.js",
    "limit": "82 KB",
    "gzip": true
  }
]
```

预期 bundle 分析（全懒加载，初始 0 增量）：
- main-*.js 应在 78.45KB（Phase 16 基线）附近，无显著增量
- 解析库 chunk：mammoth-*.js（~250KB）/ xlsx-*.js（~180KB）/ pdf-*.js（~150KB）/ worker（~400KB）
  均为懒加载 chunk，不计入 main-*.js size gate
- 若 Vite manualChunks 更新：parsers chunk 会单独出现，size-limit 不需要改（只守 main-*.js）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量验证 gate（build + size + test + typecheck + audit）</name>
  <files>.size-limit.json</files>
  <read_first>
    - .size-limit.json (当前配置)
    - .planning/phases/17-file/17-CONTEXT.md (D-15 NFR-09；D-16 NFR-10；memory project_bundle_size_guard)
    - package.json (scripts: build/size/typecheck/audit)
  </read_first>
  <action>
    按顺序运行四重 gate（memory `project_bundle_size_guard`：动 bundle 前先 build 再 size）：

    **Gate 1: Build + Size（NFR-10）**
    ```bash
    npm run build
    npm run size
    ```
    预期：`main-*.js` 报 ≤82KB gzip（与 Phase 16 基线 78.45KB 相近）。
    若超出 82KB：
    - 检查是否有解析库意外被静态 import（非 await import()）
    - 运行 `npx rollup-plugin-visualizer` 或直接看 build 输出找大包
    - 确认全部 4 个解析器模块均为动态 import

    **Gate 2: npm audit（mammoth CVE-2025-11849 守门）**
    ```bash
    npm audit --audit-level=high
    ```
    预期：0 high/critical。
    若报 mammoth 漏洞：检查安装的版本是否 ≥1.11.0（`node -e "console.log(require('mammoth/package.json').version)"`）。

    **Gate 3: 全量测试**
    ```bash
    npm test -- --run
    ```
    预期：全绿（830+ tests，含路径 D + 四个解析器测试）。
    3 个 retry.test.ts errors 是已知噪音（STATE.md 记录），不算 failure。

    **Gate 4: TypeScript 检查**
    ```bash
    npm run typecheck
    ```
    预期：无 TS 错误。
    若有类型错误，逐一修复（常见：attachments.ts 演进后消费者类型不兼容；store 旧 `s.images` 读取点）。

    若所有 gate 通过，记录 phase 17 最终 bundle 大小 + test count 在 SUMMARY.md。
    若 size gate 失败（超 82KB），不修改 .size-limit.json 的 limit——先查原因修代码。
    仅当 .size-limit.json 需要补充新 entry（如单独守 parsers chunk）时才修改此文件。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep "dist/assets/main" | tail -3 && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run size` 输出 main-*.js ≤82KB gzip；
    `npm audit --audit-level=high` 输出 "found 0 vulnerabilities" 或无 high/critical；
    `npm test -- --run` 全绿（"N passed" 行无真失败，3 个 retry errors 是已知噪音）；
    `npm run typecheck` 无错误
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 17 全部 5 个 plan 已完成：
    - Plan 01：测试脚手架（6 个 stub 文件）
    - Plan 02：4 个解析库安装 + attachments.ts 演进
    - Plan 03：docx/xlsx/pptx/text 解析器（3 个测试绿）
    - Plan 04：pdf 解析器 + worker 构建验证
    - Plan 05：InputBar 演进 + sendMessage 注入（D-03 反转 + D-13 分隔符）
    - Plan 06：bundle/audit/test/typecheck 四重 gate 全绿

    完整功能：上传 docx/xlsx/pdf/pptx → eager 解析 → chip「仅供 AI 阅读」标注 → agent 据附件内容作答。多轮复用（chip 常驻，每轮重注入缓存文本）。
  </what-built>
  <how-to-verify>
    本地 dev 验证（不需要 Office，浏览器直接开）：
    1. `npm start`（或 `npm run dev`）
    2. 在 Task Pane 中点击回形针按钮（现在应标注「参考文件」）
    3. 上传一个 .docx 文件 → 确认 chip 出现并显示「解析中…」→ 很快变为「文件名.docx + 仅供 AI 阅读」
    4. 输入「总结这份文档的主要内容」发送 → AI 回复应基于文档内容（说明注入生效）
    5. 再次发送消息「用一句话概括」→ chip 仍常驻（D-03 多轮复用）→ AI 仍记得文档内容
    6. 点 × 移除 chip → 再发消息 → AI 不再引用文档
    7. 尝试上传不支持的文件（如 .zip）→ 应看到诚实提示（不是「文件解析即将开放」）

    注：pdf.js worker 在 GitHub Pages + Office for Web iframe CSP 真机验证延后 Phase 19。
    本次验证在本地 dev 或 Chrome 普通标签页即可。
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build 产物→GitHub Pages | 确认 main-*.js 符合 size gate，不引入意外静态依赖 |
| npm registry→解析库 | npm audit 确认无已知高危漏洞（mammoth CVE-2025-11849 关键）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | Elevation | mammoth CVE-2025-11849 路径遍历 | mitigate | npm audit --audit-level=high gate：mammoth ≥1.11.0 修复（externalFileAccess 默认 false）；CI 每次构建运行 |
| T-17-06-02 | DoS | main bundle 超 82KB gzip | mitigate | .size-limit.json gate + `npm run build && npm run size` 顺序强制；解析库全部 await import() 懒加载，Vite 自动分 chunk |
| T-17-06-03 | Tampering | derivedText 进持久化历史（路径 D 回归） | mitigate | chat.test.ts 路径 D 断言（Plan 01 新建）在全量测试中运行；结构性守门（serializeForStorage 白名单只取 content≤2000 字符，不含附件字段）|
</threat_model>

<verification>
- `npm run build` 无错误
- `npm run size` 报 main-*.js ≤82KB
- `npm audit --audit-level=high` 无 high/critical
- `npm test -- --run` 全绿（"N passed"，3 个 retry errors 是噪音）
- `npm run typecheck` 无 TS 错误
- human-verify checkpoint 通过（文档上传→解析→多轮复用端到端验证）
</verification>

<success_criteria>
1. npm run build && npm run size — main-*.js ≤82KB gzip（NFR-10 gate）
2. npm audit --audit-level=high — 0 high/critical（FILE-02 mammoth CVE gate）
3. npm test -- --run — 全绿（含路径 D + 四个解析器测试）
4. npm run typecheck — 无错误
5. 本地 dev 手工验证通过（上传 docx → chip 就绪 → 多轮复用 → agent 据内容作答）
</success_criteria>

<output>
After completion, create `.planning/phases/17-file/17-06-SUMMARY.md`
</output>

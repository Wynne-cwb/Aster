---
phase: 17-file
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - package.json
  - src/store/attachments.ts
  - src/store/attachments.test.ts
autonomous: true
requirements: [FILE-01, FILE-07, NFR-10]

must_haves:
  truths:
    - "4 个解析库（mammoth/xlsx/pdfjs-dist/jszip）已安装并在 package.json 中正确记录"
    - "attachments.ts 导出 Attachment 判别联合（AttachedImage | AttachedDocument）及 getImages/getDocuments 辅助方法"
    - "旧 addImages/clearImages/removeImage API 可继续使用（向后兼容）"
    - "新增 addAttachment/updateAttachment/removeAttachment/clearAttachments API"
    - "npm test -- --run src/store/attachments.test.ts 全绿"
  artifacts:
    - path: "package.json"
      provides: "mammoth/xlsx(cdn tgz)/pdfjs-dist/jszip 依赖记录"
      contains: "mammoth"
    - path: "src/store/attachments.ts"
      provides: "Attachment 判别联合 + 统一 store"
      exports: ["AttachedImage", "AttachedDocument", "Attachment", "useAttachmentStore"]
    - path: "src/store/attachments.test.ts"
      provides: "store 演进守门测试"
  key_links:
    - from: "src/store/attachments.ts"
      to: "src/components/InputBar.tsx"
      via: "useAttachmentStore import"
      pattern: "useAttachmentStore|AttachedImage|Attachment"
    - from: "src/store/attachments.ts"
      to: "src/store/chat.ts"
      via: "sendMessage 读 getImages/getDocuments"
      pattern: "getImages\\(\\)|getDocuments\\(\\)"
---

<objective>
Wave 1：安装 4 个解析库 + 演进 attachments.ts 为 Attachment 判别联合 store（向后兼容）。

Purpose: 这是整个 Phase 17 的基座——所有 Wave 2/3 的解析器和 InputBar 代码都依赖这里定义的 Attachment 类型。store 演进必须向后兼容：Phase 15 已有的 addImages/clearImages/removeImage 调用点在 Wave 3 统一迁移之前继续工作。

Output: 更新的 package.json（4 个新依赖）+ 演进的 attachments.ts（判别联合 + 新 API）+ 扩展的测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-file/17-CONTEXT.md
@.planning/phases/17-file/17-RESEARCH.md
@src/store/attachments.ts
@src/store/attachments.test.ts
@src/store/chat.ts

<interfaces>
<!-- 从现有 attachments.ts 提取的当前接口 -->

From src/store/attachments.ts（当前，L20-47）:
```typescript
export interface AttachedImage {
  id: string;
  base64: string;
  mimeType: 'image/png' | 'image/jpeg' | 'image/webp';
  fileName: string;
  sizeBytes: number;
}

interface AttachmentState {
  images: AttachedImage[];
  addImages: (imgs: AttachedImage[]) => void;
  clearImages: () => void;
  removeImage: (id: string) => void;
}
```

目标演进后 attachments.ts 接口（D-05，Pattern 1 from RESEARCH.md L230-278）:
```typescript
export interface AttachedImage {
  kind: 'image';  // 新增 kind 字段
  id: string;
  base64: string;
  mimeType: 'image/png' | 'image/jpeg' | 'image/webp';
  fileName: string;
  sizeBytes: number;
  visionEvidence?: string;  // D-03 多轮复用缓存
}

export type FileKind = 'docx' | 'xlsx' | 'pdf' | 'pptx' | 'text';
export type ParseStatus = 'parsing' | 'ready' | 'error';

export interface AttachedDocument {
  kind: 'document';
  id: string;
  fileName: string;
  sizeBytes: number;
  fileKind: FileKind;
  status: ParseStatus;
  derivedText?: string;   // status=ready 时有值
  truncated?: boolean;    // D-04 软截断标记
  errorMessage?: string;
}

export type Attachment = AttachedImage | AttachedDocument;

// 向后兼容 API（Wave 3 之前保留）:
addImages(imgs: AttachedImage[]): void   // 仍可用
clearImages(): void                       // 仍可用（Wave 3 后移除）
removeImage(id: string): void             // 迁移到 removeAttachment(id)

// 新 API:
addAttachment(a: Attachment): void
updateAttachment(id: string, patch: Partial<AttachedDocument> | Partial<AttachedImage>): void
removeAttachment(id: string): void
clearAttachments(): void
getImages(): AttachedImage[]
getDocuments(): AttachedDocument[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 4 个解析库</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/17-file/17-RESEARCH.md (Standard Stack 安装命令 L134-140，SheetJS CDN URL)
    - package.json (当前依赖，确认无重复)
  </read_first>
  <action>
    按顺序运行（SheetJS 必须从 CDN tgz 安装，不能 npm i xlsx）：

    ```bash
    npm install mammoth
    npm install https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz
    npm install pdfjs-dist@5.7.284
    npm install jszip
    ```

    安装完成后验证：
    1. `package.json` dependencies 包含 `mammoth`（版本 ≥1.11.0，预期 1.12.x）
    2. `package.json` dependencies 包含 xlsx（URL 形式，非版本号）
    3. `package.json` dependencies 包含 `pdfjs-dist`（5.7.284）
    4. `package.json` dependencies 包含 `jszip`（3.x）

    安装后运行 npm audit 检查：
    ```bash
    npm audit --audit-level=high
    ```
    high/critical 级别漏洞必须为 0（mammoth ≥1.11.0 已修 CVE-2025-11849）。
    若 audit 报 mammoth high/critical，需检查是否安装了正确版本。
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(p.dependencies.mammoth, JSON.stringify(p.dependencies.xlsx).includes('cdn.sheetjs.com'), p.dependencies['pdfjs-dist'], p.dependencies.jszip)"</automated>
  </verify>
  <done>
    `package.json` dependencies 包含 mammoth（版本 ≥1.11.0）、xlsx（cdn.sheetjs.com URL）、pdfjs-dist（5.7.284）、jszip（3.x）；
    `npm audit --audit-level=high` 无 high/critical（NFR-10 + FILE-02 CVE gate）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 演进 src/store/attachments.ts 为 Attachment 判别联合 store</name>
  <files>src/store/attachments.ts, src/store/attachments.test.ts</files>
  <read_first>
    - src/store/attachments.ts (全文，当前实现，需全面重写)
    - src/store/attachments.test.ts (现有测试，不得破坏)
    - .planning/phases/17-file/17-RESEARCH.md (Pattern 1 L224-278，完整 TypeScript 接口)
    - src/store/chat.ts (L176-212，消费 useAttachmentStore 的 sendMessage，用 images/clearImages)
    - src/components/InputBar.tsx (L61-63 attachedImages 读取；L153 addImages；L209 removeImage)
  </read_first>
  <behavior>
    attachments.ts 演进断言：
    - Test 1: addAttachment({kind:'image',...}) + getImages() 返回该 image
    - Test 2: addAttachment({kind:'document',...}) + getDocuments() 返回该 doc
    - Test 3: updateAttachment(id, {status:'ready', derivedText:'解析文本'}) 更新正确字段
    - Test 4: removeAttachment(id) 删除附件
    - Test 5: clearAttachments() 清空所有
    - Test 6: 向后兼容 addImages([]) 仍可调用（内部追加 kind:'image' 附件）
    - Test 7: 向后兼容 clearImages() 仍可调用（清空全部或仅图片）
    - Test 8: NFR-09：store 为纯内存态，localStorage.setItem 不被调用
  </behavior>
  <action>
    重写 `src/store/attachments.ts`，完整代码：

    ```typescript
    /**
     * src/store/attachments.ts — 统一附件内存态 store（Phase 17 FILE 演进）
     *
     * Phase 17 演进：AttachedImage[] → Attachment 判别联合（image | document）
     * 向后兼容：addImages/clearImages/removeImage 保留（Wave 3 迁移后废弃）
     * 纯内存 Zustand slice，无 persist middleware（NFR-09 硬约束）
     *
     * D-03：多轮复用——sendMessage 不再 clearImages()；visionEvidence 缓存首次 vision 结果
     * D-05：统一 store — image/document 混合；document 字节解析后即丢（只存 derivedText）
     */

    import { create } from 'zustand';

    export interface AttachedImage {
      kind: 'image';
      id: string;
      /** 裸 base64，不含 data:...;base64, 前缀 */
      base64: string;
      mimeType: 'image/png' | 'image/jpeg' | 'image/webp';
      fileName: string;
      sizeBytes: number;
      /** D-03：首次 vision 调用后缓存结果，多轮不重复调 vision */
      visionEvidence?: string;
    }

    export type FileKind = 'docx' | 'xlsx' | 'pdf' | 'pptx' | 'text';
    export type ParseStatus = 'parsing' | 'ready' | 'error';

    export interface AttachedDocument {
      kind: 'document';
      id: string;
      fileName: string;
      sizeBytes: number;
      fileKind: FileKind;
      /** parsing → ready | error */
      status: ParseStatus;
      /** 解析后的纯文本（status=ready 时有值）*/
      derivedText?: string;
      /** D-04 软截断：解析文本超 ~30 万字符时 true */
      truncated?: boolean;
      /** 用户可读错误消息（status=error 时有值）*/
      errorMessage?: string;
    }

    export type Attachment = AttachedImage | AttachedDocument;

    interface AttachmentState {
      attachments: Attachment[];

      // 新 API（Phase 17）
      addAttachment: (a: Attachment) => void;
      updateAttachment: (id: string, patch: Partial<AttachedDocument> | Partial<AttachedImage>) => void;
      removeAttachment: (id: string) => void;
      clearAttachments: () => void;
      getImages: () => AttachedImage[];
      getDocuments: () => AttachedDocument[];

      // 向后兼容 API（Phase 15 调用点，Wave 3 迁移后废弃）
      /** @deprecated 用 addAttachment({kind:'image',...}) 替代 */
      addImages: (imgs: Omit<AttachedImage, 'kind'>[]) => void;
      /** @deprecated 用 clearAttachments() 替代 */
      clearImages: () => void;
      /** @deprecated 用 removeAttachment(id) 替代 */
      removeImage: (id: string) => void;
    }

    export const useAttachmentStore = create<AttachmentState>((set, get) => ({
      attachments: [],

      addAttachment: (a) =>
        set((s) => ({ attachments: [...s.attachments, a] })),

      updateAttachment: (id, patch) =>
        set((s) => ({
          attachments: s.attachments.map((a) =>
            a.id === id ? { ...a, ...patch } as Attachment : a
          ),
        })),

      removeAttachment: (id) =>
        set((s) => ({ attachments: s.attachments.filter((a) => a.id !== id) })),

      clearAttachments: () => set({ attachments: [] }),

      getImages: () =>
        get().attachments.filter((a): a is AttachedImage => a.kind === 'image'),

      getDocuments: () =>
        get().attachments.filter((a): a is AttachedDocument => a.kind === 'document'),

      // 向后兼容
      addImages: (imgs) =>
        set((s) => ({
          attachments: [
            ...s.attachments,
            ...imgs.map((img) => ({ ...img, kind: 'image' as const })),
          ],
        })),

      clearImages: () =>
        set((s) => ({ attachments: s.attachments.filter((a) => a.kind !== 'image') })),

      removeImage: (id) =>
        set((s) => ({ attachments: s.attachments.filter((a) => a.id !== id) })),
    }));
    ```

    然后更新 `src/store/attachments.test.ts`：在现有 4 个测试之后新增 Task 2 behavior 中的 8 个测试（addAttachment/getImages/getDocuments/updateAttachment/removeAttachment/clearAttachments/向后兼容 addImages/向后兼容 clearImages/NFR-09）。

    注意：现有测试引用 `images` 字段（直接读 store.images）需更新为 `getImages()`，或保持兼容（store.attachments.filter(...)）。检查并修复现有测试中的 `attachedImages = useAttachmentStore((s) => s.images)` 读取——Wave 3 的 InputBar 迁移会统一处理，此处只需确认测试不报类型错误即可（如果测试读 s.images 直接失败，在测试里改为 `s.attachments.filter(a => a.kind==='image')`）。
  </action>
  <verify>
    <automated>npm test -- --run src/store/attachments.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>
    `npm test -- --run src/store/attachments.test.ts` 全绿；
    `grep -c "kind: 'image'\|kind: 'document'\|AttachedDocument\|ParseStatus" src/store/attachments.ts` 输出 ≥ 4；
    `grep "addAttachment\|getImages\|getDocuments" src/store/attachments.ts` 有输出
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install→package.json | 确保 SheetJS 从 CDN tgz 安装而非废弃 npm 包 |
| attachments.ts→localStorage | store 演进不引入 persist middleware（NFR-09 硬约束）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Elevation | mammoth CVE-2025-11849 | mitigate | 安装 mammoth ≥1.11.0（1.12.0 已修复）+ npm audit --audit-level=high gate（D-06）|
| T-17-02-02 | Spoofing | SheetJS 废弃 npm 版本 | mitigate | 强制从 cdn.sheetjs.com tgz 安装，安装后验证 package.json URL 格式（D-07）|
| T-17-02-03 | Information Disclosure | attachments.ts persist | mitigate | store 不引入 persist() middleware——Zustand 纯内存；NFR-09 测试守门 |
| T-17-02-04 | Tampering | derivedText 进 localStorage | mitigate | attachments.ts 无 persist；派生文本只活在内存；路径 D 守门断言（17-01）|
</threat_model>

<verification>
- `node -e "const p=require('./package.json'); console.log('mammoth' in p.dependencies, 'xlsx' in p.dependencies, 'pdfjs-dist' in p.dependencies, 'jszip' in p.dependencies)"` — 全 true
- `grep 'cdn.sheetjs.com' package.json` — 有输出（SheetJS CDN tgz URL）
- `npm audit --audit-level=high` — 无 high/critical
- `npm test -- --run src/store/attachments.test.ts` — 全绿
- `grep -c "AttachedDocument\|ParseStatus\|kind: 'image'" src/store/attachments.ts` — ≥ 3
</verification>

<success_criteria>
1. 4 个解析库安装完成，package.json 正确记录（SheetJS 为 CDN URL 格式）
2. npm audit --audit-level=high 无高危漏洞
3. attachments.ts 导出 Attachment 判别联合 + getImages/getDocuments + 向后兼容 API
4. src/store/attachments.test.ts 全绿（含向后兼容测试）
5. npm test -- --run 全套通过（不破坏现有 830 tests）
</success_criteria>

<output>
After completion, create `.planning/phases/17-file/17-02-SUMMARY.md`
</output>

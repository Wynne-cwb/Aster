---
phase: 17-file
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/lib/parsers/docx.ts
  - src/lib/parsers/xlsx.ts
  - src/lib/parsers/pptx.ts
  - src/lib/parsers/text.ts
autonomous: true
requirements: [FILE-02, FILE-03, FILE-05]

must_haves:
  truths:
    - "parseDocx/parseXlsx/parsePptx/parseText 四个函数存在并通过测试"
    - "各解析器用 await import() 懒加载自己的库（Vite 自动分 chunk）"
    - "xlsx 输出每 sheet 前加 '=== Sheet: X ===' 表头 + CSV 内容"
    - "pptx 按数字序排列 slide（非字典序），含演讲者备注"
    - "超长文本（>300000 字符）被软截断并标注 truncated:true（D-04）"
  artifacts:
    - path: "src/lib/parsers/docx.ts"
      provides: "parseDocx(file): Promise<string>"
      exports: ["parseDocx"]
    - path: "src/lib/parsers/xlsx.ts"
      provides: "parseXlsx(file): Promise<string>"
      exports: ["parseXlsx"]
    - path: "src/lib/parsers/pptx.ts"
      provides: "parsePptx(file): Promise<string>"
      exports: ["parsePptx"]
    - path: "src/lib/parsers/text.ts"
      provides: "parseText(file): Promise<string>"
      exports: ["parseText"]
  key_links:
    - from: "src/lib/parsers/docx.ts"
      to: "mammoth"
      via: "await import('mammoth')"
      pattern: "await import\\('mammoth'\\)"
    - from: "src/lib/parsers/xlsx.ts"
      to: "xlsx"
      via: "await import('xlsx')"
      pattern: "await import\\('xlsx'\\)"
    - from: "src/lib/parsers/pptx.ts"
      to: "jszip"
      via: "await import('jszip')"
      pattern: "await import\\('jszip'\\)"
---

<objective>
Wave 2（并行 A）：实现 docx/xlsx/pptx/text 四个解析器，让 Plan 01 创建的测试从红转绿。

Purpose: 三个解析器（docx/xlsx/pptx）+ 零库纯文本解析器。各自 `await import()` 懒加载，Vite 自动分 chunk，初始 bundle 0 增量。这是 FILE-02/03/05 的核心实现。

Output: src/lib/parsers/ 下 4 个 .ts 文件，Plan 01 的对应 test 转绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-file/17-CONTEXT.md
@.planning/phases/17-file/17-RESEARCH.md
@src/store/attachments.ts
@src/lib/parsers/docx.test.ts
@src/lib/parsers/xlsx.test.ts
@src/lib/parsers/pptx.test.ts

<interfaces>
<!-- 从 RESEARCH.md 提取的 concrete code，executor 直接照此实现 -->

docx.ts（D-06，RESEARCH L286-297）:
```typescript
export async function parseDocx(file: File): Promise<string> {
  const arrayBuffer = await file.arrayBuffer();
  const { default: mammoth } = await import('mammoth');
  const result = await mammoth.extractRawText({ arrayBuffer });
  return result.value;
}
```

xlsx.ts（D-07，RESEARCH L299-315）:
```typescript
export async function parseXlsx(file: File): Promise<string> {
  const arrayBuffer = await file.arrayBuffer();
  const XLSX = await import('xlsx');
  const wb = XLSX.read(new Uint8Array(arrayBuffer), { type: 'array' });
  const parts: string[] = [];
  for (const sheetName of wb.SheetNames) {
    const ws = wb.Sheets[sheetName];
    const csv = XLSX.utils.sheet_to_csv(ws);
    parts.push(`=== Sheet: ${sheetName} ===\n${csv}`);
  }
  return parts.join('\n\n');
}
```

pptx.ts（D-09，RESEARCH L358-399 完整版，含数字序排序 + 备注）:
见 RESEARCH L358-399 完整代码。

text.ts（D-10，零库）:
```typescript
export async function parseText(file: File): Promise<string> {
  return file.text();
}
```

超长截断（D-04，Claude's Discretion）：
在各解析器中，解析完成后检查文本长度：
```typescript
const MAX_CHARS = 300_000;
if (text.length > MAX_CHARS) {
  const preview = text.slice(0, MAX_CHARS);
  return preview + '\n\n[注：文件内容过长，已读取前约 30 万字符]';
  // 调用方可通过检查返回值末尾判断是否被截断
}
```
或通过返回 { text, truncated } 对象——此处选择在文本末尾拼接截断提示（简单方案，无需修改接口）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 parseDocx（mammoth） + parseXlsx（SheetJS）</name>
  <files>src/lib/parsers/docx.ts, src/lib/parsers/xlsx.ts</files>
  <read_first>
    - src/lib/parsers/docx.test.ts (Wave 0 stubs，理解测试期望)
    - src/lib/parsers/xlsx.test.ts (Wave 0 stubs，理解测试期望)
    - .planning/phases/17-file/17-RESEARCH.md (Code Examples L506-530，docx/xlsx 完整代码)
    - .planning/phases/17-file/17-CONTEXT.md (D-06 + D-07 locked decisions)
  </read_first>
  <behavior>
    docx.ts：
    - Test 1 pass: parseDocx(mockFile) 调用 mammoth.extractRawText + 返回 result.value
    - Test 2 pass: 超长文本（>300000 字符）在返回文本末尾含截断提示
    - Test 3 pass: mammoth 通过 await import() 懒加载（测试 mock 中 import 工厂被调用）

    xlsx.ts：
    - Test 1 pass: parseXlsx(mockFile) 返回 "=== Sheet: Sheet1 ===" 格式的多 sheet 内容
    - Test 2 pass: 单 sheet 返回正确 CSV
    - Test 3 pass: 超长 CSV（>300000 字符）有截断提示
  </behavior>
  <action>
    1. 新建 `src/lib/parsers/docx.ts`（完整内容）：
    ```typescript
    /**
     * src/lib/parsers/docx.ts — docx → 纯文本（FILE-02）
     * D-06：mammoth ≥1.11.0 extractRawText，await import() 懒加载，
     * 超长文本软截断（D-04 ~30 万字符）
     */

    const MAX_CHARS = 300_000;

    export async function parseDocx(file: File): Promise<string> {
      const arrayBuffer = await file.arrayBuffer();
      const { default: mammoth } = await import('mammoth');
      const result = await mammoth.extractRawText({ arrayBuffer });
      const text = result.value;
      if (text.length > MAX_CHARS) {
        return text.slice(0, MAX_CHARS) + '\n\n[注：文件内容过长，已读取前约 30 万字符]';
      }
      return text;
    }
    ```

    2. 新建 `src/lib/parsers/xlsx.ts`（完整内容）：
    ```typescript
    /**
     * src/lib/parsers/xlsx.ts — xlsx → CSV（FILE-03）
     * D-07：SheetJS 0.20.3（cdn.sheetjs.com tgz），await import() 懒加载，
     * 多 sheet 全转 CSV，行数超大时软截断（D-04）
     */

    const MAX_CHARS = 300_000;
    const MAX_ROWS = 1000;  // 单 sheet 最大行数（Claude's Discretion）

    export async function parseXlsx(file: File): Promise<string> {
      const arrayBuffer = await file.arrayBuffer();
      const XLSX = await import('xlsx');
      const wb = XLSX.read(new Uint8Array(arrayBuffer), { type: 'array' });
      const parts: string[] = [];
      for (const sheetName of wb.SheetNames) {
        const ws = wb.Sheets[sheetName];
        let csv = XLSX.utils.sheet_to_csv(ws);
        // 行数截断（D-04）
        const rows = csv.split('\n');
        if (rows.length > MAX_ROWS) {
          csv = rows.slice(0, MAX_ROWS).join('\n') + `\n[注：表格行数过多，已读取前 ${MAX_ROWS} 行]`;
        }
        parts.push(`=== Sheet: ${sheetName} ===\n${csv}`);
      }
      const fullText = parts.join('\n\n');
      if (fullText.length > MAX_CHARS) {
        return fullText.slice(0, MAX_CHARS) + '\n\n[注：文件内容过长，已读取前约 30 万字符]';
      }
      return fullText;
    }
    ```

    3. 运行两个测试确认绿：
       `npm test -- --run src/lib/parsers/docx.test.ts`
       `npm test -- --run src/lib/parsers/xlsx.test.ts`
  </action>
  <verify>
    <automated>npm test -- --run src/lib/parsers/docx.test.ts src/lib/parsers/xlsx.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>
    两个 test 文件全绿；
    `grep "await import('mammoth')" src/lib/parsers/docx.ts` 有输出；
    `grep "await import('xlsx')" src/lib/parsers/xlsx.ts` 有输出；
    `grep "=== Sheet:" src/lib/parsers/xlsx.ts` 有输出
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 parsePptx（jszip+DOMParser）+ parseText（零库）</name>
  <files>src/lib/parsers/pptx.ts, src/lib/parsers/text.ts</files>
  <read_first>
    - src/lib/parsers/pptx.test.ts (Wave 0 stubs，理解测试期望：数字序排序/备注)
    - .planning/phases/17-file/17-RESEARCH.md (Code Examples L566-604 pptx 完整代码；Pitfall 3 L482-485 命名空间)
    - .planning/phases/17-file/17-CONTEXT.md (D-09 + D-10 decisions)
  </read_first>
  <behavior>
    pptx.ts：
    - Test 1 pass: 3 个 slide 文件（slide1/slide2/slide10）按数字序排序 → [Slide 1]/[Slide 2]/[Slide 10]
    - Test 2 pass: querySelectorAll('t') 提取 <a:t> 文本节点
    - Test 3 pass: notesSlide1.xml 存在 → "[Slide 1 备注]" 前缀文本出现在结果

    text.ts：
    - parseText(file) 直接返回 file.text()（0 KB 依赖）
  </behavior>
  <action>
    1. 新建 `src/lib/parsers/pptx.ts`（完整内容，参考 RESEARCH L358-399）：
    ```typescript
    /**
     * src/lib/parsers/pptx.ts — pptx → 文本（FILE-05）
     * D-09：jszip 3.10.1 + 原生 DOMParser，await import() 懒加载，
     * slide 数字序排序 + 演讲者备注（notesSlides）
     * text-only 不保真（不还原版式/图）
     */

    const MAX_CHARS = 300_000;

    export async function parsePptx(file: File): Promise<string> {
      const arrayBuffer = await file.arrayBuffer();
      const { default: JSZip } = await import('jszip');
      const zip = await JSZip.loadAsync(arrayBuffer);
      const parts: string[] = [];

      // 数字序排 slide（非字典序，防 slide10 排在 slide2 前）
      const slideFiles = Object.keys(zip.files)
        .filter((name) => /^ppt\/slides\/slide\d+\.xml$/.test(name))
        .sort((a, b) => {
          const toNum = (s: string) => parseInt(s.match(/\d+/)![0], 10);
          return toNum(a) - toNum(b);
        });

      for (const [idx, fname] of slideFiles.entries()) {
        const xml = await zip.files[fname].async('string');
        const doc = new DOMParser().parseFromString(xml, 'application/xml');
        // querySelectorAll('t')：实测对中文 pptx 正常（spike #8 验证）
        // 若遇命名空间误匹配，改用 getElementsByTagNameNS(DRAWINGML_NS, 't')
        const slideText = Array.from(doc.querySelectorAll('t'))
          .map((n) => n.textContent ?? '')
          .filter(Boolean)
          .join(' ');
        if (slideText) parts.push(`[Slide ${idx + 1}] ${slideText}`);

        // 演讲者备注（D-09）
        const notesFname = fname.replace('slides/slide', 'notesSlides/notesSlide');
        if (zip.files[notesFname]) {
          const notesXml = await zip.files[notesFname].async('string');
          const notesDoc = new DOMParser().parseFromString(notesXml, 'application/xml');
          const notesText = Array.from(notesDoc.querySelectorAll('t'))
            .map((n) => n.textContent ?? '')
            .filter(Boolean)
            .join(' ');
          if (notesText) parts.push(`[Slide ${idx + 1} 备注] ${notesText}`);
        }
      }

      const fullText = parts.join('\n');
      if (fullText.length > MAX_CHARS) {
        return fullText.slice(0, MAX_CHARS) + '\n\n[注：文件内容过长，已读取前约 30 万字符]';
      }
      return fullText;
    }
    ```

    2. 新建 `src/lib/parsers/text.ts`（极简）：
    ```typescript
    /**
     * src/lib/parsers/text.ts — txt/md/csv/json → 文本（FILE-05 extra）
     * D-10：File.text() 零库，0 KB 懒加载开销
     */

    const MAX_CHARS = 300_000;

    export async function parseText(file: File): Promise<string> {
      const text = await file.text();
      if (text.length > MAX_CHARS) {
        return text.slice(0, MAX_CHARS) + '\n\n[注：文件内容过长，已读取前约 30 万字符]';
      }
      return text;
    }
    ```

    3. 运行测试确认绿：
       `npm test -- --run src/lib/parsers/pptx.test.ts`
       （text.ts 无独立测试文件，类型检查确认即可）
  </action>
  <verify>
    <automated>npm test -- --run src/lib/parsers/pptx.test.ts 2>&1 | tail -6</automated>
  </verify>
  <done>
    `npm test -- --run src/lib/parsers/pptx.test.ts` 全绿；
    `grep "数字序\|slide.*sort\|\\.sort(" src/lib/parsers/pptx.ts` 有输出；
    `grep "notesSlide" src/lib/parsers/pptx.ts` 有输出（演讲者备注）；
    `grep "await import('jszip')" src/lib/parsers/pptx.ts` 有输出；
    `npm test -- --run src/lib/parsers/` 全部 4 个测试文件通过（若 text.ts 无测试则 3 个 pass 亦可）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 上传文件内容→解析器 | 解析器输入来自用户文件，内容不可信 |
| 解析输出→finalPrompt | 解析文本将注入 LLM prompt，是 OWASP LLM01 的注入面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | OWASP LLM01 文件内容注入 | mitigate | 解析输出为纯文本数据（非 HTML/JS）；注入时由 Plan 05 D-13 分隔符 + 「非指令」前置提示隔离；本层不需额外 sanitize（输入只进 LLM，不渲染）|
| T-17-03-02 | DoS | 超大文件解析卡死 | mitigate | 三个解析器均有 MAX_CHARS=300_000 软截断（D-04）；InputBar 上传阶段有 ~20MB 文件大小前置拒绝（Plan 05）；解析失败通过 throw 早暴露 |
| T-17-03-03 | Elevation | pptx 命名空间误匹配 | accept | querySelectorAll('t') 对简单/中文 pptx spike #8 验证 PASS；复杂 pptx（SmartArt）可能误匹配，但仅影响解析质量而非安全（text-only，不执行）；Phase 19 真机可补 fallback |
</threat_model>

<verification>
- `npm test -- --run src/lib/parsers/docx.test.ts src/lib/parsers/xlsx.test.ts src/lib/parsers/pptx.test.ts` — 全绿
- `grep "await import('mammoth')" src/lib/parsers/docx.ts` — 有输出
- `grep "await import('xlsx')" src/lib/parsers/xlsx.ts` — 有输出
- `grep "await import('jszip')" src/lib/parsers/pptx.ts` — 有输出
- `grep "=== Sheet:" src/lib/parsers/xlsx.ts` — 有输出（多 sheet 表头）
- `grep "notesSlides" src/lib/parsers/pptx.ts` — 有输出（演讲者备注）
- `npm test -- --run` — 全量通过（不破坏现有测试）
</verification>

<success_criteria>
1. 4 个解析器文件（docx.ts/xlsx.ts/pptx.ts/text.ts）存在于 src/lib/parsers/
2. Plan 01 创建的 3 个解析器测试（docx/xlsx/pptx）从红转绿
3. 各解析器用 await import() 懒加载（grep 可验证）
4. xlsx 多 sheet 输出含 "=== Sheet: X ===" 表头格式
5. pptx slide 按数字序排序（spike #8 验证的方案）+ 演讲者备注支持
6. 超长文本 >300000 字符时末尾有截断提示（D-04）
</success_criteria>

<output>
After completion, create `.planning/phases/17-file/17-03-SUMMARY.md`
</output>

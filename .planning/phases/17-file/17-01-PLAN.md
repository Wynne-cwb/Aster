---
phase: 17-file
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/parsers/docx.test.ts
  - src/lib/parsers/xlsx.test.ts
  - src/lib/parsers/pdf.test.ts
  - src/lib/parsers/pptx.test.ts
  - src/store/chat.test.ts
autonomous: true
requirements: [FILE-02, FILE-03, FILE-04, FILE-05, NFR-09]

must_haves:
  truths:
    - "6 个测试文件存在且全部以红灯（failing）启动——无实现时 npm test 显示明确 fail，而非 pass"
    - "pdf.test.ts 用 vi.mock('pdfjs-dist') 桩住 worker 与 getDocument，jsdom 不崩"
    - "chat.test.ts 路径 D describe 块存在，断言文档 derivedText 不出现在 serializeForStorage 结果"
  artifacts:
    - path: "src/lib/parsers/docx.test.ts"
      provides: "FILE-02 red stubs"
    - path: "src/lib/parsers/xlsx.test.ts"
      provides: "FILE-03 red stubs"
    - path: "src/lib/parsers/pdf.test.ts"
      provides: "FILE-04 red stubs + pdfjs mock"
    - path: "src/lib/parsers/pptx.test.ts"
      provides: "FILE-05 red stubs"
    - path: "src/store/chat.test.ts"
      provides: "NFR-09 路径 D describe 块（扩展）"
  key_links:
    - from: "src/lib/parsers/pdf.test.ts"
      to: "vi.mock('pdfjs-dist')"
      via: "vi.hoisted + vi.mock factory"
      pattern: "vi\\.mock\\('pdfjs-dist'"
    - from: "src/store/chat.test.ts"
      to: "路径 D describe block"
      via: "新增 describe 块"
      pattern: "路径 D|kind.*document|derivedText"
---

<objective>
Wave 0：建立测试脚手架（红灯）。新建 4 个解析器测试文件 + 扩展 chat.test.ts 路径 D。

Purpose: TDD 规范——实现代码在 Wave 2/3 写入之前，测试用例必须先存在且以红灯运行。这样可以确认测试真的在守门，而非因为没有 import 就被 skip 掉。

Output: 5 个文件（4 个新建 + 1 个扩展），全部 failing（import 路径不存在或接口未实现）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-file/17-CONTEXT.md
@.planning/phases/17-file/17-RESEARCH.md
@.planning/phases/17-file/17-VALIDATION.md
@src/store/chat.test.ts
@src/store/attachments.ts

<interfaces>
<!-- 从现有代码提取，executor 直接使用 -->

From src/store/chat.test.ts（现有三路 NFR-09 守门，L297-398）:
```typescript
// 路径 D 要模仿的模式（参考路径 C）:
it('NFR-09 路径 C: ...', () => {
  useChatStore.setState({ messages: [...] } as never);
  useChatStore.getState().saveHistory('aster:chat:img-test');
  const call = mockedStorage.set.mock.calls.at(-1)!;
  const payload = call[1] as { messages: Array<{ role: string; content: string }> };
  expect(payload.messages.every((m) => m.role !== 'tool')).toBe(true);
  const allContent = payload.messages.map((m) => m.content).join('');
  expect(allContent).not.toContain('thumbnail');
});
```

From 17-RESEARCH.md pdfjs mock（L730-745）:
```typescript
vi.mock('pdfjs-dist', () => ({
  GlobalWorkerOptions: { workerSrc: '' },
  getDocument: vi.fn().mockReturnValue({
    promise: Promise.resolve({
      numPages: 2,
      getPage: vi.fn().mockResolvedValue({
        getTextContent: vi.fn().mockResolvedValue({
          items: [{ str: '测试文本' }],
        }),
      }),
    }),
  }),
}));
```

解析器统一接口（Claude's Discretion，executor 用此签名：）:
```typescript
// src/lib/parsers/docx.ts（Wave 2 将实现）
export async function parseDocx(file: File): Promise<string>

// src/lib/parsers/xlsx.ts
export async function parseXlsx(file: File): Promise<string>

// src/lib/parsers/pdf.ts
export async function parsePdf(file: File): Promise<string>

// src/lib/parsers/pptx.ts
export async function parsePptx(file: File): Promise<string>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 src/lib/parsers/ 目录 + 4 个解析器测试 stub 文件（红灯）</name>
  <files>src/lib/parsers/docx.test.ts, src/lib/parsers/xlsx.test.ts, src/lib/parsers/pdf.test.ts, src/lib/parsers/pptx.test.ts</files>
  <read_first>
    - src/store/chat.test.ts (import 结构 + vi.mock 范式，L1-40)
    - .planning/phases/17-file/17-RESEARCH.md (pdf mock L730-745；解析器 code examples L506-604)
    - .planning/phases/17-file/17-VALIDATION.md (Wave 0 stubs 清单)
  </read_first>
  <behavior>
    docx.test.ts:
    - 用 vi.mock('mammoth') 桩住库（`extractRawText` 返回 { value: '测试内容' }）
    - Test 1: parseDocx(file) 返回解析出的文本字符串
    - Test 2: 超长文本（>300000 字符）被软截断，truncated 提示出现在返回值中
    - Test 3: mammoth import 只在首次调用时触发（lazy）

    xlsx.test.ts:
    - 用 vi.mock('xlsx') 桩住库（`read`/`utils.sheet_to_csv`/`SheetNames`）
    - Test 1: parseXlsx(file) 多 sheet 各返回 "=== Sheet: X ===" 表头 + CSV 内容
    - Test 2: 单 sheet 返回正确 CSV
    - Test 3: 超出行数上限时有截断提示

    pdf.test.ts:
    - 用 vi.mock('pdfjs-dist', ...) 桩住（GlobalWorkerOptions + getDocument，见 RESEARCH L730-745）
    - Test 1: parsePdf(file) 正常 PDF → 拼接 numPages 页文本
    - Test 2: 扫描件（所有页 items 为空）→ throw Error with code 'PDF_NO_TEXT_LAYER'
    - Test 3: GlobalWorkerOptions.workerSrc 被设置（验证 new URL 调用点存在）

    pptx.test.ts:
    - 用 vi.mock('jszip') 桩住（loadAsync 返回含 slide1.xml/slide2.xml/slide10.xml 的 files map）
    - Test 1: parsePptx(file) slide 按数字序排序（slide1 < slide2 < slide10）
    - Test 2: `<a:t>` 文本被正确提取
    - Test 3: 演讲者备注（notesSlide1.xml）也被提取，前缀 "[Slide N 备注]"

    全部 4 个文件：Wave 0 stub 仅 import 不存在的模块（`from '../parsers/docx'`，路径尚不存在），
    所有 test 以 import error 报红（failing），确认 Wave 0 红灯。
    注意：pdf.test.ts 用 vi.mock + vi.hoisted 确保 mock 在 import 前提升（参考 attachments.test.ts L8-18 import 顺序；pdfjs mock factory 需在 import pdfjs 之前生效）。
  </behavior>
  <action>
    1. 创建目录 `src/lib/parsers/`（如不存在）。
    2. 新建 `src/lib/parsers/docx.test.ts`：
       - 顶部 `vi.mock('mammoth', () => ({ default: { extractRawText: vi.fn().mockResolvedValue({ value: '测试内容', messages: [] }) } }))`
       - import `{ parseDocx } from './docx'`（此路径 Wave 2 之前不存在 → 报红）
       - 3 个 it 用例（见 behavior）
    3. 新建 `src/lib/parsers/xlsx.test.ts`：
       - `vi.mock('xlsx', () => ({ read: vi.fn(), utils: { sheet_to_csv: vi.fn().mockReturnValue('col1,col2\n1,2') }, SheetNames: ['Sheet1'] }))`
       - import `{ parseXlsx } from './xlsx'`（不存在 → 报红）
       - 3 个 it 用例
    4. 新建 `src/lib/parsers/pdf.test.ts`：
       - 顶部用 `vi.mock('pdfjs-dist', ...)` factory（完整 mock，含 GlobalWorkerOptions + getDocument；见 RESEARCH L730-745）
       - import `{ parsePdf } from './pdf'`（不存在 → 报红）
       - 3 个 it 用例（含扫描件错误路径：items 全空 → throw code=PDF_NO_TEXT_LAYER）
    5. 新建 `src/lib/parsers/pptx.test.ts`：
       - `vi.mock('jszip', ...)` 返回含 3 个 slide + notesSlide1 的 files map；每个 file 有 `.async('string')` mock
       - import `{ parsePptx } from './pptx'`（不存在 → 报红）
       - 3 个 it 用例（slide 数字排序断言是核心）
    6. 验证：运行 `npm test -- --run src/lib/parsers/` — 全部失败（import error），不得有任何 pass。
  </action>
  <verify>
    <automated>npm test -- --run src/lib/parsers/ 2>&1 | grep -E "FAIL|Cannot find module|failed" | head -20</automated>
  </verify>
  <done>4 个 .test.ts 文件存在；`npm test -- --run src/lib/parsers/` 输出包含 "Cannot find module" 或 "FAIL"（不是 "passed"）；pdfjs mock 使用 vi.mock('pdfjs-dist') 工厂形式</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 src/store/chat.test.ts 新增 NFR-09 路径 D（文档附件）</name>
  <files>src/store/chat.test.ts</files>
  <read_first>
    - src/store/chat.test.ts (全文，L275-399 三路 A/B/C 守门范式)
    - .planning/phases/17-file/17-CONTEXT.md (D-15 NFR-09 路径 D 描述)
    - src/store/attachments.ts (当前 AttachedImage 接口，用于理解路径 D 的附件结构)
  </read_first>
  <behavior>
    路径 D 断言（参考路径 A/B/C 模式）:
    - Test 1: 文档附件 derivedText 不出现在 serializeForStorage 结果
      - setState messages 含 user message（原始 prompt）+ assistant message
      - 模拟「文档注入路径」：user message.content 只是原始 prompt，不含 derivedText
      - saveHistory 后检查序列化 payload.messages.every(content 不含 derivedText 字符串)
    - Test 2: 文档附件 kind:'document' 标记不出现在序列化结果
      - 确认序列化后的消息不含 'derivedText' / 'kind:document' / fileKind 字符串
    注意：路径 D 是「结构性保证」——user message.content 只存原始 prompt，
    derivedText 只进 finalPrompt（内存运行时路径），绝不进 chatStore messages。
    这是白名单天然过滤：serializeForStorage 只取 content 字段（≤2000 字符）。
  </behavior>
  <action>
    在 src/store/chat.test.ts 文件末尾（或现有 NFR-09 describe 块内）新增一个 describe 块：
    ```typescript
    // NFR-09 路径 D：文档附件 derivedText 不进持久化历史（D-15 + FILE-07 守门）
    describe('NFR-09 路径 D：文档附件 derivedText 不进序列化', () => {
      it('文档附件 derivedText 不出现在 serializeForStorage 结果', () => {
        const derivedText = '这是从 docx 解析出来的参考内容，仅作背景信息';
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: '基于这份文档写一份报告', ts: 1 },
            { id: 'a1', role: 'assistant', content: '好的，以下是报告…', ts: 2 },
          ],
        } as never);
        useChatStore.getState().saveHistory('aster:chat:doc-test');
        const call = mockedStorage.set.mock.calls.at(-1)!;
        const payload = call[1] as { messages: Array<{ role: string; content: string }> };
        const allContent = payload.messages.map((m) => m.content).join('');
        // derivedText 绝不出现在序列化消息中
        expect(allContent).not.toContain(derivedText);
        expect(allContent).not.toContain('derivedText');
      });

      it('kind:document 附件标记不出现在序列化消息内容', () => {
        useChatStore.setState({
          messages: [
            { id: 'u1', role: 'user', content: '分析这份报表', ts: 1 },
            { id: 'a1', role: 'assistant', content: '报表显示…', ts: 2 },
          ],
        } as never);
        useChatStore.getState().saveHistory('aster:chat:kind-test');
        const call = mockedStorage.set.mock.calls.at(-1)!;
        const payload = call[1] as { messages: Array<{ role: string; content: string }> };
        const allContent = payload.messages.map((m) => m.content).join('');
        expect(allContent).not.toContain("kind:'document'");
        expect(allContent).not.toContain('fileKind');
        expect(allContent).not.toContain('sizeBytes');
      });
    });
    ```
    路径 D 的两个 it 本身通过（因为 messages 本来就不含那些字段），但这是「守门断言」——
    一旦未来有人误把 derivedText 写进 message.content，测试就会红。
    运行 `npm test -- --run src/store/chat.test.ts` 确认全部通过（路径 D 两个新 it pass）。
  </action>
  <verify>
    <automated>npm test -- --run src/store/chat.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>chat.test.ts 含 "路径 D" describe 块（`grep -c "路径 D\|kind.*document\|derivedText" src/store/chat.test.ts` 输出 ≥1）；`npm test -- --run src/store/chat.test.ts` 通过（所有测试绿）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码→解析器接口 | 测试 stub 验证接口契约，不实际读取文件或调用外部库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-W0-01 | Tampering | chat.test.ts 路径 D stub | mitigate | 路径 D 断言验证 derivedText 不进序列化——防止未来实现误把文档内容写进持久化历史；测试本身为守门 |
| T-17-W0-02 | Spoofing | pdfjs-dist vi.mock | accept | mock 只在测试环境生效，不影响生产代码；jsdom 无原生 Worker API 必须 mock |
</threat_model>

<verification>
- `ls src/lib/parsers/*.test.ts` — 4 个文件存在
- `npm test -- --run src/lib/parsers/` — 输出含 "Cannot find module" 或 "FAIL"（红灯确认）
- `npm test -- --run src/store/chat.test.ts` — 全部通过（路径 D 两个新 it 绿）
- `grep -c "路径 D\|derivedText" src/store/chat.test.ts` — 输出 ≥ 2
</verification>

<success_criteria>
1. `src/lib/parsers/` 目录存在，含 4 个 .test.ts stub 文件
2. 运行 `npm test -- --run src/lib/parsers/` 报 "Cannot find module './docx'" 等 import error（确认红灯）
3. 运行 `npm test -- --run src/store/chat.test.ts` 全绿（路径 D 两个新 it pass）
4. pdf.test.ts 使用 `vi.mock('pdfjs-dist', ...)` 工厂形式（grep 可验证）
</success_criteria>

<output>
After completion, create `.planning/phases/17-file/17-01-SUMMARY.md`
</output>

---
phase: 17-file
plan: 04
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/lib/parsers/pdf.ts
  - vite.config.ts
autonomous: true
requirements: [FILE-04]

must_haves:
  truths:
    - "parsePdf(file) 调用 pdfjs-dist.getDocument + 逐页 getTextContent 提取文本"
    - "GlobalWorkerOptions.workerSrc 使用 new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url).href（禁止 ?url）"
    - "扫描件（所有页文本为空）抛出含 code='PDF_NO_TEXT_LAYER' 的错误"
    - "npm run build && ls dist/assets/ | grep -i worker 确认 worker 文件 emit（若不在，执行 public/ fallback）"
  artifacts:
    - path: "src/lib/parsers/pdf.ts"
      provides: "parsePdf(file): Promise<string>"
      contains: "GlobalWorkerOptions.workerSrc"
      exports: ["parsePdf"]
    - path: "vite.config.ts"
      provides: "worker chunk（可选 manualChunks）"
  key_links:
    - from: "src/lib/parsers/pdf.ts"
      to: "pdfjs-dist"
      via: "await import('pdfjs-dist')"
      pattern: "await import\\('pdfjs-dist'\\)"
    - from: "src/lib/parsers/pdf.ts"
      to: "worker"
      via: "new URL('...', import.meta.url).href"
      pattern: "new URL.*pdfjs-dist.*import\\.meta\\.url"
---

<objective>
Wave 2（并行 B）：实现 pdf 解析器 + 验证 Vite 生产构建 worker emit（本阶段最高技术不确定项）。

Purpose: pdf.js worker 在 Vite 生产构建中的行为是 Phase 17 最高风险点。本 plan 在 Wave 2 独立实现 parsePdf，并强制运行 `npm run build` 验证 worker 文件是否被正确 emit 到 dist/assets/。若 new URL 方案失败，在 plan 内执行 public/ fallback。

Output: src/lib/parsers/pdf.ts + worker 构建验证结果 + vite.config.ts 可能更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-file/17-CONTEXT.md
@.planning/phases/17-file/17-RESEARCH.md
@src/lib/parsers/pdf.test.ts
@vite.config.ts

<interfaces>
<!-- 从 RESEARCH.md 提取的 pdf 解析 concrete code -->

From RESEARCH.md Code Examples L533-563（pdf 解析完整代码）:
```typescript
const pdfjsLib = await import('pdfjs-dist');
// WORKER RULE（vite.config.ts L1-6）：禁 ?url，用 new URL 静态字符串字面量
pdfjsLib.GlobalWorkerOptions.workerSrc = new URL(
  'pdfjs-dist/build/pdf.worker.min.mjs',
  import.meta.url
).href;

const data = new Uint8Array(await file.arrayBuffer());
const loadingTask = pdfjsLib.getDocument({ data });
const pdfDoc = await loadingTask.promise;

const pageTexts: string[] = [];
for (let i = 1; i <= pdfDoc.numPages; i++) {
  const page = await pdfDoc.getPage(i);
  const content = await page.getTextContent();
  const text = content.items
    .map((item) => ('str' in item ? item.str : ''))
    .join(' ');
  pageTexts.push(text);
}
const fullText = pageTexts.join('\n');
if (!fullText.trim()) {
  throw Object.assign(new Error('PDF_NO_TEXT_LAYER'), { code: 'PDF_NO_TEXT_LAYER' });
}
```

Worker fallback（RESEARCH Pitfall 1 L472-475）:
若 `npm run build && ls dist/assets/ | grep worker` 无输出，执行 fallback：
1. `cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs public/`
2. 在 pdf.ts 中改为：`pdfjsLib.GlobalWorkerOptions.workerSrc = '/Aster/pdf.worker.min.mjs';`
注意：GitHub Pages base 路径 `/Aster/`（vite.config.ts L31）。

From vite.config.ts WORKER RULE（L1-6）:
禁止 `?url` 导入 worker 文件。必须用 `new URL('path', import.meta.url).href`。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 parsePdf（pdfjs-dist + worker）</name>
  <files>src/lib/parsers/pdf.ts</files>
  <read_first>
    - src/lib/parsers/pdf.test.ts (Wave 0 stubs，理解测试期望：worker mock + 扫描件报错)
    - .planning/phases/17-file/17-RESEARCH.md (Code Examples L533-563；Pitfall 1 L471-475；worker fallback)
    - vite.config.ts (WORKER RULE L1-6，禁 ?url)
    - .planning/phases/17-file/17-CONTEXT.md (D-08 locked decision)
  </read_first>
  <behavior>
    pdf.ts：
    - Test 1 pass: parsePdf(mockFile) 调用 getDocument + 逐页 getTextContent，拼接多页文本
    - Test 2 pass: 扫描件（所有页 items 全空 → fullText 全空）→ throw Error with .code='PDF_NO_TEXT_LAYER'
    - Test 3 pass: GlobalWorkerOptions.workerSrc 被设置（含 'pdf.worker.min.mjs' 字符串）
  </behavior>
  <action>
    新建 `src/lib/parsers/pdf.ts`（完整内容）：
    ```typescript
    /**
     * src/lib/parsers/pdf.ts — pdf → 文本（FILE-04）
     * D-08：pdfjs-dist 5.7.284，await import() 懒加载
     *
     * WORKER RULE（vite.config.ts L1-6）：
     * GlobalWorkerOptions.workerSrc 必须用 new URL(..., import.meta.url).href
     * 禁止 ?url import（dev 能跑但 build 后 worker 404）
     *
     * 扫描件检测：fullText 全空 → throw Error({ code: 'PDF_NO_TEXT_LAYER' })
     * 本地 dev/build 验证；PDF_NO_TEXT_LAYER → D-14 诚实错误（InputBar Plan 05 处理）
     *
     * Phase 19 延后：pdf.js worker 在 GitHub Pages + Office for Web iframe CSP 真机验证
     */

    const MAX_CHARS = 300_000;

    export async function parsePdf(file: File): Promise<string> {
      const pdfjsLib = await import('pdfjs-dist');

      // WORKER RULE：new URL 静态字符串字面量，Vite 构建时 emit worker 文件
      // 禁止 ?url import（vite.config.ts L1-6）
      pdfjsLib.GlobalWorkerOptions.workerSrc = new URL(
        'pdfjs-dist/build/pdf.worker.min.mjs',
        import.meta.url,
      ).href;

      const data = new Uint8Array(await file.arrayBuffer());
      const loadingTask = pdfjsLib.getDocument({ data });
      const pdfDoc = await loadingTask.promise;

      const pageTexts: string[] = [];
      for (let i = 1; i <= pdfDoc.numPages; i++) {
        const page = await pdfDoc.getPage(i);
        const content = await page.getTextContent();
        const pageText = content.items
          .map((item) => ('str' in item ? item.str : ''))
          .join(' ');
        pageTexts.push(pageText);
      }

      const fullText = pageTexts.join('\n');

      // 扫描件检测（D-08）：全空 → 诚实结构化报错
      if (!fullText.trim()) {
        const err = new Error(
          '这个 PDF 没有可提取的文字（可能是扫描件），暂不支持 OCR',
        ) as Error & { code: string };
        err.code = 'PDF_NO_TEXT_LAYER';
        throw err;
      }

      if (fullText.length > MAX_CHARS) {
        return fullText.slice(0, MAX_CHARS) + '\n\n[注：文件内容过长，已读取前约 30 万字符]';
      }
      return fullText;
    }
    ```

    运行测试：`npm test -- --run src/lib/parsers/pdf.test.ts`
  </action>
  <verify>
    <automated>npm test -- --run src/lib/parsers/pdf.test.ts 2>&1 | tail -6</automated>
  </verify>
  <done>
    `npm test -- --run src/lib/parsers/pdf.test.ts` 全绿；
    `grep "GlobalWorkerOptions.workerSrc" src/lib/parsers/pdf.ts` 有输出；
    `grep -L "?url" src/lib/parsers/pdf.ts` 输出该文件路径（即文件内无 ?url）；
    `grep "PDF_NO_TEXT_LAYER" src/lib/parsers/pdf.ts` 有输出（扫描件错误码）
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证 worker 在 Vite 生产构建中正确 emit（若失败执行 public/ fallback）</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts (全文，了解现有 build 配置 + manualChunks)
    - .planning/phases/17-file/17-RESEARCH.md (Pitfall 1 L471-475 + Open Questions L655-659 fallback 步骤)
    - src/lib/parsers/pdf.ts (刚创建，new URL worker 配置)
  </read_first>
  <action>
    1. 先运行 build 验证（memory `project_bundle_size_guard`：动 bundle 前先 build 再 size）：
       ```bash
       npm run build
       ls dist/assets/ | grep -i worker
       ```

    2a. 若 worker 文件在 dist/assets/（如 `pdf.worker.min-XXXXXXXX.mjs`）：
        - new URL 方案有效，无需修改
        - 在 vite.config.ts 的 manualChunks 中可选添加解析库 chunk（提升 size-limit 可见性）：
          在现有 `manualChunks: { markdown: [...], react: [...] }` 对象中追加：
          ```
          parsers: ['mammoth', 'xlsx', 'jszip'],
          // pdfjs-dist worker 由 Vite 自动分 chunk（new URL 触发）
          ```
        - 记录验证结果在 SUMMARY.md

    2b. 若 worker 文件不在 dist/assets/（new URL 方案 Vite 未 emit）：
        执行 fallback（RESEARCH Pitfall 1 fallback 方案）：
        ```bash
        # 检查 pdfjs-dist 中 worker 文件名
        ls node_modules/pdfjs-dist/build/ | grep worker
        # 复制到 public/（GitHub Pages 子路径下会被服务为 /Aster/pdf.worker.min.mjs）
        cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs public/
        ```
        然后修改 src/lib/parsers/pdf.ts，把 new URL 替换为静态路径：
        ```typescript
        // fallback：worker 文件复制到 public/，用静态路径（/Aster/ 前缀 = GitHub Pages base）
        pdfjsLib.GlobalWorkerOptions.workerSrc = '/Aster/pdf.worker.min.mjs';
        ```
        再次 `npm run build && ls dist/assets/ | grep worker`（此时 worker 在 public/，不在 assets/ 是正常的）
        记录 fallback 被触发 + 原因在 SUMMARY.md

    3. 无论哪个分支，最终运行 `npm run build && npm run size` 确认 main-*.js ≤82KB（NFR-10）。
       解析库全部懒加载，初始 chunk 应 0 增量。
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npm run size 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` 无错误；
    `npm run size` 报 main-*.js ≤82KB gzip（NFR-10 gate）；
    worker 已 emit（dist/assets/ 中有 worker 文件）或 public/ fallback 已执行（public/pdf.worker.min.mjs 存在）；
    SUMMARY.md 记录了 worker 验证路径选择（new URL 成功 or fallback 触发）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF 内容→pdfjs 解析 | pdfjs-dist 解析不可信 PDF 内容（防 DoS + 解析器漏洞）|
| worker→CSP | 生产环境 Office for Web iframe CSP 可能拦截 worker 加载（Phase 19 真机验证）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | DoS | pdfjs-dist 解析超大 PDF | mitigate | MAX_CHARS=300_000 软截断（D-04）；InputBar 上传阶段 ~20MB 文件大小前置拒绝（Plan 05）；解析耗时过长由浏览器 tab 本身内存限制兜底 |
| T-17-04-02 | Tampering | PDF 扫描件注入（无文本层） | accept | 扫描件检测到 PDF_NO_TEXT_LAYER 时诚实报错，不进入 LLM 注入路径（D-08 + D-14）；无 OCR 功能 |
| T-17-04-03 | Spoofing | worker CSP 被 iframe 拦截 | accept | Phase 19 UAT 真机验证；本阶段按 WORKER RULE 正确配置；fallback=public/ 方案已准备；若失败通过 Phase 19 评估 |
| T-17-04-04 | Elevation | pdfjs-dist 解析器漏洞 | mitigate | 使用锁版本 5.7.284 + npm audit gate；pdf 内容不执行（纯文本提取）|
</threat_model>

<verification>
- `npm test -- --run src/lib/parsers/pdf.test.ts` — 全绿
- `grep "new URL.*pdfjs-dist.*import.meta.url" src/lib/parsers/pdf.ts` — 有输出（WORKER RULE）
- `grep "PDF_NO_TEXT_LAYER" src/lib/parsers/pdf.ts` — 有输出
- `npm run build && npm run size` — build 成功，size ≤82KB
- worker emit 验证：`ls dist/assets/ | grep -i worker`（或 `ls public/ | grep worker` 若触发 fallback）
</verification>

<success_criteria>
1. parsePdf 实现完整：await import('pdfjs-dist') + new URL worker + 逐页 getTextContent + 扫描件检测
2. Plan 01 创建的 pdf.test.ts 从红转绿（3 个 it 全 pass）
3. npm run build 成功，worker 文件 emit（dist/assets/ 或 public/ fallback）
4. npm run size 报 main-*.js ≤82KB（懒加载 0 增量）
5. SUMMARY.md 记录 worker 验证结果（new URL 成功 or fallback 触发原因）
</success_criteria>

<output>
After completion, create `.planning/phases/17-file/17-04-SUMMARY.md`
</output>

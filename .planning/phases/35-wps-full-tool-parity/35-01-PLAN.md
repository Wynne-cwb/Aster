# Phase 35 — WPS 三宿主全工具拉齐（WPS-D1 代码侧完成）

> **⚠️ 投机性预写第三批（STATE.md 2026-06-29）**：Phase 30 真机门仍未过；本 phase 把 Phase 34
> 的「核心最小集」三宿主扩到**对齐 Office.js 全工具集**，等于把原 WPS-D1（三宿主完整移植）
> 的剩余工具一次性盲写完。用户 2026-06-29 第三次授权（「大胆点，放心干……直接把功能从代码维度
> 拉齐先。到时候我会找到 windows 电脑一起测试」）。产物 = 代码草稿，真机 pending，**不可标 Complete**。

**Requirements:** WPS-D1（主体；正式归属后续独立 milestone）
**Depends on:** Phase 34（三宿主核心集 code-drafted）

## 目标

把 Phase 34 每宿主的核心最小集，补齐到对位 Office.js WordAdapter/PptAdapter/ExcelAdapter
被工具复用的**全部**同名 forward + inverse 方法。接缝上方（loop/operationLog/UI/dispatch/工具定义）
零改动——WPS adapter 复用全套 Office.js 工具，只补 adapter 方法 + 类型 + 诚实裁剪白名单 + 集成测试。

## 范围（全工具拉齐，非核心最小集）

- **Word +9 write + 9 inverse**：set_word_character_format / set_word_paragraph_format /
  apply_paragraph_style / find_and_replace / insert_table / set_word_list_format(inverse=noop) /
  insert_word_comment / set_word_header_footer / edit_table_cell。
- **PPT +9 write（+渐变降级）**：set_shape_property / set_shape_text_font / set_shape_text_alignment /
  rotate_shape / set_slide_background / copy_slide / manage_slides(inverse=noop) / apply_slide_layout /
  insert_ppt_table / add_line / set_shape_gradient（降级走 setShapeProperty）。
- **Excel +13 write + 6 inverse**：format_excel_range / set_column_row_size / set_auto_filter /
  add_conditional_format / create_table / sort_range / excel_find_and_replace / merge_cells /
  remove_duplicates / manage_worksheet / set_chart_title / insert_chart / create_pivot_table。
- **仍排除（未实现，诚实裁剪不暴露）**：生图/图库插入工具（insert image 未实现）、get_shape_image（视觉，
  WPS webview 无可靠 base64 形状/图表导出）、visual_check_slide（PPT 视觉自查）、batch_write（executeBatch
  未实现）、freeze_panes（Excel，未实现）。

## Tasks

- **35-T1** WpsWordAdapter 补 9 write + 9 inverse（同步 VBA：Font/ParagraphFormat/Style/Find.Execute/
  Tables/Comments/Headers-Footers）；inverse 收 Record；buildTableFingerprint 与 Office.js 逐字一致。
- **35-T2** WpsPptAdapter 补 9 write + inverse（Fill/Line/Rotation/Background/Duplicate/AddTable/AddLine/
  ParagraphFormat）；颜色 BGR↔#RRGGBB 双向转换 helper；保留「刻意不实现 readPptSlideTitle」。
- **35-T3** WpsExcelAdapter 补 13 write + 6 inverse（NumberFormat/Interior/Font/HAlign/ColumnWidth-RowHeight/
  AutoFilter/FormatConditions/ListObjects/Sort/Replace/Merge/RemoveDuplicates/ChartObjects/Worksheets.Add/
  PivotCaches）；snapshot/tooLarge→noop_inverse 路径；HR-01 去重列展开门；颜色 BGR 转换。
- **35-T4** 类型扩展：各 adapter 文件顶部 `declare global` 块增补 VBA 对象模型成员（声明合并，不碰共享
  `wps-jsapi.d.ts` 以避免三 agent 并发写冲突），全程 [真机待验]。
- **35-T5** 诚实裁剪：`tools/index.ts` 把三宿主核心集扩到含全部新实现工具，仍排除未实现项；裁剪守门测试
  加「构建工具数 === 核心集大小」不变式（抓核心集拼错的工具名）。
- **35-T6** operationLog 集成测试：三 adapter 各补新方法 round-trip undo 守门（真 adapter × mock 同步 VBA
  全局；验 reverse 派发 + Record 签名 + undo-all 逆序；tooLarge→noop_inverse→skipped_error）。

## 验证（代码侧 / 真机 pending）

代码侧（全过）：tsc 0 / build（WpsExcelAdapter gz 5.08KB + WpsWordAdapter 5.28KB + WpsPptAdapter 5.99KB
懒 chunk；主 Office 入口 main-*.js 仍 1.96KB 零膨胀）/ size gate（1KB ≪ 100KB）/ eslint 0 error /
全量 1194 tests 全过（+39：Word 10 / PPT 13 / Excel 16）。

真机侧（纯真机，不可勾）：三宿主各高级工具多步 agent loop + undo all UAT PASS；VBA gotcha 真机坐实——
尤以颜色 BGR 字节序、Shape.Id 稳定性、AddTable/AddLine/AddConnector 签名、FormatConditions/ListObjects/
PivotCaches/ChartObjects 对象模型、Find/Replace 计数、Style 区域设置（英文 builtin 名在中文 WPS 上）等。

## 为何不可标 Complete

Phase 30 两条 make-or-break（CEF/React19、SSE 直连不被拦）仍未真机验；no-go 则含本 phase 在内全部 WPS
代码作废。本 phase 全部新方法基于 WPS 桌面 VBA JSAPI **盲写推断**，未在 Windows WPS 真机跑过一次，
高级 OM（透视表/图表/条件格式/连接线/原生表）大概率要修。

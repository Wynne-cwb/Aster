---
phase: 15-vis
plan: 04
type: execute
wave: 3
depends_on:
  - "15-02"
  - "15-03"
files_modified:
  - src/store/chat.test.ts
autonomous: true
requirements:
  - NFR-09

must_haves:
  truths:
    - "chat.test.ts 包含 NFR-09 serialize-test：tool role 消息含 base64 → 序列化后不出现 base64/data:image"
    - "chat.test.ts 包含 NFR-09 serialize-test：user message.content 不含 base64（上传图路径）"
    - "chat.test.ts 包含 tool role 完全不在序列化结果中的断言"
    - "现有 serializeForStorage 白名单测试未被修改/删除"
    - "npm test -- --run src/store/chat.test.ts 全 PASS"
    - "npm run build && npm run size bundle ≤82KB gzip"
  artifacts:
    - path: "src/store/chat.test.ts"
      provides: "NFR-09 serialize-test 守门（2 个新 it() 断言）"
      contains: "base64"
      min_lines: 5
  key_links:
    - from: "src/store/chat.test.ts"
      to: "src/store/chat.ts serializeForStorage"
      via: "saveHistory + localStorage mock 验证"
      pattern: "serializeForStorage"
---

<objective>
补全 NFR-09 结构性守门：在 chat.test.ts 追加两个 it() 断言，验证 base64 图片字节（无论来自 vision tool result 还是上传图路径）永远不写入 persisted 聊天历史。并验证 bundle size 仍符合 ≤82KB gzip CI gate。

Purpose: 这是 memory「feedback_recurring_failure_add_gate」的落地——同类问题（base64 进历史）不靠纪律防，靠测试守门防。
Output: chat.test.ts 扩展 2 个 NFR-09 守门断言 + bundle size 验证
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-vis/15-CONTEXT.md
@.planning/phases/15-vis/15-RESEARCH.md
@.planning/phases/15-vis/15-PATTERNS.md
@.planning/phases/15-vis/15-03-SUMMARY.md

<interfaces>
<!-- 现有 serializeForStorage 白名单测试范式（chat.test.ts L204-224）-->

```typescript
// 现有白名单测试（已存在，照此范式追加）：
it('serializeForStorage 白名单：只存 user|assistant 文字，每条 ≤2000 字符', () => {
  useChatStore.setState({
    messages: [
      { id: 'u1', role: 'user', content: 'a'.repeat(3000), ts: 1 },
      { id: 'a1', role: 'assistant', content: 'ok', ts: 2 },
      { id: 't1', role: 'tool', content: 'tool_result', ts: 3 },   // 期望被过滤
      { id: 'e1', role: 'error', content: 'err', ts: 4 },          // 期望被过滤
    ],
  } as never);
  useChatStore.getState().saveHistory('aster:chat:testDoc');
  const call = mockedStorage.set.mock.calls[0];
  const payload = call[1] as { messages: Array<{ role: string; content: string }> };
  const roles = payload.messages.map((m) => m.role);
  expect(roles).not.toContain('tool');
  expect(roles).not.toContain('error');
  const userMsg = payload.messages.find((m) => m.role === 'user');
  expect(userMsg?.content.length).toBeLessThanOrEqual(2000);
});
```

<!-- serializeForStorage 实现（chat.ts L119-131）-->
```typescript
function serializeForStorage(messages: Message[]): StorableMessage[] {
  return messages
    .filter(
      (m): m is Message & { role: 'user' | 'assistant' } =>
        (m.role === 'user' || m.role === 'assistant') && !m.isStreaming,
    )
    .map((m) => ({
      id: m.id,
      role: m.role,
      content: m.content.slice(0, 2000),
      ts: m.ts,
    }));
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 追加 NFR-09 serialize-test 守门 + bundle size 验证</name>
  <files>src/store/chat.test.ts</files>

  <read_first>
  - src/store/chat.test.ts（完整内容，重点 L204-224 现有白名单测试区域；确认 mockedStorage 定义方式 + beforeEach reset）
  - src/store/chat.ts（serializeForStorage L119-131 + saveHistory 调用方式）
  - .planning/phases/15-vis/15-PATTERNS.md §chat.test.ts（两个 NFR-09 断言的完整代码模板）
  - .planning/phases/15-vis/15-RESEARCH.md §问题 4（NFR-09 守门的两条路径分析）
  </read_first>

  <behavior>
  - Test 1（NFR-09 路径 A）：tool role 消息 toolResult 含 base64 字段 → saveHistory 后序列化结果不含 'base64' 也不含 'data:image'；tool role 完全不在 payload.messages 中
  - Test 2（NFR-09 路径 B）：user message.content 不含 base64（上传图路径：finalPrompt 含 evidence 文本但不含 base64，且用户气泡用 original prompt）
  </behavior>

  <action>
  在 src/store/chat.test.ts 的现有白名单测试（L204-224）之后，追加两个新 it() 断言：

  **断言 1 — vision tool result 路径（文档选中图）：**
  ```typescript
  it('NFR-09 serialize-test：tool role 含 vision base64 → 序列化后 base64 不出现', () => {
    // 模拟文档选中图路径：tool role 含 vision_result + base64_raw 字段
    const fakeBase64 = 'data:image/png;base64,' + 'A'.repeat(500);
    useChatStore.setState({
      messages: [
        { id: 'u1', role: 'user', content: '分析这张图', ts: 1 },
        {
          id: 'tool1',
          role: 'tool',
          content: '正在看这张图…',
          toolResult: {
            ok: true,
            data: { vision_result: '图中是一张饼图，显示 Q1 销售占 35%', base64_raw: fakeBase64 },
          },
          ts: 2,
        },
        { id: 'a1', role: 'assistant', content: '根据图片，Q1 销售占 35%', ts: 3 },
      ],
    } as never);

    useChatStore.getState().saveHistory('aster:chat:vis-test');
    const call = mockedStorage.set.mock.calls[0];
    const payload = call[1] as { messages: Array<{ role: string; content: string }> };

    // tool role 必须完全不在序列化结果中
    expect(payload.messages.every((m) => m.role !== 'tool')).toBe(true);

    // 所有 content 不含 base64 相关字符串
    const allContent = payload.messages.map((m) => m.content).join('');
    expect(allContent).not.toContain('base64');
    expect(allContent).not.toContain('data:image');
    expect(allContent).not.toContain('A'.repeat(100)); // 模拟 base64 payload 不出现
  });
  ```

  **断言 2 — 上传图路径（FILE-06）：**
  ```typescript
  it('NFR-09 serialize-test：上传图路径 user message.content 不含 base64', () => {
    // 上传图路径：sendMessage 中 finalPrompt 含 vision evidence 文本（不含 base64），
    // user message.content = 原始 prompt（不含 evidence / base64）
    // vision evidence 只在 runAgent 内部使用，不写进 chatStore 消息
    useChatStore.setState({
      messages: [
        {
          id: 'u1',
          role: 'user',
          // 原始 prompt，不含 base64（sendMessage 中 pushMessage 传 content: prompt）
          content: '基于这张图写一份销售报告',
          ts: 1,
        },
        {
          id: 'a1',
          role: 'assistant',
          content: '好的，根据图片中的饼图数据，Q1 销售报告如下…',
          ts: 2,
        },
      ],
    } as never);

    useChatStore.getState().saveHistory('aster:chat:upload-test');
    const call = mockedStorage.set.mock.calls[0];
    const payload = call[1] as { messages: Array<{ role: string; content: string }> };

    const allContent = payload.messages.map((m) => m.content).join('');
    expect(allContent).not.toContain('base64');
    expect(allContent).not.toContain('data:image');

    // 用户消息内容为原始 prompt（不含 evidence 注入文本）
    const userMsg = payload.messages.find((m) => m.role === 'user');
    expect(userMsg?.content).toBe('基于这张图写一份销售报告');
  });
  ```

  注意：
  - 确认 mockedStorage.set.mock.calls 在 beforeEach 中被 reset（如 `mockedStorage.set.mockClear()`），若无则需在每个 it 前手动 `mockedStorage.set.mockClear()`
  - 两个测试独立，调用 saveHistory 时用不同 docKey（'aster:chat:vis-test' / 'aster:chat:upload-test'）
  - 不修改现有任何测试（只追加）
  </action>

  <verify>
    <automated>npm test -- --run src/store/chat.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
  - npm test -- --run src/store/chat.test.ts 全 PASS（含新增 2 个 NFR-09 用例）
  - grep -c "NFR-09" src/store/chat.test.ts 结果 >= 2（两个守门用例都有注释标识）
  - grep "base64" src/store/chat.test.ts | grep "not.toContain" 结果 >= 2 行（守门断言）
  - grep "not.toContain('data:image')" src/store/chat.test.ts 结果 >= 2 行
  - 现有白名单测试仍存在（不被删除）：grep "serializeForStorage 白名单" src/store/chat.test.ts 存在
  - npm test -- --run 全绿（全量测试不退步）
  </acceptance_criteria>

  <done>NFR-09 serialize-test 守门 2 个用例全 PASS；全量测试绿</done>
</task>

<task type="auto">
  <name>Task 2: bundle size 验证 + 全量测试 gate</name>
  <files></files>

  <read_first>
  - package.json（size-limit 配置，确认 npm run size 命令）
  - .planning/phases/15-vis/15-RESEARCH.md §Standard Stack（零新增 npm 依赖确认）
  </read_first>

  <action>
  验证 Phase 15 改动不突破 82KB gzip CI gate（不修改任何文件，仅运行命令）：

  1. 先 build：`npm run build`
  2. 再检查 size：`npm run size` 确认 main-*.js gzip ≤82KB
  3. 若超限，找出 Phase 15 中可懒加载的模块并移出初始 chunk（但 Phase 15 zero new deps，超限概率极低）

  同时确认：
  - `npm test -- --run` 全绿（所有 Phase 15 新增测试 PASS）
  - `npm run extract` 不报 Lingui catalog 错误（Phase 03 新增 t`...` 宏须已提取）
  - `npx tsc --noEmit` 不报类型错误

  注意 memory `project_bundle_size_guard`：「动 bundle 前先 build 再 size；npm run size 测陈旧 dist 给假绿」——必须先 build 才 size。
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
  - npm run build 成功（无 build 错误）
  - npm run size 输出中 main-*.js gzip 数字 ≤82KB（保持 CI gate）
  - npm test -- --run 全绿（最终全量测试 gate）
  - npx tsc --noEmit 无报错
  - grep "import.*attachments" src/store/chat.ts 存在（确认动态依赖链正确）
  </acceptance_criteria>

  <done>bundle ≤82KB，全量测试绿，TypeScript 编译干净，Phase 15 自动化实现部分交付完毕</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serializeForStorage → localStorage | 白名单过滤保证 base64 不进持久化存储 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Information Disclosure | serializeForStorage 未来改动破坏 NFR-09 | mitigate | 结构性守门（chat.test.ts 两个 it 断言）——未来任何改动破坏 base64 过滤逻辑时测试红，CI 阻断（memory feedback_recurring_failure_add_gate）|
| T-15-15 | Information Disclosure | bundle 内嵌 base64 资产泄漏 | accept | Phase 15 不内嵌任何 base64 资产（零新增静态资源）；bundle size 验证检查 ≤82KB |
</threat_model>

<verification>
1. `npm test -- --run src/store/chat.test.ts` — NFR-09 两个新用例全 PASS
2. `npm run build && npm run size` — bundle ≤82KB（CI gate 不突破）
3. `npm test -- --run` — 全量测试全绿
4. `npx tsc --noEmit` — TypeScript 严格编译干净
5. `npm run extract` — Lingui catalog 无 coverage 报错
</verification>

<success_criteria>
- chat.test.ts 含 2 个 NFR-09 serialize-test 守门用例，全 PASS
- serializeForStorage 白名单测试未被修改，仍 PASS
- bundle gzip ≤82KB（CI gate 不突破）
- 全量 npm test -- --run 绿
- Phase 15 自动化部分全部交付（Plan 01–04 完成）
</success_criteria>

<output>
完成后创建 `.planning/phases/15-vis/15-04-SUMMARY.md`，记录：
- NFR-09 守门测试用例数量（含原有白名单 + 2 个新守门）
- bundle size 数字（实测 gzip）
- npm test -- --run 通过用例总数
- Phase 15 自动化部分完成状态
</output>

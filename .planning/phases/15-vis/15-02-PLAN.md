---
phase: 15-vis
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - src/agent/tools/read/vision.ts
  - src/agent/tools/read/vision.test.ts
  - src/adapters/PptAdapter.ts
  - src/adapters/ExcelAdapter.ts
  - src/adapters/WordAdapter.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "get_shape_image read tool 存在，kind='read'，可选 focus 参数"
    - "三宿主 adapter.read() 均处理 get_shape_image case"
    - "PPT case 用 getSelectedShapes() + getImageAsBase64()（Preview API）；失败返回 HOST_API_FAILED 引导上传"
    - "Excel case 用 getActiveChartOrNullObject() + getImage()；未激活图表返回 NOT_FOUND 引导激活"
    - "Word case 用 selection.inlinePictures.items[0].getBase64ImageSrc()（两次 sync）；无图返回 NOT_FOUND"
    - "get_shape_image 已注册到三宿主 buildToolsForHost + PPT_TOOLS set"
    - "三宿主 adapter 内，base64 被 vision 消费，ToolResult.data 只含 vision_result 纯文本"
  artifacts:
    - path: "src/agent/tools/read/vision.ts"
      provides: "getShapeImage ToolDef（第 12 个 read tool）"
      contains: "get_shape_image"
      exports: ["getShapeImage"]
    - path: "src/adapters/PptAdapter.ts"
      provides: "case 'get_shape_image' 实现（SPIKE 路径）"
      contains: "getImageAsBase64"
    - path: "src/adapters/ExcelAdapter.ts"
      provides: "case 'get_shape_image' 实现（getActiveChartOrNullObject）"
      contains: "getActiveChartOrNullObject"
    - path: "src/adapters/WordAdapter.ts"
      provides: "case 'get_shape_image' 实现（getBase64ImageSrc）"
      contains: "getBase64ImageSrc"
    - path: "src/agent/tools/index.ts"
      provides: "PPT_TOOLS + buildToolsForHost 注册"
      contains: "getShapeImage"
  key_links:
    - from: "src/agent/tools/read/vision.ts"
      to: "src/adapters/DocumentAdapter.ts"
      via: "ctx.adapter.read({ kind: 'get_shape_image', focus })"
      pattern: "adapter.read"
    - from: "src/adapters/PptAdapter.ts"
      to: "AihubmixVisionClient.analyzeImages"
      via: "vision client 调用"
      pattern: "analyzeImages"
    - from: "src/agent/tools/index.ts"
      to: "PPT_TOOLS"
      via: "Set 成员追加"
      pattern: "get_shape_image"
---

<objective>
实现 get_shape_image read tool 完整链路：第 12 个 read tool ToolDef + 三宿主 adapter read case + buildToolsForHost 注册。Spike 路径（PPT Preview API）失败时返回结构化错误引导用户点回形针上传。

Purpose: VIS-01/VIS-02 的核心实现，让 agent 能读取当前选中图/图表、调 vision、返回文本 evidence 给主 LLM。
Output: vision.ts ToolDef + 三宿主 adapter case + tools 注册（PPT_TOOLS + buildToolsForHost）
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-vis/15-CONTEXT.md
@.planning/phases/15-vis/15-RESEARCH.md
@.planning/phases/15-vis/15-PATTERNS.md
@.planning/phases/15-vis/15-01-SUMMARY.md

<interfaces>
<!-- 从 Plan 01 产出 + 现有 codebase 提取 -->

已完成的接口（Plan 01 产出）：
```typescript
// src/providers/aihubmix-vision.ts
export interface VisionImage { base64: string; mimeType: string; }
export class AihubmixVisionClient {
  analyzeImages(userText: string, images: VisionImage[], config: VisionConfig): Promise<VisionResult>;
  analyze(userText: string, imageBase64: string, mimeType: string, config: VisionConfig): Promise<VisionResult>;
}

// src/adapters/DocumentAdapter.ts（Plan 01 追加）
export type ReadableQuery =
  | ... （现有 kinds）
  | { kind: 'get_shape_image'; focus?: string };
export type ReadableResult = { ok: true; data: unknown } | { ok: false; error: ReadToolError };
```

read tool 范式（src/agent/tools/read/ppt.ts 照抄）：
```typescript
import type { ToolDef, ToolResult } from '../index';
import { wrapReadResult } from '../../read-result';

export const getShapeImage: ToolDef<GetShapeImageArgs> = {
  name: 'get_shape_image',
  kind: 'read',
  description: '...',
  parameters: { type: 'object', properties: { focus: { type: 'string', description: '...' } }, required: [] },
  humanLabel: ({ focus }) => focus ? `正在看这张图（${focus}）…` : '正在看这张图…',
  async execute({ focus }, ctx): Promise<ToolResult> {
    const r = await ctx.adapter.read({ kind: 'get_shape_image', focus });
    return wrapReadResult(r, { result_type: 'document_content', source: 'selection.image' });
  },
};
```

PPT_TOOLS set（src/agent/tools/index.ts L27-40）：
```typescript
const PPT_TOOLS = new Set([
  'insert_slide', 'set_shape_property', 'move_shape', 'set_shape_text',
  'set_shape_text_font', 'add_shape', 'copy_slide', 'set_shape_text_alignment',
  'delete_shape', 'rotate_shape', 'manage_slides', 'set_slide_background',
  // Phase 15 新增：'get_shape_image'  <-- 需追加
]);
```

wrapReadResult（来自 src/agent/read-result.ts）：
```typescript
export function wrapReadResult(
  r: ReadableResult,
  meta: { result_type: string; source: string }
): ToolResult
```

ProviderRegistry.resolve('vision', getDefaultLLM)（src/providers/registry.ts L112-123）：
```typescript
// 返回 ImageConfig，若 aihubmix key 未配置 → throw KeyInvalidError
const cfg = ProviderRegistry.resolve('vision', getDefaultLLM) as ImageConfig;
```

三宿主 adapter read case 错误格式：
```typescript
return {
  ok: false,
  error: {
    code: 'NOT_FOUND',       // 或 'UNSUPPORTED' / 'HOST_API_FAILED'
    message: '用户可读中文错误',
    recoverable: true,
    hint: 'LLM 可读操作提示',
  },
} satisfies ReadableResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 src/agent/tools/read/vision.ts（getShapeImage ToolDef）+ vision.test.ts（TDD RED）</name>
  <files>src/agent/tools/read/vision.ts, src/agent/tools/read/vision.test.ts</files>

  <read_first>
  - src/agent/tools/read/ppt.ts（exact analog，getShape ToolDef L95-123 为范式）
  - src/agent/tools/read/ppt.test.ts（若存在，read tool 单测范式：mock ctx.adapter.read + 断言 wrapReadResult；否则参照 src/providers/*.test.ts 的 vitest 范式）
  - src/agent/read-result.ts（wrapReadResult 签名确认）
  - src/agent/tools/index.ts（ToolDef + ToolResult type 所在）
  - .planning/phases/15-vis/15-PATTERNS.md §src/agent/tools/read/vision.ts
  - .planning/phases/15-vis/15-01-SUMMARY.md（analyzeImages 签名确认）
  </read_first>

  <behavior>
  - Test 1: getShapeImage.name === 'get_shape_image' 且 kind === 'read'
  - Test 2: execute() 调用 ctx.adapter.read({ kind: 'get_shape_image', focus })
  - Test 3: execute() 返回 wrapReadResult(r, { result_type: 'document_content', source: 'selection.image' })
  - Test 4: focus 为 undefined 时 humanLabel 返回 '正在看这张图…'
  - Test 5: focus 为 '图表数值' 时 humanLabel 返回 '正在看这张图（图表数值）…'
  </behavior>

  <action>
  新建 src/agent/tools/read/vision.ts，内容如下（严格照抄 ppt.ts read tool 范式）：

  ```typescript
  /**
   * src/agent/tools/read/vision.ts — get_shape_image read tool（VIS-01/VIS-02）
   *
   * 第 12 个 read tool。adapter.read({ kind: 'get_shape_image', focus }) 调三宿主取图，
   * adapter 内部已调 AihubmixVisionClient 并返回 vision 文本（base64 不出 adapter）。
   * wrapReadResult 把 vision_result 文本打包为 ToolResult.data，LLM 据此作答。
   */
  import type { ToolDef, ToolResult } from '../index';
  import { wrapReadResult } from '../../read-result';

  interface GetShapeImageArgs {
    focus?: string;
  }

  export const getShapeImage: ToolDef<GetShapeImageArgs> = {
    name: 'get_shape_image',
    description:
      '读取当前文档选中的图片或图表，调用视觉分析返回文字描述作为 evidence。' +
      '有 focus 参数时按问题针对性描述；无则客观描述整体内容。' +
      '无选中图或宿主不支持时返回错误引导，可改用回形针上传图片。',
    parameters: {
      type: 'object',
      properties: {
        focus: {
          type: 'string',
          description:
            '可选。想从图里了解什么——如「图表中的数值」「产品名称和价格」「页面布局」，' +
            '不填则通用客观描述。',
        },
      },
      required: [],
    },
    humanLabel: ({ focus }) =>
      focus ? `正在看这张图（${focus}）…` : '正在看这张图…',
    kind: 'read',
    async execute({ focus }, ctx): Promise<ToolResult> {
      const r = await ctx.adapter.read({ kind: 'get_shape_image', focus });
      return wrapReadResult(r, {
        result_type: 'document_content',
        source: 'selection.image',
      });
    },
  };
  ```

  **TDD RED（先写测试）：** 本任务 `tdd="true"`。先新建 src/agent/tools/read/vision.test.ts，按上面 `<behavior>` 的 5 个 Test 写 vitest 用例（mock `ctx.adapter.read` 返回 ReadableResult，断言 execute 调用参数 `{ kind: 'get_shape_image', focus }`、断言返回值经 wrapReadResult 包装、断言 name/kind、断言两种 humanLabel 输出）；确认测试先红，再实现 vision.ts 转绿。测试用例骨架：
  ```typescript
  import { describe, it, expect, vi } from 'vitest';
  import { getShapeImage } from './vision';
  describe('getShapeImage ToolDef', () => {
    it('name=get_shape_image, kind=read', () => {
      expect(getShapeImage.name).toBe('get_shape_image');
      expect(getShapeImage.kind).toBe('read');
    });
    it('execute 调 adapter.read({ kind: "get_shape_image", focus })', async () => {
      const read = vi.fn().mockResolvedValue({ ok: true, data: { vision_result: 'x' } });
      await getShapeImage.execute({ focus: '图表数值' }, { adapter: { read } } as never);
      expect(read).toHaveBeenCalledWith({ kind: 'get_shape_image', focus: '图表数值' });
    });
    it('humanLabel 无 focus → 正在看这张图…', () => {
      expect(getShapeImage.humanLabel({})).toBe('正在看这张图…');
    });
    it('humanLabel 有 focus → 正在看这张图（图表数值）…', () => {
      expect(getShapeImage.humanLabel({ focus: '图表数值' })).toBe('正在看这张图（图表数值）…');
    });
  });
  ```

  注意：
  - base64 在 adapter 内被 vision 消费，ToolResult.data 只含 vision_result 纯文本（base64 不出 adapter，满足 NFR-09 设计契约）
  - 此 ToolDef 三宿主共用，adapter 各自实现宿主取图逻辑
  </action>

  <verify>
    <automated>npm test -- --run src/agent/tools/read/vision.test.ts 2>/dev/null | tail -5; echo "---"; npx tsc --noEmit 2>&1 | grep "vision.ts" | head -5</automated>
  </verify>

  <acceptance_criteria>
  - ls src/agent/tools/read/vision.ts 存在
  - ls src/agent/tools/read/vision.test.ts 存在（TDD 测试文件，Wave 0 VALIDATION.md 要求）
  - grep "get_shape_image" src/agent/tools/read/vision.ts 输出 >= 2 行（name 字段 + adapter.read 调用）
  - grep "kind: 'read'" src/agent/tools/read/vision.ts 存在
  - grep "wrapReadResult" src/agent/tools/read/vision.ts 存在
  - grep "selection.image" src/agent/tools/read/vision.ts 存在（source 字段）
  - grep "base64" src/agent/tools/read/vision.ts 的结果为 0（base64 不进 tool 层）
  - npm test -- --run src/agent/tools/read/vision.test.ts 全 PASS（5 个 behavior 用例）
  - npx tsc --noEmit 不报 vision.ts 新错误
  </acceptance_criteria>

  <done>getShapeImage ToolDef + vision.test.ts 创建，kind='read'，execute 调 adapter.read，5 个 behavior 测试全绿，base64 零出现</done>
</task>

<task type="auto">
  <name>Task 2: 三宿主 adapter 追加 get_shape_image case + tools 注册</name>
  <files>src/adapters/PptAdapter.ts, src/adapters/ExcelAdapter.ts, src/adapters/WordAdapter.ts, src/agent/tools/index.ts</files>

  <read_first>
  - src/adapters/PptAdapter.ts（现有 read() 方法，case 'list_slides' L310-363 为 case 追加范式；TEXT_SHAPE_TYPES L39；warnHostErr L28-31）
  - src/adapters/ExcelAdapter.ts（read() 方法，case 'list_worksheets' L191-205 为 analog）
  - src/adapters/WordAdapter.ts（read() 方法，case 'get_paragraph_at' L1538-1566 为两次 sync 范式）
  - src/agent/tools/index.ts（PPT_TOOLS set L27-40；buildToolsForHost L242-295；import 区域 L16-19）
  - src/providers/registry.ts（resolve('vision', ...) 签名；getDefaultLLM 来源确认）
  - src/providers/aihubmix-vision.ts（AihubmixVisionClient 导入路径）
  - .planning/phases/15-vis/15-PATTERNS.md（三宿主 case 完整代码模板）
  - .planning/phases/15-vis/15-RESEARCH.md §问题 1（三宿主 API 细节 + 错误码）
  </read_first>

  <action>
  **Step A — PptAdapter.ts：**

  1. 在文件顶部 import 区域追加（查找 ProviderRegistry 和 AihubmixVisionClient 的实际导入路径）：
     ```typescript
     import { ProviderRegistry } from '../providers/registry';
     import { AihubmixVisionClient } from '../providers/aihubmix-vision';
     import type { ImageConfig } from '../providers/registry';  // 确认 ImageConfig 导出路径
     ```
     查看 registry.ts 确认 ImageConfig 导出，如不存在改用 `VisionConfig` from aihubmix-vision.ts。

  2. 在 TEXT_SHAPE_TYPES（或 IMAGE_SHAPE_TYPES 声明区域）追加：
     ```typescript
     const IMAGE_SHAPE_TYPES = new Set<string>(['Picture', 'Chart']);
     ```

  3. 在 read() 方法的 switch-case 末尾（default 前）追加 case（SPIKE 路径）：
     ```typescript
     case 'get_shape_image': {
       const focus = query.focus;
       try {
         return await PowerPoint.run(async (ctx) => {
           const selection = ctx.presentation.getSelectedShapes();
           selection.load('items/type');
           await ctx.sync();

           if (!selection.items.length) {
             return {
               ok: false,
               error: {
                 code: 'NOT_FOUND',
                 message: '请先选中一张图片或图表，或点回形针上传一张图',
                 recoverable: true,
                 hint: '选中图片或图表 shape 后再试，或使用回形针按钮上传图片',
               },
             } satisfies ReadableResult;
           }

           // D-05：多选取第一张（items 按 index 排序，PPT Web bug #3618 兼容）
           const shape = selection.items[0];
           if (!IMAGE_SHAPE_TYPES.has(shape.type)) {
             return {
               ok: false,
               error: {
                 code: 'UNSUPPORTED',
                 message: '选中形状不是图片或图表',
                 recoverable: true,
                 hint: '请选中图片或图表 shape，或点回形针上传图片',
               },
             } satisfies ReadableResult;
           }

           // SPIKE: PowerPoint Preview API — powerpoint-js-preview requirement set
           // 若 getImageAsBase64 不存在或抛错，catch 块返回 HOST_API_FAILED fallback
           const imageResult = (shape as unknown as { getImageAsBase64: () => { value: string } }).getImageAsBase64();
           await ctx.sync();
           const base64 = imageResult.value;

           const cfg = ProviderRegistry.resolve('vision', getDefaultLLM) as VisionConfig;
           const userText = focus
             ? `${focus}（请从图中抽取能直接用于撰写文档的具体细节）`
             : '请客观描述图片的所有关键内容：文字、数据、人物/物品、版式结构，用于协助撰写办公文档。';
           const { content } = await new AihubmixVisionClient().analyzeImages(
             userText,
             [{ base64, mimeType: 'image/png' }],
             cfg,
           );
           return {
             ok: true,
             data: { vision_result: content, shape_count: selection.items.length },
           } satisfies ReadableResult;
         });
       } catch (err) {
         // Spike 失败 fallback：返回结构化错误引导用户改用回形针上传（D-07/D-13）
         warnHostErr('get_shape_image', err);
         // 检查是否为 AsterError 子类（如 KeyInvalidError），则重新抛出让 dispatchTool 处理
         if (err instanceof AsterError) throw err;
         return {
           ok: false,
           error: {
             code: 'HOST_API_FAILED',
             message: '当前无法读取选中图（宿主限制），可点回形针上传这张图',
             recoverable: true,
             hint: '改用 InputBar 回形针按钮上传图片，绕过宿主限制',
           },
         } satisfies ReadableResult;
       }
     }
     ```

  **Step B — ExcelAdapter.ts：**

  1. 在文件 import 区域追加（查找 registry 和 vision client 导入路径）。
  2. 在 read() switch-case 末尾追加：
     ```typescript
     case 'get_shape_image': {
       const focus = query.focus;
       try {
         return await Excel.run(async (ctx) => {
           const chartOrNull = ctx.workbook.getActiveChartOrNullObject();
           await ctx.sync();
           if (chartOrNull.isNullObject) {
             return {
               ok: false,
               error: {
                 code: 'NOT_FOUND',
                 message: '请先点击一下图表使其激活，或点回形针上传图片',
                 recoverable: true,
                 hint: '单击图表后再调用此工具，或使用回形针上传图片',
               },
             } satisfies ReadableResult;
           }
           const imageResult = chartOrNull.getImage(); // ExcelApi 1.2，返回 JPEG base64
           await ctx.sync();
           const base64 = imageResult.value;

           const cfg = ProviderRegistry.resolve('vision', getDefaultLLM) as VisionConfig;
           const userText = focus
             ? `${focus}（请从图中抽取能直接用于撰写文档的具体细节）`
             : '请客观描述图片的所有关键内容，用于协助撰写办公文档。';
           const { content } = await new AihubmixVisionClient().analyzeImages(
             userText,
             [{ base64, mimeType: 'image/jpeg' }],
             cfg,
           );
           return { ok: true, data: { vision_result: content } } satisfies ReadableResult;
         });
       } catch (err) {
         if (err instanceof AsterError) throw err;
         throw new HostApiError('Excel get_shape_image 失败', err);
       }
     }
     ```

  **Step C — WordAdapter.ts：**

  1. 在文件 import 区域追加（查找导入路径）。
  2. 在 read() switch-case 末尾追加（注意两次 ctx.sync() — Pitfall 3）：
     ```typescript
     case 'get_shape_image': {
       const focus = query.focus;
       try {
         return await Word.run(async (ctx) => {
           const selection = ctx.document.getSelection();
           selection.inlinePictures.load('items');
           await ctx.sync(); // sync 1: load inlinePictures

           if (selection.inlinePictures.items.length === 0) {
             return {
               ok: false,
               error: {
                 code: 'NOT_FOUND',
                 message: '选区内没有内嵌图片，请先选中图片，或点回形针上传图片',
                 recoverable: true,
                 hint: '选中文档内的内嵌图片后再试，或使用回形针上传图片',
               },
             } satisfies ReadableResult;
           }

           // D-05：多选取第一张
           const pic = selection.inlinePictures.items[0];
           const base64Result = pic.getBase64ImageSrc(); // ClientResult<string>，需 sync 后才可读（Pitfall 3）
           await ctx.sync(); // sync 2: 触发 ClientResult 值
           const base64 = base64Result.value;

           const cfg = ProviderRegistry.resolve('vision', getDefaultLLM) as VisionConfig;
           const userText = focus
             ? `${focus}（请从图中抽取能直接用于撰写文档的具体细节）`
             : '请客观描述图片的所有关键内容，用于协助撰写办公文档。';
           const { content } = await new AihubmixVisionClient().analyzeImages(
             userText,
             [{ base64, mimeType: 'image/png' }],
             cfg,
           );
           return {
             ok: true,
             data: { vision_result: content, pic_count: selection.inlinePictures.items.length },
           } satisfies ReadableResult;
         });
       } catch (err) {
         if (err instanceof AsterError) throw err;
         throw new HostApiError('Word get_shape_image 失败', err);
       }
     }
     ```

  **Step D — tools/index.ts：**

  1. 在 import 区域（L16-19）追加：
     ```typescript
     import { getShapeImage } from './read/vision';
     ```

  2. 在 PPT_TOOLS set（L27-40）追加 `'get_shape_image'`。

  3. 在 buildToolsForHost 三个 case 各追加 getShapeImage：
     - case 'word'：`getDocumentFullText, getParagraphCount, getParagraphAt, getDocumentOutline, getShapeImage,`
     - case 'excel'：`listWorksheets, getRangeValues, getUsedRangeSummary, getShapeImage,`
     - case 'ppt'：`listSlides, getSlide, listShapesOnSlide, getShape, getShapeImage,`

  注意：
  - `getShapeImage.kind = 'read'`，不走 assertWriteToolRegisterable（只验 write 工具）
  - 关于 `getDefaultLLM`：查看 adapter 文件现有 import，确认 getDefaultLLM 来自 agentStore 还是 providers store（查 PptAdapter.ts 中其他 ProviderRegistry.resolve 调用的 getDefaultLLM 来源）
  - 若 ImageConfig type 从 registry.ts 不直接导出，用 VisionConfig from aihubmix-vision.ts 替代（两者结构相同）
  - PPT SPIKE 说明：`shape.getImageAsBase64()` 是 Preview API，类型可能不在 @types/office-js 中，用 `as unknown as {...}` 转型，并在注释中注明 SPIKE + Preview API + 失败走 catch fallback
  </action>

  <verify>
    <automated>npm test -- --run src/adapters/PptAdapter.read.test.ts src/adapters/ExcelAdapter.read.test.ts src/adapters/WordAdapter.read.test.ts src/agent/tools/tools.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
  - grep "get_shape_image" src/adapters/PptAdapter.ts 输出 >= 1 行（case 实现）
  - grep "getImageAsBase64" src/adapters/PptAdapter.ts 存在（SPIKE API 调用）
  - grep "HOST_API_FAILED" src/adapters/PptAdapter.ts 存在（spike 失败 fallback 错误码）
  - grep "getActiveChartOrNullObject" src/adapters/ExcelAdapter.ts 存在（ExcelApi 1.9 API）
  - grep "getBase64ImageSrc" src/adapters/WordAdapter.ts 存在（WordApi 1.1 API）
  - grep "ctx.sync" src/adapters/WordAdapter.ts の该 case 区域出现 2 次（两次 sync，Pitfall 3）
  - grep "'get_shape_image'" src/agent/tools/index.ts 输出 >= 2 行（PPT_TOOLS + buildToolsForHost）
  - npm test -- --run src/agent/tools/tools.test.ts 全 PASS
  - npm test -- --run 全绿（现有测试不退步）
  - npx tsc --noEmit 不报新错误
  </acceptance_criteria>

  <done>三宿主 adapter 均有 get_shape_image case；get_shape_image 已加入 PPT_TOOLS + 三宿主 buildToolsForHost；测试套件全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter.read() → Office.js API | Office.js run 闭包内取图，base64 仅在 adapter 内被 vision 消费 |
| adapter → AihubmixVisionClient | base64 以 VisionImage 形式传入；vision client 调 aihubmix；apiKey 在 header |
| dispatchTool → adapter read | AsterError 子类（KeyInvalidError）由 adapter throw → dispatchTool catch → sanitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | PptAdapter get_shape_image | mitigate | catch 块检查 err instanceof AsterError 后重抛（保留 sanitize 路径）；陌生异常返回结构化错误不 throw err.message，防 stack 泄漏 |
| T-15-04 | Information Disclosure | base64 不出 adapter | mitigate | ToolResult.data 只含 `{ vision_result: string }`，不含 base64 字段；acceptance_criteria grep "base64" 守门 |
| T-15-05 | Denial of Service | PPT shape.getImageAsBase64 大图 | accept | 5MB 上限在 FILE-06 上传路径；文档选中图走 Office.js 原生 API，大小由宿主控制，无前端拦截点；RESEARCH 建议 accept |
| T-15-06 | Tampering | PPT Preview API 不存在时 as unknown 转型 | mitigate | try/catch 包裹整个 PowerPoint.run；getImageAsBase64 不存在 → catch → HOST_API_FAILED 结构化错误（不 throw ，不泄漏原始错误） |
| T-15-07 | Elevation of Privilege | vision 结果 prompt injection | mitigate | vision 结果进入 ToolResult.data（纯文本），经 wrapReadResult applySizeCap 后作为 tool response 传给主 LLM；主 LLM 为 DeepSeek 纯文本模型，agent 只「引用」evidence 而非执行；沿用 v2.0 sanitize 注入边界 |
</threat_model>

<verification>
1. TypeScript 编译：`npx tsc --noEmit` 无新错误
2. 三宿主 adapter 测试扩展：`npm test -- --run src/adapters/PptAdapter.read.test.ts src/adapters/ExcelAdapter.read.test.ts src/adapters/WordAdapter.read.test.ts` PASS
3. tools 注册测试：`npm test -- --run src/agent/tools/tools.test.ts` PASS（含 PPT_TOOLS 守门）
4. vision.ts ToolDef 测试：`npm test -- --run src/agent/tools/read/vision.test.ts` PASS
5. 全量测试：`npm test -- --run` 全绿
6. base64 不出 tool 层：`grep -v "^//" src/agent/tools/read/vision.ts | grep "base64"` 结果为空
</verification>

<success_criteria>
- get_shape_image read tool 存在，kind='read'，execute 调 adapter.read，结果经 wrapReadResult 返回
- 三宿主 adapter 均处理 get_shape_image case，错误分别为 NOT_FOUND / UNSUPPORTED / HOST_API_FAILED
- PPT spike 路径失败 → HOST_API_FAILED 结构化错误（引导回形针上传）
- Word 两次 ctx.sync（Pitfall 3 守门）
- get_shape_image 在 PPT_TOOLS set + 三宿主 buildToolsForHost（D-13 守则）
- npm test -- --run 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/15-vis/15-02-SUMMARY.md`，记录：
- vision.ts ToolDef 导出的 getShapeImage 已注册到三宿主
- PPT spike 结果（getImageAsBase64 的 @types/office-js 情况 + as unknown 转型注记）
- 三宿主 case 的 import 路径（getDefaultLLM 来源）
- 任何实现偏差说明
</output>

---
phase: 15-vis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/aihubmix-vision.ts
  - src/adapters/DocumentAdapter.ts
  - src/providers/aihubmix-vision.test.ts
  - src/store/attachments.test.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - FILE-06
  - NFR-09

must_haves:
  truths:
    - "AihubmixVisionClient.analyzeImages 支持多张图片 content array"
    - "analyze() 向后兼容（内部调 analyzeImages）"
    - "DocumentAdapter.ReadableQuery 包含 get_shape_image kind"
    - "aihubmix-vision.test.ts 和 attachments.test.ts 测试脚手架存在（Wave 0 RED 状态）"
  artifacts:
    - path: "src/providers/aihubmix-vision.ts"
      provides: "扩展 VisionImage 接口 + analyzeImages 多图方法"
      contains: "analyzeImages"
    - path: "src/adapters/DocumentAdapter.ts"
      provides: "ReadableQuery 新增 get_shape_image kind"
      contains: "get_shape_image"
    - path: "src/providers/aihubmix-vision.test.ts"
      provides: "Wave 0 测试脚手架（初始 RED）"
      min_lines: 30
    - path: "src/store/attachments.test.ts"
      provides: "Wave 0 测试脚手架（初始 RED）"
      min_lines: 20
  key_links:
    - from: "src/providers/aihubmix-vision.ts"
      to: "analyzeImages"
      via: "analyze 内部调用"
      pattern: "analyzeImages"
    - from: "src/adapters/DocumentAdapter.ts"
      to: "ReadableQuery"
      via: "union type 追加"
      pattern: "get_shape_image"
---

<objective>
Phase 15 的类型合约地基：扩展 aihubmix-vision 客户端支持多图 + focus 参数，在 DocumentAdapter 中声明新 ReadableQuery kind，并建立两个 Wave 0 测试文件脚手架（初始 RED 状态，Wave 2 实现后变绿）。

Purpose: 所有下游 Plan（02 read tool、03 上传路径）都依赖这里定义的接口类型；先建接口契约，executor 实现时对照合约，不需要猜测签名。
Output: 扩展后的 aihubmix-vision.ts + DocumentAdapter.ts 类型补丁 + 2 个测试文件脚手架
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-vis/15-CONTEXT.md
@.planning/phases/15-vis/15-RESEARCH.md
@.planning/phases/15-vis/15-PATTERNS.md

<interfaces>
<!-- 关键现有接口，executor 直接使用，不需要探索 -->

src/providers/aihubmix-vision.ts（当前完整内容）：
```typescript
export interface VisionConfig {
  baseURL: string;
  apiKey: string;
}
export interface VisionResult {
  content: string;
}
export class AihubmixVisionClient {
  async analyze(
    userText: string,
    imageBase64: string,
    mimeType: string,
    config: VisionConfig,
  ): Promise<VisionResult>
}
```

src/adapters/DocumentAdapter.ts ReadableQuery（L164-179）：
```typescript
export type ReadableQuery =
  | { kind: 'selection_detail' }
  | { kind: 'list_slides' }
  | { kind: 'get_slide'; slideIndex: number }
  | { kind: 'list_shapes_on_slide'; slideIndex: number }
  | { kind: 'get_shape'; slideIndex: number; shapeId: string }
  | { kind: 'list_worksheets' }
  | { kind: 'get_range_values'; address: string }
  | { kind: 'get_used_range_summary'; sheetName?: string }
  | { kind: 'get_paragraph_count' }
  | { kind: 'get_paragraph_at'; index: number }
  | { kind: 'get_document_outline' }
  | { kind: 'get_document_full_text' };
```

src/store/attachments.ts（目标接口，此 Plan 建测试脚手架时用）：
```typescript
export interface AttachedImage {
  id: string;
  base64: string;
  mimeType: 'image/png' | 'image/jpeg' | 'image/webp';
  fileName: string;
  sizeBytes: number;
}
interface AttachmentState {
  images: AttachedImage[];
  addImages: (imgs: AttachedImage[]) => void;
  clearImages: () => void;
  removeImage: (id: string) => void;
}
export const useAttachmentStore = create<AttachmentState>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 aihubmix-vision.ts 支持多图 + focus（向后兼容）</name>
  <files>src/providers/aihubmix-vision.ts</files>

  <read_first>
  - src/providers/aihubmix-vision.ts（现有完整内容，L1-71）
  - src/providers/registry.ts（AIHUBMIX_VISION_MODEL const 路径，确认 import）
  - src/lib/sse.ts（mapHttpError 导出路径）
  - src/errors.ts（NetworkError 导出路径）
  - .planning/phases/15-vis/15-PATTERNS.md §src/providers/aihubmix-vision.ts（扩展方向详解）
  </read_first>

  <behavior>
  - Test 1: analyzeImages 构建 content array：text block 在前，image_url blocks 在后；每张图 url = `data:${mimeType};base64,${base64}`
  - Test 2: analyze() 向后兼容：调用时内部代理到 analyzeImages，不破坏现有行为
  - Test 3: apiKey 仅在 Authorization header，不在请求 body（T-01-04 安全守门）
  - Test 4: 网络失败 → throw NetworkError；HTTP 4xx → throw mapHttpError(status, body)
  - Test 5: stream: false 在请求 body（非流式 vision 调用）
  </behavior>

  <action>
  在 src/providers/aihubmix-vision.ts 中做以下最小改动：

  1. 在 VisionResult interface 之前新增 VisionImage interface：
  ```typescript
  export interface VisionImage {
    base64: string;
    mimeType: string;
  }
  ```

  2. 在 AihubmixVisionClient class 内新增 analyzeImages 方法（在现有 analyze 之前）：
  ```typescript
  async analyzeImages(
    userText: string,
    images: VisionImage[],
    config: VisionConfig,
  ): Promise<VisionResult> {
    const url = `${config.baseURL.replace(/\/$/, '')}/chat/completions`;
    const imageBlocks = images.map(({ base64, mimeType }) => ({
      type: 'image_url' as const,
      image_url: { url: `data:${mimeType};base64,${base64}` },
    }));
    const content = [
      { type: 'text' as const, text: userText },
      ...imageBlocks,
    ];

    let resp: Response;
    try {
      resp = await fetch(url, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${config.apiKey}`,  // apiKey 仅 header（T-01-04）
        },
        body: JSON.stringify({
          model: AIHUBMIX_VISION_MODEL,
          stream: false,
          messages: [{ role: 'user', content }],
        }),
      });
    } catch {
      throw new NetworkError('aihubmix 视觉请求网络失败');
    }

    if (!resp.ok) {
      const errBody = await resp.json().catch(() => ({}));
      throw mapHttpError(resp.status, errBody);
    }

    const json = await resp.json() as { choices?: Array<{ message?: { content?: string } }> };
    const text = json.choices?.[0]?.message?.content ?? '';
    return { content: text };
  }
  ```

  3. 修改现有 analyze() 方法：改为 delegate 到 analyzeImages（向后兼容）：
  ```typescript
  async analyze(
    userText: string,
    imageBase64: string,
    mimeType: string,
    config: VisionConfig,
  ): Promise<VisionResult> {
    return this.analyzeImages(userText, [{ base64: imageBase64, mimeType }], config);
  }
  ```

  注意：
  - 保留所有现有 import（mapHttpError, NetworkError, AIHUBMIX_VISION_MODEL）
  - apiKey 不出现在 body，不出现在 error message（T-01-04 安全约束）
  </action>

  <verify>
    <automated>npm test -- --run src/providers/aihubmix-vision.test.ts 2>/dev/null || echo "PASS: tests will be written in task 3 (Wave 0 scaffold)"</automated>
  </verify>

  <acceptance_criteria>
  - grep -c "analyzeImages" src/providers/aihubmix-vision.ts の结果 >= 2（声明 + 实现）
  - grep -c "VisionImage" src/providers/aihubmix-vision.ts の结果 >= 2（interface 定义 + 参数使用）
  - grep "Authorization" src/providers/aihubmix-vision.ts 含 Bearer（apiKey 在 header）
  - grep "apiKey" src/providers/aihubmix-vision.ts 不含 body JSON.stringify 内（apiKey 不进 body）
  - grep "stream: false" src/providers/aihubmix-vision.ts 存在（非流式）
  - 现有 analyze() 仍存在，内部调用 analyzeImages（向后兼容）
  - TypeScript 严格模式不报错：npx tsc --noEmit 不报新错误
  </acceptance_criteria>

  <done>AihubmixVisionClient 同时提供 analyze()（向后兼容）和 analyzeImages()（多图），两个方法均通过测试</done>
</task>

<task type="auto">
  <name>Task 2: DocumentAdapter.ts 追加 get_shape_image kind + Wave 0 测试脚手架</name>
  <files>src/adapters/DocumentAdapter.ts, src/providers/aihubmix-vision.test.ts, src/store/attachments.test.ts</files>

  <read_first>
  - src/adapters/DocumentAdapter.ts（L155-215，ReadableQuery + ReadableResult 区域）
  - src/store/providers.test.ts（Vitest + vi.stubGlobal fetch mock 范式，作为 vision.test.ts analog）
  - src/store/selection.test.ts（最小内存 store 测试范式，作为 attachments.test.ts analog）
  - .planning/phases/15-vis/15-PATTERNS.md §DocumentAdapter.ts 和 §attachments.test.ts
  - .planning/phases/15-vis/15-RESEARCH.md §问题 4（NFR-09 序列化守门测试范式）
  </read_first>

  <action>
  **Step A — DocumentAdapter.ts 类型扩展（0-import 约束：不引入任何 runtime 依赖）：**

  在 ReadableQuery union type（L179 末尾 `| { kind: 'get_document_full_text' };` 之后）追加：
  ```typescript
    | { kind: 'get_shape_image'; focus?: string };
  ```
  注意：`focus` 为可选字段；三宿主共用此 kind，各自在 adapter.read() 内解释执行。

  **Step B — 新建 src/providers/aihubmix-vision.test.ts（Wave 0 脚手架）：**

  覆盖点（测试初始状态为 RED，Task 1 完成后这里会变绿）：
  ```typescript
  import { describe, it, expect, vi, beforeEach } from 'vitest';
  import { AihubmixVisionClient } from './aihubmix-vision';

  const mockFetch = vi.fn();
  vi.stubGlobal('fetch', mockFetch);

  describe('AihubmixVisionClient', () => {
    const config = { baseURL: 'https://api.aihubmix.com/v1', apiKey: 'test-key' };

    beforeEach(() => mockFetch.mockReset());

    describe('analyzeImages', () => {
      it('多图：content array 格式正确（text 在前，image_url blocks 在后）', async () => {
        mockFetch.mockResolvedValueOnce({
          ok: true,
          json: async () => ({ choices: [{ message: { content: '图片描述' } }] }),
        });
        const client = new AihubmixVisionClient();
        const result = await client.analyzeImages(
          '描述这些图片',
          [
            { base64: 'base64abc', mimeType: 'image/png' },
            { base64: 'base64def', mimeType: 'image/jpeg' },
          ],
          config,
        );
        expect(result.content).toBe('图片描述');
        const body = JSON.parse(mockFetch.mock.calls[0][1].body);
        const msgContent = body.messages[0].content;
        expect(msgContent[0]).toMatchObject({ type: 'text', text: '描述这些图片' });
        expect(msgContent[1]).toMatchObject({
          type: 'image_url',
          image_url: { url: 'data:image/png;base64,base64abc' },
        });
        expect(msgContent[2]).toMatchObject({
          type: 'image_url',
          image_url: { url: 'data:image/jpeg;base64,base64def' },
        });
      });

      it('apiKey 仅在 Authorization header，不在 body（T-01-04）', async () => {
        mockFetch.mockResolvedValueOnce({
          ok: true,
          json: async () => ({ choices: [{ message: { content: '' } }] }),
        });
        const client = new AihubmixVisionClient();
        await client.analyzeImages('test', [{ base64: 'b64', mimeType: 'image/png' }], config);
        const [, reqInit] = mockFetch.mock.calls[0];
        expect((reqInit.headers as Record<string, string>).Authorization).toBe('Bearer test-key');
        const body = JSON.parse(reqInit.body);
        expect(JSON.stringify(body)).not.toContain('test-key');
      });

      it('stream: false 在请求 body', async () => {
        mockFetch.mockResolvedValueOnce({
          ok: true,
          json: async () => ({ choices: [{ message: { content: '' } }] }),
        });
        const client = new AihubmixVisionClient();
        await client.analyzeImages('test', [{ base64: 'b64', mimeType: 'image/png' }], config);
        const body = JSON.parse(mockFetch.mock.calls[0][1].body);
        expect(body.stream).toBe(false);
      });

      it('网络失败 → NetworkError', async () => {
        mockFetch.mockRejectedValueOnce(new TypeError('fetch failed'));
        const client = new AihubmixVisionClient();
        await expect(
          client.analyzeImages('test', [{ base64: 'b64', mimeType: 'image/png' }], config)
        ).rejects.toThrow('aihubmix 视觉请求网络失败');
      });
    });

    describe('analyze（向后兼容）', () => {
      it('单图调用内部委托给 analyzeImages', async () => {
        const spy = vi.spyOn(AihubmixVisionClient.prototype, 'analyzeImages');
        spy.mockResolvedValueOnce({ content: '单图结果' });
        const client = new AihubmixVisionClient();
        const result = await client.analyze('描述', 'b64abc', 'image/png', config);
        expect(result.content).toBe('单图结果');
        expect(spy).toHaveBeenCalledWith('描述', [{ base64: 'b64abc', mimeType: 'image/png' }], config);
      });
    });
  });
  ```

  **Step C — 新建 src/store/attachments.test.ts（Wave 0 脚手架，初始 RED）：**

  ```typescript
  import { describe, it, expect, vi } from 'vitest';

  // Wave 0: 此测试文件在 attachments.ts 创建（Plan 03）前保持 RED
  // 测试 import 会失败直到 attachments.ts 存在

  describe('useAttachmentStore（Wave 0 脚手架）', () => {
    it('WAVE_0_STUB: attachments.ts 不存在时此用例失败（正常 RED 状态）', async () => {
      // Dynamic import 方式：attachments.ts 不存在则报错
      const mod = await import('../store/attachments').catch(() => null);
      expect(mod).not.toBeNull(); // 失败直到 Plan 03 创建 attachments.ts
    });
  });
  ```

  注意：attachments.test.ts 的完整断言将在 Plan 03（attachments.ts 实现完成后）由执行者补全。此处只建立文件脚手架确保 Wave 0 有测试占位。
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
  - grep "get_shape_image" src/adapters/DocumentAdapter.ts 输出 1 行（union 末尾追加，含 focus?: string）
  - grep "focus\?" src/adapters/DocumentAdapter.ts 的内容出现在 get_shape_image kind 行
  - ls src/providers/aihubmix-vision.test.ts 存在
  - ls src/store/attachments.test.ts 存在
  - npx tsc --noEmit 不报 DocumentAdapter.ts 相关错误
  - npm test -- --run src/providers/aihubmix-vision.test.ts 全部通过（Task 1 已完成，测试变绿）
  </acceptance_criteria>

  <done>DocumentAdapter ReadableQuery 已包含 get_shape_image kind；两个测试文件已建立；npm test -- --run src/providers/aihubmix-vision.test.ts 全部 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AihubmixVisionClient → aihubmix API | 用户 apiKey 从 config 注入 header，base64 图片字节从浏览器直发；apiKey 不经 Aster 服务器 |
| 测试文件 → mock fetch | 测试用 vi.stubGlobal 替换 fetch，apiKey 仅在 headers 内，不写入 body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | AihubmixVisionClient.analyzeImages | mitigate | apiKey 仅放 Authorization header，body JSON.stringify 不含 apiKey；测试断言 `JSON.stringify(body)` 不含 apiKey 字符串 |
| T-15-02 | Information Disclosure | aihubmix-vision.test.ts | accept | 测试仅用 'test-key' 占位，无真实 key 泄漏风险 |
</threat_model>

<verification>
1. 修改后 TypeScript 编译无新错误：`npx tsc --noEmit` 干净
2. vision 测试全 PASS：`npm test -- --run src/providers/aihubmix-vision.test.ts`
3. 现有全量测试不退步：`npm test -- --run` 绿
</verification>

<success_criteria>
- AihubmixVisionClient 暴露 analyzeImages(userText, images: VisionImage[], config) 方法
- analyze() 向后兼容（内部调 analyzeImages）
- DocumentAdapter.ReadableQuery 包含 `{ kind: 'get_shape_image'; focus?: string }` 变体
- 两个 Wave 0 测试文件存在，aihubmix-vision.test.ts 全 PASS
- npm test -- --run 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/15-vis/15-01-SUMMARY.md`，记录：
- analyzeImages 签名（供 Plan 02/03 executor 引用）
- VisionImage interface 定义
- DocumentAdapter ReadableQuery 新增 kind 确认
- 任何实现偏差说明
</output>

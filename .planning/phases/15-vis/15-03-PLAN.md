---
phase: 15-vis
plan: 03
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - src/store/attachments.ts
  - src/components/InputBar.tsx
  - src/store/chat.ts
autonomous: true
requirements:
  - FILE-06
  - NFR-09

must_haves:
  truths:
    - "useAttachmentStore 为纯内存态 Zustand store，无 persist middleware"
    - "InputBar 回形针按钮从 aria-disabled 激活为可点击，链接隐藏 file input"
    - "file input accept 限 image/png,image/jpeg,image/webp，onChange 二次检查 file.type"
    - "非图片文件返回错误「文件解析即将开放，当前可上传图片」（D-14 诚实）"
    - "单图 >5MB 返回错误「图片过大，请选择 5MB 以下的图片」"
    - "Ctrl+V 粘贴通过 paste 事件 clipboardData.items 取图（同步 DataTransfer API，不用 navigator.clipboard）"
    - "sendMessage 发消息前若有附件图，一次性调 analyzeImages + augmented prompt 注入"
    - "用户气泡显示 original prompt（不含 base64），base64 不写入 Message.content"
    - "vision 失败时诚实降级「[注：图片分析失败]」，不阻断发送"
    - "上传图多轮复用：发消息后不自动清除，用户手动点 chip × 删除（D-10）"
    - "composer 区域显示上传图缩略图 chip（含 × 删除按钮）"
  artifacts:
    - path: "src/store/attachments.ts"
      provides: "内存态附件 store（AttachedImage[]，无 persist）"
      exports: ["useAttachmentStore", "AttachedImage"]
      min_lines: 30
    - path: "src/components/InputBar.tsx"
      provides: "激活的回形针按钮 + file input + paste handler + 缩略图 chip"
      contains: "fileInputRef"
    - path: "src/store/chat.ts"
      provides: "sendMessage 扩展：vision 注入 + augmented prompt"
      contains: "useAttachmentStore"
  key_links:
    - from: "src/components/InputBar.tsx"
      to: "src/store/attachments.ts"
      via: "useAttachmentStore addImages"
      pattern: "addImages"
    - from: "src/store/chat.ts"
      to: "AihubmixVisionClient.analyzeImages"
      via: "sendMessage 发消息前调用"
      pattern: "analyzeImages"
    - from: "src/store/chat.ts"
      to: "src/store/attachments.ts"
      via: "useAttachmentStore.getState()"
      pattern: "useAttachmentStore"
---

<objective>
FILE-06 图片上传路径完整实现：新建内存态 attachments store + 激活 InputBar 回形针（file input + Ctrl+V 粘贴 + 缩略图 chip UI）+ chat.ts sendMessage 注入 vision evidence。

Purpose: 用户可上传图片让 agent「据图作答」，图片 base64 仅在内存态，不进 message.content，不进 localStorage。
Output: attachments.ts 内存 store + InputBar 激活 + chat.ts sendMessage 扩展
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-vis/15-CONTEXT.md
@.planning/phases/15-vis/15-RESEARCH.md
@.planning/phases/15-vis/15-PATTERNS.md
@.planning/phases/15-vis/15-01-SUMMARY.md

<interfaces>
<!-- 现有关键接口 -->

src/store/providers.ts（Zustand create 范式，attachments.ts analog）：
```typescript
import { create } from 'zustand';
export const useProviderStore = create<ProviderState>((set, get) => ({ ... }));
```

src/components/InputBar.tsx 关键现有区域：
- L42: textareaRef = useRef<HTMLTextAreaElement>(null)
- L113-123: textarea JSX（onKeyDown, onChange, value, disabled）
- L144-153: 当前 aria-disabled 回形针按钮（激活目标）
- L62: const sendMessage = useChatStore((s) => s.sendMessage)

src/store/chat.ts sendMessage（当前实现）：
```typescript
async sendMessage(prompt, selectionCtx, adapter) {
  get().pushMessage({ role: 'user', content: prompt, ts: Date.now() });
  await useAgentStore.getState().runAgent(prompt, selectionCtx, adapter);
},
```

Plan 01 产出：
```typescript
export interface VisionImage { base64: string; mimeType: string; }
export class AihubmixVisionClient {
  analyzeImages(userText: string, images: VisionImage[], config: VisionConfig): Promise<VisionResult>;
}
```

ProviderRegistry.resolve('vision', getDefaultLLM)：若 aihubmix key 未配置 → throw KeyInvalidError；
src/components/InputBar.tsx 设计系统类名：tool-btn（按钮），参照 GearIcon 按钮激活范式（L127-134）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 src/store/attachments.ts（纯内存态 Zustand store）</name>
  <files>src/store/attachments.ts</files>

  <read_first>
  - src/store/providers.ts（Zustand create 无 persist 范式，import 路径）
  - src/store/selection.ts（最小内存 store 范式，analog）
  - .planning/phases/15-vis/15-PATTERNS.md §src/store/attachments.ts（完整接口定义）
  - src/store/attachments.test.ts（Wave 0 测试脚手架，需在此 Plan 补全断言）
  </read_first>

  <behavior>
  - Test 1: addImages 追加图片，不覆盖现有（images 数组递增）
  - Test 2: clearImages 后 images.length === 0
  - Test 3: removeImage(id) 精确移除目标，保留其余
  - Test 4: NFR-09 守门：store 无 persist middleware —— 检查 localStorage.setItem 不被调用
  - Test 5: 初始状态 images 为空数组
  </behavior>

  <action>
  新建 src/store/attachments.ts：

  ```typescript
  /**
   * src/store/attachments.ts — 图片附件内存态 store（FILE-06，VIS Phase 15）
   *
   * 纯内存 Zustand slice，**无 persist middleware**（NFR-09 硬约束）。
   * base64 图片字节仅存在于此内存 store，绝不写入 localStorage / sessionStorage。
   * 刷新即丢，用户每次 session 开始需重新上传（D-10 明确约定）。
   *
   * 多轮复用：发送消息后不自动清除，用户手动点缩略图 chip 上的 × 删除（D-10）。
   */
  import { create } from 'zustand';

  export interface AttachedImage {
    id: string;
    base64: string;  // 裸 base64，不含 data: 前缀
    mimeType: 'image/png' | 'image/jpeg' | 'image/webp';
    fileName: string;
    sizeBytes: number;
  }

  interface AttachmentState {
    images: AttachedImage[];
    /** 追加图片列表（不覆盖现有）*/
    addImages: (imgs: AttachedImage[]) => void;
    /** 清空所有附件图（session 结束 / 用户主动清空）*/
    clearImages: () => void;
    /** 移除指定 id 的图片（chip × 按钮触发）*/
    removeImage: (id: string) => void;
  }

  // 纯内存 store：不使用 persist()，满足 NFR-09（base64 不进 localStorage）
  export const useAttachmentStore = create<AttachmentState>((set) => ({
    images: [],
    addImages: (imgs) =>
      set((s) => ({ images: [...s.images, ...imgs] })),
    clearImages: () => set({ images: [] }),
    removeImage: (id) =>
      set((s) => ({ images: s.images.filter((i) => i.id !== id) })),
  }));
  ```

  然后补全 src/store/attachments.test.ts，用完整断言替换 Wave 0 stub：

  ```typescript
  import { describe, it, expect, vi, beforeEach } from 'vitest';
  import { useAttachmentStore } from './attachments';
  import type { AttachedImage } from './attachments';

  const makeImage = (id: string): AttachedImage => ({
    id,
    base64: 'abc123',
    mimeType: 'image/png',
    fileName: `test-${id}.png`,
    sizeBytes: 1024,
  });

  describe('useAttachmentStore', () => {
    beforeEach(() => useAttachmentStore.getState().clearImages());

    it('初始状态 images 为空数组', () => {
      expect(useAttachmentStore.getState().images).toHaveLength(0);
    });

    it('addImages 追加图片，不覆盖现有', () => {
      useAttachmentStore.getState().addImages([makeImage('a')]);
      useAttachmentStore.getState().addImages([makeImage('b')]);
      expect(useAttachmentStore.getState().images).toHaveLength(2);
      expect(useAttachmentStore.getState().images[0].id).toBe('a');
      expect(useAttachmentStore.getState().images[1].id).toBe('b');
    });

    it('clearImages 后 images 为空', () => {
      useAttachmentStore.getState().addImages([makeImage('a'), makeImage('b')]);
      useAttachmentStore.getState().clearImages();
      expect(useAttachmentStore.getState().images).toHaveLength(0);
    });

    it('removeImage 精确移除目标 id，保留其余', () => {
      useAttachmentStore.getState().addImages([makeImage('a'), makeImage('b'), makeImage('c')]);
      useAttachmentStore.getState().removeImage('b');
      const ids = useAttachmentStore.getState().images.map((i) => i.id);
      expect(ids).toEqual(['a', 'c']);
    });

    it('NFR-09：store 为纯内存态，localStorage.setItem 不被调用', () => {
      const spy = vi.spyOn(Storage.prototype, 'setItem');
      useAttachmentStore.getState().addImages([makeImage('x')]);
      // 无论 store 内部有何 side effect，localStorage.setItem 均不应被调用
      expect(spy).not.toHaveBeenCalled();
      spy.mockRestore();
    });
  });
  ```
  </action>

  <verify>
    <automated>npm test -- --run src/store/attachments.test.ts 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - npm test -- --run src/store/attachments.test.ts 全部 PASS（5 个用例）
  - grep "persist" src/store/attachments.ts 结果为 0（无 persist middleware，NFR-09）
  - grep "localStorage" src/store/attachments.ts 结果为 0（不写 storage）
  - grep "export.*AttachedImage" src/store/attachments.ts 存在（类型导出）
  - grep "export.*useAttachmentStore" src/store/attachments.ts 存在（store 导出）
  - npx tsc --noEmit 不报新错误
  </acceptance_criteria>

  <done>useAttachmentStore 存在，5 个测试 PASS，无 persist，NFR-09 守门通过</done>
</task>

<task type="auto">
  <name>Task 2: 激活 InputBar 回形针 + 粘贴 + chip UI + chat.ts vision 注入</name>
  <files>src/components/InputBar.tsx, src/store/chat.ts</files>

  <read_first>
  - src/components/InputBar.tsx（全文，重点 L18-27 import 区、L42 useRef 区、L94-99 handleKeyDown、L101-169 JSX return）
  - src/store/chat.ts（全文，重点 L171-176 sendMessage + L119-131 serializeForStorage）
  - src/providers/registry.ts（ProviderRegistry.resolve 导出路径；getDefaultLLM 导入来源）
  - src/store/agentStore.ts（getDefaultLLM 是否在此导出，供 chat.ts import）
  - src/styles.css（缩略图 chip 相关 CSS 变量 + 设计系统类名，确认没有现有 chip 样式）
  - .planning/phases/15-vis/15-PATTERNS.md §InputBar.tsx（激活范式 + handleFileSelect + handlePaste）
  - .planning/phases/15-vis/15-PATTERNS.md §chat.ts（sendMessage 扩展范式）
  - .planning/phases/15-vis/15-RESEARCH.md §问题 2（粘贴路径：clipboardData.items 同步 DataTransfer）
  - .planning/phases/15-vis/15-RESEARCH.md §问题 5（augmented prompt 注入格式）
  </read_first>

  <action>
  **Step A — src/components/InputBar.tsx 改造：**

  1. 在 import 区域追加：
     ```typescript
     import { useAttachmentStore } from '../store/attachments';
     import type { AttachedImage } from '../store/attachments';
     ```

  2. 在 textareaRef（L42）旁追加 ref：
     ```typescript
     const fileInputRef = useRef<HTMLInputElement>(null);
     ```

  3. 在 component function 内（handleKeyDown 之后）追加工具函数和处理函数：

     ```typescript
     // 图片 File → base64 + AttachedImage（去掉 data:前缀）
     const fileToBase64 = (file: File): Promise<string> =>
       new Promise((resolve, reject) => {
         const reader = new FileReader();
         reader.onload = () => resolve((reader.result as string).split(',')[1]);
         reader.onerror = reject;
         reader.readAsDataURL(file);
       });

     const MAX_IMAGE_SIZE = 5 * 1024 * 1024; // 5MB（RESEARCH §问题 2 推荐）

     const processImageFiles = async (files: File[]): Promise<void> => {
       const validMimes = new Set(['image/png', 'image/jpeg', 'image/webp']);
       const results: AttachedImage[] = [];
       for (const file of files) {
         // D-11/D-14 MIME 双重检查
         if (!validMimes.has(file.type)) {
           // 诚实提示（D-14）：非图片文件→告知 Phase 17 再支持
           alert(t`文件解析即将开放，当前可上传图片（png/jpg/webp）`);
           continue;
         }
         if (file.size > MAX_IMAGE_SIZE) {
           alert(t`图片过大，请选择 5MB 以下的图片`);
           continue;
         }
         const base64 = await fileToBase64(file);
         results.push({
           id: crypto.randomUUID(),
           base64,
           mimeType: file.type as 'image/png' | 'image/jpeg' | 'image/webp',
           fileName: file.name,
           sizeBytes: file.size,
         });
       }
       if (results.length > 0) {
         useAttachmentStore.getState().addImages(results);
       }
     };

     const handleFileSelect = (e: React.ChangeEvent<HTMLInputElement>): void => {
       const files = Array.from(e.target.files ?? []);
       void processImageFiles(files);
       e.target.value = ''; // 允许重复选同一文件
     };

     const handlePaste = (e: React.ClipboardEvent<HTMLTextAreaElement>): void => {
       const items = Array.from(e.clipboardData?.items ?? []);
       const imageItems = items.filter(i => i.kind === 'file' && i.type.startsWith('image/'));
       if (!imageItems.length) return;
       // 同步 DataTransfer API，不受 iframe Permissions Policy 限制（RESEARCH §问题 2）
       e.preventDefault(); // 防止文字粘贴路径
       const files = imageItems
         .map(i => i.getAsFile())
         .filter((f): f is File => f !== null);
       void processImageFiles(files);
     };
     ```

  4. 从 store 读取 images + removeImage（用于 chip 渲染）：
     ```typescript
     const attachedImages = useAttachmentStore((s) => s.images);
     const removeImage = useAttachmentStore((s) => s.removeImage);
     ```

  5. 修改 JSX，将 L144-153 禁用回形针替换为：
     ```tsx
     <button
       type="button"
       className="tool-btn"
       aria-label={t`上传图片`}
       title={t`上传图片`}
       onClick={() => fileInputRef.current?.click()}
     >
       <PaperclipIcon size={15} />
     </button>
     <input
       ref={fileInputRef}
       type="file"
       accept="image/png,image/jpeg,image/webp"
       multiple
       style={{ display: 'none' }}
       onChange={handleFileSelect}
     />
     ```

  6. 在 textarea 的 onKeyDown 之后追加 `onPaste={handlePaste}`（见 L113-123 区域）。

  7. 在 textarea 上方（或 selpill-row 之后、textarea 之前）追加缩略图 chip 区域：
     ```tsx
     {/* 上传图缩略图 chip 行（FILE-06 D-10 多轮复用 UI）*/}
     {attachedImages.length > 0 && (
       <div className="attachment-chips">
         {attachedImages.map((img) => (
           <div key={img.id} className="attachment-chip">
             <img
               src={`data:${img.mimeType};base64,${img.base64}`}
               alt={img.fileName}
               className="attachment-chip-thumb"
             />
             <span className="attachment-chip-name">{img.fileName}</span>
             <button
               type="button"
               className="attachment-chip-remove"
               aria-label={t`移除图片`}
               onClick={() => removeImage(img.id)}
             >
               ×
             </button>
           </div>
         ))}
       </div>
     )}
     ```

  8. 在 src/styles.css 末尾追加 chip 样式（遵循 teal 克制设计系统，CSS 变量驱动）：
     ```css
     /* Phase 15 图片附件 chip（FILE-06）*/
     .attachment-chips {
       display: flex;
       flex-wrap: wrap;
       gap: var(--sp-1, 4px);
       padding: var(--sp-1, 4px) var(--sp-2, 8px);
       border-bottom: 1px solid var(--border);
     }
     .attachment-chip {
       display: flex;
       align-items: center;
       gap: var(--sp-1, 4px);
       background: var(--surface-2);
       border-radius: 6px;
       padding: 2px 6px 2px 4px;
       font-size: 11px;
       color: var(--text-2);
       max-width: 120px;
     }
     .attachment-chip-thumb {
       width: 20px;
       height: 20px;
       object-fit: cover;
       border-radius: 3px;
       flex-shrink: 0;
     }
     .attachment-chip-name {
       overflow: hidden;
       text-overflow: ellipsis;
       white-space: nowrap;
       flex: 1;
     }
     .attachment-chip-remove {
       background: none;
       border: none;
       cursor: pointer;
       color: var(--text-3);
       padding: 0;
       line-height: 1;
       font-size: 13px;
       flex-shrink: 0;
     }
     .attachment-chip-remove:hover {
       color: var(--text);
     }
     ```

  **Step B — src/store/chat.ts sendMessage 扩展：**

  查找 sendMessage 实现（当前 L171-176），将其扩展为（注意保持 get() 调用 + pushMessage 的顺序）：

  ```typescript
  async sendMessage(prompt, selectionCtx, adapter) {
    // Phase 15 FILE-06：发消息前若有附件图，一次性调 vision，结果注入 augmented prompt
    const { images } = useAttachmentStore.getState();
    let finalPrompt = prompt;

    if (images.length > 0) {
      try {
        // ProviderRegistry.resolve('vision', getDefaultLLM) — 未配 key → throw KeyInvalidError
        // KeyInvalidError 被下方 catch 捕获 → finalPrompt 降级，不阻断发送
        const cfg = ProviderRegistry.resolve('vision', getDefaultLLM) as VisionConfig;
        const visionImages = images.map(({ base64, mimeType }) => ({ base64, mimeType }));
        const userText = `请分析以下图片内容，然后回答用户的问题：${prompt}`;
        const { content } = await new AihubmixVisionClient().analyzeImages(
          userText,
          visionImages,
          cfg,
        );
        // SUMMARY L67 范式：evidence 注入 prompt 头部，原 prompt 保留
        finalPrompt = `[图片分析 evidence]\n${content}\n---\n${prompt}`;
      } catch {
        // vision 失败（网络/key 未配）：诚实降级，不阻断发送（Pitfall 6 守则）
        finalPrompt = `[注：图片分析失败，将在无图情况下回答]\n${prompt}`;
      }
      // D-10 多轮复用：发消息后不自动清除图片，用户手动点 chip × 删除
    }

    // 用户气泡显示 original prompt（不含 evidence / base64，NFR-09 天然满足）
    get().pushMessage({ role: 'user', content: prompt, ts: Date.now() });
    // runAgent 拿到 finalPrompt（可能含 vision evidence）
    await useAgentStore.getState().runAgent(finalPrompt, selectionCtx, adapter);
  },
  ```

  在 chat.ts 顶部 import 区域追加（查看现有 imports 确认路径）：
  ```typescript
  import { useAttachmentStore } from './attachments';
  import { AihubmixVisionClient } from '../providers/aihubmix-vision';
  import type { VisionConfig } from '../providers/aihubmix-vision';
  import { ProviderRegistry } from '../providers/registry';
  ```

  注意：`getDefaultLLM` 来源——查看 chat.ts 现有 import，若已有 getDefaultLLM 直接用；否则查 agentStore.ts 导出路径。
  
  注意：若 registry.ts 中 resolve('vision', ...) 第二参数 `getDefaultLLM` 不可用，查 registry.ts 签名，调整为正确参数（如 `getProviders()`）。
  </action>

  <verify>
    <automated>npm test -- --run src/store/chat.test.ts src/store/attachments.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
  - grep "aria-disabled" src/components/InputBar.tsx 结果为 0（回形针按钮已激活）
  - grep "fileInputRef" src/components/InputBar.tsx 存在
  - grep "handlePaste" src/components/InputBar.tsx 存在
  - grep "accept=\"image/png" src/components/InputBar.tsx 存在（MIME 白名单）
  - grep "clipboardData" src/components/InputBar.tsx 存在（同步 DataTransfer API）
  - grep "navigator.clipboard" src/components/InputBar.tsx 的结果为 0（不用异步 clipboard API）
  - grep "attachment-chips" src/components/InputBar.tsx 存在（缩略图 chip 区域）
  - grep "useAttachmentStore" src/store/chat.ts 存在
  - grep "analyzeImages" src/store/chat.ts 存在（vision 调用注入）
  - grep "图片分析 evidence" src/store/chat.ts 存在（augmented prompt 格式）
  - grep "content: prompt" src/store/chat.ts 存在（pushMessage 用 original prompt）
  - grep "base64" src/store/chat.ts | grep "content:" 的结果为 0（base64 不进 message.content）
  - npm test -- --run src/store/chat.test.ts 全 PASS
  - npm test -- --run src/store/attachments.test.ts 全 PASS
  - npm run extract（Lingui 宏提取）不报错，新增中文字符串不触发 coverage.test.ts 失败
  - npx tsc --noEmit 不报新错误
  </acceptance_criteria>

  <done>回形针激活，file input + paste handler 工作，attachments store 已集成，chat.ts sendMessage 注入 vision evidence，npm test -- --run 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户浏览器 → InputBar → attachments store | base64 图片字节仅驻留内存 store，不写任何持久化存储 |
| attachments store → sendMessage → vision client | vision 调用在发消息前；用户气泡只含文本 prompt；base64 不进 message.content |
| sendMessage → runAgent | finalPrompt 含 vision 文本 evidence；主 LLM 不见 base64 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Information Disclosure | base64 进 message.content | mitigate | pushMessage 传 content: prompt（原始文本），不传 finalPrompt；acceptance_criteria: grep "base64" chat.ts content 行结果为 0 |
| T-15-09 | Tampering | 非图片文件伪装 MIME | mitigate | file input accept 属性白名单 + onChange 内 validMimes.has(file.type) 双重检查（RESEARCH §问题 2 §非图片文件拦截）|
| T-15-10 | Denial of Service | 大图撑爆内存/触发 vision quota | mitigate | MAX_IMAGE_SIZE = 5MB per image（RESEARCH §问题 2 建议）；超限 alert 提示拦截；多图上限隐性由用户控制（每次 processImageFiles 拦截单图） |
| T-15-11 | Information Disclosure | InputBar.tsx 图片 src= 含 base64 | accept | 缩略图 img src 含 base64 仅渲染在用户自己的 Task Pane 内，无网络传输；base64 不进 localStorage；DOM 仅存在于当前 session |
| T-15-12 | Elevation of Privilege | vision 结果 prompt injection（图片内嵌恶意指令）| mitigate | vision 结果走 augmented prompt evidence 块（[图片分析 evidence] 前缀标记），主 LLM 把它作为 evidence 引用而非执行；沿用 v2.0 sanitize 注入边界（sendMessage → runAgent → loop，无额外执行路径）|
| T-15-13 | Information Disclosure | apiKey 泄漏进 vision 失败 finalPrompt | mitigate | catch {} 块（不读 err）只追加固定字符串「[注：图片分析失败...]」，不拼接 err.message；AsterError 子类（KeyInvalidError）不含 apiKey 字面量 |
</threat_model>

<verification>
1. `npm test -- --run src/store/attachments.test.ts` — 5 个 NFR-09 守门用例全 PASS
2. `npm test -- --run src/store/chat.test.ts` — 现有白名单测试 + 新 sendMessage 用例全 PASS
3. `npx tsc --noEmit` — 无新类型错误
4. `npm run extract` — Lingui catalog 提取不报错
5. `npm test -- --run` — 全量测试全绿
6. 手工检查：`grep "aria-disabled" src/components/InputBar.tsx` 结果为 0（按钮已激活）
</verification>

<success_criteria>
- useAttachmentStore 存在，无 persist，NFR-09 守门通过
- InputBar 回形针从 aria-disabled 激活为可点击，链接 file input + onPaste
- MIME 双重检查（accept + onChange file.type）+ 大小上限 5MB
- 非图片文件返回「文件解析即将开放」（D-14 诚实）
- sendMessage 有附件时调 analyzeImages，augmented prompt 注入，用户气泡显示原始 prompt
- 缩略图 chip UI 展示，× 按钮删除
- npm test -- --run 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/15-vis/15-03-SUMMARY.md`，记录：
- attachments.ts 导出接口确认
- sendMessage 扩展的 import 路径（getDefaultLLM 来源）
- 任何 InputBar 实现偏差（如 chip CSS 类名调整）
- npm run extract 执行情况
</output>

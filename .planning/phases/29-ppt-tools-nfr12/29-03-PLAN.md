---
phase: 29-ppt-tools-nfr12
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - src/agent/tools/write/ppt.ts
  - src/agent/tools/index.ts
autonomous: true
requirements:
  - PPT-11
  - NFR-12

must_haves:
  truths:
    - "Agent 能给指定形状设渐变填充；因 Office.js ShapeFill 全平台无渐变写 API（HIGH 负面），降级为取渐变首色 setSolidColor + 明确告知「平台不支持渐变，已用纯色 #RRGGBB 代替」（D-29-02 诚实降级）"
    - "set_shape_gradient 复用既有 PptAdapter.setShapeProperty 的 fillColor 路径（before-image fill）→ reverse = restore_shape_property，可撤销（复用 Plan 01 接线，0 新 adapter 方法、0 新 inverse）"
    - "set_shape_gradient 工具名加入 PPT_TOOLS Set + 注册进 pptWriteTools 数组"
    - "降级告知文案精确量化首色十六进制值（不静默假装上了渐变 —— ROADMAP SC#4 诚实降级判据）"
    - "NFR-12 全里程碑收口：所有 v2.4 代码（Phase 26+27+28+29）就位后 npm run build && npm run size → dist/assets/main-*.js ≤100KB gzip（先 build 再 size）"
  artifacts:
    - path: "src/agent/tools/write/ppt.ts"
      provides: "setShapeGradientTool ToolDef（pickFirstStopColor + 复用 adapter.setShapeProperty + restore_shape_property reverse + ppt_shape_gradient postState + 纯色告知文案）"
      contains: "set_shape_gradient"
    - path: "src/agent/tools/index.ts"
      provides: "PPT_TOOLS Set +1 名 + pptWriteTools +1 ToolDef + import"
      contains: "set_shape_gradient"
  key_links:
    - from: "ppt.ts setShapeGradientTool.execute"
      to: "PptAdapter.setShapeProperty"
      via: "ctx.adapter as PptAdapter, { fillColor: firstColor }"
      pattern: "setShapeProperty"
    - from: "ppt.ts setShapeGradientTool reverse"
      to: "restore_shape_property"
      via: "ReverseDescriptor { tool: 'restore_shape_property', args: { ...beforeImage } }"
      pattern: "restore_shape_property"
    - from: "tools/index.ts dispatch"
      to: "normalizeToSnakeCase"
      via: "PPT_TOOLS.has('set_shape_gradient')"
      pattern: "set_shape_gradient"
---

<objective>
实现 Phase 29 末位工具 + 全里程碑 bundle 收口（Wave 3，依赖 Plan 02）：
- **PPT-11 `set_shape_gradient`**：给形状设渐变填充。**Office.js ShapeFill 全平台（含桌面）无渐变写 API**（HIGH 负面，RESEARCH 逐一核查 ShapeFill 类仅 setSolidColor/setImage/clear，含 preview moniker）→ D-29-02 降级纯色是**唯一路径**：取渐变 stops 首色，复用既有 `PptAdapter.setShapeProperty` 的 fillColor 路径 setSolidColor + **明确告知**「平台不支持渐变，已用纯色 #RRGGBB 代替」。undo = `restore_shape_property`（before-image fill，复用 Plan 01 接线，**0 新 adapter 方法**——方案 A：ToolDef 直接调既有 setShapeProperty，不写新 adapter 方法）。
- **NFR-12 全里程碑 bundle 收口**：所有 v2.4 功能代码（Phase 26 配置导入导出 + 27 Word + 28 Excel + 29 PPT 工具）就位后 `npm run build && npm run size`，断言 `dist/assets/main-*.js` ≤100KB gzip。

⚠️ **关键事实（采用更新后的，不照抄 CONTEXT 旧框架）：**
- **bundle gate = 100KB**（`.size-limit.json` `limit:"100 KB"` gzip + ROADMAP SC#5，2026-06-05 用户上调自 82KB）。**实测基线 82.47KB / 余 ~17.5KB，宽松**。CONTEXT 通篇「≤82KB / 余 0.7KB / 极紧」**全部过时**。三工具纯 Office.js 调用 0 净新增依赖，main 增量预估 ~1.5-2.5KB → 预期无需懒加载（收口任务仍须真 build+size 实测确认）。
- **PPT-11 唯一路径 = 降级纯色**（不存在「原生支持渐变的宿主」分支，全平台无 API，RESEARCH HIGH 负面）。纯色填充与现有 set_shape_property（fill_color）功能重叠，用户 D-29-02 已接受；set_shape_gradient 的差异化价值 = 接收 LLM 的渐变 stops + 方向参数，内部降级 + 量化告知，让 agent 不必知道平台限制就能调用。

Purpose: 兑现 v2.1 Phase 10 deferred 的 set_shape_fill_advanced（渐变）；收口 v2.4 全里程碑 bundle gate（末位 phase 承接 NFR-12）。
Output: ppt.ts（+1 ToolDef）、tools/index.ts（PPT_TOOLS +1 + 注册 +1 + import）+ build+size 收口验证
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ppt-tools-nfr12/29-CONTEXT.md
@.planning/phases/29-ppt-tools-nfr12/29-RESEARCH.md
@.planning/phases/29-ppt-tools-nfr12/29-PATTERNS.md
@.planning/phases/29-ppt-tools-nfr12/29-01-SUMMARY.md
@.planning/phases/29-ppt-tools-nfr12/29-02-SUMMARY.md

<interfaces>
<!-- 关键接口与 analog（已从 codebase 提取，执行者直接使用，无需搜索） -->

【既有 PptAdapter.setShapeProperty 签名（L889，方案 A 直接复用——不写新 adapter 方法）】：
```typescript
async setShapeProperty(
  slideIndex: number,
  shapeId: string,
  props: { fillColor?: string; lineColor?: string; lineWeight?: number; width?: number; height?: number },
  expected?: { fillColor?: unknown; lineColor?: unknown },
): Promise<{ beforeImage: {
  fillType: string; fillColor: string | null; lineColor: string | null;
  lineWeight: number | null; lineVisible: boolean; width: number; height: number;
} }>
```
（写前读 before-image fill + setSolidColor(props.fillColor)；返回 beforeImage 供 restore_shape_property reverse。set_shape_gradient 只需传 `{ fillColor: firstColor }`。）

【既有 setShapeProperty ToolDef 的 reverse 范式（ppt.ts L188-201，逐字复用 beforeImage → Record args）】：
```typescript
const reverse: ReverseDescriptor = {
  tool: 'restore_shape_property',
  args: {
    slide_index, shape_id,
    fill_type: beforeImage.fillType, fill_color: beforeImage.fillColor,
    line_color: beforeImage.lineColor, line_weight: beforeImage.lineWeight,
    line_visible: beforeImage.lineVisible, width: beforeImage.width, height: beforeImage.height,
  },
};
```

【tools/index.ts 接线点（Plan 02 已加 insert_ppt_table / add_line，本 plan 在其后追加 set_shape_gradient）】：
- L14 import：`... applySlideLayoutTool, insertPptTableTool, addLineTool } from './write/ppt';` → 加 `setShapeGradientTool`
- PPT_TOOLS Set：Plan 02 的 `'add_line',` 后加 `'set_shape_gradient',`
- pptWriteTools 数组：Plan 02 的 `addLineTool,` 后加 `setShapeGradientTool,`

【.size-limit.json（gate 真相源）】：
```json
[{ "name": "initial-js", "path": "dist/assets/main-*.js", "limit": "100 KB", "gzip": true }]
```
package.json：`build`=`lingui compile --typescript && vite build`、`size`=`size-limit`。**必须先 build 再 size**（陈旧 dist 假绿，memory `project_bundle_size_guard`）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setShapeGradientTool ToolDef（降级纯色 + 告知）+ 注册 + 守门保持绿</name>
  <files>src/agent/tools/write/ppt.ts, src/agent/tools/index.ts</files>
  <behavior>
    - set_shape_gradient 接收 gradient_stops（色标数组）+ 可选 direction → 取首色 → 调既有 adapter.setShapeProperty({ fillColor: firstColor }) → 返回 beforeImage
    - reverse = restore_shape_property（beforeImage 完整 Record args）→ 可撤销
    - data 含降级告知「平台不支持渐变，已用纯色 #RRGGBB 代替」（首色十六进制精确量化）
    - pickFirstStopColor：取 gradient_stops[0]（容错：字符串色值或 { color } 对象；空数组 → 报错或用默认主色 #009887，plan 决定）
  </behavior>
  <read_first>
    - src/agent/tools/write/ppt.ts（重点 L141-207 setShapeProperty ToolDef 全段——reverse beforeImage 范式 + humanLabel 中文模板；L84-94 notEffectiveResult）
    - src/agent/tools/index.ts（L14 import、L34-51 PPT_TOOLS Set、L320-331 pptWriteTools——Plan 02 已加 insert_ppt_table/add_line，本 plan 在其后追加）
    - src/adapters/PptAdapter.ts L889-1010（setShapeProperty 返回 { beforeImage } 签名）
    - .planning/phases/29-ppt-tools-nfr12/29-PATTERNS.md（§1c + §2 PPT-11 渐变 ToolDef + Pattern 2）
    - .planning/phases/29-ppt-tools-nfr12/29-RESEARCH.md（PPT-11 verdict 详解 + Pattern 2 渐变降级纯色 + before-image undo）
    - src/agent/operationLog.integration.test.ts（Plan 01 已加的 set_shape_gradient 守门用例——确认 toolName 字面量 + reverse=restore_shape_property 一致）
  </read_first>
  <action>
**改 src/agent/tools/write/ppt.ts（新增 setShapeGradientTool ToolDef + pickFirstStopColor helper）：**

在 ppt.ts 适当位置（建议 setShapeProperty ToolDef 之后，逻辑相邻）加：

```typescript
/** 取渐变 stops 首色（PPT-11 降级纯色取色逻辑）。容错字符串色值或 { color } 对象；空数组兜底主色。 */
function pickFirstStopColor(stops: unknown): string {
  const DEFAULT = '#009887'; // teal 主品牌色兜底
  if (Array.isArray(stops) && stops.length > 0) {
    const first = stops[0];
    if (typeof first === 'string') return first;
    if (first && typeof first === 'object' && typeof (first as { color?: unknown }).color === 'string') {
      return (first as { color: string }).color;
    }
  }
  return DEFAULT;
}

export const setShapeGradientTool: ToolDef = {
  name: 'set_shape_gradient',
  kind: 'write',
  description:
    '给 PPT 形状设渐变填充。平台不支持渐变写入，将自动降级为取首色的纯色填充并告知。slide_index 1-based，shape_id 来自 list_shapes_on_slide。',
  parameters: {
    type: 'object',
    properties: {
      slide_index: { type: 'number', description: '幻灯片编号（1开始）' },
      shape_id: { type: 'string', description: '形状 ID，来自 list_shapes_on_slide' },
      gradient_stops: { type: 'array', items: { type: 'string' }, description: '渐变色标（#RRGGBB 数组，首色用于纯色降级）' },
      direction: { type: 'string', description: '渐变方向（linear/radial 等，平台不支持时忽略）' },
    },
    required: ['slide_index', 'shape_id', 'gradient_stops'],
  },
  humanLabel: (args) => {
    const a = args as Record<string, unknown>;
    const firstColor = pickFirstStopColor(a.gradient_stops);
    return `将第 ${a.slide_index as number} 张幻灯片形状「${a.shape_id as string}」填充设为纯色 ${firstColor}（渐变降级）`;
  },
  async execute(args, ctx): Promise<ToolResult> {
    const a = args as Record<string, unknown>;
    const slide_index = a.slide_index as number;
    const shape_id = a.shape_id as string;
    const firstColor = pickFirstStopColor(a.gradient_stops);
    const { beforeImage } = await (ctx.adapter as PptAdapter).setShapeProperty(slide_index, shape_id, { fillColor: firstColor });
    const reverse: ReverseDescriptor = {
      tool: 'restore_shape_property',
      args: {
        slide_index, shape_id,
        fill_type: beforeImage.fillType, fill_color: beforeImage.fillColor,
        line_color: beforeImage.lineColor, line_weight: beforeImage.lineWeight,
        line_visible: beforeImage.lineVisible, width: beforeImage.width, height: beforeImage.height,
      },
    };
    const postState: PostStateSnapshot = { kind: 'ppt_shape_gradient', content: { slide_index, shape_id } };
    return {
      ok: true,
      data: { slide_index, shape_id, applied_color: firstColor, degraded: 'gradient_to_solid',
              notice: `平台不支持渐变填充，已用纯色 ${firstColor} 代替` },
      reverse, postState,
    };
  },
};
```
- humanLabel + notice 纯中文模板字符串（**非 Lingui 宏**，避开 `npm run extract`）。
- notice 精确量化首色（memory `precision_over_brevity`；ROADMAP SC#4 诚实降级判据——不静默假装上了渐变）。

**改 src/agent/tools/index.ts（3 处，在 Plan 02 接线之后追加）：**
1. L14 import 末尾加 `setShapeGradientTool`。
2. PPT_TOOLS Set：Plan 02 的 `'add_line',` 后加 `'set_shape_gradient', // Phase 29 PPT-11`。
3. pptWriteTools 数组：Plan 02 的 `addLineTool,` 后加 `setShapeGradientTool,`。
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "name: 'set_shape_gradient'" src/agent/tools/write/ppt.ts && grep -c "'set_shape_gradient'" src/agent/tools/index.ts && grep -c "setShapeGradientTool" src/agent/tools/index.ts && grep -c "pickFirstStopColor" src/agent/tools/write/ppt.ts && npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name: 'set_shape_gradient'" src/agent/tools/write/ppt.ts` 输出 `1`
    - `grep -c "'set_shape_gradient'" src/agent/tools/index.ts` 输出 `>= 2`（PPT_TOOLS Set + import 或注册）
    - `grep -c "setShapeGradientTool" src/agent/tools/index.ts` 输出 `>= 2`（import + pptWriteTools 注册）
    - `grep -c "pickFirstStopColor" src/agent/tools/write/ppt.ts` 输出 `>= 2`（定义 + execute/humanLabel 调用）
    - setShapeGradientTool.execute 复用 `(ctx.adapter as PptAdapter).setShapeProperty(...)`（grep `setShapeProperty` 在 ppt.ts ≥ 2 处：既有 ToolDef + gradient 复用）
    - reverse 为 `{ tool: 'restore_shape_property', ... }`，postState.kind 为 'ppt_shape_gradient'
    - data 含 `notice` 字段，文案含「平台不支持渐变」+ 首色十六进制（如 `#009887`）
    - **无新 adapter 方法**（方案 A：PptAdapter.ts 不改，git diff 不含 PptAdapter.ts）
    - `npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts` 全套绿（Plan 01 的 set_shape_gradient 守门用例仍 rolled_back）
  </acceptance_criteria>
  <done>ppt.ts 含 setShapeGradientTool（降级纯色 + 量化告知 + restore_shape_property undo，复用既有 setShapeProperty，0 新 adapter 方法）；tools/index.ts 三处接线；全套测试绿</done>
</task>

<task type="auto">
  <name>Task 2: NFR-12 全里程碑 bundle 收口 — 先 build 再 size，断言 main-*.js ≤100KB gzip</name>
  <files>(no source files — runs npm run build && npm run size; reads .size-limit.json / package.json)</files>
  <read_first>
    - .size-limit.json（gate 配置：dist/assets/main-*.js ≤100KB gzip）
    - package.json（build = lingui compile + vite build；size = size-limit）
    - .planning/phases/29-ppt-tools-nfr12/29-RESEARCH.md（§NFR-12 bundle 收口章节——实测基线 82.47KB / 余 17.53KB / 新代码落点分析）
    - .planning/phases/29-ppt-tools-nfr12/29-PATTERNS.md（§NFR-12 bundle 收口）
  </read_first>
  <action>
全 v2.4 功能代码就位（Phase 26 配置导入导出 + 27 Word + 28 Excel + 29 PPT 三工具——Plan 02/03 Task 1 完成后）后，执行 bundle 收口验证：

1. **先 build**（陈旧 dist 给假绿，memory `project_bundle_size_guard`）：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
npm run build
```
2. **再 size**：
```bash
npm run size
```
3. 读 size-limit 输出，确认 `dist/assets/main-*.js` 的 gzip 值 < 100 KB（实测基线 82.47KB，三工具增量 ~1.5-2.5KB → 预期 ~84-85KB，PASS）。

**若意外超 100KB（不预期）：** 优先把非热路径懒加载——配置导入导出的 JSON 校验/序列化/文件下载逻辑、或任何重模块。PPT 三工具本身 0 净新增依赖（纯 Office.js），adapter 经 createAdapter 已懒加载；若是 ppt.ts ToolDef 增量超标，可把 execute 主体委托给懒加载 adapter（本就是此结构）。**质量 >> 包体积**（memory `project_quality_over_cost`），但 gate 仍硬守——超标先懒加载，不抠工具完整度。

**记录实测值**到 SUMMARY（main-*.js 文件名 + gzip 数字 + 余量）。
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && npm run build && npm run size
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` 退出 0（lingui compile + vite build 成功，dist/assets/main-*.js 重新生成）
    - `npm run size` 退出 0（size-limit 报 PASS——main-*.js gzip < 100 KB）
    - size-limit 输出含 `dist/assets/main-*.js`（或等价 initial-js 条目）且 gzip 值 < 100 KB
    - 全流程顺序为先 build 后 size（不依赖陈旧 dist）
    - SUMMARY 记录实测 main-*.js gzip 数字 + 与 100KB gate 的余量
  </acceptance_criteria>
  <done>npm run build 后 npm run size 通过，dist/assets/main-*.js gzip ≤100KB（全里程碑 v2.4 代码就位的累积值）；实测值已记录</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_call → setShapeGradientTool.execute | gradient_stops/shape_id/slide_index 为不可信输入，经 pickFirstStopColor 容错 + adapter bounds check + 宿主 setSolidColor 色值校验 |
| ToolDef → 既有 PptAdapter.setShapeProperty 闭包 | 复用已验证的 before-image + setSolidColor + HostApiError 范式，无新宿主 API 路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-1 | Repudiation（虚假能力声明） | 渐变降级静默假装上了渐变 | mitigate | data.notice 量化告知「平台不支持渐变填充，已用纯色 #RRGGBB 代替」（ROADMAP SC#4 诚实降级判据），不静默假成功 |
| T-29-03-2 | Tampering / DoS | 越界 slide_index / 不存在 shape_id | mitigate | 复用 setShapeProperty 既有 bounds check + 形状定位失败 → HostApiError NOT_FOUND（PptAdapter:919 范式） |
| T-29-03-3 | Information Disclosure | 错误链泄漏 err.message | mitigate | 复用 setShapeProperty 既有 HostApiError 字面量包装（不 interpolate err.message，T-16-05） |
| T-29-03-4 | Tampering（输入崩溃） | gradient_stops 空数组 / 非法元素 | mitigate | pickFirstStopColor 容错（字符串/对象/空数组兜底 #009887），不抛未捕获异常 |
| T-29-03-5 | DoS（bundle 膨胀） | v2.4 累积代码超 100KB gate | accept/mitigate | NFR-12 收口先 build 再 size 实测；超标则非热路径懒加载（重模块仍懒加载纪律），gate 硬守 |
</threat_model>

<verification>
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. 类型 + 单元/集成测试
npx tsc --noEmit
npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts
# 2. set_shape_gradient 接线 + 复用既有 adapter（无新 adapter 方法）
grep -c "set_shape_gradient" src/agent/tools/index.ts
git diff --name-only | grep -c PptAdapter.ts   # 期望 0（方案 A 不改 adapter）
# 3. NFR-12 收口：先 build 再 size
npm run build && npm run size   # main-*.js gzip ≤100KB
```
真机 UAT 由 phase gate 统一跑（U-3 渐变降级纯色 + 告知 / U-4 降级诚实性 / U-5 bundle）。set_shape_gradient 降级纯色 + 告知是确定性行为（无 API 风险，纯色路径已 GA），真机主要验告知文案展示 + undo 还原。
</verification>

<success_criteria>
- ppt.ts 含 setShapeGradientTool（pickFirstStopColor 首色降级 + 复用既有 setShapeProperty + restore_shape_property reverse + ppt_shape_gradient postState + 量化纯色告知），**0 新 adapter 方法**（方案 A）
- tools/index.ts：PPT_TOOLS Set 含 set_shape_gradient、pptWriteTools 注册、import 齐全
- 全套 `npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts` 绿（Plan 01 set_shape_gradient 守门 rolled_back）
- 降级告知诚实（notice 含「平台不支持渐变」+ 首色十六进制，不静默假成功——ROADMAP SC#4）
- **NFR-12 收口：先 build 再 size，dist/assets/main-*.js gzip ≤100KB**（全里程碑 v2.4 累积值，实测记录）
</success_criteria>

<output>
完成后创建 `.planning/phases/29-ppt-tools-nfr12/29-03-SUMMARY.md`，记录：
- ppt.ts setShapeGradientTool（pickFirstStopColor 取色逻辑、复用 setShapeProperty、reverse=restore_shape_property、notice 告知文案、确认 0 新 adapter 方法）
- tools/index.ts 三处接线（PPT_TOOLS +1、pptWriteTools +1、import）
- 守门状态（Plan 01 set_shape_gradient 用例仍 rolled_back）
- **NFR-12 收口实测：main-*.js 文件名 + gzip 数字 + 与 100KB gate 余量（先 build 再 size）**
- ⚠️ 真机 UAT 待验项：渐变降级纯色 + 告知文案展示（U-3）；降级诚实性三工具（U-4）；本机 bundle gate（U-5）
- v2.4 全里程碑 3 工具诚实降级总结：PPT-09 原生表格（真机若证伪→网格 follow-up）/ PPT-10 线条可插+箭头诚实告知 / PPT-11 渐变降级纯色+告知（允许部分诚实降级为成功，ROADMAP SC#4）
</output>

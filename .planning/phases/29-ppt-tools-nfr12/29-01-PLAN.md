---
phase: 29-ppt-tools-nfr12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/operationLog.ts
  - src/agent/contract.test.ts
  - src/agent/operationLog.integration.test.ts
autonomous: true
requirements:
  - PPT-09
  - PPT-10
  - PPT-11

must_haves:
  truths:
    - "operationLog.ts 的 PostStateSnapshot.kind union 新增 'ppt_table' / 'ppt_line' / 'ppt_shape_gradient' 三个 kind（readTargetState 走保守 default → undefined，不加 case）"
    - "operationLog.ts 不新增 DocumentAdapterForReplay 接口方法、不新增 executeReverse case（三工具复用既有 deleteShapeById / restoreShapeProperty 已声明 + 已有 case 'delete_shape_by_id'(L477) / case 'restore_shape_property'(L371)）"
    - "contract.test.ts 的 CONTRACT[] 新增 3 个 Phase 29 行（integrationTest: true），PhaseNum 联合类型含 29，长度断言上调到 ≥35"
    - "operationLog.integration.test.ts 新增 3 个守门用例，3 个 toolName 字面量（'insert_ppt_table' / 'add_line' / 'set_shape_gradient'）逐字出现在文件内（D-17 fs.readFileSync 硬卡）"
    - "三个守门用例在 Wave 0 即 rolled_back（reverse 工具 delete_shape_by_id / restore_shape_property + adapter 方法均已存在，与 Phase 28 不同——无需等后续 wave 变绿）"
    - "mockPpt() 工厂扩展：shapes 加 addTable / addLine vi.fn；reverse 目标 shape（new-shape-uuid）补 fill(setSolidColor/clear) + lineFormat(color/weight/visible) 供 restoreShapeProperty 定位"
  artifacts:
    - path: "src/agent/operationLog.ts"
      provides: "Phase 29 骨架：3 个新 kind（接口/case 0 新增——全复用）"
      contains: "ppt_table"
    - path: "src/agent/contract.test.ts"
      provides: "Phase 29 CONTRACT 3 行 + PhaseNum 加 29 + 长度断言 ≥35 + D-17 守门"
      contains: "insert_ppt_table"
    - path: "src/agent/operationLog.integration.test.ts"
      provides: "3 个守门用例 + 扩展 mockPpt"
      contains: "set_shape_gradient"
  key_links:
    - from: "contract.test.ts CONTRACT[]"
      to: "operationLog.integration.test.ts"
      via: "fs.readFileSync D-17 — 3 个 toolName 字面量必出现"
      pattern: "insert_ppt_table"
    - from: "operationLog.ts executeReverse"
      to: "adapter.deleteShapeById"
      via: "复用既有 case 'delete_shape_by_id'（L477）"
      pattern: "case 'delete_shape_by_id'"
    - from: "operationLog.ts executeReverse"
      to: "adapter.restoreShapeProperty"
      via: "复用既有 case 'restore_shape_property'（L371）"
      pattern: "case 'restore_shape_property'"
---

<objective>
完成 Phase 29 的**合约骨架接线（Wave 0）**：在 operationLog.ts 注册 3 个新 PostStateSnapshot kind（`ppt_table` / `ppt_line` / `ppt_shape_gradient`，readTargetState 保守 default → undefined）；在 contract.test.ts 追加 3 条 Phase 29 CONTRACT 行（**integrationTest: true**——与 Phase 28 不同，本 phase 三工具的 reverse 工具与 adapter 方法均已存在，守门用例 Wave 0 即 rolled_back）；在 operationLog.integration.test.ts 扩展 mockPpt 工厂并追加 3 个守门用例（每工具一条正向 rolled_back）。

⚠️ **本 phase 与 Phase 28 的关键差异（不要照抄 Phase 28 的 skipped_error 模式）：**
- 三工具 undo 全部**复用既有反操作**：`insert_ppt_table` + `add_line` → `delete_shape_by_id`（已有 case L477 + 接口 `deleteShapeById` 已声明 + PptAdapter.deleteShapeById 已实现）；`set_shape_gradient`（降级纯色）→ `restore_shape_property`（已有 case L371 + 接口 `restoreShapeProperty` 已声明 + PptAdapter.restoreShapeProperty 已实现 L1033）。
- 因此 operationLog.ts **0 个新接口方法、0 个新 executeReverse case**——只加 3 个 kind union 成员。
- 因此 3 个守门用例在 **Wave 0 即 rolled_back**（不像 Phase 28 的 merge/pivot 要等 adapter 实现才变绿）。本 plan 自洽全绿。

Purpose: 合约骨架先行——D-17 硬门（fs.readFileSync 断言 3 个 toolName 字面量）、CONTRACT 行、kind union、mockPpt 扩展全部到位，后续 Wave 1（insert_ppt_table + add_line 的 ToolDef + adapter）和 Wave 2（set_shape_gradient ToolDef + NFR-12 收口）只需补 ToolDef/adapter 正向方法，反操作侧零接线。

Output: operationLog.ts（+3 kind）、contract.test.ts（+3 行 + PhaseNum +29 + 长度断言 ≥35）、operationLog.integration.test.ts（扩展 mockPpt + 3 个守门用例）

**Wave 0 验收 gate：** `npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts` 全绿（3 个新用例 rolled_back，D-17 不挂）。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ppt-tools-nfr12/29-CONTEXT.md
@.planning/phases/29-ppt-tools-nfr12/29-RESEARCH.md
@.planning/phases/29-ppt-tools-nfr12/29-PATTERNS.md
@.planning/phases/29-ppt-tools-nfr12/29-VALIDATION.md

<interfaces>
<!-- 关键接口与 analog（已从 codebase 提取，执行者直接使用，无需搜索） -->

【operationLog.ts kind union 末尾（L34-56，Phase 28 段在 `| 'batch'` 之前）当前状态】：
```typescript
    // Phase 28 新增：Excel 工具补全 2 个 kind（readTargetState 走保守 default → undefined）
    | 'excel_merge'   // merge_cells 快照式（unmerge + values 覆写 undo）
    | 'excel_pivot'   // create_pivot_table 简单逆向（delete undo）
    // remove_duplicates 复用现有 'excel_snapshot'，无需新增
    // Phase 11 新增：batch 整体快照 kind
    | 'batch';
```
Phase 29 在 `// Phase 11 新增：batch 整体快照 kind` 行之前插入：
```typescript
    // Phase 29 新增：PPT 工具补全 3 个 kind（readTargetState 走保守 default → undefined，不加 case）
    | 'ppt_table'           // insert_ppt_table 简单逆向（新建单 shape，delete_shape_by_id undo）
    | 'ppt_line'            // add_line 简单逆向（新建单 shape，delete_shape_by_id undo）
    | 'ppt_shape_gradient'  // set_shape_gradient 降级纯色（before-image fill，restore_shape_property undo）
```

【operationLog.ts 接口/case 现状——确认无需改动】：
- `DocumentAdapterForReplay.deleteShapeById?`（L167）已声明；`restoreShapeProperty?`（L122 附近）已声明 → **不加新接口方法**。
- `executeReverse` 已有 `case 'delete_shape_by_id'`（L477-480，调 adapter.deleteShapeById）+ `case 'restore_shape_property'`（L371-376，调 adapter.restoreShapeProperty） → **不加新 case**。

【contract.test.ts 现状】：
```typescript
// L18 当前：
type PhaseNum = 9 | 10 | 11 | 23 | 27 | 28;   // 改为 → | 29
// L154 当前长度断言：
expect(CONTRACT.length).toBeGreaterThanOrEqual(24);   // 上调为 35（现有 32 行 + 3 = 35）
// CONTRACT 数组末尾（Phase 28 三行 L70-72 之后，`];` 前）追加 3 行（见 action）
```

【contract.test.ts D-17 硬卡逻辑（L127-150）】：只扫描 `integrationTest === true` 的工具，断言其 toolName 字面量出现在 operationLog.integration.test.ts。本 phase 3 行设 `integrationTest: true` → 守门用例必须就位（本 plan Task 2 完成）。

【integration.test.ts mockPpt 工厂现状（L176-259）】：
- `makeShape(id, text, extraProps?)`（L185）返回带 `id`/`type:'TextBox'`/`rotation`/`textFrame`/`delete`/`load` 的 shape；**无 `fill`/`lineFormat`**。
- 已注册 shape：`shape-01`（shapeMain）/ `new-shape-uuid`（shapeNew，供 deleteShapeById 定位，add_shape 测试复用）/ `shape-03`（shapeRotate）。
- `slide.shapes`（L237-249）有 `load`/`items`/`addTextBox`/`addGeometricShape`，**无 `addTable`/`addLine`**。
- `addGeometricShape` 返回对象含 `fill: { setSolidColor: vi.fn() }`, `lineFormat: { color, weight, visible }`（L247）——这是 fill/lineFormat mock 的范式。

【add_shape 守门用例 analog（L1245-1260，rolled_back 范式）】：
```typescript
it('D-17: add_shape → delete_shape_by_id → rolled_back', async () => {
  mockPpt('');
  const adapter = new PptAdapter();
  const entry: OperationLogEntry = {
    runId: 'r10', stepIndex: 12,
    toolName: 'add_shape',
    args: { slideIndex: 1, shapeType: 'TextBox', position: { left: 100, top: 100, width: 200, height: 50 }, text: '季度总结' },
    humanLabel: '在第 1 页插入文本框「季度总结」',
    reverse: { tool: 'delete_shape_by_id', args: { slide_index: 1, shape_id: 'new-shape-uuid' } },
    postState: { kind: 'ppt_shape_new', content: { slideIndex: 1, shapeId: 'new-shape-uuid' } },
    timestamp: 0,
  };
  const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
  expect(detail.status).toBe('rolled_back');
});
```
关键：守门用例只 replay **reverse**（不跑正向 execute）。`delete_shape_by_id` 的 reverse.args.shape_id 指向 mockPpt 中**已存在**的 `new-shape-uuid` shape → deleteShapeById 定位成功 → rolled_back。同理 set_shape_gradient 的 `restore_shape_property` reverse 需目标 shape 在 mockPpt 内存在且带 fill/lineFormat（见 Task 2 mockPpt 扩展）。

【restoreShapeProperty adapter 所需 shape 形状（PptAdapter.ts L1064-1107）】：定位 shape 后访问 `shape.fill.setSolidColor / shape.fill.clear`、`shape.lineFormat.color / weight / visible`、`shape.width / height`。mockPpt 的 reverse 目标 shape 须补这些字段。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 operationLog.ts — 3 个新 PostStateSnapshot kind（接口/case 0 新增）</name>
  <files>src/agent/operationLog.ts</files>
  <read_first>
    - src/agent/operationLog.ts（全文，重点 L34-56 PostStateSnapshot.kind union；确认 L167 deleteShapeById 接口声明已存在、L477 case 'delete_shape_by_id' 已存在、L371 case 'restore_shape_property' 已存在、L122 restoreShapeProperty 接口声明已存在）
    - .planning/phases/29-ppt-tools-nfr12/29-PATTERNS.md（§4 operationLog.ts — kind union + 保守 read 段）
    - .planning/phases/29-ppt-tools-nfr12/29-RESEARCH.md（Code Examples 合约接线四处对齐——确认「executeReverse 大概率 0 新 case」）
  </read_first>
  <action>
在 src/agent/operationLog.ts 做**单处**精确修改（kind union），不动接口、不动 switch、不动 readTargetState：

**修改：PostStateSnapshot.kind union（在 `// Phase 11 新增：batch 整体快照 kind` 注释行之前，`// remove_duplicates 复用现有 'excel_snapshot'，无需新增` 注释行之后插入）**
```typescript
    // Phase 29 新增：PPT 工具补全 3 个 kind（readTargetState 走保守 default → undefined，不加 case）
    | 'ppt_table'           // insert_ppt_table 简单逆向（新建单 shape，delete_shape_by_id undo）
    | 'ppt_line'            // add_line 简单逆向（新建单 shape，delete_shape_by_id undo）
    | 'ppt_shape_gradient'  // set_shape_gradient 降级纯色（before-image fill，restore_shape_property undo）
```

**严禁做的事（D-29-05/06 硬约束）：**
- 不新增 `DocumentAdapterForReplay` 接口方法——`deleteShapeById`（L167）、`restoreShapeProperty`（L122）均已声明。
- 不新增 `executeReverse` case——`case 'delete_shape_by_id'`（L477）、`case 'restore_shape_property'`（L371）均已存在，三工具复用。
- 不在 `readTargetState` / `isTargetStateConsistent` 为 3 个新 kind 加 case——走 default 返回 undefined（被 isTargetStateConsistent 的 `if (current === undefined) return true` 保守视为一致 → 不误判手改，memory `project_adapter_inverse_signature`）。
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "ppt_table" src/agent/operationLog.ts && grep -c "ppt_line" src/agent/operationLog.ts && grep -c "ppt_shape_gradient" src/agent/operationLog.ts && grep -c "case 'delete_shape_by_id'" src/agent/operationLog.ts && grep -c "case 'restore_shape_property'" src/agent/operationLog.ts && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ppt_table" src/agent/operationLog.ts` 输出 `1`（kind union 成员，含行内注释只算 1 行）
    - `grep -c "ppt_line" src/agent/operationLog.ts` 输出 `1`
    - `grep -c "ppt_shape_gradient" src/agent/operationLog.ts` 输出 `1`
    - `grep -c "case 'delete_shape_by_id'" src/agent/operationLog.ts` 输出 `1`（既有，未新增）
    - `grep -c "case 'restore_shape_property'" src/agent/operationLog.ts` 输出 `1`（既有，未新增）
    - `npx tsc --noEmit` 退出 0
    - readTargetState 中**未**出现 `case 'ppt_table'` / `case 'ppt_line'` / `case 'ppt_shape_gradient'`（保守路径）
    - DocumentAdapterForReplay 接口**未**新增方法（git diff 仅 kind union 一处变更）
  </acceptance_criteria>
  <done>operationLog.ts 仅新增 3 个 PostStateSnapshot kind；接口/executeReverse/readTargetState 零改动；TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: contract.test.ts 加 3 行 + PhaseNum +29 + 长度断言 ≥35；扩展 mockPpt + 3 个守门用例（D-17）</name>
  <files>src/agent/contract.test.ts, src/agent/operationLog.integration.test.ts</files>
  <read_first>
    - src/agent/contract.test.ts（全文，重点 L18 PhaseNum、L33-73 CONTRACT 数组末尾、L127-150 D-17 fs.readFileSync 守门、L152-155 长度断言）
    - src/agent/operationLog.integration.test.ts（L176-259 mockPpt 工厂、L247 addGeometricShape 返回的 fill/lineFormat 范式、L1245-1260 add_shape→delete_shape_by_id rolled_back 用例、L1730 Phase 28 守门 describe 块格式）
    - .planning/phases/29-ppt-tools-nfr12/29-PATTERNS.md（§5 contract.test.ts 四处对齐、§6 integration.test 守门用例 + mock 工厂复用）
    - PptAdapter.ts L1033-1107（restoreShapeProperty 定位 shape 后访问 fill/lineFormat/width/height——决定 mockPpt 目标 shape 需补哪些字段）
  </read_first>
  <action>
**改 contract.test.ts（3 处）：**

1. L18 PhaseNum 类型追加 `| 29`：
```typescript
type PhaseNum = 9 | 10 | 11 | 23 | 27 | 28 | 29;
```

2. CONTRACT[] 末尾（Phase 28 三行 L70-72 之后，`];` 前）追加 3 行。**integrationTest: true**（reverse 工具 + adapter 方法均已存在，守门用例 Wave 0 即 rolled_back）：
```typescript
  // ─── Phase 29 PPT 工具补全 ───
  { toolName: 'insert_ppt_table', host: 'ppt', undoType: '简单逆向', reverseTool: 'delete_shape_by_id', phase: 29, integrationTest: true },
  { toolName: 'add_line', host: 'ppt', undoType: '简单逆向', reverseTool: 'delete_shape_by_id', phase: 29, integrationTest: true },
  { toolName: 'set_shape_gradient', host: 'ppt', undoType: '简单逆向', reverseTool: 'restore_shape_property', phase: 29, integrationTest: true },
```
注意：工具名 `insert_ppt_table`（**不撞** Word 既有 `insert_table` L39，host 隔离 + 不撞名硬要求，RESEARCH/PATTERNS 锚定名）。

3. L154 长度断言上调到 35（现有 32 行 + 3 = 35，收紧守门，VALIDATION Wave 0 建议）。同步更新该 it 描述字符串里的数字说明：
```typescript
  it('CONTRACT 数组长度 ≥ 35（Phase 9/10/11/23 共 25 行 + Phase 27 四行 + Phase 28 三行 + Phase 29 三行 = 35）', () => {
    expect(CONTRACT.length).toBeGreaterThanOrEqual(35);
  });
```

**改 src/agent/operationLog.integration.test.ts（2 处）：**

1. 扩展 mockPpt() 工厂（L176-259）：
   - **a.** 在 `slide.shapes` 对象内（L249 `addGeometricShape: vi.fn(...)` 之后，闭合 `}` 前）追加 `addTable` / `addLine` mock（正向 execute 不在守门用例触发，加上供完整性 + 防 Wave 1 引用报错）：
```typescript
          addTable: vi.fn(() => ({ load: vi.fn(), id: 'new-table-id', type: 'Table' })),
          addLine: vi.fn(() => ({ load: vi.fn(), id: 'new-line-id', type: 'Line' })),
```
   - **b.** 给 reverse 目标 shape 补 `fill` + `lineFormat`（restoreShapeProperty 定位后需访问）。在 `makeShape`（L185）的返回对象内 `delete: shapeDelete` 之前追加（用 extraProps 默认值不足，直接补到 makeShape body）：
```typescript
    fill: { setSolidColor: vi.fn(), clear: vi.fn() },          // 供 restoreShapeProperty（set_shape_gradient undo）
    lineFormat: { color: '#000000', weight: 1, visible: true },// 供 restoreShapeProperty line 还原
    width: 200 as number,                                       // 供 restoreShapeProperty 几何还原
    height: 100 as number,
```
（注：makeShape 同时给 shape-01/new-shape-uuid/shape-03 加这些字段，set_shape_gradient 守门用例 reverse 目标 = `shape-01`。）

2. 文件末尾追加一个新 describe 块（Phase 29 守门，镜像 add_shape rolled_back 范式，**3 个 toolName 字面量逐字出现**满足 D-17）：
```typescript
// ---------------------------------------------------------------------------
// Phase 29 PPT 工具补全 inverse replay 守门（D-17 / D-29-05）
//   三工具 undo 全复用既有反操作 → Wave 0 即 rolled_back（与 Phase 28 skipped_error 不同）
//   reverse 目标 shape 均为 mockPpt 已注册 shape（new-shape-uuid / shape-01）
// ---------------------------------------------------------------------------
describe('集成：Phase 29 PPT 工具补全 inverse replay 守门', () => {
  afterEach(() => {
    delete (global as unknown as Record<string, unknown>).PowerPoint;
    delete (global as unknown as Record<string, unknown>).Office;
  });

  it('D-29: insert_ppt_table → delete_shape_by_id → rolled_back', async () => {
    mockPpt('');
    mockOfficeSupportsAll();
    const adapter = new PptAdapter();
    const entry: OperationLogEntry = {
      runId: 'r29', stepIndex: 0,
      toolName: 'insert_ppt_table',
      args: { slideIndex: 1, rows: 3, cols: 4 },
      humanLabel: '在第 1 张幻灯片插入 3×4 表格',
      reverse: { tool: 'delete_shape_by_id', args: { slide_index: 1, shape_id: 'new-shape-uuid' } },
      postState: { kind: 'ppt_table', content: { slide_index: 1, shape_id: 'new-shape-uuid' } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('rolled_back');
  });

  it('D-29: add_line → delete_shape_by_id → rolled_back', async () => {
    mockPpt('');
    mockOfficeSupportsAll();
    const adapter = new PptAdapter();
    const entry: OperationLogEntry = {
      runId: 'r29', stepIndex: 1,
      toolName: 'add_line',
      args: { slideIndex: 1, connector_type: 'Straight', start: { left: 50, top: 50 }, end: { left: 200, top: 50 } },
      humanLabel: '在第 1 张幻灯片插入直线连接符',
      reverse: { tool: 'delete_shape_by_id', args: { slide_index: 1, shape_id: 'new-shape-uuid' } },
      postState: { kind: 'ppt_line', content: { slide_index: 1, shape_id: 'new-shape-uuid' } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('rolled_back');
  });

  it('D-29: set_shape_gradient（降级纯色）→ restore_shape_property → rolled_back', async () => {
    mockPpt('');
    mockOfficeSupportsAll();
    const adapter = new PptAdapter();
    const entry: OperationLogEntry = {
      runId: 'r29', stepIndex: 2,
      toolName: 'set_shape_gradient',
      args: { slideIndex: 1, shapeId: 'shape-01', gradient_stops: ['#009887', '#0E0E10'] },
      humanLabel: '将第 1 张幻灯片形状「shape-01」设为渐变（降级纯色 #009887）',
      reverse: {
        tool: 'restore_shape_property',
        args: { slide_index: 1, shape_id: 'shape-01', fill_type: 'Solid', fill_color: '#FFFFFF',
                line_color: '#000000', line_weight: 1, line_visible: true, width: 200, height: 100 },
      },
      postState: { kind: 'ppt_shape_gradient', content: { slide_index: 1, shape_id: 'shape-01' } },
      timestamp: 0,
    };
    const detail = await replayUndoSingle(entry, adapter as unknown as DocumentAdapterForReplay);
    expect(detail.status).toBe('rolled_back');
  });
});
```
（若文件顶部尚未 import `afterEach`，沿用现有 import；Phase 28 describe 块已用 mockOfficeSupportsAll，确认 import 齐全。）
  </action>
  <verify>
    <automated>
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH" && grep -c "phase: 29" src/agent/contract.test.ts && grep -c "insert_ppt_table" src/agent/operationLog.integration.test.ts && grep -c "add_line" src/agent/operationLog.integration.test.ts && grep -c "set_shape_gradient" src/agent/operationLog.integration.test.ts && npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phase: 29" src/agent/contract.test.ts` 输出 `3`
    - `grep -c "insert_ppt_table" src/agent/operationLog.integration.test.ts` 输出 `>= 1`（D-17 toolName 字面量）
    - `grep -c "add_line" src/agent/operationLog.integration.test.ts` 输出 `>= 1`
    - `grep -c "set_shape_gradient" src/agent/operationLog.integration.test.ts` 输出 `>= 1`
    - `npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts` 全套通过（无 "N failed"；尾部 retry errors 是噪音，memory `i18n_extract_and_test_noise`）
    - contract.test.ts D-17 守门通过（3 个 integrationTest:true 工具的 toolName 字面量均在 integration.test.ts 找到）
    - 3 个 Phase 29 守门用例状态均为 `rolled_back`（reverse 工具 + adapter 方法 + mockPpt 目标 shape 均就位）
    - PhaseNum 含 29（tsc 编译通过）；长度断言 `toBeGreaterThanOrEqual(35)` 通过（CONTRACT 共 35 行）
    - mockPpt 含 `addTable` / `addLine` vi.fn，且 makeShape 返回的 shape 含 `fill.setSolidColor` / `fill.clear` / `lineFormat` / `width` / `height`
  </acceptance_criteria>
  <done>contract.test.ts 含 3 条 Phase 29 行（integrationTest:true）+ PhaseNum +29 + 长度断言 ≥35；integration.test.ts 含 3 个 toolName 字面量 + 3 个 rolled_back 守门用例 + 扩展后 mockPpt（addTable/addLine + fill/lineFormat）。Wave 0 验收 gate = 两测试文件全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 生产代码接口 | contract.test.ts 通过 fs.readFileSync 读 integration.test.ts，确保 D-17 守门覆盖（无用户数据流） |
| replay engine → adapter 方法 | executeReverse switch 路由到既有 adapter 方法（deleteShapeById / restoreShapeProperty）——本 plan 不新增 case，复用既有路由 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-1 | Tampering | PostStateSnapshot.kind union 新增 3 成员 | accept | kind 仅为 union 字面量，readTargetState 走 default 返 undefined（保守一致），无执行路径变更；TypeScript 编译验证类型封闭 |
| T-29-01-2 | Elevation of Privilege | 复用既有 executeReverse case（不新增） | accept | 未引入新反操作路由；既有 `if (!adapter.xxx) throw` 守卫不变，adapter 未实现 → skipped_error，不越权 |
| T-29-01-3 | Information Disclosure | mockPpt 扩展（测试环境 fill/lineFormat/addTable/addLine mock） | accept | 仅测试环境，无 PII，无生产代码路径影响 |
| T-29-01-4 | Tampering | contract.test.ts 长度断言上调 34 | accept | 收紧守门（更严），降低未来漏行风险；纯 CI 断言，无运行时面 |
</threat_model>

<verification>
Wave 0 验收命令：
```bash
export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"
# 1. TypeScript 编译
npx tsc --noEmit
# 2. Wave 0 验收 gate：两测试文件全绿（含 3 个 rolled_back 守门 + D-17）
npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts
# 3. 确认 operationLog.ts 仅加 3 个 kind（接口/case 零新增）
grep -c "ppt_table\|ppt_line\|ppt_shape_gradient" src/agent/operationLog.ts   # 期望 3
# 4. 确认 3 个 toolName 字面量在 integration.test.ts
grep -c "insert_ppt_table" src/agent/operationLog.integration.test.ts        # 期望 >= 1
grep -c "set_shape_gradient" src/agent/operationLog.integration.test.ts      # 期望 >= 1
```
</verification>

<success_criteria>
- operationLog.ts 含 3 个新 PostStateSnapshot kind（ppt_table / ppt_line / ppt_shape_gradient）；**0 个新接口方法、0 个新 executeReverse case、0 个 readTargetState case**（全复用），TypeScript 编译无错误
- contract.test.ts 含 3 条 Phase 29 行（integrationTest: true）+ PhaseNum 含 29 + 长度断言 ≥35，D-17 守门通过
- integration.test.ts 含 3 个 toolName 字面量（insert_ppt_table / add_line / set_shape_gradient）+ 3 个守门用例（全 rolled_back）+ 扩展 mockPpt（addTable/addLine + fill/lineFormat/width/height）
- **Wave 0 验收 gate = `npm test -- --run src/agent/contract.test.ts src/agent/operationLog.integration.test.ts` 全绿（无真失败）**——本 plan 自洽，无需等后续 wave 变绿
</success_criteria>

<output>
完成后创建 `.planning/phases/29-ppt-tools-nfr12/29-01-SUMMARY.md`，记录：
- operationLog.ts 改动（行号）：kind union +3（确认接口/case/readTargetState 零改动）
- contract.test.ts 新增 3 行（工具名、phase 29、integrationTest: true）+ PhaseNum +29 + 长度断言 24→34
- integration.test.ts mockPpt 扩展内容（addTable/addLine vi.fn + makeShape 补 fill/lineFormat/width/height）+ 3 个 rolled_back 守门用例摘要
- 当前测试状态（3 个新用例全 rolled_back——本 phase 复用既有反操作，Wave 0 即全绿）
- 给 Wave 1（Plan 02 insert_ppt_table + add_line）+ Wave 2（Plan 03 set_shape_gradient + NFR-12）的接线就绪说明：反操作侧零接线，只需补 ToolDef + adapter 正向方法
</output>

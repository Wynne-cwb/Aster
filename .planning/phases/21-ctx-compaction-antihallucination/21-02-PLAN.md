---
phase: 21-ctx-compaction-antihallucination
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/system-prompt.ts
  - src/agent/system-prompt.test.ts
autonomous: true
requirements:
  - CTX-06

must_haves:
  truths:
    - "PPT/Excel/Word 三宿主 system prompt 领域段各新增一条独立抗幻觉指引，统一含锚点句「旧读数早已过时」"
    - "CTX-06 指引写成自成一条、与坐标/自查规则解耦的独立项，使 Phase 23（PVQ-05）能删 PPT 冗余坐标/自查规则而保留此指引"
    - "system-prompt.test.ts 加三宿主守门断言：buildSystemPrompt(host) 含「旧读数早已过时」；现有全套测试 green；bundle 纯字符串改动 0 增量"
  artifacts:
    - path: "src/agent/system-prompt.ts"
      provides: "getDomainSegment 三宿主 case 各加一条 CTX-06 抗幻觉独立项"
      exports: ["buildSystemPrompt", "buildTimeContext"]
      contains: "旧读数早已过时"
    - path: "src/agent/system-prompt.test.ts"
      provides: "CTX-06 三宿主守门断言（it.each 含锚点句）"
      contains: "旧读数早已过时"
  key_links:
    - from: "src/agent/system-prompt.test.ts"
      to: "src/agent/system-prompt.ts"
      via: "import { buildSystemPrompt } — 三宿主断言含 CTX-06 锚点句"
      pattern: "旧读数早已过时"
---

<objective>
给 PPT/Excel/Word 三宿主的 system prompt 领域段各加一条抗幻觉指引：「永远信任你刚重读的文档现状，不要相信历史里几十轮前的旧读取记忆——文档会被用户/agent 改动，旧读数早已过时」。这是 prompt 层补强（loop.ts 已 filter 掉历史里的旧 tool 结果，本条让模型在认知上也不依赖陈旧读数）。

Purpose: 长对话里 agent 易把几十轮前的旧读取结果当现状（文档早被改）。本指引让模型「需要确认就重读、不凭记忆」。⚠️ 跨 phase：写成自成一条、与坐标/自查规则解耦的独立项——Phase 23（PVQ-05）将删 PPT 段冗余坐标/自查规则，CTX-06 必须能干净保留（STATE.md 2026-06-03 跨 phase 提醒）。
Output: getDomainSegment 三宿主各加一条独立项（统一锚点句「旧读数早已过时」）+ system-prompt.test.ts 三宿主守门断言。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-ctx-compaction-antihallucination/21-CONTEXT.md

<interfaces>
<!-- 执行前已读关键结构，供参考；以源码现状为准 -->

system-prompt.ts getDomainSegment（第 62-95 行）三宿主 case 结构：
- ppt（第 64-74 行）：现有编号项 1-9；其中 #6「版式意识」(坐标)、#8「宪法式自查」(list_shapes_on_slide) 是 Phase 23 将删的冗余项。CTX-06 须**另起一条独立项**（不要并进 #8）。
- excel（第 76-83 行）：现有编号项 1-6。
- word（第 85-93 行）：现有编号项 1-7。

每个 case 返回一个反引号模板字符串，形如：
```typescript
case 'ppt':
  return `【PowerPoint 领域指导】
1. ...
9. ...`;
```

CTX-06 锚点句（三宿主统一）：`旧读数早已过时`。

system-prompt.test.ts 现有 Phase 20 守门范式（参照）：`it.each(['word','excel','ppt'] as const)('host=%s ...', (host)=>{ const prompt = buildSystemPrompt(host); expect(prompt).not.toMatch(...) })`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getDomainSegment 三宿主各加一条 CTX-06 抗幻觉独立项</name>
  <files>src/agent/system-prompt.ts</files>
  <read_first>
    - src/agent/system-prompt.ts（全文，重点 getDomainSegment 第 62-95 行三宿主 case）
    - .planning/phases/21-ctx-compaction-antihallucination/21-CONTEXT.md D-21-09（独立项 + Phase-23-friendly 约束）
  </read_first>
  <action>
在 `getDomainSegment` 三宿主 case 的列表**末尾各追加一条新编号项**（统一含锚点句 `旧读数早已过时`，写成与坐标/自查解耦的独立条目）：

**① ppt case**：现有最后一项是 `9. 【诚实能力边界】…手动配图"。`。在其后追加 `10.`：
```
10. 【文档现状权威】永远以你刚用 read 工具读到的文档现状为准；不要相信历史里几十轮前的旧读取记忆——幻灯片会被用户或你自己改动，旧读数早已过时。需要确认时重新读，不要凭记忆下手。
```

**② excel case**：现有最后一项是 `6. 分析完成后用 set_cell…口头说结果。`。在其后追加 `7.`：
```
7. 【文档现状权威】永远以你刚用 read 工具读到的表格现状为准；不要相信历史里几十轮前的旧读取记忆——单元格会被用户或你自己改动，旧读数早已过时。需要确认时重新读，不要凭记忆下手。
```

**③ word case**：现有最后一项是 `7. 【宪法式自查】批量替换段落完成后…没自查不许说做完了。`。在其后追加 `8.`：
```
8. 【文档现状权威】永远以你刚用 read 工具读到的文档现状为准；不要相信历史里几十轮前的旧读取记忆——段落会被用户或你自己改动，旧读数早已过时。需要确认时重新读，不要凭记忆下手。
```

**约束：**
- 三条统一含锚点句 `旧读数早已过时`（守门断言据此）。
- 各自**独立成条**，不并进现有自查/坐标项——确保 Phase 23（PVQ-05）删 PPT #6/#8 冗余坐标/自查项时，本条（PPT #10）能干净保留。
- 不动 getSharedBase、buildTimeContext、buildPrefBlock、buildSystemPrompt、HOST_LABEL；不动现有编号项内容（只追加）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c '旧读数早已过时' src/agent/system-prompt.ts; grep -c '文档现状权威' src/agent/system-prompt.ts; npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '旧读数早已过时' src/agent/system-prompt.ts` 返回 3（三宿主各一条 CTX-06 锚点句）
    - `grep -c '文档现状权威' src/agent/system-prompt.ts` 返回 3（三宿主各一条独立项标签）
    - `grep -A2 "case 'ppt'" src/agent/system-prompt.ts` 之后 PPT 段仍含原 #8「宪法式自查」与 #6「版式意识」（只追加未删；Phase 23 才删）——可人工核对 grep -c '宪法式自查' 返回 >=1
    - npx tsc --noEmit 退出 0（纯字符串改动，类型无影响）
  </acceptance_criteria>
  <done>三宿主 getDomainSegment 各追加一条独立 CTX-06 抗幻觉项（统一锚点「旧读数早已过时」），未动现有项；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 2: system-prompt.test.ts 加 CTX-06 三宿主守门断言</name>
  <files>src/agent/system-prompt.test.ts</files>
  <read_first>
    - src/agent/system-prompt.test.ts（全文，重点 Phase 20 CTX-02 守门 describe 的 it.each 范式）
    - src/agent/system-prompt.ts（Task 1 后含锚点句）
  </read_first>
  <action>
在 `system-prompt.test.ts` 末尾新增一个 describe 块（沿用现有 `it.each` 范式）：
```typescript
// ---------------------------------------------------------------------------
// Phase 21 — CTX-06 抗幻觉指引守门：三宿主 system prompt 含「旧读数早已过时」
// （Phase 23 删 PPT 冗余坐标/自查规则时，此守门确保 CTX-06 独立项被保留）
// ---------------------------------------------------------------------------

describe('buildSystemPrompt — CTX-06 抗幻觉指引守门', () => {
  it.each(['word', 'excel', 'ppt'] as const)(
    'host=%s 含「旧读数早已过时」抗幻觉锚点句',
    (host) => {
      expect(buildSystemPrompt(host)).toContain('旧读数早已过时');
    },
  );

  it.each(['word', 'excel', 'ppt'] as const)(
    'host=%s 含「文档现状权威」独立项标签',
    (host) => {
      expect(buildSystemPrompt(host)).toContain('文档现状权威');
    },
  );
});
```
（`buildSystemPrompt` 已在文件顶部 import；若未含 ppt/excel/word 之外别名，直接复用现有 import。）
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && grep -c '旧读数早已过时' src/agent/system-prompt.test.ts; grep -c 'CTX-06' src/agent/system-prompt.test.ts; npx vitest run src/agent/system-prompt.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '旧读数早已过时' src/agent/system-prompt.test.ts` >= 1（守门断言含锚点句）
    - `grep -c 'CTX-06' src/agent/system-prompt.test.ts` >= 1（守门 describe 存在）
    - `grep -c 'it.each' src/agent/system-prompt.test.ts` >= 1（三宿主一行覆盖，不按行数验收）
    - `npx vitest run src/agent/system-prompt.test.ts` 退出 0，无真实「N failed」
  </acceptance_criteria>
  <done>system-prompt.test.ts 加 CTX-06 三宿主守门（it.each 含「旧读数早已过时」+「文档现状权威」），全绿。</done>
</task>

<task type="auto">
  <name>Task 3: 最终验证 — tsc + test + build + size</name>
  <files></files>
  <read_first>
    - 无（仅运行验证命令）
    - memory project_bundle_size_guard：先 build 再 size（陈旧 dist 假绿）
    - memory i18n_extract_and_test_noise：「N failed」才是真失败；本 phase 不动 Lingui 宏，无需 extract
  </read_first>
  <action>
按顺序运行并确认：
1. `npx tsc --noEmit`（退出 0）
2. `npm test -- --run`（全套 green，含 CTX-06 守门）
3. `npm run build && npm run size`（先 build 再 size；main gzip <= 82KB，预期与 ~80.53KB 持平——纯字符串改动）
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -5 && npm test -- --run 2>&1 | tail -10 && npm run build 2>&1 | tail -3 && npm run size 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 退出 0
    - `npm test -- --run` 退出 0，无「N failed」
    - `npm run size` main bundle gzip <= 82KB（预期与 ~80.53KB 持平）
  </acceptance_criteria>
  <done>tsc 0 + 全套 green + bundle <=82KB；CTX-06 交付完毕。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getDomainSegment 字符串 -> system prompt 静态前缀 | 纯写死的指引字符串，非用户输入、非网络数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | getDomainSegment 抗幻觉指引字符串 | accept | 纯静态写死字符串（开发期固定），无运行时数据流入；不引入新注入向量，不改变信任级别。ASVS L1 trivially 满足。 |
| T-21-05 | Information Disclosure | system prompt 内容 | accept | 仅追加指引文本，不含任何敏感信息（无 Key、无 URL）；不改变 system prompt 既有信息边界。 |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit
npm test -- --run
npx vitest run src/agent/system-prompt.test.ts
npm run build && npm run size
```
</verification>

<success_criteria>
1. `buildSystemPrompt('word')`、`buildSystemPrompt('excel')`、`buildSystemPrompt('ppt')` 三宿主返回值均含锚点句「旧读数早已过时」与独立项标签「文档现状权威」。
2. CTX-06 指引写成自成一条、与坐标/自查规则解耦的独立项（PPT 段为 #10，未动 #6/#8），使 Phase 23（PVQ-05）能删 PPT 冗余坐标/自查规则而保留此指引。
3. `system-prompt.test.ts` 含 CTX-06 三宿主守门断言（`it.each` 含「旧读数早已过时」），CI 通过。
4. 现有全套测试 green；tsc 0；bundle main gzip <= 82KB（纯字符串改动，0 净新增依赖，预期持平）。
</success_criteria>

<output>
完成后创建 `.planning/phases/21-ctx-compaction-antihallucination/21-02-SUMMARY.md`
</output>
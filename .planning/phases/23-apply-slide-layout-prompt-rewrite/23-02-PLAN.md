---
phase: 23-apply-slide-layout-prompt-rewrite
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/agent/system-prompt.ts
  - src/agent/system-prompt.test.ts
autonomous: true
requirements:
  - PVQ-05

must_haves:
  truths:
    - "删冗余机制规则：现 PPT 段 #6（版式意识/拿 list_shapes 坐标推算重叠落点）+ #8（宪法式自查清单/每次 batch 后 list_shapes 查重叠溢出）——已被 apply_slide_layout（PVQ-03）+ check_slide_layout/几何自查（PVQ-02）机制保证（满足 Phase 22 SC#3 + PVQ-02 SC#3）"
    - "⚠️ 保留 #10（CTX-06 抗幻觉「文档现状权威/旧读数早已过时」）——绝不删，仅 renumber（Phase 21 已加，与坐标/自查解耦）"
    - "⚠️ 修 stale #9（「图片/背景功能 v2.1 暂不可用…图片功能即将开放」）——已过时（v2.2 交付 generate_ppt_image + Pexels 图库），改为图片现已可用 + 图文左右图片位走 autonomous-insert（同轮直插不留空）"
    - "加判断级指引（只留模型能判断的 + 硬底线）：用 apply_slide_layout 一次建整页/选合适版式；配色按客户意图自主选 hex（商务密实/克制/保对比，涨跌用语义绿红）；故事线（金字塔）/标题写断言式洞察句；硬底线（可编辑优先/收到自查反馈就改/诚实边界）"
    - "⚠️ 删的是冗余机制规则、不是精确描述（标题质量定义/每页容量/左对齐正文等判断标准保留并保持精确无歧义，memory precision_over_brevity）；不动 Lingui 宏（普通模板字符串，无需 npm run extract）；现有测全 green、tsc 0、bundle 无变化（纯字符串）"
  artifacts:
    - path: "src/agent/system-prompt.ts"
      provides: "getDomainSegment('ppt') 重写：删 #6/#8、保 #10、修 stale #9、加判断级指引 + 配色指引 + 硬底线"
      contains: "apply_slide_layout"
    - path: "src/agent/system-prompt.test.ts"
      provides: "PVQ-05 守门：PPT 段不含坐标脑补/宪法自查表述、保抗幻觉锚点、无 stale 图片文案、提及 apply_slide_layout"
      contains: "apply_slide_layout"
  key_links:
    - from: "src/agent/system-prompt.ts"
      to: "src/agent/tools/write/ppt.ts"
      via: "prompt 引导 AI 用 apply_slide_layout 建整页（机制由 23-01 交付，prompt 描述对齐已落地工具语义）"
      pattern: "apply_slide_layout"
---

<objective>
PVQ-05 PPT 领域段 system prompt 重写。PVQ-01/02/03/04 机制就位后，把「教模型怎么机械摆版（拿坐标推算重叠 / 宪法式自查清单）」的冗余规则**下沉到机制并从 prompt 移除**——这些已被 `apply_slide_layout`（盖印章一次建整页）+ `check_slide_layout`/几何自查（确定性版面自查）保证，prompt 再写就是冗余（同时满足 Phase 22 SC#3「prompt 不再脑补坐标」、PVQ-02 SC#3）。prompt 重新聚焦「只有模型能判断的」（故事线 / 选哪个版式 / 填什么内容 / 标题怎么写出洞察 / 配色按客户意图）+ 硬底线（可编辑优先 / 收到自查反馈就改 / 诚实边界）。

⚠️ 两个不可错的点：① **保留** Phase 21 CTX-06 的抗幻觉项（「文档现状权威/旧读数早已过时」）——绝不删；② **修复** stale 的「图片功能即将开放」诚实边界——v2.2 已交付生图 + 图库插入，旧文案会让模型拒绝本可做的配图。

Purpose: 让模型聚焦判断而非机械摆版；机制保证排版正确，prompt 只管「想清楚再用工具」。
Output: 改 `getDomainSegment('ppt')`（system-prompt.ts）+ `system-prompt.test.ts` 守门。
⚠️ 删的是冗余机制规则、不是精确描述（边界/禁则/判断标准务必精确无歧义，不怕长，memory precision_over_brevity）。真机 A/B「模型照没照做」攒 v2.3 末 UAT。
</objective>

<execution_context>
@/Users/wb.chen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wb.chen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-apply-slide-layout-prompt-rewrite/23-CONTEXT.md

<interfaces>
<!-- 现 getDomainSegment('ppt') 全文（system-prompt.ts L65-75），逐条标处置：以源码现状为准 -->

```
【PowerPoint 领域指导】
1. 先用 list_slides 了解现有结构...再规划全部页面标题和内容骨架——不要盲目插入。     [保留——规划判断]
2. 创建多张 slide 时，一次 batch emit 多个 insert_slide tool_call...（平行推进）。     [保留/微调——可改提「用 apply_slide_layout 建整页」并仍鼓励 batch]
3. 【标题质量】每页标题必须是断言式完整结论句（如"华东 Q3 超目标 15%..."）...≤15 字，含具体数字或结论，主动语态。  [保留——精确判断标准，不删]
4. 每页 ≤5 个要点，每要点 ≤15 字；超出则拆页。正文左对齐，禁止居中正文。            [保留——精确容量/禁则，不删]
5. 【故事线】默认金字塔原则：一个核心结论 → 3-5 条支撑理由 → 证据...逻辑链。          [保留——故事线判断]
6. 【版式意识】新元素不与现有形状重叠：用 list_shapes_on_slide 返回的 {left,top,width,height} 推算空间位置再落点...对齐。  [⚠️删——机制已保证（apply_slide_layout 固化坐标 + 几何自查）]
7. 修改形状前先用 get_shape 确认 id...set_shape_text...selection_detail...selectedShapeId...   [保留——精确工具用法判断，不删]
8. 【宪法式自查】每次 batch 完成后用 list_shapes_on_slide 检查重叠/溢出/错位——没自查不许说做完了。  [⚠️删——机制已保证（check_slide_layout/几何自查）；改为「收到自查反馈就改」硬底线]
9. 【诚实能力边界】图片/背景功能 v2.1 暂不可用；若用户要求配图，诚实告知"图片功能即将开放..."  [⚠️修 stale——v2.2 已交付生图+图库；改为图片现已可用 + autonomous-insert]
10. 【文档现状权威】永远以你刚用 read 工具读到的文档现状为准...旧读数早已过时...重新读，不要凭记忆下手。  [⚠️保留——CTX-06 抗幻觉，仅 renumber]
```

23-01 已交付（本 plan 描述需对齐）：`apply_slide_layout`（六版式 cover/kpi/two_column/timeline/image_text/bullet_list；返 image_slots + layout_check）；`generate_ppt_image` / `search_and_insert_stock_image`（autonomous-insert 图片位）；`check_slide_layout`（Phase 22 版面自查 read 工具）。

getDomainSegment 返回**普通模板字符串**（非 `t\`\`` Lingui 宏，L65-96 三宿主都是）→ 改它**无需** npm run extract（memory i18n_extract_and_test_noise）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 getDomainSegment('ppt')（删 #6/#8、保 #10、修 stale #9、加判断指引，PVQ-05）</name>
  <files>src/agent/system-prompt.ts</files>
  <read_first>
    - src/agent/system-prompt.ts 全文（重点 getDomainSegment('ppt') L65-75；对照 excel/word 段 L77-96 的精度风格）
    - .planning/phases/23-apply-slide-layout-prompt-rewrite/23-CONTEXT.md D-23-08
    - .planning/STATE.md line 169（跨 phase 提醒：CTX-06 已先改此段，按最新内容删冗余保抗幻觉）、line 179（删 #6/#8 保 #10）
  </read_first>
  <action>
重写 `getDomainSegment` 的 `case 'ppt'` 返回串。处置矩阵（见 <interfaces>）：

**删除**：
- #6（版式意识/拿 list_shapes 坐标推算重叠落点对齐）——机制已保证（apply_slide_layout 固化坐标 + 几何自查）。
- #8（宪法式自查清单/每次 batch 后 list_shapes 查重叠溢出「没自查不许说做完了」）——机制已保证（check_slide_layout/几何自查）。其「自查」精神转写为硬底线「收到自查反馈就改」。

**保留**（精确判断标准，不删，措辞可微调但语义不弱化）：
- #1 先 list_slides 规划骨架；#3 标题质量（断言式结论句 ≤15 字含数字/主动语态）；#4 每页 ≤5 要点/≤15 字/正文左对齐禁居中；#5 故事线金字塔；#7 改形状先 get_shape/selection_detail→selectedShapeId 精确定位。
- ⚠️ #10 抗幻觉（文档现状权威/旧读数早已过时）——**原样保留**，仅随重编号调整序号。

**修复 stale #9**：删「图片/背景功能 v2.1 暂不可用 / 图片功能即将开放 / 已为您预留占位文字建议手动配图」。改为诚实的**新现状**：图片现已可用——可用 `generate_ppt_image` 生成或 `search_and_insert_stock_image` 检索图库并自动插入；**图文左右版式留有图片位**（apply_slide_layout 返回 image_slots 坐标），随后用上述图片工具把图直接插入该坐标，**不要留空文字让用户手动配图**（autonomous-insert）。

**新增判断级指引 + 配色指引 + 硬底线**（融入重写后的段，只留「只有模型能判断的」）：
- 【盖印章建页】优先用 `apply_slide_layout` 一个调用建好整页（按内容选版式：封面/大数字KPI/两栏对比/时间线/图文左右/要点列表），而不是逐个 add_shape 摆形状——工具按固化版式坐标保证版面整齐，你专注选版式 + 填内容。
- 【配色由你定】没有固定调色板：按客户/内容意图为这套 deck 选强调色（传 apply_slide_layout 的 accent_color hex）；商务密实、克制、保证文字与背景对比清晰；涨跌/正负用语义绿红，不挤占强调色。不传则用默认 teal。
- 【硬底线】① 可编辑优先——产出原生形状（apply_slide_layout）而非整图截图，用户能继续编辑；② 收到自查反馈就改——apply_slide_layout 结果里的版面自查（layout_check）或 check_slide_layout 指出溢出/重叠/低对比时，调整文本长度或配色后再交付；③ 诚实边界——做不到/宿主不支持的诚实说明，不假装、不承诺做不到的事。

⚠️ **精度纪律**（memory precision_over_brevity）：保留项的边界/禁则/判断标准（标题定义、每页容量、左对齐等）保持精确无歧义，**不为简短牺牲信息**；只删「机制已保证的机械规则」。重编号后条目连续。

⚠️ **FIX 3（plan-review MED）—— 既有绿测对重写的硬约束**（删 #6/#8 时若不留意会打破两个**未在本 plan 显式列出**的 pre-existing 绿测）：
- **`system-prompt.test.ts` L59-65** 断言 ppt prompt 必含 `'list_shapes_on_slide'` AND `'set_shape_text'`（现仅靠 KEPT 的 #7 同时含这两词存活）→ 重写**必须保 #7 完整**（其 `list_shapes_on_slide` + `set_shape_text` 提及不丢）。
- **`system-prompt.test.ts` L115-117** 断言 `/自查|没自查.*不许|verify.*after/`（现靠含 `'自查'` 子串存活）→ 重写后的新 ppt 段**必须保留子串 `'自查'`**（自然落在硬底线「收到**版面自查**反馈就改」/「**版面自查**」措辞里——把 #8 的「自查」精神转写为硬底线即可满足）。
- **只删 ppt #6 + ppt #8（均在 case 'ppt'）；绝不触碰 word 段 #7「宪法式自查」（L95，OUT OF SCOPE）**——precision_over_brevity + 越界回归双重风险。

**不动**：`getSharedBase` / excel / word 段（含 word #7「宪法式自查」L95）/ `buildTimeContext` / `buildSystemPrompt` 签名。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && node -e "const s=require('./src/agent/system-prompt.ts');" 2>/dev/null; grep -c 'apply_slide_layout' src/agent/system-prompt.ts; grep -c '旧读数早已过时' src/agent/system-prompt.ts; grep -c '即将开放' src/agent/system-prompt.ts; grep -c '推算空间位置' src/agent/system-prompt.ts; npx tsc --noEmit 2>&1 | head -10</automated>
    <!-- FIX 1（plan-review HIGH）：file-wide grep 只查 '推算空间位置'（checker 确认 ppt 段独有，删 #6 后 file-wide=0 有效）。**不**在此 file-wide grep '宪法式自查'——它在 word 段 #7（L95，OUT OF SCOPE 不可动）也出现，删 ppt #8 后 file-wide count=1 非 0，会假失败或误导 executor 误删 word #7。#8 的删除改由 Task 2 的 SCOPED 断言 expect(buildSystemPrompt('ppt')).not.toContain('宪法式自查') 证明（buildSystemPrompt('ppt') 只 emit ppt 段、不含 word 段）。-->
  </verify>
  <acceptance_criteria>
    - `grep -c 'apply_slide_layout' src/agent/system-prompt.ts` ≥ 1（PPT 段提及盖印章工具）
    - `grep -c '旧读数早已过时' src/agent/system-prompt.ts` ≥ 1（CTX-06 抗幻觉锚点保留——⚠️ 绝不可为 0）
    - `grep -c '即将开放' src/agent/system-prompt.ts` = 0（stale 图片文案已删）且 `grep -c 'v2.1 暂不可用' src/agent/system-prompt.ts` = 0
    - `grep -c '推算空间位置' src/agent/system-prompt.ts` = 0（#6 冗余机制规则已删；'推算空间位置' 仅 ppt 段独有，file-wide grep 有效）
    - #8「宪法式自查」删除的验收**不靠 file-wide grep**（'宪法式自查' 在 word 段 #7 L95 也出现且 OUT OF SCOPE 不可动 → 删 ppt #8 后 file-wide count = 1 非 0）；改由 Task 2 SCOPED 断言 `expect(buildSystemPrompt('ppt')).not.toContain('宪法式自查')` 证明（该断言为 0 即证 ppt #8 已删、word #7 未被波及）
    - PPT 段仍含精确判断标准（人工核对：标题断言式 ≤15 字 / 每页 ≤5 要点 / 左对齐禁居中 / 故事线金字塔 / get_shape+selection_detail 定位）——保留未弱化
    - `grep -c 'generate_ppt_image\|search_and_insert_stock_image' src/agent/system-prompt.ts` ≥ 1（图片现可用 + autonomous-insert）
    - `npx tsc --noEmit` 退出 0
  </acceptance_criteria>
  <done>getDomainSegment('ppt') 已重写：删 #6/#8（机制保证的冗余坐标/自查规则）、保 #10（抗幻觉，renumber）、修 stale #9（图片现可用 + autonomous-insert）、加判断级指引（apply_slide_layout 建页/选版式/配色按意图/故事线/洞察标题）+ 硬底线（可编辑优先/收自查反馈就改/诚实边界）；精确判断标准保留未弱化；tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 2: system-prompt.test.ts 加 PVQ-05 守门</name>
  <files>src/agent/system-prompt.test.ts</files>
  <read_first>
    - src/agent/system-prompt.test.ts（现有断言风格；Phase 20 CTX-01 的 /\d{1,2}:\d{2}/ 守门 + Phase 21 CTX-06 抗幻觉断言范式）
    - src/agent/system-prompt.ts（Task 1 重写后）
  </read_first>
  <action>
在 `system-prompt.test.ts` 加 PPT 段守门断言（用 `buildSystemPrompt('ppt')` 取串）：
```ts
describe('PVQ-05 PPT 段重写守门', () => {
  const ppt = buildSystemPrompt('ppt');
  it('提及盖印章工具 apply_slide_layout（建整页机制）', () => {
    expect(ppt).toContain('apply_slide_layout');
  });
  it('保留 CTX-06 抗幻觉锚点（绝不可删）', () => {
    expect(ppt).toContain('旧读数早已过时');
  });
  it('删除机制已保证的冗余规则（拿坐标推算重叠 / 宪法式自查清单）', () => {
    expect(ppt).not.toContain('推算空间位置');
    expect(ppt).not.toContain('宪法式自查');
  });
  it('修复 stale 图片边界（图片功能现已可用，无"即将开放/v2.1 暂不可用"）', () => {
    expect(ppt).not.toContain('即将开放');
    expect(ppt).not.toContain('v2.1 暂不可用');
    expect(ppt).toMatch(/generate_ppt_image|search_and_insert_stock_image/);
  });
  it('保留精确判断标准（标题质量 + 正文左对齐禁居中未弱化）', () => {
    expect(ppt).toContain('断言式');      // 标题质量定义保留
    expect(ppt).toMatch(/左对齐/);         // 正文左对齐禁则保留
  });
});
```
> 断言锚点字符串须与 Task 1 重写实际用词一致（executor 落地后对齐）；若措辞不同，调整断言文本但守门语义不变（提及工具/保抗幻觉/删两冗余/修 stale 图片/保精确标准）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx vitest run src/agent/system-prompt.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/agent/system-prompt.test.ts` 全 green（5 条 PVQ-05 守门 + 现有 CTX-01/CTX-06 断言）
    - `grep -c "apply_slide_layout" src/agent/system-prompt.test.ts` ≥ 1 且 `grep -c "旧读数早已过时" src/agent/system-prompt.test.ts` ≥ 1（守门含工具提及 + 抗幻觉保留）
    - `grep -c "不含\|not.toContain" src/agent/system-prompt.test.ts` ≥ 1（删冗余 + 修 stale 的负向守门存在）
  </acceptance_criteria>
  <done>system-prompt.test.ts 已加 PVQ-05 守门：apply_slide_layout 提及 + 抗幻觉锚点保留 + 删两冗余（推算坐标/宪法自查）负向断言 + 修 stale 图片（无"即将开放"+含生图/图库工具）+ 保精确标准（断言式标题/左对齐）；全 green。</done>
</task>

<task type="auto">
  <name>Task 3: 最终验证 — tsc + 全套 test + build + size（纯字符串改动，bundle 无变化）</name>
  <files></files>
  <read_first>
    - memory i18n_extract_and_test_noise：getDomainSegment 是普通模板字符串非 Lingui 宏 → 无需 npm run extract；「N failed」才是真失败
    - memory project_bundle_size_guard：先 build 再 size
  </read_first>
  <action>
按顺序：
1. `npx tsc --noEmit`
2. `npm test -- --run`
3. `npm run build && npm run size`

确认：tsc 0；全套 green（含 23-01 新增 + 本 plan PVQ-05 守门）；bundle main gzip ≤82KB 且与 23-01 后持平（纯字符串改 system prompt，0 新依赖、不进/不影响 bundle 体积量级）。
  </action>
  <verify>
    <automated>cd /Users/wb.chen/Documents/Project/Aster && npx tsc --noEmit 2>&1 | head -5 && npm test -- --run 2>&1 | tail -12 && npm run build 2>&1 | tail -5 && npm run size 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 退出 0
    - `npm test -- --run` 退出 0，无「N failed」（尾部 3 retry errors 是噪音）；全套 green
    - `npm run size` main bundle gzip ≤82KB（与 23-01 后持平；纯字符串改动）
  </acceptance_criteria>
  <done>tsc 0 + 全套 green + bundle ≤82KB；PVQ-05 PPT 段 prompt 重写交付完毕（删冗余机制规则、保抗幻觉与精确描述、修 stale 图片边界、加判断级指引）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| system prompt PPT 段（静态字符串）→ wire system 前缀 | 纯静态字符串，无用户/网络输入；缓存前缀的一部分（与 Phase 20 时间脱前缀一致，prompt 仍静态可缓存） |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Integrity | 误删 CTX-06 抗幻觉项（回归 Phase 21） | mitigate | system-prompt.test 守门「旧读数早已过时」必须存在；处置矩阵显式标 #10 保留仅 renumber。 |
| T-23-08 | Integrity | stale 图片文案残留致模型拒绝本可做的配图 | mitigate | 守门负向断言「即将开放」/「v2.1 暂不可用」= 0 + 正向断言含生图/图库工具名。 |
| T-23-09 | Quality | 删冗余时误删精确判断标准（precision_over_brevity 违背） | mitigate | 守门断言保留「断言式」标题质量 + 「左对齐」禁则；处置矩阵逐条标保留/删/修。 |
| T-23-10 | Caching | prompt 改动破坏缓存前缀稳定性 | accept | system 前缀仍静态（仅内容变，结构/位置不变），与 Phase 20/21 缓存铁律一致；一次性改动后稳定。 |
</threat_model>

<verification>
```bash
cd /Users/wb.chen/Documents/Project/Aster
npx tsc --noEmit
npx vitest run src/agent/system-prompt.test.ts
npm test -- --run            # 963 + 新增；「N failed」才是真失败，尾部 3 retry errors 是噪音
npm run build && npm run size # 先 build 再 size；main gzip ≤82KB（纯字符串改动持平）
# 不动 Lingui 宏 → 无需 npm run extract
```
</verification>

<success_criteria>
1. `getDomainSegment('ppt')` 已移除「教模型如何摆坐标/排字号/自查清单」等机制已保证的冗余规则（删 #6 拿坐标推算重叠 + #8 宪法式自查清单），满足 Phase 22 SC#3 / PVQ-02 SC#3「prompt 不再脑补坐标」。
2. prompt 聚焦故事线 / 选版式（用 apply_slide_layout）/ 填内容 / 标题写出洞察 / 配色按客户意图自主选，以及硬底线（可编辑优先 / 收到自查反馈就改 / 诚实边界）；精确描述（标题质量定义、每页容量、左对齐禁则、工具定位用法）保留不删（precision_over_brevity）。
3. ⚠️ Phase 21 CTX-06 抗幻觉项（「文档现状权威/旧读数早已过时」）原样保留（仅 renumber）；stale 图片边界（「图片功能即将开放/v2.1 暂不可用」）已修为「图片现已可用 + 图文左右图片位 autonomous-insert」。
4. `system-prompt.test.ts` 守门覆盖：提及 apply_slide_layout / 保抗幻觉锚点 / 删两冗余负向断言 / 修 stale 图片 / 保精确标准；现有测全 green、tsc 0、bundle 无变化（纯字符串、0 依赖、不动 Lingui 宏无需 extract）。
</success_criteria>

<output>
完成后创建 `.planning/phases/23-apply-slide-layout-prompt-rewrite/23-02-SUMMARY.md`
</output>
